{
  "engine_version": "2026-09-05.12",
  "repo": "alxmax/Requirement-manager",
  "nodes": [
    {
      "id": "ARCH-ACVERIFY-019",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Per-criterion test coverage",
      "intent": "Today the gate can tell you a requirement has *some* test, but not that *each* of its acceptance criteria is actually checked — so a requirement with five criteria and one test looks just as \"covered\" as one with five tests. This links each labelled criterion to the test that verifies it, so the gate can name the exact criterion that nobody tests. Without it, \"Verifiable\" is only true at the whole-requirement level and a half-tested requirement passes silently.",
      "contract": [
        "The gate scans code for `# verifies: <id>#AC-N` tags and maps each to the labelled criterion it covers — the per-criterion half of behaviour-sync. [[REQ-ACVERIFY-821]]",
        "A confirmed requirement with no `verifies` tag, with unlabelled criteria, or with an inspection-only criterion is exempt from the per-criterion warning; the coarser `tested-by` check still applies to it. [[REQ-ACVERIFY-822]]",
        "The map emits `clauses`/`covered`/`gap` only on a requirement that has adopted per-criterion tagging, and the check never changes the gate's exit code. [[REQ-ACVERIFY-823]]"
      ],
      "verify": [],
      "notes": [
        "The link asserts a test *exists* for a criterion, not that the test's assertions actually exercise it — same lexical-trust limitation as `tested-by` (ARCH-TESTLINK-018).",
        "A criterion can carry more than one `verifies` tag (several tests for one criterion); the check only asks for at least one."
      ],
      "current_impl": [
        "`scan_ac_verifies`, `_acc_blocks`, `_labeled_acs`, `_automatable_acs`, `AC_VERIFY_RE` in `reqmap.py`, consumed by `cmd_check` and by `_attach_ac_coverage` — `scan_ac_verifies` collects `# verifies: <id>#AC-N` tags into `{cap: {AC-N: locations}}`, `_acc_blocks` is the single parser of the Acceptance section (labels, folded prose, and the `verifiable by:` marker), `_automatable_acs` drops the criteria a machine can never verify, `cmd_check` emits one aggregated warning naming the untagged ones, and `_attach_ac_coverage` puts `clauses`/`covered`/`gap` on the map node only when the requirement has adopted tagging.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a requirement with labelled CASE-1 and CASE-2 and a test tagging `# verifies: REQ-X#CASE-1` When   the gate runs Then   it warns that CASE-2 is unverified and says nothing about CASE-1",
        "CASE-2 — Given  a requirement whose every labelled criterion has a matching `verifies` tag When   the gate runs Then   it adds no per-criterion warning",
        "CASE-3 — Given  a confirmed requirement with labelled criteria but zero `verifies` tags When   the gate runs Then   it adds no per-criterion warning (per-criterion tagging is opt-in)",
        "CASE-4 — Given  a confirmed requirement whose criteria are unlabelled bullets When   the gate runs Then   it adds no per-criterion warning",
        "CASE-5 — Given  a confirmed requirement with a criterion marked `verifiable by: inspection` When   the gate runs Then   that criterion appears in no warning and in no emitted count",
        "CASE-6 — Given  a requirement with two labelled criteria of which one carries a `verifies` tag When   the map is generated Then   its node carries `clauses: 2`, `covered: 1`, and a `gap` naming the untagged one"
      ],
      "accept": "CASE-1\n  Given  a requirement with labelled CASE-1 and CASE-2 and a test tagging `# verifies: REQ-X#CASE-1`\n  When   the gate runs\n  Then   it warns that CASE-2 is unverified and says nothing about CASE-1\nCASE-2\n  Given  a requirement whose every labelled criterion has a matching `verifies` tag\n  When   the gate runs\n  Then   it adds no per-criterion warning\nCASE-3\n  Given  a confirmed requirement with labelled criteria but zero `verifies` tags\n  When   the gate runs\n  Then   it adds no per-criterion warning (per-criterion tagging is opt-in)\nCASE-4\n  Given  a confirmed requirement whose criteria are unlabelled bullets\n  When   the gate runs\n  Then   it adds no per-criterion warning\nCASE-5\n  Given  a confirmed requirement with a criterion marked `verifiable by: inspection`\n  When   the gate runs\n  Then   that criterion appears in no warning and in no emitted count\nCASE-6\n  Given  a requirement with two labelled criteria of which one carries a `verifies` tag\n  When   the map is generated\n  Then   its node carries `clauses: 2`, `covered: 1`, and a `gap` naming the untagged one",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-CHECK-006",
        "ARCH-SCAN-002"
      ],
      "used_by": [
        "ARCH-SUGGESTVERIFIES-047"
      ],
      "satisfies": [
        "SYS-GATE-102"
      ],
      "satisfied_by": [
        "REQ-ACVERIFY-821",
        "REQ-ACVERIFY-822",
        "REQ-ACVERIFY-823"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1283"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1645"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1748"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1822"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1834"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2616"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3941"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3875"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3973"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4014"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6204"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.16",
      "priority": "should-have",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Acoperire de test per criteriu",
          "intent": "În prezent, gate-ul poate spune că o cerință are *un* test, dar nu că *fiecare* dintre criteriile sale de acceptare este verificat efectiv — astfel încât o cerință cu cinci criterii și un singur test pare la fel de \"acoperită\" ca una cu cinci teste. Aceasta leagă fiecare criteriu etichetat de testul care îl verifică, astfel încât gate-ul poate numi exact criteriul pe care nimeni nu îl testează. Fără aceasta, \"Verificabil\" este adevărat doar la nivelul întregii cerințe, iar o cerință testată doar pe jumătate trece în tăcere.",
          "contract": "> În prezent, gate-ul poate spune că o cerință are *un* test, dar nu că *fiecare*\n> dintre criteriile sale de acceptare este verificat efectiv — astfel încât o cerință\n> cu cinci criterii și un singur test pare la fel de \"acoperită\" ca una cu cinci teste.\n> Aceasta leagă fiecare criteriu etichetat de testul care îl verifică, astfel încât\n> gate-ul poate numi exact criteriul pe care nimeni nu îl testează. Fără aceasta,\n> \"Verificabil\" este adevărat doar la nivelul întregii cerințe, iar o cerință testată\n> doar pe jumătate trece în tăcere.\n\nFiecare punct de mai jos este obligatoriu.\n- Gate-ul scanează codul pentru etichete `# verifies: <id>#AC-N` și mapează fiecare la criteriul etichetat pe care îl acoperă — jumătatea per-criteriu a sincronizării comportamentale. [[REQ-ACVERIFY-821]]\n- O cerință confirmată fără nicio etichetă `verifies`, cu criterii neetichetate, sau cu un criteriu verificabil doar prin inspecție este exceptată de la avertismentul per-criteriu; verificarea mai grosieră `tested-by` i se aplică în continuare. [[REQ-ACVERIFY-822]]\n- Harta emite `clauses`/`covered`/`gap` doar pentru o cerință care a adoptat etichetarea per-criteriu, iar verificarea nu modifică niciodată codul de ieșire al gate-ului. [[REQ-ACVERIFY-823]]",
          "acceptance": "CASE-1\n  Given  o cerință cu CASE-1 și CASE-2 etichetate și un test care etichetează `# verifies: REQ-X#CASE-1`\n  When   gate-ul rulează\n  Then   avertizează că CASE-2 nu este verificat și nu spune nimic despre CASE-1\nCASE-2\n  Given  o cerință ale cărei criterii etichetate au toate câte o etichetă `verifies` corespunzătoare\n  When   gate-ul rulează\n  Then   nu adaugă niciun avertisment per-criteriu\nCASE-3\n  Given  o cerință confirmată cu criterii etichetate dar zero etichete `verifies`\n  When   gate-ul rulează\n  Then   nu adaugă niciun avertisment per-criteriu (etichetarea per-criteriu este opțională)\nCASE-4\n  Given  o cerință confirmată ale cărei criterii sunt puncte neetichetate\n  When   gate-ul rulează\n  Then   nu adaugă niciun avertisment per-criteriu\nCASE-5\n  Given  o cerință confirmată cu un criteriu marcat `verifiable by: inspection`\n  When   gate-ul rulează\n  Then   acel criteriu nu apare în niciun avertisment și în nicio numărătoare emisă\nCASE-6\n  Given  o cerință cu două criterii etichetate, dintre care unul poartă o etichetă `verifies`\n  When   harta este generată\n  Then   nodul său poartă `clauses: 2`, `covered: 1`, și un `gap` care numește criteriul neetichetat"
        }
      }
    },
    {
      "id": "REQ-ACVERIFY-821",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Mapping verifies tags to labelled criteria",
      "intent": "The gate can prove a requirement has *a* test, but not that each of its criteria is tested individually. `scan_ac_verifies` reads `# verifies: <id>#AC-N` tags so the gate can name exactly which labelled criterion still has no test, instead of treating the whole requirement as pass-or-fail.",
      "contract": [
        "The gate scans code for `# verifies: <id>#AC-N` tags and maps each tag to the labelled criterion it covers. This is the per-criterion half of behaviour-sync.",
        "The gate recognises a `verifies` tag only with an `#AC-N` suffix, so a plain requirement reference is never mistaken for a per-criterion one.",
        "For a confirmed requirement that labels its criteria and carries at least one `verifies` tag, the gate warns once, naming every labelled criterion that has no tag.",
        "That single warning also states how many labelled criteria are tagged, so partial adoption reads as progress rather than as a growing pile of warnings."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a verifies tag is mapped to its labelled criterion Given  a test file carrying `# verifies: AREA-A-001#CASE-1` and a requirement labelling CASE-1 and CASE-2 When   `gate` runs Then   it reports that 1 of the 2 automatable criteria carries a `verifies:` tag, naming CASE-2 as missing",
        "CASE-2 — — a verifies tag with no #AC-N suffix is not per-criterion coverage Given  a file carrying `# verifies: REQ-X-001` with no trailing `#AC-N` When   `scan_ac_verifies` scans it Then   it returns an empty mapping for `REQ-X-001`",
        "CASE-3 — — partial verifies coverage produces one warning naming every gap Given  a confirmed requirement with five labelled criteria, `AC-1` tagged and AC-2..AC-5 untagged When   `gate` runs Then   it prints exactly one \"automatable criteria\" line naming \"missing AC-2, AC-3, AC-4, AC-5\"",
        "CASE-4 — — the warning states the tagged-of-total count Given  a confirmed requirement with AC-1 and AC-2 labelled, only AC-1 tagged When   `gate` runs Then   its output contains \"1/2 automatable criteria\" --------------------"
      ],
      "accept": "CASE-1 — a verifies tag is mapped to its labelled criterion\n  Given  a test file carrying `# verifies: AREA-A-001#CASE-1` and a requirement labelling CASE-1 and CASE-2\n  When   `gate` runs\n  Then   it reports that 1 of the 2 automatable criteria carries a `verifies:` tag, naming CASE-2 as missing\n\nCASE-2 — a verifies tag with no #AC-N suffix is not per-criterion coverage\n  Given  a file carrying `# verifies: REQ-X-001` with no trailing `#AC-N`\n  When   `scan_ac_verifies` scans it\n  Then   it returns an empty mapping for `REQ-X-001`\n\nCASE-3 — partial verifies coverage produces one warning naming every gap\n  Given  a confirmed requirement with five labelled criteria, `AC-1` tagged and AC-2..AC-5\n         untagged\n  When   `gate` runs\n  Then   it prints exactly one \"automatable criteria\" line naming \"missing AC-2, AC-3, AC-4, AC-5\"\n\nCASE-4 — the warning states the tagged-of-total count\n  Given  a confirmed requirement with AC-1 and AC-2 labelled, only AC-1 tagged\n  When   `gate` runs\n  Then   its output contains \"1/2 automatable criteria\"\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-ACVERIFY-019"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1645"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3875"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Maparea etichetelor verifies la criteriile etichetate",
          "intent": "Gate poate dovedi că o cerință are *un* test, dar nu că fiecare dintre criteriile ei este testat individual. `scan_ac_verifies` citește etichete `# verifies: <id>#AC-N` astfel încât gate poate numi exact ce criteriu etichetat încă nu are niciun test, în loc să trateze întreaga cerință ca trece-sau-pică.",
          "contract": "> Gate poate dovedi că o cerință are *un* test, dar nu că fiecare dintre criteriile ei este\n> testat individual. `scan_ac_verifies` citește etichete `# verifies: <id>#AC-N` astfel\n> încât gate poate numi exact ce criteriu etichetat încă nu are niciun test, în loc să\n> trateze întreaga cerință ca trece-sau-pică.\n\nFiecare punct de mai jos este obligatoriu.\n- Gate scanează codul după etichete `# verifies: <id>#AC-N` și mapează fiecare etichetă la\n  criteriul etichetat pe care îl acoperă. Aceasta este jumătatea per-criteriu a behaviour-sync.\n- Gate recunoaște o etichetă `verifies` doar cu un sufix `#AC-N`, astfel încât o referință\n  simplă la cerință nu este niciodată confundată cu una per-criteriu.\n- Pentru o cerință confirmed care își etichetează criteriile și poartă cel puțin o etichetă\n  `verifies`, gate avertizează o dată, numind fiecare criteriu etichetat care nu are\n  etichetă.\n- Acel singur avertisment mai indică și câte criterii etichetate sunt marcate, astfel încât\n  adoptarea parțială se citește ca progres, nu ca o grămadă tot mai mare de avertismente.",
          "acceptance": "CASE-1 — o etichetă verifies este mapată la criteriul ei etichetat\n  Given  un fișier de test care poartă `# verifies: AREA-A-001#CASE-1` și o cerință care etichetează CASE-1 și CASE-2\n  When   rulează `gate`\n  Then   raportează că 1 din cele 2 criterii automatizabile poartă o etichetă `verifies:`, numind CASE-2 ca lipsă\n\nCASE-2 — o etichetă verifies fără sufix #AC-N nu este acoperire per-criteriu\n  Given  un fișier care poartă `# verifies: REQ-X-001` fără `#AC-N` final\n  When   `scan_ac_verifies` îl scanează\n  Then   returnează o mapare goală pentru `REQ-X-001`\n\nCASE-3 — acoperirea verifies parțială produce un singur avertisment care numește fiecare gol\n  Given  o cerință confirmed cu cinci criterii etichetate, `AC-1` etichetat și AC-2..AC-5\n         neetichetate\n  When   rulează `gate`\n  Then   afișează exact o linie „criterii automatizabile” care numește „lipsesc AC-2, AC-3, AC-4, AC-5”\n\nCASE-4 — avertismentul indică numărul etichetat-din-total\n  Given  o cerință confirmed cu AC-1 și AC-2 etichetate, doar AC-1 etichetat\n  When   rulează `gate`\n  Then   ieșirea sa conține „1/2 criterii automatizabile”\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-ACVERIFY-822",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "When per-criterion coverage stays silent",
      "intent": "Per-criterion tagging is opt-in: a requirement that never adopted it, has no labelled criteria, or marks a criterion as manually-verifiable must not be penalized for a check it never signed up for. Without these exemptions every confirmed requirement lacking full `verifies` coverage would warn, drowning real gaps in noise nobody can act on.",
      "contract": [
        "A confirmed requirement with no `verifies` tag is exempt. Per-criterion tagging is opt-in, and the coarser `tested-by` check still applies to it.",
        "A requirement whose criteria are unlabelled bullets is exempt, because there is no criterion label for a tag to address.",
        "A criterion marked `<!-- verifiable by: inspection -->` or `manual` is excluded from the warning and from the counts. No tag can ever cover it."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — zero verifies tags is silent, not a violation Given  a confirmed requirement with two labelled criteria and zero `# verifies:` tags anywhere in the tree When   `gate` runs Then   its output contains no \"criterion unverified\" finding",
        "CASE-2 — — unlabelled bullet criteria never trigger the per-criterion warning Given  a confirmed requirement whose Cases are plain bullets, no `AC-N`/`CASE-N` labels, and a `# verifies:` tag present in code When   `gate` runs Then   its output contains no \"criterion unverified\" finding",
        "CASE-3 — — an inspection-only criterion never triggers the automatable-criteria warning Given  a confirmed requirement with `AC-1` tagged and `AC-2` marked `` When   `gate` runs Then   its output contains no \"automatable criteria\" line --------------------"
      ],
      "accept": "CASE-1 — zero verifies tags is silent, not a violation\n  Given  a confirmed requirement with two labelled criteria and zero `# verifies:` tags\n         anywhere in the tree\n  When   `gate` runs\n  Then   its output contains no \"criterion unverified\" finding\n\nCASE-2 — unlabelled bullet criteria never trigger the per-criterion warning\n  Given  a confirmed requirement whose Cases are plain bullets, no `AC-N`/`CASE-N` labels,\n         and a `# verifies:` tag present in code\n  When   `gate` runs\n  Then   its output contains no \"criterion unverified\" finding\n\nCASE-3 — an inspection-only criterion never triggers the automatable-criteria warning\n  Given  a confirmed requirement with `AC-1` tagged and `AC-2` marked\n         `<!-- verifiable by: inspection -->`\n  When   `gate` runs\n  Then   its output contains no \"automatable criteria\" line\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-ACVERIFY-019"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1822"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1834"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3875"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 2,
      "covered": 2,
      "i18n": {
        "ro": {
          "title": "Când acoperirea per-criteriu rămâne tăcută",
          "intent": "Etichetarea per-criteriu este opt-in: o cerință care nu a adoptat-o niciodată, nu are criterii etichetate, sau marchează un criteriu ca verificabil manual nu trebuie penalizată pentru o verificare la care nu a subscris niciodată. Fără aceste scutiri, fiecare cerință confirmed căreia îi lipsește acoperirea `verifies` completă ar avertiza, înecând golurile reale într-un zgomot pe care nimeni nu poate acționa.",
          "contract": "> Etichetarea per-criteriu este opt-in: o cerință care nu a adoptat-o niciodată, nu are\n> criterii etichetate, sau marchează un criteriu ca verificabil manual nu trebuie\n> penalizată pentru o verificare la care nu a subscris niciodată. Fără aceste scutiri,\n> fiecare cerință confirmed căreia îi lipsește acoperirea `verifies` completă ar avertiza, înecând golurile reale într-un zgomot pe care nimeni nu poate acționa.\n\nFiecare punct de mai jos este obligatoriu.\n- O cerință confirmed fără nicio etichetă `verifies` este scutită. Etichetarea per-criteriu\n  este opt-in, iar verificarea mai grosieră `tested-by` tot i se aplică.\n- O cerință ale cărei criterii sunt puncte neetichetate este scutită, pentru că nu există\n  nicio etichetă de criteriu la care o etichetă să se poată adresa.\n- Un criteriu marcat `<!-- verifiable by: inspection -->` sau `manual` este exclus din\n  avertisment și din numărători. Nicio etichetă nu îl poate acoperi vreodată.",
          "acceptance": "CASE-1 — zero etichete verifies este tăcere, nu o încălcare\n  Given  o cerință confirmed cu două criterii etichetate și zero etichete `# verifies:`\n         oriunde în arbore\n  When   rulează `gate`\n  Then   ieșirea sa nu conține nicio constatare „criteriu neverificat”\n\nCASE-2 — criteriile de tip punct neetichetat nu declanșează niciodată avertismentul per-criteriu\n  Given  o cerință confirmed ale cărei Cases sunt puncte simple, fără etichete `AC-N`/`CASE-N`,\n         și o etichetă `# verifies:` prezentă în cod\n  When   rulează `gate`\n  Then   ieșirea sa nu conține nicio constatare „criteriu neverificat”\n\nCASE-3 — un criteriu doar-inspecție nu declanșează niciodată avertismentul de criterii automatizabile\n  Given  o cerință confirmed cu `AC-1` etichetat și `AC-2` marcat\n         `<!-- verifiable by: inspection -->`\n  When   rulează `gate`\n  Then   ieșirea sa nu conține nicio linie „criterii automatizabile”\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-ACVERIFY-823",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Emitting clauses, covered and gap on the map",
      "intent": "Once a requirement adopts per-criterion tagging, the map records how many criteria exist, how many are covered, and which are not — so a reviewer sees partial coverage without reading the gate output. An absent `clauses` field means the requirement was never measured, never a substituted zero.",
      "contract": [
        "The map emits `clauses` and `covered` on a requirement that has adopted per-criterion tagging, and omits both fields otherwise.",
        "The map emits a `gap` naming the untagged criteria when coverage is partial.",
        "An absent `clauses` means \"not measured\". No reader may substitute a number of its own.",
        "The check is warn-only. It never changes the gate's exit code."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — clauses/covered appear only once tagging is adopted Given  a requirement with two labelled criteria and no `verifies` coverage passed to `_build_map_data`, and separately the same requirement with `AC-1` covered When   `_build_map_data` builds each node Then   the untagged node has no `clauses`/`covered` keys — absent means not measured, never a substituted zero — and the tagged node has `clauses: 2, covered: 1`",
        "CASE-2 — — gap names the untagged criteria only when coverage is partial Given  a two-criterion requirement with only `AC-1` covered, and separately one with both `AC-1` and its only criterion covered When   `_build_map_data` builds each node Then   the partial node's `gap` contains \"AC-2\" and the fully-covered node carries no `gap` key",
        "CASE-3 — — an unverified criterion warns but the gate exits 0 Given  a confirmed requirement with `AC-1` tagged and `AC-2` untagged, nothing else causing an error When   `gate` runs Then   the run warns about AC-2 and still exits 0"
      ],
      "accept": "CASE-1 — clauses/covered appear only once tagging is adopted\n  Given  a requirement with two labelled criteria and no `verifies` coverage passed to\n         `_build_map_data`, and separately the same requirement with `AC-1` covered\n  When   `_build_map_data` builds each node\n  Then   the untagged node has no `clauses`/`covered` keys — absent means not measured,\n         never a substituted zero — and the tagged node has `clauses: 2, covered: 1`\n\nCASE-2 — gap names the untagged criteria only when coverage is partial\n  Given  a two-criterion requirement with only `AC-1` covered, and separately one with\n         both `AC-1` and its only criterion covered\n  When   `_build_map_data` builds each node\n  Then   the partial node's `gap` contains \"AC-2\" and the fully-covered node carries no\n         `gap` key\n\nCASE-3 — an unverified criterion warns but the gate exits 0\n  Given  a confirmed requirement with `AC-1` tagged and `AC-2` untagged, nothing else\n         causing an error\n  When   `gate` runs\n  Then   the run warns about AC-2 and still exits 0",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-ACVERIFY-019"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3941"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4014"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Emiterea clauzelor, acoperite și lacunelor pe hartă",
          "intent": "Odată ce o cerință adoptă etichetarea per-criteriu, harta înregistrează câte criterii există, câte sunt acoperite și care nu sunt — astfel încât un revizor vede acoperirea parțială fără să citească ieșirea gate-ului. Un câmp `clauses` absent înseamnă că cerința nu a fost niciodată măsurată, niciodată un zero substituit.",
          "contract": "> Odată ce o cerință adoptă etichetarea per-criteriu, harta înregistrează câte criterii există,\n> câte sunt acoperite și care nu sunt — astfel încât un revizor vede acoperirea parțială fără să\n> citească ieșirea gate-ului. Un câmp `clauses` absent înseamnă că cerința nu a fost niciodată\n> măsurată, niciodată un zero substituit.\n\nFiecare punct de mai jos este obligatoriu.\n- Harta emite `clauses` și `covered` pentru o cerință care a adoptat etichetarea per-criteriu,\n  și omite ambele câmpuri altfel.\n- Harta emite un `gap` care numește criteriile neetichetate atunci când acoperirea este parțială.\n- Un `clauses` absent înseamnă \"nemăsurat\". Niciun cititor nu poate substitui un număr al său.\n- Verificarea este doar de avertizare. Nu schimbă niciodată codul de ieșire al gate-ului.",
          "acceptance": "CASE-1 — clauses/covered apar doar odată ce etichetarea este adoptată\n  Given  o cerință cu două criterii etichetate și fără acoperire `verifies` transmisă către\n         `_build_map_data`, și separat aceeași cerință cu `AC-1` acoperit\n  When   `_build_map_data` construiește fiecare nod\n  Then   nodul neetichetat nu are cheile `clauses`/`covered` — absența înseamnă nemăsurat,\n         niciodată un zero substituit — iar nodul etichetat are `clauses: 2, covered: 1`\n\nCASE-2 — gap numește criteriile neetichetate doar când acoperirea este parțială\n  Given  o cerință cu două criterii, cu doar `AC-1` acoperit, și separat una cu\n         `AC-1` și singurul ei criteriu acoperite\n  When   `_build_map_data` construiește fiecare nod\n  Then   `gap`-ul nodului parțial conține \"AC-2\" iar nodul complet acoperit nu poartă\n         cheia `gap`\n\nCASE-3 — un criteriu neverificat avertizează dar gate-ul iese cu 0\n  Given  o cerință confirmată cu `AC-1` etichetat și `AC-2` neetichetat, nimic altceva\n         care să cauzeze o eroare\n  When   `gate` rulează\n  Then   rularea avertizează despre AC-2 și tot iese cu 0"
        }
      }
    },
    {
      "id": "ARCH-ATOMICFORM-053",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "The atomic requirement form",
      "intent": "One obligation per file makes the clause the unit a test answers to, and removes the two headings that only restated each other.",
      "contract": [
        "A requirement stating a single obligation may be written as one `>` story blockquote plus one unlabelled `Scenario:` block, with no `## Contract` or `## Cases` heading at all — the file then carries the obligation and its proof and nothing else.",
        "The atomic form is detected from the body itself (a story quote immediately followed by a `Scenario:` block, both before the first `## ` heading), never from `form: atomic` in the frontmatter, so every consumer that only sees a body — the gate, the linter, the drift hash — agrees on whether a given file is atomic.",
        "`binding_hash` covers the story and the Scenario, never the empty string, so an atomic requirement drifts like any other when its wording changes.",
        "The linter treats a recognised atomic body as carrying both normative sections and one acceptance criterion (the Scenario), raising no missing-section, legacy-schema or under-specified finding — but it does check that every fact enumerated in the story has a matching `Then` line in the Scenario, and that the story does not grow past a fixed bullet ceiling."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — an atomic body reads as both normative sections Given  a body whose only content before the first `## ` heading is a `>` story followed by a `Scenario:` block When   the gate and linter read it Then   both `## Contract` and `## Cases` count as present, and the Scenario counts as exactly one acceptance criterion",
        "CASE-2 — — the drift hash covers the statement and the Scenario Given  an atomic body When   `binding_hash` runs on it, and again after editing either the story or the Scenario Then   the hash is never the hash of the empty string, and each edit changes it",
        "CASE-3 — — a well-formed atomic requirement lints clean Given  a confirmed requirement with `form: atomic` and a recognised story + Scenario body When   the linter runs Then   it raises no finding",
        "CASE-4 — — a classic body with headings is never mistaken for atomic Given  a body using `## WHAT — Contract` and `## HOW — Acceptance` headings When   the atomic-form detector reads it Then   it returns no match, and every existing classic-form code path is unchanged",
        "CASE-5 — — a story fact with no matching Then line warns Given  an atomic story enumerating more `- ` facts than the Scenario has `Then` lines When   the linter runs Then   it warns `atomic-bullet-then-mismatch`; a story past the bullet ceiling instead warns `atomic-story-overlong`"
      ],
      "accept": "CASE-1 — an atomic body reads as both normative sections\n  Given  a body whose only content before the first `## ` heading is a `>` story followed by\n         a `Scenario:` block\n  When   the gate and linter read it\n  Then   both `## Contract` and `## Cases` count as present, and the Scenario counts as\n         exactly one acceptance criterion\n\nCASE-2 — the drift hash covers the statement and the Scenario\n  Given  an atomic body\n  When   `binding_hash` runs on it, and again after editing either the story or the Scenario\n  Then   the hash is never the hash of the empty string, and each edit changes it\n\nCASE-3 — a well-formed atomic requirement lints clean\n  Given  a confirmed requirement with `form: atomic` and a recognised story + Scenario body\n  When   the linter runs\n  Then   it raises no finding\n\nCASE-4 — a classic body with headings is never mistaken for atomic\n  Given  a body using `## WHAT — Contract` and `## HOW — Acceptance` headings\n  When   the atomic-form detector reads it\n  Then   it returns no match, and every existing classic-form code path is unchanged\n\nCASE-5 — a story fact with no matching Then line warns\n  Given  an atomic story enumerating more `- ` facts than the Scenario has `Then` lines\n  When   the linter runs\n  Then   it warns `atomic-bullet-then-mismatch`; a story past the bullet ceiling instead\n         warns `atomic-story-overlong`",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001",
        "ARCH-DRIFT-003",
        "ARCH-CHECK-006"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-AUTHOR-101"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1748"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1892"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1895"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1933"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1950"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1960"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2494"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2803"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6343"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7346"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "should-have",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Forma atomică a cerinței",
          "intent": "O singură obligație per fișier face din clauză unitatea la care răspunde un test și elimină cele două titluri care doar se reformulau reciproc.",
          "contract": "> O singură obligație per fișier face din clauză unitatea la care răspunde un test și elimină cele două titluri care doar se reformulau reciproc.\n\nFiecare punct de mai jos este obligatoriu.\n- O cerință care afirmă o singură obligație poate fi scrisă ca un singur citat-poveste `>`\n  plus un bloc `Scenario:` neetichetat, fără niciun titlu `## Contract` sau `## Cases` —\n  fișierul poartă atunci obligația și dovada ei și nimic altceva.\n- Forma atomică este detectată din corpul textului însuși (un citat-poveste urmat imediat de\n  un bloc `Scenario:`, ambele înainte de primul titlu `## `), niciodată din `form: atomic` din\n  frontmatter, astfel încât orice consumator care vede doar corpul textului — gate-ul, linter-ul,\n  hash-ul de drift — este de acord dacă un fișier dat este atomic.\n- `binding_hash` acoperă povestea și Scenario-ul, niciodată șirul vid, astfel încât o cerință\n  atomică suferă drift ca oricare alta atunci când formularea ei se schimbă.\n- Linter-ul tratează un corp atomic recunoscut ca purtând ambele secțiuni normative și un\n  criteriu de acceptare (Scenario-ul), fără a ridica nicio constatare de secțiune lipsă,\n  schemă-veche sau sub-specificare — dar verifică totuși ca fiecare fapt enumerat în poveste\n  să aibă o linie `Then` corespunzătoare în Scenario, și că povestea nu depășește un plafon\n  fix de puncte.",
          "acceptance": "CASE-1 — un corp atomic se citește ca ambele secțiuni normative\n  Given  un corp al cărui unic conținut înainte de primul titlu `## ` este o poveste `>` urmată de\n         un bloc `Scenario:`\n  When   gate-ul și linter-ul îl citesc\n  Then   atât `## Contract` cât și `## Cases` contează ca prezente, iar Scenario-ul contează ca\n         exact un criteriu de acceptare\n\nCASE-2 — hash-ul de drift acoperă afirmația și Scenario-ul\n  Given  un corp atomic\n  When   `binding_hash` rulează pe el, apoi din nou după editarea poveștii sau a Scenario-ului\n  Then   hash-ul nu este niciodată hash-ul șirului vid, iar fiecare editare îl schimbă\n\nCASE-3 — o cerință atomică bine formată trece linterul curat\n  Given  o cerință confirmată cu `form: atomic` și un corp poveste + Scenario recunoscut\n  When   linter-ul rulează\n  Then   nu ridică nicio constatare\n\nCASE-4 — un corp clasic cu titluri nu este niciodată confundat cu unul atomic\n  Given  un corp care folosește titlurile `## WHAT — Contract` și `## HOW — Acceptance`\n  When   detectorul formei atomice îl citește\n  Then   nu returnează nicio potrivire, iar orice cale de cod existentă pentru forma clasică rămâne neschimbată\n\nCASE-5 — un fapt din poveste fără linie Then corespunzătoare avertizează\n  Given  o poveste atomică ce enumeră mai multe fapte `- ` decât are Scenario-ul linii `Then`\n  When   linter-ul rulează\n  Then   avertizează `atomic-bullet-then-mismatch`; o poveste ce depășește plafonul de puncte\n         avertizează în schimb `atomic-story-overlong`"
        }
      }
    },
    {
      "id": "ARCH-ATOMICITY-049",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Statement atomicity",
      "intent": "A requirement file here is a capability dossier, not one sentence — its Contract holds many clauses, and each clause is the real unit a test answers to. This says what makes a clause well-formed: one obligation, verifiable on its own. It also ships a word-count heuristic that points a reader at clauses worth re-reading. The two are deliberately not the same thing, and the second never decides the first.",
      "contract": [
        "A clause in a Contract section describes a single obligation that can be verified independently; `lint`'s `statement-size` check enforces a word-count threshold on top of that rule, advisory and silencable, never a proxy for the rule itself. [[REQ-ATOMICITY-824]]",
        "`statement-size` measures textual size, never semantic atomicity: a short clause with two obligations passes, and a long single-obligation clause merely earns a re-read. [[REQ-ATOMICITY-825]]"
      ],
      "verify": [],
      "notes": [
        "The two halves are not the same kind of statement, and the file keeps them apart on purpose. Atomicity is the **normative rule**: a clause carrying two obligations is malformed, whoever notices it. The 150 words are a **heuristic threshold**: a coarse proxy for clause complexity, advisory only, and never a determination of atomicity. Promoting the threshold into a validity criterion would assert something the engine cannot observe.",
        "The rule as the author stated it: *\"A clause shall describe a single independently verifiable obligation\"* (normative), and *\"A clause should normally not exceed 150 words\"* (heuristic) — with *\"Exceeding 150 words does not make a clause invalid and shall not affect the lint exit code\"* and *\"shall not be interpreted as a determination of atomicity.\"* The Contract restates these without `shall`/`should`, because the section header already binds every clause and `redundant-modal` flags the modal (ARCH-LINTCHECKS-025).",
        "The epistemic limit is stated in the Contract rather than left here, so no later reader or tool can promote the heuristic into the rule. CASE-6 pins it as behaviour: a short clause with two obligations passes, and that is correct, not a gap to close.",
        "Measured on this corpus before the check was specified: 586 Contract clauses across 52 requirements, mean 18.4 words, median 16, p90 31, **max 62**. At the 150-word threshold **0 of 586 clauses (0.0%) are over**, in 0 of 52 files — as at 100 and at 75, since the longest clause in the corpus is 62 words. So the heuristic ships as a line already held, not as a defect hunt: its value is the clause written next year. The threshold was raised 75 -> 100 -> 150 on 2026-09-02/03; each step widened the margin before it speaks without changing anything it says today, and 150 is now 2.4x the longest clause ever written here.",
        "The two checks that already read this text measure a different unit, which is why this one could not reuse them. `long-sentence` (25 words per sentence) and `statement-too-long` (3 sentences per clause) run over `_lint_prose`, which yields physical LINES. These files are hard-wrapped near 95 columns, so the longest line `_lint_prose` produced for `ARCH-CHECK-006` is 15 words, and both checks report **0** across the corpus — not because the prose is short, but because a clause is never seen whole. `statement-size` joins continuation lines first (`_contract_clauses`), so an 80-word clause wrapped over six lines is visible to it and invisible to them. The per-line checks are left alone on purpose: widening their unit would flip the corpus from 0 warnings to many, on confirmed requirements, which is a separate decision.",
        "At 0% the heuristic sits below ADR-0016's 5-40% pre-ship fire-rate bar. That bar gates checks proposed to *find* problems; an advisory line-holder is a different instrument, which is why it ships warn-only with an exemption rather than as a gate (ADR-0002, ADR-0014)."
      ],
      "current_impl": [
        "`LINT_STATEMENT_WORDS`, `_clause_words` and `_contract_clauses` in `reqmap.py`. The `statement-size` block in `lint_requirement` appends one warn finding per over-threshold clause, carrying `clause_n` and `clause_text` so [[ARCH-DECOMPOSE-050]] scaffolds from the same parse instead of re-reading the file. `_clause_words` collapses a backticked span to a bare token with no padding, because `\" x \"` split trailing punctuation into a second word and inflated every such clause by one.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a Contract clause of 80 plain words When   `lint` runs Then   exactly one `statement-size` finding names that clause, and the exit code is unchanged",
        "CASE-2 — Given  a Contract clause of 70 plain words When   `lint` runs Then   no `statement-size` finding is reported for it",
        "CASE-3 — Given  a Contract clause of 80 words, 60 of them inside one backticked span When   `lint` runs Then   the span counts as one word, the clause counts 21, and nothing is reported",
        "CASE-4 — Given  a parent clause of 40 words carrying a nested sub-bullet of 80 words When   `lint` runs Then   the sub-bullet alone is reported, and the parent is not",
        "CASE-5 — Given  a requirement carrying `lint_exempt: [statement-size]` and an over-threshold clause When   `lint` runs Then   no `statement-size` finding is reported for that requirement",
        "CASE-6 — Given  a 20-word Contract clause stating two independent obligations When   `lint` runs Then   no `statement-size` finding is reported, because the check reads size and not atomicity"
      ],
      "accept": "CASE-1\n  Given  a Contract clause of 80 plain words\n  When   `lint` runs\n  Then   exactly one `statement-size` finding names that clause, and the exit code is unchanged\nCASE-2\n  Given  a Contract clause of 70 plain words\n  When   `lint` runs\n  Then   no `statement-size` finding is reported for it\nCASE-3\n  Given  a Contract clause of 80 words, 60 of them inside one backticked span\n  When   `lint` runs\n  Then   the span counts as one word, the clause counts 21, and nothing is reported\nCASE-4\n  Given  a parent clause of 40 words carrying a nested sub-bullet of 80 words\n  When   `lint` runs\n  Then   the sub-bullet alone is reported, and the parent is not\nCASE-5\n  Given  a requirement carrying `lint_exempt: [statement-size]` and an over-threshold clause\n  When   `lint` runs\n  Then   no `statement-size` finding is reported for that requirement\nCASE-6\n  Given  a 20-word Contract clause stating two independent obligations\n  When   `lint` runs\n  Then   no `statement-size` finding is reported, because the check reads size and not\n         atomicity",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001",
        "ARCH-LINT-014"
      ],
      "used_by": [
        "ARCH-DECOMPOSE-050"
      ],
      "satisfies": [
        "SYS-AUTHOR-101"
      ],
      "satisfied_by": [
        "REQ-ATOMICITY-824",
        "REQ-ATOMICITY-825"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4525"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4533"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6951"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9175"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "should-have",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Atomicitatea afirmației",
          "intent": "Aici, un fișier de cerință este un dosar de capabilitate, nu o singură propoziție — Contractul său conține multe clauze, iar fiecare clauză este unitatea reală la care răspunde un test. Aceasta stabilește ce face o clauză bine formată: o singură obligație, verificabilă de sine stătător. Livrează de asemenea o euristică bazată pe numărul de cuvinte care îi indică cititorului clauzele care merită recitite. Cele două lucruri nu sunt, în mod deliberat, același lucru, iar al doilea nu decide niciodată în locul primului.",
          "contract": "> Aici, un fișier de cerință este un dosar de capabilitate, nu o singură propoziție —\n> Contractul său conține multe clauze, iar fiecare clauză este unitatea reală la care\n> răspunde un test. Aceasta stabilește ce face o clauză bine formată: o singură obligație,\n> verificabilă de sine stătător. Livrează de asemenea o euristică bazată pe numărul de\n> cuvinte care îi indică cititorului clauzele care merită recitite. Cele două lucruri nu\n> sunt, în mod deliberat, același lucru, iar al doilea nu decide niciodată în locul primului.\n\nFiecare punct de mai jos este obligatoriu.\n- O clauză dintr-o secțiune Contract descrie o singură obligație care poate fi verificată independent; `lint` impune, prin verificarea sa `statement-size`, un prag bazat pe numărul de cuvinte peste această regulă, cu titlu consultativ și poate fi dezactivată, niciodată un substitut pentru regula însăși. [[REQ-ATOMICITY-824]]\n- `statement-size` măsoară dimensiunea textuală, niciodată atomicitatea semantică: o clauză scurtă cu două obligații trece, iar o clauză lungă cu o singură obligație doar merită o recitire. [[REQ-ATOMICITY-825]]",
          "acceptance": "CASE-1\n  Given  o clauză Contract de 80 de cuvinte simple\n  When   `lint` rulează\n  Then   exact o constatare `statement-size` numește acea clauză, iar codul de ieșire rămâne neschimbat\nCASE-2\n  Given  o clauză Contract de 70 de cuvinte simple\n  When   `lint` rulează\n  Then   nu se raportează nicio constatare `statement-size` pentru ea\nCASE-3\n  Given  o clauză Contract de 80 de cuvinte, 60 dintre ele într-un singur segment încadrat cu backtick\n  When   `lint` rulează\n  Then   segmentul contează ca un singur cuvânt, clauza numără 21, și nu se raportează nimic\nCASE-4\n  Given  o clauză părinte de 40 de cuvinte care poartă un sub-punct imbricat de 80 de cuvinte\n  When   `lint` rulează\n  Then   doar sub-punctul este raportat, iar părintele nu\nCASE-5\n  Given  o cerință care poartă `lint_exempt: [statement-size]` și o clauză peste prag\n  When   `lint` rulează\n  Then   nu se raportează nicio constatare `statement-size` pentru acea cerință\nCASE-6\n  Given  o clauză Contract de 20 de cuvinte care afirmă două obligații independente\n  When   `lint` rulează\n  Then   nu se raportează nicio constatare `statement-size`, deoarece verificarea citește dimensiunea și nu\n         atomicitatea"
        }
      }
    },
    {
      "id": "REQ-ATOMICITY-824",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "One obligation per clause, with an advisory length backstop",
      "intent": "A Contract clause is the unit a test answers to, so a clause stating two obligations hides one of them from review. This is the normative rule a human applies when writing or reviewing a clause, plus the one mechanical thing `lint` can check on top of it: a word-count threshold (`LINT_STATEMENT_WORDS`, default 150) that flags a clause worth a second look — advisory only, never a substitute for the human judgment above it.",
      "contract": [
        "A clause in a Contract section describes a single obligation that can be verified independently.",
        "A clause carrying two independent obligations counts as two clauses, not one.",
        "Atomicity is judged by a human reader. No check in the engine determines it.",
        "A clause normally holds no more than `LINT_STATEMENT_WORDS` words, default 150.",
        "The `statement-size` check reports a Contract clause above that threshold.",
        "The threshold is advisory. A clause above it stays valid, and the exit code is unchanged.",
        "`lint_exempt: [statement-size]` silences the check for one requirement."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a clause stating two obligations is split, not shortened   Given  a clause covering both \"the service issues a token on valid credentials\" and \"the service revokes it on logout\" When   an author applies the atomicity rule during review Then   the clause becomes two clauses, each stating one obligation and independently verifiable — trimming words alone would not satisfy the rule",
        "CASE-2 — — atomicity is a human judgment; the check never asserts it Given  a short clause under the word-count threshold that still states two obligations When   `lint` runs Then   it reports no finding for that clause — the threshold check has nothing to say about atomicity, only about length",
        "CASE-3 — — the default statement-size threshold is 150 words Given  the module constant `LINT_STATEMENT_WORDS` When   its value is read Then   it equals `150`",
        "CASE-4 — — a 155-word clause produces exactly one statement-size finding Given  a Contract clause of 155 plain words When   `lint_requirement` runs Then   exactly one finding names `check: \"statement-size\"` and `clause_n: 1`",
        "CASE-5 — — an over-threshold clause's finding carries warn severity, not error Given  a Contract clause of 155 plain words When   `lint_requirement` runs Then   the `statement-size` finding's `severity` reads `\"warn\"`, never `\"error\"`",
        "CASE-6 — — lint_exempt: [statement-size] silences an otherwise-reported finding Given  a 155-word clause on a requirement carrying `lint_exempt: [statement-size]` When   `lint_requirement` runs Then   no `statement-size` finding appears, though it appears without the exemption --------------------"
      ],
      "accept": "CASE-1 — a clause stating two obligations is split, not shortened  <!-- verifiable by: inspection -->\n  Given  a clause covering both \"the service issues a token on valid credentials\" and\n         \"the service revokes it on logout\"\n  When   an author applies the atomicity rule during review\n  Then   the clause becomes two clauses, each stating one obligation and independently\n         verifiable — trimming words alone would not satisfy the rule\n\nCASE-2 — atomicity is a human judgment; the check never asserts it\n  Given  a short clause under the word-count threshold that still states two obligations\n  When   `lint` runs\n  Then   it reports no finding for that clause — the threshold check has nothing to say\n         about atomicity, only about length\n\nCASE-3 — the default statement-size threshold is 150 words\n  Given  the module constant `LINT_STATEMENT_WORDS`\n  When   its value is read\n  Then   it equals `150`\n\nCASE-4 — a 155-word clause produces exactly one statement-size finding\n  Given  a Contract clause of 155 plain words\n  When   `lint_requirement` runs\n  Then   exactly one finding names `check: \"statement-size\"` and `clause_n: 1`\n\nCASE-5 — an over-threshold clause's finding carries warn severity, not error\n  Given  a Contract clause of 155 plain words\n  When   `lint_requirement` runs\n  Then   the `statement-size` finding's `severity` reads `\"warn\"`, never `\"error\"`\n\nCASE-6 — lint_exempt: [statement-size] silences an otherwise-reported finding\n  Given  a 155-word clause on a requirement carrying `lint_exempt: [statement-size]`\n  When   `lint_requirement` runs\n  Then   no `statement-size` finding appears, though it appears without the exemption\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-ATOMICITY-049"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4525"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4533"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6951"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9175"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "O obligație per clauză, cu un prag de lungime consultativ",
          "intent": "O clauză de Contract este unitatea la care răspunde un test, deci o clauză care afirmă două obligații ascunde una dintre ele de la revizuire. Aceasta este regula normativă pe care un om o aplică la scrierea sau revizuirea unei clauze, plus singurul lucru mecanic pe care `lint` îl poate verifica pe lângă ea: un prag de număr de cuvinte (`LINT_STATEMENT_WORDS`, implicit 150) care marchează o clauză ce merită o a doua privire — doar consultativ, niciodată un substitut pentru judecata umană de mai sus.",
          "contract": "> O clauză de Contract este unitatea la care răspunde un test, deci o clauză care afirmă\n> două obligații ascunde una dintre ele de la revizuire. Aceasta este regula normativă pe care\n> un om o aplică la scrierea sau revizuirea unei clauze, plus singurul lucru mecanic pe care\n> `lint` îl poate verifica pe lângă ea: un prag de număr de cuvinte (`LINT_STATEMENT_WORDS`,\n> implicit 150) care marchează o clauză ce merită o a doua privire — doar consultativ, niciodată\n> un substitut pentru judecata umană de mai sus.\n\nFiecare punct de mai jos este obligatoriu.\n- O clauză dintr-o secțiune de Contract descrie o singură obligație care poate fi verificată independent.\n- O clauză care poartă două obligații independente contează ca două clauze, nu una.\n- Atomicitatea este judecată de un cititor uman. Nicio verificare din motor nu o determină.\n- O clauză conține în mod normal cel mult `LINT_STATEMENT_WORDS` cuvinte, implicit 150.\n- Verificarea `statement-size` raportează o clauză de Contract peste acel prag.\n- Pragul este consultativ. O clauză peste el rămâne validă, iar codul de ieșire rămâne neschimbat.\n- `lint_exempt: [statement-size]` dezactivează verificarea pentru o singură cerință.",
          "acceptance": "CASE-1 — o clauză care afirmă două obligații este despărțită, nu scurtată  <!-- verifiable by: inspection -->\n  Given  o clauză care acoperă atât \"serviciul emite un token la credențiale valide\" cât și\n         \"serviciul îl revocă la delogare\"\n  When   un autor aplică regula de atomicitate la revizuire\n  Then   clauza devine două clauze, fiecare afirmând o obligație și verificabilă\n         independent — doar scurtarea cuvintelor nu ar satisface regula\n\nCASE-2 — atomicitatea este o judecată umană; verificarea nu o afirmă niciodată\n  Given  o clauză scurtă sub pragul de număr de cuvinte care totuși afirmă două obligații\n  When   `lint` rulează\n  Then   nu raportează nicio constatare pentru acea clauză — verificarea de prag nu are\n         nimic de spus despre atomicitate, doar despre lungime\n\nCASE-3 — pragul implicit statement-size este 150 de cuvinte\n  Given  constanta modulului `LINT_STATEMENT_WORDS`\n  When   valoarea ei este citită\n  Then   este egală cu `150`\n\nCASE-4 — o clauză de 155 de cuvinte produce exact o constatare statement-size\n  Given  o clauză de Contract de 155 de cuvinte simple\n  When   `lint_requirement` rulează\n  Then   exact o constatare numește `check: \"statement-size\"` și `clause_n: 1`\n\nCASE-5 — constatarea unei clauze peste prag poartă severitate warn, nu error\n  Given  o clauză de Contract de 155 de cuvinte simple\n  When   `lint_requirement` rulează\n  Then   câmpul `severity` al constatării `statement-size` citește `\"warn\"`, niciodată `\"error\"`\n\nCASE-6 — lint_exempt: [statement-size] dezactivează o constatare care altfel ar fi raportată\n  Given  o clauză de 155 de cuvinte pe o cerință care poartă `lint_exempt: [statement-size]`\n  When   `lint_requirement` rulează\n  Then   nu apare nicio constatare `statement-size`, deși apare fără scutire\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-ATOMICITY-825",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "statement-size measures length, not atomicity",
      "intent": "`statement-size` is a coarse proxy, not a determination: it counts words per Contract clause (backticked spans collapsed to one token, comments and Acceptance prose excluded) and flags one that runs long. A short clause with two obligations still passes — the finding only ever asks for a re-read, never claims a defect.",
      "contract": [
        "The `statement-size` check measures textual size, never semantic atomicity.",
        "A short clause may carry several independent obligations and still pass the check.",
        "A finding asks the author to re-read the clause for decomposition, and asserts no defect.",
        "The check counts words after each backticked code span is replaced by one token.",
        "A nested sub-bullet is counted as its own clause, because it states its own obligation.",
        "The check reads the Contract section only. Acceptance prose carries no size ceiling."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a short two-obligation clause produces no statement-size finding Given  a 16-word clause stating both \"issues a token\" and \"revokes it on logout\" When   `lint_requirement` runs Then   no `statement-size` finding is reported for that clause",
        "CASE-2 — — the finding's detail text asks for a re-read, not a defect claim Given  a Contract clause of 155 plain words When   the `statement-size` finding is produced Then   its `detail` text reads \"re-read it for decomposition\"",
        "CASE-3 — — a backticked span collapses to one token before counting Given  a clause of 20 plain words plus one backticked span of 140 words When   `_clause_words` counts the clause Then   it returns 21, not the raw 161-word split count",
        "CASE-4 — — a nested sub-bullet is flagged on its own, the parent is not Given  a 40-word parent bullet with a nested 155-word sub-bullet When   `lint_requirement` runs Then   exactly one `statement-size` finding appears, naming `clause_n: 2` (the sub-bullet)",
        "CASE-5 — — a 200-word Acceptance step produces no statement-size finding Given  a requirement whose Contract clauses are all short but whose Acceptance section holds a 200-word step When   `lint_requirement` runs Then   no `statement-size` finding is reported",
        "CASE-6 — — a glossary HTML comment is not counted as a clause Given  a Contract section opening with a 160-word HTML comment followed by one short clause When   `_contract_clauses` parses it Then   only the short clause is counted, at `clause_n: 1`, and no finding is reported",
        "CASE-7 — — a clause wrapped over several lines is joined before counting Given  a 155-word clause hard-wrapped as continuation lines of about 13 words each, none individually over 25 words When   `lint_requirement` runs Then   exactly one `statement-size` finding is reported for the joined clause"
      ],
      "accept": "CASE-1 — a short two-obligation clause produces no statement-size finding\n  Given  a 16-word clause stating both \"issues a token\" and \"revokes it on logout\"\n  When   `lint_requirement` runs\n  Then   no `statement-size` finding is reported for that clause\n\nCASE-2 — the finding's detail text asks for a re-read, not a defect claim\n  Given  a Contract clause of 155 plain words\n  When   the `statement-size` finding is produced\n  Then   its `detail` text reads \"re-read it for decomposition\"\n\nCASE-3 — a backticked span collapses to one token before counting\n  Given  a clause of 20 plain words plus one backticked span of 140 words\n  When   `_clause_words` counts the clause\n  Then   it returns 21, not the raw 161-word split count\n\nCASE-4 — a nested sub-bullet is flagged on its own, the parent is not\n  Given  a 40-word parent bullet with a nested 155-word sub-bullet\n  When   `lint_requirement` runs\n  Then   exactly one `statement-size` finding appears, naming `clause_n: 2` (the sub-bullet)\n\nCASE-5 — a 200-word Acceptance step produces no statement-size finding\n  Given  a requirement whose Contract clauses are all short but whose Acceptance\n         section holds a 200-word step\n  When   `lint_requirement` runs\n  Then   no `statement-size` finding is reported\n\nCASE-6 — a glossary HTML comment is not counted as a clause\n  Given  a Contract section opening with a 160-word HTML comment followed by one short clause\n  When   `_contract_clauses` parses it\n  Then   only the short clause is counted, at `clause_n: 1`, and no finding is reported\n\nCASE-7 — a clause wrapped over several lines is joined before counting\n  Given  a 155-word clause hard-wrapped as continuation lines of about 13 words each, none\n         individually over 25 words\n  When   `lint_requirement` runs\n  Then   exactly one `statement-size` finding is reported for the joined clause",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-ATOMICITY-049"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4525"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4533"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6951"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9175"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 7,
      "covered": 7,
      "i18n": {
        "ro": {
          "title": "statement-size măsoară lungimea, nu atomicitatea",
          "intent": "`statement-size` este un proxy grosier, nu o determinare: numără cuvinte per clauză de Contract (segmentele cu backtick sunt reduse la un singur token, comentariile și proza din Acceptance sunt excluse) și marchează una care se întinde prea mult. O clauză scurtă cu două obligații tot trece — constatarea cere doar o recitire, nu afirmă niciodată un defect.",
          "contract": "> `statement-size` este un proxy grosier, nu o determinare: numără cuvinte per clauză de\n> Contract (segmentele cu backtick sunt reduse la un singur token, comentariile și proza din\n> Acceptance sunt excluse) și marchează una care se întinde prea mult. O clauză scurtă cu două\n> obligații tot trece — constatarea cere doar o recitire, nu afirmă niciodată un defect.\n\nFiecare punct de mai jos este obligatoriu.\n- Verificarea `statement-size` măsoară dimensiunea textuală, niciodată atomicitatea semantică.\n- O clauză scurtă poate purta mai multe obligații independente și tot trece verificarea.\n- O constatare cere autorului să recitească clauza pentru descompunere, și nu afirmă niciun defect.\n- Verificarea numără cuvintele după ce fiecare segment de cod cu backtick este înlocuit cu un singur token.\n- Un sub-punct imbricat este numărat ca propria sa clauză, pentru că afirmă propria sa obligație.\n- Verificarea citește doar secțiunea de Contract. Proza din Acceptance nu poartă niciun plafon de dimensiune.",
          "acceptance": "CASE-1 — o clauză scurtă cu două obligații nu produce nicio constatare statement-size\n  Given  o clauză de 16 cuvinte care afirmă atât \"emite un token\" cât și \"îl revocă la delogare\"\n  When   `lint_requirement` rulează\n  Then   nu este raportată nicio constatare `statement-size` pentru acea clauză\n\nCASE-2 — textul de detaliu al constatării cere o recitire, nu afirmă un defect\n  Given  o clauză de Contract de 155 de cuvinte simple\n  When   este produsă constatarea `statement-size`\n  Then   textul `detail` citește \"re-read it for decomposition\"\n\nCASE-3 — un segment cu backtick se reduce la un singur token înainte de numărare\n  Given  o clauză de 20 de cuvinte simple plus un segment cu backtick de 140 de cuvinte\n  When   `_clause_words` numără clauza\n  Then   returnează 21, nu numărul brut de 161 de cuvinte prin despărțire\n\nCASE-4 — un sub-punct imbricat este marcat separat, părintele nu este\n  Given  un punct părinte de 40 de cuvinte cu un sub-punct imbricat de 155 de cuvinte\n  When   `lint_requirement` rulează\n  Then   apare exact o constatare `statement-size`, numind `clause_n: 2` (sub-punctul)\n\nCASE-5 — un pas Acceptance de 200 de cuvinte nu produce nicio constatare statement-size\n  Given  o cerință ale cărei clauze de Contract sunt toate scurte dar a cărei secțiune\n         Acceptance conține un pas de 200 de cuvinte\n  When   `lint_requirement` rulează\n  Then   nu este raportată nicio constatare `statement-size`\n\nCASE-6 — un comentariu HTML de glosar nu este numărat drept clauză\n  Given  o secțiune de Contract care se deschide cu un comentariu HTML de 160 de cuvinte urmat de o clauză scurtă\n  When   `_contract_clauses` o parsează\n  Then   este numărată doar clauza scurtă, la `clause_n: 1`, și nu este raportată nicio constatare\n\nCASE-7 — o clauză întinsă pe mai multe linii este unită înainte de numărare\n  Given  o clauză de 155 de cuvinte împărțită pe linii de continuare de aproximativ 13 cuvinte fiecare, niciuna\n         individual peste 25 de cuvinte\n  When   `lint_requirement` rulează\n  Then   este raportată exact o constatare `statement-size` pentru clauza unită"
        }
      }
    },
    {
      "id": "ARCH-AUDIT-065",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "One report of everything the engine can discover",
      "intent": "The engine grew one verb per question — is it linked, is it drifted, is it duplicated, is it tagged, is the design rotting — and answering \"how is this repo doing\" came to mean remembering all of them. A reader who does not already know the command list never meets most of the answers. `audit` runs those passes together and prints one report, and it ends with the two things no verb reports because they only make sense here: which findings somebody silenced, and whether the corpus has a shape at all.",
      "contract": [
        "`audit` runs every discovery pass and prints one report, taking its exit code from the gate alone. [[REQ-AUDIT-970]]",
        "Every exemption in force is listed, and one that records no reason is itself a warning. [[REQ-AUDIT-971]]",
        "The report says how the corpus sits on the V-model's left arm, and says so without failing. [[REQ-AUDIT-972]]",
        "`sync` ends by naming what the audit would report, so the moment the corpus changes is the moment its problems surface. [[REQ-AUDIT-973]]"
      ],
      "verify": [],
      "notes": [
        "`audit` is not the gate and never becomes it. Only the gate's own findings reach the exit code; a duplicate contract, a design candidate or a flat corpus is advice, and advice that can fail a build stops being read and starts being suppressed.",
        "It deliberately re-runs existing commands rather than reimplementing their checks, so there is no second definition of any finding to drift away from the first.",
        "-------------------"
      ],
      "current_impl": [],
      "acc": [
        "CASE-1 — Given  a corpus with a clean gate When   `audit` runs Then   it exits 0 and its report names every section it ran",
        "CASE-2 — Given  a corpus whose gate reports an error When   `audit` runs Then   it exits non-zero, and the advisory sections still appear in full",
        "CASE-3 — Given  any corpus When   `audit --json` runs Then   the output parses as JSON and carries the gate, health, exemption and shape records"
      ],
      "accept": "CASE-1\n  Given  a corpus with a clean gate\n  When   `audit` runs\n  Then   it exits 0 and its report names every section it ran\n\nCASE-2\n  Given  a corpus whose gate reports an error\n  When   `audit` runs\n  Then   it exits non-zero, and the advisory sections still appear in full\n\nCASE-3\n  Given  any corpus\n  When   `audit --json` runs\n  Then   the output parses as JSON and carries the gate, health, exemption and shape records",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-CHECK-006",
        "ARCH-NEXT-013",
        "ARCH-SIMILAR-016",
        "ARCH-COVERAGE-029",
        "ARCH-DESIGN-061"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-REPORT-105"
      ],
      "satisfied_by": [
        "REQ-AUDIT-970",
        "REQ-AUDIT-971",
        "REQ-AUDIT-972",
        "REQ-AUDIT-973"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2714"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5139"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5149"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5167"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5466"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5478"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5518"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8049"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Un singur raport cu tot ce poate descoperi motorul",
          "intent": "Motorul a căpătat câte un verb per întrebare — este legat, a derivat, este duplicat, este etichetat, putrezește designul — iar a răspunde la „cum stă acest repo\" a ajuns să însemne să ți le amintești pe toate. Un cititor care nu cunoaște deja lista de comenzi nu întâlnește niciodată majoritatea răspunsurilor. `audit` rulează acele treceri împreună și tipărește un singur raport, iar la final aduce cele două lucruri pe care niciun verb nu le raportează, fiindcă au sens doar aici: ce constatări a amuțit cineva și dacă corpusul are, în general, o formă.",
          "contract": "> Motorul a căpătat câte un verb per întrebare — este legat, a derivat, este duplicat, este\n> etichetat, putrezește designul — iar a răspunde la „cum stă acest repo\" a ajuns să însemne\n> să ți le amintești pe toate. Un cititor care nu cunoaște deja lista de comenzi nu întâlnește\n> niciodată majoritatea răspunsurilor. `audit` rulează acele treceri împreună și tipărește un\n> singur raport, iar la final aduce cele două lucruri pe care niciun verb nu le raportează,\n> fiindcă au sens doar aici: ce constatări a amuțit cineva și dacă corpusul are, în general, o formă.\n\nFiecare punct de mai jos este obligatoriu.\n- `audit` rulează fiecare trecere de descoperire și tipărește un singur raport, luându-și codul de ieșire doar de la poartă. [[REQ-AUDIT-970]]\n- Fiecare exceptare în vigoare este listată, iar una care nu consemnează niciun motiv este ea însăși un avertisment. [[REQ-AUDIT-971]]\n- Raportul spune cum stă corpusul pe brațul stâng al modelului V și o spune fără a eșua. [[REQ-AUDIT-972]]\n- `sync` se încheie numind ceea ce ar raporta auditul, astfel încât momentul în care corpusul se schimbă este momentul în care ies la iveală problemele lui. [[REQ-AUDIT-973]]",
          "acceptance": "CASE-1\n  Given  un corpus cu o poartă curată\n  When   rulează `audit`\n  Then   iese cu 0, iar raportul său numește fiecare secțiune pe care a rulat-o\n\nCASE-2\n  Given  un corpus a cărui poartă raportează o eroare\n  When   rulează `audit`\n  Then   iese cu cod diferit de zero, iar secțiunile consultative apar în continuare integral\n\nCASE-3\n  Given  orice corpus\n  When   rulează `audit --json`\n  Then   ieșirea se parsează ca JSON și poartă înregistrările pentru poartă, sănătate, exceptări și formă"
        }
      }
    },
    {
      "id": "REQ-AUDIT-970",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Every discovery pass, one report, one exit code",
      "intent": "Running five commands by hand to learn what is wrong with a repo means four of them are never run. The report puts its summary first — one line per signal, each naming the verb that produced it — then each section's own output underneath, so a reader can act on the summary or read the detail without running anything a second time.",
      "contract": [
        "`audit` runs the gate, the corpus risk report, the duplicate-contract scan, the design review and the tag-coverage report, and prints each one's output under its own heading.",
        "The report opens with a summary naming, for each signal, what was found and which command produces it on its own.",
        "The exit code is the gate's. Every other section is advice and can never change it.",
        "A section that raises is reported as a failed section and the rest of the report still prints. A partial report is worth more than a traceback.",
        "`--json` emits the gate findings, the health record, the design record, the untagged count, the exemptions and the corpus shape as one object."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — one command runs them all Given  a corpus with requirements and code When   `audit` runs Then   the output carries a section for the gate, risk, duplicates, design and tag coverage",
        "CASE-2 — — only the gate decides the exit code Given  a corpus whose gate is clean but which has duplicate contracts and design candidates When   `audit` runs Then   it exits 0",
        "CASE-3 — — a failing gate fails the audit Given  a corpus with a dangling membership tag When   `audit` runs Then   it exits non-zero",
        "CASE-4 — — a raising section does not take the report down Given  a discovery pass that raises When   `audit` runs Then   that section reports the failure and every other section still prints",
        "CASE-5 — — the structured form carries the same signals Given  any corpus When   `audit --json` runs Then   the parsed object carries `gate`, `health`, `exemptions` and `shape` --------------------"
      ],
      "accept": "CASE-1 — one command runs them all\n  Given  a corpus with requirements and code\n  When   `audit` runs\n  Then   the output carries a section for the gate, risk, duplicates, design and tag coverage\n\nCASE-2 — only the gate decides the exit code\n  Given  a corpus whose gate is clean but which has duplicate contracts and design candidates\n  When   `audit` runs\n  Then   it exits 0\n\nCASE-3 — a failing gate fails the audit\n  Given  a corpus with a dangling membership tag\n  When   `audit` runs\n  Then   it exits non-zero\n\nCASE-4 — a raising section does not take the report down\n  Given  a discovery pass that raises\n  When   `audit` runs\n  Then   that section reports the failure and every other section still prints\n\nCASE-5 — the structured form carries the same signals\n  Given  any corpus\n  When   `audit --json` runs\n  Then   the parsed object carries `gate`, `health`, `exemptions` and `shape`\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-AUDIT-065"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5466"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5518"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8049"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Fiecare pas de descoperire, un singur raport, un singur cod de ieșire",
          "intent": "Rularea manuală a cinci comenzi pentru a afla ce este în neregulă cu un repo înseamnă că patru dintre ele nu sunt rulate niciodată. Raportul își pune rezumatul primul — o linie per semnal, fiecare numind verbul care l-a produs — apoi, dedesubt, rezultatul propriu al fiecărei secțiuni, astfel încât un cititor poate acționa pe baza rezumatului sau poate citi detaliul fără a rula nimic a doua oară.",
          "contract": "> Rularea manuală a cinci comenzi pentru a afla ce este în neregulă cu un repo înseamnă că patru\n> dintre ele nu sunt rulate niciodată. Raportul își pune rezumatul primul — o linie per semnal,\n> fiecare numind verbul care l-a produs — apoi, dedesubt, rezultatul propriu al fiecărei secțiuni,\n> astfel încât un cititor poate acționa pe baza rezumatului sau poate citi detaliul fără a rula\n> nimic a doua oară.\n\nFiecare punct de mai jos este obligatoriu.\n- `audit` rulează gate-ul, raportul de risc al corpusului, scanarea de contracte duplicate,\n  analiza de design și raportul de acoperire cu tag-uri și tipărește rezultatul fiecăruia sub\n  propriul titlu.\n- Raportul se deschide cu un rezumat care numește, pentru fiecare semnal, ce a fost găsit și ce\n  comandă îl produce pe cont propriu.\n- Codul de ieșire este cel al gate-ului. Orice altă secțiune este consultativă și nu îl poate\n  schimba niciodată.\n- O secțiune care ridică o excepție este raportată ca secțiune eșuată, iar restul raportului tot\n  se tipărește. Un raport parțial valorează mai mult decât un traceback.\n- `--json` emite constatările gate-ului, înregistrarea de sănătate, înregistrarea de design,\n  numărul de elemente fără tag, exceptările și forma corpusului ca un singur obiect.",
          "acceptance": "CASE-1 — o singură comandă le rulează pe toate\n  Given  un corpus cu cerințe și cod\n  When   rulează `audit`\n  Then   rezultatul conține o secțiune pentru gate, risc, duplicate, design și acoperirea cu tag-uri\n\nCASE-2 — doar gate-ul decide codul de ieșire\n  Given  un corpus al cărui gate este curat, dar care are contracte duplicate și candidați de design\n  When   rulează `audit`\n  Then   iese cu 0\n\nCASE-3 — un gate care eșuează face auditul să eșueze\n  Given  un corpus cu un tag de apartenență orfan\n  When   rulează `audit`\n  Then   iese cu un cod diferit de zero\n\nCASE-4 — o secțiune care ridică o excepție nu doboară raportul\n  Given  un pas de descoperire care ridică o excepție\n  When   rulează `audit`\n  Then   acea secțiune raportează eșecul și fiecare altă secțiune tot se tipărește\n\nCASE-5 — forma structurată poartă aceleași semnale\n  Given  orice corpus\n  When   rulează `audit --json`\n  Then   obiectul parsat conține `gate`, `health`, `exemptions` și `shape`"
        }
      }
    },
    {
      "id": "REQ-AUDIT-971",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "An exemption nobody justified is itself a finding",
      "intent": "An exemption is a finding somebody decided not to see. It was also the cheapest thing to reach for: one frontmatter token silences a check permanently, nobody has to say why, and nothing ever mentions it again — so \"split this requirement\" and \"add one word here\" cost a reader the same, and the wrong one won. Listing every exemption keeps silenced from becoming invisible, and warning on a bare one makes exempting cost a sentence a reviewer can argue with.",
      "contract": [
        "The report lists every `lint_exempt:` and `gate_exempt:` entry in the corpus with its requirement and the check it silences.",
        "A reason counts as recorded when the requirement's own prose mentions the silenced check by name. The test cannot judge whether the reason is a good one, and does not try to: it makes the exemption cost a sentence, no more.",
        "The gate warns once per unjustified exemption. The finding is warn-only and is never promoted under `--strict` — the point is to make silencing cost something, not to make it impossible.",
        "The report names `clarify <ID> --decompose` as what to do with an over-scoped requirement instead, and the `ac-count-high` and `over-scoped` findings name it too."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — an exemption with a written reason is clean Given  a requirement carrying `lint_exempt: [file-spread]` whose prose explains why When   the gate runs Then   no exemption warning is raised for it",
        "CASE-2 — — a bare exemption is a warning Given  a requirement carrying `lint_exempt: [ac-count-high]` and never mentioning it again When   the gate runs Then   one warning names the requirement, the field and the silenced check",
        "CASE-3 — — the exemption warning never fails a build Given  a corpus whose only finding is an unjustified exemption When   `gate --strict` runs Then   the finding is still a warning and the run still exits 0",
        "CASE-4 — — the split findings name the tool that splits Given  a requirement with more acceptance criteria than the ceiling When   the linter reports it Then   the finding names `clarify <ID> --decompose` --------------------"
      ],
      "accept": "CASE-1 — an exemption with a written reason is clean\n  Given  a requirement carrying `lint_exempt: [file-spread]` whose prose explains why\n  When   the gate runs\n  Then   no exemption warning is raised for it\n\nCASE-2 — a bare exemption is a warning\n  Given  a requirement carrying `lint_exempt: [ac-count-high]` and never mentioning it again\n  When   the gate runs\n  Then   one warning names the requirement, the field and the silenced check\n\nCASE-3 — the exemption warning never fails a build\n  Given  a corpus whose only finding is an unjustified exemption\n  When   `gate --strict` runs\n  Then   the finding is still a warning and the run still exits 0\n\nCASE-4 — the split findings name the tool that splits\n  Given  a requirement with more acceptance criteria than the ceiling\n  When   the linter reports it\n  Then   the finding names `clarify <ID> --decompose`\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-AUDIT-065"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2714"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5139"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5149"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8049"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "O derogare pe care nimeni nu a justificat-o este ea însăși o constatare",
          "intent": "O derogare este o constatare pe care cineva a decis să nu o vadă. A fost totodată lucrul cel mai ieftin la care se putea recurge: un singur token în frontmatter reduce la tăcere o verificare permanent, nimeni nu trebuie să spună de ce, iar nimic nu o mai menționează vreodată — așa că „împarte această cerință” și „adaugă un cuvânt aici” costă cititorul la fel, iar varianta greșită a câștigat. Listarea fiecărei derogări împiedică lucrurile reduse la tăcere să devină invizibile, iar avertizarea asupra uneia neexplicate face ca derogarea să coste o frază cu care un recenzent poate polemiza.",
          "contract": "> O derogare este o constatare pe care cineva a decis să nu o vadă. A fost totodată lucrul\n> cel mai ieftin la care se putea recurge: un singur token în frontmatter reduce la tăcere o\n> verificare permanent, nimeni nu trebuie să spună de ce, iar nimic nu o mai menționează\n> vreodată — așa că „împarte această cerință” și „adaugă un cuvânt aici” costă cititorul la\n> fel, iar varianta greșită a câștigat. Listarea fiecărei derogări împiedică lucrurile reduse\n> la tăcere să devină invizibile, iar avertizarea asupra uneia neexplicate face ca derogarea\n> să coste o frază cu care un recenzent poate polemiza.\n\nFiecare punct de mai jos este obligatoriu.\n- Raportul listează fiecare intrare `lint_exempt:` și `gate_exempt:` din corpus, împreună cu\n  cerința sa și verificarea pe care o reduce la tăcere.\n- Un motiv este considerat consemnat atunci când proza cerinței însăși menționează pe nume\n  verificarea redusă la tăcere. Testul nu poate judeca dacă motivul este unul bun și nici nu\n  încearcă: face ca derogarea să coste o frază, nimic mai mult.\n- Poarta avertizează o dată pentru fiecare derogare nejustificată. Constatarea este doar de\n  tip avertisment și nu este niciodată promovată sub `--strict` — scopul este ca reducerea la\n  tăcere să coste ceva, nu ca ea să devină imposibilă.\n- Raportul indică `clarify <ID> --decompose` drept ceea ce trebuie făcut în schimb cu o\n  cerință supradimensionată, iar constatările `ac-count-high` și `over-scoped` îl indică și ele.",
          "acceptance": "CASE-1 — o derogare cu un motiv scris este curată\n  Given  o cerință care poartă `lint_exempt: [file-spread]` a cărei proză explică de ce\n  When   rulează poarta\n  Then   nu se emite niciun avertisment de derogare pentru ea\n\nCASE-2 — o derogare neexplicată este un avertisment\n  Given  o cerință care poartă `lint_exempt: [ac-count-high]` și nu o mai menționează niciodată\n  When   rulează poarta\n  Then   un avertisment numește cerința, câmpul și verificarea redusă la tăcere\n\nCASE-3 — avertismentul de derogare nu face niciodată să eșueze o construcție\n  Given  un corpus a cărui singură constatare este o derogare nejustificată\n  When   rulează `gate --strict`\n  Then   constatarea este în continuare un avertisment, iar rularea se încheie tot cu 0\n\nCASE-4 — constatările privind împărțirea numesc unealta care împarte\n  Given  o cerință cu mai multe criterii de acceptare decât plafonul\n  When   linterul o raportează\n  Then   constatarea numește `clarify <ID> --decompose`"
        }
      }
    },
    {
      "id": "REQ-AUDIT-972",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Whether the corpus has a shape at all",
      "intent": "`level:` ships commented out in the template, so an existing corpus keeps its behaviour and a new one never gains the axis by itself. The consequence nobody was told about: a repo can run the engine for months with every requirement on one rung, and every surface that reads a level quietly defaults it. The report says so once, and says what the three rungs are, because a reader who has never seen the pyramid cannot ask for it.",
      "contract": [
        "The report states how many requirements declare a `level:`, how they spread across the rungs, and how many `satisfies:` edges exist.",
        "A corpus where fewer than a tenth of requirements declare a level is called flat, and the report then names the three rungs and the `satisfies:` edge that builds the pyramid.",
        "Nothing about this fails or warns in the gate. Adopting a level axis is a decision, and a corpus that has decided otherwise is not defective."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a levelled corpus is described, not advised Given  a corpus where every requirement declares a level When   `audit` runs Then   the counts per rung are printed and no flat-corpus advice appears",
        "CASE-2 — — a flat corpus is named as flat Given  a corpus where no requirement declares a level When   `audit` runs Then   the report says the corpus is flat and names the three rungs",
        "CASE-3 — — the shape never reaches the exit code Given  a flat corpus with a clean gate When   `audit` runs Then   it exits 0 --------------------"
      ],
      "accept": "CASE-1 — a levelled corpus is described, not advised\n  Given  a corpus where every requirement declares a level\n  When   `audit` runs\n  Then   the counts per rung are printed and no flat-corpus advice appears\n\nCASE-2 — a flat corpus is named as flat\n  Given  a corpus where no requirement declares a level\n  When   `audit` runs\n  Then   the report says the corpus is flat and names the three rungs\n\nCASE-3 — the shape never reaches the exit code\n  Given  a flat corpus with a clean gate\n  When   `audit` runs\n  Then   it exits 0\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-AUDIT-065"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5167"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8049"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Dacă corpusul are sau nu o formă",
          "intent": "`level:` este livrat comentat în șablon, astfel încât un corpus existent își păstrează comportamentul, iar unul nou nu dobândește niciodată axa de la sine. Consecința despre care nimeni nu a fost înștiințat: un repo poate rula motorul luni de zile cu fiecare cerință pe aceeași treaptă, iar fiecare suprafață care citește un nivel îi atribuie tacit valoarea implicită. Raportul spune asta o dată și spune care sunt cele trei trepte, pentru că un cititor care nu a văzut niciodată piramida nu o poate cere.",
          "contract": "> `level:` este livrat comentat în șablon, astfel încât un corpus existent își păstrează\n> comportamentul, iar unul nou nu dobândește niciodată axa de la sine. Consecința despre care\n> nimeni nu a fost înștiințat: un repo poate rula motorul luni de zile cu fiecare cerință pe\n> aceeași treaptă, iar fiecare suprafață care citește un nivel îi atribuie tacit valoarea\n> implicită. Raportul spune asta o dată și spune care sunt cele trei trepte, pentru că un\n> cititor care nu a văzut niciodată piramida nu o poate cere.\n\nFiecare punct de mai jos este obligatoriu.\n- Raportul indică câte cerințe declară un `level:`, cum se distribuie acestea pe\n  trepte și câte muchii `satisfies:` există.\n- Un corpus în care mai puțin de o zecime dintre cerințe declară un nivel este numit plat, iar\n  raportul numește atunci cele trei trepte și muchia `satisfies:` care construiește piramida.\n- Nimic din toate acestea nu eșuează și nu avertizează în gate. Adoptarea unei axe de nivel este o decizie, iar\n  un corpus care a decis altfel nu este defect.",
          "acceptance": "CASE-1 — un corpus cu niveluri este descris, nu consiliat\n  Given  un corpus în care fiecare cerință declară un nivel\n  When   rulează `audit`\n  Then   sunt afișate numărătorile pe trepte și nu apare niciun sfat privind corpusul plat\n\nCASE-2 — un corpus plat este numit ca fiind plat\n  Given  un corpus în care nicio cerință nu declară un nivel\n  When   rulează `audit`\n  Then   raportul spune că acest corpus este plat și numește cele trei trepte\n\nCASE-3 — forma nu ajunge niciodată la codul de ieșire\n  Given  un corpus plat cu un gate curat\n  When   rulează `audit`\n  Then   iese cu 0"
        }
      }
    },
    {
      "id": "REQ-AUDIT-973",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Sync says what it found",
      "intent": "`sync` is the moment the corpus was just rewritten, and until now it reported only what it regenerated. A repo could sync for months without ever meeting the duplicate scan, the design review, the exemption list, or the fact that its TODO file stopped tracking what shipped. Sync now ends with one line per signal that is not clean, and nothing at all when everything is.",
      "contract": [
        "After a successful `sync`, one line is printed for each signal that is not clean: unjustified exemptions, a flat corpus, design candidates, untagged code, and a TODO file whose newest milestone is behind the requirements'.",
        "A repo where every one of those is clean sees no extra output, so a line that does appear is news rather than noise.",
        "The tail points at `audit` for the full report, and changes neither what `sync` writes nor its exit code."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a signal that is not clean is named Given  a corpus with an unjustified exemption When   `sync` completes Then   its output names that signal and points at `audit`",
        "CASE-2 — — a clean corpus stays quiet Given  a corpus where every audited signal is clean When   `sync` completes Then   no advisory tail is printed",
        "CASE-3 — — the tail is report-only Given  any corpus When   `sync` completes Then   the exit code and the files written are exactly what they were without the tail"
      ],
      "accept": "CASE-1 — a signal that is not clean is named\n  Given  a corpus with an unjustified exemption\n  When   `sync` completes\n  Then   its output names that signal and points at `audit`\n\nCASE-2 — a clean corpus stays quiet\n  Given  a corpus where every audited signal is clean\n  When   `sync` completes\n  Then   no advisory tail is printed\n\nCASE-3 — the tail is report-only\n  Given  any corpus\n  When   `sync` completes\n  Then   the exit code and the files written are exactly what they were without the tail",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-AUDIT-065"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5478"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:9318"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8049"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Sync spune ce a găsit",
          "intent": "`sync` este momentul în care corpusul tocmai a fost rescris, iar până acum raporta doar ce a regenerat. Un repo putea rula sync luni întregi fără să întâlnească vreodată scanarea de duplicate, revizuirea de design, lista de excepții sau faptul că fișierul său TODO a încetat să mai urmărească ce a fost livrat. Sync se încheie acum cu o linie pentru fiecare semnal care nu este curat, și cu absolut nimic atunci când totul este curat.",
          "contract": "> `sync` este momentul în care corpusul tocmai a fost rescris, iar până acum raporta doar ce\n> a regenerat. Un repo putea rula sync luni întregi fără să întâlnească vreodată scanarea de\n> duplicate, revizuirea de design, lista de excepții sau faptul că fișierul său TODO a încetat\n> să mai urmărească ce a fost livrat. Sync se încheie acum cu o linie pentru fiecare semnal\n> care nu este curat, și cu absolut nimic atunci când totul este curat.\n\nFiecare punct de mai jos este obligatoriu.\n- După un `sync` reușit, se afișează câte o linie pentru fiecare semnal care nu este curat:\n  excepții nejustificate, un corpus plat, candidați de design, cod fără etichete și un fișier TODO\n  al cărui cel mai recent milestone este în urma celui al cerințelor.\n- Un repo în care fiecare dintre acestea este curat nu vede nicio ieșire suplimentară, astfel încât o linie care chiar apare\n  este o noutate, nu zgomot.\n- Coada indică spre `audit` pentru raportul complet și nu schimbă nici ce scrie `sync`,\n  nici codul său de ieșire.",
          "acceptance": "CASE-1 — un semnal care nu este curat este numit\n  Given  un corpus cu o excepție nejustificată\n  When   `sync` se finalizează\n  Then   ieșirea sa numește acel semnal și indică spre `audit`\n\nCASE-2 — un corpus curat rămâne tăcut\n  Given  un corpus în care fiecare semnal auditat este curat\n  When   `sync` se finalizează\n  Then   nu se afișează nicio coadă consultativă\n\nCASE-3 — coada este doar de raportare\n  Given  orice corpus\n  When   `sync` se finalizează\n  Then   codul de ieșire și fișierele scrise sunt exact cele care erau fără coadă"
        }
      }
    },
    {
      "id": "ARCH-CANDIDATES-009",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Capability candidates (extraction plan)",
      "intent": "When you point this tool at an old codebase that has no requirements written down yet, this is the first, look-but-don't-touch step. It reads the code and produces a tidy machine-readable plan that guesses what each capability is — which files belong together, what they depend on, how central each one is — without writing or changing a single requirement file. An author (or an AI assistant) then uses that plan to decide what to actually write up. Without it, you would face a wall of untagged code with no starting map.",
      "contract": [
        "`draft --plan` emits a single JSON object, to stdout or to `--out PATH`, shaped `{engine_version, bus[], candidates[]}`, and writes no `.md` files. [[REQ-CANDIDATES-826]]",
        "Each candidate carries `{suggested_id, suggested_layer, files[], docstrings{}, signatures[], imports[], depends_on[], tested_by[], importer_count, existing_req, loc, split_candidate, is_test}`. [[REQ-CANDIDATES-827]]"
      ],
      "verify": [],
      "notes": [
        "Read-only by design — merge/split judgment is left to the Stage-2 authoring agent.",
        "Import→candidate resolution matches on file stem, so a stdlib-shadowing or same-basename import can produce a false `depends_on` edge for the author to prune."
      ],
      "current_impl": [
        "`cmd_candidates` and the `_py_facts`/`_js_facts`/`_file_facts`/`_load_capmap`/`_collect_files` helpers in `reqmap.py`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  any corpus When   `draft --plan` runs Then   it writes zero `.md` files and emits valid JSON",
        "CASE-2 — Given  a file listed in `.reqmapignore` (including one placed in `requirements/`) When   `draft --plan` runs Then   that file is absent from every candidate's `files`",
        "CASE-3 — Given  an import of a local module When   `draft --plan` runs Then   a `depends_on` edge points at that module's candidate",
        "CASE-4 — Given  a `requirements/_capmap.json` grouping two files under one id When   `draft --plan` runs Then   they appear as a single candidate carrying both files with the declared layer",
        "CASE-5 — Given  a file already carrying an `implements:` tag When   `draft --plan` runs Then   it is reported via `existing_req`",
        "CASE-6 — Given  a module imported by `BUS_FANIN_THRESHOLD` or more candidates When   `draft --plan` runs Then   it is suggested as `bus`",
        "CASE-7 — Given  a `.go` file and a `tests/test_x.py` file, and no `_capmap.json` When   `draft --plan` runs Then   both are candidates; the `.go` one carries empty signatures and the test one carries `is_test: true`"
      ],
      "accept": "CASE-1\n  Given  any corpus\n  When   `draft --plan` runs\n  Then   it writes zero `.md` files and emits valid JSON\n\nCASE-2\n  Given  a file listed in `.reqmapignore` (including one placed in `requirements/`)\n  When   `draft --plan` runs\n  Then   that file is absent from every candidate's `files`\n\nCASE-3\n  Given  an import of a local module\n  When   `draft --plan` runs\n  Then   a `depends_on` edge points at that module's candidate\n\nCASE-4\n  Given  a `requirements/_capmap.json` grouping two files under one id\n  When   `draft --plan` runs\n  Then   they appear as a single candidate carrying both files with the declared layer\n\nCASE-5\n  Given  a file already carrying an `implements:` tag\n  When   `draft --plan` runs\n  Then   it is reported via `existing_req`\n\nCASE-6\n  Given  a module imported by `BUS_FANIN_THRESHOLD` or more candidates\n  When   `draft --plan` runs\n  Then   it is suggested as `bus`\n\nCASE-7\n  Given  a `.go` file and a `tests/test_x.py` file, and no `_capmap.json`\n  When   `draft --plan` runs\n  Then   both are candidates; the `.go` one carries empty signatures and the test one carries `is_test: true`",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-SCAN-002"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-READ-103"
      ],
      "satisfied_by": [
        "REQ-CANDIDATES-826",
        "REQ-CANDIDATES-827"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3518"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3528"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3569"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3592"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3615"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3634"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3653"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3659"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3684"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1201"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1504"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1837"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1859"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1946"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2809"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6843"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9610"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.06",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Candidați de capabilitate (plan de extracție)",
          "intent": "Când îndrepți acest instrument spre o bază de cod veche care nu are încă nicio cerință scrisă, acesta este primul pas, de tip privește-dar-nu-atinge. Citește codul și produce un plan ordonat, citibil de mașină, care ghicește ce este fiecare capabilitate — ce fișiere aparțin împreună, de ce depind, cât de centrală este fiecare — fără a scrie sau modifica vreun fișier de cerință. Un autor (sau un asistent AI) folosește apoi acel plan pentru a decide ce să scrie de fapt. Fără el, te-ai confrunta cu un zid de cod neetichetat, fără nicio hartă de pornire.",
          "contract": "> Când îndrepți acest instrument spre o bază de cod veche care nu are încă nicio cerință\n> scrisă, acesta este primul pas, de tip privește-dar-nu-atinge. Citește codul și produce\n> un plan ordonat, citibil de mașină, care ghicește ce este fiecare capabilitate — ce\n> fișiere aparțin împreună, de ce depind, cât de centrală este fiecare — fără a scrie sau\n> modifica vreun fișier de cerință. Un autor (sau un asistent AI) folosește apoi acel plan\n> pentru a decide ce să scrie de fapt. Fără el, te-ai confrunta cu un zid de cod neetichetat,\n> fără nicio hartă de pornire.\n\nFiecare punct de mai jos este obligatoriu.\n- `draft --plan` emite un singur obiect JSON, către stdout sau către `--out PATH`, cu forma `{engine_version, bus[], candidates[]}`, și nu scrie niciun fișier `.md`. [[REQ-CANDIDATES-826]]\n- Fiecare candidat poartă `{suggested_id, suggested_layer, files[], docstrings{}, signatures[], imports[], depends_on[], tested_by[], importer_count, existing_req, loc, split_candidate, is_test}`. [[REQ-CANDIDATES-827]]",
          "acceptance": "CASE-1\n  Given  orice corpus\n  When   `draft --plan` rulează\n  Then   scrie zero fișiere `.md` și emite JSON valid\n\nCASE-2\n  Given  un fișier listat în `.reqmapignore` (inclusiv unul plasat în `requirements/`)\n  When   `draft --plan` rulează\n  Then   acel fișier lipsește din `files` al fiecărui candidat\n\nCASE-3\n  Given  un import al unui modul local\n  When   `draft --plan` rulează\n  Then   o muchie `depends_on` indică spre candidatul acelui modul\n\nCASE-4\n  Given  un `requirements/_capmap.json` care grupează două fișiere sub un singur id\n  When   `draft --plan` rulează\n  Then   ele apar ca un singur candidat care poartă ambele fișiere cu layer-ul declarat\n\nCASE-5\n  Given  un fișier care poartă deja o etichetă `implements:`\n  When   `draft --plan` rulează\n  Then   este raportat prin `existing_req`\n\nCASE-6\n  Given  un modul importat de `BUS_FANIN_THRESHOLD` sau mai mulți candidați\n  When   `draft --plan` rulează\n  Then   este sugerat ca `bus`\n\nCASE-7\n  Given  un fișier `.go` și un fișier `tests/test_x.py`, și niciun `_capmap.json`\n  When   `draft --plan` rulează\n  Then   ambele sunt candidați; cel `.go` poartă semnături goale, iar cel de test poartă `is_test: true`"
        }
      }
    },
    {
      "id": "REQ-CANDIDATES-826",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The plan's JSON shape and read-only scanning",
      "intent": "Everything else in the pipeline — `draft`, `review`, `confirm` — writes files. `draft --plan` deliberately does not: it is the look-before-you-leap step for an untagged codebase, so a wrong or half-formed plan can be thrown away with zero cleanup. It walks the code with the same exclusions as scanning, so it never lists a file a real scan would skip.",
      "contract": [
        "`draft --plan` emits a single JSON object, to stdout or to `--out PATH`, shaped `{engine_version, bus[], candidates[]}`.",
        "`draft --plan` writes NO `.md` files. It cannot repeat `draft`'s empty-stub failure.",
        "`draft --plan` walks the code with the same exclusions as scanning: noise dirs, the SSOT dir, and `.reqmapignore` resolved in `requirements/` first.",
        "`draft --plan` gathers per-file facts: module and symbol docstrings, top-level signatures, import targets, and line count.",
        "`draft --plan` lists every scannable code file as a candidate, the same set `draft` walks. A file in a language `draft --plan` cannot parse is still a candidate, with empty facts.",
        "`draft --plan` reads top-level signatures from Python via `ast` — functions, classes and the public methods of each class — and from JS/TS via best-effort parsing.",
        "An unparseable file yields empty facts. It never aborts the plan."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — plan emits one JSON object with the documented shape Given  a small codebase When   `draft --plan` runs with no `--out` Then   stdout parses as one JSON object carrying `engine_version`, `bus` and `candidates`",
        "CASE-2 — — plan writes zero .md files Given  an untagged codebase When   `plan --out plan.json` runs Then   no `.md` file appears anywhere in the requirements directory",
        "CASE-3 — — plan skips the same paths scanning skips Given  a file listed in `.reqmapignore` and a file inside `node_modules` When   `draft --plan` runs Then   neither file appears in any candidate's `files`",
        "CASE-4 — — per-file facts capture docstrings, signatures, imports and size Given  a Python file with a module docstring, one function, and one import When   `draft --plan` runs Then   that file's candidate carries the docstring, the function signature, the import, and a line count",
        "CASE-5 — — an unparseable file still becomes a candidate Given  a `.go` file among the scanned files When   `draft --plan` runs Then   that file appears as a candidate with empty facts, not omitted",
        "CASE-6 — — Python signatures come from ast, not text matching Given  a Python file defining a class with one public and one `_private` method When   `draft --plan` reads its signatures Then   the public method appears in the candidate's `signatures` and the private one does not",
        "CASE-7 — — an unparseable file never aborts the run Given  a codebase containing one syntactically broken Python file among valid ones When   `draft --plan` runs Then   it completes and emits candidates for the valid files, with empty facts for the broken one --------------------"
      ],
      "accept": "CASE-1 — plan emits one JSON object with the documented shape\n  Given  a small codebase\n  When   `draft --plan` runs with no `--out`\n  Then   stdout parses as one JSON object carrying `engine_version`, `bus` and `candidates`\n\nCASE-2 — plan writes zero .md files\n  Given  an untagged codebase\n  When   `plan --out plan.json` runs\n  Then   no `.md` file appears anywhere in the requirements directory\n\nCASE-3 — plan skips the same paths scanning skips\n  Given  a file listed in `.reqmapignore` and a file inside `node_modules`\n  When   `draft --plan` runs\n  Then   neither file appears in any candidate's `files`\n\nCASE-4 — per-file facts capture docstrings, signatures, imports and size\n  Given  a Python file with a module docstring, one function, and one import\n  When   `draft --plan` runs\n  Then   that file's candidate carries the docstring, the function signature, the import, and a line count\n\nCASE-5 — an unparseable file still becomes a candidate\n  Given  a `.go` file among the scanned files\n  When   `draft --plan` runs\n  Then   that file appears as a candidate with empty facts, not omitted\n\nCASE-6 — Python signatures come from ast, not text matching\n  Given  a Python file defining a class with one public and one `_private` method\n  When   `draft --plan` reads its signatures\n  Then   the public method appears in the candidate's `signatures` and the private one does not\n\nCASE-7 — an unparseable file never aborts the run\n  Given  a codebase containing one syntactically broken Python file among valid ones\n  When   `draft --plan` runs\n  Then   it completes and emits candidates for the valid files, with empty facts for the broken one\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-CANDIDATES-009"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3528"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3569"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3615"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3659"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3684"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1201"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2809"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6843"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 7,
      "covered": 7,
      "i18n": {
        "ro": {
          "title": "Forma JSON a planului și scanarea read-only",
          "intent": "Tot restul din pipeline — `draft`, `review`, `confirm` — scrie fișiere. `draft --plan` în mod deliberat nu scrie: este pasul de \"privește înainte de a sări\" pentru o bază de cod neetichetată, astfel încât un plan greșit sau pe jumătate format poate fi aruncat cu zero curățenie. Parcurge codul cu aceleași excluderi ca scanarea, deci nu listează niciodată un fișier pe care o scanare reală l-ar sări.",
          "contract": "> Tot restul din pipeline — `draft`, `review`, `confirm` — scrie fișiere. `draft --plan`\n> în mod deliberat nu scrie: este pasul de \"privește înainte de a sări\" pentru o bază de cod\n> neetichetată, astfel încât un plan greșit sau pe jumătate format poate fi aruncat cu zero\n> curățenie. Parcurge codul cu aceleași excluderi ca scanarea, deci nu listează niciodată un\n> fișier pe care o scanare reală l-ar sări.\n\nFiecare punct de mai jos este obligatoriu.\n- `draft --plan` emite un singur obiect JSON, la stdout sau la `--out PATH`, cu forma\n  `{engine_version, bus[], candidates[]}`.\n- `draft --plan` NU scrie fișiere `.md`. Nu poate repeta eșecul de stub gol al lui `draft`.\n- `draft --plan` parcurge codul cu aceleași excluderi ca scanarea: directoare de zgomot, directorul SSOT,\n  și `.reqmapignore` rezolvat mai întâi în `requirements/`.\n- `draft --plan` adună fapte per fișier: docstring-uri de modul și simbol, semnături de nivel superior,\n  ținte de import și numărul de linii.\n- `draft --plan` listează fiecare fișier de cod scanabil drept candidat, același set pe care `draft` îl parcurge.\n  Un fișier într-un limbaj pe care `draft --plan` nu îl poate parsa este tot un candidat, cu fapte goale.\n- `draft --plan` citește semnăturile de nivel superior din Python via `ast` — funcții, clase și\n  metodele publice ale fiecărei clase — și din JS/TS via parsare best-effort.\n- Un fișier neparsabil produce fapte goale. Nu abandonează niciodată planul.",
          "acceptance": "CASE-1 — planul emite un obiect JSON cu forma documentată\n  Given  o bază de cod mică\n  When   `draft --plan` rulează fără `--out`\n  Then   stdout se parsează ca un obiect JSON purtând `engine_version`, `bus` și `candidates`\n\nCASE-2 — planul scrie zero fișiere .md\n  Given  o bază de cod neetichetată\n  When   `plan --out plan.json` rulează\n  Then   niciun fișier `.md` nu apare nicăieri în directorul requirements\n\nCASE-3 — planul sare aceleași căi pe care le sare scanarea\n  Given  un fișier listat în `.reqmapignore` și un fișier în interiorul `node_modules`\n  When   `draft --plan` rulează\n  Then   niciun fișier nu apare în `files`-ul vreunui candidat\n\nCASE-4 — faptele per fișier captează docstring-uri, semnături, importuri și dimensiune\n  Given  un fișier Python cu un docstring de modul, o funcție și un import\n  When   `draft --plan` rulează\n  Then   candidatul acelui fișier poartă docstring-ul, semnătura funcției, importul și un număr de linii\n\nCASE-5 — un fișier neparsabil tot devine candidat\n  Given  un fișier `.go` printre fișierele scanate\n  When   `draft --plan` rulează\n  Then   acel fișier apare ca un candidat cu fapte goale, nu este omis\n\nCASE-6 — semnăturile Python provin din ast, nu din potrivire de text\n  Given  un fișier Python care definește o clasă cu o metodă publică și una `_private`\n  When   `draft --plan` citește semnăturile ei\n  Then   metoda publică apare în `signatures`-ul candidatului iar cea privată nu\n\nCASE-7 — un fișier neparsabil nu abandonează niciodată rularea\n  Given  o bază de cod conținând un fișier Python sintactic stricat printre altele valide\n  When   `draft --plan` rulează\n  Then   se termină și emite candidați pentru fișierele valide, cu fapte goale pentru cel stricat\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-CANDIDATES-827",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Fields a candidate carries",
      "intent": "A candidate's fields are the raw material a human or an AI author turns into a real requirement: what files it groups, what it imports, how central it is, and whether someone already tagged it. `is_test`, `existing_req` and `importer_count` exist so the author does not have to re-derive them by hand from the files themselves.",
      "contract": [
        "Each candidate carries `{suggested_id, suggested_layer, files[], docstrings{}, signatures[], imports[], depends_on[], tested_by[], importer_count, existing_req, loc, split_candidate, is_test}`.",
        "`is_test` is true when every file of the candidate is test code by convention: a `tests/`-style directory segment, a `test_*` basename, or a `*_test`/`*.spec` suffix.",
        "`depends_on` is derived from imports resolved to other candidates.",
        "`suggested_layer` is `bus` when `importer_count ≥ BUS_FANIN_THRESHOLD`, else `feature`.",
        "A file already carrying an `implements:` tag is reported via `existing_req`.",
        "`draft --plan` groups files by `requirements/_capmap.json` when that file is present, and treats it as authoritative.",
        "Absent `_capmap.json`, `draft --plan` falls back to one candidate per file."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — every candidate carries the full documented field set Given  any file scanned by `draft --plan` When   its candidate is built Then   the candidate dict carries all of `suggested_id`, `suggested_layer`, `files`, `docstrings`, `signatures`, `imports`, `depends_on`, `tested_by`, `importer_count`, `existing_req`, `loc`, `split_candidate` and `is_test`",
        "CASE-2 — — is_test follows test-file convention, not content Given  a file at `tests/test_foo.py` alongside an ordinary module When   `draft --plan` runs Then   the test file's candidate carries `is_test: true` and the ordinary module's does not",
        "CASE-3 — — an import edge becomes a depends_on edge Given  file `a.py` importing local module `b.py` When   `draft --plan` runs Then   `a.py`'s candidate lists `b.py`'s candidate in `depends_on`",
        "CASE-4 — — high fan-in earns a bus suggestion Given  a module imported by at least `BUS_FANIN_THRESHOLD` other candidates When   `draft --plan` runs Then   that module's candidate carries `suggested_layer: \"bus\"`",
        "CASE-5 — — an already-tagged file reports its existing requirement Given  a file carrying `# implements: ARCH-EXAMPLE-001` When   `draft --plan` runs Then   that file's candidate carries `existing_req: \"ARCH-EXAMPLE-001\"`",
        "CASE-6 — — _capmap.json groups files into one candidate Given  a `requirements/_capmap.json` grouping two files under one id When   `draft --plan` runs Then   both files appear together as one candidate carrying the declared layer",
        "CASE-7 — — no capmap means one candidate per file Given  a codebase with no `requirements/_capmap.json` When   `draft --plan` runs Then   every file becomes its own separate candidate"
      ],
      "accept": "CASE-1 — every candidate carries the full documented field set\n  Given  any file scanned by `draft --plan`\n  When   its candidate is built\n  Then   the candidate dict carries all of `suggested_id`, `suggested_layer`, `files`, `docstrings`, `signatures`, `imports`, `depends_on`, `tested_by`, `importer_count`, `existing_req`, `loc`, `split_candidate` and `is_test`\n\nCASE-2 — is_test follows test-file convention, not content\n  Given  a file at `tests/test_foo.py` alongside an ordinary module\n  When   `draft --plan` runs\n  Then   the test file's candidate carries `is_test: true` and the ordinary module's does not\n\nCASE-3 — an import edge becomes a depends_on edge\n  Given  file `a.py` importing local module `b.py`\n  When   `draft --plan` runs\n  Then   `a.py`'s candidate lists `b.py`'s candidate in `depends_on`\n\nCASE-4 — high fan-in earns a bus suggestion\n  Given  a module imported by at least `BUS_FANIN_THRESHOLD` other candidates\n  When   `draft --plan` runs\n  Then   that module's candidate carries `suggested_layer: \"bus\"`\n\nCASE-5 — an already-tagged file reports its existing requirement\n  Given  a file carrying `# implements: ARCH-EXAMPLE-001`\n  When   `draft --plan` runs\n  Then   that file's candidate carries `existing_req: \"ARCH-EXAMPLE-001\"`\n\nCASE-6 — _capmap.json groups files into one candidate\n  Given  a `requirements/_capmap.json` grouping two files under one id\n  When   `draft --plan` runs\n  Then   both files appear together as one candidate carrying the declared layer\n\nCASE-7 — no capmap means one candidate per file\n  Given  a codebase with no `requirements/_capmap.json`\n  When   `draft --plan` runs\n  Then   every file becomes its own separate candidate",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-CANDIDATES-009"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3518"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3634"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3653"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3754"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3769"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1201"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1859"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6843"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9610"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 7,
      "covered": 7,
      "i18n": {
        "ro": {
          "title": "Câmpurile pe care le poartă un candidat",
          "intent": "Câmpurile unui candidat sunt materia primă pe care un autor uman sau AI o transformă într-o cerință reală: ce fișiere grupează, ce importă, cât de central este și dacă cineva l-a etichetat deja. `is_test`, `existing_req` și `importer_count` există astfel încât autorul să nu trebuiască să le re-deducă manual din fișierele înseși.",
          "contract": "> Câmpurile unui candidat sunt materia primă pe care un autor uman sau AI o transformă într-o\n> cerință reală: ce fișiere grupează, ce importă, cât de central este și dacă cineva l-a\n> etichetat deja. `is_test`, `existing_req` și `importer_count` există astfel încât autorul\n> să nu trebuiască să le re-deducă manual din fișierele înseși.\n\nFiecare punct de mai jos este obligatoriu.\n- Fiecare candidat poartă `{suggested_id, suggested_layer, files[], docstrings{},\n  signatures[], imports[], depends_on[], tested_by[], importer_count, existing_req, loc,\n  split_candidate, is_test}`.\n- `is_test` este true când fiecare fișier al candidatului este cod de test prin convenție: un\n  segment de director în stil `tests/`, un nume de bază `test_*`, sau un sufix `*_test`/`*.spec`.\n- `depends_on` este derivat din importuri rezolvate către alți candidați.\n- `suggested_layer` este `bus` când `importer_count ≥ BUS_FANIN_THRESHOLD`, altfel `feature`.\n- Un fișier care poartă deja o etichetă `implements:` este raportat via `existing_req`.\n- `draft --plan` grupează fișierele după `requirements/_capmap.json` când acel fișier este prezent, și\n  îl tratează ca autoritativ.\n- În absența `_capmap.json`, `draft --plan` revine la câte un candidat per fișier.",
          "acceptance": "CASE-1 — fiecare candidat poartă setul complet de câmpuri documentat\n  Given  orice fișier scanat de `draft --plan`\n  When   candidatul lui este construit\n  Then   dicționarul candidatului poartă toate câmpurile `suggested_id`, `suggested_layer`, `files`, `docstrings`, `signatures`, `imports`, `depends_on`, `tested_by`, `importer_count`, `existing_req`, `loc`, `split_candidate` și `is_test`\n\nCASE-2 — is_test urmează convenția fișierelor de test, nu conținutul\n  Given  un fișier la `tests/test_foo.py` alături de un modul obișnuit\n  When   `draft --plan` rulează\n  Then   candidatul fișierului de test poartă `is_test: true` iar cel al modulului obișnuit nu\n\nCASE-3 — o muchie de import devine o muchie depends_on\n  Given  fișierul `a.py` care importă modulul local `b.py`\n  When   `draft --plan` rulează\n  Then   candidatul lui `a.py` listează candidatul lui `b.py` în `depends_on`\n\nCASE-4 — fan-in ridicat câștigă o sugestie bus\n  Given  un modul importat de cel puțin `BUS_FANIN_THRESHOLD` alți candidați\n  When   `draft --plan` rulează\n  Then   candidatul acelui modul poartă `suggested_layer: \"bus\"`\n\nCASE-5 — un fișier deja etichetat raportează cerința sa existentă\n  Given  un fișier care poartă `# implements: ARCH-EXAMPLE-001`\n  When   `draft --plan` rulează\n  Then   candidatul acelui fișier poartă `existing_req: \"ARCH-EXAMPLE-001\"`\n\nCASE-6 — _capmap.json grupează fișiere într-un candidat\n  Given  un `requirements/_capmap.json` care grupează două fișiere sub un id\n  When   `draft --plan` rulează\n  Then   ambele fișiere apar împreună ca un candidat care poartă layer-ul declarat\n\nCASE-7 — fără capmap înseamnă câte un candidat per fișier\n  Given  o bază de cod fără `requirements/_capmap.json`\n  When   `draft --plan` rulează\n  Then   fiecare fișier devine propriul său candidat separat"
        }
      }
    },
    {
      "id": "ARCH-CHECK-006",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "The gate",
      "intent": "Specs rot when the code moves on and nobody updates the file that describes it. This is the guard that catches that: run before every commit, it fails the build when code and requirements no longer match. The whole tool exists so this check can run.",
      "contract": [
        "`gate` reports an `ERROR` and exits non-zero for a dangling tag, an invalid status/layer/form/level, a missing `depends_on` target, or an enforced requirement with no `implements:` member. [[REQ-CHECK-828]] details the behaviour.",
        "`gate` warns (not errors) on contract drift against the lock, a confirmed requirement with no `tested-by:` link, or a confirmed requirement missing its `## Description`/`## Cases` section; `--strict` promotes most of these to errors. [[REQ-CHECK-829]] details the behaviour.",
        "`gate` warns on a malformed `milestone:` value, and on a corrupt or git-untracked lock file, without affecting the exit code. [[REQ-CHECK-830]] details the behaviour.",
        "`gate` counts legacy-schema requirements in its summary and warns, without affecting the exit code, on an unvalidated confirmed need, a bus requirement tested only at `@system`, or a `depends_on` cycle. [[REQ-CHECK-831]] details the behaviour.",
        "`gate` prints the open verify-intent finding count and a summary of requirements, members, errors and warnings; neither affects the exit code. [[REQ-CHECK-832]] details the behaviour.",
        "With `--update-lock` — always passed by `sync` — `gate` writes the current binding hashes to `requirements/_reqlock.json`; the bare `gate` verb is otherwise report-only. [[REQ-CHECK-833]] details the behaviour."
      ],
      "verify": [],
      "notes": [
        "Why `ac-count-high` and `over-scoped` are exempt: this requirement is the gate's severity table, not a bundle of checks. Each individual check the gate runs already has its own requirement ([[ARCH-TESTLINK-018]], [[ARCH-ACVERIFY-019]], [[ARCH-MEMBERDRIFT-027]], [[ARCH-DOCBUNDLE-026]], [[ARCH-ORPHANCODE-034]], [[ARCH-DRIFTIMPACT-035]]). What remains here is one behaviour — which condition is an ERROR, which is a WARN, and what that does to the exit code — so every clause and criterion shares a single failure mode. Splitting it would scatter the exit-code contract across files instead of clarifying it. Revisit this exemption if a clause ever lands here that is not about severity or exit code.",
        "Errors stop CI (exit 1); warnings do not. Intent sync (promote `baseline → confirmed`) is not automatable and surfaces at human review.",
        "When `CLAUDE_PLUGIN_ROOT` is set and the vendored engine is older than the installed plugin's copy, `gate` prints an advisory staleness notice (`warn_if_stale`); the variable is unset in CI, so the notice is silent and exit-neutral there. The CI half of that signal cannot live here — a stale engine does not contain the check that reports it stale — so it ships with the published action instead ([[ARCH-STALEENGINE-043]]).",
        "The dev-CI version-coherence script `scripts/check_versions.py` (repo root, never seeded into consumer repos) enforces plugin manifest alignment as the first step in this repo's own CI pipeline (`check_versions.py → gate → map --check → test_reqmap.py`). It is outside the plugin scan root and has no requirement of its own; its invariant (semver consistency across `plugin.json` + `marketplace.json`) is documented in `CLAUDE.md`."
      ],
      "current_impl": [
        "`cmd_check`, `warn_if_stale` in `reqmap.py`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a tag referencing a non-existent capability When   `gate` runs Then   it produces an `ERROR` and exit 1",
        "CASE-2 — Given  a `confirmed` requirement with no `implements` member When   `gate` runs Then   it produces an `ERROR`",
        "CASE-3 — Given  an invalid status or layer, or a `depends_on` pointing at a missing id When   `gate` runs Then   each produces an `ERROR`",
        "CASE-4 — Given  a `confirmed` requirement whose binding hash differs from the lock When   `gate` runs Then   it produces a `WARN` (not an error) naming the member `file:line` locations",
        "CASE-5 — Given  a present-but-corrupt lock file When   `gate` runs Then   it produces a `WARN` and does not change the exit code",
        "CASE-6 — Given  a requirement with a malformed `milestone:` (e.g. `next` or `1.14`) When   `gate` runs Then   it produces a `WARN`; a valid `v1.14`, an absent milestone, or a `deprecated` requirement produces none",
        "CASE-7 — Given  a `confirmed` requirement with no `## Description` section When   `gate` runs Then   it produces a `WARN` and does not affect the exit code",
        "CASE-8 — Given  a `confirmed` requirement with no `## Cases` section When   `gate` runs Then   it produces a `WARN` and does not affect the exit code",
        "CASE-9 — Given  a `confirmed` requirement with both sections present When   `gate` runs Then   it produces no section-lint warning",
        "CASE-10 — Given  a `confirmed` requirement with a `test_exempt: <reason>` and no `tested-by` member When   `gate` runs Then   it produces no test warning",
        "CASE-11 — Given  a requirement without a `## Verify intent` section When   `gate` runs Then   it is counted as legacy-schema in the summary",
        "CASE-12 — Given  an advancing run (`sync`, or the deprecated `check --update-lock`) When   it runs Then   the current hashes are written to `requirements/_reqlock.json`",
        "CASE-13 — Given  a `_reqlock.json` (or `_memberlock.json`) present on disk but not git-tracked, inside a git work tree When   `gate` runs Then   it produces a `WARN` naming the file; once the file is tracked, or when run outside a git work tree, it produces none",
        "CASE-14 — Given  two requirements whose `depends_on` fields point at each other When   the gate runs Then   it warns once, naming the chain, and the exit code stays 0"
      ],
      "accept": "CASE-1\n  Given  a tag referencing a non-existent capability\n  When   `gate` runs\n  Then   it produces an `ERROR` and exit 1\n\nCASE-2\n  Given  a `confirmed` requirement with no `implements` member\n  When   `gate` runs\n  Then   it produces an `ERROR`\n\nCASE-3\n  Given  an invalid status or layer, or a `depends_on` pointing at a missing id\n  When   `gate` runs\n  Then   each produces an `ERROR`\n\nCASE-4\n  Given  a `confirmed` requirement whose binding hash differs from the lock\n  When   `gate` runs\n  Then   it produces a `WARN` (not an error) naming the member `file:line` locations\n\nCASE-5\n  Given  a present-but-corrupt lock file\n  When   `gate` runs\n  Then   it produces a `WARN` and does not change the exit code\n\nCASE-6\n  Given  a requirement with a malformed `milestone:` (e.g. `next` or `1.14`)\n  When   `gate` runs\n  Then   it produces a `WARN`; a valid `v1.14`, an absent milestone, or a `deprecated`\n         requirement produces none\n\nCASE-7\n  Given  a `confirmed` requirement with no `## Description` section\n  When   `gate` runs\n  Then   it produces a `WARN` and does not affect the exit code\n\nCASE-8\n  Given  a `confirmed` requirement with no `## Cases` section\n  When   `gate` runs\n  Then   it produces a `WARN` and does not affect the exit code\n\nCASE-9\n  Given  a `confirmed` requirement with both sections present\n  When   `gate` runs\n  Then   it produces no section-lint warning\n\nCASE-10\n  Given  a `confirmed` requirement with a `test_exempt: <reason>` and no `tested-by` member\n  When   `gate` runs\n  Then   it produces no test warning\n\nCASE-11\n  Given  a requirement without a `## Verify intent` section\n  When   `gate` runs\n  Then   it is counted as legacy-schema in the summary\n\nCASE-12\n  Given  an advancing run (`sync`, or the deprecated `check --update-lock`)\n  When   it runs\n  Then   the current hashes are written to `requirements/_reqlock.json`\n\nCASE-13\n  Given  a `_reqlock.json` (or `_memberlock.json`) present on disk but not git-tracked, inside a git work tree\n  When   `gate` runs\n  Then   it produces a `WARN` naming the file; once the file is tracked, or when run outside a git work tree, it produces none\n\nCASE-14\n  Given  two requirements whose `depends_on` fields point at each other\n  When   the gate runs\n  Then   it warns once, naming the chain, and the exit code stays 0",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001",
        "ARCH-SCAN-002",
        "ARCH-DRIFT-003"
      ],
      "used_by": [
        "ARCH-ACVERIFY-019",
        "ARCH-ATOMICFORM-053",
        "ARCH-AUDIT-065",
        "ARCH-DOCBUNDLE-026",
        "ARCH-DRIFTIMPACT-035",
        "ARCH-INIT-012",
        "ARCH-LEVEL-051",
        "ARCH-MEMBERDRIFT-027",
        "ARCH-ORPHANCODE-034",
        "ARCH-RULES-059",
        "ARCH-SELFGATE-039",
        "ARCH-STALEENGINE-043",
        "ARCH-TESTLINK-018",
        "ARCH-TRACE-020",
        "ARCH-TRACKED-042",
        "ARCH-UNSCANNEDTAG-045",
        "ARCH-VLEVEL-037",
        "ARCH-VRUNGS-054"
      ],
      "satisfies": [
        "SYS-GATE-102"
      ],
      "satisfied_by": [
        "REQ-CHECK-828",
        "REQ-CHECK-829",
        "REQ-CHECK-830",
        "REQ-CHECK-831",
        "REQ-CHECK-832",
        "REQ-CHECK-833"
      ],
      "members": [
        {
          "role": "generated-from",
          "loc": "docs/full_architecture.html:4"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1878"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2125"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2212"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2261"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2269"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2820"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2887"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5704"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7275"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:144"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:208"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:215"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:232"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:245"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:258"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1518"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1562"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2017"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4332"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4399"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4567"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:5890"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:5965"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6220"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6243"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6402"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9567"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.02",
      "priority": "",
      "risks": [],
      "clauses": 14,
      "covered": 14,
      "i18n": {
        "ro": {
          "title": "Gate-ul",
          "intent": "Specificațiile putrezesc atunci când codul evoluează și nimeni nu actualizează fișierul care îl descrie. Aceasta este garda care prinde acest lucru: rulată înainte de fiecare commit, ea face build-ul să eșueze atunci când codul și cerințele nu mai corespund. Întregul instrument există pentru ca această verificare să poată rula.",
          "contract": "> Specificațiile putrezesc atunci când codul evoluează și nimeni nu actualizează fișierul\n> care îl descrie. Aceasta este garda care prinde acest lucru: rulată înainte de fiecare\n> commit, ea face build-ul să eșueze atunci când codul și cerințele nu mai corespund.\n> Întregul instrument există pentru ca această verificare să poată rula.\n\nFiecare punct de mai jos este obligatoriu.\n- `gate` raportează un `ERROR` și iese cu cod diferit de zero pentru o etichetă suspendată, un status/layer/form/level invalid, o țintă `depends_on` lipsă, sau o cerință impusă fără niciun membru `implements:`. [[REQ-CHECK-828]] detaliază comportamentul.\n- `gate` avertizează (nu generează erori) pentru drift de contract față de lock, o cerință confirmată fără nicio legătură `tested-by:`, sau o cerință confirmată căreia îi lipsește secțiunea `## Description`/`## Cases`; `--strict` promovează majoritatea acestora la erori. [[REQ-CHECK-829]] detaliază comportamentul.\n- `gate` avertizează pentru o valoare `milestone:` malformată, și pentru un fișier lock corupt sau neurmărit de git, fără a afecta codul de ieșire. [[REQ-CHECK-830]] detaliază comportamentul.\n- `gate` numără cerințele cu schemă veche în rezumatul său și avertizează, fără a afecta codul de ieșire, pentru o nevoie confirmată nevalidată, o cerință bus testată doar la `@system`, sau un ciclu `depends_on`. [[REQ-CHECK-831]] detaliază comportamentul.\n- `gate` afișează numărul de constatări deschise verify-intent și un rezumat al cerințelor, membrilor, erorilor și avertismentelor; niciunul nu afectează codul de ieșire. [[REQ-CHECK-832]] detaliază comportamentul.\n- Cu `--update-lock` — transmis mereu de `sync` — `gate` scrie hash-urile obligatorii curente în `requirements/_reqlock.json`; verbul simplu `gate` este altfel doar de raportare. [[REQ-CHECK-833]] detaliază comportamentul.",
          "acceptance": "CASE-1\n  Given  o etichetă care referențiază o capabilitate inexistentă\n  When   `gate` rulează\n  Then   produce un `ERROR` și exit 1\n\nCASE-2\n  Given  o cerință `confirmed` fără niciun membru `implements`\n  When   `gate` rulează\n  Then   produce un `ERROR`\n\nCASE-3\n  Given  un status sau layer invalid, sau un `depends_on` care indică spre un id lipsă\n  When   `gate` rulează\n  Then   fiecare produce un `ERROR`\n\nCASE-4\n  Given  o cerință `confirmed` al cărei hash obligatoriu diferă de cel din lock\n  When   `gate` rulează\n  Then   produce un `WARN` (nu o eroare) care numește locațiile `file:line` ale membrului\n\nCASE-5\n  Given  un fișier lock prezent dar corupt\n  When   `gate` rulează\n  Then   produce un `WARN` și nu schimbă codul de ieșire\n\nCASE-6\n  Given  o cerință cu un `milestone:` malformat (de ex. `next` sau `1.14`)\n  When   `gate` rulează\n  Then   produce un `WARN`; un `v1.14` valid, un milestone absent, sau o cerință\n         `deprecated` nu produc niciunul\n\nCASE-7\n  Given  o cerință `confirmed` fără secțiunea `## Description`\n  When   `gate` rulează\n  Then   produce un `WARN` și nu afectează codul de ieșire\n\nCASE-8\n  Given  o cerință `confirmed` fără secțiunea `## Cases`\n  When   `gate` rulează\n  Then   produce un `WARN` și nu afectează codul de ieșire\n\nCASE-9\n  Given  o cerință `confirmed` cu ambele secțiuni prezente\n  When   `gate` rulează\n  Then   nu produce niciun avertisment de lint pentru secțiuni\n\nCASE-10\n  Given  o cerință `confirmed` cu `test_exempt: <reason>` și fără niciun membru `tested-by`\n  When   `gate` rulează\n  Then   nu produce niciun avertisment de test\n\nCASE-11\n  Given  o cerință fără secțiunea `## Verify intent`\n  When   `gate` rulează\n  Then   este numărată ca schemă veche în rezumat\n\nCASE-12\n  Given  o rulare care avansează baseline-ul (`sync`, sau varianta învechită `check --update-lock`)\n  When   rulează\n  Then   hash-urile curente sunt scrise în `requirements/_reqlock.json`\n\nCASE-13\n  Given  un `_reqlock.json` (sau `_memberlock.json`) prezent pe disc dar neurmărit de git, într-un work tree git\n  When   `gate` rulează\n  Then   produce un `WARN` care numește fișierul; odată ce fișierul este urmărit, sau când rulează în afara unui work tree git, nu produce niciunul\n\nCASE-14\n  Given  două cerințe ale căror câmpuri `depends_on` indică una spre cealaltă\n  When   gate-ul rulează\n  Then   avertizează o singură dată, numind lanțul, iar codul de ieșire rămâne 0"
        }
      }
    },
    {
      "id": "REQ-CHECK-828",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Gate errors that block a commit",
      "intent": "An `ERROR` is the gate's hard-stop signal: it exits the process non-zero and blocks CI. It is reserved for problems a human must fix before the requirement corpus can be trusted at all — a broken tag, an invalid enum value, or a requirement claiming code exists when no tag proves it.",
      "contract": [
        "`gate` reports an `ERROR` and exits non-zero for every condition in this group.",
        "A dangling tag — a code tag referencing a capability no requirement defines — is such a condition.",
        "An invalid `status` or an invalid `layer` is such a condition.",
        "A `depends_on` pointing at a missing id is such a condition.",
        "An enforced requirement with no `implements:` member is such a condition.",
        "A requirement is enforced when its status is `in-progress`, `implemented` or `confirmed`.",
        "A `layer: need` requirement is exempt from that `implements:` rule — see [[ARCH-TRACE-020]]."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a dangling tag is a gate ERROR Given  `mod.py` carrying `# implements: GHOST-CAP-001` and no such requirement When   `gate` runs Then   its output contains \"dangling tag\" and it exits 1",
        "CASE-2 — — an invalid status or layer is a gate ERROR Given  a requirement with `status: bogus`, and separately one with `layer: bogus` When   `gate` runs on each Then   each output contains \"invalid status\" or \"invalid layer\" respectively, and each exits 1",
        "CASE-3 — — a depends_on target that does not exist is a gate ERROR Given  a requirement with `depends_on: [GHOST-X-999]` and no such requirement When   `gate` runs Then   its output contains \"depends_on missing GHOST-X-999\" and it exits 1",
        "CASE-4 — — a confirmed requirement with no implements tag is a gate ERROR Given  a `status: confirmed` requirement with no `# implements:` tag anywhere When   `gate` runs Then   its output contains \"no implements\" and it exits 1",
        "CASE-5 — — ENFORCED names exactly the three enforced statuses Given  the module-level `R.ENFORCED` set When   it is inspected Then   it equals `{\"in-progress\", \"implemented\", \"confirmed\"}`, and `\"draft\"` is absent",
        "CASE-6 — — a confirmed need with no implements tag raises no gate error Given  a confirmed `layer: need` requirement with no `implements:` tag, satisfied by another requirement When   `gate` runs Then   `_link_sync_errors` reports no missing-implements error for that need --------------------"
      ],
      "accept": "CASE-1 — a dangling tag is a gate ERROR\n  Given  `mod.py` carrying `# implements: GHOST-CAP-001` and no such requirement\n  When   `gate` runs\n  Then   its output contains \"dangling tag\" and it exits 1\n\nCASE-2 — an invalid status or layer is a gate ERROR\n  Given  a requirement with `status: bogus`, and separately one with `layer: bogus`\n  When   `gate` runs on each\n  Then   each output contains \"invalid status\" or \"invalid layer\" respectively, and each\n         exits 1\n\nCASE-3 — a depends_on target that does not exist is a gate ERROR\n  Given  a requirement with `depends_on: [GHOST-X-999]` and no such requirement\n  When   `gate` runs\n  Then   its output contains \"depends_on missing GHOST-X-999\" and it exits 1\n\nCASE-4 — a confirmed requirement with no implements tag is a gate ERROR\n  Given  a `status: confirmed` requirement with no `# implements:` tag anywhere\n  When   `gate` runs\n  Then   its output contains \"no implements\" and it exits 1\n\nCASE-5 — ENFORCED names exactly the three enforced statuses\n  Given  the module-level `R.ENFORCED` set\n  When   it is inspected\n  Then   it equals `{\"in-progress\", \"implemented\", \"confirmed\"}`, and `\"draft\"` is absent\n\nCASE-6 — a confirmed need with no implements tag raises no gate error\n  Given  a confirmed `layer: need` requirement with no `implements:` tag, satisfied by\n         another requirement\n  When   `gate` runs\n  Then   `_link_sync_errors` reports no missing-implements error for that need\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-CHECK-006"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2487"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2494"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2513"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2542"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1562"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Erori de gate care blochează un commit",
          "intent": "Un `ERROR` este semnalul de oprire dură al gate-ului: iese din proces cu cod diferit de zero și blochează CI. Este rezervat pentru probleme pe care un om trebuie să le repare înainte ca ansamblul de cerințe să poată fi considerat de încredere — o etichetă stricată, o valoare enum invalidă, sau o cerință care pretinde că există cod când nicio etichetă nu o dovedește.",
          "contract": "> Un `ERROR` este semnalul de oprire dură al gate-ului: iese din proces cu cod diferit de zero\n> și blochează CI. Este rezervat pentru probleme pe care un om trebuie să le repare înainte ca\n> ansamblul de cerințe să poată fi considerat de încredere — o etichetă stricată, o valoare enum\n> invalidă, sau o cerință care pretinde că există cod când nicio etichetă nu o dovedește.\n\nFiecare punct de mai jos este obligatoriu.\n- `gate` raportează un `ERROR` și iese cu cod diferit de zero pentru fiecare condiție din acest grup.\n- O etichetă atârnândă — o etichetă de cod care referențiază o capacitate pe care nicio cerință\n  nu o definește — este o astfel de condiție.\n- Un `status` invalid sau un `layer` invalid este o astfel de condiție.\n- Un `depends_on` care indică spre un id inexistent este o astfel de condiție.\n- O cerință impusă fără niciun membru `implements:` este o astfel de condiție.\n- O cerință este impusă când starea ei este `in-progress`, `implemented` sau\n  `confirmed`.\n- O cerință `layer: need` este scutită de acea regulă `implements:` — vezi\n  [[ARCH-TRACE-020]].",
          "acceptance": "CASE-1 — o etichetă atârnândă este un ERROR de gate\n  Given  `mod.py` care poartă `# implements: GHOST-CAP-001` și nicio astfel de cerință\n  When   `gate` rulează\n  Then   ieșirea lui conține \"dangling tag\" și iese cu 1\n\nCASE-2 — un status sau layer invalid este un ERROR de gate\n  Given  o cerință cu `status: bogus`, și separat una cu `layer: bogus`\n  When   `gate` rulează pentru fiecare\n  Then   fiecare ieșire conține \"invalid status\" sau \"invalid layer\" respectiv, și fiecare\n         iese cu 1\n\nCASE-3 — o țintă depends_on care nu există este un ERROR de gate\n  Given  o cerință cu `depends_on: [GHOST-X-999]` și nicio astfel de cerință\n  When   `gate` rulează\n  Then   ieșirea lui conține \"depends_on missing GHOST-X-999\" și iese cu 1\n\nCASE-4 — o cerință confirmată fără etichetă implements este un ERROR de gate\n  Given  o cerință `status: confirmed` fără nicio etichetă `# implements:` nicăieri\n  When   `gate` rulează\n  Then   ieșirea lui conține \"no implements\" și iese cu 1\n\nCASE-5 — ENFORCED numește exact cele trei stări impuse\n  Given  mulțimea `R.ENFORCED` de la nivel de modul\n  When   este inspectată\n  Then   este egală cu `{\"in-progress\", \"implemented\", \"confirmed\"}`, iar `\"draft\"` lipsește\n\nCASE-6 — o nevoie confirmată fără etichetă implements nu ridică nicio eroare de gate\n  Given  o cerință confirmată `layer: need` fără etichetă `implements:`, satisfăcută de\n         altă cerință\n  When   `gate` rulează\n  Then   `_link_sync_errors` nu raportează nicio eroare de implements lipsă pentru acea nevoie\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-CHECK-829",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Contract drift and missing-coverage warnings",
      "intent": "A `WARN` never blocks a commit on its own, but each one names a real gap: a confirmed contract that moved since its lock, a requirement with no test coverage linked, or a confirmed requirement missing its Description/Cases section. `--strict` turns most of these into hard failures once a team is ready to enforce them.",
      "contract": [
        "`gate` reports drift as a `WARN` under plain `gate`: a `confirmed` requirement whose binding hash differs from the lock.",
        "The drift warning names the member `file:line` locations to re-check.",
        "`gate --strict` promotes drift (and the no-`tested-by:` warning below, and member drift) to an `ERROR`. The `depends_on`-cycle warning and the legacy-schema warning are the two exceptions — they stay warnings even under `--strict`.",
        "A `confirmed` requirement with no `tested-by:` member is a `WARN`.",
        "A requirement carrying a `test_exempt: <reason>` opt-out in its frontmatter is exempt from that test warning.",
        "A `layer: need` requirement is exempt from it too.",
        "A `confirmed` requirement missing a `## Description` section is a `WARN`, in both the `bus` and `feature` layers. It does not affect the exit code.",
        "A `confirmed` requirement missing a `## Cases` section is a `WARN`, in both the `bus` and `feature` layers. It does not affect the exit code."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a confirmed requirement's changed contract warns DRIFT, prefixed WARN not ERROR under plain gate Given  a confirmed requirement locked at an old hash, whose Description text has since changed When   plain `gate` runs (no `--strict`) Then   its output contains \"DRIFT\" on a line printed with the \"WARN\" prefix, never \"ERROR\"",
        "CASE-2 — — the drift warning names the member's file and line Given  a confirmed, drifted requirement with one member tagged at `mod.py:1` When   `gate` runs Then   its output contains \"re-check 1 member\" and \"mod.py:1\"",
        "CASE-3 — — a confirmed requirement with no tested-by tag warns, not errors Given  a confirmed requirement with an `implements:` tag but no `tested-by:` tag anywhere, and no `test_exempt` field When   `gate` runs Then   its output contains \"confirmed but no tested-by\" and it exits 0",
        "CASE-4 — — test_exempt suppresses the no-tested-by warning Given  a confirmed requirement with `test_exempt: covered by manual QA` and an `implements:` tag but no `tested-by:` tag When   `gate` runs Then   its output contains no \"tested-by\" finding and it exits 0",
        "CASE-5 — — a confirmed need with no tested-by tag raises no test warning Given  a confirmed `layer: need` requirement with no `implements:` or `tested-by:` tag, satisfied by another requirement When   `gate` runs Then   its output contains no missing-tested-by warning for that need",
        "CASE-6 — — a confirmed requirement missing Description warns and exits 0 Given  a confirmed `layer: bus` requirement whose body has no `## Description`/ `## Contract` section When   `gate` runs Then   its output contains \"missing '## Description'\" and it exits 0",
        "CASE-7 — — a confirmed requirement missing Cases warns and exits 0 Given  a confirmed `layer: bus` requirement whose body has a Description but no `## Cases`/`## Acceptance` section When   `gate` runs Then   its output contains \"missing '## Cases'\" and it exits 0 --------------------"
      ],
      "accept": "CASE-1 — a confirmed requirement's changed contract warns DRIFT, prefixed WARN not ERROR under plain gate\n  Given  a confirmed requirement locked at an old hash, whose Description text has since\n         changed\n  When   plain `gate` runs (no `--strict`)\n  Then   its output contains \"DRIFT\" on a line printed with the \"WARN\" prefix, never\n         \"ERROR\"\n\nCASE-2 — the drift warning names the member's file and line\n  Given  a confirmed, drifted requirement with one member tagged at `mod.py:1`\n  When   `gate` runs\n  Then   its output contains \"re-check 1 member\" and \"mod.py:1\"\n\nCASE-3 — a confirmed requirement with no tested-by tag warns, not errors\n  Given  a confirmed requirement with an `implements:` tag but no `tested-by:` tag\n         anywhere, and no `test_exempt` field\n  When   `gate` runs\n  Then   its output contains \"confirmed but no tested-by\" and it exits 0\n\nCASE-4 — test_exempt suppresses the no-tested-by warning\n  Given  a confirmed requirement with `test_exempt: covered by manual QA` and an\n         `implements:` tag but no `tested-by:` tag\n  When   `gate` runs\n  Then   its output contains no \"tested-by\" finding and it exits 0\n\nCASE-5 — a confirmed need with no tested-by tag raises no test warning\n  Given  a confirmed `layer: need` requirement with no `implements:` or `tested-by:` tag,\n         satisfied by another requirement\n  When   `gate` runs\n  Then   its output contains no missing-tested-by warning for that need\n\nCASE-6 — a confirmed requirement missing Description warns and exits 0\n  Given  a confirmed `layer: bus` requirement whose body has no `## Description`/\n         `## Contract` section\n  When   `gate` runs\n  Then   its output contains \"missing '## Description'\" and it exits 0\n\nCASE-7 — a confirmed requirement missing Cases warns and exits 0\n  Given  a confirmed `layer: bus` requirement whose body has a Description but no\n         `## Cases`/`## Acceptance` section\n  When   `gate` runs\n  Then   its output contains \"missing '## Cases'\" and it exits 0\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-CHECK-006"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2551"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2631"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2733"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1562"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9567"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 7,
      "covered": 7,
      "i18n": {
        "ro": {
          "title": "Avertismente de drift al contractului și de acoperire lipsă",
          "intent": "Un `WARN` nu blochează niciodată un commit de unul singur, dar fiecare numește o lacună reală: un contract confirmat care s-a mișcat de la blocarea lui, o cerință fără nicio acoperire de test legată, sau o cerință confirmată căreia îi lipsește secțiunea Description/Cases. `--strict` transformă majoritatea acestora în eșecuri dure odată ce o echipă este pregătită să le impună.",
          "contract": "> Un `WARN` nu blochează niciodată un commit de unul singur, dar fiecare numește o lacună\n> reală: un contract confirmat care s-a mișcat de la blocarea lui, o cerință fără nicio\n> acoperire de test legată, sau o cerință confirmată căreia îi lipsește secțiunea\n> Description/Cases. `--strict` transformă majoritatea acestora în eșecuri dure odată ce o\n> echipă este pregătită să le impună.\n\nFiecare punct de mai jos este obligatoriu.\n- `gate` raportează drift ca un `WARN` sub `gate` simplu: o cerință `confirmed` al cărei\n  hash de legare diferă de blocare.\n- Avertismentul de drift numește locațiile `file:line` ale membrilor de reverificat.\n- `gate --strict` promovează drift-ul (și avertismentul lipsă de `tested-by:` de mai jos, și\n  drift-ul de membru) la un `ERROR`. Avertismentul de ciclu `depends_on` și avertismentul de\n  schemă legacy sunt cele două excepții — rămân avertismente chiar și sub `--strict`.\n- O cerință `confirmed` fără membru `tested-by:` este un `WARN`.\n- O cerință care poartă o scutire `test_exempt: <reason>` în frontmatter este scutită\n  de acel avertisment de test.\n- O cerință `layer: need` este scutită și ea.\n- O cerință `confirmed` căreia îi lipsește o secțiune `## Description` este un `WARN`, atât în\n  layer-ul `bus` cât și `feature`. Nu afectează codul de ieșire.\n- O cerință `confirmed` căreia îi lipsește o secțiune `## Cases` este un `WARN`, atât în\n  layer-ul `bus` cât și `feature`. Nu afectează codul de ieșire.",
          "acceptance": "CASE-1 — contractul schimbat al unei cerințe confirmate avertizează DRIFT, prefixat WARN nu ERROR sub gate simplu\n  Given  o cerință confirmată blocată la un hash vechi, al cărei text Description s-a\n         schimbat între timp\n  When   `gate` simplu rulează (fără `--strict`)\n  Then   ieșirea lui conține \"DRIFT\" pe o linie tipărită cu prefixul \"WARN\", niciodată\n         \"ERROR\"\n\nCASE-2 — avertismentul de drift numește fișierul și linia membrului\n  Given  o cerință confirmată, cu drift, cu un membru etichetat la `mod.py:1`\n  When   `gate` rulează\n  Then   ieșirea lui conține \"re-check 1 member\" și \"mod.py:1\"\n\nCASE-3 — o cerință confirmată fără etichetă tested-by avertizează, nu dă eroare\n  Given  o cerință confirmată cu o etichetă `implements:` dar fără nicio etichetă `tested-by:`\n         nicăieri, și fără câmp `test_exempt`\n  When   `gate` rulează\n  Then   ieșirea lui conține \"confirmed but no tested-by\" și iese cu 0\n\nCASE-4 — test_exempt suprimă avertismentul lipsă de tested-by\n  Given  o cerință confirmată cu `test_exempt: covered by manual QA` și o etichetă\n         `implements:` dar fără etichetă `tested-by:`\n  When   `gate` rulează\n  Then   ieșirea lui nu conține nicio constatare \"tested-by\" și iese cu 0\n\nCASE-5 — o nevoie confirmată fără etichetă tested-by nu ridică niciun avertisment de test\n  Given  o cerință confirmată `layer: need` fără etichetă `implements:` sau `tested-by:`,\n         satisfăcută de altă cerință\n  When   `gate` rulează\n  Then   ieșirea lui nu conține niciun avertisment de tested-by lipsă pentru acea nevoie\n\nCASE-6 — o cerință confirmată căreia îi lipsește Description avertizează și iese cu 0\n  Given  o cerință `layer: bus` confirmată al cărei corp nu are secțiune `## Description`/\n         `## Contract`\n  When   `gate` rulează\n  Then   ieșirea lui conține \"missing '## Description'\" și iese cu 0\n\nCASE-7 — o cerință confirmată căreia îi lipsește Cases avertizează și iese cu 0\n  Given  o cerință `layer: bus` confirmată care are Description dar nu\n         secțiune `## Cases`/`## Acceptance`\n  When   `gate` rulează\n  Then   ieșirea lui conține \"missing '## Cases'\" și iese cu 0\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-CHECK-830",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Milestone shape and lock-file warnings",
      "intent": "Milestone typos and lock-file problems are roadmap or bookkeeping issues, not contract breaks, so they warn instead of failing the build. A corrupt or git-untracked lock is worth flagging anyway — silently disabling drift detection on a fresh checkout is worse than a loud warning.",
      "contract": [
        "The requirement `milestone:` field is optional. When present it matches the version shape `v<digits>[.<digits>…]`, for example `v1.14`.",
        "A malformed `milestone:` value is a `WARN`, because that field is roadmap-only metadata and never build-critical.",
        "A `deprecated` requirement is exempt from the `milestone:` shape check.",
        "A present-but-unreadable `_reqlock.json` is a `WARN`. Drift is skipped for that run rather than crashing.",
        "A lock sidecar (`_reqlock.json` or `_memberlock.json`) that exists on disk but is **not git-tracked** is a `WARN` naming the file.",
        "An uncommitted lock silently disables drift detection on a fresh CI checkout, which has no baseline to compare against.",
        "That git-tracking check is fail-open: `gate` stays silent when git is unavailable or the tree is not a work tree.",
        "`gate` names every sectioned-form requirement whose body lacks a `## Verify intent` section in one aggregated legacy-schema `WARN`. Atomic-form requirements (`form: atomic`) carry no such heading by design and are excluded from this check."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a well-shaped milestone value is silent Given  a confirmed requirement with `milestone: v1.14` (also tried: `v1.04`, `v2`) When   `gate` runs Then   its output contains no \"malformed\" finding",
        "CASE-2 — — a malformed milestone value warns Given  a confirmed requirement with `milestone: next` (also tried: `1.14`, `V1.0`, `v1.14-beta`) When   `gate` runs Then   its output contains \"malformed\"",
        "CASE-3 — — a deprecated requirement's malformed milestone is silent Given  a `status: deprecated` requirement with `milestone: next` (malformed shape) When   `gate` runs Then   its output contains no \"malformed\" finding",
        "CASE-4 — — a corrupt _reqlock.json warns and does not crash the gate Given  a `_reqlock.json` containing invalid JSON (\"{ not json\") When   `gate` runs Then   its output contains \"unreadable\" and it exits 0",
        "CASE-5 — — an untracked lock file is flagged, then clears once tracked Given  a git work tree with `_reqlock.json` written to disk but never `git add`ed When   `untracked_locks(reqs_dir)` runs before and after `git add -A` Then   it names `_reqlock.json` before the add and returns `[]` after it, closing the gap where a fresh CI checkout would otherwise have no baseline for drift detection",
        "CASE-6 — — the untracked-lock check is silent outside a git work tree Given  a `_reqlock.json` written to a plain directory that is not yet a git repository When   `untracked_locks(reqs_dir)` runs Then   it returns `[]`, not an error",
        "CASE-7 — — a sectioned-form requirement missing Verify intent is named in the legacy-schema warning Given  a sectioned-form requirement whose body has no `## Verify intent` section When   `gate` runs Then   its output contains \"legacy schema\" naming that requirement's id, and \"findings` is inactive\" --------------------"
      ],
      "accept": "CASE-1 — a well-shaped milestone value is silent\n  Given  a confirmed requirement with `milestone: v1.14` (also tried: `v1.04`, `v2`)\n  When   `gate` runs\n  Then   its output contains no \"malformed\" finding\n\nCASE-2 — a malformed milestone value warns\n  Given  a confirmed requirement with `milestone: next` (also tried: `1.14`, `V1.0`,\n         `v1.14-beta`)\n  When   `gate` runs\n  Then   its output contains \"malformed\"\n\nCASE-3 — a deprecated requirement's malformed milestone is silent\n  Given  a `status: deprecated` requirement with `milestone: next` (malformed shape)\n  When   `gate` runs\n  Then   its output contains no \"malformed\" finding\n\nCASE-4 — a corrupt _reqlock.json warns and does not crash the gate\n  Given  a `_reqlock.json` containing invalid JSON (\"{ not json\")\n  When   `gate` runs\n  Then   its output contains \"unreadable\" and it exits 0\n\nCASE-5 — an untracked lock file is flagged, then clears once tracked\n  Given  a git work tree with `_reqlock.json` written to disk but never `git add`ed\n  When   `untracked_locks(reqs_dir)` runs before and after `git add -A`\n  Then   it names `_reqlock.json` before the add and returns `[]` after it, closing the gap\n         where a fresh CI checkout would otherwise have no baseline for drift detection\n\nCASE-6 — the untracked-lock check is silent outside a git work tree\n  Given  a `_reqlock.json` written to a plain directory that is not yet a git repository\n  When   `untracked_locks(reqs_dir)` runs\n  Then   it returns `[]`, not an error\n\nCASE-7 — a sectioned-form requirement missing Verify intent is named in the legacy-schema warning\n  Given  a sectioned-form requirement whose body has no `## Verify intent` section\n  When   `gate` runs\n  Then   its output contains \"legacy schema\" naming that requirement's id, and \"findings`\n         is inactive\"\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-CHECK-006"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2125"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1562"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 7,
      "covered": 7,
      "i18n": {
        "ro": {
          "title": "Forma milestone-ului și avertismentele fișierului lock",
          "intent": "Greșelile de tastare la milestone și problemele fișierului lock sunt probleme de foaie de parcurs sau de contabilitate, nu rupturi de contract, deci avertizează în loc să eșueze build-ul. Un lock corupt sau ne-urmărit de git merită totuși semnalat — dezactivarea silențioasă a detecției drift-ului la un checkout curat este mai rea decât un avertisment zgomotos.",
          "contract": "> Greșelile de tastare la milestone și problemele fișierului lock sunt probleme de foaie de\n> parcurs sau de contabilitate, nu rupturi de contract, deci avertizează în loc să eșueze\n> build-ul. Un lock corupt sau ne-urmărit de git merită totuși semnalat — dezactivarea\n> silențioasă a detecției drift-ului la un checkout curat este mai rea decât un avertisment\n> zgomotos.\n\nFiecare punct de mai jos este obligatoriu.\n- Câmpul `milestone:` al cerinței este opțional. Când este prezent se potrivește cu forma de\n  versiune `v<digits>[.<digits>…]`, de exemplu `v1.14`.\n- O valoare `milestone:` malformată este un `WARN`, pentru că acel câmp este metadate\n  doar de foaie de parcurs și niciodată critic pentru build.\n- O cerință `deprecated` este scutită de verificarea formei `milestone:`.\n- Un `_reqlock.json` prezent dar necitibil este un `WARN`. Drift-ul este sărit pentru acea\n  rulare în loc să provoace un crash.\n- Un sidecar de lock (`_reqlock.json` sau `_memberlock.json`) care există pe disc dar nu este\n  **urmărit de git** este un `WARN` care numește fișierul.\n- Un lock necomis dezactivează silențios detecția drift-ului la un checkout CI curat, care\n  nu are nicio bază de comparație.\n- Acea verificare de urmărire git este fail-open: `gate` rămâne tăcut când git nu este\n  disponibil sau arborele nu este un work tree.\n- `gate` numește fiecare cerință în formă sectionată al cărei corp nu are secțiune\n  `## Verify intent` într-un singur `WARN` agregat de schemă legacy. Cerințele în formă\n  atomică (`form:\n  atomic`) nu poartă niciun astfel de titlu prin design și sunt excluse\n  din această verificare.",
          "acceptance": "CASE-1 — o valoare milestone bine formată este tăcută\n  Given  o cerință confirmată cu `milestone: v1.14` (încercat și cu: `v1.04`, `v2`)\n  When   `gate` rulează\n  Then   ieșirea lui nu conține nicio constatare \"malformed\"\n\nCASE-2 — o valoare milestone malformată avertizează\n  Given  o cerință confirmată cu `milestone: next` (încercat și cu: `1.14`, `V1.0`,\n         `v1.14-beta`)\n  When   `gate` rulează\n  Then   ieșirea lui conține \"malformed\"\n\nCASE-3 — milestone-ul malformat al unei cerințe deprecate este tăcut\n  Given  o cerință `status: deprecated` cu `milestone: next` (formă malformată)\n  When   `gate` rulează\n  Then   ieșirea lui nu conține nicio constatare \"malformed\"\n\nCASE-4 — un _reqlock.json corupt avertizează și nu provoacă un crash al gate-ului\n  Given  un `_reqlock.json` conținând JSON invalid (\"{ not json\")\n  When   `gate` rulează\n  Then   ieșirea lui conține \"unreadable\" și iese cu 0\n\nCASE-5 — un fișier lock ne-urmărit este semnalat, apoi se curăță odată urmărit\n  Given  un work tree git cu `_reqlock.json` scris pe disc dar niciodată adăugat prin `git add`\n  When   `untracked_locks(reqs_dir)` rulează înainte și după `git add -A`\n  Then   numește `_reqlock.json` înainte de adăugare și returnează `[]` după ea, închizând\n         lacuna în care un checkout CI curat nu ar avea altfel nicio bază pentru detecția drift-ului\n\nCASE-6 — verificarea lock-ului ne-urmărit este tăcută în afara unui work tree git\n  Given  un `_reqlock.json` scris într-un director simplu care nu este încă un repozitoriu git\n  When   `untracked_locks(reqs_dir)` rulează\n  Then   returnează `[]`, nu o eroare\n\nCASE-7 — o cerință în formă sectionată căreia îi lipsește Verify intent este numită în avertismentul de schemă legacy\n  Given  o cerință în formă sectionată al cărei corp nu are secțiune `## Verify intent`\n  When   `gate` rulează\n  Then   ieșirea lui conține \"legacy schema\" numind id-ul acelei cerințe, și \"findings`\n         is inactive\"\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-CHECK-831",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Corpus-health warnings: needs, levels, cycles",
      "intent": "These warnings are opt-in signals about corpus health rather than individual contract breaks: an unvalidated stakeholder need, foundation code tested only end-to-end, or a `depends_on` cycle that makes build order ambiguous. They stay warnings even under `--strict` so upgrading the engine never flips a green build red.",
      "contract": [
        "`gate` counts those legacy-schema requirements in the summary.",
        "The legacy-schema warning does not affect the exit code.",
        "A confirmed `need` with no `validated-against:` member is a `WARN`, once the repo carries at least one such tag (see [[ARCH-VLEVEL-037]]).",
        "A confirmed `bus` requirement whose levelled `tested-by:` links are all `@system` is a `WARN`.",
        "A `depends_on` cycle is a `WARN` naming the whole chain, once per distinct cycle.",
        "The cycle warning stays a warning under `--strict`, so an existing corpus keeps its exit code when the engine is upgraded."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the summary line reports the legacy-schema count Given  one requirement whose body has no `## Verify intent` section When   `gate` runs Then   its final summary line contains \"1 legacy-schema\"",
        "CASE-2 — — a legacy-schema requirement warns but the gate exits 0 Given  a `status: baseline` requirement with no `## Verify intent` section When   `gate` runs Then   its output contains \"legacy schema\" and it exits 0",
        "CASE-3 — — an unvalidated confirmed need warns once the repo has opted in Given  a confirmed need with no `validated-against:` tag, in a repo where at least one `validated-against:` tag exists elsewhere When   `gate` runs Then   its output names that need alongside \"validated-against\"",
        "CASE-4 — — a bus requirement verified only at @system warns Given  a confirmed `layer: bus` requirement whose only levelled `tested-by:` link is `@system` When   `gate` runs Then   its output contains \"@system\"",
        "CASE-5 — — a depends_on cycle warns once, naming the whole chain Given  two requirements whose `depends_on` fields point at each other When   `gate` runs Then   its output contains \"depends_on cycle\" and \"A-X-001 -> A-X-002 -> A-X-001\", and it exits 0",
        "CASE-6 — — --strict does not promote the cycle warning to an error Given  two requirements whose `depends_on` fields point at each other When   `gate --strict` runs Then   it still exits 0 --------------------"
      ],
      "accept": "CASE-1 — the summary line reports the legacy-schema count\n  Given  one requirement whose body has no `## Verify intent` section\n  When   `gate` runs\n  Then   its final summary line contains \"1 legacy-schema\"\n\nCASE-2 — a legacy-schema requirement warns but the gate exits 0\n  Given  a `status: baseline` requirement with no `## Verify intent` section\n  When   `gate` runs\n  Then   its output contains \"legacy schema\" and it exits 0\n\nCASE-3 — an unvalidated confirmed need warns once the repo has opted in\n  Given  a confirmed need with no `validated-against:` tag, in a repo where at least one\n         `validated-against:` tag exists elsewhere\n  When   `gate` runs\n  Then   its output names that need alongside \"validated-against\"\n\nCASE-4 — a bus requirement verified only at @system warns\n  Given  a confirmed `layer: bus` requirement whose only levelled `tested-by:` link is\n         `@system`\n  When   `gate` runs\n  Then   its output contains \"@system\"\n\nCASE-5 — a depends_on cycle warns once, naming the whole chain\n  Given  two requirements whose `depends_on` fields point at each other\n  When   `gate` runs\n  Then   its output contains \"depends_on cycle\" and \"A-X-001 -> A-X-002 -> A-X-001\", and\n         it exits 0\n\nCASE-6 — --strict does not promote the cycle warning to an error\n  Given  two requirements whose `depends_on` fields point at each other\n  When   `gate --strict` runs\n  Then   it still exits 0\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-CHECK-006"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2560"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2573"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2811"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2820"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4332"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Avertismente de sănătate a corpusului: nevoi, niveluri, cicluri",
          "intent": "Aceste avertismente sunt semnale opt-in despre sănătatea corpusului mai degrabă decât rupturi de contract individuale: o nevoie de stakeholder nevalidată, cod de fundație testat doar end-to-end, sau un ciclu `depends_on` care face ambiguă ordinea de build. Rămân avertismente chiar și sub `--strict` astfel încât actualizarea motorului nu transformă niciodată un build verde în roșu.",
          "contract": "> Aceste avertismente sunt semnale opt-in despre sănătatea corpusului mai degrabă decât\n> rupturi de contract individuale: o nevoie de stakeholder nevalidată, cod de fundație testat\n> doar end-to-end, sau un ciclu `depends_on` care face ambiguă ordinea de build. Rămân\n> avertismente chiar și sub `--strict` astfel încât actualizarea motorului nu transformă\n> niciodată un build verde în roșu.\n\nFiecare punct de mai jos este obligatoriu.\n- `gate` numără acele cerințe cu schemă legacy în rezumat.\n- Avertismentul de schemă legacy nu afectează codul de ieșire.\n- O `need` confirmată fără membru `validated-against:` este un `WARN`, odată ce repo-ul poartă\n  cel puțin o astfel de etichetă (vezi [[ARCH-VLEVEL-037]]).\n- O cerință `bus` confirmată ale cărei legături `tested-by:` de nivel sunt toate `@system` este un `WARN`.\n- Un ciclu `depends_on` este un `WARN` care numește întregul lanț, o dată per ciclu distinct.\n- Avertismentul de ciclu rămâne un avertisment sub `--strict`, astfel încât un corpus existent\n  își păstrează codul de ieșire când motorul este actualizat.",
          "acceptance": "CASE-1 — linia de rezumat raportează numărul de schemă legacy\n  Given  o cerință al cărei corp nu are secțiune `## Verify intent`\n  When   `gate` rulează\n  Then   linia finală de rezumat conține \"1 legacy-schema\"\n\nCASE-2 — o cerință cu schemă legacy avertizează dar gate-ul iese cu 0\n  Given  o cerință `status: baseline` fără secțiune `## Verify intent`\n  When   `gate` rulează\n  Then   ieșirea lui conține \"legacy schema\" și iese cu 0\n\nCASE-3 — o nevoie confirmată nevalidată avertizează odată ce repo-ul a optat pentru asta\n  Given  o nevoie confirmată fără etichetă `validated-against:`, într-un repo unde există cel\n         puțin o etichetă `validated-against:` altundeva\n  When   `gate` rulează\n  Then   ieșirea lui numește acea nevoie alături de \"validated-against\"\n\nCASE-4 — o cerință bus verificată doar la @system avertizează\n  Given  o cerință `layer: bus` confirmată a cărei singură legătură `tested-by:` de nivel este\n         `@system`\n  When   `gate` rulează\n  Then   ieșirea lui conține \"@system\"\n\nCASE-5 — un ciclu depends_on avertizează o dată, numind întregul lanț\n  Given  două cerințe ale căror câmpuri `depends_on` indică una spre cealaltă\n  When   `gate` rulează\n  Then   ieșirea lui conține \"depends_on cycle\" și \"A-X-001 -> A-X-002 -> A-X-001\", și\n         iese cu 0\n\nCASE-6 — --strict nu promovează avertismentul de ciclu la o eroare\n  Given  două cerințe ale căror câmpuri `depends_on` indică una spre cealaltă\n  When   `gate --strict` rulează\n  Then   tot iese cu 0\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-CHECK-832",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "What the gate prints beyond pass or fail",
      "intent": "Besides an exit code, `gate` reports what it saw: how many verify-intent questions are still open, and a one-line summary of requirements, members, errors and warnings. Neither line changes the exit code — they are for the person reading the terminal, not for CI.",
      "contract": [
        "`gate` prints an advisory line carrying the open verify-intent finding count when that count is above zero.",
        "That advisory line does not affect the exit code.",
        "`gate` prints a summary of requirements, members, errors and warnings."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — gate reports the open verify-intent finding count Given  a requirement with one open `## Verify intent` question When   `gate` runs Then   its output contains \"1 open verify-intent finding(s)\"",
        "CASE-2 — — an open verify-intent finding does not change an otherwise-clean exit code Given  a `baseline` requirement with one open `## Verify intent` question and no other gate finding When   `gate` runs Then   it prints the finding count and returns exit code 0",
        "CASE-3 — — the summary line reports the confirmed and legacy-schema counts Given  one confirmed requirement (implemented) and one baseline requirement, both in the current schema When   `gate` runs Then   its final line contains \"1 confirmed\" and \"0 legacy-schema\" --------------------"
      ],
      "accept": "CASE-1 — gate reports the open verify-intent finding count\n  Given  a requirement with one open `## Verify intent` question\n  When   `gate` runs\n  Then   its output contains \"1 open verify-intent finding(s)\"\n\nCASE-2 — an open verify-intent finding does not change an otherwise-clean exit code\n  Given  a `baseline` requirement with one open `## Verify intent` question and no other\n         gate finding\n  When   `gate` runs\n  Then   it prints the finding count and returns exit code 0\n\nCASE-3 — the summary line reports the confirmed and legacy-schema counts\n  Given  one confirmed requirement (implemented) and one baseline requirement, both in\n         the current schema\n  When   `gate` runs\n  Then   its final line contains \"1 confirmed\" and \"0 legacy-schema\"\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-CHECK-006"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2887"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1356"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9567"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Ce tipărește gate-ul dincolo de succes sau eșec",
          "intent": "Pe lângă un cod de ieșire, `gate` raportează ce a văzut: câte întrebări verify-intent sunt încă deschise, și un rezumat de o linie al cerințelor, membrilor, erorilor și avertismentelor. Niciuna dintre linii nu schimbă codul de ieșire — sunt pentru persoana care citește terminalul, nu pentru CI.",
          "contract": "> Pe lângă un cod de ieșire, `gate` raportează ce a văzut: câte întrebări verify-intent sunt\n> încă deschise, și un rezumat de o linie al cerințelor, membrilor, erorilor și\n> avertismentelor. Niciuna dintre linii nu schimbă codul de ieșire — sunt pentru persoana care\n> citește terminalul, nu pentru CI.\n\nFiecare punct de mai jos este obligatoriu.\n- `gate` tipărește o linie consultativă care poartă numărul de constatări verify-intent deschise\n  când acel număr este peste zero.\n- Acea linie consultativă nu afectează codul de ieșire.\n- `gate` tipărește un rezumat al cerințelor, membrilor, erorilor și avertismentelor.",
          "acceptance": "CASE-1 — gate raportează numărul de constatări verify-intent deschise\n  Given  o cerință cu o întrebare deschisă `## Verify intent`\n  When   `gate` rulează\n  Then   ieșirea lui conține \"1 open verify-intent finding(s)\"\n\nCASE-2 — o constatare verify-intent deschisă nu schimbă un cod de ieșire altfel curat\n  Given  o cerință `baseline` cu o întrebare deschisă `## Verify intent` și nicio altă\n         constatare de gate\n  When   `gate` rulează\n  Then   tipărește numărul de constatări și returnează codul de ieșire 0\n\nCASE-3 — linia de rezumat raportează numărul de confirmate și de schemă legacy\n  Given  o cerință confirmată (implementată) și una baseline, ambele în\n         schema curentă\n  When   `gate` rulează\n  Then   linia lui finală conține \"1 confirmed\" și \"0 legacy-schema\"\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-CHECK-833",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Advancing the lock file",
      "intent": "The lock file (`_reqlock.json`) only advances when explicitly told to — plain `gate` is read-only so a developer can check the corpus without accidentally re-baselining a drifted contract. `sync` always passes `--update-lock`; the deprecated `check` alias does not, matching its older, more cautious default.",
      "contract": [
        "With `--update-lock`, `gate` writes the current binding hashes to `requirements/_reqlock.json`.",
        "`sync` always passes `--update-lock`. The deprecated `check` alias does not — it only advances the lock when the caller explicitly passes `--update-lock`.",
        "The `gate` verb itself is report-only."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — --update-lock writes the requirement's hash into the lock Given  a `baseline` requirement with no existing lock entry When   `cmd_check(..., update_lock=True)` runs Then   `load_lock(d)` afterward contains that requirement's id",
        "CASE-2 — — a clean sync run advances the lock and regenerates the map Given  a clean corpus (no gate errors) When   `reqmap sync --root <d> --code <d>` runs Then   it exits 0 and `requirements/_map.json` exists, confirming the full check-with-lock-then-map pipeline ran",
        "CASE-3 — — the gate verb never writes lock updated Given  a plain draft requirement, no `--update-lock` flag When   `reqmap.py gate --root <d>` runs Then   it exits 0 and its stdout contains no \"lock updated\" line"
      ],
      "accept": "CASE-1 — --update-lock writes the requirement's hash into the lock\n  Given  a `baseline` requirement with no existing lock entry\n  When   `cmd_check(..., update_lock=True)` runs\n  Then   `load_lock(d)` afterward contains that requirement's id\n\nCASE-2 — a clean sync run advances the lock and regenerates the map\n  Given  a clean corpus (no gate errors)\n  When   `reqmap sync --root <d> --code <d>` runs\n  Then   it exits 0 and `requirements/_map.json` exists, confirming the full\n         check-with-lock-then-map pipeline ran\n\nCASE-3 — the gate verb never writes lock updated\n  Given  a plain draft requirement, no `--update-lock` flag\n  When   `reqmap.py gate --root <d>` runs\n  Then   it exits 0 and its stdout contains no \"lock updated\" line",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-CHECK-006"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2887"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1562"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Avansarea fișierului lock",
          "intent": "Fișierul lock (`_reqlock.json`) avansează doar când i se cere explicit — `gate` simplu este read-only astfel încât un dezvoltator poate verifica corpusul fără să re-stabilească accidental baza unui contract cu drift. `sync` transmite mereu `--update-lock`; alias-ul deprecat `check` nu, potrivindu-se cu comportamentul lui mai vechi, mai precaut, implicit.",
          "contract": "> Fișierul lock (`_reqlock.json`) avansează doar când i se cere explicit — `gate` simplu\n> este read-only astfel încât un dezvoltator poate verifica corpusul fără să re-stabilească\n> accidental baza unui contract cu drift. `sync` transmite mereu `--update-lock`; alias-ul\n> deprecat `check` nu, potrivindu-se cu comportamentul lui mai vechi, mai precaut, implicit.\n\nFiecare punct de mai jos este obligatoriu.\n- Cu `--update-lock`, `gate` scrie hash-urile curente de legare în\n  `requirements/_reqlock.json`.\n- `sync` transmite mereu `--update-lock`. Alias-ul deprecat `check` nu — el\n  avansează lock-ul doar când apelantul transmite explicit `--update-lock`.\n- Verbul `gate` în sine este doar de raportare.",
          "acceptance": "CASE-1 — --update-lock scrie hash-ul cerinței în lock\n  Given  o cerință `baseline` fără nicio intrare existentă în lock\n  When   `cmd_check(..., update_lock=True)` rulează\n  Then   `load_lock(d)` conține ulterior id-ul acelei cerințe\n\nCASE-2 — o rulare sync curată avansează lock-ul și regenerează harta\n  Given  un corpus curat (fără erori de gate)\n  When   `reqmap sync --root <d> --code <d>` rulează\n  Then   iese cu 0 și `requirements/_map.json` există, confirmând că întregul\n         pipeline check-with-lock-apoi-map a rulat\n\nCASE-3 — verbul gate nu scrie niciodată lock updated\n  Given  o cerință draft simplă, fără flag-ul `--update-lock`\n  When   `reqmap.py gate --root <d>` rulează\n  Then   iese cu 0 și stdout-ul lui nu conține nicio linie \"lock updated\""
        }
      }
    },
    {
      "id": "ARCH-CLARIFY-062",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Questions a requirement has not answered",
      "intent": "The expensive ambiguity is the one nobody notices while the requirement is being written. \"The gate reports errors quickly\" reads fine, passes the linter, and is discovered to mean nothing three weeks later by whoever has to write the assertion. `clarify` asks the questions a careful reviewer would ask at authoring time — what threshold, what unit, what happens when it fails — so the answer lands in the requirement instead of being guessed in code. It only ever asks.",
      "contract": [
        "`clarify` detects the shapes that cannot be verified as written and turns each into one question about the requirement's own text. [[REQ-CLARIFY-956]]",
        "`clarify` prints the questions grouped by severity, or emits them as JSON, writes nothing, and always exits 0. [[REQ-CLARIFY-957]]"
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a requirement with nothing detectable says so Given  a requirement whose clauses carry no hedge, no bare number and a case per clause When   `clarify` runs on it Then   it reports nothing unclear and points at `implement`",
        "CASE-2 — — the questions are deterministic Given  the same requirement, unchanged When   `clarify` runs twice Then   both runs emit the same questions in the same order",
        "CASE-3 — — clarify never fails a build Given  a requirement with several blocking questions When   `clarify` runs Then   it exits 0"
      ],
      "accept": "CASE-1 — a requirement with nothing detectable says so\n  Given  a requirement whose clauses carry no hedge, no bare number and a case per clause\n  When   `clarify` runs on it\n  Then   it reports nothing unclear and points at `implement`\n\nCASE-2 — the questions are deterministic\n  Given  the same requirement, unchanged\n  When   `clarify` runs twice\n  Then   both runs emit the same questions in the same order\n\nCASE-3 — clarify never fails a build\n  Given  a requirement with several blocking questions\n  When   `clarify` runs\n  Then   it exits 0",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001"
      ],
      "used_by": [
        "ARCH-IMPLEMENT-063"
      ],
      "satisfies": [
        "SYS-AUTHOR-101"
      ],
      "satisfied_by": [
        "REQ-CLARIFY-956",
        "REQ-CLARIFY-957",
        "REQ-CLARIFY-975"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7805"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7897"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10100"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10714"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10798"
        }
      ],
      "test_exempt": null,
      "milestone": "v4.0",
      "priority": "should-have",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Întrebări la care o cerință nu a răspuns",
          "intent": "Ambiguitatea costisitoare este cea pe care nimeni nu o observă în timp ce cerința este scrisă. \"Gate-ul raportează erorile rapid\" sună bine, trece de linter, și este descoperită ca neînsemnând nimic trei săptămâni mai târziu de cel care trebuie să scrie assertion-ul. `clarify` pune întrebările pe care un revizor atent le-ar pune la momentul redactării — ce prag, ce unitate, ce se întâmplă când eșuează — astfel încât răspunsul să ajungă în cerință, nu să fie ghicit în cod. Nu face niciodată altceva decât să întrebe.",
          "contract": "> Ambiguitatea costisitoare este cea pe care nimeni nu o observă în timp ce cerința este\n> scrisă. \"Gate-ul raportează erorile rapid\" sună bine, trece de linter, și este\n> descoperită ca neînsemnând nimic trei săptămâni mai târziu de cel care trebuie să scrie\n> assertion-ul. `clarify` pune întrebările pe care un revizor atent le-ar pune la momentul\n> redactării — ce prag, ce unitate, ce se întâmplă când eșuează — astfel încât răspunsul\n> să ajungă în cerință, nu să fie ghicit în cod. Nu face niciodată altceva decât să întrebe.\n\nFiecare punct de mai jos este obligatoriu.\n- `clarify` detectează formele care nu pot fi verificate așa cum sunt scrise și transformă fiecare într-o întrebare despre textul propriu al cerinței. [[REQ-CLARIFY-956]]\n- `clarify` afișează întrebările grupate pe severitate, sau le emite ca JSON, nu scrie nimic, și iese întotdeauna cu 0. [[REQ-CLARIFY-957]]",
          "acceptance": "CASE-1 — o cerință fără nimic detectabil spune acest lucru\n  Given  o cerință ale cărei clauze nu poartă nicio formulare vagă, niciun număr simplu și au câte un caz per clauză\n  When   `clarify` rulează pe ea\n  Then   nu raportează nimic neclar și indică spre `implement`\n\nCASE-2 — întrebările sunt deterministe\n  Given  aceeași cerință, neschimbată\n  When   `clarify` rulează de două ori\n  Then   ambele rulări emit aceleași întrebări în aceeași ordine\n\nCASE-3 — clarify nu face niciodată build-ul să eșueze\n  Given  o cerință cu mai multe întrebări blocante\n  When   `clarify` rulează\n  Then   iese cu 0"
        }
      }
    },
    {
      "id": "REQ-CLARIFY-956",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Detecting what a requirement leaves open",
      "intent": "Each detector encodes one question a reviewer asks by reflex. They are deliberately shallow — lexical, per clause, no model of meaning — because a question that is occasionally unnecessary costs a sentence, while a missed ambiguity costs a rewrite.",
      "contract": [
        "A clause carrying a hedge word with no measurable threshold raises one question naming that word.",
        "A bare number in a clause with no unit beside it raises a question asking for the unit; an identifier such as a version or a case label is not a bare number.",
        "A clause quantified over \"all\", \"every\" or \"any\" with no stated limit raises a question about the upper bound.",
        "A clause whose subject is \"It\" or \"the system\" raises a question asking which component acts.",
        "A requirement with more clauses than cases raises ONE question naming how many are uncovered, and saying that a count cannot tell which. It never accuses a clause by position: it compares two numbers and never reads a case, so pointing at the tail clauses was a guess — wrong whenever an early clause is the uncovered one, such as a clause that delegates its cases to another requirement. A requirement whose cases never mention a failure path raises a question about that.",
        "A requirement whose cases nearly all start from the same kind of input raises one question about the kind that is missing, unless that kind is the corpus's own subject.",
        "A requirement with no clause, or with no labelled case, raises a blocking question; every other question is advisory."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a hedge word is questioned by name Given  a clause reading \"the gate reports errors quickly\" When   the detectors run Then   one question names \"quickly\" and asks for the measurable threshold",
        "CASE-2 — — a bare number is questioned, an identifier is not Given  one clause reading \"retries 3 times\" and one reading \"emits CASE-2 for v4.0.0\" When   the detectors run Then   the first raises a missing-unit question and the second raises none",
        "CASE-3 — — a happy-path-only acceptance raises the failure question Given  a requirement whose cases mention no invalid, missing or failing input When   the detectors run Then   one question asks what happens on the failure path",
        "CASE-4 — — no clause or no case is blocking Given  a requirement with an empty Description, and one with no labelled case When   the detectors run on each Then   each raises a question of severity blocking",
        "CASE-5 — — cases that all start from one kind of input are questioned Given  a requirement whose every case opens on the same noun, and a corpus in which that noun is not the subject every other requirement starts from When   the detectors run Then   one question names that noun and asks which other kind a caller would supply",
        "CASE-6 — — the corpus's own subject is not that signal Given  a corpus in which nearly every case of every requirement starts from the same noun When   the detectors run on one of them Then   no question is raised about it, because the noun is the domain, not a narrow focus"
      ],
      "accept": "CASE-1 — a hedge word is questioned by name\n  Given  a clause reading \"the gate reports errors quickly\"\n  When   the detectors run\n  Then   one question names \"quickly\" and asks for the measurable threshold\n\nCASE-2 — a bare number is questioned, an identifier is not\n  Given  one clause reading \"retries 3 times\" and one reading \"emits CASE-2 for v4.0.0\"\n  When   the detectors run\n  Then   the first raises a missing-unit question and the second raises none\n\nCASE-3 — a happy-path-only acceptance raises the failure question\n  Given  a requirement whose cases mention no invalid, missing or failing input\n  When   the detectors run\n  Then   one question asks what happens on the failure path\n\nCASE-4 — no clause or no case is blocking\n  Given  a requirement with an empty Description, and one with no labelled case\n  When   the detectors run on each\n  Then   each raises a question of severity blocking\n\nCASE-5 — cases that all start from one kind of input are questioned\n  Given  a requirement whose every case opens on the same noun, and a corpus in which that\n         noun is not the subject every other requirement starts from\n  When   the detectors run\n  Then   one question names that noun and asks which other kind a caller would supply\n\nCASE-6 — the corpus's own subject is not that signal\n  Given  a corpus in which nearly every case of every requirement starts from the same noun\n  When   the detectors run on one of them\n  Then   no question is raised about it, because the noun is the domain, not a narrow focus",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-CLARIFY-062"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7805"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10100"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10798"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6
    },
    {
      "id": "REQ-CLARIFY-957",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Reporting the open questions",
      "intent": "A list of questions is only useful if the reader can see which one to answer first and where it came from. The text form quotes the clause it is about; the JSON form is what an agent answers against.",
      "contract": [
        "`clarify <ID>` prints the questions for that requirement, blocking ones first, each with its rule name, its location, the clause it quotes, and a suggested shape of answer.",
        "`clarify` with no id reports only the blocking questions across the corpus, so a large corpus answers \"what is unimplementable right now\".",
        "`--json` emits the same records as `{engine_version, advisory, requirements}` and prints nothing else.",
        "An unknown id exits 1 so a typo is visible; every other run exits 0 and writes no file."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — questions are printed with their rule and quote Given  a requirement with one hedge-word clause When   `clarify <ID>` runs Then   the output carries the rule name, the clause text and a suggestion line",
        "CASE-2 — — the corpus view is blocking-only Given  one requirement with a blocking question and one with only advisory questions When   `clarify` runs with no id Then   only the first requirement appears",
        "CASE-3 — — JSON carries the same records Given  any requirement When   `clarify <ID> --json` runs Then   stdout parses as JSON holding that requirement's questions",
        "CASE-4 — — an unknown id is an error, an answered requirement is not Given  an id that does not exist, and a requirement with no detectable question When   `clarify` runs on each Then   the first exits 1 and the second exits 0 --------------------"
      ],
      "accept": "CASE-1 — questions are printed with their rule and quote\n  Given  a requirement with one hedge-word clause\n  When   `clarify <ID>` runs\n  Then   the output carries the rule name, the clause text and a suggestion line\n\nCASE-2 — the corpus view is blocking-only\n  Given  one requirement with a blocking question and one with only advisory questions\n  When   `clarify` runs with no id\n  Then   only the first requirement appears\n\nCASE-3 — JSON carries the same records\n  Given  any requirement\n  When   `clarify <ID> --json` runs\n  Then   stdout parses as JSON holding that requirement's questions\n\nCASE-4 — an unknown id is an error, an answered requirement is not\n  Given  an id that does not exist, and a requirement with no detectable question\n  When   `clarify` runs on each\n  Then   the first exits 1 and the second exits 0\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-CLARIFY-062"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7897"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10100"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4
    },
    {
      "id": "REQ-CLARIFY-975",
      "layer": "feature",
      "level": "code",
      "status": "draft",
      "area": "REQ",
      "title": "An answer can raise a question the old text never had",
      "intent": "Clarifying a requirement means editing it, and an edit is new text: a clause that answers one question can introduce an unbounded quantity, a number with no unit, or a case with no failure path. Nobody re-reads the whole corpus after an edit, so the new question sits unread. `sync` runs after every edit anyway, which makes it the one place the comparison costs nothing.",
      "contract": [
        "`sync` records, per requirement, the RULES of its blocking open questions, in `requirements/_clarifylock.json`. **Requirements with no questions are recorded too**, so \"absent from the snapshot\" means \"never seen\" and nothing else. Otherwise a requirement going from zero questions to one would be mistaken for a new file and silenced, which is the case this check exists to catch.",
        "On the next `sync`, a rule present now and absent from the snapshot is reported as a NEW question, named with its requirement.",
        "**The rule is the fingerprint, not the prose.** Rewording a clause that still has the same defect is the same question, not a new one.",
        "**A requirement absent from the snapshot is not reported.** A newly-added requirement has no previous state, so all its questions would read as new, which is noise.",
        "The sidecar fails open: absent, corrupt, or written by a newer schema, it reads as empty and the run reports nothing rather than crashing.",
        "This is advisory. It never changes an exit code, because an unanswered question is a conversation, not a build failure."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — an edit that raises a new question is reported Given  a requirement synced once, then edited so a new blocking rule fires When   `sync` runs Then   it names that requirement and the new rule, and the exit code is unchanged",
        "CASE-2 — — an unchanged question is not re-reported Given  a requirement whose blocking questions are the same as at the last sync When   `sync` runs Then   nothing is reported as new",
        "CASE-3 — — a new requirement's questions are not reported as new Given  a requirement that has never been in the snapshot When   `sync` runs Then   its questions are not reported as new"
      ],
      "accept": "CASE-1 — an edit that raises a new question is reported\n  Given  a requirement synced once, then edited so a new blocking rule fires\n  When   `sync` runs\n  Then   it names that requirement and the new rule, and the exit code is unchanged\n\nCASE-2 — an unchanged question is not re-reported\n  Given  a requirement whose blocking questions are the same as at the last sync\n  When   `sync` runs\n  Then   nothing is reported as new\n\nCASE-3 — a new requirement's questions are not reported as new\n  Given  a requirement that has never been in the snapshot\n  When   `sync` runs\n  Then   its questions are not reported as new",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-CLARIFY-062"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2082"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2086"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2102"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2110"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2986"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10714"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [
        {
          "signal": "unreviewed",
          "advice": "Draft/baseline, not yet validated: review the contract, wire its `tested-by` tests, then promote to `confirmed`. Until then it is tracked, not enforced."
        }
      ],
      "clauses": 3,
      "covered": 3
    },
    {
      "id": "ARCH-CMDREGISTRY-033",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "CLI command registry + generated integration artifacts",
      "intent": "`tool_definition.json` and the `SKILL.universal.md` command table were hand-maintained mirrors of the CLI and silently drifted whenever a command was added or renamed. A single declarative `COMMANDS` registry — with generation and a gate drift-guard — makes those mirrors impossible to diverge undetected.",
      "contract": [
        "A `COMMANDS` dict is the single source of truth for the CLI's commands: argparse's choices, the generated `tool_definition.json`, and the `SKILL.universal.md` command table all derive from it, and the gate fails when a generated artifact goes stale. [[REQ-CMDREGISTRY-834]]",
        "The registry is also emitted as data on the map, so a surface can document the CLI without running it. [[REQ-CMDREGISTRY-963]]"
      ],
      "verify": [],
      "notes": [
        "`gen-integration` is the only command that writes generated artifacts; running it is required after any `COMMANDS` change before committing.",
        "The gate check (`_check_integration_fresh`) re-generates in a temp dir and compares byte-for-byte; it is deterministic because `_generate_schema` sorts JSON object keys and `_generate_command_table` iterates `COMMANDS` in insertion order (which is also the order exposed by `_cli_choices()` and `--help`)."
      ],
      "current_impl": [
        "`COMMANDS` dict and `_cli_choices()` in `reqmap.py` (registry + choices derivation).",
        "`_generate_schema()` and `_generate_command_table()` / `_write_region()` in `reqmap.py` (artifact generators).",
        "`cmd_gen_integration()` and the `gen-integration` verb in `reqmap.py` (runner).",
        "`_check_integration_fresh()` wired into `gate` in `reqmap.py` (drift guard).",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  the `COMMANDS` registry and the live argparse parser When   `_cli_choices()` is called Then   its return value equals `list(COMMANDS)` (insertion order) — no literal choices exist",
        "CASE-2 — Given  a committed `tool_definition.json` whose content differs from a fresh generation When   `gate` runs Then   the gate exits non-zero (stale artifact is a hard error)",
        "CASE-3 — Given  `gen-integration` is run When   `tool_definition.json` and the `SKILL.universal.md` command table are written Then   their content is byte-for-byte reproducible on a second run with the same `COMMANDS`",
        "CASE-4 — Given  any existing CLI command (e.g. `init`, `gate`, `sync`) When   it is invoked via the standard CLI entry point Then   it executes without error — no regression from the registry migration",
        "CASE-5 — Given  the generated artifacts are inspected for imports When   the generator code runs Then   only stdlib modules are imported; no third-party dependency is present"
      ],
      "accept": "CASE-1\n  Given  the `COMMANDS` registry and the live argparse parser\n  When   `_cli_choices()` is called\n  Then   its return value equals `list(COMMANDS)` (insertion order) — no literal choices exist\n\nCASE-2\n  Given  a committed `tool_definition.json` whose content differs from a fresh generation\n  When   `gate` runs\n  Then   the gate exits non-zero (stale artifact is a hard error)\n\nCASE-3\n  Given  `gen-integration` is run\n  When   `tool_definition.json` and the `SKILL.universal.md` command table are written\n  Then   their content is byte-for-byte reproducible on a second run with the same `COMMANDS`\n\nCASE-4\n  Given  any existing CLI command (e.g. `init`, `gate`, `sync`)\n  When   it is invoked via the standard CLI entry point\n  Then   it executes without error — no regression from the registry migration\n\nCASE-5\n  Given  the generated artifacts are inspected for imports\n  When   the generator code runs\n  Then   only stdlib modules are imported; no third-party dependency is present",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001"
      ],
      "used_by": [
        "ARCH-DESIGN-061",
        "ARCH-PIPE-046"
      ],
      "satisfies": [
        "SYS-SHIP-108"
      ],
      "satisfied_by": [
        "REQ-CMDREGISTRY-834",
        "REQ-CMDREGISTRY-963"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:262"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:616"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:649"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6029"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8848"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10464"
        }
      ],
      "test_exempt": null,
      "milestone": "v2.7",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Registrul de comenzi CLI + artefacte de integrare generate",
          "intent": "`tool_definition.json` și tabelul de comenzi din `SKILL.universal.md` erau oglinzi întreținute manual ale CLI-ului și sufereau drift în tăcere de fiecare dată când o comandă era adăugată sau redenumită. Un singur registru declarativ `COMMANDS` — cu generare și o gardă de drift în gate — face ca acele oglinzi să nu mai poată diverge nedetectate.",
          "contract": "> `tool_definition.json` și tabelul de comenzi din `SKILL.universal.md` erau oglinzi\n> întreținute manual ale CLI-ului și sufereau drift în tăcere de fiecare dată când o\n> comandă era adăugată sau redenumită. Un singur registru declarativ `COMMANDS` — cu\n> generare și o gardă de drift în gate — face ca acele oglinzi să nu mai poată diverge\n> nedetectate.\n\nFiecare punct de mai jos este obligatoriu.\n- Un dicționar `COMMANDS` este sursa unică de adevăr pentru comenzile CLI-ului: opțiunile argparse, `tool_definition.json` generat, și tabelul de comenzi din `SKILL.universal.md` derivă toate din el, iar gate-ul eșuează atunci când un artefact generat devine învechit. [[REQ-CMDREGISTRY-834]]\n- Registrul este de asemenea emis ca date în hartă, astfel încât o suprafață poate documenta CLI-ul fără să îl ruleze. [[REQ-CMDREGISTRY-963]]",
          "acceptance": "CASE-1\n  Given  registrul `COMMANDS` și parserul argparse activ\n  When   `_cli_choices()` este apelat\n  Then   valoarea returnată este egală cu `list(COMMANDS)` (ordinea de inserare) — nu există opțiuni literale\n\nCASE-2\n  Given  un `tool_definition.json` commis al cărui conținut diferă de o generare proaspătă\n  When   `gate` rulează\n  Then   gate-ul iese cu cod diferit de zero (artefactul învechit este o eroare fermă)\n\nCASE-3\n  Given  `gen-integration` este rulat\n  When   `tool_definition.json` și tabelul de comenzi din `SKILL.universal.md` sunt scrise\n  Then   conținutul lor este reproductibil octet-cu-octet la o a doua rulare cu același `COMMANDS`\n\nCASE-4\n  Given  orice comandă CLI existentă (de ex. `init`, `gate`, `sync`)\n  When   este invocată prin punctul de intrare CLI standard\n  Then   se execută fără eroare — nicio regresie din migrarea registrului\n\nCASE-5\n  Given  artefactele generate sunt inspectate pentru importuri\n  When   codul generatorului rulează\n  Then   sunt importate doar module stdlib; nicio dependență terță nu este prezentă"
        }
      }
    },
    {
      "id": "REQ-CMDREGISTRY-834",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "One COMMANDS dict drives argparse, schema and docs",
      "intent": "`tool_definition.json` and the `SKILL.universal.md` command table used to be hand-maintained mirrors of the CLI that silently drifted whenever a command was added or renamed. Deriving argparse's choices, the generated function-calling schema, and the docs table from one `COMMANDS` dict — plus a gate check that fails on staleness — makes that kind of drift impossible to miss.",
      "contract": [
        "A `COMMANDS` dict is the single source of truth for the CLI command set; no other location may enumerate commands authoritatively.",
        "Argparse choices are derived from `COMMANDS` at runtime; no hard-coded choices literal is permitted.",
        "`tool_definition.json` (the function-calling schema) is generated from `COMMANDS` by the `gen-integration` command.",
        "The `SKILL.universal.md` command table is generated from `COMMANDS` by `gen-integration` and written into the `<!--##REQMAP:COMMANDS##-->` region; prose outside that region is never touched.",
        "The `SKILL.md` command list is generated the same way, into the same region marker, as a grouped bullet list. It is the contract an assistant reads on a fresh repo, so a verb that exists is documented there and one that does not, is not.",
        "Internal commands (e.g. `gen-integration`) are excluded from AI-facing generated artifacts.",
        "The gate fails (exit non-zero) when a committed generated artifact is stale relative to a fresh generation.",
        "All generators and the gate check are stdlib-only; no third-party imports are permitted."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — argparse choices trace back to COMMANDS with no hardcoded list Given  the live argparse parser built from `COMMANDS` When   `_cli_choices()` is called Then   its return value equals `list(COMMANDS)` in insertion order",
        "CASE-2 — — gen-integration derives tool_definition.json from COMMANDS Given  a `COMMANDS` entry with a summary, a positional arg and a flag When   `gen-integration` runs Then   `tool_definition.json` carries a matching function-calling schema entry for that command",
        "CASE-3 — — gen-integration rewrites only the marked command-table region Given  `SKILL.universal.md` with hand-written prose outside `` When   `gen-integration` runs Then   the region's table refreshes, the surrounding prose is byte-identical, and the written body carries the file's own line endings, so a rerun leaves no diff",
        "CASE-4 — — gen-integration omits internal commands from AI-facing output Given  `COMMANDS` including the internal `gen-integration` entry itself When   `gen-integration` runs Then   `tool_definition.json` lists no `gen-integration` function",
        "CASE-5 — — gate fails on a stale generated artifact Given  a committed `tool_definition.json` that differs from a fresh generation When   `gate` runs Then   it exits non-zero, naming the stale artifact",
        "CASE-6 — — the generator and gate-check code import no third-party module Given  the source of `_generate_schema`, `_generate_command_table` and `_check_integration_fresh` When   their imports are inspected Then   every import resolves to the Python standard library",
        "CASE-7 — — SKILL.md documents exactly the registry Given  the committed `SKILL.md` When   its command region is compared with a fresh rendering from `COMMANDS` Then   they match, every non-internal verb appears exactly once, and no other verb appears"
      ],
      "accept": "CASE-1 — argparse choices trace back to COMMANDS with no hardcoded list\n  Given  the live argparse parser built from `COMMANDS`\n  When   `_cli_choices()` is called\n  Then   its return value equals `list(COMMANDS)` in insertion order\n\nCASE-2 — gen-integration derives tool_definition.json from COMMANDS\n  Given  a `COMMANDS` entry with a summary, a positional arg and a flag\n  When   `gen-integration` runs\n  Then   `tool_definition.json` carries a matching function-calling schema entry for that command\n\nCASE-3 — gen-integration rewrites only the marked command-table region\n  Given  `SKILL.universal.md` with hand-written prose outside `<!--##REQMAP:COMMANDS##-->`\n  When   `gen-integration` runs\n  Then   the region's table refreshes, the surrounding prose is byte-identical, and the\n         written body carries the file's own line endings, so a rerun leaves no diff\n\nCASE-4 — gen-integration omits internal commands from AI-facing output\n  Given  `COMMANDS` including the internal `gen-integration` entry itself\n  When   `gen-integration` runs\n  Then   `tool_definition.json` lists no `gen-integration` function\n\nCASE-5 — gate fails on a stale generated artifact\n  Given  a committed `tool_definition.json` that differs from a fresh generation\n  When   `gate` runs\n  Then   it exits non-zero, naming the stale artifact\n\nCASE-6 — the generator and gate-check code import no third-party module\n  Given  the source of `_generate_schema`, `_generate_command_table` and `_check_integration_fresh`\n  When   their imports are inspected\n  Then   every import resolves to the Python standard library\n\nCASE-7 — SKILL.md documents exactly the registry\n  Given  the committed `SKILL.md`\n  When   its command region is compared with a fresh rendering from `COMMANDS`\n  Then   they match, every non-internal verb appears exactly once, and no other verb appears",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-CMDREGISTRY-033"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:611"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:672"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:684"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:726"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:747"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:774"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6029"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8848"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 7,
      "covered": 7
    },
    {
      "id": "REQ-CMDREGISTRY-963",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The command registry as data on the map",
      "intent": "A command reference kept by hand goes stale the first time a verb is renamed — which is exactly what `v4.0.0` did to eleven of them. Emitting the registry onto `_map.json` means the reference a reader sees was generated from the same table the parser was built from, so a command that exists is documented and one that was removed disappears on the next `sync`.",
      "contract": [
        "The map payload carries a `commands` list generated from the command registry, one entry per user-facing command, with its name, its positional argument, its summary and its flags.",
        "Each entry names the moment of work it belongs to — authoring, building or reading — from a grouping declared once beside the registry rather than restated per surface.",
        "A command marked internal is absent from the list, exactly as it is absent from the generated schema and the command table.",
        "A map produced before this field existed carries no `commands` key, and every reader treats its absence as \"no reference available\" rather than as an error."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — every user-facing command appears Given  the command registry When   the manifest is generated Then   it holds one entry per non-internal command, and none for an internal one",
        "CASE-2 — — an entry carries what a reader needs Given  a command that takes an argument and two flags When   its manifest entry is read Then   the entry carries its name, argument, summary and both flags with their help text",
        "CASE-3 — — each command is placed in a group Given  the manifest When   its entries are inspected Then   every entry names one of the declared groups"
      ],
      "accept": "CASE-1 — every user-facing command appears\n  Given  the command registry\n  When   the manifest is generated\n  Then   it holds one entry per non-internal command, and none for an internal one\n\nCASE-2 — an entry carries what a reader needs\n  Given  a command that takes an argument and two flags\n  When   its manifest entry is read\n  Then   the entry carries its name, argument, summary and both flags with their help text\n\nCASE-3 — each command is placed in a group\n  Given  the manifest\n  When   its entries are inspected\n  Then   every entry names one of the declared groups",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-CMDREGISTRY-033"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:649"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10464"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Registrul de comenzi ca date pe hartă",
          "intent": "O referință de comenzi întreținută manual devine învechită de îndată ce un verb este redenumit — exact ceea ce `v4.0.0` a făcut cu unsprezece dintre ele. Emiterea registrului în `_map.json` înseamnă că referința pe care o vede cititorul a fost generată din același tabel din care a fost construit parserul, astfel încât o comandă care există este documentată, iar una care a fost eliminată dispare la următorul `sync`.",
          "contract": "> O referință de comenzi întreținută manual devine învechită de îndată ce un verb este redenumit — exact ceea ce\n> `v4.0.0` a făcut cu unsprezece dintre ele. Emiterea registrului în `_map.json` înseamnă că referința\n> pe care o vede cititorul a fost generată din același tabel din care a fost construit parserul, astfel încât o comandă\n> care există este documentată, iar una care a fost eliminată dispare la următorul `sync`.\n\nFiecare punct de mai jos este obligatoriu.\n- Payload-ul hărții conține o listă `commands` generată din registrul de comenzi, câte o intrare pentru\n  fiecare comandă orientată către utilizator, cu numele, argumentul pozițional, rezumatul și flag-urile ei.\n- Fiecare intrare numește momentul de lucru căruia îi aparține — redactare, construire sau citire — dintr-o\n  grupare declarată o singură dată lângă registru, nu repetată pentru fiecare suprafață.\n- O comandă marcată internă lipsește din listă, exact așa cum lipsește din schema generată și din\n  tabelul de comenzi.\n- O hartă produsă înainte ca acest câmp să existe nu conține cheia `commands`, iar orice cititor tratează\n  absența ei ca „nicio referință disponibilă\" nu ca o eroare.",
          "acceptance": "CASE-1 — fiecare comandă orientată către utilizator apare\n  Given  registrul de comenzi\n  When   manifestul este generat\n  Then   conține câte o intrare pentru fiecare comandă non-internă și niciuna pentru o comandă internă\n\nCASE-2 — o intrare conține ce are nevoie un cititor\n  Given  o comandă care primește un argument și două flag-uri\n  When   este citită intrarea ei din manifest\n  Then   intrarea conține numele, argumentul, rezumatul și ambele flag-uri, cu textul lor de ajutor\n\nCASE-3 — fiecare comandă este plasată într-un grup\n  Given  manifestul\n  When   intrările lui sunt inspectate\n  Then   fiecare intrare numește unul dintre grupurile declarate"
        }
      }
    },
    {
      "id": "ARCH-CONFIG-060",
      "layer": "bus",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Per-repo configuration file",
      "intent": "Every threshold the engine judges by (`LINT_AC_MAX`, `SIMILAR_THRESHOLD`, `ORPHAN_CODE_MIN_LOC`, ...) was a module constant, so a consumer could change none of them without forking `reqmap.py`. A small JSON file next to the requirements sets the named constants at startup, read fail-open, so a repo tunes the engine without rewiring it.",
      "contract": [
        "`requirements/_config.json` overrides the constants named in `CONFIG_KEYS` and extends the scanned extensions; a missing, malformed or wrongly-typed entry never breaks a command. [[REQ-CONFIG-949]]"
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — Given  a `requirements/_config.json` setting `LINT_AC_MAX` to 12 When   any command starts Then   `lint` and `next` judge oversize at 12 criteria",
        "CASE-2 — Given  a config file that is not valid JSON When   any command starts Then   the defaults apply and the command runs",
        "CASE-3 — Given  a config key the engine does not know When   any command starts Then   one line on stderr names the ignored key and nothing else changes --------------------"
      ],
      "accept": "CASE-1\n  Given  a `requirements/_config.json` setting `LINT_AC_MAX` to 12\n  When   any command starts\n  Then   `lint` and `next` judge oversize at 12 criteria\n\nCASE-2\n  Given  a config file that is not valid JSON\n  When   any command starts\n  Then   the defaults apply and the command runs\n\nCASE-3\n  Given  a config key the engine does not know\n  When   any command starts\n  Then   one line on stderr names the ignored key and nothing else changes\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001"
      ],
      "used_by": [
        "ARCH-DESIGN-061"
      ],
      "satisfies": [
        "SYS-AUTHOR-101"
      ],
      "satisfied_by": [
        "REQ-CONFIG-949"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8994"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:9004"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:9194"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7936"
        }
      ],
      "test_exempt": null,
      "milestone": "v3.3",
      "priority": "should-have",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Fișier de configurare per-repo",
          "intent": "Fiecare prag după care judecă motorul (`LINT_AC_MAX`, `SIMILAR_THRESHOLD`, `ORPHAN_CODE_MIN_LOC`, ...) era o constantă de modul, astfel încât un consumator nu putea schimba niciuna dintre ele fără a face fork la `reqmap.py`. Un fișier JSON mic, alături de cerințe, setează constantele numite la pornire, citit fail-open, astfel încât un repo reglează motorul fără a-l recabla.",
          "contract": "> Fiecare prag după care judecă motorul (`LINT_AC_MAX`, `SIMILAR_THRESHOLD`,\n> `ORPHAN_CODE_MIN_LOC`, ...) era o constantă de modul, astfel încât un consumator nu\n> putea schimba niciuna dintre ele fără a face fork la `reqmap.py`. Un fișier JSON mic,\n> alături de cerințe, setează constantele numite la pornire, citit fail-open, astfel\n> încât un repo reglează motorul fără a-l recabla.\n\nFiecare punct de mai jos este obligatoriu.\n- `requirements/_config.json` suprascrie constantele numite în `CONFIG_KEYS` și extinde extensiile scanate; o intrare lipsă, malformată sau cu tip greșit nu strică niciodată o comandă. [[REQ-CONFIG-949]]",
          "acceptance": "CASE-1\n  Given  un `requirements/_config.json` care setează `LINT_AC_MAX` la 12\n  When   orice comandă pornește\n  Then   `lint` și `next` judecă supradimensionarea la 12 criterii\n\nCASE-2\n  Given  un fișier de configurare care nu este JSON valid\n  When   orice comandă pornește\n  Then   valorile implicite se aplică și comanda rulează\n\nCASE-3\n  Given  o cheie de configurare pe care motorul nu o cunoaște\n  When   orice comandă pornește\n  Then   o linie pe stderr numește cheia ignorată și nimic altceva nu se schimbă\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-CONFIG-949",
      "layer": "bus",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Reading and applying `_config.json`",
      "intent": "`load_config` reads the file; `apply_config` sets module constants from it. The split keeps the file format and the application rules testable on their own, and `main` calls both once before any command runs. A silently-ignored typo would be worse than no config at all, so an unknown or mistyped key is reported.",
      "contract": [
        "`load_config(reqs_dir)` returns the parsed object from `requirements/_config.json`, or `{}` when the file is absent, unreadable, not JSON, or not a JSON object.",
        "`apply_config(cfg)` sets each key named in `CONFIG_KEYS` on the module and returns the list of names it applied.",
        "A numeric constant accepts a number of the same kind; a dictionary constant such as `LINT_FANOUT_BANDS` is merged key by key, a JSON list becoming a tuple.",
        "`extra_code_exts`, a list of extensions with or without the leading dot, is appended to `CODE_EXTS`, so every scan site sees the new file types.",
        "A key not in `CONFIG_KEYS`, or a value of the wrong type, is reported on stderr as `config: ignoring ...` and skipped; every other key still applies.",
        "`main` calls `apply_config(load_config(reqs_dir))` before loading requirements or scanning, so every command reads the configured values."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a numeric threshold applies Given  `{\"LINT_AC_MAX\": 12}` When   `apply_config` runs Then   it returns `[\"LINT_AC_MAX\"]`, the constant reads 12 and stderr stays empty",
        "CASE-2 — — unknown and mistyped keys are reported and skipped Given  `{\"NOPE\": 1, \"LINT_AC_MAX\": \"seven\"}` When   `apply_config` runs Then   nothing is applied, the constant keeps its default and stderr names both keys",
        "CASE-3 — — dictionaries merge and extensions extend Given  `{\"LINT_FANOUT_BANDS\": {\"system\": [null, 12]}, \"extra_code_exts\": [\"foo\", \".bar\"]}` When   `apply_config` runs Then   the `system` band reads `(None, 12)`, the `architecture` band is unchanged and `x.foo` counts as a code file",
        "CASE-4 — — the file is read fail-open Given  no file, a malformed file, a JSON list, and a valid object in turn When   `load_config` reads each Then   it returns `{}` for the first three and the object for the last"
      ],
      "accept": "CASE-1 — a numeric threshold applies\n  Given  `{\"LINT_AC_MAX\": 12}`\n  When   `apply_config` runs\n  Then   it returns `[\"LINT_AC_MAX\"]`, the constant reads 12 and stderr stays empty\n\nCASE-2 — unknown and mistyped keys are reported and skipped\n  Given  `{\"NOPE\": 1, \"LINT_AC_MAX\": \"seven\"}`\n  When   `apply_config` runs\n  Then   nothing is applied, the constant keeps its default and stderr names both keys\n\nCASE-3 — dictionaries merge and extensions extend\n  Given  `{\"LINT_FANOUT_BANDS\": {\"system\": [null, 12]}, \"extra_code_exts\": [\"foo\", \".bar\"]}`\n  When   `apply_config` runs\n  Then   the `system` band reads `(None, 12)`, the `architecture` band is unchanged and `x.foo` counts as a code file\n\nCASE-4 — the file is read fail-open\n  Given  no file, a malformed file, a JSON list, and a valid object in turn\n  When   `load_config` reads each\n  Then   it returns `{}` for the first three and the object for the last",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-CONFIG-060"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8994"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:9004"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7936"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Citirea și aplicarea `_config.json`",
          "intent": "`load_config` citește fișierul; `apply_config` setează constantele modulului pe baza lui. Separarea păstrează formatul fișierului și regulile de aplicare testabile independent, iar `main` apelează ambele o singură dată înainte de rularea oricărei comenzi. O greșeală de scriere ignorată silențios ar fi mai rea decât lipsa completă a configurației, așa că o cheie necunoscută sau scrisă greșit este raportată.",
          "contract": "> `load_config` citește fișierul; `apply_config` setează constantele modulului pe baza lui. Separarea\n> păstrează formatul fișierului și regulile de aplicare testabile independent, iar `main`\n> apelează ambele o singură dată înainte de rularea oricărei comenzi. O greșeală de scriere ignorată silențios\n> ar fi mai rea decât lipsa completă a configurației, așa că o cheie necunoscută sau scrisă greșit este raportată.\n\nFiecare punct de mai jos este obligatoriu.\n- `load_config(reqs_dir)` returnează obiectul parsat din `requirements/_config.json`, sau `{}` când fișierul este absent, nu poate fi citit, nu este JSON sau nu este un obiect JSON.\n- `apply_config(cfg)` setează pe modul fiecare cheie numită în `CONFIG_KEYS` și returnează lista numelor pe care le-a aplicat.\n- O constantă numerică acceptă un număr de același tip; o constantă de tip dicționar precum `LINT_FANOUT_BANDS` este îmbinată cheie cu cheie, o listă JSON devenind un tuple.\n- `extra_code_exts`, o listă de extensii cu sau fără punctul inițial, este adăugată la `CODE_EXTS`, astfel încât fiecare loc de scanare vede noile tipuri de fișiere.\n- O cheie care nu se află în `CONFIG_KEYS`, sau o valoare de tip greșit, este raportată pe stderr ca `config: ignoring ...` și este ignorată; orice altă cheie tot se aplică.\n- `main` apelează `apply_config(load_config(reqs_dir))` înainte de a încărca cerințele sau de a scana, astfel încât fiecare comandă citește valorile configurate.",
          "acceptance": "CASE-1 — un prag numeric se aplică\n  Given  `{\"LINT_AC_MAX\": 12}`\n  When   `apply_config` rulează\n  Then   returnează `[\"LINT_AC_MAX\"]`, constanta are valoarea 12, iar stderr rămâne gol\n\nCASE-2 — cheile necunoscute și cele scrise greșit sunt raportate și ignorate\n  Given  `{\"NOPE\": 1, \"LINT_AC_MAX\": \"seven\"}`\n  When   `apply_config` rulează\n  Then   nu se aplică nimic, constanta își păstrează valoarea implicită, iar stderr numește ambele chei\n\nCASE-3 — dicționarele se îmbină, iar extensiile se extind\n  Given  `{\"LINT_FANOUT_BANDS\": {\"system\": [null, 12]}, \"extra_code_exts\": [\"foo\", \".bar\"]}`\n  When   `apply_config` rulează\n  Then   banda `system` are valoarea `(None, 12)`, banda `architecture` rămâne neschimbată, iar `x.foo` contează ca fișier de cod\n\nCASE-4 — fișierul este citit fail-open\n  Given  pe rând: niciun fișier, un fișier malformat, o listă JSON și un obiect valid\n  When   `load_config` citește fiecare caz\n  Then   returnează `{}` pentru primele trei și obiectul pentru ultimul"
        }
      }
    },
    {
      "id": "ARCH-CONTEXT-048",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Consolidated Context section",
      "intent": "A requirement author choosing between `## WHAT — Notes`, `## Example — in practice` and `## WHERE — Current implementation` was picking between three near-synonymous informative buckets, not making a decision about the contract. This narrows that choice to one section — `## Context` — while keeping every existing requirement file that still uses the three separate headings working exactly as it does today.",
      "contract": [
        "`new`'s built-in template scaffolds one `## Context` section (bold `**Notes**`/`**Example**`/`**Current implementation**` sub-groups) for every newly-created requirement, while the legacy three-heading form remains fully valid and unhashed either way. [[REQ-CONTEXT-835]]"
      ],
      "verify": [],
      "notes": [
        "The two draft-emitting code paths (`draft`'s prose-capability and code-capability branches) were updated to scaffold the Context form too, so a promoted draft needs no reshaping — matching the existing \"emission schema matches `REQUIREMENT_TEMPLATE`\" intent.",
        "[ADR-0017](../../docs/adr/0017-consolidated-context-section.md) records the decision and the Senate audit it came out of, including why the `>` WHY blockquote was deliberately left alone (it is not heading-based — `_first_quote` reads the first blockquote in the file regardless of section — so merging it into `## Context` would not have simplified anything a parser cares about)."
      ],
      "current_impl": [
        "`_context_group` in `reqmap.py`, called from `_build_map_data`'s `notes`/`current_impl` emission as a fallback after `_bullets` on the legacy heading.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a requirement body with `## Context (non-binding)` holding `**Notes**` and `**Current implementation**` sub-groups When   `_context_group(body, \"notes\")` and `_context_group(body, \"current implementation\")` run Then   each returns exactly the bullets under its own sub-group",
        "CASE-2 — Given  a requirement body with no `## Context` section at all (a legacy-form or Contract-only body) When   `_context_group` runs for any label Then   it returns an empty list",
        "CASE-3 — Given  a `## Context` section that has an `**Example**` sub-group but no `**Notes**` sub-group When   `_context_group(body, \"notes\")` runs Then   it returns an empty list rather than the Example bullets",
        "CASE-4 — Given  a confirmed requirement using only the Context form When   `map` regenerates `_map.json` Then   the node's `notes` and `current_impl` fields are populated from the matching Context sub-groups",
        "CASE-5 — Given  a confirmed requirement using the legacy `## WHAT — Notes` heading When   `map` regenerates `_map.json` Then   the node's `notes` field is populated from that legacy heading, unchanged from before this requirement existed"
      ],
      "accept": "CASE-1\n  Given  a requirement body with `## Context (non-binding)` holding `**Notes**` and\n         `**Current implementation**` sub-groups\n  When   `_context_group(body, \"notes\")` and `_context_group(body, \"current implementation\")` run\n  Then   each returns exactly the bullets under its own sub-group\n\nCASE-2\n  Given  a requirement body with no `## Context` section at all (a legacy-form or\n         Contract-only body)\n  When   `_context_group` runs for any label\n  Then   it returns an empty list\n\nCASE-3\n  Given  a `## Context` section that has an `**Example**` sub-group but no `**Notes**` sub-group\n  When   `_context_group(body, \"notes\")` runs\n  Then   it returns an empty list rather than the Example bullets\n\nCASE-4\n  Given  a confirmed requirement using only the Context form\n  When   `map` regenerates `_map.json`\n  Then   the node's `notes` and `current_impl` fields are populated from the matching\n         Context sub-groups\n\nCASE-5\n  Given  a confirmed requirement using the legacy `## WHAT — Notes` heading\n  When   `map` regenerates `_map.json`\n  Then   the node's `notes` field is populated from that legacy heading, unchanged from\n         before this requirement existed",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-MAP-007"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-AUTHOR-101"
      ],
      "satisfied_by": [
        "REQ-CONTEXT-835"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6413"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1678"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8829"
        }
      ],
      "test_exempt": null,
      "milestone": "v2.30",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Secțiune Context consolidată",
          "intent": "Un autor de cerințe care alegea între `## WHAT — Notes`, `## Example — in practice` și `## WHERE — Current implementation` alegea de fapt între trei categorii informative aproape sinonime, nu lua o decizie despre contract. Aceasta restrânge acea alegere la o singură secțiune — `## Context` — păstrând în același timp fiecare fișier de cerință existent care încă folosește cele trei titluri separate să funcționeze exact ca astăzi.",
          "contract": "> Un autor de cerințe care alegea între `## WHAT — Notes`, `## Example — in practice` și\n> `## WHERE — Current implementation` alegea de fapt între trei categorii informative\n> aproape sinonime, nu lua o decizie despre contract. Aceasta restrânge acea alegere la\n> o singură secțiune — `## Context` — păstrând în același timp fiecare fișier de cerință\n> existent care încă folosește cele trei titluri separate să funcționeze exact ca astăzi.\n\nFiecare punct de mai jos este obligatoriu.\n- Șablonul încorporat al lui `new` schelează o singură secțiune `## Context` (subgrupuri îngroșate `**Notes**`/`**Example**`/`**Current implementation**`) pentru fiecare cerință nou-creată, în timp ce forma veche cu trei titluri rămâne pe deplin validă și nehash-uită în ambele cazuri. [[REQ-CONTEXT-835]]",
          "acceptance": "CASE-1\n  Given  un corp de cerință cu `## Context (non-binding)` conținând subgrupurile `**Notes**` și\n         `**Current implementation**`\n  When   `_context_group(body, \"notes\")` și `_context_group(body, \"current implementation\")` rulează\n  Then   fiecare returnează exact punctele din subgrupul propriu\n\nCASE-2\n  Given  un corp de cerință fără nicio secțiune `## Context` (o formă veche sau\n         un corp doar cu Contract)\n  When   `_context_group` rulează pentru orice etichetă\n  Then   returnează o listă goală\n\nCASE-3\n  Given  o secțiune `## Context` care are un subgrup `**Example**` dar niciun subgrup `**Notes**`\n  When   `_context_group(body, \"notes\")` rulează\n  Then   returnează o listă goală în loc de punctele Example\n\nCASE-4\n  Given  o cerință confirmată care folosește doar forma Context\n  When   `map` regenerează `_map.json`\n  Then   câmpurile `notes` și `current_impl` ale nodului sunt populate din subgrupurile\n         Context corespunzătoare\n\nCASE-5\n  Given  o cerință confirmată care folosește vechiul titlu `## WHAT — Notes`\n  When   `map` regenerează `_map.json`\n  Then   câmpul `notes` al nodului este populat din acel titlu vechi, neschimbat față de\n         cum era înainte ca această cerință să existe"
        }
      }
    },
    {
      "id": "REQ-CONTEXT-835",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "One Context section replaces three near-synonymous headings",
      "intent": "Choosing between `## WHAT — Notes`, `## Example — in practice` and `## WHERE — Current implementation` was picking between three informative buckets that meant almost the same thing, not making a decision about the contract. `new` now scaffolds one `## Context` section with bold sub-group labels instead, while every existing file using the three old headings keeps working unchanged — neither form is hashed, linted, or read by name outside the map's `notes`/`current_impl` fallback.",
      "contract": [
        "`new`'s built-in template scaffolds the Context form for every newly-created requirement.",
        "The Context form groups sub-topics with a bold label line (`**Notes**`, `**Example**`, `**Current implementation**`), the same clause-group convention the Contract section already uses for five-plus clauses.",
        "`_context_group(body, label)` returns the bullets under one bold sub-group inside `## Context`, or an empty list when that sub-group or the section itself is absent.",
        "The legacy form remains fully valid. Nothing in the gate, the lint checks, or the drift hash reads `## WHAT — Notes`, `## Example — in practice` or `## WHERE — Current implementation` by name, so no existing requirement file needs to change.",
        "`_build_map_data`'s `notes` and `current_impl` fields try the legacy heading first (`_bullets`), and fall back to the matching Context sub-group only when the legacy heading is absent — never both at once in one file, so the fallback cannot mask real content a legacy-form file already has.",
        "`## Context` and its sub-groups are commentary: not hashed by `binding_hash`, not read by `_has_section`, and not linted. Neither form can trip drift or `missing-section`."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a newly scaffolded requirement carries the Context form Given  no requirement named `AREA-X-001` When   `new AREA-X-001` runs Then   the created file holds a `## Context (non-binding)` section",
        "CASE-2 — — bold sub-group labels keep each group's bullets separate Given  a `## Context` section with `**Notes**` bullets followed by `**Current implementation**` bullets When   `_context_group(body, \"notes\")` and `_context_group(body, \"current implementation\")` run Then   each call returns only the bullets under its own label, none from the other",
        "CASE-3 — — an absent sub-group returns empty instead of a neighboring group's bullets Given  a `## Context` section with an `**Example**` sub-group but no `**Notes**` sub-group When   `_context_group(body, \"notes\")` runs Then   it returns `[]`, not the Example bullets",
        "CASE-4 — — notes come from the legacy heading, not the Context fallback, when present Given  a confirmed requirement carrying `## WHAT — Notes` with a legacy note (no `## Context` section) When   `map` regenerates `_map.json` via `_build_map_data` Then   the node's `notes` field holds the legacy bullet, unchanged from before this feature, because the gate, the lint checks and the drift hash never read that heading by name either — the file needs no change",
        "CASE-5 — — editing only the Context bullets leaves binding_hash unchanged Given  two bodies identical except for the bullets under a `## Context (non-binding)` section When   `binding_hash` runs on each body Then   both hashes are equal — the Context edit produces no drift"
      ],
      "accept": "CASE-1 — a newly scaffolded requirement carries the Context form\n  Given  no requirement named `AREA-X-001`\n  When   `new AREA-X-001` runs\n  Then   the created file holds a `## Context (non-binding)` section\n\nCASE-2 — bold sub-group labels keep each group's bullets separate\n  Given  a `## Context` section with `**Notes**` bullets followed by `**Current\n         implementation**` bullets\n  When   `_context_group(body, \"notes\")` and `_context_group(body, \"current implementation\")` run\n  Then   each call returns only the bullets under its own label, none from the other\n\nCASE-3 — an absent sub-group returns empty instead of a neighboring group's bullets\n  Given  a `## Context` section with an `**Example**` sub-group but no `**Notes**` sub-group\n  When   `_context_group(body, \"notes\")` runs\n  Then   it returns `[]`, not the Example bullets\n\nCASE-4 — notes come from the legacy heading, not the Context fallback, when present\n  Given  a confirmed requirement carrying `## WHAT — Notes` with a legacy note (no `##\n         Context` section)\n  When   `map` regenerates `_map.json` via `_build_map_data`\n  Then   the node's `notes` field holds the legacy bullet, unchanged from before this\n         feature, because the gate, the lint checks and the drift hash never read that\n         heading by name either — the file needs no change\n\nCASE-5 — editing only the Context bullets leaves binding_hash unchanged\n  Given  two bodies identical except for the bullets under a `## Context (non-binding)` section\n  When   `binding_hash` runs on each body\n  Then   both hashes are equal — the Context edit produces no drift",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-CONTEXT-048"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6413"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1678"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8829"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "O singură secțiune Context înlocuiește trei titluri aproape sinonime",
          "intent": "Alegerea între `## WHAT — Notes`, `## Example — in practice` și `## WHERE — Current implementation` însemna alegerea între trei categorii informative care spuneau aproape același lucru, nu luarea unei decizii despre contract. `new` schițează acum o singură secțiune `## Context` cu etichete de subgrup îngroșate, în timp ce orice fișier existent care folosește cele trei titluri vechi continuă să funcționeze neschimbat — niciuna dintre forme nu este hash-uită, verificată de lint sau citită după nume în afara mecanismului de rezervă `notes`/`current_impl` al hărții.",
          "contract": "> Alegerea între `## WHAT — Notes`, `## Example — in practice` și `## WHERE — Current\n> implementation` însemna alegerea între trei categorii informative care spuneau aproape același\n> lucru, nu luarea unei decizii despre contract. `new` schițează acum o singură secțiune `## Context`\n> cu etichete de subgrup îngroșate, în timp ce orice fișier existent care folosește cele trei titluri vechi\n> continuă să funcționeze neschimbat — niciuna dintre forme nu este hash-uită, verificată de lint sau citită după nume în afara\n> mecanismului de rezervă `notes`/`current_impl` al hărții.\n\nFiecare punct de mai jos este obligatoriu.\n- Șablonul integrat al lui `new` schițează forma Context pentru fiecare cerință nou-creată.\n- Forma Context grupează subtopicuri printr-o linie de etichetă îngroșată (`**Notes**`, `**Example**`,\n  `**Current implementation**`), aceeași convenție de grupare a clauzelor pe care secțiunea Contract\n  o folosește deja pentru cinci sau mai multe clauze.\n- `_context_group(body, label)` returnează punctele aflate sub un subgrup îngroșat din interiorul\n  secțiunii `## Context`, sau o listă goală când acel subgrup sau secțiunea însăși lipsește.\n- Forma veche rămâne pe deplin validă. Nimic din gate, din verificările de lint sau din hash-ul de\n  drift nu citește `## WHAT — Notes`, `## Example — in practice` sau `## WHERE — Current\n  implementation` după nume, așa că niciun fișier de cerință existent nu trebuie modificat.\n- `_build_map_data`: câmpurile `notes` și `current_impl` încearcă mai întâi titlul vechi\n  (`_bullets`) și trec la subgrupul Context corespunzător doar când titlul vechi lipsește — niciodată\n  amândouă simultan într-un fișier, astfel încât mecanismul de rezervă nu poate masca un conținut\n  real pe care un fișier în forma veche îl are deja.\n- `## Context` și subgrupurile sale sunt comentarii: nu sunt hash-uite de `binding_hash`, nu sunt citite de\n  `_has_section` și nu sunt verificate de lint. Niciuna dintre forme nu poate declanșa drift sau `missing-section`.",
          "acceptance": "CASE-1 — o cerință nou schițată conține forma Context\n  Given  nicio cerință numită `AREA-X-001`\n  When   `new AREA-X-001` rulează\n  Then   fișierul creat conține o secțiune `## Context (non-binding)`\n\nCASE-2 — etichetele de subgrup îngroșate păstrează separate punctele fiecărui grup\n  Given  o secțiune `## Context` cu puncte `**Notes**` urmate de puncte `**Current\n         implementation**`\n  When   `_context_group(body, \"notes\")` și `_context_group(body, \"current implementation\")` rulează\n  Then   fiecare apel returnează doar punctele de sub propria etichetă, niciunul din celălalt\n\nCASE-3 — un subgrup absent returnează gol, nu punctele grupului vecin\n  Given  o secțiune `## Context` cu un subgrup `**Example**` dar fără subgrup `**Notes**`\n  When   `_context_group(body, \"notes\")` rulează\n  Then   returnează `[]`, nu punctele Example\n\nCASE-4 — notele provin din titlul vechi, nu din mecanismul de rezervă Context, atunci când acesta este prezent\n  Given  o cerință confirmată care conține `## WHAT — Notes` cu o notă veche (fără `##\n         Context` secțiune)\n  When   `map` regenerează `_map.json` prin `_build_map_data`\n  Then   câmpul `notes` al nodului conține punctul vechi, neschimbat față de dinainte de această\n         funcționalitate, pentru că nici gate, nici verificările de lint, nici hash-ul de drift nu au citit\n         vreodată acel titlu după nume — fișierul nu are nevoie de nicio modificare\n\nCASE-5 — editarea doar a punctelor Context lasă binding_hash neschimbat\n  Given  două corpuri identice cu excepția punctelor de sub o secțiune `## Context (non-binding)`\n  When   `binding_hash` rulează pe fiecare corp\n  Then   ambele hash-uri sunt egale — editarea Context nu produce drift"
        }
      }
    },
    {
      "id": "ARCH-COVERAGE-029",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Untagged-code coverage signal",
      "intent": "reqmap links code to requirements by tags, but nothing reported how much code is linked to nothing — code traceable to no requirement was only visible by running `draft`/`plan`. This surfaces that gap as a read-only count, so a reviewer sees at a glance whether code is drifting ahead of the spec. A Senate audit (2026-06-21) rejected enforcing coverage as a hard gate — it is gameable with hollow tags and conflicts with the model where a requirement is a behavior, not a file — and approved only this non-blocking visibility signal.",
      "contract": [
        "`health` reports the count of scannable code files carrying no membership tag as a read-only `untagged` signal — present only when a code root is scanned, and never affecting the score or exit code. [[REQ-COVERAGE-836]] details the behaviour."
      ],
      "verify": [],
      "notes": [
        "This is the read-only half of coverage; the matching list of which files is the \"Untagged files\" bucket in `next` ([[ARCH-NEXT-013]]). Same denominator, two surfaces.",
        "It measures tag PRESENCE, not tag QUALITY: a hollow `# implements:` tag counts as covered. This is accepted — the Senate flagged that a hard gate would make hollow tags the rational way to pass CI, which is why this stays advisory.",
        "Granularity is per file, not per member; the engine does not parse members."
      ],
      "current_impl": [
        "The untagged-count block in `cmd_health` (`reqmap.py`), reusing `_scan_untagged` (ARCH-NEXT-013) when a code root is supplied. Surfaced read-only in `health` text output and `--json`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a code root with one tagged and one untagged scannable file When   `health --json` runs with that code root Then   the `untagged` key equals 1 and the score is unchanged by it",
        "CASE-2 — Given  no code root (a unit-test caller) When   `health --json` runs Then   the output carries no `untagged` key",
        "CASE-3 — Given  an untagged scannable file that is then tagged or added to `.reqmapignore` When   `health --json` runs again with the code root Then   the `untagged` count drops by one (the file is silenced from the signal)"
      ],
      "accept": "CASE-1\n  Given  a code root with one tagged and one untagged scannable file\n  When   `health --json` runs with that code root\n  Then   the `untagged` key equals 1 and the score is unchanged by it\n\nCASE-2\n  Given  no code root (a unit-test caller)\n  When   `health --json` runs\n  Then   the output carries no `untagged` key\n\nCASE-3\n  Given  an untagged scannable file that is then tagged or added to `.reqmapignore`\n  When   `health --json` runs again with the code root\n  Then   the `untagged` count drops by one (the file is silenced from the signal)",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-SCAN-002",
        "ARCH-NEXT-013"
      ],
      "used_by": [
        "ARCH-AUDIT-065"
      ],
      "satisfies": [
        "SYS-REPORT-105"
      ],
      "satisfied_by": [
        "REQ-COVERAGE-836"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1592"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5895"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3576"
        }
      ],
      "test_exempt": null,
      "milestone": "v2.6",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Semnal de acoperire pentru codul neetichetat",
          "intent": "reqmap leagă codul de cerințe prin etichete, dar nimic nu raporta cât cod este legat de nimic — codul netrasabil la nicio cerință era vizibil doar rulând `draft`/`plan`. Aceasta scoate la suprafață acel gol ca o numărătoare doar-citire, astfel încât un revizor vede dintr-o privire dacă codul o ia înaintea specificației. Un audit Senate (2026-06-21) a respins impunerea acoperirii ca gate ferm — este ușor de păcălit cu etichete goale și intră în conflict cu modelul în care o cerință este un comportament, nu un fișier — și a aprobat doar acest semnal de vizibilitate neblocant.",
          "contract": "> reqmap leagă codul de cerințe prin etichete, dar nimic nu raporta cât cod este legat\n> de nimic — codul netrasabil la nicio cerință era vizibil doar rulând `draft`/`plan`.\n> Aceasta scoate la suprafață acel gol ca o numărătoare doar-citire, astfel încât un\n> revizor vede dintr-o privire dacă codul o ia înaintea specificației. Un audit Senate\n> (2026-06-21) a respins impunerea acoperirii ca gate ferm — este ușor de păcălit cu\n> etichete goale și intră în conflict cu modelul în care o cerință este un comportament,\n> nu un fișier — și a aprobat doar acest semnal de vizibilitate neblocant.\n\nFiecare punct de mai jos este obligatoriu.\n- `health` raportează numărul de fișiere de cod scanabile care nu poartă nicio etichetă de apartenență ca un semnal `untagged` doar-citire — prezent doar când o rădăcină de cod este scanată, și nu afectează niciodată scorul sau codul de ieșire. [[REQ-COVERAGE-836]] detaliază comportamentul.",
          "acceptance": "CASE-1\n  Given  o rădăcină de cod cu un fișier scanabil etichetat și unul neetichetat\n  When   `health --json` rulează cu acea rădăcină de cod\n  Then   cheia `untagged` este egală cu 1 iar scorul rămâne neschimbat de aceasta\n\nCASE-2\n  Given  nicio rădăcină de cod (un apelant din unit-test)\n  When   `health --json` rulează\n  Then   ieșirea nu poartă nicio cheie `untagged`\n\nCASE-3\n  Given  un fișier scanabil neetichetat care este apoi etichetat sau adăugat în `.reqmapignore`\n  When   `health --json` rulează din nou cu rădăcina de cod\n  Then   numărătoarea `untagged` scade cu unu (fișierul este redus la tăcere din semnal)"
        }
      }
    },
    {
      "id": "REQ-COVERAGE-836",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Counting untagged code as a read-only signal",
      "intent": "Code can drift ahead of the spec with nobody noticing, because a tag's absence was only visible by running `draft`/`plan`. `health` now counts scannable files with no membership tag and reports it as `untagged` — visible, but never a gate: a Senate audit rejected enforcing it, since a hollow tag would then become the rational way to pass CI.",
      "contract": [
        "The capability reports the count of scannable code files that carry no membership tag — \"untagged code\", code traced to no requirement.",
        "The denominator is exactly `_scan_untagged`'s (see [[ARCH-NEXT-013]]): the files matched by the scanner extension walk, minus `.reqmapignore`.",
        "Any membership tag counts a file as covered. The tags are `implements`, `tested-by`, `generated-from` and `validated-against`.",
        "The `health` command includes this count as an `untagged` integer key in its `--json` output.",
        "The `health` command also includes it as a labelled line in its text output.",
        "The `untagged` key is absent, not zero, when no code root is scanned — a unit-test caller, for example. An existing `--json` consumer therefore keeps its schema.",
        "The signal is read-only and is never a gate. It changes no exit code.",
        "The signal never lowers the health score, because it counts files, not requirements.",
        "A file is silenced from the count either by tagging it, or by adding it to `.reqmapignore`.",
        "There is no separate exemption mechanism."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the untagged count reflects untagged files Given  a code root with one tagged file and one untagged scannable file When   `health --json` runs with that code root Then   the `untagged` key equals 1, the health score is unchanged, and the exit code stays 0",
        "CASE-2 — — the untagged count shares its denominator with _scan_untagged Given  a code root scanned by both `health` and `_scan_untagged` directly When   both are run over the same tree Then   the `untagged` count equals the length of `_scan_untagged`'s file list",
        "CASE-3 — — a tested-by tag alone covers a file Given  an untagged file that is then given only a `# tested-by:` comment When   `health --json` runs Then   that file no longer counts toward `untagged`",
        "CASE-4 — — health --json exposes the untagged integer key Given  a code root with one untagged scannable file When   `health --json` runs Then   its output is valid JSON carrying an integer `untagged` key",
        "CASE-5 — — health's text output labels the untagged count Given  a code root with one untagged scannable file When   `health` runs without `--json` Then   its text output includes a labelled line naming the untagged count",
        "CASE-6 — — the untagged key is absent without a code root Given  a `health --json` invocation with no code root supplied When   it runs Then   the output carries no `untagged` key at all",
        "CASE-7 — — tagging or ignoring a file drops it from the count Given  an untagged file, then tagged in one run and `.reqmapignore`-listed in another When   `health --json` runs after each change Then   the `untagged` count drops by one either way, with no separate exemption mechanism"
      ],
      "accept": "CASE-1 — the untagged count reflects untagged files\n  Given  a code root with one tagged file and one untagged scannable file\n  When   `health --json` runs with that code root\n  Then   the `untagged` key equals 1, the health score is unchanged, and the exit code stays 0\n\nCASE-2 — the untagged count shares its denominator with _scan_untagged\n  Given  a code root scanned by both `health` and `_scan_untagged` directly\n  When   both are run over the same tree\n  Then   the `untagged` count equals the length of `_scan_untagged`'s file list\n\nCASE-3 — a tested-by tag alone covers a file\n  Given  an untagged file that is then given only a `# tested-by:` comment\n  When   `health --json` runs\n  Then   that file no longer counts toward `untagged`\n\nCASE-4 — health --json exposes the untagged integer key\n  Given  a code root with one untagged scannable file\n  When   `health --json` runs\n  Then   its output is valid JSON carrying an integer `untagged` key\n\nCASE-5 — health's text output labels the untagged count\n  Given  a code root with one untagged scannable file\n  When   `health` runs without `--json`\n  Then   its text output includes a labelled line naming the untagged count\n\nCASE-6 — the untagged key is absent without a code root\n  Given  a `health --json` invocation with no code root supplied\n  When   it runs\n  Then   the output carries no `untagged` key at all\n\nCASE-7 — tagging or ignoring a file drops it from the count\n  Given  an untagged file, then tagged in one run and `.reqmapignore`-listed in another\n  When   `health --json` runs after each change\n  Then   the `untagged` count drops by one either way, with no separate exemption mechanism",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-COVERAGE-029"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1592"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5896"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3577"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3593"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9750"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 7,
      "covered": 7,
      "i18n": {
        "ro": {
          "title": "Numărarea codului netaguit ca semnal doar-pentru-citire",
          "intent": "Codul poate devansa specificația prin drift fără ca nimeni să observe, pentru că absența unui tag era vizibilă doar rulând `draft`/`plan`. `health` numără acum fișierele scanabile fără niciun tag de apartenență și raportează asta ca `untagged` — vizibil, dar niciodată ca gate: un audit Senate a respins impunerea lui, pentru că un tag gol ar deveni astfel calea rațională de a trece CI.",
          "contract": "> Codul poate devansa specificația prin drift fără ca nimeni să observe, pentru că absența unui tag era\n> vizibilă doar rulând `draft`/`plan`. `health` numără acum fișierele scanabile fără niciun\n> tag de apartenență și raportează asta ca `untagged` — vizibil, dar niciodată ca gate: un audit Senate\n> a respins impunerea lui, pentru că un tag gol ar deveni astfel calea rațională de a\n> trece CI.\n\nFiecare punct de mai jos este obligatoriu.\n- Capabilitatea raportează numărul fișierelor de cod scanabile care nu poartă niciun tag de apartenență —\n  „cod netaguit\", cod netrasabil la nicio cerință.\n- Numitorul este exact cel al lui `_scan_untagged` (vezi [[ARCH-NEXT-013]]): fișierele găsite de\n  parcurgerea pe extensii a scanerului, minus `.reqmapignore`.\n- Orice tag de apartenență numără fișierul ca fiind acoperit. Tag-urile sunt `implements`, `tested-by`,\n  `generated-from` și `validated-against`.\n- Comanda `health` include acest număr ca o cheie întreagă `untagged` în ieșirea sa `--json`.\n- Comanda `health` îl include de asemenea ca o linie etichetată în ieșirea sa text.\n- Cheia `untagged` lipsește, nu este zero, atunci când nu este scanată nicio rădăcină de cod —\n  de exemplu un apelant dintr-un test unitar. Astfel un consumator `--json` existent își păstrează schema.\n- Semnalul este doar-pentru-citire și nu este niciodată gate. Nu modifică niciun cod de ieșire.\n- Semnalul nu scade niciodată scorul de health, pentru că numără fișiere, nu cerințe.\n- Un fișier este exclus din numărătoare fie prin taguire, fie prin adăugare în `.reqmapignore`.\n- Nu există niciun mecanism separat de exceptare.",
          "acceptance": "CASE-1 — numărul untagged reflectă fișierele netaguite\n  Given  o rădăcină de cod cu un fișier taguit și un fișier scanabil netaguit\n  When   `health --json` rulează cu acea rădăcină de cod\n  Then   cheia `untagged` este egală cu 1, scorul de health rămâne neschimbat, iar codul de ieșire rămâne 0\n\nCASE-2 — numărul untagged împarte numitorul cu _scan_untagged\n  Given  o rădăcină de cod scanată atât de `health`, cât și direct de `_scan_untagged`\n  When   ambele rulează peste același arbore\n  Then   numărul `untagged` este egal cu lungimea listei de fișiere a lui `_scan_untagged`\n\nCASE-3 — un singur tag tested-by acoperă un fișier\n  Given  un fișier netaguit căruia i se adaugă apoi doar un comentariu `# tested-by:`\n  When   `health --json` rulează\n  Then   acel fișier nu mai contează la `untagged`\n\nCASE-4 — health --json expune cheia întreagă untagged\n  Given  o rădăcină de cod cu un fișier scanabil netaguit\n  When   `health --json` rulează\n  Then   ieșirea sa este JSON valid și conține o cheie întreagă `untagged`\n\nCASE-5 — ieșirea text a lui health etichetează numărul untagged\n  Given  o rădăcină de cod cu un fișier scanabil netaguit\n  When   `health` rulează fără `--json`\n  Then   ieșirea sa text include o linie etichetată care numește numărul untagged\n\nCASE-6 — cheia untagged lipsește fără o rădăcină de cod\n  Given  o invocare `health --json` fără nicio rădăcină de cod furnizată\n  When   rulează\n  Then   ieșirea nu conține deloc cheia `untagged`\n\nCASE-7 — taguirea sau ignorarea unui fișier îl scoate din numărătoare\n  Given  un fișier netaguit, apoi taguit într-o rulare și listat în `.reqmapignore` în alta\n  When   `health --json` rulează după fiecare schimbare\n  Then   numărul `untagged` scade cu unu în ambele cazuri, fără niciun mecanism separat de exceptare"
        }
      }
    },
    {
      "id": "ARCH-DECOMPOSE-050",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Clause decomposition scaffold",
      "intent": "When `statement-size` reports an over-long clause, the remedy is to split it into its own requirement — and that means retyping the clause, inventing an id, and wiring the dependency by hand. This does the mechanical half on request. It never runs on its own, because `lint` also runs inside the pre-commit hook and CI, where writing a new file would break the very commit it was helping.",
      "contract": [
        "`lint` writes no file during the default run; only the opt-in `--decompose` flag creates one draft per reported `statement-size` clause, and no invocation site (the gate, the pre-commit hook, CI) ever passes it. [[REQ-DECOMPOSE-837]]",
        "Each created draft carries `status: draft`, a `depends_on` entry naming its parent, the offending clause seeded verbatim, and an id that reuses the parent's area/name at the next free corpus number. [[REQ-DECOMPOSE-838]]",
        "`lint --decompose` leaves the parent byte-identical, discloses that the split was chosen by word count and not by obligation, skips a clause already decomposed, and never scaffolds from an `ac-count-high` finding. [[REQ-DECOMPOSE-839]]"
      ],
      "verify": [],
      "notes": [
        "This exists because the author asked for it after the objection below was put to them, and reaffirmed it. The objection stands and is recorded here rather than dropped: a word count cannot determine that a clause holds two obligations, so a draft created this way may split a clause that was atomic all along. That is why every created file is a `draft`, carries its provenance in its own text, and is undone by deleting it.",
        "The `--decompose` flag is opt-in for a mechanical reason, not a stylistic one. `.githooks/pre-commit` runs `gate` -> `lint --strict` -> `map --check` in that order. A file written during the `lint` step makes the `map --check` step fail in the same hook run, because the committed `_map.*` has no node for it — the hook would block the commit and blame a stale map it had just caused. In CI the checkout is ephemeral, so the file would be written and discarded.",
        "\"When the warning is resolved\" is not an observable event. The engine sees a finding as present or absent, and absent is indistinguishable from never-fired: an author who rewrites the clause shorter without decomposing it clears the finding the same way. So creation hangs off an explicit flag rather than off a transition the engine cannot see.",
        "Ids follow `AREA-NAME-NNN` rather than a derived suffix such as `REQ-AUTH-012-B`. The suffix form passes `_ID_PAT`, but `_warn_number_collision` reads `parts[-1]` as the number and would compare \"B\" against real numbers.",
        "The parent stays untouched on purpose. Rewriting a clause in a `confirmed` requirement changes its contract hash, which raises drift and forces `sync --accept-drift` — a large consequence for a warn-only finding (ADR-0002).",
        "Measured reachability, this corpus, 2026-09-03: `statement-size` has never fired here — `LINT_STATEMENT_WORDS` is 150 and the longest Contract clause anywhere is 61 words (`ARCH-PAGES-021`), so no clause in this corpus is even half the threshold. `ac-count-high` is not currently reachable at all: 6 of the 72 non-draft requirements exceed `LINT_AC_MAX` (8.3% raw), but all 6 carry `lint_exempt: [ac-count-high]`, so the post-exempt count is 0 of 72 (0.0%) today — a deliberate author choice, not a design gap. That zero is why `--decompose` covers `statement-size` only: automation over a signal with no fire rate and no confirmation sample is what ADR-0022 forbids, and an `ac-count-high` triage path was removed before it shipped for exactly that reason."
      ],
      "current_impl": [
        "`DECOMPOSED_TEMPLATE`, `_next_free_number`, `_already_decomposed` and `_decompose_clause` in `reqmap.py`; `cmd_lint` takes `decompose` and `reqs_dir` and writes only when the `--decompose` flag is passed. The skip keys off a `<!-- decomposed-from: <parent>#<n> -->` marker in the created file, not off the target filename: the id comes from the next free number, so a second run picks a fresh name and an existence check never fires. That was a real defect — the first implementation created a second file on every re-run, and CASE-6 caught it.",
        "There is no `ac-count-high` sibling. One was written (`AC_COUNT_TRIAGE_TEMPLATE`, `_decompose_ac_count_high`) and removed before it shipped: it reached nobody, and ADR-0022 — adopted in the same change — forbids shipping on a signal with no published fire rate and no human-confirmation sample. `OversizeUnify`'s `test_no_ac_count_high_decompose_symbols_remain` asserts neither symbol comes back, so re-adding one is a deliberate act that has to meet that bar first.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a corpus with one clause above the `statement-size` threshold When   `lint` runs without `--decompose` Then   the finding is reported and the requirements directory holds no new file",
        "CASE-2 — Given  the same corpus When   `lint --decompose` runs Then   one draft is created, carrying `status: draft` and `depends_on` naming the parent",
        "CASE-3 — Given  the same corpus When   `lint --decompose` runs Then   the parent file is byte-identical to what it was before the run",
        "CASE-4 — Given  a created draft When   its text is read Then   it states that the split point was chosen by word count, not by obligation",
        "CASE-5 — Given  a parent `REQ-AUTH-012` in a corpus whose highest number is 049 When   `lint --decompose` runs Then   the created id is `REQ-AUTH-050`",
        "CASE-6 — Given  a corpus where a previous `lint --decompose` already created the target file When   `lint --decompose` runs again Then   the clause is skipped, the skip is reported, and the existing file is unchanged",
        "CASE-7 — Given  a non-exempt requirement above `LINT_AC_MAX` acceptance criteria When   `lint --decompose` runs Then   the `ac-count-high` finding is printed and no file is created for it"
      ],
      "accept": "CASE-1\n  Given  a corpus with one clause above the `statement-size` threshold\n  When   `lint` runs without `--decompose`\n  Then   the finding is reported and the requirements directory holds no new file\nCASE-2\n  Given  the same corpus\n  When   `lint --decompose` runs\n  Then   one draft is created, carrying `status: draft` and `depends_on` naming the parent\nCASE-3\n  Given  the same corpus\n  When   `lint --decompose` runs\n  Then   the parent file is byte-identical to what it was before the run\nCASE-4\n  Given  a created draft\n  When   its text is read\n  Then   it states that the split point was chosen by word count, not by obligation\nCASE-5\n  Given  a parent `REQ-AUTH-012` in a corpus whose highest number is 049\n  When   `lint --decompose` runs\n  Then   the created id is `REQ-AUTH-050`\nCASE-6\n  Given  a corpus where a previous `lint --decompose` already created the target file\n  When   `lint --decompose` runs again\n  Then   the clause is skipped, the skip is reported, and the existing file is unchanged\nCASE-7\n  Given  a non-exempt requirement above `LINT_AC_MAX` acceptance criteria\n  When   `lint --decompose` runs\n  Then   the `ac-count-high` finding is printed and no file is created for it",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-ATOMICITY-049",
        "ARCH-LINT-014",
        "ARCH-NEW-004"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-AUTHOR-101"
      ],
      "satisfied_by": [
        "REQ-DECOMPOSE-837",
        "REQ-DECOMPOSE-838",
        "REQ-DECOMPOSE-839"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2340"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4878"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4896"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4921"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4957"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7029"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7115"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7209"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7789"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9284"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "could-have",
      "risks": [],
      "clauses": 7,
      "covered": 7,
      "i18n": {
        "ro": {
          "title": "Schelă de descompunere a clauzelor",
          "intent": "Când `statement-size` raportează o clauză prea lungă, remediul este să o desparți în propria ei cerință — iar asta înseamnă retranscrierea clauzei, inventarea unui id și cablarea dependenței de mână. Aceasta face jumătatea mecanică la cerere. Nu rulează niciodată de la sine, deoarece `lint` rulează și în hook-ul pre-commit și în CI, unde scrierea unui fișier nou ar strica exact commit-ul pe care îl ajuta.",
          "contract": "> Când `statement-size` raportează o clauză prea lungă, remediul este să o desparți în\n> propria ei cerință — iar asta înseamnă retranscrierea clauzei, inventarea unui id și\n> cablarea dependenței de mână. Aceasta face jumătatea mecanică la cerere. Nu rulează\n> niciodată de la sine, deoarece `lint` rulează și în hook-ul pre-commit și în CI, unde\n> scrierea unui fișier nou ar strica exact commit-ul pe care îl ajuta.\n\nFiecare punct de mai jos este obligatoriu.\n- `lint` nu scrie niciun fișier în rularea implicită; doar flag-ul opțional `--decompose` creează câte un draft per clauză `statement-size` raportată, și niciun punct de invocare (gate-ul, hook-ul pre-commit, CI) nu îl transmite vreodată. [[REQ-DECOMPOSE-837]]\n- Fiecare draft creat poartă `status: draft`, o intrare `depends_on` care numește părintele, clauza incriminată însămânțată verbatim, și un id care refolosește zona/numele părintelui la următorul număr liber din corpus. [[REQ-DECOMPOSE-838]]\n- `lint --decompose` lasă părintele identic octet-cu-octet, dezvăluie faptul că despărțirea a fost aleasă după numărul de cuvinte și nu după obligație, sare peste o clauză deja descompusă, și nu scheluiește niciodată dintr-o constatare `ac-count-high`. [[REQ-DECOMPOSE-839]]",
          "acceptance": "CASE-1\n  Given  un corpus cu o clauză peste pragul `statement-size`\n  When   `lint` rulează fără `--decompose`\n  Then   constatarea este raportată iar directorul de cerințe nu conține niciun fișier nou\nCASE-2\n  Given  același corpus\n  When   `lint --decompose` rulează\n  Then   se creează un draft, care poartă `status: draft` și `depends_on` care numește părintele\nCASE-3\n  Given  același corpus\n  When   `lint --decompose` rulează\n  Then   fișierul părinte este identic octet-cu-octet cu ce era înainte de rulare\nCASE-4\n  Given  un draft creat\n  When   textul său este citit\n  Then   afirmă că punctul de despărțire a fost ales după numărul de cuvinte, nu după obligație\nCASE-5\n  Given  un părinte `REQ-AUTH-012` într-un corpus al cărui număr cel mai mare este 049\n  When   `lint --decompose` rulează\n  Then   id-ul creat este `REQ-AUTH-050`\nCASE-6\n  Given  un corpus unde un `lint --decompose` anterior a creat deja fișierul țintă\n  When   `lint --decompose` rulează din nou\n  Then   clauza este sărită, saltul este raportat, iar fișierul existent rămâne neschimbat\nCASE-7\n  Given  o cerință neexceptată peste `LINT_AC_MAX` criterii de acceptare\n  When   `lint --decompose` rulează\n  Then   constatarea `ac-count-high` este afișată și niciun fișier nu este creat pentru ea"
        }
      }
    },
    {
      "id": "REQ-DECOMPOSE-837",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "--decompose is opt-in; the default lint run never writes",
      "intent": "`lint` also runs inside the pre-commit hook and CI, where writing a new file mid-run would break the very commit it was helping — the freshly-written draft would have no node in the already-committed map, failing the next `map --check` step in the same hook run. So scaffolding a draft only happens when a developer explicitly asks for it with `--decompose`, never on a bare `lint`.",
      "contract": [
        "`lint` writes no file during the default run, whatever `statement-size` or `ac-count-high` reports.",
        "`lint --decompose` creates one draft requirement for each reported `statement-size` clause.",
        "The gate, the pre-commit hook and CI never pass `--decompose`, so those runs stay read-only."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a default lint run reports the finding but writes no file Given  a requirement carrying a clause over the `statement-size` word threshold When   `lint` runs without `--decompose` Then   stdout names the \"statement-size\" finding and the requirements directory holds no new file",
        "CASE-2 — — --decompose creates exactly one draft file per reported clause Given  a parent with one clause over the `statement-size` threshold When   `lint --decompose` runs Then   exactly one new file appears in the requirements directory",
        "CASE-3 — — no invocation site passes --decompose Given  `.githooks/pre-commit` and `.github/workflows/ci.yml` When   their `lint` invocation lines are read Then   neither contains the `--decompose` flag --------------------"
      ],
      "accept": "CASE-1 — a default lint run reports the finding but writes no file\n  Given  a requirement carrying a clause over the `statement-size` word threshold\n  When   `lint` runs without `--decompose`\n  Then   stdout names the \"statement-size\" finding and the requirements directory\n         holds no new file\n\nCASE-2 — --decompose creates exactly one draft file per reported clause\n  Given  a parent with one clause over the `statement-size` threshold\n  When   `lint --decompose` runs\n  Then   exactly one new file appears in the requirements directory\n\nCASE-3 — no invocation site passes --decompose\n  Given  `.githooks/pre-commit` and `.github/workflows/ci.yml`\n  When   their `lint` invocation lines are read\n  Then   neither contains the `--decompose` flag\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-DECOMPOSE-050"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4921"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7029"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9284"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "--decompose este opțional; rularea implicită a lint nu scrie niciodată",
          "intent": "`lint` rulează și în hook-ul pre-commit și în CI, unde scrierea unui fișier nou în mijlocul rulării ar strica exact commit-ul pe care îl ajuta — draft-ul proaspăt scris nu ar avea niciun nod în harta deja comisă, făcând să eșueze pasul `map --check` următor din aceeași rulare a hook-ului. Așa că schițarea unui draft se întâmplă doar când un dezvoltator o cere explicit cu `--decompose`, niciodată la un `lint` simplu.",
          "contract": "> `lint` rulează și în hook-ul pre-commit și în CI, unde scrierea unui fișier nou în mijlocul rulării ar\n> strica exact commit-ul pe care îl ajuta — draft-ul proaspăt scris nu ar avea niciun nod în\n> harta deja comisă, făcând să eșueze pasul `map --check` următor din aceeași rulare a hook-ului. Așa că\n> schițarea unui draft se întâmplă doar când un dezvoltator o cere explicit cu\n> `--decompose`, niciodată la un `lint` simplu.\n\nFiecare punct de mai jos este obligatoriu.\n- `lint` nu scrie niciun fișier în timpul rulării implicite, indiferent ce raportează `statement-size` sau `ac-count-high`.\n- `lint --decompose` creează o cerință draft pentru fiecare clauză `statement-size` raportată.\n- Gate-ul, hook-ul pre-commit și CI nu transmit niciodată `--decompose`, astfel încât acele rulări rămân doar-pentru-citire.",
          "acceptance": "CASE-1 — o rulare implicită de lint raportează constatarea, dar nu scrie niciun fișier\n  Given  o cerință care conține o clauză peste pragul de cuvinte al `statement-size`\n  When   `lint` rulează fără `--decompose`\n  Then   stdout numește constatarea \"statement-size\" iar directorul de cerințe\n         nu conține niciun fișier nou\n\nCASE-2 — --decompose creează exact un fișier draft per clauză raportată\n  Given  un părinte cu o clauză peste pragul `statement-size`\n  When   `lint --decompose` rulează\n  Then   apare exact un fișier nou în directorul de cerințe\n\nCASE-3 — niciun loc de invocare nu transmite --decompose\n  Given  `.githooks/pre-commit` și `.github/workflows/ci.yml`\n  When   sunt citite liniile lor de invocare a `lint`\n  Then   niciuna nu conține flag-ul `--decompose`\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-DECOMPOSE-838",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "A created draft's shape: status, parent link, seeded clause, id",
      "intent": "A word-count split cannot prove the clause it cut actually held two obligations, so the created file is never trusted outright — it starts life as a `draft` naming its parent in `depends_on`, carries the exact clause text so the author can judge it, and gets an id that fits the corpus's existing naming scheme rather than a derived suffix.",
      "contract": [
        "Each created draft carries `status: draft` and a `depends_on` entry naming its parent.",
        "The reported clause text is seeded into the created draft's Contract section.",
        "The created id keeps the parent's area and name, and takes the next free corpus number."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the created draft carries status: draft and depends_on the parent Given  `lint --decompose` creates a draft from `REQ-AUTH-012` When   the created file's frontmatter is read Then   it contains `status: draft` and `depends_on: [REQ-AUTH-012]`",
        "CASE-2 — — the offending clause's own text appears verbatim in the created draft Given  a parent clause of 155 repeated words flagged by `statement-size` When   `lint --decompose` creates the draft Then   the draft's Contract section contains that exact clause text",
        "CASE-3 — — the created id reuses the parent's area/name with the next free number Given  parent `REQ-AUTH-012` in a corpus whose highest existing number is 049 When   `lint --decompose` runs Then   the created file is named `REQ-AUTH-050.md` --------------------"
      ],
      "accept": "CASE-1 — the created draft carries status: draft and depends_on the parent\n  Given  `lint --decompose` creates a draft from `REQ-AUTH-012`\n  When   the created file's frontmatter is read\n  Then   it contains `status: draft` and `depends_on: [REQ-AUTH-012]`\n\nCASE-2 — the offending clause's own text appears verbatim in the created draft\n  Given  a parent clause of 155 repeated words flagged by `statement-size`\n  When   `lint --decompose` creates the draft\n  Then   the draft's Contract section contains that exact clause text\n\nCASE-3 — the created id reuses the parent's area/name with the next free number\n  Given  parent `REQ-AUTH-012` in a corpus whose highest existing number is 049\n  When   `lint --decompose` runs\n  Then   the created file is named `REQ-AUTH-050.md`\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-DECOMPOSE-050"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4878"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4921"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7029"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Forma unui draft creat: status, legătură cu părintele, clauză însămânțată, id",
          "intent": "O separare bazată pe numărul de cuvinte nu poate dovedi că clauza tăiată conținea de fapt două obligații, așa că fișierul creat nu este niciodată de încredere necondiționat — el începe ca `draft`, numindu-și părintele în `depends_on`, poartă textul exact al clauzei pentru ca autorul să îl poată evalua și primește un id care se potrivește cu schema de denumire existentă a corpusului, nu un sufix derivat.",
          "contract": "> O separare bazată pe numărul de cuvinte nu poate dovedi că clauza tăiată conținea de fapt două obligații, așa că\n> fișierul creat nu este niciodată de încredere necondiționat — el începe ca `draft`, numindu-și\n> părintele în `depends_on`, poartă textul exact al clauzei pentru ca autorul să îl poată evalua, și primește un id\n> care se potrivește cu schema de denumire existentă a corpusului, nu un sufix derivat.\n\nFiecare punct de mai jos este obligatoriu.\n- Fiecare draft creat poartă `status: draft` și o intrare `depends_on` care își numește părintele.\n- Textul clauzei raportate este însămânțat în secțiunea Contract a draft-ului creat.\n- Id-ul creat păstrează zona și numele părintelui și preia următorul număr liber din corpus.",
          "acceptance": "CASE-1 — draft-ul creat poartă status: draft și depends_on către părinte\n  Given  `lint --decompose` creează un draft din `REQ-AUTH-012`\n  When   este citit frontmatter-ul fișierului creat\n  Then   conține `status: draft` și `depends_on: [REQ-AUTH-012]`\n\nCASE-2 — textul propriu al clauzei incriminate apare verbatim în draft-ul creat\n  Given  o clauză a părintelui de 155 de cuvinte repetate, semnalată de `statement-size`\n  When   `lint --decompose` creează draft-ul\n  Then   secțiunea Contract a draft-ului conține exact acel text al clauzei\n\nCASE-3 — id-ul creat reutilizează zona/numele părintelui cu următorul număr liber\n  Given  părintele `REQ-AUTH-012` într-un corpus al cărui cel mai mare număr existent este 049\n  When   `lint --decompose` rulează\n  Then   fișierul creat se numește `REQ-AUTH-050.md`\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-DECOMPOSE-839",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The parent never changes, and the command knows its own limits",
      "intent": "Rewriting a clause inside a `confirmed` requirement would change its contract hash and force `sync --accept-drift` — too large a consequence for a warn-only lint finding. So the parent is never touched, deleting a created draft restores the corpus exactly, a re-run skips a clause it already scaffolded, and the command only ever acts on `statement-size` findings — never on `ac-count-high`, a signal this corpus has no confirmed fire-rate evidence for.",
      "contract": [
        "`lint --decompose` leaves the parent unchanged, so no confirmed contract drifts.",
        "The command chooses the split by word count, never by obligation, and says so on stdout.",
        "Each created draft records that its split point was chosen by word count alone.",
        "`lint --decompose` scaffolds from `statement-size` findings only; an `ac-count-high` finding is reported and nothing is written for it.",
        "Deleting a created draft restores the corpus exactly, because the parent was never edited.",
        "`lint --decompose` skips a clause whose target file already exists, and reports the skip."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the parent file is byte-identical after a decompose run Given  the parent file's bytes captured before `lint --decompose` runs When   the run completes Then   the parent file's bytes are unchanged from the captured snapshot, so deleting the created draft restores the corpus exactly",
        "CASE-2 — — stdout discloses the split was by word count, not obligation Given  `lint --decompose` running on a flagged clause When   the run completes Then   stdout includes \"word count, not by obligation\"",
        "CASE-3 — — the created draft's own text discloses the word-count-only split Given  a draft created by `lint --decompose` When   its file text is read Then   it contains \"WORD COUNT, never by obligation\"",
        "CASE-4 — — a second decompose run skips the already-decomposed clause and reports it Given  a clause already decomposed by a prior `lint --decompose` run When   `lint --decompose` runs again Then   stdout reports \"skipped\" and no second file is created for that clause",
        "CASE-5 — — an ac-count-high finding is reported but never scaffolded Given  a non-exempt requirement above `LINT_AC_MAX` acceptance criteria When   `lint --decompose` runs Then   the `ac-count-high` finding still prints and no file is created for it"
      ],
      "accept": "CASE-1 — the parent file is byte-identical after a decompose run\n  Given  the parent file's bytes captured before `lint --decompose` runs\n  When   the run completes\n  Then   the parent file's bytes are unchanged from the captured snapshot, so deleting the\n         created draft restores the corpus exactly\n\nCASE-2 — stdout discloses the split was by word count, not obligation\n  Given  `lint --decompose` running on a flagged clause\n  When   the run completes\n  Then   stdout includes \"word count, not by obligation\"\n\nCASE-3 — the created draft's own text discloses the word-count-only split\n  Given  a draft created by `lint --decompose`\n  When   its file text is read\n  Then   it contains \"WORD COUNT, never by obligation\"\n\nCASE-4 — a second decompose run skips the already-decomposed clause and reports it\n  Given  a clause already decomposed by a prior `lint --decompose` run\n  When   `lint --decompose` runs again\n  Then   stdout reports \"skipped\" and no second file is created for that clause\n\nCASE-5 — an ac-count-high finding is reported but never scaffolded\n  Given  a non-exempt requirement above `LINT_AC_MAX` acceptance criteria\n  When   `lint --decompose` runs\n  Then   the `ac-count-high` finding still prints and no file is created for it",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-DECOMPOSE-050"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4896"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7029"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7115"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Părintele nu se schimbă niciodată, iar comanda își cunoaște propriile limite",
          "intent": "Rescrierea unei clauze într-o cerință `confirmed` i-ar schimba hash-ul contractului și ar forța `sync --accept-drift` — o consecință prea mare pentru o constatare de lint doar-avertizare. Așa că părintele nu este niciodată atins, ștergerea unui draft creat restaurează corpusul exact, o rulare repetată sare peste o clauză deja schițată, iar comanda acționează întotdeauna doar asupra constatărilor `statement-size` — niciodată asupra `ac-count-high`, un semnal pentru care acest corpus nu are dovezi confirmate ale ratei de declanșare.",
          "contract": "> Rescrierea unei clauze într-o cerință `confirmed` i-ar schimba hash-ul contractului și ar forța\n> `sync --accept-drift` — o consecință prea mare pentru o constatare de lint doar-avertizare. Așa că\n> părintele nu este niciodată atins, ștergerea unui draft creat restaurează corpusul exact, o rulare repetată sare peste o\n> clauză deja schițată, iar comanda acționează întotdeauna doar asupra constatărilor `statement-size` —\n> niciodată asupra `ac-count-high`, un semnal pentru care acest corpus nu are dovezi confirmate ale ratei de declanșare.\n\nFiecare punct de mai jos este obligatoriu.\n- `lint --decompose` lasă părintele neschimbat, astfel încât niciun contract confirmat nu suferă drift.\n- Comanda alege separarea după numărul de cuvinte, niciodată după obligație, și afirmă acest lucru pe stdout.\n- Fiecare draft creat înregistrează că punctul său de separare a fost ales exclusiv după numărul de cuvinte.\n- `lint --decompose` schițează doar din constatările `statement-size`; o constatare `ac-count-high` este raportată, dar nu se scrie nimic pentru ea.\n- Ștergerea unui draft creat restaurează corpusul exact, pentru că părintele nu a fost niciodată editat.\n- `lint --decompose` sare peste o clauză al cărei fișier țintă există deja și raportează omiterea.",
          "acceptance": "CASE-1 — fișierul părinte este identic la nivel de byte după o rulare decompose\n  Given  octeții fișierului părinte capturați înainte ca `lint --decompose` să ruleze\n  When   rularea se încheie\n  Then   octeții fișierului părinte rămân neschimbați față de instantaneul capturat, astfel încât ștergerea\n         draft-ului creat restaurează corpusul exact\n\nCASE-2 — stdout dezvăluie că separarea a fost după numărul de cuvinte, nu după obligație\n  Given  `lint --decompose` rulând pe o clauză semnalată\n  When   rularea se încheie\n  Then   stdout include \"word count, not by obligation\"\n\nCASE-3 — textul propriu al draft-ului creat dezvăluie separarea exclusiv după numărul de cuvinte\n  Given  un draft creat de `lint --decompose`\n  When   este citit textul fișierului său\n  Then   conține \"WORD COUNT, never by obligation\"\n\nCASE-4 — o a doua rulare decompose sare peste clauza deja descompusă și raportează asta\n  Given  o clauză deja descompusă de o rulare anterioară a `lint --decompose`\n  When   `lint --decompose` rulează din nou\n  Then   stdout raportează \"skipped\" și nu se creează un al doilea fișier pentru acea clauză\n\nCASE-5 — o constatare ac-count-high este raportată, dar nu este niciodată schițată\n  Given  o cerință neexceptată peste `LINT_AC_MAX` criterii de acceptare\n  When   `lint --decompose` rulează\n  Then   constatarea `ac-count-high` tot se afișează, iar niciun fișier nu este creat pentru ea"
        }
      }
    },
    {
      "id": "ARCH-DESCRIPTION-057",
      "layer": "bus",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "One Description section, and Cases instead of Acceptance",
      "intent": "A reader met the same capability twice under two headings that both said WHAT — once as rationale, once as obligation — and the acceptance section was named after a sign-off step rather than after the cases it holds.",
      "contract": [
        "`## Description` merges the intent quote with the binding clauses into one section, and `## Cases` (labelled `CASE-1`, `CASE-2`, …) replaces the older acceptance heading and its `AC-N` labels.",
        "A requirement still written with the older `## WHAT — Contract`, `## HOW — Acceptance` and `AC-N` spellings parses to the identical clause list and criterion count.",
        "A `# verifies: <ID>#CASE-N` or `#AC-N` tag resolves under either spelling.",
        "The intent quote sits inside the normative `## Description` section but is excluded from the drift hash, so improving the WHY never drifts a confirmed contract; editing a Contract clause or a Cases criterion still drifts."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — both spellings parse to the same clauses and criterion count Given  the same requirement written once in the current form and once in the legacy form When   the engine parses each Then   both yield the identical Contract clause list and the identical criterion count",
        "CASE-2 — — a verifies tag resolves under either label Given  a `# verifies: <ID>#CASE-N` tag and a `# verifies: <ID>#AC-N` tag When   the engine matches each tag to its requirement's criteria Then   both resolve, because the label is an identifier a tag points at, not a fixed spelling",
        "CASE-3 — — the intent quote is excluded from the drift hash Given  a confirmed requirement whose `>` intent quote is edited but whose Contract and Cases are not When   `binding_hash` runs before and after the edit Then   the hash is unchanged",
        "CASE-4 — — editing a Contract clause or a Cases criterion still drifts Given  the same requirement edited once in a Contract clause and once in a Cases criterion When   `binding_hash` runs before and after each edit Then   both edits change the hash",
        "CASE-5 — — the intent is still read from inside the section Given  a requirement in the current form and one in the legacy form, both with the same intent quote When   `_first_quote` reads each Then   both return the identical intent text"
      ],
      "accept": "CASE-1 — both spellings parse to the same clauses and criterion count\n  Given  the same requirement written once in the current form and once in the legacy form\n  When   the engine parses each\n  Then   both yield the identical Contract clause list and the identical criterion count\n\nCASE-2 — a verifies tag resolves under either label\n  Given  a `# verifies: <ID>#CASE-N` tag and a `# verifies: <ID>#AC-N` tag\n  When   the engine matches each tag to its requirement's criteria\n  Then   both resolve, because the label is an identifier a tag points at, not a fixed spelling\n\nCASE-3 — the intent quote is excluded from the drift hash\n  Given  a confirmed requirement whose `>` intent quote is edited but whose Contract and\n         Cases are not\n  When   `binding_hash` runs before and after the edit\n  Then   the hash is unchanged\n\nCASE-4 — editing a Contract clause or a Cases criterion still drifts\n  Given  the same requirement edited once in a Contract clause and once in a Cases criterion\n  When   `binding_hash` runs before and after each edit\n  Then   both edits change the hash\n\nCASE-5 — the intent is still read from inside the section\n  Given  a requirement in the current form and one in the legacy form, both with the same\n         intent quote\n  When   `_first_quote` reads each\n  Then   both return the identical intent text",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001",
        "ARCH-DRIFT-003"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-AUTHOR-101"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "generated-from",
          "loc": "docs/full_architecture.html:4"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1849"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1857"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2231"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2238"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7648"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "should-have",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "O singură secțiune Description, și Cases în loc de Acceptance",
          "intent": "Un cititor întâlnea aceeași capabilitate de două ori sub două titluri care spuneau amândouă WHAT — o dată ca motivație, o dată ca obligație — iar secțiunea de acceptare era denumită după un pas de aprobare, nu după cazurile pe care le conține.",
          "contract": "> Un cititor întâlnea aceeași capabilitate de două ori sub două titluri care spuneau amândouă WHAT — o dată ca motivație, o dată ca obligație — iar secțiunea de acceptare era denumită după un pas de aprobare, nu după cazurile pe care le conține.\n\nFiecare punct de mai jos este obligatoriu.\n- `## Description` fuzionează citatul de intenție cu clauzele obligatorii într-o singură secțiune, iar `## Cases` (etichetat `CASE-1`, `CASE-2`, …) înlocuiește vechiul titlu de acceptare și etichetele sale `AC-N`.\n- O cerință scrisă încă cu vechile forme `## WHAT — Contract`, `## HOW — Acceptance` și `AC-N` se analizează la aceeași listă de clauze și același număr de criterii.\n- O etichetă `# verifies: <ID>#CASE-N` sau `#AC-N` se rezolvă sub oricare dintre forme.\n- Citatul de intenție se află în interiorul secțiunii normative `## Description`, dar este exclus din hash-ul de drift, astfel încât îmbunătățirea explicației WHY nu produce niciodată drift pentru un contract confirmat; editarea unei clauze Contract sau a unui criteriu Cases produce în continuare drift.",
          "acceptance": "CASE-1 — ambele forme se analizează la aceleași clauze și număr de criterii\n  Given  aceeași cerință scrisă o dată în forma curentă și o dată în forma veche\n  When   motorul analizează fiecare\n  Then   ambele produc aceeași listă de clauze Contract și același număr de criterii\n\nCASE-2 — o etichetă verifies se rezolvă sub oricare etichetă\n  Given  o etichetă `# verifies: <ID>#CASE-N` și o etichetă `# verifies: <ID>#AC-N`\n  When   motorul potrivește fiecare etichetă cu criteriile cerinței sale\n  Then   ambele se rezolvă, deoarece eticheta este un identificator spre care indică o etichetă, nu o formă fixă\n\nCASE-3 — citatul de intenție este exclus din hash-ul de drift\n  Given  o cerință confirmată al cărei citat de intenție `>` este editat, dar al cărei Contract și\n         Cases nu sunt\n  When   `binding_hash` rulează înainte și după editare\n  Then   hash-ul rămâne neschimbat\n\nCASE-4 — editarea unei clauze Contract sau a unui criteriu Cases produce în continuare drift\n  Given  aceeași cerință editată o dată într-o clauză Contract și o dată într-un criteriu Cases\n  When   `binding_hash` rulează înainte și după fiecare editare\n  Then   ambele editări schimbă hash-ul\n\nCASE-5 — intenția este încă citită din interiorul secțiunii\n  Given  o cerință în forma curentă și una în forma veche, ambele cu același\n         citat de intenție\n  When   `_first_quote` citește fiecare\n  Then   ambele returnează textul de intenție identic"
        }
      }
    },
    {
      "id": "ARCH-DESIGN-061",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Advisory design review",
      "intent": "Code written in a long AI session drifts toward procedural sprawl: state in module globals, six-parameter functions, `isinstance` chains that dispatch by hand, files that never stop growing. `design` reads the repo's code (Python through `ast`, the brace languages through masked-text heuristics) and names the shapes the four OOP pillars would fix, plus the house standards a reviewer checks by eye, so the reader who came to understand a file also sees where its design pulls against it. It advises; it never gates.",
      "contract": [
        "`design` reports encapsulation and abstraction candidates: module state written from functions, long parameter lists, data clumps, long or deeply nested functions, prefix families. [[REQ-DESIGN-950]]",
        "`design` reports inheritance and polymorphism candidates: unrelated classes sharing method names or bodies, `isinstance` chains, equality switches on one value. [[REQ-DESIGN-951]]",
        "`design` prints the candidates grouped by pillar with one advice line each, emits JSON on request, skips test files, always exits 0, never enters the gate. [[REQ-DESIGN-952]]",
        "`design` reports house standards per file: length, wide lines, public definitions without a docstring, definitions per file. [[REQ-DESIGN-953]]",
        "The same analysis folds into one design score that rides in `_map.json`, the `_map.md` header (as `design OOP:`) and `health`. [[REQ-DESIGN-954]]",
        "JS/TS, C/C++, Java, C#, Go, Rust, Kotlin, Swift, Scala, Dart and PHP are read through brace-matching heuristics that feed the same shape checks. [[REQ-DESIGN-955]]",
        "The candidates themselves ride in `_map.json` beside their score, so the viewer lists them instead of only counting them. [[REQ-DESIGN-976]]",
        "A `metrics` pillar measures each Python class with the three Chidamber & Kemerer metrics that apply here — WMC, RFC and LCOM. [[REQ-DESIGN-978]]"
      ],
      "verify": [],
      "notes": [
        "Python is the only language read through a real parser (`ast`); the brace languages go through heuristics, so a candidate there is a stronger invitation to look than a fact.",
        "A candidate is a shape worth a look, never a defect. Run on this repo it reports the engine's own long functions and its 7,800-line file honestly; ADR-0014 keeps that file whole on purpose, and an advisory line does not reopen a decision.",
        "-------------------"
      ],
      "current_impl": [],
      "acc": [
        "CASE-1 — Given  a repo with one Python module writing a global from a function, and a test file doing the same When   `design` runs Then   the module's candidate is printed under Encapsulation, the test file is not mentioned, and the exit code is 0",
        "CASE-2 — Given  a `requirements/_config.json` setting `DESIGN_PARAMS_MAX` to 2 When   `design` runs over a three-parameter function Then   it reports a long parameter list",
        "CASE-3 — Given  the engine's command registry and gate rule registry When   both are inspected Then   `design` is a registered command and no gate rule invokes it",
        "CASE-4 — Given  a repo with one clean Python module When   `map` and `health --json` run Then   `_map.json` carries `design.score` 100 and the health JSON carries `design_score` 100",
        "CASE-5 — Given  a JavaScript file with a seven-parameter function and a four-case switch When   `design` runs Then   it reports a long parameter list and a type switch, the same kinds a Python file would"
      ],
      "accept": "CASE-1\n  Given  a repo with one Python module writing a global from a function, and a test file doing the same\n  When   `design` runs\n  Then   the module's candidate is printed under Encapsulation, the test file is not mentioned, and the exit code is 0\n\nCASE-2\n  Given  a `requirements/_config.json` setting `DESIGN_PARAMS_MAX` to 2\n  When   `design` runs over a three-parameter function\n  Then   it reports a long parameter list\n\nCASE-3\n  Given  the engine's command registry and gate rule registry\n  When   both are inspected\n  Then   `design` is a registered command and no gate rule invokes it\n\nCASE-4\n  Given  a repo with one clean Python module\n  When   `map` and `health --json` run\n  Then   `_map.json` carries `design.score` 100 and the health JSON carries `design_score` 100\n\nCASE-5\n  Given  a JavaScript file with a seven-parameter function and a four-case switch\n  When   `design` runs\n  Then   it reports a long parameter list and a type switch, the same kinds a Python file would",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-SCAN-002",
        "ARCH-CONFIG-060",
        "ARCH-CMDREGISTRY-033"
      ],
      "used_by": [
        "ARCH-AUDIT-065"
      ],
      "satisfies": [
        "SYS-READ-103"
      ],
      "satisfied_by": [
        "REQ-DESIGN-950",
        "REQ-DESIGN-951",
        "REQ-DESIGN-952",
        "REQ-DESIGN-953",
        "REQ-DESIGN-954",
        "REQ-DESIGN-955",
        "REQ-DESIGN-976",
        "REQ-DESIGN-978"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8461"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8644"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8881"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8902"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8944"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8202"
        }
      ],
      "test_exempt": null,
      "milestone": "v3.4",
      "priority": "could-have",
      "risks": [],
      "clauses": 5,
      "covered": 5
    },
    {
      "id": "REQ-DESIGN-950",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Encapsulation and abstraction candidates",
      "intent": "Encapsulation asks who owns a piece of state; abstraction asks whether a name says what a block does. `_design_file` answers both from the syntax tree alone, with thresholds a repo can tune, so the signals stay cheap and deterministic.",
      "contract": [
        "A function containing a `global` statement is a `global-state` candidate naming the globals it writes.",
        "A function with more than `DESIGN_PARAMS_MAX` parameters (`self`/`cls` excluded) is a `long-parameter-list` candidate.",
        "A set of at least `DESIGN_CLUMP_MIN` parameter names shared by at least `DESIGN_CLUMP_FUNCS` functions is one `data-clump` candidate, reported once at the first carrier.",
        "A function spanning more than `DESIGN_FUNC_MAX_LINES` lines is a `long-function` candidate; one nesting `if`/`for`/`while`/`with`/`try` deeper than `DESIGN_NESTING_MAX` is a `deep-nesting` candidate.",
        "At least `DESIGN_PREFIX_GROUP` top-level functions whose names share the token before the first underscore form one `prefix-family` candidate named by that token.",
        "Every candidate carries `pillar`, `kind`, `file`, `line`, `name`, `detail` and an `advice` sentence from `_DESIGN_ADVICE`."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — global state and a long parameter list Given  a module with a function writing a global and a seven-parameter function When   `_design_file` reads it Then   it reports `global-state` and `long-parameter-list`",
        "CASE-2 — — a data clump needs three carriers Given  two functions sharing `host, port, user`, then a third When   `_design_file` reads each version Then   nothing is reported for two, one `data-clump` under encapsulation for three",
        "CASE-3 — — long and deeply nested functions Given  a 90-line function, a five-level nested function and a flat one When   `_design_file` reads them Then   `long-function` and `deep-nesting` are reported and the flat one is silent",
        "CASE-4 — — a prefix family Given  six top-level functions named `_scan_0` to `_scan_5` When   `_design_file` reads them Then   one `prefix-family` candidate named `scan` is reported under abstraction --------------------"
      ],
      "accept": "CASE-1 — global state and a long parameter list\n  Given  a module with a function writing a global and a seven-parameter function\n  When   `_design_file` reads it\n  Then   it reports `global-state` and `long-parameter-list`\n\nCASE-2 — a data clump needs three carriers\n  Given  two functions sharing `host, port, user`, then a third\n  When   `_design_file` reads each version\n  Then   nothing is reported for two, one `data-clump` under encapsulation for three\n\nCASE-3 — long and deeply nested functions\n  Given  a 90-line function, a five-level nested function and a flat one\n  When   `_design_file` reads them\n  Then   `long-function` and `deep-nesting` are reported and the flat one is silent\n\nCASE-4 — a prefix family\n  Given  six top-level functions named `_scan_0` to `_scan_5`\n  When   `_design_file` reads them\n  Then   one `prefix-family` candidate named `scan` is reported under abstraction\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-DESIGN-061"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8479"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8694"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8881"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8202"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Candidați pentru încapsulare și abstractizare",
          "intent": "Încapsularea întreabă cine deține o bucată de stare; abstractizarea întreabă dacă un nume spune ce face un bloc. `_design_file` răspunde la ambele doar din arborele de sintaxă, cu praguri pe care un repo le poate ajusta, astfel încât semnalele rămân ieftine și deterministe.",
          "contract": "> Încapsularea întreabă cine deține o bucată de stare; abstractizarea întreabă dacă un nume spune\n> ce face un bloc. `_design_file` răspunde la ambele doar din arborele de sintaxă, cu\n> praguri pe care un repo le poate ajusta, astfel încât semnalele rămân ieftine și deterministe.\n\nFiecare punct de mai jos este obligatoriu.\n- O funcție care conține o instrucțiune `global` este un candidat `global-state` care numește variabilele globale pe care le scrie.\n- O funcție cu mai mult de `DESIGN_PARAMS_MAX` parametri (excluzând `self`/`cls`) este un candidat `long-parameter-list`.\n- Un set de cel puțin `DESIGN_CLUMP_MIN` nume de parametri, partajate de cel puțin `DESIGN_CLUMP_FUNCS` funcții, este un candidat `data-clump`, raportat o singură dată la primul purtător.\n- O funcție care se întinde pe mai mult de `DESIGN_FUNC_MAX_LINES` linii este un candidat `long-function`; o imbricare `if`/`for`/`while`/`with`/`try` mai adâncă decât `DESIGN_NESTING_MAX` este un candidat `deep-nesting`.\n- Cel puțin `DESIGN_PREFIX_GROUP` funcții de nivel superior ale căror nume partajează tokenul dinaintea primei linii de subliniere formează un candidat `prefix-family` numit după acel token.\n- Fiecare candidat poartă `pillar`, `kind`, `file`, `line`, `name`, `detail` și o propoziție `advice` din `_DESIGN_ADVICE`.",
          "acceptance": "CASE-1 — stare globală și o listă lungă de parametri\n  Given  un modul cu o funcție care scrie o variabilă globală și o funcție cu șapte parametri\n  When   `_design_file` îl citește\n  Then   raportează `global-state` și `long-parameter-list`\n\nCASE-2 — un data clump are nevoie de trei purtători\n  Given  două funcții care partajează `host, port, user`, apoi o a treia\n  When   `_design_file` citește fiecare versiune\n  Then   nu se raportează nimic pentru două, un `data-clump` sub încapsulare pentru trei\n\nCASE-3 — funcții lungi și profund imbricate\n  Given  o funcție de 90 de linii, o funcție imbricată pe cinci niveluri și una plată\n  When   `_design_file` le citește\n  Then   `long-function` și `deep-nesting` sunt raportate, iar cea plată rămâne tăcută\n\nCASE-4 — o familie de prefix\n  Given  șase funcții de nivel superior numite de la `_scan_0` la `_scan_5`\n  When   `_design_file` le citește\n  Then   este raportat un candidat `prefix-family` numit `scan` sub abstractizare\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-DESIGN-951",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Inheritance and polymorphism candidates",
      "intent": "Two classes with the same three method names describe one interface nobody wrote down; a chain of `isinstance` tests is dispatch done by hand. Both are visible in the tree without running anything, and both are exactly the shapes a reader new to the file has to reconstruct in their head.",
      "contract": [
        "Two classes in one file with no common base, neither deriving from the other, sharing at least `DESIGN_SHARED_METHODS` non-dunder method names are one `shared-methods` candidate.",
        "A method whose body is identical (by `ast.dump`) in two such classes is a `duplicate-method` candidate reported at the second class.",
        "Classes related through a base, in either direction, are never reported.",
        "An `if`/`elif` chain holding at least `DESIGN_ISINSTANCE_CHAIN` `isinstance` tests on one name is an `isinstance-chain` candidate.",
        "An `if`/`elif` chain holding at least `DESIGN_BRANCH_CHAIN` equality tests of one name against constants is a `type-switch` candidate.",
        "A chain below either threshold is silent, and an `elif` is never reported as its own chain."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — shared and duplicated methods Given  classes `A` and `B` with no base sharing `load`, `save`, `close`, two bodies identical When   `_design_file` reads them Then   one `shared-methods` and two `duplicate-method` candidates (`B.close`, `B.save`) are reported",
        "CASE-2 — — related classes are silent Given  `A(Base)` and `B(Base)` sharing three methods When   `_design_file` reads them Then   no `shared-methods` candidate is reported",
        "CASE-3 — — isinstance chain and type switch Given  a three-branch `isinstance` chain on `x` and a four-branch equality chain on `kind` When   `_design_file` reads them Then   exactly one `isinstance-chain` (`x`) and one `type-switch` (`kind`) are reported, both under polymorphism",
        "CASE-4 — — short chains are silent Given  a two-branch `isinstance` chain and a three-branch equality chain When   `_design_file` reads them Then   nothing is reported --------------------"
      ],
      "accept": "CASE-1 — shared and duplicated methods\n  Given  classes `A` and `B` with no base sharing `load`, `save`, `close`, two bodies identical\n  When   `_design_file` reads them\n  Then   one `shared-methods` and two `duplicate-method` candidates (`B.close`, `B.save`) are reported\n\nCASE-2 — related classes are silent\n  Given  `A(Base)` and `B(Base)` sharing three methods\n  When   `_design_file` reads them\n  Then   no `shared-methods` candidate is reported\n\nCASE-3 — isinstance chain and type switch\n  Given  a three-branch `isinstance` chain on `x` and a four-branch equality chain on `kind`\n  When   `_design_file` reads them\n  Then   exactly one `isinstance-chain` (`x`) and one `type-switch` (`kind`) are reported, both under polymorphism\n\nCASE-4 — short chains are silent\n  Given  a two-branch `isinstance` chain and a three-branch equality chain\n  When   `_design_file` reads them\n  Then   nothing is reported\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-DESIGN-061"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8479"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8535"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8694"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8881"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8202"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Candidați pentru moștenire și polimorfism",
          "intent": "Două clase cu aceleași trei nume de metode descriu o interfață pe care nimeni nu a scris-o; un lanț de teste `isinstance` este dispatch făcut manual. Ambele sunt vizibile în arbore fără a rula ceva, și ambele sunt exact formele pe care un cititor nou al fișierului trebuie să le reconstruiască în minte.",
          "contract": "> Două clase cu aceleași trei nume de metode descriu o interfață pe care nimeni nu a scris-o; un\n> lanț de teste `isinstance` este dispatch făcut manual. Ambele sunt vizibile în\n> arbore fără a rula ceva, și ambele sunt exact formele pe care un cititor nou al\n> fișierului trebuie să le reconstruiască în minte.\n\nFiecare punct de mai jos este obligatoriu.\n- Două clase dintr-un fișier fără bază comună, niciuna derivând din cealaltă, care partajează cel puțin `DESIGN_SHARED_METHODS` nume de metode non-dunder sunt un candidat `shared-methods`.\n- O metodă al cărei corp este identic (după `ast.dump`) în două astfel de clase este un candidat `duplicate-method` raportat la a doua clasă.\n- Clasele legate printr-o bază, în oricare direcție, nu sunt niciodată raportate.\n- Un lanț `if`/`elif` care conține cel puțin `DESIGN_ISINSTANCE_CHAIN` teste `isinstance` pe un singur nume este un candidat `isinstance-chain`.\n- Un lanț `if`/`elif` care conține cel puțin `DESIGN_BRANCH_CHAIN` teste de egalitate ale unui nume față de constante este un candidat `type-switch`.\n- Un lanț sub oricare dintre praguri rămâne tăcut, iar un `elif` nu este niciodată raportat ca propriul său lanț.",
          "acceptance": "CASE-1 — metode partajate și duplicate\n  Given  clasele `A` și `B` fără bază, partajând `load`, `save`, `close`, cu două corpuri identice\n  When   `_design_file` le citește\n  Then   este raportat un `shared-methods` și două candidate `duplicate-method` (`B.close`, `B.save`)\n\nCASE-2 — clasele înrudite rămân tăcute\n  Given  `A(Base)` și `B(Base)` partajând trei metode\n  When   `_design_file` le citește\n  Then   nu este raportat niciun candidat `shared-methods`\n\nCASE-3 — lanț isinstance și type switch\n  Given  un lanț `isinstance` cu trei ramuri pe `x` și un lanț de egalitate cu patru ramuri pe `kind`\n  When   `_design_file` le citește\n  Then   sunt raportate exact un `isinstance-chain` (`x`) și un `type-switch` (`kind`), ambele sub polimorfism\n\nCASE-4 — lanțurile scurte rămân tăcute\n  Given  un lanț `isinstance` cu două ramuri și un lanț de egalitate cu trei ramuri\n  When   `_design_file` le citește\n  Then   nu se raportează nimic\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-DESIGN-952",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The `design` report",
      "intent": "The report is read by a person deciding what to refactor next, and by a tool that wants the same list as data. It walks the same tree the scanner walks, leaves test files out (a long test is not a design smell), and never changes an exit code.",
      "contract": [
        "`design` walks `code_root` with the scanner's walk (`.reqmapignore` honoured), reads the program-logic files (`DESIGN_EXTS`) and skips test paths (`_is_test_path`).",
        "`design` prints one block per group in the order encapsulation, abstraction, inheritance, polymorphism, standards, each line as `file:line  kind  detail`, followed by the distinct advice sentences of that block.",
        "`design --json` emits `{\"files\": N, \"findings\": [...]}` with every candidate record and nothing on stdout besides the JSON.",
        "With no candidate, `design` prints one line saying so and the file count.",
        "A file that does not parse yields no candidate and no error.",
        "`design` exits 0 in every case and no gate rule reads it; the thresholds are `CONFIG_KEYS` entries."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — grouped report, tests skipped, exit 0 Given  a module and a test file both writing a global When   `cmd_design` runs Then   it returns 0, prints `Encapsulation (1)` with the module's line, omits the test file and ends with the advisory note",
        "CASE-2 — — JSON and the clean case Given  a repo with one clean, documented module When   `cmd_design` runs with and without `--json` Then   the JSON reads `files 1, findings []` and the text run says no candidates were found",
        "CASE-3 — — a syntax error is not a finding Given  a file that does not parse When   `_design_file` reads it Then   it returns an empty list",
        "CASE-4 — — thresholds come from the config Given  `DESIGN_PARAMS_MAX` set to 2 through `apply_config` When   `_design_file` reads a three-parameter function Then   it reports `long-parameter-list` --------------------"
      ],
      "accept": "CASE-1 — grouped report, tests skipped, exit 0\n  Given  a module and a test file both writing a global\n  When   `cmd_design` runs\n  Then   it returns 0, prints `Encapsulation (1)` with the module's line, omits the test file and ends with the advisory note\n\nCASE-2 — JSON and the clean case\n  Given  a repo with one clean, documented module\n  When   `cmd_design` runs with and without `--json`\n  Then   the JSON reads `files 1, findings []` and the text run says no candidates were found\n\nCASE-3 — a syntax error is not a finding\n  Given  a file that does not parse\n  When   `_design_file` reads it\n  Then   it returns an empty list\n\nCASE-4 — thresholds come from the config\n  Given  `DESIGN_PARAMS_MAX` set to 2 through `apply_config`\n  When   `_design_file` reads a three-parameter function\n  Then   it reports `long-parameter-list`\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-DESIGN-061"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8944"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8202"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Raportul `design`",
          "intent": "Raportul este citit de o persoană care decide ce să refactorizeze în continuare și de un tool care vrea aceeași listă sub formă de date. Parcurge același arbore pe care îl parcurge scanerul, exclude fișierele de test (un test lung nu este un miros de design) și nu modifică niciodată un cod de ieșire.",
          "contract": "> Raportul este citit de o persoană care decide ce să refactorizeze în continuare, și de un tool care\n> vrea aceeași listă sub formă de date. Parcurge același arbore pe care îl parcurge scanerul, exclude\n> fișierele de test (un test lung nu este un miros de design), și nu modifică niciodată un cod de ieșire.\n\nFiecare punct de mai jos este obligatoriu.\n- `design` parcurge `code_root` cu parcurgerea scanerului (respectând `.reqmapignore`), citește fișierele de logică a programului (`DESIGN_EXTS`) și sare peste căile de test (`_is_test_path`).\n- `design` afișează un bloc pentru fiecare grup, în ordinea încapsulare, abstractizare, moștenire, polimorfism, standards, fiecare linie ca `file:line  kind  detail`, urmată de propozițiile de sfat distincte ale acelui bloc.\n- `design --json` emite `{\"files\": N, \"findings\": [...]}` cu fiecare înregistrare de candidat și nimic altceva pe stdout în afara JSON-ului.\n- Când nu există niciun candidat, `design` afișează o linie care spune asta și numărul de fișiere.\n- Un fișier care nu poate fi parsat nu produce niciun candidat și nicio eroare.\n- `design` iese cu 0 în orice caz, iar nicio regulă de gate nu îl citește; pragurile sunt intrări `CONFIG_KEYS`.",
          "acceptance": "CASE-1 — raport grupat, testele sunt omise, ieșire 0\n  Given  un modul și un fișier de test, ambele scriind o variabilă globală\n  When   `cmd_design` rulează\n  Then   returnează 0, afișează `Encapsulation (1)` cu linia modulului, omite fișierul de test și se încheie cu nota consultativă\n\nCASE-2 — JSON și cazul curat\n  Given  un repo cu un modul curat, documentat\n  When   `cmd_design` rulează cu și fără `--json`\n  Then   JSON-ul indică `files 1, findings []`, iar rularea text spune că nu a fost găsit niciun candidat\n\nCASE-3 — o eroare de sintaxă nu este o constatare\n  Given  un fișier care nu poate fi parsat\n  When   `_design_file` îl citește\n  Then   returnează o listă goală\n\nCASE-4 — pragurile provin din configurație\n  Given  `DESIGN_PARAMS_MAX` setat la 2 prin `apply_config`\n  When   `_design_file` citește o funcție cu trei parametri\n  Then   raportează `long-parameter-list`\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-DESIGN-953",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Code-writing standards",
      "intent": "The rules a reviewer checks by eye on every file, made mechanical and tunable: how long a file may grow, how wide a line, whether a public name explains itself, how many definitions one module holds. One finding per file per rule, so a 2,000-line file is one line in the report, not two thousand.",
      "contract": [
        "A file with more than `DESIGN_FILE_MAX_LINES` lines is one `file-too-long` candidate at line 1.",
        "A file with lines wider than `DESIGN_LINE_MAX` columns is one `line-too-long` candidate reporting the count and the first such line.",
        "A file with more than `DESIGN_FILE_MAX_FUNCS` top-level functions and classes is one `too-many-definitions` candidate.",
        "With `DESIGN_DOCSTRING_PUBLIC` set, a Python file whose public top-level functions or classes (no leading underscore) lack a docstring is one `missing-docstring` candidate naming them; set to 0, the check is off.",
        "Standards candidates carry the pillar `standards` and print as the last block of the report."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — every standard fires once per file Given  thresholds of 5 lines and 2 definitions, a file with three undocumented functions and one 120-column line When   `_design_file` reads it Then   `file-too-long`, `too-many-definitions`, one `line-too-long` at that line and one `missing-docstring` naming three definitions are reported, all under standards",
        "CASE-2 — — a small documented file is silent Given  a short file whose public function has a docstring and whose helper is private When   `_design_file` reads it Then   nothing is reported",
        "CASE-3 — — the docstring rule can be switched off Given  `DESIGN_DOCSTRING_PUBLIC` set to 0 through `apply_config` When   `_design_file` reads an undocumented public function Then   no `missing-docstring` candidate is reported",
        "CASE-4 — — standards print last Given  a module with a global write and no docstring When   `cmd_design` prints its report Then   the Encapsulation block precedes the Standards block --------------------"
      ],
      "accept": "CASE-1 — every standard fires once per file\n  Given  thresholds of 5 lines and 2 definitions, a file with three undocumented functions and one 120-column line\n  When   `_design_file` reads it\n  Then   `file-too-long`, `too-many-definitions`, one `line-too-long` at that line and one `missing-docstring` naming three definitions are reported, all under standards\n\nCASE-2 — a small documented file is silent\n  Given  a short file whose public function has a docstring and whose helper is private\n  When   `_design_file` reads it\n  Then   nothing is reported\n\nCASE-3 — the docstring rule can be switched off\n  Given  `DESIGN_DOCSTRING_PUBLIC` set to 0 through `apply_config`\n  When   `_design_file` reads an undocumented public function\n  Then   no `missing-docstring` candidate is reported\n\nCASE-4 — standards print last\n  Given  a module with a global write and no docstring\n  When   `cmd_design` prints its report\n  Then   the Encapsulation block precedes the Standards block\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-DESIGN-061"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8549"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8881"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8202"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Standarde de scriere a codului",
          "intent": "Regulile pe care un reviewer le verifică din ochi la fiecare fișier, transformate în ceva mecanic și ajustabil: cât de mult poate crește un fișier, cât de lată poate fi o linie, dacă un nume public se explică singur, câte definiții conține un modul. O constatare per fișier per regulă, astfel încât un fișier de 2,000 de linii este o singură linie în raport, nu două mii.",
          "contract": "> Regulile pe care un reviewer le verifică din ochi la fiecare fișier, transformate în ceva mecanic\n> și ajustabil: cât de mult poate crește un fișier, cât de lată poate fi o linie, dacă un nume public se\n> explică singur, câte definiții conține un modul. O constatare per fișier per regulă, astfel\n> încât un fișier de 2,000 de linii este o singură linie în raport, nu două mii.\n\nFiecare punct de mai jos este obligatoriu.\n- Un fișier cu mai mult de `DESIGN_FILE_MAX_LINES` linii este un candidat `file-too-long` la linia 1.\n- Un fișier cu linii mai late decât `DESIGN_LINE_MAX` coloane este un candidat `line-too-long` care raportează numărul și prima astfel de linie.\n- Un fișier cu mai mult de `DESIGN_FILE_MAX_FUNCS` funcții și clase de nivel superior este un candidat `too-many-definitions`.\n- Cu `DESIGN_DOCSTRING_PUBLIC` setat, un fișier Python ale cărui funcții sau clase publice de nivel superior (fără linie de subliniere inițială) nu au docstring este un candidat `missing-docstring` care le numește; setat la 0, verificarea este dezactivată.\n- Candidații standards poartă pilonul `standards` și sunt afișați ca ultimul bloc al raportului.",
          "acceptance": "CASE-1 — fiecare standard se declanșează o dată per fișier\n  Given  praguri de 5 linii și 2 definiții, un fișier cu trei funcții nedocumentate și o linie de 120 de coloane\n  When   `_design_file` îl citește\n  Then   sunt raportate `file-too-long`, `too-many-definitions`, un `line-too-long` la acea linie și un `missing-docstring` care numește trei definiții, toate sub standards\n\nCASE-2 — un fișier mic și documentat rămâne tăcut\n  Given  un fișier scurt a cărui funcție publică are docstring și al cărui helper este privat\n  When   `_design_file` îl citește\n  Then   nu se raportează nimic\n\nCASE-3 — regula docstring poate fi dezactivată\n  Given  `DESIGN_DOCSTRING_PUBLIC` setat la 0 prin `apply_config`\n  When   `_design_file` citește o funcție publică nedocumentată\n  Then   nu este raportat niciun candidat `missing-docstring`\n\nCASE-4 — standards se afișează ultimul\n  Given  un modul cu o scriere globală și fără docstring\n  When   `cmd_design` își afișează raportul\n  Then   blocul Encapsulation precede blocul Standards\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-DESIGN-954",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Design health in the map",
      "intent": "A list of candidates is something you read once; a number is something you watch. The same walk `design` runs is folded into one record — how many Python files carry no candidate at all — and that record rides in the committed map next to the requirement graph, in the map's header, and in `health`, so design drift shows up where requirement drift already does.",
      "contract": [
        "`_design_summary(code_root)` returns `{files, clean_files, score, candidates}` over the non-test program-logic files, `score` being the percentage of files with no candidate, `candidates` the count per group; it returns `None` when there is no such file.",
        "`_assemble_map_data` attaches that record as `design` in `_map.json`, and omits the key when the record is `None`, so a repo without Python gains no empty key.",
        "`_map.md`'s header carries a `design: S/100 (C/F source files without a design candidate)` line when the record exists.",
        "`health` prints a design line and `health --json` carries `design_score` and `design_files` when the record exists; both are absent otherwise.",
        "The record is deterministic, so `map --check` treats a changed design score like any other change to the committed map."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the score counts clean files Given  a clean documented module, a module writing a global, and a test file writing a global When   `_design_summary` runs Then   it reports 2 files, 1 clean, score 50, one encapsulation and one standards candidate",
        "CASE-2 — — the map header and health carry the score Given  a corpus and one clean module When   the map data is assembled and `health --json` runs Then   `_map.json` carries `design.score` 100, the `_map.md` header (as `design OOP:`) names it, and the JSON carries `design_score` 100",
        "CASE-3 — — no program logic, no key Given  a repo whose only code is a stylesheet When   the map data is assembled and `health --json` runs Then   neither `design` nor `design_score` is present --------------------"
      ],
      "accept": "CASE-1 — the score counts clean files\n  Given  a clean documented module, a module writing a global, and a test file writing a global\n  When   `_design_summary` runs\n  Then   it reports 2 files, 1 clean, score 50, one encapsulation and one standards candidate\n\nCASE-2 — the map header and health carry the score\n  Given  a corpus and one clean module\n  When   the map data is assembled and `health --json` runs\n  Then   `_map.json` carries `design.score` 100, the `_map.md` header (as `design OOP:`) names it, and the JSON carries `design_score` 100\n\nCASE-3 — no program logic, no key\n  Given  a repo whose only code is a stylesheet\n  When   the map data is assembled and `health --json` runs\n  Then   neither `design` nor `design_score` is present\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-DESIGN-061"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4249"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5908"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7368"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8902"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8202"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Sănătatea designului în hartă",
          "intent": "O listă de candidați este ceva ce citești o dată; un număr este ceva ce urmărești. Aceeași parcurgere pe care o face `design` este condensată într-o singură înregistrare — câte fișiere Python nu conțin niciun candidat — iar acea înregistrare călătorește în harta comisă alături de graful cerințelor, în antetul hărții și în `health`, astfel încât deriva de design apare acolo unde apare deja deriva cerințelor.",
          "contract": "> O listă de candidați este ceva ce citești o dată; un număr este ceva ce urmărești.\n> Aceeași parcurgere pe care o face `design` este condensată într-o singură înregistrare — câte fișiere Python\n> nu conțin niciun candidat — iar acea înregistrare călătorește în harta comisă alături de\n> graful cerințelor, în antetul hărții și în `health`, astfel încât deriva de design apare\n> acolo unde apare deja deriva cerințelor.\n\nFiecare punct de mai jos este obligatoriu.\n- `_design_summary(code_root)` returnează `{files, clean_files, score, candidates}` peste fișierele de logică de program care nu sunt de test, `score` fiind procentul de fișiere fără niciun candidat, iar `candidates` numărul pe grup; returnează `None` când nu există niciun astfel de fișier.\n- `_assemble_map_data` atașează acea înregistrare drept `design` în `_map.json` și omite cheia când înregistrarea este `None`, astfel încât un repo fără Python nu capătă o cheie goală.\n- Antetul lui `_map.md` conține o linie `design: S/100 (C/F source files without a design candidate)` atunci când înregistrarea există.\n- `health` afișează o linie de design, iar `health --json` conține `design_score` și `design_files` atunci când înregistrarea există; ambele lipsesc în caz contrar.\n- Înregistrarea este deterministă, așa că `map --check` tratează un scor de design modificat ca pe orice altă modificare a hărții comise.",
          "acceptance": "CASE-1 — scorul numără fișierele curate\n  Given  un modul curat, documentat, un modul care scrie într-o variabilă globală și un fișier de test care scrie într-o variabilă globală\n  When   rulează `_design_summary`\n  Then   raportează 2 fișiere, 1 curat, scor 50, un candidat de încapsulare și unul de standarde\n\nCASE-2 — antetul hărții și health conțin scorul\n  Given  un corpus și un modul curat\n  When   datele hărții sunt asamblate și rulează `health --json`\n  Then   `_map.json` conține `design.score` 100, antetul `_map.md` (ca `design OOP:`) îl numește, iar JSON-ul conține `design_score` 100\n\nCASE-3 — fără logică de program, fără cheie\n  Given  un repo al cărui unic cod este o foaie de stiluri\n  When   datele hărții sunt asamblate și rulează `health --json`\n  Then   nici `design`, nici `design_score` nu este prezent"
        }
      }
    },
    {
      "id": "REQ-DESIGN-955",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Brace-language heuristics",
      "intent": "The engine ships no parser for JavaScript, C++ or Java and never will (stdlib only), but a reviewer's eye does not need one: a function head, a matched pair of braces, a `switch` with many cases, two classes listing the same methods. `_design_brace` reads those shapes from the source with comments and strings masked out, and feeds the same shape checks the Python analyzer feeds, so every language reports the same kinds at the same thresholds.",
      "contract": [
        "`_design_mask` replaces comments (`//`, `/* */`) plus string, char and template literals with spaces of the same length. Every newline is kept, so brace matching never sees text.",
        "A function is a head matching `_BRACE_FUNC_RE` (`name(params) {`, with optional modifiers, return type or arrow form) whose name is not a control keyword; its length, nesting depth (brace depth inside the body) and parameter names feed `_design_shape_findings` exactly as Python's do.",
        "A class, struct or interface body is matched by braces. Its methods are the functions inside it, its bases the identifiers after `extends`/`implements`/`:`. Shared and duplicated methods come from the shared checks.",
        "An `if`/`else if` chain collects `instanceof`, `typeof`, `dynamic_cast` and `x is T` tests as type tests, and `x == literal` tests as equality tests; a `switch (x)` counts one equality test per `case`; both feed `_design_chain_findings`.",
        "The docstring rule does not apply outside Python; a program-logic file in a language with neither analyzer (Ruby, Elixir) gets the standards checks only."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — JavaScript shapes Given  a JS file with a seven-parameter function, two unrelated classes sharing three methods, a four-case switch and a three-branch instanceof chain When   `_design_file` reads it Then   it reports long-parameter-list, shared-methods, duplicate-method, a type-switch on `kind` and an isinstance-chain on `v`",
        "CASE-2 — — C++ shapes Given  a C++ file with a 90-line function and a three-branch dynamic_cast chain When   `_design_file` reads it Then   it reports long-function for `compute` and an isinstance-chain, and no missing-docstring",
        "CASE-3 — — masking Given  a JS function whose string literal and comment contain braces When   `_design_mask` runs and the file is analysed Then   the literal is gone from the masked text, the newline count is unchanged, and no length or nesting candidate is reported",
        "CASE-4 — — standards only elsewhere Given  a Ruby file with one 120-column line When   `_design_file` reads it Then   only line-too-long is reported, and a clean Ruby file reports nothing"
      ],
      "accept": "CASE-1 — JavaScript shapes\n  Given  a JS file with a seven-parameter function, two unrelated classes sharing three methods, a four-case switch and a three-branch instanceof chain\n  When   `_design_file` reads it\n  Then   it reports long-parameter-list, shared-methods, duplicate-method, a type-switch on `kind` and an isinstance-chain on `v`\n\nCASE-2 — C++ shapes\n  Given  a C++ file with a 90-line function and a three-branch dynamic_cast chain\n  When   `_design_file` reads it\n  Then   it reports long-function for `compute` and an isinstance-chain, and no missing-docstring\n\nCASE-3 — masking\n  Given  a JS function whose string literal and comment contain braces\n  When   `_design_mask` runs and the file is analysed\n  Then   the literal is gone from the masked text, the newline count is unchanged, and no length or nesting candidate is reported\n\nCASE-4 — standards only elsewhere\n  Given  a Ruby file with one 120-column line\n  When   `_design_file` reads it\n  Then   only line-too-long is reported, and a clean Ruby file reports nothing",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-DESIGN-061"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8812"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8881"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8202"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Euristici pentru limbaje cu acolade",
          "intent": "Motorul nu livrează niciun parser pentru JavaScript, C++ sau Java și nu o va face niciodată (doar stdlib), dar ochiul unui reviewer nu are nevoie de unul: un antet de funcție, o pereche potrivită de acolade, un `switch` cu multe cazuri, două clase care listează aceleași metode. `_design_brace` citește acele forme din sursă cu comentariile și șirurile mascate, și alimentează aceleași verificări de formă pe care le alimentează analizorul Python, astfel încât fiecare limbaj raportează aceleași tipuri la aceleași praguri.",
          "contract": "> Motorul nu livrează niciun parser pentru JavaScript, C++ sau Java și nu o va face niciodată (doar stdlib),\n> dar ochiul unui reviewer nu are nevoie de unul: un antet de funcție, o pereche potrivită de acolade, un\n> `switch` cu multe cazuri, două clase care listează aceleași metode. `_design_brace` citește\n> acele forme din sursă cu comentariile și șirurile mascate, și alimentează aceleași verificări de formă\n> pe care le alimentează analizorul Python, astfel încât fiecare limbaj raportează aceleași tipuri la\n> aceleași praguri.\n\nFiecare punct de mai jos este obligatoriu.\n- `_design_mask` înlocuiește comentariile (`//`, `/* */`) plus literalii de tip string, char și template cu spații de aceeași lungime. Fiecare linie nouă este păstrată, astfel încât potrivirea acoladelor nu vede niciodată text.\n- O funcție este un antet care se potrivește cu `_BRACE_FUNC_RE` (`name(params) {`, cu modificatori opționali, tip de retur sau formă arrow) al cărui nume nu este un cuvânt cheie de control; lungimea, adâncimea de imbricare (adâncimea acoladelor în interiorul corpului) și numele parametrilor alimentează `_design_shape_findings` exact ca la Python.\n- Un corp de class, struct sau interface este potrivit prin acolade. Metodele sale sunt funcțiile din interiorul lui, bazele sale sunt identificatorii de după `extends`/`implements`/`:`. Metodele partajate și duplicate provin din verificările partajate.\n- Un lanț `if`/`else if` colectează testele `instanceof`, `typeof`, `dynamic_cast` și `x is T` ca teste de tip, iar testele `x == literal` ca teste de egalitate; un `switch (x)` numără un test de egalitate per `case`; ambele alimentează `_design_chain_findings`.\n- Regula docstring nu se aplică în afara Python; un fișier de logică a programului într-un limbaj fără niciun analizor (Ruby, Elixir) primește doar verificările standards.",
          "acceptance": "CASE-1 — forme JavaScript\n  Given  un fișier JS cu o funcție cu șapte parametri, două clase neînrudite care partajează trei metode, un switch cu patru cazuri și un lanț instanceof cu trei ramuri\n  When   `_design_file` îl citește\n  Then   raportează long-parameter-list, shared-methods, duplicate-method, un type-switch pe `kind` și un isinstance-chain pe `v`\n\nCASE-2 — forme C++\n  Given  un fișier C++ cu o funcție de 90 de linii și un lanț dynamic_cast cu trei ramuri\n  When   `_design_file` îl citește\n  Then   raportează long-function pentru `compute` și un isinstance-chain, și niciun missing-docstring\n\nCASE-3 — mascare\n  Given  o funcție JS al cărei literal de tip string și comentariu conțin acolade\n  When   `_design_mask` rulează, iar fișierul este analizat\n  Then   literalul dispare din textul mascat, numărul de linii noi rămâne neschimbat, și nu este raportat niciun candidat de lungime sau imbricare\n\nCASE-4 — doar standards în altă parte\n  Given  un fișier Ruby cu o linie de 120 de coloane\n  When   `_design_file` îl citește\n  Then   este raportat doar line-too-long, iar un fișier Ruby curat nu raportează nimic"
        }
      }
    },
    {
      "id": "REQ-DESIGN-976",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Design candidates in the map",
      "intent": "The score told a reader that design was at 23/100 and stopped there. To learn *which* shapes cost the other 77 they had to leave the map and run `gate --design` in a terminal, which is the moment most readers stop looking. The candidates now travel in the committed map beside the score they explain, so the viewer can list them in a tab of their own.",
      "contract": [
        "`_design_summary` takes `with_findings`, off by default; when it is on the returned record also carries `findings`, one entry per candidate with its `pillar`, `kind`, `file`, `line`, `name` and `detail`.",
        "The advice text is emitted once per `kind` in a sibling `advice` object, never repeated on each entry, because the advice belongs to the rule rather than to the occurrence.",
        "`_assemble_map_data` is the only caller that asks for the candidates, so `_map.json` carries them and `health --json` keeps the small score-only record a CI badge reads.",
        "The candidates are ordered by the walk that produced them, so a repo that did not change produces a byte-identical `design` block and `map --check` stays quiet."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — off by default Given  a repo with one Python file carrying a design candidate When   `_design_summary` runs without `with_findings` Then   the record carries `files`, `clean_files`, `score` and `candidates`, and neither `findings` nor `advice`",
        "CASE-2 — — the map carries them Given  the same repo When   `map` runs Then   `_map.json`'s `design` object carries a `findings` list whose length equals the summed `candidates` counts, and an `advice` entry for every `kind` present",
        "CASE-3 — — the badge payload is unchanged Given  the same repo When   `health --json` runs Then   its output carries no `findings` key",
        "CASE-4 — — deterministic Given  a repo whose code did not change When   `map` runs twice Then   both runs write the same `design` block"
      ],
      "accept": "CASE-1 — off by default\n  Given  a repo with one Python file carrying a design candidate\n  When   `_design_summary` runs without `with_findings`\n  Then   the record carries `files`, `clean_files`, `score` and `candidates`, and neither `findings` nor `advice`\n\nCASE-2 — the map carries them\n  Given  the same repo\n  When   `map` runs\n  Then   `_map.json`'s `design` object carries a `findings` list whose length equals the summed `candidates` counts, and an `advice` entry for every `kind` present\n\nCASE-3 — the badge payload is unchanged\n  Given  the same repo\n  When   `health --json` runs\n  Then   its output carries no `findings` key\n\nCASE-4 — deterministic\n  Given  a repo whose code did not change\n  When   `map` runs twice\n  Then   both runs write the same `design` block",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-DESIGN-061"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4249"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8902"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8202"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4
    },
    {
      "id": "REQ-DESIGN-978",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Class metrics, the C&K half that applies",
      "intent": "Every other check in this review measures a function or a file, so the one shape it could never name is the class that quietly became several: `Scene` carries 62 methods over 14 fields and each individual method looks reasonable. Chidamber & Kemerer measure the class itself, and three of their six say something about code shaped like this one. The pillar reports those three and deliberately omits the rest.",
      "contract": [
        "`_design_metrics` reports `god-class` when a class declares more than `DESIGN_WMC_MAX` methods (C&K WMC, unweighted).",
        "`_design_metrics` reports `high-response` when a class's own methods plus the distinct method names they call exceed `DESIGN_RFC_MAX` (C&K RFC).",
        "`_design_metrics` reports `low-cohesion` when LCOM1 exceeds `DESIGN_LCOM_MAX`. LCOM1 counts the method pairs sharing no instance field, less the pairs that share one.",
        "A class's instance fields are the names assigned through `self.<name>` plus those `__slots__` declares; a class with no such field is skipped for cohesion, because a `dict` subclass keys its state elsewhere and would otherwise score as maximally incohesive.",
        "The pillar reads Python only, through `ast`, since counting methods and field access needs a parser rather than the brace-language heuristics.",
        "DIT, NOC and CBO are not reported. Their absence is a decision, not a gap: the first two measure an inheritance tree a composing codebase does not have. The third needs type inference this engine does not do.",
        "The three thresholds are `CONFIG_KEYS` entries, so a repo can retune them like every other number in the review; it stays advisory and never enters the gate."
      ],
      "verify": [],
      "notes": [
        "LCOM1 has a known blind spot this engine does not correct: a constructor that assigns every field pairs with every other method, so a class with two otherwise disjoint halves scores zero as soon as its `__init__` touches both. The metric still finds the shape it was added for — `Scene`, whose 62 methods spread over 14 fields score 1409 — but a low LCOM is weaker evidence than a high one.",
        "WMC is unweighted here. C&K define it as the sum of a per-method complexity weight; counting each method as 1 is the conventional simplification, and it is the reading `god-class` reports.",
        "These numbers are advisory like every other candidate. `Scene` is reported and nothing gates on it; splitting it would change the public builder API, which is a semver decision rather than a lint fix."
      ],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a wide class is named Given  a Python class with more methods than `DESIGN_WMC_MAX` When   `design` runs Then   a `god-class` candidate is reported under Metrics, naming the class and its method count",
        "CASE-2 — — a dict subclass is not accused of incohesion Given  a class subclassing `dict` with several methods and no `self.<name>` assignment When   `design` runs Then   no `low-cohesion` candidate is reported for it",
        "CASE-3 — — split state is reported Given  a class whose methods touch two disjoint groups of fields, scoring LCOM over the limit When   `design` runs Then   a `low-cohesion` candidate names the class, its LCOM, its method count and its field count",
        "CASE-4 — — the thresholds are per repo Given  a `requirements/_config.json` setting `DESIGN_WMC_MAX` to 1 When   `design` runs over a two-method class Then   that class is reported as a `god-class`",
        "CASE-5 — — a cohesive class is silent Given  a small class whose every method pair shares a field When   `design` runs Then   it reports no metrics candidate for that class"
      ],
      "accept": "CASE-1 — a wide class is named\n  Given  a Python class with more methods than `DESIGN_WMC_MAX`\n  When   `design` runs\n  Then   a `god-class` candidate is reported under Metrics, naming the class and its method count\n\nCASE-2 — a dict subclass is not accused of incohesion\n  Given  a class subclassing `dict` with several methods and no `self.<name>` assignment\n  When   `design` runs\n  Then   no `low-cohesion` candidate is reported for it\n\nCASE-3 — split state is reported\n  Given  a class whose methods touch two disjoint groups of fields, scoring LCOM over the limit\n  When   `design` runs\n  Then   a `low-cohesion` candidate names the class, its LCOM, its method count and its field count\n\nCASE-4 — the thresholds are per repo\n  Given  a `requirements/_config.json` setting `DESIGN_WMC_MAX` to 1\n  When   `design` runs over a two-method class\n  Then   that class is reported as a `god-class`\n\nCASE-5 — a cohesive class is silent\n  Given  a small class whose every method pair shares a field\n  When   `design` runs\n  Then   it reports no metrics candidate for that class",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-DESIGN-061"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8598"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8626"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8644"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8202"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5
    },
    {
      "id": "ARCH-DOCBUNDLE-026",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Untagged doc-bundle warning",
      "intent": "A whole-system doc — an architecture or explainer page generated from many requirements — can drift from those requirements for days with nothing linking the two, because a `generated-from:` tag was never added. Drift detection only fires through the tags it can see, so an untagged bundle is a silent blind spot. This surfaces the gap: the gate names a large `docs/` HTML doc that carries no lineage tag, so the author either links it (one tag, possibly a multi-id list) or marks it intentionally out of scope. Without it, \"the docs are in sync\" is an assumption the gate can neither confirm nor deny.",
      "contract": [
        "The gate warns, without affecting its exit code, for each `docs/*.html` file at least `DOC_BUNDLE_MIN_BYTES` in size carrying no `generated-from:` tag — skipping engine outputs, `.reqmapignore`-matched files, and anything outside `docs/`. [[REQ-DOCBUNDLE-840]] details the behaviour."
      ],
      "verify": [],
      "notes": [
        "A byte-size threshold is a deterministic proxy for \"generated bundle\", not a semantic one: a large hand-authored doc that legitimately has no requirement lineage is silenced via `.reqmapignore`, not by the check guessing intent.",
        "It asserts a lineage tag is *present*, not that the doc's content actually matches the requirements it names — the same lexical-trust limitation as `tested-by`."
      ],
      "current_impl": [
        "`untagged_doc_bundles`, `DOC_BUNDLE_MIN_BYTES` in `reqmap.py`, consumed by `cmd_check` — `untagged_doc_bundles` walks the code root (honoring `.reqmapignore`) for large `docs/*.html` files absent from the `generated-from:` member set and skipping engine outputs, and `cmd_check` emits one warn-only line per result.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a `docs/` HTML file at or above the size threshold with no `generated-from:` tag When   the scan runs Then   the file is reported as an untagged doc bundle",
        "CASE-2 — Given  a `docs/` HTML file below the size threshold When   the scan runs Then   it is not reported",
        "CASE-3 — Given  a large `docs/` HTML file that carries a `generated-from:` tag When   the scan runs Then   it is not reported",
        "CASE-4 — Given  a large engine output (`_`-prefixed or `map.html`) or a large `.html` outside `docs/` When   the scan runs Then   none of them are reported",
        "CASE-5 — Given  a large untagged `docs/` HTML file matched by a `.reqmapignore` pattern When   the scan runs Then   it is not reported",
        "CASE-6 — Given  a large untagged `docs/` HTML file When   the gate runs Then   its output names the file and the missing `generated-from:` tag"
      ],
      "accept": "CASE-1\n  Given  a `docs/` HTML file at or above the size threshold with no `generated-from:` tag\n  When   the scan runs\n  Then   the file is reported as an untagged doc bundle\nCASE-2\n  Given  a `docs/` HTML file below the size threshold\n  When   the scan runs\n  Then   it is not reported\nCASE-3\n  Given  a large `docs/` HTML file that carries a `generated-from:` tag\n  When   the scan runs\n  Then   it is not reported\nCASE-4\n  Given  a large engine output (`_`-prefixed or `map.html`) or a large `.html` outside `docs/`\n  When   the scan runs\n  Then   none of them are reported\nCASE-5\n  Given  a large untagged `docs/` HTML file matched by a `.reqmapignore` pattern\n  When   the scan runs\n  Then   it is not reported\nCASE-6\n  Given  a large untagged `docs/` HTML file\n  When   the gate runs\n  Then   its output names the file and the missing `generated-from:` tag",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-CHECK-006",
        "ARCH-SCAN-002"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-GATE-102"
      ],
      "satisfied_by": [
        "REQ-DOCBUNDLE-840"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1552"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2762"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:586"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.17",
      "priority": "should-have",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Avertisment pentru pachet de documentație neetichetat",
          "intent": "Un document la nivelul întregului sistem — o pagină de arhitectură sau explicativă generată din multe cerințe — poate suferi drift față de acele cerințe zile în șir fără nimic care să lege cele două, deoarece o etichetă `generated-from:` nu a fost adăugată niciodată. Detecția de drift se declanșează doar prin etichetele pe care le poate vedea, așa că un pachet neetichetat este un punct orb tăcut. Aceasta scoate la suprafață golul: gate-ul numește un document HTML mare din `docs/` care nu poartă nicio etichetă de descendență, astfel încât autorul fie îl leagă (o etichetă, posibil o listă multi-id), fie îl marchează ca fiind intenționat în afara domeniului. Fără aceasta, \"documentele sunt sincronizate\" este o presupunere pe care gate-ul nu o poate nici confirma, nici infirma.",
          "contract": "> Un document la nivelul întregului sistem — o pagină de arhitectură sau explicativă\n> generată din multe cerințe — poate suferi drift față de acele cerințe zile în șir fără\n> nimic care să lege cele două, deoarece o etichetă `generated-from:` nu a fost adăugată\n> niciodată. Detecția de drift se declanșează doar prin etichetele pe care le poate\n> vedea, așa că un pachet neetichetat este un punct orb tăcut. Aceasta scoate la\n> suprafață golul: gate-ul numește un document HTML mare din `docs/` care nu poartă\n> nicio etichetă de descendență, astfel încât autorul fie îl leagă (o etichetă, posibil\n> o listă multi-id), fie îl marchează ca fiind intenționat în afara domeniului. Fără\n> aceasta, \"documentele sunt sincronizate\" este o presupunere pe care gate-ul nu o poate\n> nici confirma, nici infirma.\n\nFiecare punct de mai jos este obligatoriu.\n- Gate-ul avertizează, fără a-i afecta codul de ieșire, pentru fiecare fișier `docs/*.html` de cel puțin `DOC_BUNDLE_MIN_BYTES` care nu poartă nicio etichetă `generated-from:` — sărind peste ieșirile motorului, fișierele potrivite de `.reqmapignore`, și orice se află în afara `docs/`. [[REQ-DOCBUNDLE-840]] detaliază comportamentul.",
          "acceptance": "CASE-1\n  Given  un fișier HTML din `docs/` la sau peste pragul de dimensiune, fără nicio etichetă `generated-from:`\n  When   scanarea rulează\n  Then   fișierul este raportat ca pachet de documentație neetichetat\nCASE-2\n  Given  un fișier HTML din `docs/` sub pragul de dimensiune\n  When   scanarea rulează\n  Then   nu este raportat\nCASE-3\n  Given  un fișier HTML mare din `docs/` care poartă o etichetă `generated-from:`\n  When   scanarea rulează\n  Then   nu este raportat\nCASE-4\n  Given  o ieșire mare a motorului (cu prefix `_` sau `map.html`) sau un `.html` mare din afara `docs/`\n  When   scanarea rulează\n  Then   niciunul dintre ele nu este raportat\nCASE-5\n  Given  un fișier HTML mare, neetichetat, din `docs/`, potrivit de un tipar `.reqmapignore`\n  When   scanarea rulează\n  Then   nu este raportat\nCASE-6\n  Given  un fișier HTML mare, neetichetat, din `docs/`\n  When   gate-ul rulează\n  Then   ieșirea sa numește fișierul și eticheta `generated-from:` lipsă"
        }
      }
    },
    {
      "id": "REQ-DOCBUNDLE-840",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Flagging a large, unlinked docs/ HTML bundle",
      "intent": "A generated architecture page can drift from the requirements it was built from for days with nobody noticing, because drift detection only fires through tags it can see. The gate warns on any large `docs/*.html` file with no `generated-from:` lineage tag — skipping the engine's own generated outputs, anything `.reqmapignore` excludes, and anything outside `docs/`, since a byte-size threshold is only a proxy for \"generated bundle\", not proof of one.",
      "contract": [
        "The gate warns for each file under `docs/` ending in `.html` that carries no `generated-from:` member tag, once that file is at least `DOC_BUNDLE_MIN_BYTES` in size.",
        "The gate considers only files under `docs/`.",
        "The check skips engine-generated outputs: a file whose basename starts with `_`, and the published `map.html` viewer.",
        "The engine owns those two and freshness-checks them separately.",
        "The check honors `.reqmapignore` and the standard scan walk, so a repo can mark a regenerable artifact out of scope rather than tag it.",
        "The walk itself — what it prunes, what it ignores, and how it treats a file it cannot read — is [[ARCH-SCAN-002]]'s contract, not restated here.",
        "The check is warn-only and never changes the gate's exit code."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a large untagged docs/ HTML file is flagged Given  `docs/arch.html` at or above `DOC_BUNDLE_MIN_BYTES` with no `generated-from:` tag When   `untagged_doc_bundles` scans the repo Then   it returns `[\"docs/arch.html\"]`, which `gate` folds into its warnings only — never an error, leaving the exit code unaffected",
        "CASE-2 — — a large untagged HTML file outside docs/ is never flagged Given  a large, untagged `top.html` at the repo root, outside `docs/` When   `untagged_doc_bundles` scans the repo Then   `top.html` is absent from the result",
        "CASE-3 — — engine outputs are excluded even when large and untagged Given  large, untagged `docs/map.html` and `docs/_x.html` When   `untagged_doc_bundles` scans the repo Then   neither file appears in the result, because the engine freshness-checks those two separately, on its own path",
        "CASE-4 — — a .reqmapignore pattern suppresses the finding Given  a large, untagged `docs/poster.html` matched by a `.reqmapignore` line `docs/poster.html` When   `untagged_doc_bundles` scans the repo Then   `docs/poster.html` is absent from the result"
      ],
      "accept": "CASE-1 — a large untagged docs/ HTML file is flagged\n  Given  `docs/arch.html` at or above `DOC_BUNDLE_MIN_BYTES` with no `generated-from:` tag\n  When   `untagged_doc_bundles` scans the repo\n  Then   it returns `[\"docs/arch.html\"]`, which `gate` folds into its warnings only —\n         never an error, leaving the exit code unaffected\n\nCASE-2 — a large untagged HTML file outside docs/ is never flagged\n  Given  a large, untagged `top.html` at the repo root, outside `docs/`\n  When   `untagged_doc_bundles` scans the repo\n  Then   `top.html` is absent from the result\n\nCASE-3 — engine outputs are excluded even when large and untagged\n  Given  large, untagged `docs/map.html` and `docs/_x.html`\n  When   `untagged_doc_bundles` scans the repo\n  Then   neither file appears in the result, because the engine freshness-checks those\n         two separately, on its own path\n\nCASE-4 — a .reqmapignore pattern suppresses the finding\n  Given  a large, untagged `docs/poster.html` matched by a `.reqmapignore` line `docs/poster.html`\n  When   `untagged_doc_bundles` scans the repo\n  Then   `docs/poster.html` is absent from the result",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-DOCBUNDLE-026"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1552"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:586"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Semnalarea unui pachet HTML mare și nelegat din docs/",
          "intent": "O pagină de arhitectură generată poate deriva prin drift față de cerințele din care a fost construită timp de zile fără ca nimeni să observe, pentru că detectarea de drift se declanșează doar prin tag-uri pe care le poate vedea. Gate-ul avertizează pentru orice fișier `docs/*.html` mare fără niciun tag de descendență `generated-from:` — excluzând ieșirile proprii generate ale motorului, orice este exclus de `.reqmapignore` și orice se află în afara `docs/`, deoarece un prag de dimensiune în octeți este doar un proxy pentru „pachet generat\", nu o dovadă a unuia.",
          "contract": "> O pagină de arhitectură generată poate deriva prin drift față de cerințele din care a fost construită\n> timp de zile fără ca nimeni să observe, pentru că detectarea de drift se declanșează doar prin tag-uri pe care le poate vedea.\n> Gate-ul avertizează pentru orice fișier `docs/*.html` mare fără niciun tag de descendență `generated-from:` —\n> excluzând ieșirile proprii generate ale motorului, orice este exclus de `.reqmapignore`, și\n> orice se află în afara `docs/`, deoarece un prag de dimensiune în octeți este doar un proxy pentru „generated\n> bundle\", nu o dovadă a unuia.\n\nFiecare punct de mai jos este obligatoriu.\n- Gate-ul avertizează pentru fiecare fișier din `docs/` care se termină în `.html` și care nu poartă\n  niciun tag de apartenență `generated-from:`, odată ce acel fișier are cel puțin `DOC_BUNDLE_MIN_BYTES`\n  în dimensiune.\n- Gate-ul ia în considerare doar fișierele din `docs/`.\n- Verificarea sare peste ieșirile generate de motor: un fișier al cărui nume de bază începe cu `_`,\n  și vizualizatorul publicat `map.html`.\n- Motorul deține aceste două și le verifică separat prospețimea.\n- Verificarea respectă `.reqmapignore` și parcurgerea standard a scanării, astfel încât un repo poate marca o\n  artefact regenerabil ca fiind în afara domeniului, în loc să îl taguiască.\n- Parcurgerea în sine — ce elimină, ce ignoră și cum tratează un fișier pe care nu îl poate citi — este\n  contractul lui [[ARCH-SCAN-002]], nu este repetat aici.\n- Verificarea este doar-avertizare și nu modifică niciodată codul de ieșire al gate-ului.",
          "acceptance": "CASE-1 — un fișier HTML mare și netaguit din docs/ este semnalat\n  Given  `docs/arch.html` la sau peste `DOC_BUNDLE_MIN_BYTES`, fără niciun tag `generated-from:`\n  When   `untagged_doc_bundles` scanează repo-ul\n  Then   returnează `[\"docs/arch.html\"]`, pe care `gate` îl încorporează doar în avertismentele sale —\n         niciodată o eroare, lăsând codul de ieșire neafectat\n\nCASE-2 — un fișier HTML mare și netaguit din afara docs/ nu este niciodată semnalat\n  Given  un `top.html` mare și netaguit la rădăcina repo-ului, în afara `docs/`\n  When   `untagged_doc_bundles` scanează repo-ul\n  Then   `top.html` lipsește din rezultat\n\nCASE-3 — ieșirile motorului sunt excluse chiar și atunci când sunt mari și netaguite\n  Given  `docs/map.html` și `docs/_x.html`, mari și netaguite\n  When   `untagged_doc_bundles` scanează repo-ul\n  Then   niciun fișier nu apare în rezultat, pentru că motorul le verifică prospețimea separat pentru\n         aceste două, pe propria cale\n\nCASE-4 — un tipar din .reqmapignore suprimă constatarea\n  Given  un `docs/poster.html` mare și netaguit, potrivit de o linie `.reqmapignore` `docs/poster.html`\n  When   `untagged_doc_bundles` scanează repo-ul\n  Then   `docs/poster.html` lipsește din rezultat"
        }
      }
    },
    {
      "id": "ARCH-DRIFT-003",
      "layer": "bus",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Contract hashing & lock",
      "intent": "A requirement file has a \"binding\" part — the precise promise of what the code must do. This takes a tiny fingerprint of just that part and remembers it. The next time anyone runs the tool, it can tell whether that promise changed since the code was last checked. Without it, someone could quietly rewrite what a capability is supposed to do and no one would be told the existing code may no longer match.",
      "contract": [
        "`binding_hash` computes a stable 12-character hex content hash over only the normative sections of a requirement body. [[REQ-DRIFT-841]]",
        "`load_lock`/`save_lock` read and write the per-id hash baseline at `requirements/_reqlock.json`, failing open to `{}` on a missing or corrupt file. [[REQ-DRIFT-842]]"
      ],
      "verify": [],
      "notes": [
        "A heading is matched by its label (anchored): only a `## …` heading whose label — optionally after a `WHAT`/`HOW` prefix — starts with \"Contract\", \"Acceptance\", \"Input\" or \"Output\" contributes to the hash; a commentary heading such as `## Notes — contract caveats` does not.",
        "The lock distinguishes \"absent\" from \"corrupt\" only at the gate (the gate warns on a present-but-unreadable lock); `load_lock` itself fails open to `{}`. (The `gate` command surfaces this warning; `check` is its deprecated alias.)"
      ],
      "current_impl": [
        "`binding_hash`, `lock_path`, `load_lock`, `save_lock` in `reqmap.py`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a requirement body When   only its Description/Notes section is edited Then   the binding hash does not change",
        "CASE-2 — Given  a requirement body When   its Output (or Contract) section is edited Then   the binding hash changes",
        "CASE-3 — Given  a missing or corrupt lock file When   `load_lock` runs Then   it returns an empty dict (no crash)",
        "CASE-4 — Given  a hash mapping When   `save_lock` then `load_lock` run Then   the same mapping round-trips"
      ],
      "accept": "CASE-1\n  Given  a requirement body\n  When   only its Description/Notes section is edited\n  Then   the binding hash does not change\n\nCASE-2\n  Given  a requirement body\n  When   its Output (or Contract) section is edited\n  Then   the binding hash changes\n\nCASE-3\n  Given  a missing or corrupt lock file\n  When   `load_lock` runs\n  Then   it returns an empty dict (no crash)\n\nCASE-4\n  Given  a hash mapping\n  When   `save_lock` then `load_lock` run\n  Then   the same mapping round-trips",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [
        "ARCH-ATOMICFORM-053",
        "ARCH-CHECK-006",
        "ARCH-DESCRIPTION-057",
        "ARCH-DRIFTIMPACT-035",
        "ARCH-HEALTH-017",
        "ARCH-MEMBERDRIFT-027",
        "ARCH-PROMOTE-011",
        "ARCH-RETIRE-064"
      ],
      "satisfies": [
        "SYS-GATE-102"
      ],
      "satisfied_by": [
        "REQ-DRIFT-841",
        "REQ-DRIFT-842"
      ],
      "members": [
        {
          "role": "generated-from",
          "loc": "docs/full_architecture.html:4"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1960"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2008"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2013"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2032"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2733"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:154"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:179"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:196"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6220"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8720"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10779"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.00",
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Hashing și lock pentru contract",
          "intent": "Un fișier de cerință are o parte \"obligatorie\" — promisiunea precisă a ceea ce trebuie să facă codul. Aceasta ia o amprentă minusculă doar din acea parte și o reține. Data viitoare când cineva rulează instrumentul, poate spune dacă acea promisiune s-a schimbat de la ultima verificare a codului. Fără aceasta, cineva ar putea rescrie în tăcere ce trebuie să facă o capabilitate, iar nimeni nu ar fi anunțat că este posibil ca codul existent să nu mai corespundă.",
          "contract": "> Un fișier de cerință are o parte \"obligatorie\" — promisiunea precisă a ceea ce trebuie\n> să facă codul. Aceasta ia o amprentă minusculă doar din acea parte și o reține. Data\n> viitoare când cineva rulează instrumentul, poate spune dacă acea promisiune s-a schimbat\n> de la ultima verificare a codului. Fără aceasta, cineva ar putea rescrie în tăcere ce\n> trebuie să facă o capabilitate, iar nimeni nu ar fi anunțat că este posibil ca codul\n> existent să nu mai corespundă.\n\nFiecare punct de mai jos este obligatoriu.\n- `binding_hash` calculează un hash de conținut hex stabil de 12 caractere, doar peste secțiunile normative ale corpului unei cerințe. [[REQ-DRIFT-841]]\n- `load_lock`/`save_lock` citesc și scriu baseline-ul de hash per-id la `requirements/_reqlock.json`, eșuând deschis către `{}` la un fișier lipsă sau corupt. [[REQ-DRIFT-842]]",
          "acceptance": "CASE-1\n  Given  un corp de cerință\n  When   este editată doar secțiunea sa Description/Notes\n  Then   hash-ul obligatoriu nu se schimbă\n\nCASE-2\n  Given  un corp de cerință\n  When   este editată secțiunea sa Output (sau Contract)\n  Then   hash-ul obligatoriu se schimbă\n\nCASE-3\n  Given  un fișier lock lipsă sau corupt\n  When   `load_lock` rulează\n  Then   returnează un dict gol (fără crash)\n\nCASE-4\n  Given  o mapare de hash-uri\n  When   `save_lock` apoi `load_lock` rulează\n  Then   aceeași mapare face round-trip identic"
        }
      }
    },
    {
      "id": "REQ-DRIFT-841",
      "layer": "bus",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Hashing only the normative sections",
      "intent": "A requirement file mixes a binding promise with prose that explains it — rationale, open questions, the member list. Hashing the whole file would report drift every time someone improved a sentence of explanation, training everyone to ignore the warning. Hashing only the Contract and Acceptance means the fingerprint changes exactly when the promise does.",
      "contract": [
        "`binding_hash` computes a stable 12-character hex content hash over only the normative sections of a requirement body.",
        "The normative sections are the `Contract` and `Acceptance` headings, plus the legacy `Input`/`Output`/`Acceptance` headings kept for back-compat.",
        "Rationale, notes, verify-intent, links and the member list stay outside the hash, so they may change without tripping drift.",
        "The hash is deterministic for identical normative content."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — binding_hash returns a fixed-length hex digest Given  a requirement body with a `## Description` and `## Cases` section When   `binding_hash` runs on that body Then   it returns a 12-character lowercase hex string",
        "CASE-2 — — the legacy Output heading is still treated as normative Given  a requirement body using the legacy `## Output` heading instead of `## Contract` When   the clause under `## Output` is edited Then   `binding_hash` changes, proving the legacy heading feeds the hash",
        "CASE-3 — — editing Notes and the member list leaves the hash unchanged Given  a requirement body with `## Notes & known limitations` and `## Members in code` sections When   only the text under those sections is edited Then   `binding_hash` returns the same value before and after the edit",
        "CASE-4 — — binding_hash is deterministic across repeated calls Given  the same requirement body When   `binding_hash` runs on it twice, independently Then   both calls return the identical 12-character hash --------------------"
      ],
      "accept": "CASE-1 — binding_hash returns a fixed-length hex digest\n  Given  a requirement body with a `## Description` and `## Cases` section\n  When   `binding_hash` runs on that body\n  Then   it returns a 12-character lowercase hex string\n\nCASE-2 — the legacy Output heading is still treated as normative\n  Given  a requirement body using the legacy `## Output` heading instead of `## Contract`\n  When   the clause under `## Output` is edited\n  Then   `binding_hash` changes, proving the legacy heading feeds the hash\n\nCASE-3 — editing Notes and the member list leaves the hash unchanged\n  Given  a requirement body with `## Notes & known limitations` and `## Members in code` sections\n  When   only the text under those sections is edited\n  Then   `binding_hash` returns the same value before and after the edit\n\nCASE-4 — binding_hash is deterministic across repeated calls\n  Given  the same requirement body\n  When   `binding_hash` runs on it twice, independently\n  Then   both calls return the identical 12-character hash\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-DRIFT-003"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1960"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:132"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6220"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8720"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10779"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Hashing doar al secțiunilor normative",
          "intent": "Un fișier de cerință amestecă o promisiune obligatorie cu text care o explică — motivația, întrebări deschise, lista de membri. Hashing-ul întregului fișier ar raporta drift de fiecare dată când cineva îmbunătățește o propoziție explicativă, obișnuind pe toată lumea să ignore avertismentul. Hashing doar al Contract și Acceptance înseamnă că amprenta se schimbă exact atunci când se schimbă promisiunea.",
          "contract": "> Un fișier de cerință amestecă o promisiune obligatorie cu text care o explică — motivația, întrebări\n> deschise, lista de membri. Hashing-ul întregului fișier ar raporta drift de fiecare dată când cineva\n> îmbunătățește o propoziție explicativă, obișnuind pe toată lumea să ignore avertismentul. Hashing doar\n> al Contract și Acceptance înseamnă că amprenta se schimbă exact atunci când se schimbă promisiunea.\n\nFiecare bullet de mai jos este obligatoriu.\n- `binding_hash` calculează un hash de conținut hex stabil de 12 caractere doar pe\n  secțiunile normative ale corpului unei cerințe.\n- Secțiunile normative sunt titlurile `Contract` și `Acceptance`, plus titlurile legacy\n  `Input`/`Output`/`Acceptance` păstrate pentru compatibilitate retroactivă.\n- Motivația, notele, verify-intent, link-urile și lista de membri rămân în afara hash-ului, astfel\n  încât se pot schimba fără a declanșa drift.\n- Hash-ul este determinist pentru conținut normativ identic.",
          "acceptance": "CASE-1 — binding_hash returnează un digest hex de lungime fixă\n  Given  un corp de cerință cu o secțiune `## Description` și `## Cases`\n  When   `binding_hash` rulează pe acel corp\n  Then   returnează un șir hex de 12 caractere, litere mici\n\nCASE-2 — titlul legacy Output este încă tratat ca normativ\n  Given  un corp de cerință care folosește titlul legacy `## Output` în loc de `## Contract`\n  When   clauza de sub `## Output` este editată\n  Then   `binding_hash` se schimbă, dovedind că titlul legacy alimentează hash-ul\n\nCASE-3 — editarea Notes și a listei de membri lasă hash-ul neschimbat\n  Given  un corp de cerință cu secțiunile `## Notes & known limitations` și `## Members in code`\n  When   doar textul de sub acele secțiuni este editat\n  Then   `binding_hash` returnează aceeași valoare înainte și după editare\n\nCASE-4 — binding_hash este determinist la apeluri repetate\n  Given  același corp de cerință\n  When   `binding_hash` rulează pe el de două ori, independent\n  Then   ambele apeluri returnează hash-ul identic de 12 caractere\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-DRIFT-842",
      "layer": "bus",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Reading and writing the drift baseline",
      "intent": "A fingerprint is only useful stored next to the last one seen. `_reqlock.json` is that memory: one hash per requirement id, so the next run can tell which contracts changed since the code was last checked. It fails open rather than crashing, because a corrupt or missing lock should degrade to \"nothing recorded yet,\" not stop the tool from running at all.",
      "contract": [
        "`load_lock` and `save_lock` read and write the per-id hash baseline at `requirements/_reqlock.json`.",
        "A missing, empty or unparseable lock loads as an empty mapping, never a crash.",
        "`save_lock` creates the requirements directory if it is absent.",
        "`save_lock` writes sorted, indented JSON, so the lock file is diff-stable."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — save_lock then load_lock round-trips through _reqlock.json Given  the hash mapping `{\"A-B-001\": \"abc123def456\", \"C-D-002\": \"0123456789ab\"}` When   `save_lock` writes it to a directory and `load_lock` reads that same directory Then   `load_lock` returns the identical mapping, read from `_reqlock.json`",
        "CASE-2 — — a missing or corrupt lock file loads as an empty dict Given  a directory with no `_reqlock.json`, and separately one holding non-UTF-8 bytes as `_reqlock.json` When   `load_lock` runs on each directory Then   both calls return `{}` and neither raises an exception",
        "CASE-3 — — save_lock creates a missing requirements directory Given  a target path whose parent directories (`does/not/exist`) do not exist yet When   `save_lock` writes a mapping to that path Then   the directory tree is created and `_reqlock.json` exists inside it",
        "CASE-4 — — save_lock writes sorted, indented JSON Given  a hash mapping with out-of-order keys (e.g. `{\"Z-9\": \"..\", \"A-1\": \"..\"}`) When   `save_lock` writes it to disk Then   the written `_reqlock.json` lists keys alphabetically and spans multiple indented lines"
      ],
      "accept": "CASE-1 — save_lock then load_lock round-trips through _reqlock.json\n  Given  the hash mapping `{\"A-B-001\": \"abc123def456\", \"C-D-002\": \"0123456789ab\"}`\n  When   `save_lock` writes it to a directory and `load_lock` reads that same directory\n  Then   `load_lock` returns the identical mapping, read from `_reqlock.json`\n\nCASE-2 — a missing or corrupt lock file loads as an empty dict\n  Given  a directory with no `_reqlock.json`, and separately one holding non-UTF-8 bytes as `_reqlock.json`\n  When   `load_lock` runs on each directory\n  Then   both calls return `{}` and neither raises an exception\n\nCASE-3 — save_lock creates a missing requirements directory\n  Given  a target path whose parent directories (`does/not/exist`) do not exist yet\n  When   `save_lock` writes a mapping to that path\n  Then   the directory tree is created and `_reqlock.json` exists inside it\n\nCASE-4 — save_lock writes sorted, indented JSON\n  Given  a hash mapping with out-of-order keys (e.g. `{\"Z-9\": \"..\", \"A-1\": \"..\"}`)\n  When   `save_lock` writes it to disk\n  Then   the written `_reqlock.json` lists keys alphabetically and spans multiple indented lines",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-DRIFT-003"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2008"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2013"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2032"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:132"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8720"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Citirea și scrierea liniei de bază pentru drift",
          "intent": "O amprentă este utilă doar dacă este stocată lângă ultima văzută. `_reqlock.json` este acea memorie: câte un hash per id de cerință, astfel încât următoarea rulare poate spune care contracte s-au schimbat de la ultima verificare a codului. Eșuează deschis (fail open) în loc să se blocheze, pentru că un lock corupt sau lipsă ar trebui să se degradeze la \"nimic înregistrat încă\", nu să oprească complet rularea instrumentului.",
          "contract": "> O amprentă este utilă doar dacă este stocată lângă ultima văzută. `_reqlock.json` este\n> acea memorie: câte un hash per id de cerință, astfel încât următoarea rulare poate spune\n> care contracte s-au schimbat de la ultima verificare a codului. Eșuează deschis (fail open)\n> în loc să se blocheze, pentru că un lock corupt sau lipsă ar trebui să se degradeze la\n> \"nimic înregistrat încă\", nu să oprească complet rularea instrumentului.\n\nFiecare bullet de mai jos este obligatoriu.\n- `load_lock` și `save_lock` citesc și scriu linia de bază a hash-urilor per id la\n  `requirements/_reqlock.json`.\n- Un lock lipsă, gol sau neparsabil se încarcă ca o mapare goală, niciodată o eroare.\n- `save_lock` creează directorul requirements dacă acesta lipsește.\n- `save_lock` scrie JSON sortat și indentat, astfel încât fișierul lock este diff-stabil.",
          "acceptance": "CASE-1 — save_lock urmat de load_lock face un round-trip prin _reqlock.json\n  Given  maparea de hash-uri `{\"A-B-001\": \"abc123def456\", \"C-D-002\": \"0123456789ab\"}`\n  When   `save_lock` o scrie într-un director și `load_lock` citește acel același director\n  Then   `load_lock` returnează maparea identică, citită din `_reqlock.json`\n\nCASE-2 — un fișier lock lipsă sau corupt se încarcă ca un dict gol\n  Given  un director fără `_reqlock.json`, și separat unul care conține octeți non-UTF-8 ca `_reqlock.json`\n  When   `load_lock` rulează pe fiecare director\n  Then   ambele apeluri returnează `{}` și niciunul nu ridică o excepție\n\nCASE-3 — save_lock creează un director requirements lipsă\n  Given  o cale țintă ale cărei directoare părinte (`does/not/exist`) nu există încă\n  When   `save_lock` scrie o mapare la acea cale\n  Then   arborele de directoare este creat și `_reqlock.json` există în interiorul lui\n\nCASE-4 — save_lock scrie JSON sortat și indentat\n  Given  o mapare de hash-uri cu chei nesortate (de ex. `{\"Z-9\": \"..\", \"A-1\": \"..\"}`)\n  When   `save_lock` o scrie pe disc\n  Then   `_reqlock.json` scris listează cheile alfabetic și se întinde pe mai multe linii indentate"
        }
      }
    },
    {
      "id": "ARCH-DRIFTIMPACT-035",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Drift blast-radius: name dependents",
      "intent": "When a confirmed requirement's contract drifts, the gate names the requirement and its code members — but not the requirements built on top of it via `depends_on`. An upstream contract change can invalidate a downstream requirement's assumptions with no signal at all, and the reviewer has to reconstruct the blast radius by hand from the map. The dependency graph is already in memory at gate time; naming the dependents turns one drift warning into an actionable review list.",
      "contract": [
        "The gate's drift warning names the requirements that depend on the drifted one via `depends_on`, so a reviewer sees the blast radius without reconstructing it from the map. [[REQ-DRIFTIMPACT-843]]"
      ],
      "verify": [],
      "notes": [
        "Naming a dependent asserts topology, not impact: the dependent may be unaffected by the specific clause that changed. The reviewer decides; the gate only stops the radius from being invisible.",
        "Member drift (ARCH-MEMBERDRIFT-027) is not fanned out: a code-ahead-of-spec change has no changed contract for dependents to have relied on."
      ],
      "current_impl": [
        "The drift loop in `cmd_check` (`reqmap.py`): a reverse-`depends_on` index is built once from the loaded requirements, and the drift warning appends \"review dependent(s): …\" when the index has entries for the drifted id.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a confirmed requirement whose contract hash differs from the lock and a second requirement that lists it in `depends_on` When   the gate runs Then   the drift warning names the dependent requirement's id",
        "CASE-2 — Given  a confirmed drifted requirement that no other requirement depends on When   the gate runs Then   the drift warning appears without any dependent clause",
        "CASE-3 — Given  a drifted requirement with two dependents When   the gate runs Then   both ids appear in sorted order on the drift line"
      ],
      "accept": "CASE-1\n  Given  a confirmed requirement whose contract hash differs from the lock and\n         a second requirement that lists it in `depends_on`\n  When   the gate runs\n  Then   the drift warning names the dependent requirement's id\nCASE-2\n  Given  a confirmed drifted requirement that no other requirement depends on\n  When   the gate runs\n  Then   the drift warning appears without any dependent clause\nCASE-3\n  Given  a drifted requirement with two dependents\n  When   the gate runs\n  Then   both ids appear in sorted order on the drift line",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-CHECK-006",
        "ARCH-DRIFT-003"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-GATE-102"
      ],
      "satisfied_by": [
        "REQ-DRIFTIMPACT-843"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2733"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:801"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9628"
        }
      ],
      "test_exempt": null,
      "milestone": "v2.10",
      "priority": "should-have",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Raza de impact a drift-ului: numește dependenții",
          "intent": "Când contractul unei cerințe confirmate suferă drift, gate-ul numește cerința și membrii ei de cod — dar nu și cerințele construite deasupra ei prin `depends_on`. O schimbare de contract în amonte poate invalida presupunerile unei cerințe din aval fără niciun semnal, iar revizorul trebuie să reconstruiască raza de impact manual din hartă. Graful de dependențe este deja în memorie la momentul gate-ului; numirea dependenților transformă un singur avertisment de drift într-o listă de revizuire acționabilă.",
          "contract": "> Când contractul unei cerințe confirmate suferă drift, gate-ul numește cerința și\n> membrii ei de cod — dar nu și cerințele construite deasupra ei prin `depends_on`. O\n> schimbare de contract în amonte poate invalida presupunerile unei cerințe din aval\n> fără niciun semnal, iar revizorul trebuie să reconstruiască raza de impact manual din\n> hartă. Graful de dependențe este deja în memorie la momentul gate-ului; numirea\n> dependenților transformă un singur avertisment de drift într-o listă de revizuire\n> acționabilă.\n\nFiecare punct de mai jos este obligatoriu.\n- Avertismentul de drift al gate-ului numește cerințele care depind de cea aflată în drift prin `depends_on`, astfel încât un revizor vede raza de impact fără a o reconstrui din hartă. [[REQ-DRIFTIMPACT-843]]",
          "acceptance": "CASE-1\n  Given  o cerință confirmată al cărei hash de contract diferă de cel din lock și\n         o a doua cerință care o listează în `depends_on`\n  When   gate-ul rulează\n  Then   avertismentul de drift numește id-ul cerinței dependente\nCASE-2\n  Given  o cerință confirmată aflată în drift de care nicio altă cerință nu depinde\n  When   gate-ul rulează\n  Then   avertismentul de drift apare fără nicio clauză de dependent\nCASE-3\n  Given  o cerință aflată în drift cu doi dependenți\n  When   gate-ul rulează\n  Then   ambele id-uri apar în ordine sortată pe linia de drift"
        }
      }
    },
    {
      "id": "REQ-DRIFTIMPACT-843",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Name a drifted requirement's direct dependents",
      "intent": "`cmd_check` builds a reverse-`depends_on` index once from the loaded requirements. When a confirmed requirement's contract hash no longer matches the lock, the same warning line that reports the drift also lists the requirement ids that depend on it — so a reviewer gets the blast radius for free instead of reconstructing it by hand from the map.",
      "contract": [
        "When the gate reports a contract drift for a requirement, and at least one other requirement lists the drifted one in `depends_on`, the same warning line also names those dependent requirement ids.",
        "The dependent list is sorted and deduplicated, so the output is deterministic across platforms and walk orders.",
        "Only direct dependents are named — one edge, not the transitive closure. A transitive closure explodes on bus-layer nodes and buries the signal; a reviewer follows the chain one hop at a time.",
        "A drifted requirement with no dependents produces the existing warning unchanged — no empty \"dependents\" clause.",
        "The addition does not change the drift warning's severity: it stays warn-only by default and promotable by `--strict`, exactly as before."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — drift warning names a requirement that depends on the drifted one Given  a confirmed drifted requirement and a second requirement with it in `depends_on` When   `gate` runs Then   the drift warning line includes \"review dependent(s):\" followed by the second id, with severity unchanged — still warn-only, promotable only by `--strict`",
        "CASE-2 — — two dependents are named in sorted order on the drift line Given  a drifted requirement with two dependents whose ids sort after each other When   `gate` runs Then   the \"review dependent(s):\" list shows both ids in ascending id order",
        "CASE-3 — — a dependent-of-a-dependent is not named on the drift line Given  A depends on drifted B, and C depends on A (but not on B) When   `gate` runs Then   the drift warning for B names A but does not name C",
        "CASE-4 — — an isolated drifted requirement's warning carries no dependents clause Given  a confirmed drifted requirement that no other requirement lists in `depends_on` When   `gate` runs Then   the drift warning line contains no \"review dependent(s):\" text at all"
      ],
      "accept": "CASE-1 — drift warning names a requirement that depends on the drifted one\n  Given  a confirmed drifted requirement and a second requirement with it in `depends_on`\n  When   `gate` runs\n  Then   the drift warning line includes \"review dependent(s):\" followed by the second id,\n         with severity unchanged — still warn-only, promotable only by `--strict`\n\nCASE-2 — two dependents are named in sorted order on the drift line\n  Given  a drifted requirement with two dependents whose ids sort after each other\n  When   `gate` runs\n  Then   the \"review dependent(s):\" list shows both ids in ascending id order\n\nCASE-3 — a dependent-of-a-dependent is not named on the drift line\n  Given  A depends on drifted B, and C depends on A (but not on B)\n  When   `gate` runs\n  Then   the drift warning for B names A but does not name C\n\nCASE-4 — an isolated drifted requirement's warning carries no dependents clause\n  Given  a confirmed drifted requirement that no other requirement lists in `depends_on`\n  When   `gate` runs\n  Then   the drift warning line contains no \"review dependent(s):\" text at all",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-DRIFTIMPACT-035"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2733"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:801"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9628"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Numirea dependenților direcți ai unei cerințe cu drift",
          "intent": "`cmd_check` construiește o dată un index invers `depends_on` din cerințele încărcate. Când hash-ul de contract al unei cerințe confirmate nu se mai potrivește cu lock-ul, aceeași linie de avertisment care raportează drift-ul listează și id-urile cerințelor care depind de ea — astfel un reviewer obține raza de impact gratuit, în loc să o reconstruiască manual din map.",
          "contract": "> `cmd_check` construiește o dată un index invers `depends_on` din cerințele încărcate. Când\n> hash-ul de contract al unei cerințe confirmate nu se mai potrivește cu lock-ul, aceeași linie\n> de avertisment care raportează drift-ul listează și id-urile cerințelor care depind de ea —\n> astfel un reviewer obține raza de impact gratuit, în loc să o reconstruiască manual din map.\n\nFiecare bullet de mai jos este obligatoriu.\n- Când gate raportează un drift de contract pentru o cerință, și cel puțin o\n  altă cerință listează cerința cu drift în `depends_on`, aceeași linie de\n  avertisment numește și acele id-uri de cerințe dependente.\n- Lista de dependenți este sortată și deduplicată, astfel încât rezultatul este\n  determinist între platforme și ordini de parcurgere.\n- Sunt numiți doar dependenții direcți — o muchie, nu închiderea tranzitivă. O\n  închidere tranzitivă explodează pe nodurile de layer bus și îngroapă semnalul; un\n  reviewer urmărește lanțul un pas o dată.\n- O cerință cu drift fără dependenți produce avertismentul existent\n  neschimbat — fără clauză \"dependents\" goală.\n- Adăugarea nu schimbă severitatea avertismentului de drift: rămâne\n  warn-only implicit și promovabil doar prin `--strict`, exact ca înainte.",
          "acceptance": "CASE-1 — avertismentul de drift numește o cerință care depinde de cea cu drift\n  Given  o cerință confirmată cu drift și o a doua cerință care o are în `depends_on`\n  When   `gate` rulează\n  Then   linia de avertisment de drift include \"review dependent(s):\" urmat de al doilea id,\n         cu severitatea neschimbată — tot warn-only, promovabilă doar prin `--strict`\n\nCASE-2 — doi dependenți sunt numiți în ordine sortată pe linia de drift\n  Given  o cerință cu drift cu doi dependenți ale căror id-uri se sortează unul după altul\n  When   `gate` rulează\n  Then   lista \"review dependent(s):\" arată ambele id-uri în ordine crescătoare\n\nCASE-3 — un dependent-al-unui-dependent nu este numit pe linia de drift\n  Given  A depinde de B (cu drift), și C depinde de A (dar nu de B)\n  When   `gate` rulează\n  Then   avertismentul de drift pentru B îl numește pe A dar nu îl numește pe C\n\nCASE-4 — avertismentul unei cerințe izolate cu drift nu poartă nicio clauză de dependenți\n  Given  o cerință confirmată cu drift pe care nicio altă cerință nu o listează în `depends_on`\n  When   `gate` rulează\n  Then   linia de avertisment de drift nu conține deloc text \"review dependent(s):\""
        }
      }
    },
    {
      "id": "ARCH-EXCALIDRAW-030",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Excalidraw scene builder — core API",
      "intent": "The excalidraw-diagram skill needs a stdlib-only Python library that turns declarative shape and arrow declarations into a valid Excalidraw scene file and a self-contained HTML viewer, with no external dependencies, so the skill can be vendored into any environment without install friction.",
      "contract": [
        "`Scene` exposes shape primitives, ISO 5807 flowchart aliases, layout helpers, and annotation helpers for building a diagram declaratively. [[REQ-EXCALIDRAW-844]] details the behaviour.",
        "`Scene` exposes connector helpers and a `.save(basename, out_dir)` that writes both a `.excalidraw` scene and a self-contained `.html` viewer once, deterministically when seeded. [[REQ-EXCALIDRAW-845]] details the behaviour."
      ],
      "verify": [],
      "notes": [
        "The authoritative usage reference is `skills/excalidraw-diagram/SKILL.md`.",
        "`save()` overlap detection raises on any two non-container shapes that overlap (co-ordinates checked after all shapes are added)."
      ],
      "current_impl": [
        "`class Scene` and all shape/layout/arrow methods in `skills/excalidraw-diagram/scripts/excalidraw_builder.py`."
      ],
      "acc": [
        "CASE-1 — Given  a `Scene` with at least one `box` and one `arrow` When   `.save()` is called Then   both `<basename>.excalidraw` and `<basename>.html` are written and the `.excalidraw` file parses as valid JSON with `type: \"excalidraw\"`",
        "CASE-2 — Given  two `box` shapes placed at overlapping coordinates When   `.save()` is called without `allow_overlap=True` Then   a `ValueError` is raised naming the overlapping shapes",
        "CASE-3 — Given  `Scene(seed=42)` with identical shape declarations run twice When   both outputs are compared byte-for-byte Then   they are identical",
        "CASE-4 — Given  `.save()` has already been called once on a `Scene` When   `.save()` is called again Then   a `RuntimeError` is raised",
        "CASE-5 — Given  a `pipeline([...])` call with step dicts When   `.save()` is called Then   the resulting scene contains connected flowchart nodes with no overlapping shapes"
      ],
      "accept": "CASE-1\n  Given  a `Scene` with at least one `box` and one `arrow`\n  When   `.save()` is called\n  Then   both `<basename>.excalidraw` and `<basename>.html` are written and the\n         `.excalidraw` file parses as valid JSON with `type: \"excalidraw\"`\n\nCASE-2\n  Given  two `box` shapes placed at overlapping coordinates\n  When   `.save()` is called without `allow_overlap=True`\n  Then   a `ValueError` is raised naming the overlapping shapes\n\nCASE-3\n  Given  `Scene(seed=42)` with identical shape declarations run twice\n  When   both outputs are compared byte-for-byte\n  Then   they are identical\n\nCASE-4\n  Given  `.save()` has already been called once on a `Scene`\n  When   `.save()` is called again\n  Then   a `RuntimeError` is raised\n\nCASE-5\n  Given  a `pipeline([...])` call with step dicts\n  When   `.save()` is called\n  Then   the resulting scene contains connected flowchart nodes with no\n         overlapping shapes",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [
        "ARCH-EXCALIDRAW-031",
        "ARCH-EXCALIDRAW-032"
      ],
      "satisfies": [
        "SYS-VISUAL-106"
      ],
      "satisfied_by": [
        "REQ-EXCALIDRAW-844",
        "REQ-EXCALIDRAW-845"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:2"
        },
        {
          "role": "tested-by",
          "loc": "plugin/skills/excalidraw-diagram/scripts/test_excalidraw.py:2"
        }
      ],
      "test_exempt": null,
      "milestone": "v2.4",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Constructorul de scene Excalidraw — API de bază",
          "intent": "Skill-ul excalidraw-diagram are nevoie de o bibliotecă Python doar-stdlib care transformă declarații declarative de forme și săgeți într-un fișier de scenă Excalidraw valid și un viewer HTML de sine stătător, fără dependențe externe, astfel încât skill-ul să poată fi vendorizat în orice mediu fără fricțiune de instalare.",
          "contract": "> Skill-ul excalidraw-diagram are nevoie de o bibliotecă Python doar-stdlib care\n> transformă declarații declarative de forme și săgeți într-un fișier de scenă\n> Excalidraw valid și un viewer HTML de sine stătător, fără dependențe externe, astfel\n> încât skill-ul să poată fi vendorizat în orice mediu fără fricțiune de instalare.\n\nFiecare punct de mai jos este obligatoriu.\n- `Scene` expune primitive de formă, aliasuri de flowchart ISO 5807, helpere de layout, și helpere de adnotare pentru construirea unei diagrame declarativ. [[REQ-EXCALIDRAW-844]] detaliază comportamentul.\n- `Scene` expune helpere de conector și un `.save(basename, out_dir)` care scrie atât o scenă `.excalidraw` cât și un viewer `.html` de sine stătător o singură dată, determinist când este seed-uit. [[REQ-EXCALIDRAW-845]] detaliază comportamentul.",
          "acceptance": "CASE-1\n  Given  o `Scene` cu cel puțin un `box` și o `arrow`\n  When   `.save()` este apelată\n  Then   sunt scrise atât `<basename>.excalidraw` cât și `<basename>.html`, iar fișierul\n         `.excalidraw` se analizează ca JSON valid cu `type: \"excalidraw\"`\n\nCASE-2\n  Given  două forme `box` plasate la coordonate suprapuse\n  When   `.save()` este apelată fără `allow_overlap=True`\n  Then   se ridică un `ValueError` care numește formele suprapuse\n\nCASE-3\n  Given  `Scene(seed=42)` cu declarații de forme identice rulate de două ori\n  When   ambele ieșiri sunt comparate octet-cu-octet\n  Then   sunt identice\n\nCASE-4\n  Given  `.save()` a fost deja apelată o dată pe o `Scene`\n  When   `.save()` este apelată din nou\n  Then   se ridică un `RuntimeError`\n\nCASE-5\n  Given  un apel `pipeline([...])` cu dict-uri de pași\n  When   `.save()` este apelată\n  Then   scena rezultată conține noduri de flowchart conectate, fără\n         forme suprapuse"
        }
      }
    },
    {
      "id": "REQ-EXCALIDRAW-844",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Shape, layout, and annotation vocabulary",
      "intent": "A diagram is built by declaring shapes and arranging them, not by computing pixel coordinates by hand. `Scene` gives a caller shape primitives, ISO 5807 flowchart aliases (so a flowchart reads like a flowchart, not a pile of boxes), layout helpers that place shapes without overlap, and annotation helpers for titles and labels.",
      "contract": [
        "`Scene()` produces a valid Excalidraw JSON scene (schema version 2) with a `type: \"excalidraw\"` root and a `elements` list compatible with excalidraw.com import.",
        "`Scene` exposes shape primitives: `box`, `ellipse`, `diamond`, `frame`.",
        "`Scene` exposes ISO 5807 flowchart aliases: `process`, `terminator`, `decision`, `data`, `predefined_process`, `preparation`, `connector`.",
        "`Scene` exposes layout helpers: `row`, `column`, `grid`, `enclose`, `lane`, `pipeline`, `section`, `align`, `distribute`.",
        "`Scene` exposes annotation helpers: `title`, `label`, `legend`, `glossary`, `role`."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — Scene() produces an excalidraw.com-importable JSON scene Given  a `Scene` with at least one shape added When   `.save()` writes the `.excalidraw` file Then   it parses as JSON with `type: \"excalidraw\"` and an `elements` list",
        "CASE-2 — — each shape primitive adds one element of its kind Given  a `Scene` When   `box`, `ellipse`, `diamond` and `frame` are each called once Then   the scene gains one element of each corresponding Excalidraw type",
        "CASE-3 — — ISO 5807 aliases produce their underlying shapes Given  a `Scene` When   `process`, `terminator`, `decision`, `data`, `predefined_process`, `preparation` and `connector` are each called Then   each adds an element without raising, using its mapped primitive shape",
        "CASE-4 — — layout helpers place shapes without overlap Given  three boxes When   `row([...])` arranges them Then   the resulting coordinates place them side by side with no overlapping pair",
        "CASE-5 — — annotation helpers attach text without raising Given  a `Scene` with one shape When   `title`, `label`, `legend`, `glossary` and `role` are each called Then   each adds its text element and `.save()` still succeeds --------------------"
      ],
      "accept": "CASE-1 — Scene() produces an excalidraw.com-importable JSON scene\n  Given  a `Scene` with at least one shape added\n  When   `.save()` writes the `.excalidraw` file\n  Then   it parses as JSON with `type: \"excalidraw\"` and an `elements` list\n\nCASE-2 — each shape primitive adds one element of its kind\n  Given  a `Scene`\n  When   `box`, `ellipse`, `diamond` and `frame` are each called once\n  Then   the scene gains one element of each corresponding Excalidraw type\n\nCASE-3 — ISO 5807 aliases produce their underlying shapes\n  Given  a `Scene`\n  When   `process`, `terminator`, `decision`, `data`, `predefined_process`, `preparation` and `connector` are each called\n  Then   each adds an element without raising, using its mapped primitive shape\n\nCASE-4 — layout helpers place shapes without overlap\n  Given  three boxes\n  When   `row([...])` arranges them\n  Then   the resulting coordinates place them side by side with no overlapping pair\n\nCASE-5 — annotation helpers attach text without raising\n  Given  a `Scene` with one shape\n  When   `title`, `label`, `legend`, `glossary` and `role` are each called\n  Then   each adds its text element and `.save()` still succeeds\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-EXCALIDRAW-030"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:109"
        },
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:150"
        },
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:172"
        },
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:183"
        },
        {
          "role": "tested-by",
          "loc": "plugin/skills/excalidraw-diagram/scripts/test_excalidraw.py:2"
        },
        {
          "role": "tested-by",
          "loc": "plugin/skills/excalidraw-diagram/scripts/test_excalidraw.py:551"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Vocabular de forme, layout și adnotare",
          "intent": "O diagramă este construită prin declararea formelor și aranjarea lor, nu prin calcularea manuală a coordonatelor în pixeli. `Scene` oferă apelantului primitive de formă, alias-uri de flowchart ISO 5807 (astfel încât un flowchart să se citească precum un flowchart, nu o grămadă de casete), helper-e de layout care plasează formele fără suprapunere, și helper-e de adnotare pentru titluri și etichete.",
          "contract": "> O diagramă este construită prin declararea formelor și aranjarea lor, nu prin calcularea\n> manuală a coordonatelor în pixeli. `Scene` oferă apelantului primitive de formă, alias-uri\n> de flowchart ISO 5807 (astfel încât un flowchart să se citească precum un flowchart, nu o\n> grămadă de casete), helper-e de layout care plasează formele fără suprapunere, și\n> helper-e de adnotare pentru titluri și etichete.\n\nFiecare bullet de mai jos este obligatoriu.\n- `Scene()` produce o scenă JSON Excalidraw validă (versiune schemă 2) cu\n  o rădăcină `type: \"excalidraw\"` și o listă `elements` compatibilă cu\n  importul excalidraw.com.\n- `Scene` expune primitive de formă: `box`, `ellipse`, `diamond`, `frame`.\n- `Scene` expune alias-uri de flowchart ISO 5807: `process`, `terminator`,\n  `decision`, `data`, `predefined_process`, `preparation`, `connector`.\n- `Scene` expune helper-e de layout: `row`, `column`, `grid`, `enclose`,\n  `lane`, `pipeline`, `section`, `align`, `distribute`.\n- `Scene` expune helper-e de adnotare: `title`, `label`, `legend`,\n  `glossary`, `role`.",
          "acceptance": "CASE-1 — Scene() produce o scenă JSON importabilă pe excalidraw.com\n  Given  un `Scene` cu cel puțin o formă adăugată\n  When   `.save()` scrie fișierul `.excalidraw`\n  Then   se parsează ca JSON cu `type: \"excalidraw\"` și o listă `elements`\n\nCASE-2 — fiecare primitivă de formă adaugă un element de tipul ei\n  Given  un `Scene`\n  When   `box`, `ellipse`, `diamond` și `frame` sunt fiecare apelate o dată\n  Then   scena câștigă câte un element din fiecare tip Excalidraw corespunzător\n\nCASE-3 — alias-urile ISO 5807 produc formele lor de bază\n  Given  un `Scene`\n  When   `process`, `terminator`, `decision`, `data`, `predefined_process`, `preparation` și `connector` sunt fiecare apelate\n  Then   fiecare adaugă un element fără a ridica o eroare, folosind forma primitivă mapată\n\nCASE-4 — helper-ele de layout plasează formele fără suprapunere\n  Given  trei casete\n  When   `row([...])` le aranjează\n  Then   coordonatele rezultate le plasează una lângă alta fără nicio pereche suprapusă\n\nCASE-5 — helper-ele de adnotare atașează text fără a ridica o eroare\n  Given  un `Scene` cu o formă\n  When   `title`, `label`, `legend`, `glossary` și `role` sunt fiecare apelate\n  Then   fiecare adaugă elementul său de text și `.save()` tot reușește\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-EXCALIDRAW-845",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Connectors and the save() contract",
      "intent": "Shapes alone are not a diagram until they are linked, and a caller needs one predictable moment where the scene becomes files on disk. Connector helpers bind arrows to shape ids; `.save()` writes the `.excalidraw` scene and its HTML viewer together, exactly once, and — when the scene was seeded — byte-identically on every run, so a regenerated diagram never shows as a spurious diff.",
      "contract": [
        "`Scene` exposes connector helpers: `arrow`, `free_arrow`, `path`, `route_under`.",
        "`.save(basename, out_dir)` writes both `<basename>.excalidraw` (the scene JSON) and `<basename>.html` (a self-contained viewer) in one call and raises `RuntimeError` if called more than once on the same `Scene`.",
        "`Scene(seed=<int>)` produces byte-identical output across re-runs.",
        "The builder has no external dependencies — stdlib only."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — connector helpers link two shapes Given  two boxes already placed in a `Scene` When   `arrow(a, b)` is called Then   the scene gains an arrow element bound to both shapes' ids",
        "CASE-2 — — one .save() call writes both output files Given  a `Scene` with at least one shape When   `.save(\"demo\", out_dir)` is called Then   both `demo.excalidraw` and `demo.html` exist in `out_dir` after the single call",
        "CASE-3 — — a fixed seed reproduces byte-identical output Given  the same shape declarations built twice with `Scene(seed=42)` When   each is saved to its own file Then   the two `.excalidraw` files are byte-for-byte identical",
        "CASE-4 — — the builder imports only the standard library Given  `excalidraw_builder.py` When   its top-level imports are inspected Then   every imported module belongs to the Python standard library"
      ],
      "accept": "CASE-1 — connector helpers link two shapes\n  Given  two boxes already placed in a `Scene`\n  When   `arrow(a, b)` is called\n  Then   the scene gains an arrow element bound to both shapes' ids\n\nCASE-2 — one .save() call writes both output files\n  Given  a `Scene` with at least one shape\n  When   `.save(\"demo\", out_dir)` is called\n  Then   both `demo.excalidraw` and `demo.html` exist in `out_dir` after the single call\n\nCASE-3 — a fixed seed reproduces byte-identical output\n  Given  the same shape declarations built twice with `Scene(seed=42)`\n  When   each is saved to its own file\n  Then   the two `.excalidraw` files are byte-for-byte identical\n\nCASE-4 — the builder imports only the standard library\n  Given  `excalidraw_builder.py`\n  When   its top-level imports are inspected\n  Then   every imported module belongs to the Python standard library",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-EXCALIDRAW-030"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:186"
        },
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:902"
        },
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:1349"
        },
        {
          "role": "tested-by",
          "loc": "plugin/skills/excalidraw-diagram/scripts/test_excalidraw.py:2"
        },
        {
          "role": "tested-by",
          "loc": "plugin/skills/excalidraw-diagram/scripts/test_excalidraw.py:551"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Conectori și contractul save()",
          "intent": "Formele singure nu sunt o diagramă până nu sunt legate, iar apelantul are nevoie de un moment predictibil în care scena devine fișiere pe disc. Helper-ele de conector leagă săgeți de id-uri de formă; `.save()` scrie scena `.excalidraw` și viewer-ul ei HTML împreună, exact o dată, și — atunci când scena a fost inițializată cu seed — identic la nivel de byte la fiecare rulare, astfel încât o diagramă regenerată nu apare niciodată ca un diff fals.",
          "contract": "> Formele singure nu sunt o diagramă până nu sunt legate, iar apelantul are nevoie\n> de un moment predictibil în care scena devine fișiere pe disc. Helper-ele de\n> conector leagă săgeți de id-uri de formă; `.save()` scrie scena `.excalidraw` și\n> viewer-ul ei HTML împreună, exact o dată, și — atunci când scena a fost inițializată\n> cu seed — identic la nivel de byte la fiecare rulare, astfel încât o diagramă\n> regenerată nu apare niciodată ca un diff fals.\n\nFiecare bullet de mai jos este obligatoriu.\n- `Scene` expune helper-e de conector: `arrow`, `free_arrow`, `path`,\n  `route_under`.\n- `.save(basename, out_dir)` scrie atât `<basename>.excalidraw` (JSON-ul\n  scenei) cât și `<basename>.html` (un viewer de sine stătător) într-un singur apel și\n  ridică `RuntimeError` dacă este apelat mai mult de o dată pe același `Scene`.\n- `Scene(seed=<int>)` produce output identic la nivel de byte între rulări repetate.\n- Builder-ul nu are dependențe externe — doar stdlib.",
          "acceptance": "CASE-1 — helper-ele de conector leagă două forme\n  Given  două casete deja plasate într-un `Scene`\n  When   `arrow(a, b)` este apelat\n  Then   scena câștigă un element săgeată legat de id-urile ambelor forme\n\nCASE-2 — un singur apel .save() scrie ambele fișiere de output\n  Given  un `Scene` cu cel puțin o formă\n  When   `.save(\"demo\", out_dir)` este apelat\n  Then   atât `demo.excalidraw` cât și `demo.html` există în `out_dir` după singurul apel\n\nCASE-3 — un seed fix reproduce output identic la nivel de byte\n  Given  aceleași declarații de formă construite de două ori cu `Scene(seed=42)`\n  When   fiecare este salvat în propriul fișier\n  Then   cele două fișiere `.excalidraw` sunt identice byte cu byte\n\nCASE-4 — builder-ul importă doar biblioteca standard\n  Given  `excalidraw_builder.py`\n  When   importurile sale de nivel superior sunt inspectate\n  Then   fiecare modul importat aparține bibliotecii standard Python"
        }
      }
    },
    {
      "id": "ARCH-EXCALIDRAW-031",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Excalidraw quality gates",
      "intent": "A diagram that ships with overlapping shapes, arrows that cut through unrelated boxes, unlegended colours, text that spills outside its box, an arrow too short to draw a visible line, or a label wider than the arrow it sits on is unreadable to an outsider. Hard-fail gates at `.save()` time make these defects impossible to ship silently.",
      "contract": [
        "`.save()` supports five named gates (`crossing_check`, `legend_check`, `overflow_check`, `text_overlap_check`, `label_fit_check`), each accepting `\"warn\"` (default, prints) or `\"error\"` (raises `ValueError`). [[REQ-EXCALIDRAW-846]]",
        "`.save()` additionally enforces two hard gates that raise `ValueError` by default — overlapping shapes and arrows clamped too short to render — each with its own opt-out flag for a deliberate exception. [[REQ-EXCALIDRAW-847]]"
      ],
      "verify": [],
      "notes": [
        "The canonical pattern for a ship-quality diagram is all five named gates set to `\"error\"` (the two hard gates already raise by default): `save(..., crossing_check=\"error\", legend_check=\"error\", overflow_check=\"error\", text_overlap_check=\"error\", label_fit_check=\"error\")`."
      ],
      "current_impl": [
        "`check_overlaps`, `check_arrow_crossings`, `check_legend_coverage`, `check_text_overflow`, `check_text_overlaps`, `check_short_arrows`, `check_arrow_label_fit` and the `save()` gate dispatch in `skills/excalidraw-diagram/scripts/excalidraw_builder.py`.",
        "`skills/excalidraw-diagram/scripts/test_excalidraw.py` (gate regression suite).",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a scene where a bound arrow's path passes through an unrelated box When   `save(crossing_check=\"error\")` is called Then   a `ValueError` is raised; `save(crossing_check=\"warn\")` emits a warning and exits normally",
        "CASE-2 — Given  a scene with `fill=\"blue\"` on a box and no `legend()` rendered When   `save(legend_check=\"error\")` is called Then   a `ValueError` is raised naming the unlegended colour",
        "CASE-3 — Given  a box whose bound text is wider than the box width When   `save(overflow_check=\"error\")` is called Then   a `ValueError` is raised; `check_text_overflow()` returns the offending shape id",
        "CASE-4 — Given  two `label()` elements placed at overlapping coordinates When   `save(text_overlap_check=\"error\")` is called Then   a `ValueError` is raised naming the overlapping pair",
        "CASE-5 — Given  each maintained example generator in `examples/` When   `test_excalidraw.py` runs it Then   the generator produces zero violations on every gate (the five named gates and both hard gates)",
        "CASE-6 — Given  two boxes placed close but not overlapping, joined by a bound arrow When   `save()` is called with defaults Then   a `ValueError` is raised (the connector is too short to render); `save(allow_short_arrows=True)` writes the files, and `check_short_arrows()` returns the offending pair",
        "CASE-7 — Given  a bound arrow whose label is wider than the arrow's visible line When   `save(label_fit_check=\"error\")` is called Then   a `ValueError` is raised; `check_arrow_label_fit()` returns the offending label"
      ],
      "accept": "CASE-1\n  Given  a scene where a bound arrow's path passes through an unrelated box\n  When   `save(crossing_check=\"error\")` is called\n  Then   a `ValueError` is raised; `save(crossing_check=\"warn\")` emits a\n         warning and exits normally\n\nCASE-2\n  Given  a scene with `fill=\"blue\"` on a box and no `legend()` rendered\n  When   `save(legend_check=\"error\")` is called\n  Then   a `ValueError` is raised naming the unlegended colour\n\nCASE-3\n  Given  a box whose bound text is wider than the box width\n  When   `save(overflow_check=\"error\")` is called\n  Then   a `ValueError` is raised; `check_text_overflow()` returns the\n         offending shape id\n\nCASE-4\n  Given  two `label()` elements placed at overlapping coordinates\n  When   `save(text_overlap_check=\"error\")` is called\n  Then   a `ValueError` is raised naming the overlapping pair\n\nCASE-5\n  Given  each maintained example generator in `examples/`\n  When   `test_excalidraw.py` runs it\n  Then   the generator produces zero violations on every gate (the five named\n         gates and both hard gates)\n\nCASE-6\n  Given  two boxes placed close but not overlapping, joined by a bound arrow\n  When   `save()` is called with defaults\n  Then   a `ValueError` is raised (the connector is too short to render);\n         `save(allow_short_arrows=True)` writes the files, and\n         `check_short_arrows()` returns the offending pair\n\nCASE-7\n  Given  a bound arrow whose label is wider than the arrow's visible line\n  When   `save(label_fit_check=\"error\")` is called\n  Then   a `ValueError` is raised; `check_arrow_label_fit()` returns the\n         offending label",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-EXCALIDRAW-030"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-VISUAL-106"
      ],
      "satisfied_by": [
        "REQ-EXCALIDRAW-846",
        "REQ-EXCALIDRAW-847"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:3"
        },
        {
          "role": "tested-by",
          "loc": "plugin/skills/excalidraw-diagram/scripts/test_excalidraw.py:3"
        }
      ],
      "test_exempt": null,
      "milestone": "v2.4",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Porți de calitate Excalidraw",
          "intent": "O diagramă livrată cu forme suprapuse, săgeți care traversează casete fără legătură, culori fără legendă, text care se revarsă în afara casetei sale, o săgeată prea scurtă pentru a desena o linie vizibilă, sau o etichetă mai lată decât săgeata pe care stă este ilizibilă pentru un outsider. Porțile de eșec ferm la momentul `.save()` fac imposibilă livrarea în tăcere a acestor defecte.",
          "contract": "> O diagramă livrată cu forme suprapuse, săgeți care traversează casete fără legătură,\n> culori fără legendă, text care se revarsă în afara casetei sale, o săgeată prea scurtă\n> pentru a desena o linie vizibilă, sau o etichetă mai lată decât săgeata pe care stă\n> este ilizibilă pentru un outsider. Porțile de eșec ferm la momentul `.save()` fac\n> imposibilă livrarea în tăcere a acestor defecte.\n\nFiecare punct de mai jos este obligatoriu.\n- `.save()` suportă cinci porți numite (`crossing_check`, `legend_check`, `overflow_check`, `text_overlap_check`, `label_fit_check`), fiecare acceptând `\"warn\"` (implicit, afișează) sau `\"error\"` (ridică `ValueError`). [[REQ-EXCALIDRAW-846]]\n- `.save()` impune suplimentar două porți ferme care ridică `ValueError` implicit — forme suprapuse și săgeți fixate prea scurt pentru a fi randate — fiecare cu propriul flag de dezactivare pentru o excepție deliberată. [[REQ-EXCALIDRAW-847]]",
          "acceptance": "CASE-1\n  Given  o scenă unde traseul unei săgeți legate trece prin o casetă fără legătură\n  When   `save(crossing_check=\"error\")` este apelată\n  Then   se ridică un `ValueError`; `save(crossing_check=\"warn\")` emite un\n         avertisment și iese normal\n\nCASE-2\n  Given  o scenă cu `fill=\"blue\"` pe o casetă și fără nicio `legend()` randată\n  When   `save(legend_check=\"error\")` este apelată\n  Then   se ridică un `ValueError` care numește culoarea fără legendă\n\nCASE-3\n  Given  o casetă al cărei text legat este mai lat decât lățimea casetei\n  When   `save(overflow_check=\"error\")` este apelată\n  Then   se ridică un `ValueError`; `check_text_overflow()` returnează id-ul\n         formei incriminate\n\nCASE-4\n  Given  două elemente `label()` plasate la coordonate suprapuse\n  When   `save(text_overlap_check=\"error\")` este apelată\n  Then   se ridică un `ValueError` care numește perechea suprapusă\n\nCASE-5\n  Given  fiecare generator de exemplu întreținut din `examples/`\n  When   `test_excalidraw.py` îl rulează\n  Then   generatorul produce zero încălcări pe fiecare poartă (cele cinci porți\n         numite și ambele porți ferme)\n\nCASE-6\n  Given  două casete plasate aproape dar fără suprapunere, unite printr-o săgeată legată\n  When   `save()` este apelată cu valorile implicite\n  Then   se ridică un `ValueError` (conectorul este prea scurt pentru a fi randat);\n         `save(allow_short_arrows=True)` scrie fișierele, iar\n         `check_short_arrows()` returnează perechea incriminată\n\nCASE-7\n  Given  o săgeată legată a cărei etichetă este mai lată decât linia vizibilă a săgeții\n  When   `save(label_fit_check=\"error\")` este apelată\n  Then   se ridică un `ValueError`; `check_arrow_label_fit()` returnează\n         eticheta incriminată"
        }
      }
    },
    {
      "id": "REQ-EXCALIDRAW-846",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Named gates: crossing, legend, and overflow checks",
      "intent": "`.save()` runs five named checks before it writes a scene. Each accepts `\"warn\"` (print and continue, the default) or `\"error\"` (raise `ValueError`), so a generator can ship diagrams where readability defects — an arrow cutting through an unrelated box, an unlegended colour, text spilling out of its shape — fail the build instead of shipping silently.",
      "contract": [
        "`.save()` supports five named gates, each accepting `\"warn\"` (default, prints) or `\"error\"` (raises `ValueError`): `crossing_check`, `legend_check`, `overflow_check`, `text_overlap_check`, `label_fit_check`.",
        "`crossing_check`: a bound arrow whose straight centre-to-centre path passes through an unrelated box triggers the gate.",
        "`legend_check`: a fill colour used on any shape but absent from the `legend()` key triggers the gate (fires only after `legend()` is rendered; a scene with no legend is exempt).",
        "`overflow_check`: a shape whose bound text is larger than the shape bounds (text spills outside) triggers the gate."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — each named gate accepts warn or error mode Given  a scene with a crossing_check violation When   `save(crossing_check=\"warn\")` and `save(crossing_check=\"error\")` are each called Then   the warn call prints a warning and returns normally, the error call raises `ValueError`",
        "CASE-2 — — crossing_check fires when an arrow's path crosses an unrelated box Given  a bound arrow whose straight centre-to-centre path passes through a third, unrelated box When   `save(crossing_check=\"error\")` is called Then   a `ValueError` naming the crossing is raised",
        "CASE-3 — — legend_check fires for an unlegended colour, exempt with no legend Given  a shape filled \"blue\" with `legend()` rendered but \"blue\" absent from its key When   `save(legend_check=\"error\")` is called Then   a `ValueError` naming the unlegended colour is raised; the same scene with no `legend()` call raises nothing",
        "CASE-4 — — overflow_check fires when bound text exceeds the shape bounds Given  a box whose bound text is wider than the box When   `save(overflow_check=\"error\")` is called Then   a `ValueError` is raised --------------------"
      ],
      "accept": "CASE-1 — each named gate accepts warn or error mode\n  Given  a scene with a crossing_check violation\n  When   `save(crossing_check=\"warn\")` and `save(crossing_check=\"error\")` are each called\n  Then   the warn call prints a warning and returns normally, the error call raises `ValueError`\n\nCASE-2 — crossing_check fires when an arrow's path crosses an unrelated box\n  Given  a bound arrow whose straight centre-to-centre path passes through a third, unrelated\n         box\n  When   `save(crossing_check=\"error\")` is called\n  Then   a `ValueError` naming the crossing is raised\n\nCASE-3 — legend_check fires for an unlegended colour, exempt with no legend\n  Given  a shape filled \"blue\" with `legend()` rendered but \"blue\" absent from its key\n  When   `save(legend_check=\"error\")` is called\n  Then   a `ValueError` naming the unlegended colour is raised; the same scene with no\n         `legend()` call raises nothing\n\nCASE-4 — overflow_check fires when bound text exceeds the shape bounds\n  Given  a box whose bound text is wider than the box\n  When   `save(overflow_check=\"error\")` is called\n  Then   a `ValueError` is raised\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-EXCALIDRAW-031"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:1119"
        },
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:1159"
        },
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:1188"
        },
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:1349"
        },
        {
          "role": "tested-by",
          "loc": "plugin/skills/excalidraw-diagram/scripts/test_excalidraw.py:99"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Gate-uri numite: verificări de crossing, legend și overflow",
          "intent": "`.save()` rulează cinci verificări numite înainte de a scrie o scenă. Fiecare acceptă `\"warn\"` (afișează și continuă, implicit) sau `\"error\"` (ridică `ValueError`), astfel încât un generator poate livra diagrame în care defecte de lizibilitate — o săgeată care taie prin o casetă nelegată, o culoare nelegendată, text care depășește forma sa — fac build-ul să eșueze în loc să fie livrate în tăcere.",
          "contract": "> `.save()` rulează cinci verificări numite înainte de a scrie o scenă. Fiecare acceptă\n> `\"warn\"` (afișează și continuă, implicit) sau `\"error\"` (ridică `ValueError`), astfel\n> încât un generator poate livra diagrame în care defecte de lizibilitate — o săgeată\n> care taie prin o casetă nelegată, o culoare nelegendată, text care depășește\n> forma sa — fac build-ul să eșueze în loc să fie livrate în tăcere.\n\nFiecare bullet de mai jos este obligatoriu.\n- `.save()` suportă cinci gate-uri numite, fiecare acceptând `\"warn\"` (implicit,\n  afișează) sau `\"error\"` (ridică `ValueError`): `crossing_check`, `legend_check`,\n  `overflow_check`, `text_overlap_check`, `label_fit_check`.\n- `crossing_check`: o săgeată legată al cărei traseu drept centru-la-centru trece\n  printr-o casetă nelegată declanșează gate-ul.\n- `legend_check`: o culoare de umplere folosită pe orice formă dar absentă din\n  cheia `legend()` declanșează gate-ul (se declanșează doar după ce `legend()` este\n  randat; o scenă fără legendă este exceptată).\n- `overflow_check`: o formă al cărei text legat este mai mare decât limitele formei\n  (textul depășește) declanșează gate-ul.",
          "acceptance": "CASE-1 — fiecare gate numit acceptă modul warn sau error\n  Given  o scenă cu o încălcare de crossing_check\n  When   `save(crossing_check=\"warn\")` și `save(crossing_check=\"error\")` sunt fiecare apelate\n  Then   apelul warn afișează un avertisment și se întoarce normal, apelul error ridică `ValueError`\n\nCASE-2 — crossing_check se declanșează când traseul unei săgeți traversează o casetă nelegată\n  Given  o săgeată legată al cărei traseu drept centru-la-centru trece printr-o a treia casetă,\n         nelegată\n  When   `save(crossing_check=\"error\")` este apelat\n  Then   este ridicat un `ValueError` care numește intersecția\n\nCASE-3 — legend_check se declanșează pentru o culoare nelegendată, exceptată fără legendă\n  Given  o formă umplută \"blue\" cu `legend()` randat dar cu \"blue\" absent din cheia sa\n  When   `save(legend_check=\"error\")` este apelat\n  Then   este ridicat un `ValueError` care numește culoarea nelegendată; aceeași scenă fără\n         apel `legend()` nu ridică nimic\n\nCASE-4 — overflow_check se declanșează când textul legat depășește limitele formei\n  Given  o casetă al cărei text legat este mai lat decât caseta\n  When   `save(overflow_check=\"error\")` este apelat\n  Then   este ridicat un `ValueError`\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-EXCALIDRAW-847",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Text-overlap, label-fit gates, and the two hard gates",
      "intent": "Two more named checks (`text_overlap_check`, `label_fit_check`) catch overlapping captions and labels that crowd their arrow. Beyond those, `.save()` enforces two hard gates that always raise unless explicitly opted out — overlapping shapes and arrows too short to render — because those two defects have no legitimate \"warn and ship\" case.",
      "contract": [
        "`text_overlap_check`: two free captions or label elements that geometrically overlap each other trigger the gate.",
        "`label_fit_check`: a bound arrow whose text label is wider than the connector it sits on — leaving less than ~24px of visible line on each side, measuring the label box projected onto the arrow direction — triggers the gate (the label crowds the arrowheads or spills onto the joined boxes).",
        "`.save()` additionally enforces two hard gates that raise `ValueError` by default (no `\"warn\"`/`\"error\"` mode), each with an opt-out flag for a deliberate exception: overlapping non-container shapes (`allow_overlap=True`) and a bound arrow clamped too short to render a visible line — only its label would show — (`allow_short_arrows=True`).",
        "The inspection methods `check_overlaps()`, `check_arrow_crossings()`, `check_legend_coverage()`, `check_text_overflow()`, `check_text_overlaps()`, `check_short_arrows()`, `check_arrow_label_fit()` each return a list of offending items (empty list = clean) and are callable before `.save()`.",
        "`test_excalidraw.py` exercises the five named gates in both `\"warn\"` and `\"error\"` modes, and the two hard gates, for each maintained example generator."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — text_overlap_check fires when two labels geometrically overlap Given  two `label()` elements placed at overlapping coordinates When   `save(text_overlap_check=\"error\")` is called Then   a `ValueError` naming the overlapping pair is raised",
        "CASE-2 — — label_fit_check fires when a label crowds a short arrow Given  a bound arrow whose label leaves less than ~24px of visible line on one side When   `save(label_fit_check=\"error\")` is called Then   a `ValueError` is raised",
        "CASE-3 — — the two hard gates raise by default and respect their opt-out flags Given  two overlapping non-container shapes, and separately a bound arrow clamped too short to render When   `save()` is called with defaults, then again with `allow_overlap=True` / `allow_short_arrows=True` Then   the default calls raise `ValueError`, and the opt-out calls write the files without raising",
        "CASE-4 — — an inspection method returns offending items without saving Given  a scene with one crossing-arrow violation and no other defects When   `check_arrow_crossings()` is called before `.save()` Then   it returns a list containing that one item, while an unaffected check like `check_text_overflow()` returns an empty list"
      ],
      "accept": "CASE-1 — text_overlap_check fires when two labels geometrically overlap\n  Given  two `label()` elements placed at overlapping coordinates\n  When   `save(text_overlap_check=\"error\")` is called\n  Then   a `ValueError` naming the overlapping pair is raised\n\nCASE-2 — label_fit_check fires when a label crowds a short arrow\n  Given  a bound arrow whose label leaves less than ~24px of visible line on one side\n  When   `save(label_fit_check=\"error\")` is called\n  Then   a `ValueError` is raised\n\nCASE-3 — the two hard gates raise by default and respect their opt-out flags\n  Given  two overlapping non-container shapes, and separately a bound arrow clamped too short to\n         render\n  When   `save()` is called with defaults, then again with `allow_overlap=True` /\n         `allow_short_arrows=True`\n  Then   the default calls raise `ValueError`, and the opt-out calls write the files without\n         raising\n\nCASE-4 — an inspection method returns offending items without saving\n  Given  a scene with one crossing-arrow violation and no other defects\n  When   `check_arrow_crossings()` is called before `.save()`\n  Then   it returns a list containing that one item, while an unaffected check like\n         `check_text_overflow()` returns an empty list",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-EXCALIDRAW-031"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:1071"
        },
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:1212"
        },
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:1236"
        },
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:1273"
        },
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:1349"
        },
        {
          "role": "tested-by",
          "loc": "plugin/skills/excalidraw-diagram/scripts/test_excalidraw.py:99"
        },
        {
          "role": "tested-by",
          "loc": "plugin/skills/excalidraw-diagram/scripts/test_excalidraw.py:636"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Gate-urile text-overlap, label-fit, și cele două gate-uri stricte",
          "intent": "Alte două verificări numite (`text_overlap_check`, `label_fit_check`) prind titluri suprapuse și etichete care își aglomerează săgeata. Dincolo de acestea, `.save()` impune două gate-uri stricte care ridică întotdeauna o eroare, cu excepția unei renunțări explicite — forme suprapuse și săgeți prea scurte pentru a fi randate — pentru că aceste două defecte nu au niciun caz legitim de \"avertizează și livrează\".",
          "contract": "> Alte două verificări numite (`text_overlap_check`, `label_fit_check`) prind titluri\n> suprapuse și etichete care își aglomerează săgeata. Dincolo de acestea, `.save()` impune\n> două gate-uri stricte care ridică întotdeauna o eroare, cu excepția unei renunțări\n> explicite — forme suprapuse și săgeți prea scurte pentru a fi randate — pentru că\n> aceste două defecte nu au niciun caz legitim de \"avertizează și livrează\".\n\nFiecare bullet de mai jos este obligatoriu.\n- `text_overlap_check`: două titluri libere sau elemente de etichetă care se suprapun\n  geometric unul pe celălalt declanșează gate-ul.\n- `label_fit_check`: o săgeată legată a cărei etichetă text este mai lată decât conectorul\n  pe care se află — lăsând mai puțin de ~24px de linie vizibilă pe fiecare parte, măsurând\n  caseta etichetei proiectată pe direcția săgeții — declanșează gate-ul\n  (eticheta aglomerează vârfurile săgeții sau se revarsă peste casetele unite).\n- `.save()` impune suplimentar două gate-uri stricte care ridică `ValueError`\n  implicit (fără mod `\"warn\"`/`\"error\"`), fiecare cu un flag de renunțare pentru o\n  excepție deliberată: forme non-container suprapuse (`allow_overlap=True`)\n  și o săgeată legată clampată prea scurt pentru a randa o linie vizibilă — doar eticheta\n  sa ar apărea — (`allow_short_arrows=True`).\n- Metodele de inspecție `check_overlaps()`, `check_arrow_crossings()`,\n  `check_legend_coverage()`, `check_text_overflow()`, `check_text_overlaps()`,\n  `check_short_arrows()`, `check_arrow_label_fit()` returnează fiecare o listă de\n  elemente cu probleme (listă goală = curat) și pot fi apelate înainte de `.save()`.\n- `test_excalidraw.py` exersează cele cinci gate-uri numite atât în modul `\"warn\"` cât și\n  `\"error\"`, plus cele două gate-uri stricte, pentru fiecare generator exemplu întreținut.",
          "acceptance": "CASE-1 — text_overlap_check se declanșează când două etichete se suprapun geometric\n  Given  două elemente `label()` plasate la coordonate suprapuse\n  When   `save(text_overlap_check=\"error\")` este apelat\n  Then   este ridicat un `ValueError` care numește perechea suprapusă\n\nCASE-2 — label_fit_check se declanșează când o etichetă aglomerează o săgeată scurtă\n  Given  o săgeată legată a cărei etichetă lasă mai puțin de ~24px de linie vizibilă pe o parte\n  When   `save(label_fit_check=\"error\")` este apelat\n  Then   este ridicat un `ValueError`\n\nCASE-3 — cele două gate-uri stricte ridică o eroare implicit și respectă flag-urile lor de renunțare\n  Given  două forme non-container suprapuse, și separat o săgeată legată clampată prea scurt pentru\n         a fi randată\n  When   `save()` este apelat cu valorile implicite, apoi din nou cu `allow_overlap=True` /\n         `allow_short_arrows=True`\n  Then   apelurile implicite ridică `ValueError`, iar apelurile de renunțare scriu fișierele fără\n         a ridica o eroare\n\nCASE-4 — o metodă de inspecție returnează elementele cu probleme fără a salva\n  Given  o scenă cu o încălcare de tip crossing-arrow și niciun alt defect\n  When   `check_arrow_crossings()` este apelat înainte de `.save()`\n  Then   returnează o listă conținând acel element, în timp ce o verificare neafectată precum\n         `check_text_overflow()` returnează o listă goală"
        }
      }
    },
    {
      "id": "ARCH-EXCALIDRAW-032",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Excalidraw builder CLI verbs",
      "intent": "Diagrams need to be rebuilt when source changes: either from a generator script (the main authoring path), from a hand-edited `.excalidraw` file (viewer rebuild only), or from an existing repo that needs a scaffolded starting point (discover). The CLI exposes these as three distinct verbs so each use-case has a single, unambiguous entry point.",
      "contract": [
        "The CLI exposes three verbs, each a single unambiguous entry point: no-arg runs the CI smoke test, `render` rebuilds a viewer HTML from an existing `.excalidraw` file, and `discover` scaffolds a starting-point stub from an existing repo; any other verb exits 2 with usage. [[REQ-EXCALIDRAW-848]]"
      ],
      "verify": [],
      "notes": [
        "`discover` only scaffolds the *components* it can see; inferring real data flow and grouping remains the author's responsibility.",
        "The smoke test (no-arg invocation) is what CI depends on — do not change the no-arg behaviour."
      ],
      "current_impl": [
        "`_main(argv)` and the `render_html`, `discover_stub` functions in `skills/excalidraw-diagram/scripts/excalidraw_builder.py`.",
        "`skills/excalidraw-diagram/scripts/test_excalidraw.py` (CLI test class).",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  no arguments When   `python excalidraw_builder.py` is run Then   it exits 0 and stdout contains a success/summary line (not an error)",
        "CASE-2 — Given  a valid `.excalidraw` file at `<path>` When   `render <path>` is run Then   a `<basename>.html` is written; the source `.excalidraw` is unchanged",
        "CASE-3 — Given  a directory containing at least one `.py` source file When   `discover <dir>` is run Then   a runnable Python stub is emitted that imports `excalidraw_builder` and calls `.save()`",
        "CASE-4 — Given  an unknown verb such as `frobnicate` When   `python excalidraw_builder.py frobnicate` Then   the process exits with code 2 and prints usage"
      ],
      "accept": "CASE-1\n  Given  no arguments\n  When   `python excalidraw_builder.py` is run\n  Then   it exits 0 and stdout contains a success/summary line (not an error)\n\nCASE-2\n  Given  a valid `.excalidraw` file at `<path>`\n  When   `render <path>` is run\n  Then   a `<basename>.html` is written; the source `.excalidraw` is unchanged\n\nCASE-3\n  Given  a directory containing at least one `.py` source file\n  When   `discover <dir>` is run\n  Then   a runnable Python stub is emitted that imports `excalidraw_builder`\n         and calls `.save()`\n\nCASE-4\n  Given  an unknown verb such as `frobnicate`\n  When   `python excalidraw_builder.py frobnicate`\n  Then   the process exits with code 2 and prints usage",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-EXCALIDRAW-030"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-VISUAL-106"
      ],
      "satisfied_by": [
        "REQ-EXCALIDRAW-848"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:4"
        },
        {
          "role": "tested-by",
          "loc": "plugin/skills/excalidraw-diagram/scripts/test_excalidraw.py:4"
        }
      ],
      "test_exempt": null,
      "milestone": "v2.4",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Verbele CLI ale constructorului Excalidraw",
          "intent": "Diagramele trebuie reconstruite atunci când sursa se schimbă: fie dintr-un script generator (calea principală de autorat), fie dintr-un fișier `.excalidraw` editat manual (reconstruire doar a viewer-ului), fie dintr-un repo existent care are nevoie de un punct de pornire schelet (discover). CLI-ul expune acestea ca trei verbe distincte, astfel încât fiecare caz de utilizare are un singur punct de intrare, fără ambiguitate.",
          "contract": "> Diagramele trebuie reconstruite atunci când sursa se schimbă: fie dintr-un script\n> generator (calea principală de autorat), fie dintr-un fișier `.excalidraw` editat\n> manual (reconstruire doar a viewer-ului), fie dintr-un repo existent care are nevoie\n> de un punct de pornire schelet (discover). CLI-ul expune acestea ca trei verbe\n> distincte, astfel încât fiecare caz de utilizare are un singur punct de intrare, fără\n> ambiguitate.\n\nFiecare punct de mai jos este obligatoriu.\n- CLI-ul expune trei verbe, fiecare un singur punct de intrare fără ambiguitate: fără argument rulează testul de fum CI, `render` reconstruiește un viewer HTML dintr-un fișier `.excalidraw` existent, iar `discover` schelează un stub de punct de pornire dintr-un repo existent; orice alt verb iese cu 2 și afișează utilizarea. [[REQ-EXCALIDRAW-848]]",
          "acceptance": "CASE-1\n  Given  niciun argument\n  When   `python excalidraw_builder.py` este rulat\n  Then   iese cu 0 iar stdout conține o linie de succes/rezumat (nu o eroare)\n\nCASE-2\n  Given  un fișier `.excalidraw` valid la `<path>`\n  When   `render <path>` este rulat\n  Then   se scrie un `<basename>.html`; sursa `.excalidraw` rămâne neschimbată\n\nCASE-3\n  Given  un director care conține cel puțin un fișier sursă `.py`\n  When   `discover <dir>` este rulat\n  Then   se emite un stub Python rulabil care importă `excalidraw_builder`\n         și apelează `.save()`\n\nCASE-4\n  Given  un verb necunoscut precum `frobnicate`\n  When   `python excalidraw_builder.py frobnicate`\n  Then   procesul iese cu codul 2 și afișează utilizarea"
        }
      }
    },
    {
      "id": "REQ-EXCALIDRAW-848",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Smoke test, render and discover verbs",
      "intent": "A diagram can need rebuilding from three different starting points — a generator script, a hand-edited `.excalidraw` scene, or an existing repo with no diagram yet — and each needs its own unambiguous command rather than one verb guessing the intent. The no-arg form is also CI's health check for the builder itself, so it must never be shadowed by a new default.",
      "contract": [
        "Invoking `python excalidraw_builder.py` with **no arguments** runs the builder smoke test, prints a human-readable summary, and exits 0 on success. This is the CI health-check entry point and is never shadowed by a new default verb.",
        "`python excalidraw_builder.py render <scene.excalidraw> [out_dir]` reads an existing `.excalidraw` file and writes a fresh self-contained `<basename>.html` viewer beside it (or into `out_dir` when given). The source `.excalidraw` file is never modified.",
        "`python excalidraw_builder.py discover <repo> [out.py]` scans `<repo>` for source files and emits a runnable multi-layer poster stub (STRUCTURE layer pre-populated, WORKFLOW / INTEGRATION / MODES / MODEL / DATA layers commented as scaffolds) to `out.py` (default: `make_diagram.py`).",
        "Any unrecognised verb exits with code 2 and prints a usage message."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — no-arg invocation runs the smoke test and exits 0 Given  no command-line arguments When   `python excalidraw_builder.py` runs Then   it prints a success summary line and exits 0",
        "CASE-2 — — render rebuilds the HTML viewer without touching the source Given  an existing `<scene>.excalidraw` file When   `render <scene>.excalidraw` runs Then   a fresh `<scene>.html` is written and the `.excalidraw` file's bytes are unchanged",
        "CASE-3 — — discover emits a runnable multi-layer stub Given  a repository containing at least one source file When   `discover <repo> out.py` runs Then   `out.py` is written, imports `excalidraw_builder`, and runs without error",
        "CASE-4 — — an unknown verb exits 2 with usage Given  the verb `frobnicate` When   `python excalidraw_builder.py frobnicate` runs Then   it exits with code 2 and prints a usage message"
      ],
      "accept": "CASE-1 — no-arg invocation runs the smoke test and exits 0\n  Given  no command-line arguments\n  When   `python excalidraw_builder.py` runs\n  Then   it prints a success summary line and exits 0\n\nCASE-2 — render rebuilds the HTML viewer without touching the source\n  Given  an existing `<scene>.excalidraw` file\n  When   `render <scene>.excalidraw` runs\n  Then   a fresh `<scene>.html` is written and the `.excalidraw` file's bytes are unchanged\n\nCASE-3 — discover emits a runnable multi-layer stub\n  Given  a repository containing at least one source file\n  When   `discover <repo> out.py` runs\n  Then   `out.py` is written, imports `excalidraw_builder`, and runs without error\n\nCASE-4 — an unknown verb exits 2 with usage\n  Given  the verb `frobnicate`\n  When   `python excalidraw_builder.py frobnicate` runs\n  Then   it exits with code 2 and prints a usage message",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-EXCALIDRAW-032"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:1600"
        },
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:1794"
        },
        {
          "role": "implements",
          "loc": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py:1903"
        },
        {
          "role": "tested-by",
          "loc": "plugin/skills/excalidraw-diagram/scripts/test_excalidraw.py:390"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Verbele smoke test, render și discover",
          "intent": "O diagramă poate necesita reconstrucție din trei puncte de plecare diferite — un script generator, o scenă `.excalidraw` editată manual, sau un repo existent fără nicio diagramă încă — și fiecare are nevoie de propria comandă neambiguă în loc de un singur verb care ghicește intenția. Forma fără argumente este de asemenea verificarea de sănătate a CI pentru builder-ul însuși, deci nu trebuie niciodată umbrită de un nou verb implicit.",
          "contract": "> O diagramă poate necesita reconstrucție din trei puncte de plecare diferite — un\n> script generator, o scenă `.excalidraw` editată manual, sau un repo existent fără\n> nicio diagramă încă — și fiecare are nevoie de propria comandă neambiguă în loc de\n> un singur verb care ghicește intenția. Forma fără argumente este de asemenea\n> verificarea de sănătate a CI pentru builder-ul însuși, deci nu trebuie niciodată\n> umbrită de un nou verb implicit.\n\nFiecare bullet de mai jos este obligatoriu.\n- Invocarea `python excalidraw_builder.py` fără **niciun argument** rulează\n  smoke test-ul builder-ului, afișează un rezumat lizibil pentru om, și iese cu 0 la succes.\n  Acesta este punctul de intrare al verificării de sănătate CI și nu este niciodată umbrit de un nou\n  verb implicit.\n- `python excalidraw_builder.py render <scene.excalidraw> [out_dir]` citește\n  un fișier `.excalidraw` existent și scrie un viewer `<basename>.html`\n  proaspăt, de sine stătător, lângă el (sau în `out_dir` dacă este dat). Fișierul\n  `.excalidraw` sursă nu este niciodată modificat.\n- `python excalidraw_builder.py discover <repo> [out.py]` scanează `<repo>`\n  pentru fișiere sursă și emite un stub de poster multi-layer rulabil (layer-ul STRUCTURE\n  pre-populat, layer-ele WORKFLOW / INTEGRATION / MODES / MODEL / DATA\n  comentate ca schele) în `out.py` (implicit: `make_diagram.py`).\n- Orice verb nerecunoscut iese cu codul 2 și afișează un mesaj de utilizare.",
          "acceptance": "CASE-1 — invocarea fără argumente rulează smoke test-ul și iese cu 0\n  Given  niciun argument de linie de comandă\n  When   `python excalidraw_builder.py` rulează\n  Then   afișează o linie de rezumat de succes și iese cu 0\n\nCASE-2 — render reconstruiește viewer-ul HTML fără a atinge sursa\n  Given  un fișier `<scene>.excalidraw` existent\n  When   `render <scene>.excalidraw` rulează\n  Then   un `<scene>.html` proaspăt este scris și octeții fișierului `.excalidraw` rămân neschimbați\n\nCASE-3 — discover emite un stub multi-layer rulabil\n  Given  un repository conținând cel puțin un fișier sursă\n  When   `discover <repo> out.py` rulează\n  Then   `out.py` este scris, importă `excalidraw_builder`, și rulează fără erori\n\nCASE-4 — un verb necunoscut iese cu 2 și afișează utilizarea\n  Given  verbul `frobnicate`\n  When   `python excalidraw_builder.py frobnicate` rulează\n  Then   iese cu codul 2 și afișează un mesaj de utilizare"
        }
      }
    },
    {
      "id": "ARCH-EXTRACT-008",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Legacy extraction",
      "intent": "When an existing project has lots of code but no requirements written down, this gives you a running start: it reads each untagged file and writes a rough draft requirement for it, clearly marked as a draft so no one mistakes a guess for a settled decision. Without it, someone would have to sit down and describe every existing capability from scratch before the tool was useful on an older project. Spec and prompt documents get the same treatment from the companion capability [[ARCH-PROSE-024]].",
      "contract": [
        "`draft` walks every untagged scannable code file, skipping tagged and `.reqmapignore`-matched ones. [[REQ-EXTRACT-849]] details the behaviour.",
        "`draft` proposes one `requirements/DRAFT-*.md` per remaining file, marked `status: draft` with a TODO contract. [[REQ-EXTRACT-850]] details the behaviour.",
        "`draft` assigns a cheap risk score from `TODO`/`FIXME`/`HACK`/`XXX` markers, suppressions and file size, and never overwrites an existing draft. [[REQ-EXTRACT-851]] details the behaviour."
      ],
      "verify": [],
      "notes": [
        "`draft` cannot recover intent; prefer `plan` (read-only plan) before authoring. The draft body uses the same Contract/Acceptance section names as a real requirement so a promoted draft needs no reshaping.",
        "Prose (`.md`/`.html`) classification and drafting is a separate capability — [[ARCH-PROSE-024]] — running under the same `draft` command.",
        "The drafted file types (`.py`/`.js`/`.ts`/`.c`/`.cpp`) are deliberately narrower than the gate's full scan set (which also enforces tags in `.go`/`.rs`/`.tsx` and more): drafting targets mainstream source, while the gate enforces tags wherever they appear."
      ],
      "current_impl": [
        "`cmd_extract`, `_draft_id`, `_risk` in `reqmap.py`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  an untagged `.py`/`.js`/`.ts`/`.c`/`.cpp` file When   `draft` runs Then   it yields one `DRAFT-*` draft",
        "CASE-2 — Given  a file already carrying a member tag When   `draft` runs Then   the file is skipped",
        "CASE-3 — Given  a file matching a `.reqmapignore` pattern When   `draft` runs Then   the file is skipped (no draft proposed for it)",
        "CASE-4 — Given  a file containing `TODO`/`FIXME` When   `draft` runs Then   it scores higher risk and is flagged `REVIEW`",
        "CASE-5 — Given  an existing draft and same-basename files in different dirs When   `draft` re-runs Then   the draft is not overwritten and the basenames do not collide",
        "CASE-6 — Given  an untagged `.py` file with a module docstring and two top-level functions When   `draft` runs Then   its proposal's WHERE section names both signatures, and its Contract is still the TODO placeholder"
      ],
      "accept": "CASE-1\n  Given  an untagged `.py`/`.js`/`.ts`/`.c`/`.cpp` file\n  When   `draft` runs\n  Then   it yields one `DRAFT-*` draft\n\nCASE-2\n  Given  a file already carrying a member tag\n  When   `draft` runs\n  Then   the file is skipped\n\nCASE-3\n  Given  a file matching a `.reqmapignore` pattern\n  When   `draft` runs\n  Then   the file is skipped (no draft proposed for it)\n\nCASE-4\n  Given  a file containing `TODO`/`FIXME`\n  When   `draft` runs\n  Then   it scores higher risk and is flagged `REVIEW`\n\nCASE-5\n  Given  an existing draft and same-basename files in different dirs\n  When   `draft` re-runs\n  Then   the draft is not overwritten and the basenames do not collide\n\nCASE-6\n  Given  an untagged `.py` file with a module docstring and two top-level functions\n  When   `draft` runs\n  Then   its proposal's WHERE section names both signatures, and its Contract is still the TODO placeholder",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-SCAN-002"
      ],
      "used_by": [
        "ARCH-INIT-012",
        "ARCH-PROSE-024"
      ],
      "satisfies": [
        "SYS-READ-103"
      ],
      "satisfied_by": [
        "REQ-EXTRACT-849",
        "REQ-EXTRACT-850",
        "REQ-EXTRACT-851"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3315"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3382"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3477"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3496"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1055"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1070"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6883"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9436"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.06",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Extracție din cod moștenit",
          "intent": "Când un proiect existent are mult cod dar nicio cerință scrisă, aceasta îți oferă un start avansat: citește fiecare fișier neetichetat și scrie o cerință-draft aproximativă pentru el, marcată clar ca draft, astfel încât nimeni să nu confunde o presupunere cu o decizie stabilită. Fără aceasta, cineva ar trebui să se așeze și să descrie fiecare capabilitate existentă de la zero înainte ca instrumentul să fie util pe un proiect mai vechi. Documentele de specificație și de prompt primesc același tratament din partea capabilității însoțitoare [[ARCH-PROSE-024]].",
          "contract": "> Când un proiect existent are mult cod dar nicio cerință scrisă, aceasta îți oferă\n> un start avansat: citește fiecare fișier neetichetat și scrie o cerință-draft\n> aproximativă pentru el, marcată clar ca draft, astfel încât nimeni să nu confunde o\n> presupunere cu o decizie stabilită. Fără aceasta, cineva ar trebui să se așeze și să\n> descrie fiecare capabilitate existentă de la zero înainte ca instrumentul să fie util\n> pe un proiect mai vechi. Documentele de specificație și de prompt primesc același\n> tratament din partea capabilității însoțitoare [[ARCH-PROSE-024]].\n\nFiecare punct de mai jos este obligatoriu.\n- `draft` parcurge fiecare fișier de cod scanabil neetichetat, sărind peste cele etichetate și cele potrivite de `.reqmapignore`. [[REQ-EXTRACT-849]] detaliază comportamentul.\n- `draft` propune câte un `requirements/DRAFT-*.md` per fișier rămas, marcat `status: draft` cu un contract TODO. [[REQ-EXTRACT-850]] detaliază comportamentul.\n- `draft` atribuie un scor de risc ieftin din markerii `TODO`/`FIXME`/`HACK`/`XXX`, suprimări și dimensiunea fișierului, și nu suprascrie niciodată un draft existent. [[REQ-EXTRACT-851]] detaliază comportamentul.",
          "acceptance": "CASE-1\n  Given  un fișier neetichetat `.py`/`.js`/`.ts`/`.c`/`.cpp`\n  When   `draft` rulează\n  Then   produce un draft `DRAFT-*`\n\nCASE-2\n  Given  un fișier care poartă deja o etichetă de membru\n  When   `draft` rulează\n  Then   fișierul este sărit\n\nCASE-3\n  Given  un fișier care se potrivește cu un tipar `.reqmapignore`\n  When   `draft` rulează\n  Then   fișierul este sărit (niciun draft propus pentru el)\n\nCASE-4\n  Given  un fișier care conține `TODO`/`FIXME`\n  When   `draft` rulează\n  Then   primește un scor de risc mai mare și este marcat `REVIEW`\n\nCASE-5\n  Given  un draft existent și fișiere cu același nume de bază în directoare diferite\n  When   `draft` rulează din nou\n  Then   draft-ul nu este suprascris iar numele de bază nu intră în coliziune\n\nCASE-6\n  Given  un fișier `.py` neetichetat cu un docstring de modul și două funcții de nivel superior\n  When   `draft` rulează\n  Then   secțiunea WHERE a propunerii sale numește ambele semnături, iar Contractul său este încă placeholder-ul TODO"
        }
      }
    },
    {
      "id": "REQ-EXTRACT-849",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Which files draft walks",
      "intent": "Drafting every file in a repo would bury real gaps under noise, so `draft` only walks scannable code files that carry no member tag yet and are not excluded by `.reqmapignore` — the same ignore file `scan` respects, which is how the engine's own vendored `reqmap.py` stays out of its own drafts.",
      "contract": [
        "`draft` walks every untagged scannable code file — the extensions and basenames the scan reads — plus prose in the capability bucket.",
        "`draft` skips a file that already carries a member tag.",
        "`draft` honors `.reqmapignore`, the same fnmatch globs `scan` respects.",
        "A file matching an ignore pattern is never drafted — notably the vendored `scripts/reqmap.py` engine itself."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — draft proposes a file for every untagged scannable extension Given  an untagged `.py` file and an untagged `.rs` file, both scannable extensions When   `draft` runs Then   it proposes one `DRAFT-*.md` for each",
        "CASE-2 — — draft skips a file already carrying a member tag Given  a `.py` file carrying `# implements: AUTH-LOGIN-001` When   `draft` runs Then   no `DRAFT-*.md` is proposed for that file",
        "CASE-3 — — draft skips a file matched by .reqmapignore Given  an untagged file matching a `.reqmapignore` glob pattern When   `draft` runs Then   no `DRAFT-*.md` is proposed for that file — notably the vendored `scripts/reqmap.py` engine itself --------------------"
      ],
      "accept": "CASE-1 — draft proposes a file for every untagged scannable extension\n  Given  an untagged `.py` file and an untagged `.rs` file, both scannable extensions\n  When   `draft` runs\n  Then   it proposes one `DRAFT-*.md` for each\n\nCASE-2 — draft skips a file already carrying a member tag\n  Given  a `.py` file carrying `# implements: AUTH-LOGIN-001`\n  When   `draft` runs\n  Then   no `DRAFT-*.md` is proposed for that file\n\nCASE-3 — draft skips a file matched by .reqmapignore\n  Given  an untagged file matching a `.reqmapignore` glob pattern\n  When   `draft` runs\n  Then   no `DRAFT-*.md` is proposed for that file — notably the vendored\n         `scripts/reqmap.py` engine itself\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-EXTRACT-008"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3382"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1070"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9436"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Ce fișiere parcurge draft",
          "intent": "Crearea unui draft pentru fiecare fișier dintr-un repo ar îngropa lacunele reale sub zgomot, deci `draft` parcurge doar fișierele de cod scanabile care nu poartă încă niciun tag de membru și nu sunt excluse de `.reqmapignore` — același fișier de ignorare pe care `scan` îl respectă, motiv pentru care propriul `reqmap.py` vendorizat al motorului rămâne în afara propriilor sale draft-uri.",
          "contract": "> Crearea unui draft pentru fiecare fișier dintr-un repo ar îngropa lacunele reale sub\n> zgomot, deci `draft` parcurge doar fișierele de cod scanabile care nu poartă încă niciun\n> tag de membru și nu sunt excluse de `.reqmapignore` — același fișier de ignorare pe care\n> `scan` îl respectă, motiv pentru care propriul `reqmap.py` vendorizat al motorului\n> rămâne în afara propriilor sale draft-uri.\n\nFiecare bullet de mai jos este obligatoriu.\n- `draft` parcurge fiecare fișier de cod scanabil netagat — extensiile și numele de bază pe care\n  scanarea le citește — plus prosa din bucket-ul de capabilități.\n- `draft` sare peste un fișier care poartă deja un tag de membru.\n- `draft` respectă `.reqmapignore`, aceleași glob-uri fnmatch pe care `scan` le respectă.\n- Un fișier care se potrivește cu un pattern de ignorare nu este niciodată transformat în draft —\n  în special motorul `scripts/reqmap.py` vendorizat însuși.",
          "acceptance": "CASE-1 — draft propune un fișier pentru fiecare extensie scanabilă netagată\n  Given  un fișier `.py` netagat și un fișier `.rs` netagat, ambele extensii scanabile\n  When   `draft` rulează\n  Then   propune câte un `DRAFT-*.md` pentru fiecare\n\nCASE-2 — draft sare peste un fișier care poartă deja un tag de membru\n  Given  un fișier `.py` care poartă `# implements: AUTH-LOGIN-001`\n  When   `draft` rulează\n  Then   niciun `DRAFT-*.md` nu este propus pentru acel fișier\n\nCASE-3 — draft sare peste un fișier potrivit de .reqmapignore\n  Given  un fișier netagat care se potrivește cu un pattern glob din `.reqmapignore`\n  When   `draft` rulează\n  Then   niciun `DRAFT-*.md` nu este propus pentru acel fișier — în special motorul\n         `scripts/reqmap.py` vendorizat însuși\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-EXTRACT-850",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Writing one draft proposal per file",
      "intent": "A draft must never be mistaken for a settled decision, so each proposal is written `status: draft` with a TODO contract, opening the same way `new` scaffolds a real requirement so promoting it later needs no reshaping. Ids are path-aware, so two files sharing a basename in different folders never collide.",
      "contract": [
        "`draft` proposes one `requirements/DRAFT-*.md` per remaining file.",
        "Every proposal carries `status: draft` and a TODO body. It captures observed behavior, and never canonizes intent or correctness.",
        "A proposal's Contract section opens with \"Every bullet below is binding.\", matching what `new` scaffolds, so promoting a draft needs no reshaping.",
        "`draft` creates the requirements directory if it is absent.",
        "Draft ids are path-aware, so two files sharing a basename do not collide."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — draft writes one proposal file per remaining source file Given  two untagged, unignored source files When   `draft` runs Then   it writes two distinct `requirements/DRAFT-*.md` files",
        "CASE-2 — — a fresh proposal is marked draft with a TODO contract Given  an untagged source file When   `draft` runs Then   the proposal's frontmatter reads `status: draft` and its Contract is a TODO placeholder",
        "CASE-3 — — a proposal's Contract opens exactly like new's scaffold Given  a fresh `draft` proposal and a requirement scaffolded by `new` When   both Contract sections are compared Then   both open with \"Every bullet below is binding.\"",
        "CASE-4 — — draft creates a missing requirements directory Given  a repo with no `requirements/` directory yet When   `draft` runs Then   `requirements/` is created and the proposals are written into it",
        "CASE-5 — — draft ids stay unique across same-named files in different folders Given  `src/a/util.py` and `src/b/util.py`, both untagged When   `draft` runs Then   it writes two proposals with distinct ids, one per path --------------------"
      ],
      "accept": "CASE-1 — draft writes one proposal file per remaining source file\n  Given  two untagged, unignored source files\n  When   `draft` runs\n  Then   it writes two distinct `requirements/DRAFT-*.md` files\n\nCASE-2 — a fresh proposal is marked draft with a TODO contract\n  Given  an untagged source file\n  When   `draft` runs\n  Then   the proposal's frontmatter reads `status: draft` and its Contract is a TODO placeholder\n\nCASE-3 — a proposal's Contract opens exactly like new's scaffold\n  Given  a fresh `draft` proposal and a requirement scaffolded by `new`\n  When   both Contract sections are compared\n  Then   both open with \"Every bullet below is binding.\"\n\nCASE-4 — draft creates a missing requirements directory\n  Given  a repo with no `requirements/` directory yet\n  When   `draft` runs\n  Then   `requirements/` is created and the proposals are written into it\n\nCASE-5 — draft ids stay unique across same-named files in different folders\n  Given  `src/a/util.py` and `src/b/util.py`, both untagged\n  When   `draft` runs\n  Then   it writes two proposals with distinct ids, one per path\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-EXTRACT-008"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3315"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3382"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1070"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9436"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Scrierea unei singure propuneri de draft per fișier",
          "intent": "Un draft nu trebuie niciodată confundat cu o decizie stabilită, deci fiecare propunere este scrisă cu `status: draft` și un contract TODO, deschizându-se la fel cum `new` schelează o cerință reală, astfel încât promovarea ei ulterioară nu necesită reformare. Id-urile sunt conștiente de cale, astfel încât două fișiere care împart același nume de bază în foldere diferite nu se ciocnesc niciodată.",
          "contract": "> Un draft nu trebuie niciodată confundat cu o decizie stabilită, deci fiecare propunere este\n> scrisă cu `status: draft` și un contract TODO, deschizându-se la fel cum `new` schelează o\n> cerință reală, astfel încât promovarea ei ulterioară nu necesită reformare. Id-urile sunt\n> conștiente de cale, astfel încât două fișiere care împart același nume de bază în foldere\n> diferite nu se ciocnesc niciodată.\n\nFiecare bullet de mai jos este obligatoriu.\n- `draft` propune un `requirements/DRAFT-*.md` pentru fiecare fișier rămas.\n- Fiecare propunere poartă `status: draft` și un corp TODO. Captează comportamentul\n  observat, și nu canonizează niciodată intenția sau corectitudinea.\n- Secțiunea Contract a unei propuneri se deschide cu \"Every bullet below is binding.\",\n  la fel ca ceea ce schelează `new`, astfel încât promovarea unui draft nu necesită reformare.\n- `draft` creează directorul requirements dacă acesta lipsește.\n- Id-urile de draft sunt conștiente de cale, astfel încât două fișiere care împart un nume de bază nu se ciocnesc.",
          "acceptance": "CASE-1 — draft scrie o propunere per fișier sursă rămas\n  Given  două fișiere sursă netagate, neignorate\n  When   `draft` rulează\n  Then   scrie două fișiere `requirements/DRAFT-*.md` distincte\n\nCASE-2 — o propunere proaspătă este marcată draft cu un contract TODO\n  Given  un fișier sursă netagat\n  When   `draft` rulează\n  Then   frontmatter-ul propunerii citește `status: draft` iar Contract-ul său este un placeholder TODO\n\nCASE-3 — Contract-ul unei propuneri se deschide exact ca schela lui new\n  Given  o propunere `draft` proaspătă și o cerință schelată de `new`\n  When   ambele secțiuni Contract sunt comparate\n  Then   ambele se deschid cu \"Every bullet below is binding.\"\n\nCASE-4 — draft creează un director requirements lipsă\n  Given  un repo fără niciun director `requirements/` încă\n  When   `draft` rulează\n  Then   `requirements/` este creat și propunerile sunt scrise în el\n\nCASE-5 — id-urile de draft rămân unice între fișiere cu același nume din foldere diferite\n  Given  `src/a/util.py` și `src/b/util.py`, ambele netagate\n  When   `draft` rulează\n  Then   scrie două propuneri cu id-uri distincte, unul per cale\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-EXTRACT-851",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Scoring risk and capturing an authoring hint",
      "intent": "Not every draft needs the same scrutiny, so `draft` scores each file cheaply from `TODO`/`FIXME`/`HACK`/`XXX` markers, suppressions and size, routing riskier files to `REVIEW`. When the language has a parser, it also records the file's docstring and top-level signatures under WHERE as a hint for the human — never as a Contract line, since the Contract stays a TODO until a person writes it.",
      "contract": [
        "`draft` assigns a cheap risk score from `TODO`/`FIXME`/`HACK`/`XXX` markers, suppressions and file size.",
        "`draft` routes a score of 2 or more to `REVIEW`, and any lower score to `auto-baseline`.",
        "Re-running `draft` never overwrites an existing draft.",
        "A code proposal's WHERE section lists the file's observed surface — the module docstring's first line and its top-level signatures — when the language has a parser.",
        "That surface is an authoring hint under WHERE, never a Contract line. The Contract stays a TODO until a human writes it."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a marker-heavy file scores as higher risk Given  an untagged file containing `TODO`, `FIXME` and a suppression comment When   `draft` runs Then   its proposal carries a higher risk score than a clean file of the same size",
        "CASE-2 — — the risk score routes the proposal's status hint Given  one file scoring 2 and one file scoring 0 When   `draft` runs Then   the first proposal is flagged `REVIEW` and the second `auto-baseline`",
        "CASE-3 — — re-running draft leaves an existing proposal untouched Given  a `DRAFT-*.md` written by an earlier `draft` run, since hand-edited When   `draft` runs again over the same file Then   the existing proposal's content is unchanged",
        "CASE-4 — — a Python proposal's WHERE names the docstring and top-level signatures Given  an untagged `.py` file with a module docstring and two top-level functions When   `draft` runs Then   the proposal's WHERE section names the docstring's first line and both function signatures",
        "CASE-5 — — the extracted surface never leaks into the Contract Given  the same `.py` file's proposal, whose WHERE lists two function signatures When   the proposal's Contract section is inspected Then   it is still the TODO placeholder, unchanged by what WHERE captured"
      ],
      "accept": "CASE-1 — a marker-heavy file scores as higher risk\n  Given  an untagged file containing `TODO`, `FIXME` and a suppression comment\n  When   `draft` runs\n  Then   its proposal carries a higher risk score than a clean file of the same size\n\nCASE-2 — the risk score routes the proposal's status hint\n  Given  one file scoring 2 and one file scoring 0\n  When   `draft` runs\n  Then   the first proposal is flagged `REVIEW` and the second `auto-baseline`\n\nCASE-3 — re-running draft leaves an existing proposal untouched\n  Given  a `DRAFT-*.md` written by an earlier `draft` run, since hand-edited\n  When   `draft` runs again over the same file\n  Then   the existing proposal's content is unchanged\n\nCASE-4 — a Python proposal's WHERE names the docstring and top-level signatures\n  Given  an untagged `.py` file with a module docstring and two top-level functions\n  When   `draft` runs\n  Then   the proposal's WHERE section names the docstring's first line and both function signatures\n\nCASE-5 — the extracted surface never leaks into the Contract\n  Given  the same `.py` file's proposal, whose WHERE lists two function signatures\n  When   the proposal's Contract section is inspected\n  Then   it is still the TODO placeholder, unchanged by what WHERE captured",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-EXTRACT-008"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3496"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6883"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9436"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Scorarea riscului și captarea unui indiciu de redactare",
          "intent": "Nu fiecare draft are nevoie de aceeași scrutin, deci `draft` scorează ieftin fiecare fișier pe baza marcajelor `TODO`/`FIXME`/`HACK`/`XXX`, suppresiilor și dimensiunii, direcționând fișierele mai riscante spre `REVIEW`. Când limbajul are un parser, înregistrează de asemenea docstring-ul fișierului și semnăturile de nivel superior sub WHERE ca indiciu pentru om — niciodată ca linie de Contract, deoarece Contract-ul rămâne un TODO până când o persoană îl scrie.",
          "contract": "> Nu fiecare draft are nevoie de aceeași scrutin, deci `draft` scorează ieftin fiecare fișier pe\n> baza marcajelor `TODO`/`FIXME`/`HACK`/`XXX`, suppresiilor și dimensiunii, direcționând\n> fișierele mai riscante spre `REVIEW`. Când limbajul are un parser, înregistrează de asemenea\n> docstring-ul fișierului și semnăturile de nivel superior sub WHERE ca indiciu pentru om — niciodată\n> ca linie de Contract, deoarece Contract-ul rămâne un TODO până când o persoană îl scrie.\n\nFiecare bullet de mai jos este obligatoriu.\n- `draft` atribuie un scor de risc ieftin pe baza marcajelor `TODO`/`FIXME`/`HACK`/`XXX`,\n  suppresiilor și dimensiunii fișierului.\n- `draft` direcționează un scor de 2 sau mai mult spre `REVIEW`, și orice scor mai mic spre\n  `auto-baseline`.\n- Rerularea `draft` nu suprascrie niciodată un draft existent.\n- Secțiunea WHERE a unei propuneri de cod listează suprafața observată a fișierului — prima\n  linie a docstring-ului modulului și semnăturile sale de nivel superior — când limbajul are un parser.\n- Acea suprafață este un indiciu de redactare sub WHERE, niciodată o linie de Contract. Contract-ul\n  rămâne un TODO până când un om îl scrie.",
          "acceptance": "CASE-1 — un fișier plin de marcaje are un scor de risc mai mare\n  Given  un fișier netagat conținând `TODO`, `FIXME` și un comentariu de suppresie\n  When   `draft` rulează\n  Then   propunerea sa poartă un scor de risc mai mare decât un fișier curat de aceeași dimensiune\n\nCASE-2 — scorul de risc direcționează indiciul de status al propunerii\n  Given  un fișier cu scor 2 și un fișier cu scor 0\n  When   `draft` rulează\n  Then   prima propunere este marcată `REVIEW` iar a doua `auto-baseline`\n\nCASE-3 — rerularea draft lasă o propunere existentă neatinsă\n  Given  un `DRAFT-*.md` scris de o rulare `draft` anterioară, de atunci editat manual\n  When   `draft` rulează din nou peste același fișier\n  Then   conținutul propunerii existente rămâne neschimbat\n\nCASE-4 — WHERE-ul unei propuneri Python numește docstring-ul și semnăturile de nivel superior\n  Given  un fișier `.py` netagat cu un docstring de modul și două funcții de nivel superior\n  When   `draft` rulează\n  Then   secțiunea WHERE a propunerii numește prima linie a docstring-ului și ambele semnături de funcție\n\nCASE-5 — suprafața extrasă nu se scurge niciodată în Contract\n  Given  propunerea aceluiași fișier `.py`, al cărei WHERE listează două semnături de funcție\n  When   secțiunea Contract a propunerii este inspectată\n  Then   este tot placeholder-ul TODO, neschimbat de ceea ce a captat WHERE"
        }
      }
    },
    {
      "id": "ARCH-FANOUT-052",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Hierarchy breadth",
      "intent": "A level is only useful if it groups. One requirement holding fifty children is a bucket wearing a level's name; one holding two is a level that saves nobody any reading. This reports a parent whose child count leaves the useful band, so a hierarchy that has quietly flattened out gets looked at again.",
      "contract": [
        "The `fan-out` check counts, per requirement, how many requirements declare `satisfies:` it, and warns when a parent's child count leaves the useful band for its level (`system`: ceiling 10, `architecture`: ceiling 30, undeclared level: 5–20). [[REQ-FANOUT-852]]"
      ],
      "verify": [],
      "notes": [
        "The band is read against the `satisfies:` graph on purpose. Measured on this corpus, the `depends_on` out-degree runs 0 to 3, so a 5-to-20 band read against that axis would flag every requirement — a check that fires on correct work gets ignored (ADR-0002).",
        "5 and 20 are an engineering heuristic, not a figure from any standard. No requirements standard specifies decomposition breadth.",
        "The leaf exemption is what keeps the check silent on a corpus that has not adopted `satisfies:` at all: with no edges, nothing is a parent and nothing is reported.",
        "The check measures breadth, never correctness of the grouping. A parent with twelve children that belong together and one with twelve that do not are indistinguishable to it."
      ],
      "current_impl": [
        "`LINT_FANOUT_BANDS` (per-level ceilings), `LINT_FANOUT_MIN`/`LINT_FANOUT_MAX` (the fallback band for a parent with no `level:`) and the `fan-out` block in `lint_requirement`, fed by the `kids` count `cmd_lint` builds from every requirement's `satisfies:` list.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  an `architecture` requirement satisfied by three others When   `lint` runs Then   no `fan-out` finding is reported for it, because that level carries no floor",
        "CASE-2 — Given  a requirement satisfied by eight others When   `lint` runs Then   no `fan-out` finding is reported for it",
        "CASE-3 — Given  an `architecture` requirement satisfied by thirty-two others When   `lint` runs Then   one `fan-out` finding names it as over its ceiling, and the exit code is unchanged",
        "CASE-4 — Given  a requirement that nothing satisfies When   `lint` runs Then   no `fan-out` finding is reported for it",
        "CASE-5 — Given  a corpus whose requirements all depend on one another but declare no `satisfies:` When   `lint` runs Then   no `fan-out` finding is reported at all",
        "CASE-6 — Given  a `system` requirement satisfied by eleven others When   `lint` runs Then   one `fan-out` finding names it as over its ceiling of ten"
      ],
      "accept": "CASE-1\n  Given  an `architecture` requirement satisfied by three others\n  When   `lint` runs\n  Then   no `fan-out` finding is reported for it, because that level carries no floor\nCASE-2\n  Given  a requirement satisfied by eight others\n  When   `lint` runs\n  Then   no `fan-out` finding is reported for it\nCASE-3\n  Given  an `architecture` requirement satisfied by thirty-two others\n  When   `lint` runs\n  Then   one `fan-out` finding names it as over its ceiling, and the exit code is unchanged\nCASE-4\n  Given  a requirement that nothing satisfies\n  When   `lint` runs\n  Then   no `fan-out` finding is reported for it\nCASE-5\n  Given  a corpus whose requirements all depend on one another but declare no `satisfies:`\n  When   `lint` runs\n  Then   no `fan-out` finding is reported at all\nCASE-6\n  Given  a `system` requirement satisfied by eleven others\n  When   `lint` runs\n  Then   one `fan-out` finding names it as over its ceiling of ten",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001",
        "ARCH-LINT-014",
        "ARCH-LEVEL-051"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-VMODEL-107"
      ],
      "satisfied_by": [
        "REQ-FANOUT-852"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4591"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4981"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7272"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9236"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "could-have",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Lățimea ierarhiei",
          "intent": "Un nivel este util doar dacă grupează. O cerință care conține cincizeci de copii este o găleată care poartă numele unui nivel; una care conține doi este un nivel care nu economisește nimănui vreo citire. Aceasta raportează un părinte al cărui număr de copii iese din banda utilă, astfel încât o ierarhie care s-a aplatizat în tăcere este privită din nou.",
          "contract": "> Un nivel este util doar dacă grupează. O cerință care conține cincizeci de copii este o\n> găleată care poartă numele unui nivel; una care conține doi este un nivel care nu\n> economisește nimănui vreo citire. Aceasta raportează un părinte al cărui număr de copii\n> iese din banda utilă, astfel încât o ierarhie care s-a aplatizat în tăcere este privită\n> din nou.\n\nFiecare punct de mai jos este obligatoriu.\n- Verificarea `fan-out` numără, per cerință, câte cerințe declară `satisfies:` spre ea, și avertizează atunci când numărul de copii al unui părinte iese din banda utilă pentru nivelul său (`system`: plafon 10, `architecture`: plafon 30, nivel nedeclarat: 5–20). [[REQ-FANOUT-852]]",
          "acceptance": "CASE-1\n  Given  o cerință `architecture` satisfăcută de alte trei\n  When   `lint` rulează\n  Then   nu se raportează nicio constatare `fan-out` pentru ea, deoarece acel nivel nu are prag minim\nCASE-2\n  Given  o cerință satisfăcută de alte opt\n  When   `lint` rulează\n  Then   nu se raportează nicio constatare `fan-out` pentru ea\nCASE-3\n  Given  o cerință `architecture` satisfăcută de alte treizeci și două\n  When   `lint` rulează\n  Then   o constatare `fan-out` o numește ca fiind peste plafonul ei, iar codul de ieșire rămâne neschimbat\nCASE-4\n  Given  o cerință pe care nimic nu o satisface\n  When   `lint` rulează\n  Then   nu se raportează nicio constatare `fan-out` pentru ea\nCASE-5\n  Given  un corpus ale cărui cerințe depind toate una de alta dar nu declară niciun `satisfies:`\n  When   `lint` rulează\n  Then   nu se raportează nicio constatare `fan-out`\nCASE-6\n  Given  o cerință `system` satisfăcută de alte unsprezece\n  When   `lint` rulează\n  Then   o constatare `fan-out` o numește ca fiind peste plafonul ei de zece"
        }
      }
    },
    {
      "id": "REQ-FANOUT-852",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Counting children and reporting an out-of-band parent",
      "intent": "A level only earns its name if it groups: one requirement with fifty children is a bucket wearing a level's name, and one with two saves nobody any reading. `fan-out` counts each requirement's children along the `satisfies:` graph (never `depends_on`, whose out-degree runs far lower on this corpus) and warns once a parent's count leaves the useful band for its declared level.",
      "contract": [
        "The `fan-out` check counts, per requirement, how many requirements declare `satisfies:` it.",
        "The count reads the `satisfies:` graph only, never `depends_on`.",
        "A requirement with no children is skipped, because it is not a parent.",
        "The `fan-out` check warns when a parent carries more children than its ceiling.",
        "A `system` parent's ceiling is ten; an `architecture` parent's is thirty.",
        "A parent declaring no `level:` keeps the older uniform band, five to twenty, so a repo that never adopts the level axis sees what it saw before.",
        "The check reports no floor at either declared level.",
        "The `fan-out` check is warn-only and never changes the gate's exit code.",
        "`lint_exempt: [fan-out]` silences the check for one requirement."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the finding names the exact child count Given  three requirements each declaring `satisfies: [REQ-P-001]` When   `lint_requirement` runs with `children=3` Then   the `fan-out` finding's detail reads \"3 requirement(s) satisfy this one\"",
        "CASE-2 — — depends_on edges never feed the fan-out count Given  a corpus where every requirement declares `depends_on` on one hub but no `satisfies:` When   `lint` runs Then   the hub gets no `fan-out` finding, because `depends_on` out-degree is never counted",
        "CASE-3 — — a leaf with zero children is never a fan-out finding Given  a requirement that nothing declares `satisfies:` on (`children=0`) When   `lint_requirement` runs Then   no `fan-out` finding appears for it",
        "CASE-4 — — crossing the ceiling flips the finding on Given  one `architecture` parent with 30 children (at its ceiling) and another with 31 When   `lint` runs on each Then   the 30-child parent gets no `fan-out` finding and the 31-child parent gets one, with the gate's exit code unchanged either way",
        "CASE-5 — — a thin parent at a declared level is not reported Given  one `architecture` parent with 3 children and one `system` parent with 2 When   `lint` runs on each Then   neither gets a `fan-out` finding, because neither declared level carries a floor",
        "CASE-6 — — a fan-out finding does not fail the run Given  an `architecture` parent with 32 children, over its ceiling When   `lint` runs without `--strict` Then   one `fan-out` finding is printed and the exit code is zero",
        "CASE-7 — — lint_exempt: [fan-out] suppresses the finding Given  a parent with 3 children and `lint_exempt: [fan-out]` in its frontmatter When   `lint_requirement` runs Then   no `fan-out` finding is reported for that requirement"
      ],
      "accept": "CASE-1 — the finding names the exact child count\n  Given  three requirements each declaring `satisfies: [REQ-P-001]`\n  When   `lint_requirement` runs with `children=3`\n  Then   the `fan-out` finding's detail reads \"3 requirement(s) satisfy this one\"\n\nCASE-2 — depends_on edges never feed the fan-out count\n  Given  a corpus where every requirement declares `depends_on` on one hub but no `satisfies:`\n  When   `lint` runs\n  Then   the hub gets no `fan-out` finding, because `depends_on` out-degree is never counted\n\nCASE-3 — a leaf with zero children is never a fan-out finding\n  Given  a requirement that nothing declares `satisfies:` on (`children=0`)\n  When   `lint_requirement` runs\n  Then   no `fan-out` finding appears for it\n\nCASE-4 — crossing the ceiling flips the finding on\n  Given  one `architecture` parent with 30 children (at its ceiling) and another with 31\n  When   `lint` runs on each\n  Then   the 30-child parent gets no `fan-out` finding and the 31-child parent gets one,\n         with the gate's exit code unchanged either way\n\nCASE-5 — a thin parent at a declared level is not reported\n  Given  one `architecture` parent with 3 children and one `system` parent with 2\n  When   `lint` runs on each\n  Then   neither gets a `fan-out` finding, because neither declared level carries a floor\n\nCASE-6 — a fan-out finding does not fail the run\n  Given  an `architecture` parent with 32 children, over its ceiling\n  When   `lint` runs without `--strict`\n  Then   one `fan-out` finding is printed and the exit code is zero\n\nCASE-7 — lint_exempt: [fan-out] suppresses the finding\n  Given  a parent with 3 children and `lint_exempt: [fan-out]` in its frontmatter\n  When   `lint_requirement` runs\n  Then   no `fan-out` finding is reported for that requirement",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-FANOUT-052"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:195"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7272"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9236"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 7,
      "covered": 7,
      "i18n": {
        "ro": {
          "title": "Numărarea copiilor și raportarea unui părinte în afara benzii",
          "intent": "Un nivel își merită numele doar dacă grupează: o cerință cu cincizeci de copii este o găleată care poartă numele unui nivel, iar una cu doi nu economisește nimănui vreo lectură. `fan-out` numără copiii fiecărei cerințe de-a lungul graficului `satisfies:` (niciodată `depends_on`, al cărui out-degree este mult mai mic pe acest corpus) și avertizează odată ce numărul unui părinte iese din banda utilă pentru nivelul său declarat.",
          "contract": "> Un nivel își merită numele doar dacă grupează: o cerință cu cincizeci de copii este o\n> găleată care poartă numele unui nivel, iar una cu doi nu economisește nimănui vreo\n> lectură. `fan-out` numără copiii fiecărei cerințe de-a lungul graficului `satisfies:`\n> (niciodată `depends_on`, al cărui out-degree este mult mai mic pe acest corpus) și\n> avertizează odată ce numărul unui părinte iese din banda utilă pentru nivelul său\n> declarat.\n\nFiecare bullet de mai jos este obligatoriu.\n- Verificarea `fan-out` numără, per cerință, câte cerințe declară `satisfies:` pentru ea.\n- Numărătoarea citește doar graficul `satisfies:`, niciodată `depends_on`.\n- O cerință fără copii este sărită, pentru că nu este un părinte.\n- Verificarea `fan-out` avertizează când un părinte poartă mai mulți copii decât plafonul său.\n- Plafonul unui părinte `system` este zece; cel al unui părinte `architecture` este treizeci.\n- Un părinte care nu declară niciun `level:` păstrează banda uniformă mai veche, cinci până la\n  douăzeci, astfel încât un repo care nu adoptă niciodată axa de nivel vede ce vedea înainte.\n- Verificarea nu raportează niciun prag minim la niciun nivel declarat.\n- Verificarea `fan-out` este doar warn-only și nu schimbă niciodată codul de ieșire al gate-ului.\n- `lint_exempt: [fan-out]` amuțește verificarea pentru o cerință.",
          "acceptance": "CASE-1 — constatarea numește numărul exact de copii\n  Given  trei cerințe care declară fiecare `satisfies: [REQ-P-001]`\n  When   `lint_requirement` rulează cu `children=3`\n  Then   detaliul constatării `fan-out` citește \"3 requirement(s) satisfy this one\"\n\nCASE-2 — muchiile depends_on nu alimentează niciodată numărătoarea fan-out\n  Given  un corpus în care fiecare cerință declară `depends_on` pe un hub dar niciun `satisfies:`\n  When   `lint` rulează\n  Then   hub-ul nu primește nicio constatare `fan-out`, pentru că out-degree-ul `depends_on` nu este niciodată numărat\n\nCASE-3 — o frunză cu zero copii nu este niciodată o constatare fan-out\n  Given  o cerință pe care nimeni nu declară `satisfies:` (`children=0`)\n  When   `lint_requirement` rulează\n  Then   nicio constatare `fan-out` nu apare pentru ea\n\nCASE-4 — depășirea plafonului activează constatarea\n  Given  un părinte `architecture` cu 30 de copii (la plafonul său) și altul cu 31\n  When   `lint` rulează pe fiecare\n  Then   părintele cu 30 de copii nu primește nicio constatare `fan-out` iar cel cu 31 primește una,\n         cu codul de ieșire al gate-ului neschimbat în ambele cazuri\n\nCASE-5 — un părinte subțire la un nivel declarat nu este raportat\n  Given  un părinte `architecture` cu 3 copii și un părinte `system` cu 2\n  When   `lint` rulează pe fiecare\n  Then   niciunul nu primește o constatare `fan-out`, pentru că niciun nivel declarat nu poartă un prag minim\n\nCASE-6 — o constatare fan-out nu face rularea să eșueze\n  Given  un părinte `architecture` cu 32 de copii, peste plafonul său\n  When   `lint` rulează fără `--strict`\n  Then   este afișată o constatare `fan-out` și codul de ieșire este zero\n\nCASE-7 — lint_exempt: [fan-out] suprimă constatarea\n  Given  un părinte cu 3 copii și `lint_exempt: [fan-out]` în frontmatter-ul său\n  When   `lint_requirement` rulează\n  Then   nicio constatare `fan-out` nu este raportată pentru acea cerință"
        }
      }
    },
    {
      "id": "ARCH-FINDINGS-010",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Open-findings report",
      "intent": "Every requirement file can carry open questions the author still wants a human to double-check. Scattered across dozens of files, those questions are easy to forget. This command gathers them all into one review list (`_findings.md`) — optionally sorted by an AI helper into real bugs, judgement calls, and false alarms. Without it, the open questions stay buried and never get answered.",
      "contract": [
        "`sync` collects the bullet items under each requirement's `## Verify intent` section. [[REQ-FINDINGS-853]]",
        "In raw mode, `sync` groups the collected findings by requirement and writes the raw report. [[REQ-FINDINGS-854]]",
        "When a `_findings_triage.json` sidecar exists and raw mode is off, `sync` renders a classified view ordered by severity. [[REQ-FINDINGS-855]]",
        "`sync` is deterministic and stdlib-only; `map` and `gate` fold its output in without ever classifying a finding themselves. [[REQ-FINDINGS-856]]"
      ],
      "verify": [],
      "notes": [
        "Classification itself is produced out-of-band by the AI triage pass and supplied through the sidecar; the command only renders what the sidecar already decided.",
        "Staleness is detected by comparing item *counts*, not content — a sidecar that is stale but happens to match the raw count is not flagged.",
        "The sidecar is rendered as the source of truth when present; newly added verify-intent items do not appear in the classified view until the AI triage pass regenerates the sidecar (use `--raw` to see the current raw set)."
      ],
      "current_impl": [
        "`collect_findings(reqs)` pulls each requirement's `## Verify intent` bullets via `_bullets`, dropping the \"None\" placeholder, and returns `(id, title, items)` groups. `cmd_findings` reads an optional `_findings_triage.json`; with valid items it calls `_render_findings_triaged` (buckets by classification, sorts REAL_BUG by `_SEV_RANK`, emits a staleness note when raw≠triaged), otherwise `_render_findings_raw`. `cmd_check` calls `collect_findings` to print the advisory count before its summary.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  two requirements, one with two Verify-intent bullets and one with only the \"None —\" placeholder When   `sync` runs in raw mode Then   `_findings.md` lists the two bullets under the first requirement and the summary reports \"2 open finding(s) across 1 requirement(s)\"",
        "CASE-2 — Given  a `_findings_triage.json` sidecar classifying one item REAL_BUG (high) and one USER_DECISION When   `sync` runs without `--raw` Then   the \"Confirmed bugs\" section precedes \"Your call\", the bug shows a HIGH badge with its location, and the summary reports \"1 confirmed bug(s)\"",
        "CASE-3 — Given  a sidecar is present When   `sync` runs with `--raw` Then   the sidecar is ignored and the raw grouped list is written",
        "CASE-4 — Given  three raw verify-intent items but only one triaged item in the sidecar When   `sync` runs Then   `_findings.md` carries a staleness warning advising a re-run of the triage pass",
        "CASE-5 — Given  at least one requirement with an open verify-intent item When   `gate` runs Then   it prints an advisory line naming the open-findings count without affecting the error count",
        "CASE-6 — Given  a committed `_findings.md`, and a requirement that then gains a verify-intent item When   `map` runs Then   `_findings.md` carries the new item, and with no `_findings.md` present `map` creates none",
        "CASE-7 — Given  a committed `_findings.md` that no longer matches the requirements When   `map --check` runs Then   it names `_findings.md` as stale and exits non-zero"
      ],
      "accept": "CASE-1\n  Given  two requirements, one with two Verify-intent bullets and one with only the \"None —\" placeholder\n  When   `sync` runs in raw mode\n  Then   `_findings.md` lists the two bullets under the first requirement and the summary reports \"2 open finding(s) across 1 requirement(s)\"\n\nCASE-2\n  Given  a `_findings_triage.json` sidecar classifying one item REAL_BUG (high) and one USER_DECISION\n  When   `sync` runs without `--raw`\n  Then   the \"Confirmed bugs\" section precedes \"Your call\", the bug shows a HIGH badge with its location, and the summary reports \"1 confirmed bug(s)\"\n\nCASE-3\n  Given  a sidecar is present\n  When   `sync` runs with `--raw`\n  Then   the sidecar is ignored and the raw grouped list is written\n\nCASE-4\n  Given  three raw verify-intent items but only one triaged item in the sidecar\n  When   `sync` runs\n  Then   `_findings.md` carries a staleness warning advising a re-run of the triage pass\n\nCASE-5\n  Given  at least one requirement with an open verify-intent item\n  When   `gate` runs\n  Then   it prints an advisory line naming the open-findings count without affecting the error count\n\nCASE-6\n  Given  a committed `_findings.md`, and a requirement that then gains a verify-intent item\n  When   `map` runs\n  Then   `_findings.md` carries the new item, and with no `_findings.md` present `map` creates none\n\nCASE-7\n  Given  a committed `_findings.md` that no longer matches the requirements\n  When   `map --check` runs\n  Then   it names `_findings.md` as stale and exits non-zero",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-REPORT-105"
      ],
      "satisfied_by": [
        "REQ-FINDINGS-853",
        "REQ-FINDINGS-854",
        "REQ-FINDINGS-855",
        "REQ-FINDINGS-856"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3807"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3894"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3920"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4230"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6181"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6393"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1274"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1915"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6568"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8880"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.08",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Raport de constatări deschise",
          "intent": "Fiecare fișier de cerință poate purta întrebări deschise pe care autorul dorește ca un om să le verifice încă o dată. Împrăștiate în zeci de fișiere, acele întrebări sunt ușor de uitat. Această comandă le adună pe toate într-o singură listă de revizuire (`_findings.md`) — sortate opțional de un asistent AI în bug-uri reale, decizii de judecată și alarme false. Fără aceasta, întrebările deschise rămân îngropate și nu primesc niciodată răspuns.",
          "contract": "> Fiecare fișier de cerință poate purta întrebări deschise pe care autorul dorește ca\n> un om să le verifice încă o dată. Împrăștiate în zeci de fișiere, acele întrebări sunt\n> ușor de uitat. Această comandă le adună pe toate într-o singură listă de revizuire\n> (`_findings.md`) — sortate opțional de un asistent AI în bug-uri reale, decizii de\n> judecată și alarme false. Fără aceasta, întrebările deschise rămân îngropate și nu\n> primesc niciodată răspuns.\n\nFiecare punct de mai jos este obligatoriu.\n- `sync` colectează elementele-punct de sub secțiunea `## Verify intent` a fiecărei cerințe. [[REQ-FINDINGS-853]]\n- În modul raw, `sync` grupează constatările colectate pe cerință și scrie raportul brut. [[REQ-FINDINGS-854]]\n- Când există un sidecar `_findings_triage.json` iar modul raw este dezactivat, `sync` randează o vizualizare clasificată ordonată după severitate. [[REQ-FINDINGS-855]]\n- `sync` este determinist și doar-stdlib; `map` și `gate` pliază ieșirea sa fără a clasifica ele însele vreodată o constatare. [[REQ-FINDINGS-856]]",
          "acceptance": "CASE-1\n  Given  două cerințe, una cu două puncte Verify-intent și una doar cu placeholder-ul \"None —\"\n  When   `sync` rulează în modul raw\n  Then   `_findings.md` listează cele două puncte sub prima cerință, iar rezumatul raportează \"2 open finding(s) across 1 requirement(s)\"\n\nCASE-2\n  Given  un sidecar `_findings_triage.json` care clasifică un element REAL_BUG (high) și unul USER_DECISION\n  When   `sync` rulează fără `--raw`\n  Then   secțiunea \"Confirmed bugs\" precede \"Your call\", bug-ul afișează o etichetă HIGH cu locația sa, iar rezumatul raportează \"1 confirmed bug(s)\"\n\nCASE-3\n  Given  un sidecar este prezent\n  When   `sync` rulează cu `--raw`\n  Then   sidecar-ul este ignorat iar lista brută grupată este scrisă\n\nCASE-4\n  Given  trei elemente verify-intent brute dar doar un element triat în sidecar\n  When   `sync` rulează\n  Then   `_findings.md` poartă un avertisment de învechire care recomandă o rerulare a pasului de triaj\n\nCASE-5\n  Given  cel puțin o cerință cu un element verify-intent deschis\n  When   `gate` rulează\n  Then   afișează o linie consultativă care numește numărul de constatări deschise fără a afecta numărul de erori\n\nCASE-6\n  Given  un `_findings.md` commis, și o cerință care ulterior primește un element verify-intent\n  When   `map` rulează\n  Then   `_findings.md` poartă noul element, iar fără niciun `_findings.md` prezent `map` nu creează niciunul\n\nCASE-7\n  Given  un `_findings.md` commis care nu mai corespunde cerințelor\n  When   `map --check` rulează\n  Then   numește `_findings.md` ca fiind învechit și iese cu cod diferit de zero"
        }
      }
    },
    {
      "id": "REQ-FINDINGS-853",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Collecting open verify-intent bullets",
      "intent": "`sync` is the only place that reads a requirement's `## Verify intent` bullets back out — everywhere else they are write-only notes an author leaves for later. Without this collection step, an open question buried in file #40 of 60 never surfaces again.",
      "contract": [
        "`sync` scans every requirement and collects the bullet items under each one's `## Verify intent` section.",
        "`sync` writes them into a single `_findings.md` in the requirements directory.",
        "`sync` excludes the \"None — …\" placeholder bullet. A requirement that recorded no open question therefore contributes nothing.",
        "`sync` excludes anything below a line in that section declaring itself a non-binding authoring hint — the scaffold's own list of a source file's headings is context, not a question. Every verify-intent reader goes through the one collection step, so the viewer, the CLI and the gate summary cannot report different counts."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — findings collects verify-intent bullets from every requirement Given  two requirements, each with one non-placeholder Verify-intent bullet When   `sync` runs Then   `_findings.md` contains both bullets, one from each requirement",
        "CASE-2 — — findings writes its report to a single file Given  a requirement with one open Verify-intent bullet When   `sync` runs Then   the requirements directory gains one new file, `_findings.md`, holding that bullet, and no other file appears",
        "CASE-3 — — findings drops the None placeholder bullet Given  a requirement whose Verify-intent section holds only the \"None — …\" bullet When   `sync` runs Then   that requirement contributes no bullets to `_findings.md`",
        "CASE-4 — — findings drops the scaffold's own authoring hint Given  a requirement whose Verify-intent section holds one open question followed by a line reading \"authoring hint, not the contract\" and a list of source headings When   `sync` runs Then   only the open question is reported, and none of the headings below that line are --------------------"
      ],
      "accept": "CASE-1 — findings collects verify-intent bullets from every requirement\n  Given  two requirements, each with one non-placeholder Verify-intent bullet\n  When   `sync` runs\n  Then   `_findings.md` contains both bullets, one from each requirement\n\nCASE-2 — findings writes its report to a single file\n  Given  a requirement with one open Verify-intent bullet\n  When   `sync` runs\n  Then   the requirements directory gains one new file, `_findings.md`, holding that\n         bullet, and no other file appears\n\nCASE-3 — findings drops the None placeholder bullet\n  Given  a requirement whose Verify-intent section holds only the \"None — …\" bullet\n  When   `sync` runs\n  Then   that requirement contributes no bullets to `_findings.md`\n\nCASE-4 — findings drops the scaffold's own authoring hint\n  Given  a requirement whose Verify-intent section holds one open question followed by a\n         line reading \"authoring hint, not the contract\" and a list of source headings\n  When   `sync` runs\n  Then   only the open question is reported, and none of the headings below that line are\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-FINDINGS-010"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3807"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6393"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1274"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8880"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Colectarea bullet-urilor verify-intent deschise",
          "intent": "`sync` este singurul loc care citește înapoi bullet-urile `## Verify intent` ale unei cerințe — peste tot altundeva sunt note write-only pe care un autor le lasă pentru mai târziu. Fără acest pas de colectare, o întrebare deschisă îngropată în fișierul #40 din 60 nu mai iese niciodată la suprafață.",
          "contract": "> `sync` este singurul loc care citește înapoi bullet-urile `## Verify intent` ale unei\n> cerințe — peste tot altundeva sunt note write-only pe care un autor le lasă pentru mai\n> târziu. Fără acest pas de colectare, o întrebare deschisă îngropată în fișierul #40 din 60\n> nu mai iese niciodată la suprafață.\n\nFiecare bullet de mai jos este obligatoriu.\n- `sync` scanează fiecare cerință și colectează elementele bullet de sub secțiunea\n  `## Verify intent` a fiecăreia.\n- `sync` le scrie într-un singur `_findings.md` în directorul requirements.\n- `sync` exclude bullet-ul placeholder \"None — …\". O cerință care nu a înregistrat nicio\n  întrebare deschisă nu contribuie deci cu nimic.\n- `sync` exclude tot ce se află sub o linie din acea secțiune care se declară pe sine un\n  indiciu de redactare non-obligatoriu — lista proprie a schelei cu titlurile unui fișier\n  sursă este context, nu o întrebare. Fiecare cititor de verify-intent trece prin același\n  pas de colectare, astfel încât viewer-ul, CLI-ul și rezumatul gate-ului nu pot raporta\n  numere diferite.",
          "acceptance": "CASE-1 — findings colectează bullet-uri verify-intent din fiecare cerință\n  Given  două cerințe, fiecare cu un bullet Verify-intent non-placeholder\n  When   `sync` rulează\n  Then   `_findings.md` conține ambele bullet-uri, unul din fiecare cerință\n\nCASE-2 — findings își scrie raportul într-un singur fișier\n  Given  o cerință cu un bullet Verify-intent deschis\n  When   `sync` rulează\n  Then   directorul requirements câștigă un fișier nou, `_findings.md`, care conține acel\n         bullet, și niciun alt fișier nu apare\n\nCASE-3 — findings elimină bullet-ul placeholder None\n  Given  o cerință a cărei secțiune Verify-intent conține doar bullet-ul \"None — …\"\n  When   `sync` rulează\n  Then   acea cerință nu contribuie cu niciun bullet la `_findings.md`\n\nCASE-4 — findings elimină indiciul de redactare propriu al schelei\n  Given  o cerință a cărei secțiune Verify-intent conține o întrebare deschisă urmată de o\n         linie care citește \"authoring hint, not the contract\" și o listă de titluri sursă\n  When   `sync` rulează\n  Then   este raportată doar întrebarea deschisă, și niciunul dintre titlurile de sub acea linie nu\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-FINDINGS-854",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The raw findings report",
      "intent": "The raw report is the fallback view: it exists so `sync` produces a useful `_findings.md` even before any AI triage pass has run. It groups by requirement because that is how an author will go fix them — one file at a time.",
      "contract": [
        "In raw mode, `sync` groups the findings by requirement.",
        "Each group and the document header carry a count.",
        "With zero findings, `sync` still writes a well-formed file stating that none are open.",
        "With the raw flag set, `sync` ignores any sidecar and emits the raw grouped list."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — raw mode groups findings under their requirement Given  two requirements each carrying one open Verify-intent bullet When   `sync` runs in raw mode Then   `_findings.md` lists each bullet nested under its own requirement's heading",
        "CASE-2 — — raw report prints a count per group and in the header Given  a requirement with two open Verify-intent bullets When   `sync` runs in raw mode Then   the requirement's group heading and the document header both show the count 2",
        "CASE-3 — — findings writes a clean report when nothing is open Given  a corpus where every Verify-intent section holds only the \"None —\" placeholder When   `sync` runs Then   `_findings.md` is written and states that no findings are open",
        "CASE-4 — — --raw ignores a present triage sidecar Given  a `_findings_triage.json` sidecar and at least one open Verify-intent bullet When   `sync` runs with `--raw` Then   `_findings.md` shows the raw grouped list, not the sidecar's classified sections --------------------"
      ],
      "accept": "CASE-1 — raw mode groups findings under their requirement\n  Given  two requirements each carrying one open Verify-intent bullet\n  When   `sync` runs in raw mode\n  Then   `_findings.md` lists each bullet nested under its own requirement's heading\n\nCASE-2 — raw report prints a count per group and in the header\n  Given  a requirement with two open Verify-intent bullets\n  When   `sync` runs in raw mode\n  Then   the requirement's group heading and the document header both show the count 2\n\nCASE-3 — findings writes a clean report when nothing is open\n  Given  a corpus where every Verify-intent section holds only the \"None —\" placeholder\n  When   `sync` runs\n  Then   `_findings.md` is written and states that no findings are open\n\nCASE-4 — --raw ignores a present triage sidecar\n  Given  a `_findings_triage.json` sidecar and at least one open Verify-intent bullet\n  When   `sync` runs with `--raw`\n  Then   `_findings.md` shows the raw grouped list, not the sidecar's classified sections\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-FINDINGS-010"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3821"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3920"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1274"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Raportul findings brut",
          "intent": "Raportul brut este vizualizarea de rezervă: există pentru ca `sync` să producă un `_findings.md` util chiar înainte de a rula orice pas de triaj AI. Grupează pe cerință pentru că așa va merge un autor să le repare — câte un fișier o dată.",
          "contract": "> Raportul brut este vizualizarea de rezervă: există pentru ca `sync` să producă un\n> `_findings.md` util chiar înainte de a rula orice pas de triaj AI. Grupează pe cerință\n> pentru că așa va merge un autor să le repare — câte un fișier o dată.\n\nFiecare bullet de mai jos este obligatoriu.\n- În modul brut, `sync` grupează constatările pe cerință.\n- Fiecare grup și antetul documentului poartă un număr.\n- Cu zero constatări, `sync` scrie totuși un fișier bine format care afirmă că niciuna nu este deschisă.\n- Cu flag-ul brut activat, `sync` ignoră orice sidecar și emite lista brută grupată.",
          "acceptance": "CASE-1 — modul brut grupează constatările sub cerința lor\n  Given  două cerințe fiecare purtând un bullet Verify-intent deschis\n  When   `sync` rulează în mod brut\n  Then   `_findings.md` listează fiecare bullet cuibărit sub titlul propriei sale cerințe\n\nCASE-2 — raportul brut afișează un număr per grup și în antet\n  Given  o cerință cu două bullet-uri Verify-intent deschise\n  When   `sync` rulează în mod brut\n  Then   titlul grupului cerinței și antetul documentului arată amândouă numărul 2\n\nCASE-3 — findings scrie un raport curat când nimic nu este deschis\n  Given  un corpus în care fiecare secțiune Verify-intent conține doar placeholder-ul \"None —\"\n  When   `sync` rulează\n  Then   `_findings.md` este scris și afirmă că nicio constatare nu este deschisă\n\nCASE-4 — --raw ignoră un sidecar de triaj prezent\n  Given  un sidecar `_findings_triage.json` și cel puțin un bullet Verify-intent deschis\n  When   `sync` rulează cu `--raw`\n  Then   `_findings.md` arată lista brută grupată, nu secțiunile clasificate ale sidecar-ului\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-FINDINGS-855",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The triaged findings report",
      "intent": "Once an AI triage pass has classified each finding into a bug, a decision, or noise, re-reading `_findings.md` requirement-by-requirement is the wrong order — a developer wants the confirmed bugs first. The classified view is that reordering, and it renders only the classes the sidecar itself assigned, never a classification `sync` computes on its own.",
      "contract": [
        "When the sidecar exists and raw mode is off, `sync` renders a classified view.",
        "That view puts confirmed bugs first, ordered by severity from high to low, then product/config decisions, then intentional, then false-positive.",
        "A bug entry shows its location and its recommended fix when those are present.",
        "`sync` emits an advisory staleness note when the count of raw verify-intent items differs from the count of triaged items in the sidecar."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — findings renders a classified view when a sidecar exists Given  a `_findings_triage.json` sidecar present and no `--raw` flag When   `sync` runs Then   `_findings.md` is organized into classified sections using only the classes the sidecar already assigned, never ones `sync` computes itself",
        "CASE-2 — — classified view orders sections by severity then class Given  a sidecar classifying items REAL_BUG (high), REAL_BUG (low), USER_DECISION, INTENTIONAL and FALSE_POSITIVE When   `sync` runs without `--raw` Then   the high-severity bug lists first, then the low-severity bug, then the decision, then intentional, then false-positive",
        "CASE-3 — — a confirmed bug entry shows its location and fix Given  a sidecar classifying one item REAL_BUG with a location and a recommended fix When   `sync` runs without `--raw` Then   that entry in `_findings.md` prints both the location and the fix",
        "CASE-4 — — findings warns when raw and triaged counts diverge Given  three raw Verify-intent items but only one item recorded in the sidecar When   `sync` runs Then   `_findings.md` carries an advisory staleness note --------------------"
      ],
      "accept": "CASE-1 — findings renders a classified view when a sidecar exists\n  Given  a `_findings_triage.json` sidecar present and no `--raw` flag\n  When   `sync` runs\n  Then   `_findings.md` is organized into classified sections using only the classes the\n         sidecar already assigned, never ones `sync` computes itself\n\nCASE-2 — classified view orders sections by severity then class\n  Given  a sidecar classifying items REAL_BUG (high), REAL_BUG (low), USER_DECISION, INTENTIONAL\n         and FALSE_POSITIVE\n  When   `sync` runs without `--raw`\n  Then   the high-severity bug lists first, then the low-severity bug, then the decision, then\n         intentional, then false-positive\n\nCASE-3 — a confirmed bug entry shows its location and fix\n  Given  a sidecar classifying one item REAL_BUG with a location and a recommended fix\n  When   `sync` runs without `--raw`\n  Then   that entry in `_findings.md` prints both the location and the fix\n\nCASE-4 — findings warns when raw and triaged counts diverge\n  Given  three raw Verify-intent items but only one item recorded in the sidecar\n  When   `sync` runs\n  Then   `_findings.md` carries an advisory staleness note\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-FINDINGS-010"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3844"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1274"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Raportul de findings triate",
          "intent": "Odată ce o trecere de triaj AI a clasificat fiecare finding într-un bug, o decizie sau zgomot, recitirea `_findings.md` cerință-cu-cerință este ordinea greșită — un dezvoltator vrea mai întâi bug-urile confirmate. Vizualizarea clasificată este exact această reordonare, și randează doar clasele pe care sidecar-ul însuși le-a atribuit, niciodată o clasificare pe care `sync` o calculează singur.",
          "contract": "> Odată ce o trecere de triaj AI a clasificat fiecare finding într-un bug, o decizie sau\n> zgomot, recitirea `_findings.md` cerință-cu-cerință este ordinea greșită — un dezvoltator\n> vrea mai întâi bug-urile confirmate. Vizualizarea clasificată este exact această reordonare,\n> și randează doar clasele pe care sidecar-ul însuși le-a atribuit, niciodată o clasificare\n> pe care `sync` o calculează singur.\n\nFiecare bullet de mai jos este obligatoriu.\n- Când sidecar-ul există și modul raw este dezactivat, `sync` randează o vizualizare clasificată.\n- Acea vizualizare pune mai întâi bug-urile confirmate, ordonate după severitate de la mare la\n  mică, apoi deciziile de produs/config, apoi cele intenționate, apoi fals-pozitivele.\n- O intrare de bug arată locația și fix-ul recomandat atunci când acestea sunt prezente.\n- `sync` emite o notă consultativă de perimare când numărul de itemi raw verify-intent\n  diferă de numărul de itemi triați din sidecar.",
          "acceptance": "CASE-1 — findings randează o vizualizare clasificată când există un sidecar\n  Given  un sidecar `_findings_triage.json` prezent și niciun flag `--raw`\n  When   rulează `sync`\n  Then   `_findings.md` este organizat în secțiuni clasificate folosind doar clasele pe care\n         sidecar-ul le-a atribuit deja, niciodată unele pe care `sync` le calculează singur\n\nCASE-2 — vizualizarea clasificată ordonează secțiunile după severitate apoi clasă\n  Given  un sidecar care clasifică itemi REAL_BUG (high), REAL_BUG (low), USER_DECISION, INTENTIONAL\n         și FALSE_POSITIVE\n  When   `sync` rulează fără `--raw`\n  Then   se listează întâi bug-ul de severitate high, apoi bug-ul de severitate low, apoi decizia, apoi\n         cel intenționat, apoi fals-pozitivul\n\nCASE-3 — o intrare de bug confirmat arată locația și fix-ul său\n  Given  un sidecar care clasifică un item ca REAL_BUG cu o locație și un fix recomandat\n  When   `sync` rulează fără `--raw`\n  Then   acea intrare din `_findings.md` afișează atât locația cât și fix-ul\n\nCASE-4 — findings avertizează când numărul raw și cel triat diverg\n  Given  trei itemi raw Verify-intent dar doar un item înregistrat în sidecar\n  When   rulează `sync`\n  Then   `_findings.md` conține o notă consultativă de perimare\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-FINDINGS-856",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Findings integration with map and gate",
      "intent": "`sync` deliberately does no judgement of its own — classification is an AI's job, done out-of-band and handed back through the sidecar. `map` and `gate` treat `_findings.md` the same way they treat the other generated artifacts: refreshed when it already exists, checked for staleness, and never silently created.",
      "contract": [
        "`sync` is deterministic and stdlib-only. It never classifies a finding itself.",
        "`sync` writes no file other than `_findings.md`.",
        "`map` rewrites `_findings.md` when that file already exists. `sync` runs `map`, so it refreshes a committed report along with the map.",
        "`map` never creates `_findings.md`. Running `sync` once opts a repo in.",
        "`map --check` reports `_findings.md` stale when the committed copy differs from a fresh render, the same way it judges `_map.md` and `_map.json`. An absent file is never stale.",
        "The gate prints a non-error advisory line carrying the open-findings count, whenever that count is greater than zero.",
        "The open-findings count never changes the gate's exit code."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — map refreshes an already-committed findings report Given  a committed `_findings.md` and a requirement that gained a new Verify-intent item When   `map` runs Then   the regenerated `_findings.md` includes the new item",
        "CASE-2 — — map never creates a findings report that does not exist yet Given  no `_findings.md` file present in the requirements directory When   `map` runs Then   no `_findings.md` is created; only a subsequent `sync` run creates one",
        "CASE-3 — — map --check flags a stale findings report but never an absent one Given  a committed `_findings.md` that no longer matches the requirements, and separately a repo with none committed When   `map --check` runs in each case Then   it reports the first `_findings.md` as stale and exits non-zero, and reports nothing for the absent one",
        "CASE-4 — — gate prints the open-findings count as an advisory Given  a requirement with one open Verify-intent item When   `gate` runs Then   its output includes a line naming the open-findings count, and its exit code is unaffected"
      ],
      "accept": "CASE-1 — map refreshes an already-committed findings report\n  Given  a committed `_findings.md` and a requirement that gained a new Verify-intent item\n  When   `map` runs\n  Then   the regenerated `_findings.md` includes the new item\n\nCASE-2 — map never creates a findings report that does not exist yet\n  Given  no `_findings.md` file present in the requirements directory\n  When   `map` runs\n  Then   no `_findings.md` is created; only a subsequent `sync` run creates one\n\nCASE-3 — map --check flags a stale findings report but never an absent one\n  Given  a committed `_findings.md` that no longer matches the requirements, and separately a\n         repo with none committed\n  When   `map --check` runs in each case\n  Then   it reports the first `_findings.md` as stale and exits non-zero, and reports nothing\n         for the absent one\n\nCASE-4 — gate prints the open-findings count as an advisory\n  Given  a requirement with one open Verify-intent item\n  When   `gate` runs\n  Then   its output includes a line naming the open-findings count, and its exit code is\n         unaffected",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-FINDINGS-010"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4206"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6163"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6568"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Integrarea findings cu map și gate",
          "intent": "`sync` în mod deliberat nu face nicio judecată proprie — clasificarea este treaba unui AI, făcută out-of-band și transmisă înapoi prin sidecar. `map` și `gate` tratează `_findings.md` la fel cum tratează celelalte artefacte generate: reîmprospătat când există deja, verificat pentru perimare, și niciodată creat în tăcere.",
          "contract": "> `sync` în mod deliberat nu face nicio judecată proprie — clasificarea este treaba unui\n> AI, făcută out-of-band și transmisă înapoi prin sidecar. `map` și `gate` tratează\n> `_findings.md` la fel cum tratează celelalte artefacte generate: reîmprospătat când există\n> deja, verificat pentru perimare, și niciodată creat în tăcere.\n\nFiecare bullet de mai jos este obligatoriu.\n- `sync` este determinist și folosește doar stdlib. Nu clasifică niciodată un finding singur.\n- `sync` nu scrie niciun alt fișier în afară de `_findings.md`.\n- `map` rescrie `_findings.md` când acel fișier există deja. `sync` rulează `map`, deci\n  reîmprospătează un raport committed odată cu map-ul.\n- `map` nu creează niciodată `_findings.md`. Rularea `sync` o singură dată înscrie un repo.\n- `map --check` raportează `_findings.md` ca fiind perimat când copia committed diferă de o\n  randare proaspătă, la fel cum judecă `_map.md` și `_map.json`. Un fișier absent nu este\n  niciodată perimat.\n- Gate-ul afișează o linie consultativă, non-eroare, care conține numărul de findings deschise,\n  ori de câte ori acel număr este mai mare decât zero.\n- Numărul de findings deschise nu schimbă niciodată codul de ieșire al gate-ului.",
          "acceptance": "CASE-1 — map reîmprospătează un raport de findings deja committed\n  Given  un `_findings.md` committed și o cerință care a căpătat un item nou Verify-intent\n  When   rulează `map`\n  Then   `_findings.md` regenerat include noul item\n\nCASE-2 — map nu creează niciodată un raport de findings care nu există încă\n  Given  niciun fișier `_findings.md` prezent în directorul de cerințe\n  When   rulează `map`\n  Then   niciun `_findings.md` nu este creat; doar o rulare ulterioară `sync` creează unul\n\nCASE-3 — map --check semnalează un raport de findings perimat dar niciodată unul absent\n  Given  un `_findings.md` committed care nu mai corespunde cerințelor, și separat un\n         repo fără niciunul committed\n  When   `map --check` rulează în fiecare caz\n  Then   raportează primul `_findings.md` ca perimat și iese cu cod non-zero, și nu raportează nimic\n         pentru cel absent\n\nCASE-4 — gate afișează numărul de findings deschise ca notă consultativă\n  Given  o cerință cu un item Verify-intent deschis\n  When   rulează `gate`\n  Then   ieșirea sa include o linie care numește numărul de findings deschise, iar codul său de\n         ieșire este neafectat"
        }
      }
    },
    {
      "id": "ARCH-HEALTH-017",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Corpus health snapshot",
      "intent": "A project can have dozens of requirements slowly rotting — some never coded, some untested, some out of sync with the code — and nobody notices until it is a mess. This boils the whole registry down to a single health score plus a few counts, so a CI badge or a reviewer sees at a glance whether things are in good shape. Without it, corpus decay stays invisible until it is expensive to fix.",
      "contract": [
        "`next` prints a read-only coherence snapshot of the whole corpus, never writing a file. [[REQ-HEALTH-857]]",
        "The headline score is the percentage of requirements that are green — passing status, coverage, a test signal, no open verify-intent question, and no drift, all at once. [[REQ-HEALTH-858]]",
        "`next` prints component counts alongside the score (confirmed, implemented, tested, drafts, orphans, untested, open verify-intent, drift), matches them in `--json`, and always exits 0. [[REQ-HEALTH-859]]",
        "The same snapshot is written into `_map.json` as a `health` record, so a viewer reads the score rather than defining a second one. [[REQ-HEALTH-968]]"
      ],
      "verify": [],
      "notes": [
        "The score is deliberately strict: one open question or one drifted contract drops a requirement out of the green count. This makes the number move when real work remains.",
        "The \"tested\" line counts actual `tested-by` members, while the green test counts a `test_exempt` reason as covered. The two can differ by the number of exempted requirements.",
        "Drift reuses the lock that `sync` maintains. A stale or missing lock yields zero drift, the same fail-open behavior the gate uses.",
        "The `implemented` and `tested` counts stay code-member counts, so a corpus with a satisfied need shows `implemented < total` while still scoring 100."
      ],
      "current_impl": [
        "`cmd_health` in `reqmap.py` — walks every requirement, classifies each axis using `_member_roles`, `_bullets`, and the lock via `load_lock` + `binding_hash`, then prints the score and counts. `--json` dumps the assembled dictionary instead of the text view.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a corpus where every requirement is confirmed, implemented, tested, intent-clean, and undrifted When   `next` runs Then   the score is 100",
        "CASE-2 — Given  an all-draft corpus When   `next` runs Then   the score is zero",
        "CASE-3 — Given  `--json` When   `next` runs Then   the output parses as JSON and carries the `score` and `total` keys",
        "CASE-4 — Given  a confirmed requirement with no `implements` member When   `next` runs Then   it is counted under orphans and not under green",
        "CASE-5 — Given  an empty corpus When   `next` runs Then   the score is zero and the exit code is zero",
        "CASE-6 — Given  a confirmed `need` satisfied by at least one requirement in an otherwise green corpus When   `next` runs Then   the score is 100 and the orphan count is zero",
        "CASE-7 — Given  a confirmed `need` that no requirement satisfies When   `next` runs Then   it is counted under orphans and not under green"
      ],
      "accept": "CASE-1\n  Given  a corpus where every requirement is confirmed, implemented, tested, intent-clean, and undrifted\n  When   `next` runs\n  Then   the score is 100\n\nCASE-2\n  Given  an all-draft corpus\n  When   `next` runs\n  Then   the score is zero\n\nCASE-3\n  Given  `--json`\n  When   `next` runs\n  Then   the output parses as JSON and carries the `score` and `total` keys\n\nCASE-4\n  Given  a confirmed requirement with no `implements` member\n  When   `next` runs\n  Then   it is counted under orphans and not under green\n\nCASE-5\n  Given  an empty corpus\n  When   `next` runs\n  Then   the score is zero and the exit code is zero\n\nCASE-6\n  Given  a confirmed `need` satisfied by at least one requirement in an otherwise green corpus\n  When   `next` runs\n  Then   the score is 100 and the orphan count is zero\n\nCASE-7\n  Given  a confirmed `need` that no requirement satisfies\n  When   `next` runs\n  Then   it is counted under orphans and not under green",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001",
        "ARCH-SCAN-002",
        "ARCH-DRIFT-003"
      ],
      "used_by": [
        "ARCH-REGISTRYLAG-035",
        "ARCH-REVIEWEDSCORE-109",
        "ARCH-ROADMAP-038"
      ],
      "satisfies": [
        "SYS-REPORT-105"
      ],
      "satisfied_by": [
        "REQ-HEALTH-857",
        "REQ-HEALTH-858",
        "REQ-HEALTH-859",
        "REQ-HEALTH-968"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2866"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5779"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5864"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3448"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3727"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9048"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.14",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Instantaneu al stării corpusului",
          "intent": "Un proiect poate avea zeci de cerințe care se degradează încet — unele niciodată implementate în cod, unele netestate, unele nesincronizate cu codul — și nimeni nu observă până când devine un haos. Aceasta reduce întregul registru la un singur scor de sănătate plus câteva numărători, astfel încât un badge de CI sau un recenzent vede dintr-o privire dacă lucrurile sunt în regulă. Fără ea, degradarea corpusului rămâne invizibilă până când remedierea devine costisitoare.",
          "contract": "> Un proiect poate avea zeci de cerințe care se degradează încet — unele niciodată\n> implementate în cod, unele netestate, unele nesincronizate cu codul — și nimeni nu observă\n> până când devine un haos. Aceasta reduce întregul registru la un singur scor de sănătate\n> plus câteva numărători, astfel încât un badge de CI sau un recenzent vede dintr-o privire\n> dacă lucrurile sunt în regulă. Fără ea, degradarea corpusului rămâne invizibilă până când\n> remedierea devine costisitoare.\n\nFiecare punct de mai jos este obligatoriu.\n- `next` afișează un instantaneu de coerență, doar pentru citire, al întregului corpus, fără a scrie vreodată un fișier. [[REQ-HEALTH-857]]\n- Scorul principal este procentajul cerințelor care sunt verzi — status care trece, acoperire, un semnal de test, nicio întrebare deschisă de verificare a intenției și nicio abatere, toate în același timp. [[REQ-HEALTH-858]]\n- `next` afișează numărătorile componentelor alături de scor (confirmate, implementate, testate, ciorne, orfane, netestate, verificări de intenție deschise, abateri), le reproduce identic în `--json` și iese întotdeauna cu 0. [[REQ-HEALTH-859]]\n- Același instantaneu este scris în `_map.json` ca înregistrare `health`, astfel încât un vizualizator citește scorul în loc să definească un al doilea. [[REQ-HEALTH-968]]",
          "acceptance": "CASE-1\n  Given  un corpus în care fiecare cerință este confirmată, implementată, testată, curată din punctul de vedere al intenției și fără abateri\n  When   rulează `next`\n  Then   scorul este 100\n\nCASE-2\n  Given  un corpus format numai din ciorne\n  When   rulează `next`\n  Then   scorul este zero\n\nCASE-3\n  Given  `--json`\n  When   rulează `next`\n  Then   ieșirea se parsează ca JSON și conține cheile `score` și `total`\n\nCASE-4\n  Given  o cerință confirmată fără niciun membru `implements`\n  When   rulează `next`\n  Then   este numărată la orfane și nu la verzi\n\nCASE-5\n  Given  un corpus gol\n  When   rulează `next`\n  Then   scorul este zero și codul de ieșire este zero\n\nCASE-6\n  Given  un `need` confirmat, satisfăcut de cel puțin o cerință, într-un corpus altfel verde\n  When   rulează `next`\n  Then   scorul este 100 și numărul de orfane este zero\n\nCASE-7\n  Given  un `need` confirmat pe care nicio cerință nu îl satisface\n  When   rulează `next`\n  Then   este numărată la orfane și nu la verzi"
        }
      }
    },
    {
      "id": "REQ-HEALTH-857",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "health is a read-only snapshot of the whole corpus",
      "intent": "`next` boils dozens of slowly-rotting requirements down to one report: it walks every requirement, classifies each against a fixed set of axes, and prints the result. It never writes a file — not the lock, not a requirement — so running it carries no risk and needs no confirmation.",
      "contract": [
        "`next` prints a coherence snapshot of the whole requirement corpus.",
        "`next` writes nothing. It only reads and prints."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — health prints a snapshot covering the whole corpus Given  a corpus of several requirements in mixed states When   `next` runs Then   it prints one coherence snapshot summarizing every requirement, not a subset",
        "CASE-2 — — health runs without writing any file Given  any corpus When   `next` runs Then   no requirement file or lock file changes, and output goes only to stdout",
        "CASE-3 — — the headline score is the percentage that are green Given  a ten-requirement corpus where six pass every axis When   `next` runs Then   the printed score is 60",
        "CASE-4 — — an open verify-intent question excludes a requirement from green Given  a confirmed, implemented, tested, undrifted requirement carrying one open verify-intent question When   `next` runs Then   that requirement is excluded from the green count though every other axis passes --------------------"
      ],
      "accept": "CASE-1 — health prints a snapshot covering the whole corpus\n  Given  a corpus of several requirements in mixed states\n  When   `next` runs\n  Then   it prints one coherence snapshot summarizing every requirement, not a subset\n\nCASE-2 — health runs without writing any file\n  Given  any corpus\n  When   `next` runs\n  Then   no requirement file or lock file changes, and output goes only to stdout\n\nCASE-3 — the headline score is the percentage that are green\n  Given  a ten-requirement corpus where six pass every axis\n  When   `next` runs\n  Then   the printed score is 60\n\nCASE-4 — an open verify-intent question excludes a requirement from green\n  Given  a confirmed, implemented, tested, undrifted requirement carrying one open verify-intent question\n  When   `next` runs\n  Then   that requirement is excluded from the green count though every other axis passes\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-HEALTH-017"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5864"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3448"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9048"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "health este o fotografie read-only a întregului corpus",
          "intent": "`next` reduce zeci de cerințe care se degradează lent la un singur raport: parcurge fiecare cerință, o clasifică pe un set fix de axe și afișează rezultatul. Nu scrie niciodată un fișier — nici lock-ul, nici o cerință — deci rularea sa nu prezintă niciun risc și nu necesită nicio confirmare.",
          "contract": "> `next` reduce zeci de cerințe care se degradează lent la un singur raport: parcurge\n> fiecare cerință, o clasifică pe un set fix de axe și afișează rezultatul. Nu scrie\n> niciodată un fișier — nici lock-ul, nici o cerință — deci rularea sa nu prezintă niciun risc\n> și nu necesită nicio confirmare.\n\nFiecare bullet de mai jos este obligatoriu.\n- `next` afișează o fotografie de coerență a întregului corpus de cerințe.\n- `next` nu scrie nimic. Doar citește și afișează.",
          "acceptance": "CASE-1 — health afișează o fotografie care acoperă întregul corpus\n  Given  un corpus de mai multe cerințe în stări mixte\n  When   rulează `next`\n  Then   afișează o singură fotografie de coerență ce rezumă fiecare cerință, nu un subset\n\nCASE-2 — health rulează fără să scrie niciun fișier\n  Given  orice corpus\n  When   rulează `next`\n  Then   niciun fișier de cerință sau fișier lock nu se schimbă, iar ieșirea merge doar la stdout\n\nCASE-3 — scorul principal este procentul care este verde\n  Given  un corpus de zece cerințe unde șase trec fiecare axă\n  When   rulează `next`\n  Then   scorul afișat este 60\n\nCASE-4 — o întrebare deschisă verify-intent exclude o cerință din verde\n  Given  o cerință confirmed, implemented, tested, fără drift, dar cu o întrebare deschisă verify-intent\n  When   rulează `next`\n  Then   acea cerință este exclusă din numărul verde deși fiecare altă axă trece\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-HEALTH-858",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The headline score: green means every axis passes at once",
      "intent": "A requirement is \"green\" only when status, coverage, a test signal, an intent-clean history, and an undrifted contract all hold together — one failing axis is enough to drop it. `bus`/`feature` requirements need code (`implements`, `tested-by`); a `need` is covered once some requirement `satisfies:` it, since a need is fulfilled by requirements, not by code, and is otherwise an orphan.",
      "contract": [
        "`next` computes a headline score: the percentage of requirements that are green.",
        "The axes are status `confirmed`, coverage, a test signal, no open verify-intent question, and no drift from the lock.",
        "For a `bus` or `feature` requirement, coverage means an `implements` member.",
        "For those same layers, the test signal means a `tested-by` member or a `test_exempt` reason.",
        "A `need` is covered when at least one requirement `satisfies:` it. Its test axis is always met, because a need is fulfilled by requirements rather than by code (see [[ARCH-TRACE-020]]).",
        "A confirmed `need` that no requirement satisfies counts as an orphan."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a bus requirement without an implements member is uncovered Given  a `layer: bus` requirement with no `implements` member When   `next` runs Then   that requirement fails the coverage axis",
        "CASE-2 — — a test_exempt reason satisfies the test axis without a tested-by member Given  a `layer: feature` requirement with no `tested-by` member but a recorded `test_exempt` reason When   `next` runs Then   that requirement passes the test axis and its green status is unaffected",
        "CASE-3 — — a satisfied need passes its test axis without code Given  a `layer: need` requirement satisfied by one other requirement's `satisfies:` When   `next` runs Then   the need is counted covered and its test axis passes without any code member",
        "CASE-4 — — an unsatisfied confirmed need is an orphan Given  a confirmed `layer: need` requirement that no requirement's `satisfies:` names When   `next` runs Then   it is counted under orphans and excluded from green --------------------"
      ],
      "accept": "CASE-1 — a bus requirement without an implements member is uncovered\n  Given  a `layer: bus` requirement with no `implements` member\n  When   `next` runs\n  Then   that requirement fails the coverage axis\n\nCASE-2 — a test_exempt reason satisfies the test axis without a tested-by member\n  Given  a `layer: feature` requirement with no `tested-by` member but a recorded `test_exempt` reason\n  When   `next` runs\n  Then   that requirement passes the test axis and its green status is unaffected\n\nCASE-3 — a satisfied need passes its test axis without code\n  Given  a `layer: need` requirement satisfied by one other requirement's `satisfies:`\n  When   `next` runs\n  Then   the need is counted covered and its test axis passes without any code member\n\nCASE-4 — an unsatisfied confirmed need is an orphan\n  Given  a confirmed `layer: need` requirement that no requirement's `satisfies:` names\n  When   `next` runs\n  Then   it is counted under orphans and excluded from green\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-HEALTH-017"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5864"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3448"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9048"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Scorul principal: verde înseamnă că fiecare axă trece simultan",
          "intent": "O cerință este \"verde\" doar când status, acoperire, un semnal de test, un istoric curat din perspectiva intent și un contract fără drift se susțin toate împreună — o singură axă eșuată e suficientă să o scoată. Cerințele `bus`/`feature` au nevoie de cod (`implements`, `tested-by`); un `need` este acoperit odată ce o cerință `satisfies:` îl acoperă, deoarece un need este împlinit de cerințe, nu de cod, și altfel este un orfan.",
          "contract": "> O cerință este \"verde\" doar când status, acoperire, un semnal de test, un istoric\n> curat din perspectiva intent și un contract fără drift se susțin toate împreună — o\n> singură axă eșuată e suficientă să o scoată. Cerințele `bus`/`feature` au nevoie de cod\n> (`implements`, `tested-by`); un `need` este acoperit odată ce o cerință `satisfies:` îl\n> acoperă, deoarece un need este împlinit de cerințe, nu de cod, și altfel este un orfan.\n\nFiecare bullet de mai jos este obligatoriu.\n- `next` calculează un scor principal: procentul de cerințe care sunt verzi.\n- Axele sunt status `confirmed`, acoperire, un semnal de test, nicio întrebare deschisă\n  verify-intent, și niciun drift față de lock.\n- Pentru o cerință `bus` sau `feature`, acoperirea înseamnă un membru `implements`.\n- Pentru aceleași layere, semnalul de test înseamnă un membru `tested-by` sau un motiv `test_exempt`.\n- Un `need` este acoperit când cel puțin o cerință îl acoperă prin `satisfies:`. Axa sa de test\n  este mereu îndeplinită, deoarece un need este împlinit de cerințe, nu de cod (vezi [[ARCH-TRACE-020]]).\n- Un `need` confirmed pe care nicio cerință nu îl satisface se numără drept orfan.",
          "acceptance": "CASE-1 — o cerință bus fără membru implements este neacoperită\n  Given  o cerință `layer: bus` fără niciun membru `implements`\n  When   rulează `next`\n  Then   acea cerință eșuează axa de acoperire\n\nCASE-2 — un motiv test_exempt satisface axa de test fără un membru tested-by\n  Given  o cerință `layer: feature` fără niciun membru `tested-by` dar cu un motiv `test_exempt` înregistrat\n  When   rulează `next`\n  Then   acea cerință trece axa de test iar statutul său verde este neafectat\n\nCASE-3 — un need satisfăcut trece axa sa de test fără cod\n  Given  o cerință `layer: need` satisfăcută de `satisfies:`-ul unei alte cerințe\n  When   rulează `next`\n  Then   need-ul este numărat ca acoperit iar axa sa de test trece fără niciun membru de cod\n\nCASE-4 — un need confirmed nesatisfăcut este un orfan\n  Given  o cerință confirmed `layer: need` pe care niciun `satisfies:` al vreunei cerințe nu o numește\n  When   rulează `next`\n  Then   este numărată la orfani și exclusă din verde\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-HEALTH-859",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Component counts, --json parity, and an always-zero exit",
      "intent": "The headline score alone does not say WHAT to fix, so `next` also prints the counts behind it — confirmed, implemented, tested, drafts, orphans, untested, open verify-intent, drift. `--json` emits the identical numbers so a CI badge and the console view can never disagree, and the command always exits 0: it reports on the corpus, it is not a gate.",
      "contract": [
        "`next` prints component counts alongside the score: confirmed, implemented, tested, drafts, orphans, untested, open verify-intent, and drift.",
        "`next` prints the reviewed-only score defined by [[ARCH-REVIEWEDSCORE-109]] when that requirement's conditions hold.",
        "`--json` emits the same numbers as a JSON object, so the console output and a CI badge never disagree.",
        "On an empty corpus `next` prints a score of zero, with no error and nothing to divide by zero.",
        "`next` always returns zero. The snapshot is a report, not a gate."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the printed counts cover every named category Given  a corpus containing at least one draft, one orphan, and one drifted requirement When   `next` runs Then   it prints counts for confirmed, implemented, tested, drafts, orphans, untested, open verify-intent, and drift",
        "CASE-2 — — --json and the console report the same numbers Given  any corpus When   `next` runs once plainly and once with `--json` Then   the score and counts in both outputs match exactly",
        "CASE-3 — — an empty corpus reports a score of zero without error Given  a `requirements/` directory with no requirement files When   `next` runs Then   it prints a score of zero, and the run does not raise a division-by-zero error",
        "CASE-4 — — health exits zero even when the corpus scores below 100 Given  a corpus containing orphans and drift, scoring under 100 When   `next` runs Then   the process still exits 0 --------------------"
      ],
      "accept": "CASE-1 — the printed counts cover every named category\n  Given  a corpus containing at least one draft, one orphan, and one drifted requirement\n  When   `next` runs\n  Then   it prints counts for confirmed, implemented, tested, drafts, orphans, untested, open verify-intent, and drift\n\nCASE-2 — --json and the console report the same numbers\n  Given  any corpus\n  When   `next` runs once plainly and once with `--json`\n  Then   the score and counts in both outputs match exactly\n\nCASE-3 — an empty corpus reports a score of zero without error\n  Given  a `requirements/` directory with no requirement files\n  When   `next` runs\n  Then   it prints a score of zero, and the run does not raise a division-by-zero error\n\nCASE-4 — health exits zero even when the corpus scores below 100\n  Given  a corpus containing orphans and drift, scoring under 100\n  When   `next` runs\n  Then   the process still exits 0\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-HEALTH-017"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5864"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3448"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9048"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Numărători pe componente, paritate --json și o ieșire mereu zero",
          "intent": "Scorul principal singur nu spune CE trebuie reparat, așa că `next` afișează și numărătorile din spatele lui — confirmate, implementate, testate, ciorne, orfane, netestate, verify-intent deschise, drift. `--json` emite exact aceleași numere, astfel încât un badge de CI și consola să nu se poată contrazice niciodată, iar comanda iese întotdeauna cu 0: raportează despre corpus, nu este un gate.",
          "contract": "> Scorul principal singur nu spune CE trebuie reparat, așa că `next` afișează și numărătorile\n> din spatele lui — confirmate, implementate, testate, ciorne, orfane, netestate, verify-intent deschise,\n> drift. `--json` emite exact aceleași numere, astfel încât un badge de CI și consola să nu se\n> poată contrazice niciodată, iar comanda iese întotdeauna cu 0: raportează despre corpus, nu este un gate.\n\nFiecare punct de mai jos este obligatoriu.\n- `next` afișează numărătorile pe componente alături de scor: confirmate, implementate, testate,\n  ciorne, orfane, netestate, verify-intent deschise și drift.\n- `next` afișează scorul doar-pentru-revizuite definit de [[ARCH-REVIEWEDSCORE-109]] atunci când\n  condițiile acelei cerințe sunt îndeplinite.\n- `--json` emite aceleași numere ca obiect JSON, astfel încât ieșirea din consolă și un badge de CI\n  să nu se contrazică niciodată.\n- Pe un corpus gol `next` afișează un scor de zero, fără eroare și fără nimic de împărțit la\n  zero.\n- `next` returnează întotdeauna zero. Instantaneul este un raport, nu un gate.",
          "acceptance": "CASE-1 — numărătorile afișate acoperă fiecare categorie numită\n  Given  un corpus care conține cel puțin o ciornă, o cerință orfană și una cu drift\n  When   rulează `next`\n  Then   afișează numărători pentru confirmate, implementate, testate, ciorne, orfane, netestate, verify-intent deschise și drift\n\nCASE-2 — --json și consola raportează aceleași numere\n  Given  orice corpus\n  When   `next` rulează o dată simplu și o dată cu `--json`\n  Then   scorul și numărătorile din ambele ieșiri coincid exact\n\nCASE-3 — un corpus gol raportează un scor de zero fără eroare\n  Given  un director `requirements/` fără fișiere de cerințe\n  When   rulează `next`\n  Then   afișează un scor de zero, iar rularea nu ridică o eroare de împărțire la zero\n\nCASE-4 — health iese cu zero chiar și când corpusul are un scor sub 100\n  Given  un corpus care conține orfane și drift, cu scor sub 100\n  When   rulează `next`\n  Then   procesul iese totuși cu 0\n--------------------"
        }
      }
    },
    {
      "id": "REQ-HEALTH-968",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The health record travels with the map",
      "intent": "The map viewer showed a registry tally but not the headline number `next` opens with, and the obvious fix — recomputing the score in JavaScript — is how two surfaces come to disagree about how the repo is doing. The engine emits the record it already computes; the viewer displays it and defines nothing.",
      "contract": [
        "`_health_record` computes the snapshot from the requirements, their members and the lock alone, printing nothing. `cmd_health` reads it and layers on the signals that need a code root, so there is one computation of the score and one place it is defined.",
        "`_map.json` carries that record under a `health` key, beside the existing `design` record.",
        "The record is a pure function of inputs the map already depends on, so a map carrying it stays deterministic and `gate`'s freshness check keeps working unchanged."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the map carries the score the console prints Given  any corpus When   `map` writes `_map.json` and `next --json` runs over the same corpus Then   the map's `health.score` equals the score in the JSON snapshot",
        "CASE-2 — — the record needs no code root Given  a caller with the requirements and members but no code root When   `_health_record` runs Then   it returns the score and component counts without raising",
        "CASE-3 — — regenerating the map twice produces the same bytes Given  an unchanged corpus and lock When   `map` runs twice Then   both runs write byte-identical `_map.json`, so freshness checks stay stable"
      ],
      "accept": "CASE-1 — the map carries the score the console prints\n  Given  any corpus\n  When   `map` writes `_map.json` and `next --json` runs over the same corpus\n  Then   the map's `health.score` equals the score in the JSON snapshot\n\nCASE-2 — the record needs no code root\n  Given  a caller with the requirements and members but no code root\n  When   `_health_record` runs\n  Then   it returns the score and component counts without raising\n\nCASE-3 — regenerating the map twice produces the same bytes\n  Given  an unchanged corpus and lock\n  When   `map` runs twice\n  Then   both runs write byte-identical `_map.json`, so freshness checks stay stable",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-HEALTH-017"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4253"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5779"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7370"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3448"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Fișa de sănătate călătorește împreună cu harta",
          "intent": "Vizualizatorul hărții afișa un total al registrului, dar nu și cifra principală cu care se deschide `next`, iar soluția evidentă — recalcularea scorului în JavaScript — este exact felul în care două suprafețe ajung să nu fie de acord cu privire la starea repo-ului. Motorul emite fișa pe care oricum o calculează; vizualizatorul o afișează și nu definește nimic.",
          "contract": "> Vizualizatorul hărții afișa un total al registrului, dar nu și cifra principală cu care se deschide `next`,\n> iar soluția evidentă — recalcularea scorului în JavaScript — este exact felul în care două suprafețe ajung\n> să nu fie de acord cu privire la starea repo-ului. Motorul emite fișa pe care oricum o calculează;\n> vizualizatorul o afișează și nu definește nimic.\n\nFiecare punct de mai jos este obligatoriu.\n- `_health_record` calculează instantaneul doar din cerințe, membrii acestora și lock,\n  fără să afișeze nimic. `cmd_health` îl citește și adaugă peste el semnalele care au nevoie de o\n  rădăcină de cod, astfel încât există un singur calcul al scorului și un singur loc în care este definit.\n- `_map.json` transportă acea fișă sub cheia `health`, alături de fișa `design` existentă.\n- Fișa este o funcție pură de intrări de care harta depinde deja, așa că o hartă care o transportă\n  rămâne deterministă, iar verificarea de prospețime a lui `gate` continuă să funcționeze neschimbată.",
          "acceptance": "CASE-1 — harta transportă scorul pe care îl afișează consola\n  Given  un corpus oarecare\n  When   `map` scrie `_map.json` și `next --json` rulează peste același corpus\n  Then   `health.score` al hărții este egal cu scorul din instantaneul JSON\n\nCASE-2 — fișa nu are nevoie de o rădăcină de cod\n  Given  un apelant care are cerințele și membrii, dar nicio rădăcină de cod\n  When   `_health_record` rulează\n  Then   returnează scorul și numărătorile componentelor fără să ridice excepție\n\nCASE-3 — regenerarea hărții de două ori produce aceiași octeți\n  Given  un corpus și un lock neschimbate\n  When   `map` rulează de două ori\n  Then   ambele rulări scriu `_map.json` identic la nivel de octeți, deci verificările de prospețime rămân stabile"
        }
      }
    },
    {
      "id": "ARCH-IMPLEMENT-063",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "The brief for implementing a requirement",
      "intent": "Between a written requirement and working code there is a gap every agent fills by guessing: which tags to carry, where this kind of code lives in this repo, what proves it landed. `implement` states all of it in one place, from facts the engine already holds. It writes no code — a deterministic tool can own the contract and the verdict, never the authorship — and it says so, so nobody waits for it to.",
      "contract": [
        "`implement` emits one requirement's obligations, cases, existing members, unanswered questions and the exact tags its code carries. [[REQ-IMPLEMENT-958]]",
        "`implement` names the requirements most similar to it that already have code, so the brief points at where this kind of work lives in this repository. [[REQ-IMPLEMENT-959]]"
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the brief carries the tags verbatim Given  a requirement with three labelled cases When   `implement` runs on it Then   the output holds `# implements: <ID>` and one `# verifies: <ID>#CASE-N` per case",
        "CASE-2 — — unanswered blocking questions are stated before the work starts Given  a requirement with no labelled case When   `implement` runs on it Then   the brief opens with that blocking question and names `clarify`",
        "CASE-3 — — the brief writes nothing Given  any requirement When   `implement` runs Then   no file in the repository changes"
      ],
      "accept": "CASE-1 — the brief carries the tags verbatim\n  Given  a requirement with three labelled cases\n  When   `implement` runs on it\n  Then   the output holds `# implements: <ID>` and one `# verifies: <ID>#CASE-N` per case\n\nCASE-2 — unanswered blocking questions are stated before the work starts\n  Given  a requirement with no labelled case\n  When   `implement` runs on it\n  Then   the brief opens with that blocking question and names `clarify`\n\nCASE-3 — the brief writes nothing\n  Given  any requirement\n  When   `implement` runs\n  Then   no file in the repository changes",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001",
        "ARCH-SCAN-002",
        "ARCH-SIMILAR-016",
        "ARCH-CLARIFY-062"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-GATE-102"
      ],
      "satisfied_by": [
        "REQ-IMPLEMENT-958",
        "REQ-IMPLEMENT-959"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7952"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7973"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10230"
        }
      ],
      "test_exempt": null,
      "milestone": "v4.0",
      "priority": "should-have",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Fișa de brief pentru implementarea unei cerințe",
          "intent": "Între o cerință scrisă și codul funcțional există un gol pe care fiecare agent îl umple prin ghicire: ce etichete să poarte, unde trăiește acest fel de cod în acest repo, ce dovedește că a ajuns la destinație. `implement` afirmă totul într-un singur loc, din fapte pe care motorul le deține deja. Nu scrie niciun cod — un instrument determinist poate deține contractul și verdictul, niciodată autorajul — și spune acest lucru, astfel încât nimeni să nu aștepte să o facă.",
          "contract": "> Între o cerință scrisă și codul funcțional există un gol pe care fiecare agent îl umple\n> prin ghicire: ce etichete să poarte, unde trăiește acest fel de cod în acest repo, ce\n> dovedește că a ajuns la destinație. `implement` afirmă totul într-un singur loc, din\n> fapte pe care motorul le deține deja. Nu scrie niciun cod — un instrument determinist\n> poate deține contractul și verdictul, niciodată autorajul — și spune acest lucru,\n> astfel încât nimeni să nu aștepte să o facă.\n\nFiecare punct de mai jos este obligatoriu.\n- `implement` emite obligațiile, cazurile, membrii existenți, întrebările fără răspuns și etichetele exacte pe care le poartă codul unei cerințe. [[REQ-IMPLEMENT-958]]\n- `implement` numește cerințele cele mai similare cu ea care au deja cod, astfel încât fișa de brief indică unde trăiește acest fel de muncă în acest repository. [[REQ-IMPLEMENT-959]]",
          "acceptance": "CASE-1 — fișa de brief poartă etichetele verbatim\n  Given  o cerință cu trei cazuri etichetate\n  When   `implement` rulează pe ea\n  Then   ieșirea conține `# implements: <ID>` și câte un `# verifies: <ID>#CASE-N` per caz\n\nCASE-2 — întrebările blocante fără răspuns sunt afirmate înainte ca munca să înceapă\n  Given  o cerință fără niciun caz etichetat\n  When   `implement` rulează pe ea\n  Then   fișa de brief se deschide cu acea întrebare blocantă și numește `clarify`\n\nCASE-3 — fișa de brief nu scrie nimic\n  Given  orice cerință\n  When   `implement` rulează\n  Then   niciun fișier din repository nu se schimbă"
        }
      }
    },
    {
      "id": "REQ-IMPLEMENT-958",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "What the implementation brief states",
      "intent": "The brief is the requirement plus the four facts a writer of code needs and would otherwise reconstruct by reading the repository: what already implements it, what is still unanswered, what tags bind the new code, and what command proves the result.",
      "contract": [
        "The brief carries the requirement's id, title, status, level, layer, intent, obligations and cases as authored.",
        "The brief lists the requirement's existing members with role and location, and says plainly when there are none.",
        "The brief carries the open questions from the same detectors `clarify` uses, blocking ones named first.",
        "The brief states the literal tag lines the new code carries: one `implements:`, one `tested-by:`, and one `verifies:` per labelled case.",
        "The brief names the verification command, and states that the requirement is not to be edited to match the code.",
        "`--json` emits the whole brief as one object; an unknown id exits 1."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a requirement with no code says so Given  a requirement carrying no member When   `implement` runs Then   the members section says there is nothing yet",
        "CASE-2 — — one verifies tag per labelled case Given  a requirement with cases CASE-1 and CASE-2 When   `implement` runs Then   exactly two `verifies:` tag lines are emitted, one per label",
        "CASE-3 — — the JSON brief is one object with the same fields Given  any requirement When   `implement <ID> --json` runs Then   stdout parses as JSON carrying contract, cases, members, tags and open_questions"
      ],
      "accept": "CASE-1 — a requirement with no code says so\n  Given  a requirement carrying no member\n  When   `implement` runs\n  Then   the members section says there is nothing yet\n\nCASE-2 — one verifies tag per labelled case\n  Given  a requirement with cases CASE-1 and CASE-2\n  When   `implement` runs\n  Then   exactly two `verifies:` tag lines are emitted, one per label\n\nCASE-3 — the JSON brief is one object with the same fields\n  Given  any requirement\n  When   `implement <ID> --json` runs\n  Then   stdout parses as JSON carrying contract, cases, members, tags and open_questions",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-IMPLEMENT-063"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7973"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10230"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Ce afirmă briefing-ul de implementare",
          "intent": "Briefing-ul este cerința plus cele patru fapte de care are nevoie un scriitor de cod și pe care altfel le-ar reconstitui citind repository-ul: ce implementează deja, ce mai este neclarificat, ce tag-uri leagă noul cod și ce comandă demonstrează rezultatul.",
          "contract": "> Briefing-ul este cerința plus cele patru fapte de care are nevoie un scriitor de cod și\n> pe care altfel le-ar reconstitui citind repository-ul: ce implementează deja, ce mai este\n> neclarificat, ce tag-uri leagă noul cod și ce comandă demonstrează rezultatul.\n\nFiecare bullet de mai jos este obligatoriu.\n- Briefing-ul conține id-ul, titlul, status-ul, level-ul, layer-ul, intent-ul, obligațiile și\n  cazurile cerinței, așa cum au fost redactate.\n- Briefing-ul listează membrii existenți ai cerinței cu rol și locație, și spune clar\n  când nu există niciunul.\n- Briefing-ul conține întrebările deschise din aceiași detectori pe care îi folosește `clarify`,\n  cele blocante fiind numite primele.\n- Briefing-ul afirmă liniile literale de tag pe care le poartă noul cod: un `implements:`, un\n  `tested-by:`, și un `verifies:` per caz etichetat.\n- Briefing-ul numește comanda de verificare, și afirmă că cerința nu trebuie editată\n  pentru a se potrivi cu codul.\n- `--json` emite întregul briefing ca un singur obiect; un id necunoscut iese cu 1.",
          "acceptance": "CASE-1 — o cerință fără cod spune asta\n  Given  o cerință fără niciun membru\n  When   rulează `implement`\n  Then   secțiunea de membri spune că nu există încă niciunul\n\nCASE-2 — câte un tag verifies per caz etichetat\n  Given  o cerință cu cazurile CASE-1 și CASE-2\n  When   rulează `implement`\n  Then   sunt emise exact două linii de tag `verifies:`, una per etichetă\n\nCASE-3 — briefing-ul JSON este un singur obiect cu aceleași câmpuri\n  Given  orice cerință\n  When   rulează `implement <ID> --json`\n  Then   stdout se parsează ca JSON conținând contract, cases, members, tags și open_questions"
        }
      }
    },
    {
      "id": "REQ-IMPLEMENT-959",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Pointing at where this kind of code lives",
      "intent": "\"Where do I put it?\" is the question a brief cannot answer from the requirement alone, and the engine knows nothing about the host project's layout. It does know which requirements read alike, and requirements that read alike are implemented next to each other far more often than not — so the neighbours' files are the honest answer, offered as a hint.",
      "contract": [
        "The brief names up to two requirements most similar to this one, ranked by the same TF-IDF cosine `search` and `dupes` use.",
        "Only requirements that already carry members are offered, and each is listed with the files its members live in.",
        "A requirement with no similar neighbour, or a corpus where nothing has members, produces no neighbours section rather than an empty one."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the nearest implemented requirement is offered with its files Given  a corpus where one other requirement shares most of its contract vocabulary and has members When   `implement` runs Then   that requirement is named, with a score and the files of its members",
        "CASE-2 — — a requirement with no implemented neighbour offers none Given  a corpus where no other requirement carries a member When   `implement` runs Then   no neighbour is named",
        "CASE-3 — — at most two neighbours are offered Given  a corpus where five other requirements are similar and carry members When   `implement` runs Then   two neighbours are named, the two ranked highest"
      ],
      "accept": "CASE-1 — the nearest implemented requirement is offered with its files\n  Given  a corpus where one other requirement shares most of its contract vocabulary and has members\n  When   `implement` runs\n  Then   that requirement is named, with a score and the files of its members\n\nCASE-2 — a requirement with no implemented neighbour offers none\n  Given  a corpus where no other requirement carries a member\n  When   `implement` runs\n  Then   no neighbour is named\n\nCASE-3 — at most two neighbours are offered\n  Given  a corpus where five other requirements are similar and carry members\n  When   `implement` runs\n  Then   two neighbours are named, the two ranked highest",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-IMPLEMENT-063"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7952"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10230"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Indicând unde trăiește acest tip de cod",
          "intent": "\"Unde îl pun?\" este întrebarea la care un briefing nu poate răspunde doar din cerință, iar engine-ul nu știe nimic despre organizarea proiectului gazdă. Știe însă care cerințe se citesc asemănător, iar cerințele care se citesc asemănător sunt implementate una lângă alta mult mai des decât nu — deci fișierele vecinilor sunt răspunsul onest, oferit ca indiciu.",
          "contract": "> \"Unde îl pun?\" este întrebarea la care un briefing nu poate răspunde doar din cerință,\n> iar engine-ul nu știe nimic despre organizarea proiectului gazdă. Știe însă care cerințe\n> se citesc asemănător, iar cerințele care se citesc asemănător sunt implementate una lângă\n> alta mult mai des decât nu — deci fișierele vecinilor sunt răspunsul onest, oferit ca\n> indiciu.\n\nFiecare bullet de mai jos este obligatoriu.\n- Briefing-ul numește până la două cerințe cele mai similare cu aceasta, clasate după aceeași\n  cosinusă TF-IDF pe care o folosesc `search` și `dupes`.\n- Sunt oferite doar cerințele care poartă deja membri, iar fiecare este listată cu fișierele în\n  care trăiesc membrii ei.\n- O cerință fără niciun vecin similar, sau un corpus în care nimic nu are membri, produce\n  nicio secțiune de vecini, în loc de una goală.",
          "acceptance": "CASE-1 — cea mai apropiată cerință implementată este oferită cu fișierele ei\n  Given  un corpus în care o altă cerință împarte cea mai mare parte a vocabularului contractului și are membri\n  When   rulează `implement`\n  Then   acea cerință este numită, cu un scor și fișierele membrilor ei\n\nCASE-2 — o cerință fără vecin implementat nu oferă niciunul\n  Given  un corpus în care nicio altă cerință nu poartă un membru\n  When   rulează `implement`\n  Then   niciun vecin nu este numit\n\nCASE-3 — sunt oferiți cel mult doi vecini\n  Given  un corpus în care cinci alte cerințe sunt similare și poartă membri\n  When   rulează `implement`\n  Then   sunt numiți doi vecini, cei doi clasați cel mai sus"
        }
      }
    },
    {
      "id": "ARCH-INIT-012",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "First-use bootstrap",
      "intent": "Setting up requirement tracking on a fresh project means doing five fiddly steps in the right order. This is the single \"get started\" command that does all of them for you: it makes the folder, reads your existing code and drafts requirements from it, records a baseline, builds the navigable map, and tells you the one next thing to do. Without it, a newcomer has to learn and chain those steps by hand, and most give up before the project is in a usable state.",
      "contract": [
        "`init` creates the requirements folder and a starter `.reqmapignore` when either is missing, never overwriting one that already exists. [[REQ-INIT-860]]",
        "`init` drafts requirements from untagged code, writes the lock, then builds the map, in that order, and ends with a short summary pointing at the one next command. [[REQ-INIT-861]]"
      ],
      "verify": [],
      "notes": [
        "The starter `.reqmapignore` assumes the engine sits at `scripts/reqmap.py` — the documented place to vendor it. The self-hosting exception looks at that same path.",
        "Self-hosting can only be detected if requirements already exist when `.reqmapignore` is first written. Bootstrapping a self-hosting repo from an empty `requirements/` folder cannot be detected — there is nothing for the tags to match yet — so the engine line does get written. Re-running later does not rewrite the file, so that repo has to delete the line by hand. Known and accepted.",
        "On a repo with no extractable code `init` still succeeds: the lock and map are empty but valid.",
        "`init` performs no analysis of its own. It only calls capabilities that already exist (draft, gate, map) in a fixed order."
      ],
      "current_impl": [
        "`cmd_init` in `reqmap.py` — creates `requirements/`, conditionally seeds `.reqmapignore` via `_reqmapignore_seed` (which applies the self-hosting exception), then calls `cmd_extract` → `cmd_check(update_lock=True)` → `cmd_map`, and prints the guided summary.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a repo with no `requirements/` When   `init` runs Then   it creates the directory and a `.reqmapignore` containing `scripts/reqmap.py`",
        "CASE-2 — Given  a self-hosting repo (an existing requirement whose id is tagged inside `scripts/reqmap.py`) When   `init` runs Then   the written `.reqmapignore` does **not** ignore `scripts/reqmap.py`, so the engine stays scanned and its members are not orphaned",
        "CASE-3 — Given  a repo that already has a `.reqmapignore` When   `init` runs Then   that file is left unchanged",
        "CASE-4 — Given  a completed `init` When   the requirements directory is inspected Then   `requirements/_map.html`, `requirements/_map.md` and the drift lock exist",
        "CASE-5 — Given  existing untagged code When   `init` runs Then   it drafts at least one requirement, reports the tracked count, and its summary points at `reqmap.py gate --risk`",
        "CASE-6 — Given  a repo with no extractable code When   `init` runs Then   it prints the distinct \"no requirements were extracted\" message (not a tracked-count summary)",
        "CASE-7 — Given  a repo with no `.reqmapignore` and a tagged file copied into `.worktrees/` and into `.claude/worktrees/` (what an isolated subagent leaves behind) When   `init` runs and the repo is then scanned Then   the seeded `.reqmapignore` lists both globs and neither copy is scanned as a member"
      ],
      "accept": "CASE-1\n  Given  a repo with no `requirements/`\n  When   `init` runs\n  Then   it creates the directory and a `.reqmapignore` containing `scripts/reqmap.py`\n\nCASE-2\n  Given  a self-hosting repo (an existing requirement whose id is tagged inside `scripts/reqmap.py`)\n  When   `init` runs\n  Then   the written `.reqmapignore` does **not** ignore `scripts/reqmap.py`, so the engine\n         stays scanned and its members are not orphaned\n\nCASE-3\n  Given  a repo that already has a `.reqmapignore`\n  When   `init` runs\n  Then   that file is left unchanged\n\nCASE-4\n  Given  a completed `init`\n  When   the requirements directory is inspected\n  Then   `requirements/_map.html`, `requirements/_map.md` and the drift lock exist\n\nCASE-5\n  Given  existing untagged code\n  When   `init` runs\n  Then   it drafts at least one requirement, reports the tracked count, and its summary\n         points at `reqmap.py gate --risk`\n\nCASE-6\n  Given  a repo with no extractable code\n  When   `init` runs\n  Then   it prints the distinct \"no requirements were extracted\" message (not a tracked-count summary)\n\nCASE-7\n  Given  a repo with no `.reqmapignore` and a tagged file copied into `.worktrees/` and\n         into `.claude/worktrees/` (what an isolated subagent leaves behind)\n  When   `init` runs and the repo is then scanned\n  Then   the seeded `.reqmapignore` lists both globs and neither copy is scanned as a member",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-EXTRACT-008",
        "ARCH-CHECK-006",
        "ARCH-MAP-007"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-SHIP-108"
      ],
      "satisfied_by": [
        "REQ-INIT-860",
        "REQ-INIT-861"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6050"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6086"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2545"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2702"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6338"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.10",
      "priority": "",
      "risks": []
    },
    {
      "id": "REQ-INIT-860",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Scaffolding the requirements folder and a starter .reqmapignore",
      "intent": "A fresh repo has neither a `requirements/` folder nor a `.reqmapignore`, and both are needed before any other command can run cleanly. `init` writes them once and never again: a second run must not clobber a `.reqmapignore` a human has since edited by hand. The starter file also excludes the vendored engine and isolated-agent-worktree copies, or the gate would immediately flag their tags as dangling.",
      "contract": [
        "`init` creates the requirements folder if it is missing.",
        "`init` writes a starter `.reqmapignore` only if the repo has none. It never overwrites one that is already there.",
        "The starter file lists `scripts/reqmap.py`. Without that line, the engine's own tags look like they point at requirements that do not exist.",
        "The starter file also lists `.worktrees/**` and `.claude/worktrees/**` — the two places an isolated agent worktree is created. Each holds a full second copy of the repo, so without those lines every member is counted twice and the copies' tags read as dangling refs: errors that are not in the code, in files a clean CI checkout never has.",
        "One exception: if the engine describes itself in this repo, `init` leaves the line out and writes a comment saying why. There the engine is ordinary tracked code, so the scan keeps reading it.",
        "\"Describes itself\" means `scripts/reqmap.py` carries tags whose ids match requirements already in the repo."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — init creates a missing requirements directory Given  a repo with no `requirements/` folder When   `cmd_init` runs Then   `requirements/` exists afterward",
        "CASE-2 — — an existing .reqmapignore is left byte-for-byte Given  a repo with `.reqmapignore` already containing `my-custom-glob/**` When   `cmd_init` runs Then   `.reqmapignore`'s content is unchanged",
        "CASE-3 — — the seeded ignore lists scripts/reqmap.py in a non-self-hosting repo Given  a repo with no `.reqmapignore` and no self-hosting requirements When   `cmd_init` runs Then   the written `.reqmapignore` contains the line `scripts/reqmap.py`",
        "CASE-4 — — the seeded ignore prunes both worktree-copy locations Given  a fresh repo with no `.reqmapignore`, after `cmd_init` runs When   a tagged file is then copied into `.worktrees/wt1/` and `.claude/worktrees/wt1/` Then   `scan_members` counts neither copy — only the original file — as a member",
        "CASE-5 — — a self-hosting repo's seeded ignore does not ignore the engine Given  `requirements/CORE-X-001.md` exists and `scripts/reqmap.py` carries `# implements: CORE-X-001` When   `cmd_init` runs Then   the written `.reqmapignore` has no live (uncommented) `scripts/reqmap.py` glob, and `scan_members` still counts the engine as a member of `CORE-X-001`",
        "CASE-6 — — a tag pointing at no local requirement is not self-hosting Given  `requirements/CORE-Y-002.md` exists and `scripts/reqmap.py` carries `# implements: CORE-GHOST-999` (an id with no matching file) When   `cmd_init` runs Then   the self-hosting exception does not fire: the written `.reqmapignore` still ignores `scripts/reqmap.py` --------------------"
      ],
      "accept": "CASE-1 — init creates a missing requirements directory\n  Given  a repo with no `requirements/` folder\n  When   `cmd_init` runs\n  Then   `requirements/` exists afterward\n\nCASE-2 — an existing .reqmapignore is left byte-for-byte\n  Given  a repo with `.reqmapignore` already containing `my-custom-glob/**`\n  When   `cmd_init` runs\n  Then   `.reqmapignore`'s content is unchanged\n\nCASE-3 — the seeded ignore lists scripts/reqmap.py in a non-self-hosting repo\n  Given  a repo with no `.reqmapignore` and no self-hosting requirements\n  When   `cmd_init` runs\n  Then   the written `.reqmapignore` contains the line `scripts/reqmap.py`\n\nCASE-4 — the seeded ignore prunes both worktree-copy locations\n  Given  a fresh repo with no `.reqmapignore`, after `cmd_init` runs\n  When   a tagged file is then copied into `.worktrees/wt1/` and `.claude/worktrees/wt1/`\n  Then   `scan_members` counts neither copy — only the original file — as a member\n\nCASE-5 — a self-hosting repo's seeded ignore does not ignore the engine\n  Given  `requirements/CORE-X-001.md` exists and `scripts/reqmap.py` carries `# implements: CORE-X-001`\n  When   `cmd_init` runs\n  Then   the written `.reqmapignore` has no live (uncommented) `scripts/reqmap.py` glob, and `scan_members` still counts the engine as a member of `CORE-X-001`\n\nCASE-6 — a tag pointing at no local requirement is not self-hosting\n  Given  `requirements/CORE-Y-002.md` exists and `scripts/reqmap.py` carries `# implements: CORE-GHOST-999` (an id with no matching file)\n  When   `cmd_init` runs\n  Then   the self-hosting exception does not fire: the written `.reqmapignore` still ignores `scripts/reqmap.py`\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-INIT-012"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6050"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2545"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10003"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Scaffolding-ul folderului de cerințe și al unui .reqmapignore de pornire",
          "intent": "Un repo nou nu are nici un folder `requirements/`, nici un `.reqmapignore`, și amândouă sunt necesare înainte ca orice altă comandă să poată rula curat. `init` le scrie o dată și niciodată din nou: o a doua rulare nu trebuie să strice un `.reqmapignore` pe care un om l-a editat între timp de mână. Fișierul de pornire exclude de asemenea engine-ul vendored și copiile izolate de worktree ale agenților, altfel gate-ul ar semnala imediat tag-urile lor ca dangling.",
          "contract": "> Un repo nou nu are nici un folder `requirements/`, nici un `.reqmapignore`, și amândouă\n> sunt necesare înainte ca orice altă comandă să poată rula curat. `init` le scrie o dată\n> și niciodată din nou: o a doua rulare nu trebuie să strice un `.reqmapignore` pe care un\n> om l-a editat între timp de mână. Fișierul de pornire exclude de asemenea engine-ul\n> vendored și copiile izolate de worktree ale agenților, altfel gate-ul ar semnala imediat\n> tag-urile lor ca dangling.\n\nFiecare bullet de mai jos este obligatoriu.\n- `init` creează folderul de cerințe dacă lipsește.\n- `init` scrie un `.reqmapignore` de pornire doar dacă repo-ul nu are niciunul. Nu suprascrie\n  niciodată unul care există deja.\n- Fișierul de pornire listează `scripts/reqmap.py`. Fără acea linie, tag-urile proprii ale\n  engine-ului par să indice cerințe care nu există.\n- Fișierul de pornire listează de asemenea `.worktrees/**` și `.claude/worktrees/**` — cele\n  două locuri unde este creat un worktree izolat de agent. Fiecare conține o a doua copie\n  completă a repo-ului, deci fără acele linii fiecare membru este numărat de două ori iar\n  tag-urile copiilor se citesc ca referințe dangling: erori care nu sunt în cod, în fișiere\n  pe care un checkout CI curat nu le are niciodată.\n- O excepție: dacă engine-ul se descrie pe sine în acest repo, `init` omite linia și scrie\n  un comentariu care spune de ce. Acolo engine-ul este cod tracked obișnuit, deci scanarea\n  continuă să-l citească.\n- \"Se descrie pe sine\" înseamnă că `scripts/reqmap.py` poartă tag-uri ale căror id-uri se\n  potrivesc cu cerințe deja prezente în repo.",
          "acceptance": "CASE-1 — init creează un director requirements lipsă\n  Given  un repo fără folder `requirements/`\n  When   rulează `cmd_init`\n  Then   `requirements/` există ulterior\n\nCASE-2 — un .reqmapignore existent este lăsat byte-cu-byte\n  Given  un repo cu `.reqmapignore` conținând deja `my-custom-glob/**`\n  When   rulează `cmd_init`\n  Then   conținutul lui `.reqmapignore` este neschimbat\n\nCASE-3 — ignore-ul seedat listează scripts/reqmap.py într-un repo non-self-hosting\n  Given  un repo fără `.reqmapignore` și fără cerințe self-hosting\n  When   rulează `cmd_init`\n  Then   `.reqmapignore`-ul scris conține linia `scripts/reqmap.py`\n\nCASE-4 — ignore-ul seedat elimină ambele locații de copie worktree\n  Given  un repo proaspăt fără `.reqmapignore`, după ce rulează `cmd_init`\n  When   un fișier tagat este apoi copiat în `.worktrees/wt1/` și `.claude/worktrees/wt1/`\n  Then   `scan_members` nu numără nicio copie — doar fișierul original — ca membru\n\nCASE-5 — ignore-ul seedat al unui repo self-hosting nu ignoră engine-ul\n  Given  `requirements/CORE-X-001.md` există iar `scripts/reqmap.py` poartă `# implements: CORE-X-001`\n  When   rulează `cmd_init`\n  Then   `.reqmapignore`-ul scris nu are niciun glob viu (necomentat) `scripts/reqmap.py`, iar `scan_members` continuă să numere engine-ul ca membru al `CORE-X-001`\n\nCASE-6 — un tag care indică o cerință inexistentă local nu este self-hosting\n  Given  `requirements/CORE-Y-002.md` există iar `scripts/reqmap.py` poartă `# implements: CORE-GHOST-999` (un id fără fișier corespunzător)\n  When   rulează `cmd_init`\n  Then   excepția self-hosting nu se declanșează: `.reqmapignore`-ul scris tot ignoră `scripts/reqmap.py`\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-INIT-861",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Running draft, lock, and map in a fixed order",
      "intent": "`init` chains capabilities that already exist — `draft`, the gate's lock write, `map` — in the one order that leaves a repo gate-passing and navigable in a single command. It ends with one named next step rather than a menu, because a newcomer facing five options after already running one command is exactly the friction `init` exists to remove.",
      "contract": [
        "`init` drafts requirements from untagged code, writes the lock, then builds the map, in that order. When it finishes, the repo passes the gate and has a map.",
        "`init` ends with a short summary naming one next command: `reqmap.py gate --risk`. Not a list of every option.",
        "If nothing was drafted, `init` says so in plain words and points at `new`. It never prints a count summary that hides an empty result.",
        "Running `init` twice is safe. The second run refreshes the lock and the map, then prints the summary again.",
        "A second run never deletes a requirement someone wrote, and never edits an existing `.reqmapignore`."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the lock and map cover the requirements init just drafted Given  an untagged `app.py` in a repo with no `requirements/` When   `cmd_init` runs Then   `_reqlock.json` carries a hash for the newly drafted `DRAFT-*` requirement, proving draft ran before the lock was written",
        "CASE-2 — — the summary names next, not the full command list Given  a repo with extractable code When   `cmd_init` runs Then   its output contains \"reqmap.py gate --risk\" but names none of \"confirm\", \"dupes\", \"search\" or \"coverage\"",
        "CASE-3 — — an extraction-free repo gets a distinct empty-result message Given  a repo containing only `README.txt` (no extractable code) When   `cmd_init` runs Then   its output says \"no requirements were extracted\" and points at `reqmap.py new`, never \"0 requirement(s) tracked\"",
        "CASE-4 — — a second init run exits clean and reprints the summary Given  a repo already initialized by one `cmd_init` run When   `cmd_init` runs again Then   it exits 0 and its output again contains \"reqmap.py gate --risk\"",
        "CASE-5 — — a hand-authored requirement and .reqmapignore survive a re-run Given  a hand-written `requirements/CORE-FOO-001.md` and an existing `.reqmapignore` When   `cmd_init` runs (no `--wipe`) Then   `CORE-FOO-001.md` still exists and `.reqmapignore`'s content is unchanged"
      ],
      "accept": "CASE-1 — the lock and map cover the requirements init just drafted\n  Given  an untagged `app.py` in a repo with no `requirements/`\n  When   `cmd_init` runs\n  Then   `_reqlock.json` carries a hash for the newly drafted `DRAFT-*` requirement, proving draft ran before the lock was written\n\nCASE-2 — the summary names next, not the full command list\n  Given  a repo with extractable code\n  When   `cmd_init` runs\n  Then   its output contains \"reqmap.py gate --risk\" but names none of \"confirm\", \"dupes\", \"search\" or \"coverage\"\n\nCASE-3 — an extraction-free repo gets a distinct empty-result message\n  Given  a repo containing only `README.txt` (no extractable code)\n  When   `cmd_init` runs\n  Then   its output says \"no requirements were extracted\" and points at `reqmap.py new`, never \"0 requirement(s) tracked\"\n\nCASE-4 — a second init run exits clean and reprints the summary\n  Given  a repo already initialized by one `cmd_init` run\n  When   `cmd_init` runs again\n  Then   it exits 0 and its output again contains \"reqmap.py gate --risk\"\n\nCASE-5 — a hand-authored requirement and .reqmapignore survive a re-run\n  Given  a hand-written `requirements/CORE-FOO-001.md` and an existing `.reqmapignore`\n  When   `cmd_init` runs (no `--wipe`)\n  Then   `CORE-FOO-001.md` still exists and `.reqmapignore`'s content is unchanged",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-INIT-012"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6086"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2545"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10003"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5
    },
    {
      "id": "ARCH-LEVEL-051",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Specification level",
      "intent": "`layer:` says where a requirement sits in the dependency graph — a `bus` is defined by high fan-in, a `need` is covered by an edge instead of a code tag. It says nothing about how abstract the requirement is. This adds that second axis, so a corpus can record whether a requirement describes a whole system, one architectural piece, or one unit of code. Without it the two questions are answered by one field that can only answer the first.",
      "contract": [
        "A requirement may carry an optional `level:` of `system`, `architecture` or `code`, independent of `layer:`; the gate accepts the three values, ignores an absent field, and errors on anything else, granting no implements-tag exemption. [[REQ-LEVEL-862]] details the behaviour."
      ],
      "verify": [],
      "notes": [
        "The three values are the V-model's left arm, collapsed for a software-only tool: what ISO/IEC/IEEE and ASPICE separate into system requirements, system architecture, software requirements, software architecture and detailed design becomes three when there is no hardware to allocate.",
        "`level:` is deliberately NOT a rename of `layer:`. The two encode different facts, and aliasing them would be a silent behaviour change: `IMPL_EXEMPT_LAYERS` keys on `layer`, so making `architecture` a synonym of `aggregate` would remove every architecture requirement from the confirmed-code-must-exist check.",
        "The field is inert until a corpus adopts it. Nothing in the gate, the map or the viewer changes for a repo that never writes one."
      ],
      "current_impl": [
        "`VALID_LEVEL` in `reqmap.py` and the validation branch in `cmd_check`, which reports an error for an unrecognised value and stays silent when the field is absent.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a requirement carrying `level: architecture` When   the gate runs Then   it reports no error about the level",
        "CASE-2 — Given  a requirement carrying `level: detailed` When   the gate runs Then   it reports one error naming the invalid level, and the exit code is 1",
        "CASE-3 — Given  a corpus in which no requirement carries a `level:` field When   the gate runs Then   its output is unchanged from a run before the field existed",
        "CASE-4 — Given  a confirmed requirement carrying `level: architecture` and no `implements:` member When   the gate runs Then   it still reports the missing-member error, because the level grants no exemption"
      ],
      "accept": "CASE-1\n  Given  a requirement carrying `level: architecture`\n  When   the gate runs\n  Then   it reports no error about the level\nCASE-2\n  Given  a requirement carrying `level: detailed`\n  When   the gate runs\n  Then   it reports one error naming the invalid level, and the exit code is 1\nCASE-3\n  Given  a corpus in which no requirement carries a `level:` field\n  When   the gate runs\n  Then   its output is unchanged from a run before the field existed\nCASE-4\n  Given  a confirmed requirement carrying `level: architecture` and no `implements:` member\n  When   the gate runs\n  Then   it still reports the missing-member error, because the level grants no exemption",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001",
        "ARCH-CHECK-006"
      ],
      "used_by": [
        "ARCH-FANOUT-052",
        "ARCH-VRUNGS-054"
      ],
      "satisfies": [
        "SYS-VMODEL-107"
      ],
      "satisfied_by": [
        "REQ-LEVEL-862"
      ],
      "members": [
        {
          "role": "generated-from",
          "loc": "docs/full_architecture.html:4"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:149"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2494"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3982"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7229"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9296"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "should-have",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Nivel de specificație",
          "intent": "`layer:` spune unde se află o cerință în graful de dependențe — un `bus` este definit de fan-in ridicat, o `need` este acoperită printr-o muchie în loc de o etichetă de cod. Nu spune nimic despre cât de abstractă este cerința. Aceasta adaugă acea a doua axă, astfel încât un corpus poate înregistra dacă o cerință descrie un sistem întreg, o piesă arhitecturală, sau o unitate de cod. Fără aceasta, cele două întrebări sunt răspunse de un singur câmp care poate răspunde doar la prima.",
          "contract": "> `layer:` spune unde se află o cerință în graful de dependențe — un `bus` este definit de\n> fan-in ridicat, o `need` este acoperită printr-o muchie în loc de o etichetă de cod. Nu\n> spune nimic despre cât de abstractă este cerința. Aceasta adaugă acea a doua axă, astfel\n> încât un corpus poate înregistra dacă o cerință descrie un sistem întreg, o piesă\n> arhitecturală, sau o unitate de cod. Fără aceasta, cele două întrebări sunt răspunse de\n> un singur câmp care poate răspunde doar la prima.\n\nFiecare punct de mai jos este obligatoriu.\n- O cerință poate purta un `level:` opțional de `system`, `architecture` sau `code`, independent de `layer:`; gate-ul acceptă cele trei valori, ignoră un câmp absent, și generează eroare pentru orice altceva, fără a acorda nicio exceptare de la eticheta implements. [[REQ-LEVEL-862]] detaliază comportamentul.",
          "acceptance": "CASE-1\n  Given  o cerință care poartă `level: architecture`\n  When   gate-ul rulează\n  Then   nu raportează nicio eroare despre nivel\nCASE-2\n  Given  o cerință care poartă `level: detailed`\n  When   gate-ul rulează\n  Then   raportează o eroare care numește nivelul invalid, iar codul de ieșire este 1\nCASE-3\n  Given  un corpus în care nicio cerință nu poartă un câmp `level:`\n  When   gate-ul rulează\n  Then   ieșirea sa este neschimbată față de o rulare dinainte ca acest câmp să existe\nCASE-4\n  Given  o cerință confirmată care poartă `level: architecture` și niciun membru `implements:`\n  When   gate-ul rulează\n  Then   raportează totuși eroarea de membru lipsă, deoarece nivelul nu acordă nicio exceptare"
        }
      }
    },
    {
      "id": "REQ-LEVEL-862",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The level field, validated independently of layer",
      "intent": "`level:` and `layer:` answer different questions — how abstract a requirement is, versus where it sits in the dependency graph — so the gate validates them separately and never lets one imply the other. An `architecture`-level requirement still owns code and still needs an `implements:` member; only `layer: aggregate` (owning no code of its own) is exempt from that, regardless of level.",
      "contract": [
        "A requirement may carry a `level:` value of `system`, `architecture` or `code`.",
        "The `level:` field is optional. A requirement without one is read exactly as before.",
        "The `level:` axis is independent of `layer:`, and neither value constrains the other.",
        "An `architecture` requirement owns code, so the gate keeps requiring an `implements:` member for it.",
        "The `aggregate` layer stays exempt from that rule, because it owns no code of its own.",
        "No `level:` value is added to the implementation-exemption set.",
        "The gate reports an error for a `level:` value outside the three named ones.",
        "The gate says nothing about a requirement that carries no `level:` at all."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the gate accepts each of the three level values Given  three requirements carrying `level: system`, `level: architecture` and `level: code` respectively When   `gate` runs Then   it reports no level-related error for any of the three",
        "CASE-2 — — a requirement without level: reads exactly as before Given  a corpus of requirements written before `level:` existed, none carrying the field When   `gate` runs Then   its output matches a run from before the field was introduced, with no level-related finding for any requirement",
        "CASE-3 — — level and layer combine freely without cross-validation Given  a requirement carrying `level: system` and `layer: bus` together When   `gate` runs Then   it reports no error tying the two fields to each other",
        "CASE-4 — — an architecture-level requirement still needs an implements member Given  a confirmed requirement carrying `level: architecture` and no `implements:` member When   `gate` runs Then   it reports the missing-member error, because no `level:` value is in the implementation-exemption set",
        "CASE-5 — — an aggregate-layer requirement stays exempt regardless of level Given  a confirmed `layer: aggregate` requirement carrying `level: architecture` and no `implements:` member When   `gate` runs Then   it reports no missing-member error",
        "CASE-6 — — an invalid level value is a gate error Given  a requirement carrying `level: detailed` When   `gate` runs Then   it reports one error naming the invalid value and exits 1"
      ],
      "accept": "CASE-1 — the gate accepts each of the three level values\n  Given  three requirements carrying `level: system`, `level: architecture` and `level: code` respectively\n  When   `gate` runs\n  Then   it reports no level-related error for any of the three\n\nCASE-2 — a requirement without level: reads exactly as before\n  Given  a corpus of requirements written before `level:` existed, none carrying the field\n  When   `gate` runs\n  Then   its output matches a run from before the field was introduced, with no\n         level-related finding for any requirement\n\nCASE-3 — level and layer combine freely without cross-validation\n  Given  a requirement carrying `level: system` and `layer: bus` together\n  When   `gate` runs\n  Then   it reports no error tying the two fields to each other\n\nCASE-4 — an architecture-level requirement still needs an implements member\n  Given  a confirmed requirement carrying `level: architecture` and no `implements:` member\n  When   `gate` runs\n  Then   it reports the missing-member error, because no `level:` value is in the\n         implementation-exemption set\n\nCASE-5 — an aggregate-layer requirement stays exempt regardless of level\n  Given  a confirmed `layer: aggregate` requirement carrying `level: architecture` and no `implements:` member\n  When   `gate` runs\n  Then   it reports no missing-member error\n\nCASE-6 — an invalid level value is a gate error\n  Given  a requirement carrying `level: detailed`\n  When   `gate` runs\n  Then   it reports one error naming the invalid value and exits 1",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-LEVEL-051"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2494"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7229"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9296"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Câmpul level, validat independent de layer",
          "intent": "`level:` și `layer:` răspund la întrebări diferite — cât de abstractă este o cerință, versus unde stă în graful de dependențe — deci gate-ul le validează separat și nu lasă niciodată una să o implice pe cealaltă. O cerință la nivel `architecture` deține tot cod și tot are nevoie de un membru `implements:`; doar `layer: aggregate` (care nu deține propriul cod) este exemptat de la asta, indiferent de level.",
          "contract": "> `level:` și `layer:` răspund la întrebări diferite — cât de abstractă este o cerință,\n> versus unde stă în graful de dependențe — deci gate-ul le validează separat și nu lasă\n> niciodată una să o implice pe cealaltă. O cerință la nivel `architecture` deține tot\n> cod și tot are nevoie de un membru `implements:`; doar `layer: aggregate` (care nu deține\n> propriul cod) este exemptat de la asta, indiferent de level.\n\nFiecare bullet de mai jos este obligatoriu.\n- O cerință poate purta o valoare `level:` de `system`, `architecture` sau `code`.\n- Câmpul `level:` este opțional. O cerință fără unul este citită exact ca înainte.\n- Axa `level:` este independentă de `layer:`, iar nicio valoare nu o constrânge pe cealaltă.\n- O cerință `architecture` deține cod, deci gate-ul continuă să ceară un membru `implements:` pentru ea.\n- Layer-ul `aggregate` rămâne exemptat de la acea regulă, deoarece nu deține propriul cod.\n- Nicio valoare `level:` nu este adăugată în setul de exemptare de implementare.\n- Gate-ul raportează o eroare pentru o valoare `level:` din afara celor trei numite.\n- Gate-ul nu spune nimic despre o cerință care nu poartă deloc `level:`.",
          "acceptance": "CASE-1 — gate-ul acceptă fiecare din cele trei valori level\n  Given  trei cerințe purtând `level: system`, `level: architecture` și respectiv `level: code`\n  When   rulează `gate`\n  Then   nu raportează nicio eroare legată de level pentru niciuna dintre ele\n\nCASE-2 — o cerință fără level: se citește exact ca înainte\n  Given  un corpus de cerințe scrise înainte ca `level:` să existe, niciuna purtând câmpul\n  When   rulează `gate`\n  Then   ieșirea sa se potrivește cu o rulare de dinainte ca acest câmp să fi fost introdus, fără niciun\n         finding legat de level pentru vreo cerință\n\nCASE-3 — level și layer se combină liber fără validare încrucișată\n  Given  o cerință purtând `level: system` și `layer: bus` împreună\n  When   rulează `gate`\n  Then   nu raportează nicio eroare care leagă cele două câmpuri unul de altul\n\nCASE-4 — o cerință la nivel architecture tot are nevoie de un membru implements\n  Given  o cerință confirmed purtând `level: architecture` și niciun membru `implements:`\n  When   rulează `gate`\n  Then   raportează eroarea de membru lipsă, deoarece nicio valoare `level:` nu este în\n         setul de exemptare de implementare\n\nCASE-5 — o cerință de layer aggregate rămâne exemptată indiferent de level\n  Given  o cerință confirmed `layer: aggregate` purtând `level: architecture` și niciun membru `implements:`\n  When   rulează `gate`\n  Then   nu raportează nicio eroare de membru lipsă\n\nCASE-6 — o valoare level invalidă este o eroare de gate\n  Given  o cerință purtând `level: detailed`\n  When   rulează `gate`\n  Then   raportează o eroare care numește valoarea invalidă și iese cu 1"
        }
      }
    },
    {
      "id": "ARCH-LINT-014",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Requirement readability linter",
      "intent": "Requirement documents are only useful if people can actually read them. This is the automatic proofreader's frame: it decides which files get checked, which sections count, how severe a finding is, and whether the build fails. Without it, the clear-writing rules rely on someone re-reading every file by hand, and prose slowly degrades until the documents stop being worth opening.",
      "contract": [
        "`gate` reports readability problems and structure problems in requirement files, scoped to non-draft requirements, and writes no file. [[REQ-LINT-863]]",
        "The prose checks read the Contract and the Acceptance sections and no other section, skipping non-prose lines; `--strict` fails only on an error-severity finding. [[REQ-LINT-864]]"
      ],
      "verify": [],
      "notes": [
        "The individual warn checks (stacked-conditions, statement-too-long, ac-count, over-scoped, file-spread, vague-term, redundant-modal) are a separate capability — [[ARCH-LINTCHECKS-025]] — running under the same `gate` command.",
        "Jargon-before-definition detection is intentionally out of scope for this version. Without a dictionary of project terms, any heuristic for \"undefined jargon\" produces too many false positives on prose that carries code references. It may arrive later as an opt-in check."
      ],
      "current_impl": [
        "`cmd_lint` selects non-draft requirements, runs `lint_requirement` on each, prints findings grouped per requirement, and decides the exit code. `_lint_prose` extracts the prose lines of one section; `_clip` truncates finding text for display. The `missing-section` check reuses `_has_section` (shared with the gate in `cmd_check`).",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a confirmed requirement whose body has no `## Cases` heading When   `gate` runs Then   it reports a `missing-section` error for that requirement",
        "CASE-2 — Given  a draft requirement with a long sentence When   `gate` runs Then   it reports nothing for that requirement, because drafts are out of scope",
        "CASE-3 — Given  a requirement whose `## Cases` heading is present but has no criteria beneath it When   `gate` runs Then   it reports an `empty-section` warning",
        "CASE-4 — Given  a normative line with four conjunctions inside a fenced code block in the Acceptance section When   `gate` runs Then   it reports no `stacked-conditions` finding for that line",
        "CASE-5 — Given  a corpus whose non-draft requirements all have both sections When   `lint --strict` runs Then   it returns zero even if warnings were printed",
        "CASE-6 — Given  one non-draft requirement missing a section When   `lint --strict` runs Then   it returns a non-zero exit code"
      ],
      "accept": "CASE-1\n  Given  a confirmed requirement whose body has no `## Cases` heading\n  When   `gate` runs\n  Then   it reports a `missing-section` error for that requirement\n\nCASE-2\n  Given  a draft requirement with a long sentence\n  When   `gate` runs\n  Then   it reports nothing for that requirement, because drafts are out of scope\n\nCASE-3\n  Given  a requirement whose `## Cases` heading is present but has no criteria beneath it\n  When   `gate` runs\n  Then   it reports an `empty-section` warning\n\nCASE-4\n  Given  a normative line with four conjunctions inside a fenced code block in the\n         Acceptance section\n  When   `gate` runs\n  Then   it reports no `stacked-conditions` finding for that line\n\nCASE-5\n  Given  a corpus whose non-draft requirements all have both sections\n  When   `lint --strict` runs\n  Then   it returns zero even if warnings were printed\n\nCASE-6\n  Given  one non-draft requirement missing a section\n  When   `lint --strict` runs\n  Then   it returns a non-zero exit code",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001"
      ],
      "used_by": [
        "ARCH-ATOMICITY-049",
        "ARCH-DECOMPOSE-050",
        "ARCH-FANOUT-052",
        "ARCH-LINTCHECKS-025"
      ],
      "satisfies": [
        "SYS-QUALITY-104"
      ],
      "satisfied_by": [
        "REQ-LINT-863",
        "REQ-LINT-864"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4485"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4520"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4591"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4957"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2846"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.14",
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Linter de lizibilitate a cerințelor",
          "intent": "Documentele de cerințe sunt utile doar dacă oamenii chiar le pot citi. Acesta este cadrul corectorului automat: decide ce fișiere sunt verificate, ce secțiuni contează, cât de severă este o constatare, și dacă build-ul eșuează. Fără aceasta, regulile de scriere clară depind de cineva care recitește fiecare fișier manual, iar proza se degradează încet până când documentele nu mai merită deschise.",
          "contract": "> Documentele de cerințe sunt utile doar dacă oamenii chiar le pot citi. Acesta este\n> cadrul corectorului automat: decide ce fișiere sunt verificate, ce secțiuni contează,\n> cât de severă este o constatare, și dacă build-ul eșuează. Fără aceasta, regulile de\n> scriere clară depind de cineva care recitește fiecare fișier manual, iar proza se\n> degradează încet până când documentele nu mai merită deschise.\n\nFiecare punct de mai jos este obligatoriu.\n- `gate` raportează probleme de lizibilitate și probleme de structură în fișierele de cerințe, limitat la cerințele care nu sunt drafturi, și nu scrie niciun fișier. [[REQ-LINT-863]]\n- Verificările de proză citesc secțiunile Contract și Acceptance și nicio altă secțiune, sărind peste liniile care nu sunt proză; `--strict` eșuează doar la o constatare de severitate error. [[REQ-LINT-864]]",
          "acceptance": "CASE-1\n  Given  o cerință confirmată al cărei corp nu are niciun titlu `## Cases`\n  When   `gate` rulează\n  Then   raportează o eroare `missing-section` pentru acea cerință\n\nCASE-2\n  Given  o cerință draft cu o propoziție lungă\n  When   `gate` rulează\n  Then   nu raportează nimic pentru acea cerință, deoarece drafturile sunt în afara domeniului\n\nCASE-3\n  Given  o cerință al cărei titlu `## Cases` este prezent dar nu are niciun criteriu sub el\n  When   `gate` rulează\n  Then   raportează un avertisment `empty-section`\n\nCASE-4\n  Given  o linie normativă cu patru conjuncții în interiorul unui bloc de cod delimitat, în\n         secțiunea Acceptance\n  When   `gate` rulează\n  Then   nu raportează nicio constatare `stacked-conditions` pentru acea linie\n\nCASE-5\n  Given  un corpus ale cărui cerințe non-draft au toate ambele secțiuni\n  When   `lint --strict` rulează\n  Then   returnează zero chiar dacă au fost afișate avertismente\n\nCASE-6\n  Given  o cerință non-draft căreia îi lipsește o secțiune\n  When   `lint --strict` rulează\n  Then   returnează un cod de ieșire diferit de zero"
        }
      }
    },
    {
      "id": "REQ-LINT-863",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "What lint checks and skips",
      "intent": "`gate` is read-only: it never edits a requirement, only reports on it, so running it carries no risk of losing work. Drafts are excluded because they are TODO stubs nobody has reviewed yet — linting them would produce noise about prose that is expected to be rough.",
      "contract": [
        "`gate` reports readability problems and structure problems in requirement files.",
        "`gate` writes no file. It only reads and prints.",
        "`gate` checks non-draft requirements only — status `baseline`, `in-progress`, `implemented` or `confirmed`. Drafts are TODO stubs, so linting them would only add noise.",
        "`gate` gives each finding one of two severities. A structural check reports an `error`; a prose check or a scope check reports a `warn`.",
        "The `missing-section` check reports an `error` when a non-draft requirement has no `## Description` section, or no `## Cases` section.",
        "The `empty-section` check reports a `warn` when one of those two headings is present but carries nothing under it: no clauses, no criteria. Such a section passes `missing-section` while documenting nothing."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — one run surfaces both a structural and a readability finding Given  a confirmed requirement with no `## Cases` section and a Contract bullet joined by four \"and\"s When   `lint_requirement` runs on it Then   the findings include both a `missing-section` error and a `stacked-conditions` warning",
        "CASE-2 — — a default lint run leaves the requirements directory untouched Given  a requirements directory with findings to report and no `--decompose` flag When   `cmd_lint` runs Then   the directory's file listing is identical before and after the call",
        "CASE-3 — — a draft with a bad clause is silently skipped Given  a `draft` requirement whose Contract bullet is a 50-word run-on sentence When   `cmd_lint` runs Then   it exits 0 and its output names no findings for that requirement",
        "CASE-4 — — structural findings are error, prose findings are warn Given  a confirmed requirement with no `## Cases` section and a Contract line opening with a bare \"It\" When   `lint_requirement` runs on it Then   the `missing-section` finding carries severity `error` and the `anonymous-subject` finding carries `warn`",
        "CASE-5 — — a confirmed requirement with no Acceptance heading gets a missing-section error Given  a `confirmed` requirement body carrying a Description section but no `## Cases` heading When   `lint_requirement` runs on it Then   its findings include `(\"error\", \"missing-section\")`",
        "CASE-6 — — a heading with no content beneath it gets an empty-section warning Given  a `confirmed` requirement whose Contract and Acceptance headings are both present but carry no bullets When   `lint_requirement` runs on it Then   its findings include an `empty-section` warning and no `missing-section` error --------------------"
      ],
      "accept": "CASE-1 — one run surfaces both a structural and a readability finding\n  Given  a confirmed requirement with no `## Cases` section and a Contract bullet joined by four \"and\"s\n  When   `lint_requirement` runs on it\n  Then   the findings include both a `missing-section` error and a `stacked-conditions` warning\n\nCASE-2 — a default lint run leaves the requirements directory untouched\n  Given  a requirements directory with findings to report and no `--decompose` flag\n  When   `cmd_lint` runs\n  Then   the directory's file listing is identical before and after the call\n\nCASE-3 — a draft with a bad clause is silently skipped\n  Given  a `draft` requirement whose Contract bullet is a 50-word run-on sentence\n  When   `cmd_lint` runs\n  Then   it exits 0 and its output names no findings for that requirement\n\nCASE-4 — structural findings are error, prose findings are warn\n  Given  a confirmed requirement with no `## Cases` section and a Contract line opening with a bare \"It\"\n  When   `lint_requirement` runs on it\n  Then   the `missing-section` finding carries severity `error` and the `anonymous-subject` finding carries `warn`\n\nCASE-5 — a confirmed requirement with no Acceptance heading gets a missing-section error\n  Given  a `confirmed` requirement body carrying a Description section but no `## Cases` heading\n  When   `lint_requirement` runs on it\n  Then   its findings include `(\"error\", \"missing-section\")`\n\nCASE-6 — a heading with no content beneath it gets an empty-section warning\n  Given  a `confirmed` requirement whose Contract and Acceptance headings are both present but carry no bullets\n  When   `lint_requirement` runs on it\n  Then   its findings include an `empty-section` warning and no `missing-section` error\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-LINT-014"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4591"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4957"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2846"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9712"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Ce verifică și ce omite lint",
          "intent": "`gate` este read-only: nu editează niciodată o cerință, doar raportează despre ea, deci rularea sa nu prezintă niciun risc de pierdere a muncii. Draft-urile sunt excluse deoarece sunt stub-uri TODO pe care nimeni nu le-a revizuit încă — a le linta ar produce zgomot despre proză care este de așteptat să fie brută.",
          "contract": "> `gate` este read-only: nu editează niciodată o cerință, doar raportează despre ea, deci\n> rularea sa nu prezintă niciun risc de pierdere a muncii. Draft-urile sunt excluse deoarece\n> sunt stub-uri TODO pe care nimeni nu le-a revizuit încă — a le linta ar produce doar zgomot\n> despre proză care este de așteptat să fie brută.\n\nFiecare bullet de mai jos este obligatoriu.\n- `gate` raportează probleme de lizibilitate și probleme de structură în fișierele de cerințe.\n- `gate` nu scrie niciun fișier. Doar citește și afișează.\n- `gate` verifică doar cerințele non-draft — status `baseline`, `in-progress`,\n  `implemented` sau `confirmed`. Draft-urile sunt stub-uri TODO, deci a le linta ar adăuga doar zgomot.\n- `gate` acordă fiecărui finding una din două severități. O verificare structurală raportează un\n  `error`; o verificare de proză sau o verificare de scope raportează un `warn`.\n- Verificarea `missing-section` raportează un `error` când o cerință non-draft nu are\n  secțiune `## Description`, sau nicio secțiune `## Cases`.\n- Verificarea `empty-section` raportează un `warn` când una din acele două titluri este\n  prezentă dar nu conține nimic dedesubt: nicio clauză, niciun criteriu. O astfel de secțiune trece\n  `missing-section` fără să documenteze nimic.",
          "acceptance": "CASE-1 — o singură rulare scoate la iveală atât un finding structural cât și unul de lizibilitate\n  Given  o cerință confirmed fără secțiune `## Cases` și un bullet de Contract unit prin patru \"and\"-uri\n  When   rulează `lint_requirement` pe ea\n  Then   findings-urile includ atât o eroare `missing-section` cât și un avertisment `stacked-conditions`\n\nCASE-2 — o rulare lint implicită lasă directorul de cerințe neatins\n  Given  un director de cerințe cu findings de raportat și fără flag-ul `--decompose`\n  When   rulează `cmd_lint`\n  Then   listarea de fișiere a directorului este identică înainte și după apel\n\nCASE-3 — un draft cu o clauză proastă este omis silențios\n  Given  o cerință `draft` a cărei clauză de Contract este o propoziție run-on de 50 de cuvinte\n  When   rulează `cmd_lint`\n  Then   iese cu 0 iar ieșirea sa nu numește niciun finding pentru acea cerință\n\nCASE-4 — findings-urile structurale sunt error, cele de proză sunt warn\n  Given  o cerință confirmed fără secțiune `## Cases` și o linie de Contract care începe cu un \"It\" simplu\n  When   rulează `lint_requirement` pe ea\n  Then   finding-ul `missing-section` poartă severitatea `error` iar finding-ul `anonymous-subject` poartă `warn`\n\nCASE-5 — o cerință confirmed fără titlu Acceptance primește o eroare missing-section\n  Given  un corp de cerință `confirmed` care poartă o secțiune Description dar niciun titlu `## Cases`\n  When   rulează `lint_requirement` pe ea\n  Then   findings-urile sale includ `(\"error\", \"missing-section\")`\n\nCASE-6 — un titlu fără conținut dedesubt primește un avertisment empty-section\n  Given  o cerință `confirmed` ale cărei titluri Contract și Acceptance sunt ambele prezente dar nu conțin bullets\n  When   rulează `lint_requirement` pe ea\n  Then   findings-urile sale includ un avertisment `empty-section` și nicio eroare `missing-section`\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-LINT-864",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Where the prose checks read from, and what fails a strict run",
      "intent": "A prose check that also read Notes or a code fence would flag text nobody meant as a readable sentence — a footgun explanation, a literal example, a `--strict` flag name. Scoping the checks to exactly the Contract and Acceptance sections, and skipping non-prose lines inside them, keeps every finding pointed at text an author actually needs to fix.",
      "contract": [
        "The prose checks read the Contract and the Acceptance sections, and no other section.",
        "The \"Notes & limitations\" section is exempt: only deep readers reach it, and it may stay dense.",
        "The prose checks skip lines that are not prose — headings, table rows, blockquotes, and any line inside a fenced code block.",
        "`gate` strips a bullet's leading marker before the checks read its text.",
        "`gate` returns zero by default, whatever it found.",
        "With `--strict`, `gate` returns non-zero when at least one finding has `error` severity.",
        "A warning never changes the exit code."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — only the first matching Contract heading's text is linted Given  a short Contract clause followed by a `## Notes — contract addendum` heading holding a 50-word run-on line When   `_lint_prose` runs against \"contract\" Then   it returns only the short Contract line, never the Notes text",
        "CASE-2 — — a stacked-conditions line under Notes never fires Given  a `confirmed` requirement whose `## Notes & known limitations` section holds a line joined by four \"and\"s When   `lint_requirement` runs on it Then   no `stacked-conditions` finding is reported for that line",
        "CASE-3 — — a blockquote line with four joins is never linted as prose Given  a Contract section whose only content is a stacked-conditions-shaped blockquote line (`>` followed by four conjunctions) When   `_lint_prose` runs against \"contract\" Then   it returns an empty list — the `>` line is skipped, not read as prose",
        "CASE-4 — — a bulleted \"It\" clause still triggers anonymous-subject Given  a Contract bullet written as `- It creates the folder.` When   `lint_requirement` runs on it Then   it reports an `anonymous-subject` warning, proving the `^It` check saw the text after the `- ` marker was stripped",
        "CASE-5 — — a missing-section error does not fail a non-strict run Given  a confirmed requirement with no `## Cases` section When   `cmd_lint` runs without `--strict` Then   it returns exit code 0 despite the error-severity finding",
        "CASE-6 — — a missing section fails a strict run Given  a confirmed requirement with a Contract section but no `## Cases` heading When   `cmd_lint` runs with `--strict` Then   it returns exit code 1 because of the error finding; a warning alone would leave the exit code at 0"
      ],
      "accept": "CASE-1 — only the first matching Contract heading's text is linted\n  Given  a short Contract clause followed by a `## Notes — contract addendum` heading holding a 50-word run-on line\n  When   `_lint_prose` runs against \"contract\"\n  Then   it returns only the short Contract line, never the Notes text\n\nCASE-2 — a stacked-conditions line under Notes never fires\n  Given  a `confirmed` requirement whose `## Notes & known limitations` section holds a line joined by four \"and\"s\n  When   `lint_requirement` runs on it\n  Then   no `stacked-conditions` finding is reported for that line\n\nCASE-3 — a blockquote line with four joins is never linted as prose\n  Given  a Contract section whose only content is a stacked-conditions-shaped blockquote line\n         (`>` followed by four conjunctions)\n  When   `_lint_prose` runs against \"contract\"\n  Then   it returns an empty list — the `>` line is skipped, not read as prose\n\nCASE-4 — a bulleted \"It\" clause still triggers anonymous-subject\n  Given  a Contract bullet written as `- It creates the folder.`\n  When   `lint_requirement` runs on it\n  Then   it reports an `anonymous-subject` warning, proving the `^It` check saw the text after the `- ` marker was stripped\n\nCASE-5 — a missing-section error does not fail a non-strict run\n  Given  a confirmed requirement with no `## Cases` section\n  When   `cmd_lint` runs without `--strict`\n  Then   it returns exit code 0 despite the error-severity finding\n\nCASE-6 — a missing section fails a strict run\n  Given  a confirmed requirement with a Contract section but no `## Cases` heading\n  When   `cmd_lint` runs with `--strict`\n  Then   it returns exit code 1 because of the error finding; a warning alone would leave\n         the exit code at 0",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-LINT-014"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4485"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4520"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2846"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9712"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "De unde citesc verificările de proză, și ce eșuează o rulare strict",
          "intent": "O verificare de proză care ar citi și Notes sau un bloc de cod ar semnala text pe care nimeni nu l-a intenționat drept propoziție lizibilă — o explicație de footgun, un exemplu literal, un nume de flag `--strict`. Limitarea verificărilor exact la secțiunile Contract și Acceptance, și omiterea liniilor non-proză din interiorul lor, păstrează fiecare finding îndreptat spre text pe care un autor chiar trebuie să-l repare.",
          "contract": "> O verificare de proză care ar citi și Notes sau un bloc de cod ar semnala text pe care\n> nimeni nu l-a intenționat drept propoziție lizibilă — o explicație de footgun, un exemplu\n> literal, un nume de flag `--strict`. Limitarea verificărilor exact la secțiunile Contract\n> și Acceptance, și omiterea liniilor non-proză din interiorul lor, păstrează fiecare finding\n> îndreptat spre text pe care un autor chiar trebuie să-l repare.\n\nFiecare bullet de mai jos este obligatoriu.\n- Verificările de proză citesc secțiunile Contract și Acceptance, și nicio altă secțiune.\n- Secțiunea \"Notes & limitations\" este exemptată: doar cititorii aprofundați ajung la ea, și poate\n  rămâne densă.\n- Verificările de proză omit liniile care nu sunt proză — titluri, rânduri de tabel, blockquote-uri,\n  și orice linie dintr-un bloc de cod fenced.\n- `gate` elimină marcatorul de început al unui bullet înainte ca verificările să-i citească textul.\n- `gate` returnează zero implicit, indiferent ce a găsit.\n- Cu `--strict`, `gate` returnează non-zero când cel puțin un finding are severitate `error`.\n- Un avertisment nu schimbă niciodată codul de ieșire.",
          "acceptance": "CASE-1 — este lintat doar textul primului titlu Contract care se potrivește\n  Given  o clauză scurtă de Contract urmată de un titlu `## Notes — contract addendum` care conține o linie run-on de 50 de cuvinte\n  When   rulează `_lint_prose` pe \"contract\"\n  Then   returnează doar linia scurtă de Contract, niciodată textul din Notes\n\nCASE-2 — o linie stacked-conditions din Notes nu se declanșează niciodată\n  Given  o cerință `confirmed` a cărei secțiune `## Notes & known limitations` conține o linie unită prin patru \"and\"-uri\n  When   rulează `lint_requirement` pe ea\n  Then   niciun finding `stacked-conditions` nu este raportat pentru acea linie\n\nCASE-3 — o linie blockquote cu patru joncțiuni nu este niciodată lintată ca proză\n  Given  o secțiune Contract al cărei singur conținut este o linie blockquote în forma stacked-conditions\n         (`>` urmat de patru conjuncții)\n  When   rulează `_lint_prose` pe \"contract\"\n  Then   returnează o listă goală — linia `>` este omisă, nu citită ca proză\n\nCASE-4 — o clauză \"It\" cu bullet declanșează totuși anonymous-subject\n  Given  un bullet de Contract scris ca `- It creates the folder.`\n  When   rulează `lint_requirement` pe ea\n  Then   raportează un avertisment `anonymous-subject`, dovedind că verificarea `^It` a văzut textul de\n         după eliminarea marcatorului `- `\n\nCASE-5 — o eroare missing-section nu eșuează o rulare non-strict\n  Given  o cerință confirmed fără secțiune `## Cases`\n  When   rulează `cmd_lint` fără `--strict`\n  Then   returnează codul de ieșire 0 în ciuda finding-ului de severitate error\n\nCASE-6 — o secțiune lipsă eșuează o rulare strict\n  Given  o cerință confirmed cu secțiune Contract dar fără titlu `## Cases`\n  When   rulează `cmd_lint` cu `--strict`\n  Then   returnează codul de ieșire 1 din cauza finding-ului de eroare; un avertisment singur ar lăsa\n         codul de ieșire la 0"
        }
      }
    },
    {
      "id": "ARCH-LINTCHECKS-025",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Readability & scope checks",
      "intent": "The linter's rulebook: the individual checks that catch hard-to-read or overloaded requirement prose — sentences that run long, lines that stack conditions, bloated or skimpy acceptance lists, contracts that bundle several capabilities, vague quality words, code spread across too many files, and an atomic-form leaf that lists more facts than its Scenario proves. The framework that runs them, scopes them and decides exit codes is [[ARCH-LINT-014]]; this is what each check flags.",
      "contract": [
        "`statement-too-long` and `stacked-conditions` warn on a normative line that runs too long or stacks too many conditions; `anonymous-subject` warns on a Contract clause opening with a bare \"It\". [[REQ-LINTCHECKS-865]] details the behaviour.",
        "`ac-count-low`/`ac-count-high`/`over-scoped` warn on too few or too many acceptance criteria or contract scope units; `file-spread` warns on an implementation scattered across too many files. [[REQ-LINTCHECKS-866]] details the behaviour.",
        "`atomic-bullet-then-mismatch` and `atomic-story-overlong` warn on an atomic-form story quote whose fact count disagrees with its Scenario; `layer-mismatch` warns on a fan-out `bus` requirement nothing depends on. [[REQ-LINTCHECKS-867]] details the behaviour.",
        "`vague-term` warns on a Contract bullet using an untestable quality word from the closed `LINT_VAGUE_TERMS` set. [[REQ-LINTCHECKS-868]] details the behaviour.",
        "`redundant-modal` warns on a Contract bullet using `shall` or `must` — the closed `LINT_MODAL_WORDS` set. [[REQ-LINTCHECKS-869]] details the behaviour."
      ],
      "verify": [],
      "notes": [
        "`statement-too-long` counts sentences and nothing else; words per clause belong to `statement-size`, so neither check reports the same clause for the same reason. It used to count words as well, which flagged a correct two-sentence clause.",
        "A per-SENTENCE word ceiling was removed on 2026-09-03. `long-sentence` had warned on any sentence over 25 words since the `gate` command shipped (2026-06-07), and it was the only check that bounded how long one sentence may run. Nothing replaces it: a clause may now hold three sentences of fifty words each and pass. What remains is the sentence count here and the 150-word clause ceiling in `statement-size` — both per clause, neither per sentence. This is a loosening, recorded so it reads as a decision rather than an oversight.",
        "`file-spread` measures where the code lives; `over-scoped` measures contract/acceptance scope — different axes, so one firing says nothing about the other.",
        "Sentence splitting is a deterministic split on `.`, `!`, and `?` — crude, so an abbreviation or a trailing code span reads as a sentence boundary. It feeds a word count and a sentence count, so the worst case is one extra sentence counted in a clause.",
        "The thresholds are module constants, not configurable from the command line.",
        "`anonymous-subject` reads physical lines, not folded clauses. A wrapped bullet whose continuation line happens to begin with \"It \" is flagged as though that line opened a clause. Rewriting the sentence is the fix; the alternative — folding continuations first — would make every other prose check measure a different unit than the one it measures now.",
        "`ac-count-high` is exempted here because this requirement is a table of checks: each of the nine criteria pins exactly one check's behaviour. Merging them to reach the ceiling would leave checks tested only implicitly, which is the outcome the count exists to prevent. Same reasoning as [[ARCH-CHECK-006]]'s severity table.",
        "`atomic-bullet-then-mismatch`/`atomic-story-overlong` fire on 0 of the corpus's atomic-form requirements at launch — no existing body enumerates more than one `- ` fact in its story quote. [ADR-0022](../../docs/adr/0022-no-minimum-requirement-size-check.md)'s launch discipline (a published fire rate AND a human-confirmed sample) is satisfied only on the fire-rate half here; there is no live finding to sample. This is an explicit, acknowledged gap, not a silent one: the check is deterministic and structural (a bullet either has a matching `Then` or it does not — no size judgement is made), the same class as `missing-section`, not a heuristic threshold like the rejected minimum-size proposal — and `warn`+`STRICT_PROMOTE` (not unconditional error) was chosen specifically so a plain `gate` run stays exactly as quiet as it was before this check existed."
      ],
      "current_impl": [
        "The check bodies live in `lint_requirement` (per-requirement checks) and `_lint_prose` (per-section prose walk) in `reqmap.py`; `_sentences` splits lines for the word and sentence counts.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a confirmed requirement whose Contract bullet uses the word \"appropriate\" When   `gate` runs Then   it reports a `vague-term` warning naming that term",
        "CASE-2 — Given  a normative line joining four clauses with conjunctions, carrying no modal verb When   `gate` runs Then   it reports a `stacked-conditions` warning",
        "CASE-3 — Given  a Contract bullet spanning four sentences When   `gate` runs Then   it reports `statement-too-long`; the same bullet cut to three sentences reports none, and a single 40-word sentence reports nothing, however long it runs",
        "CASE-4 — Given  an Acceptance section with one criterion When   `gate` runs Then   it reports `ac-count-low`; with eight criteria, `ac-count-high`; with four, neither",
        "CASE-5 — Given  a requirement over both ceilings (more than ten contract scope units and more than seven acceptance criteria) When   `gate` runs Then   it reports `over-scoped`; over only one ceiling, none",
        "CASE-6 — Given  a Contract holding thirty clauses under three bold group labels, plus eight acceptance criteria When   `gate` runs Then   it reports no `over-scoped`, because three groups is under the ceiling; the same thirty clauses ungrouped do report it",
        "CASE-7 — Given  a requirement whose `implements` members span three or more distinct files When   `gate` runs with member data Then   it reports `file-spread`; a single file or no member data produce none",
        "CASE-8 — Given  a Contract bullet containing \"appropriate\" and \"user-friendly\" When   `gate` runs Then   it reports two `vague-term` warnings; a backticked span and a precise bullet report none",
        "CASE-9 — Given  a Contract bullet reading \"It creates the folder.\" When   `gate` runs Then   it reports an `anonymous-subject` warning; \"`init` creates the folder.\" reports none, and the same bare \"It\" in an Acceptance criterion reports none",
        "CASE-10 — Given  a Contract bullet reading \"The system shall log the event and must retry once.\" When   `gate` runs Then   it reports two `redundant-modal` warnings (\"shall\" + \"must\"); a backticked `shall_retry` identifier and a plain present-tense bullet report none",
        "CASE-11 — Given  an atomic-form story quote listing 3 `- ` facts and a Scenario with 1 `Then` line When   `gate` runs Then   it reports an `atomic-bullet-then-mismatch` warning, promoted to error under `--strict`; the same story with 3 matching `Then` lines reports none",
        "CASE-12 — Given  an atomic-form story quote listing 4 `- ` facts, one over `LINT_ATOMIC_STORY_BULLETS_MAX` When   `gate` runs Then   it reports `atomic-story-overlong`, not `atomic-bullet-then-mismatch`, regardless of how many `Then` lines the Scenario carries"
      ],
      "accept": "CASE-1\n  Given  a confirmed requirement whose Contract bullet uses the word \"appropriate\"\n  When   `gate` runs\n  Then   it reports a `vague-term` warning naming that term\n\nCASE-2\n  Given  a normative line joining four clauses with conjunctions, carrying no modal verb\n  When   `gate` runs\n  Then   it reports a `stacked-conditions` warning\n\nCASE-3\n  Given  a Contract bullet spanning four sentences\n  When   `gate` runs\n  Then   it reports `statement-too-long`; the same bullet cut to three sentences reports\n         none, and a single 40-word sentence reports nothing, however long it runs\n\nCASE-4\n  Given  an Acceptance section with one criterion\n  When   `gate` runs\n  Then   it reports `ac-count-low`; with eight criteria, `ac-count-high`; with four, neither\n\nCASE-5\n  Given  a requirement over both ceilings (more than ten contract scope units and more\n         than seven acceptance criteria)\n  When   `gate` runs\n  Then   it reports `over-scoped`; over only one ceiling, none\n\nCASE-6\n  Given  a Contract holding thirty clauses under three bold group labels, plus eight\n         acceptance criteria\n  When   `gate` runs\n  Then   it reports no `over-scoped`, because three groups is under the ceiling; the same\n         thirty clauses ungrouped do report it\n\nCASE-7\n  Given  a requirement whose `implements` members span three or more distinct files\n  When   `gate` runs with member data\n  Then   it reports `file-spread`; a single file or no member data produce none\n\nCASE-8\n  Given  a Contract bullet containing \"appropriate\" and \"user-friendly\"\n  When   `gate` runs\n  Then   it reports two `vague-term` warnings; a backticked span and a precise bullet report none\n\nCASE-9\n  Given  a Contract bullet reading \"It creates the folder.\"\n  When   `gate` runs\n  Then   it reports an `anonymous-subject` warning; \"`init` creates the folder.\" reports\n         none, and the same bare \"It\" in an Acceptance criterion reports none\n\nCASE-10\n  Given  a Contract bullet reading \"The system shall log the event and must retry once.\"\n  When   `gate` runs\n  Then   it reports two `redundant-modal` warnings (\"shall\" + \"must\"); a backticked\n         `shall_retry` identifier and a plain present-tense bullet report none\n\nCASE-11\n  Given  an atomic-form story quote listing 3 `- ` facts and a Scenario with 1 `Then` line\n  When   `gate` runs\n  Then   it reports an `atomic-bullet-then-mismatch` warning, promoted to error under\n         `--strict`; the same story with 3 matching `Then` lines reports none\n\nCASE-12\n  Given  an atomic-form story quote listing 4 `- ` facts, one over `LINT_ATOMIC_STORY_BULLETS_MAX`\n  When   `gate` runs\n  Then   it reports `atomic-story-overlong`, not `atomic-bullet-then-mismatch`, regardless of\n         how many `Then` lines the Scenario carries",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001",
        "ARCH-LINT-014"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-QUALITY-104"
      ],
      "satisfied_by": [
        "REQ-LINTCHECKS-865",
        "REQ-LINTCHECKS-866",
        "REQ-LINTCHECKS-867",
        "REQ-LINTCHECKS-868",
        "REQ-LINTCHECKS-869"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4514"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4591"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4980"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2830"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2846"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4437"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.14",
      "priority": "",
      "risks": [],
      "clauses": 12,
      "covered": 12,
      "i18n": {
        "ro": {
          "title": "Verificări de lizibilitate și domeniu",
          "intent": "Cartea de reguli a linter-ului: verificările individuale care prind proza de cerințe greu de citit sau supraîncărcată — propoziții care se lungesc, linii care stivuiesc condiții, liste de acceptare umflate sau firave, contracte care înglobează mai multe capabilități, cuvinte de calitate vagi, cod răspândit pe prea multe fișiere, și o frunză de formă atomică care listează mai multe fapte decât dovedește Scenario-ul ei. Cadrul care le rulează, le limitează domeniul și decide codurile de ieșire este [[ARCH-LINT-014]]; aceasta este ceea ce marchează fiecare verificare.",
          "contract": "> Cartea de reguli a linter-ului: verificările individuale care prind proza de cerințe\n> greu de citit sau supraîncărcată — propoziții care se lungesc, linii care stivuiesc\n> condiții, liste de acceptare umflate sau firave, contracte care înglobează mai multe\n> capabilități, cuvinte de calitate vagi, cod răspândit pe prea multe fișiere, și o\n> frunză de formă atomică care listează mai multe fapte decât dovedește Scenario-ul ei.\n> Cadrul care le rulează, le limitează domeniul și decide codurile de ieșire este\n> [[ARCH-LINT-014]]; aceasta este ceea ce marchează fiecare verificare.\n\nFiecare punct de mai jos este obligatoriu.\n- `statement-too-long` și `stacked-conditions` avertizează pentru o linie normativă care se lungește prea mult sau stivuiește prea multe condiții; `anonymous-subject` avertizează pentru o clauză Contract care se deschide cu un simplu \"It\". [[REQ-LINTCHECKS-865]] detaliază comportamentul.\n- `ac-count-low`/`ac-count-high`/`over-scoped` avertizează pentru prea puține sau prea multe criterii de acceptare sau unități de domeniu de contract; `file-spread` avertizează pentru o implementare răspândită pe prea multe fișiere. [[REQ-LINTCHECKS-866]] detaliază comportamentul.\n- `atomic-bullet-then-mismatch` și `atomic-story-overlong` avertizează pentru un citat-poveste de formă atomică al cărui număr de fapte nu corespunde Scenario-ului său; `layer-mismatch` avertizează pentru o cerință `bus` cu fan-out de care nimic nu depinde. [[REQ-LINTCHECKS-867]] detaliază comportamentul.\n- `vague-term` avertizează pentru un punct Contract care folosește un cuvânt de calitate netestabil din setul închis `LINT_VAGUE_TERMS`. [[REQ-LINTCHECKS-868]] detaliază comportamentul.\n- `redundant-modal` avertizează pentru un punct Contract care folosește `shall` sau `must` — setul închis `LINT_MODAL_WORDS`. [[REQ-LINTCHECKS-869]] detaliază comportamentul.",
          "acceptance": "CASE-1\n  Given  o cerință confirmată al cărei punct Contract folosește cuvântul \"appropriate\"\n  When   `gate` rulează\n  Then   raportează un avertisment `vague-term` care numește acel termen\n\nCASE-2\n  Given  o linie normativă care unește patru clauze prin conjuncții, fără niciun verb modal\n  When   `gate` rulează\n  Then   raportează un avertisment `stacked-conditions`\n\nCASE-3\n  Given  un punct Contract care se întinde pe patru propoziții\n  When   `gate` rulează\n  Then   raportează `statement-too-long`; același punct redus la trei propoziții nu\n         raportează nimic, iar o singură propoziție de 40 de cuvinte nu raportează nimic, oricât de lungă ar fi\n\nCASE-4\n  Given  o secțiune Acceptance cu un criteriu\n  When   `gate` rulează\n  Then   raportează `ac-count-low`; cu opt criterii, `ac-count-high`; cu patru, niciunul\n\nCASE-5\n  Given  o cerință peste ambele plafoane (mai mult de zece unități de domeniu de contract și mai\n         mult de șapte criterii de acceptare)\n  When   `gate` rulează\n  Then   raportează `over-scoped`; peste doar un plafon, niciunul\n\nCASE-6\n  Given  un Contract care conține treizeci de clauze sub trei etichete de grup îngroșate, plus opt\n         criterii de acceptare\n  When   `gate` rulează\n  Then   nu raportează niciun `over-scoped`, deoarece trei grupuri este sub plafon; aceleași\n         treizeci de clauze negrupate o raportează\n\nCASE-7\n  Given  o cerință ai cărei membri `implements` se întind pe trei sau mai multe fișiere distincte\n  When   `gate` rulează cu date de membru\n  Then   raportează `file-spread`; un singur fișier sau lipsa datelor de membru nu produc niciunul\n\nCASE-8\n  Given  un punct Contract care conține \"appropriate\" și \"user-friendly\"\n  When   `gate` rulează\n  Then   raportează două avertismente `vague-term`; un segment încadrat cu backtick și un punct precis nu raportează niciunul\n\nCASE-9\n  Given  un punct Contract care citește \"It creates the folder.\"\n  When   `gate` rulează\n  Then   raportează un avertisment `anonymous-subject`; \"`init` creates the folder.\" nu\n         raportează niciunul, iar același \"It\" simplu într-un criteriu Acceptance nu raportează niciunul\n\nCASE-10\n  Given  un punct Contract care citește \"The system shall log the event and must retry once.\"\n  When   `gate` rulează\n  Then   raportează două avertismente `redundant-modal` (\"shall\" + \"must\"); un identificator\n         `shall_retry` încadrat cu backtick și un punct simplu la timpul prezent nu raportează niciunul\n\nCASE-11\n  Given  un citat-poveste de formă atomică ce listează 3 fapte `- ` și un Scenario cu 1 linie `Then`\n  When   `gate` rulează\n  Then   raportează un avertisment `atomic-bullet-then-mismatch`, promovat la eroare sub\n         `--strict`; aceeași poveste cu 3 linii `Then` corespunzătoare nu raportează niciunul\n\nCASE-12\n  Given  un citat-poveste de formă atomică ce listează 4 fapte `- `, unul peste `LINT_ATOMIC_STORY_BULLETS_MAX`\n  When   `gate` rulează\n  Then   raportează `atomic-story-overlong`, nu `atomic-bullet-then-mismatch`, indiferent de\n         câte linii `Then` poartă Scenario-ul"
        }
      }
    },
    {
      "id": "REQ-LINTCHECKS-865",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Readability checks: length, stacking, anonymous subjects",
      "intent": "A requirement is only useful if a reader can hold its obligation in their head. These checks catch the three ways prose defeats that: a bullet that runs on past a sentence ceiling, a line that stacks several conditions with conjunctions, and a clause that opens with a bare \"It\" instead of naming what does the thing.",
      "contract": [
        "The `statement-too-long` check warns on a Contract bullet spanning more than `LINT_CLAUSE_SENTENCES` sentences, default 3.",
        "The `stacked-conditions` check warns on a normative line that joins at least `LINT_STACKED_CONNECTORS` clauses, default 3, with conjunctions.",
        "`stacked-conditions` reads every normative line. It does not require a `shall` or `must` on the line, because the section already makes the line binding and the authoring voice writes no modal.",
        "The `anonymous-subject` check warns on a Contract clause opening with a bare \"It\" followed by a verb.",
        "`anonymous-subject` reads the Contract only. Acceptance prose may say \"it\" in a Then clause and still read clearly."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — statement-too-long fires past the sentence ceiling Given  a Contract bullet spanning four sentences, one more than `LINT_CLAUSE_SENTENCES` When   `gate` runs Then   it reports a `statement-too-long` warning naming that bullet",
        "CASE-2 — — stacked-conditions fires at the connector ceiling Given  a normative line joining three clauses with conjunctions, meeting `LINT_STACKED_CONNECTORS` When   `gate` runs Then   it reports a `stacked-conditions` warning naming that line",
        "CASE-3 — — stacked-conditions fires on Acceptance text too, with no modal verb Given  an Acceptance criterion joining three clauses with conjunctions and no `shall`/`must` When   `gate` runs Then   it reports `stacked-conditions`, because any normative line counts, modal or not",
        "CASE-4 — — anonymous-subject fires on a bare \"It\" opener Given  a Contract clause reading \"It logs the event.\" When   `gate` runs Then   it reports an `anonymous-subject` warning naming that clause",
        "CASE-5 — — anonymous-subject ignores Acceptance prose Given  an Acceptance Then clause reading \"it exits non-zero\" When   `gate` runs Then   it reports no `anonymous-subject` warning for that line --------------------"
      ],
      "accept": "CASE-1 — statement-too-long fires past the sentence ceiling\n  Given  a Contract bullet spanning four sentences, one more than `LINT_CLAUSE_SENTENCES`\n  When   `gate` runs\n  Then   it reports a `statement-too-long` warning naming that bullet\n\nCASE-2 — stacked-conditions fires at the connector ceiling\n  Given  a normative line joining three clauses with conjunctions, meeting `LINT_STACKED_CONNECTORS`\n  When   `gate` runs\n  Then   it reports a `stacked-conditions` warning naming that line\n\nCASE-3 — stacked-conditions fires on Acceptance text too, with no modal verb\n  Given  an Acceptance criterion joining three clauses with conjunctions and no `shall`/`must`\n  When   `gate` runs\n  Then   it reports `stacked-conditions`, because any normative line counts, modal or not\n\nCASE-4 — anonymous-subject fires on a bare \"It\" opener\n  Given  a Contract clause reading \"It logs the event.\"\n  When   `gate` runs\n  Then   it reports an `anonymous-subject` warning naming that clause\n\nCASE-5 — anonymous-subject ignores Acceptance prose\n  Given  an Acceptance Then clause reading \"it exits non-zero\"\n  When   `gate` runs\n  Then   it reports no `anonymous-subject` warning for that line\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-LINTCHECKS-025"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4514"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4591"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2846"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Verificări de lizibilitate: lungime, stivuire, subiecte anonime",
          "intent": "O cerință este utilă doar dacă un cititor îi poate ține obligația în minte. Aceste verificări prind cele trei moduri în care proza distruge asta: un bullet care se întinde peste un plafon de propoziții, o linie care stivuiește mai multe condiții cu conjuncții, și o clauză care începe cu un \"It\" simplu în loc să numească ce face lucrul respectiv.",
          "contract": "> O cerință este utilă doar dacă un cititor îi poate ține obligația în minte. Aceste\n> verificări prind cele trei moduri în care proza distruge asta: un bullet care se\n> întinde peste un plafon de propoziții, o linie care stivuiește mai multe condiții\n> cu conjuncții, și o clauză care începe cu un \"It\" simplu în loc să numească ce face\n> lucrul respectiv.\n\nFiecare bullet de mai jos este obligatoriu.\n- Verificarea `statement-too-long` avertizează pe un bullet de Contract care se întinde peste\n  `LINT_CLAUSE_SENTENCES` propoziții, implicit 3.\n- Verificarea `stacked-conditions` avertizează pe o linie normativă care unește cel puțin\n  `LINT_STACKED_CONNECTORS` clauze, implicit 3, cu conjuncții.\n- `stacked-conditions` citește fiecare linie normativă. Nu cere un `shall` sau `must` pe\n  linie, deoarece secțiunea deja face linia obligatorie iar vocea de redactare nu scrie niciun modal.\n- Verificarea `anonymous-subject` avertizează pe o clauză de Contract care începe cu un \"It\" simplu\n  urmat de un verb.\n- `anonymous-subject` citește doar Contract-ul. Proza din Acceptance poate spune \"it\" într-o\n  clauză Then și tot se citește clar.",
          "acceptance": "CASE-1 — statement-too-long se declanșează peste plafonul de propoziții\n  Given  un bullet de Contract întins pe patru propoziții, una peste `LINT_CLAUSE_SENTENCES`\n  When   rulează `gate`\n  Then   raportează un avertisment `statement-too-long` care numește acel bullet\n\nCASE-2 — stacked-conditions se declanșează la plafonul de conectori\n  Given  o linie normativă care unește trei clauze cu conjuncții, atingând `LINT_STACKED_CONNECTORS`\n  When   rulează `gate`\n  Then   raportează un avertisment `stacked-conditions` care numește acea linie\n\nCASE-3 — stacked-conditions se declanșează și pe text din Acceptance, fără verb modal\n  Given  un criteriu de Acceptance care unește trei clauze cu conjuncții și fără `shall`/`must`\n  When   rulează `gate`\n  Then   raportează `stacked-conditions`, deoarece orice linie normativă contează, modală sau nu\n\nCASE-4 — anonymous-subject se declanșează pe un opener \"It\" simplu\n  Given  o clauză de Contract care se citește \"It logs the event.\"\n  When   rulează `gate`\n  Then   raportează un avertisment `anonymous-subject` care numește acea clauză\n\nCASE-5 — anonymous-subject ignoră proza din Acceptance\n  Given  o clauză Then din Acceptance care se citește \"it exits non-zero\"\n  When   rulează `gate`\n  Then   nu raportează niciun avertisment `anonymous-subject` pentru acea linie\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-LINTCHECKS-866",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Scope checks: acceptance count, over-scoping, file spread",
      "intent": "A requirement can be under-specified (too few criteria to trust) or a grab-bag (too many obligations, or code scattered too widely to be one thing). These checks bound both ends: acceptance-count ceilings and floors, a combined contract+acceptance over-scoped signal, and a file-spread signal for implementations diffused across too many files.",
      "contract": [
        "The `ac-count-low` check warns on an Acceptance section holding fewer than `LINT_AC_MIN`, default 3, criteria.",
        "The `ac-count-high` check warns on more than `LINT_AC_MAX`, default 7, criteria, counted as `- ` bullets or `AC-N` labelled blocks.",
        "The `over-scoped` check warns on a requirement over both ceilings at once: more than `LINT_CONTRACT_MAX` scope units, default 10, with more than `LINT_AC_MAX` criteria.",
        "`over-scoped` counts clause groups when the Contract carries bold group labels, and counts clauses when it does not. Writing one obligation per bullet multiplies clauses without widening scope, so counting clauses alone would punish the authoring voice.",
        "The `file-spread` check warns on a requirement whose `implements` members span at least `LINT_FILE_SPREAD_MAX`, default 3, distinct files.",
        "`file-spread` is an architectural-diffuseness signal and is skipped when no member data is supplied."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — ac-count-low fires under the minimum Given  an Acceptance section holding two criteria, one under `LINT_AC_MIN` When   `gate` runs Then   it reports an `ac-count-low` warning",
        "CASE-2 — — ac-count-high fires over the maximum, either counting style Given  an Acceptance section holding eight `AC-N` blocks, one over `LINT_AC_MAX` When   `gate` runs Then   it reports `ac-count-high`; eight `- ` bullets trigger the same warning",
        "CASE-3 — — over-scoped fires only when both ceilings are crossed Given  a Contract with eleven scope units and an Acceptance section with eight criteria When   `gate` runs Then   it reports `over-scoped`; crossing only one ceiling reports none",
        "CASE-4 — — over-scoped counts bold groups, not raw clauses Given  a Contract holding thirty clauses under three bold group labels When   `gate` runs Then   it reports no `over-scoped`, because the scope-unit count is three groups, not thirty",
        "CASE-5 — — file-spread fires at the distinct-file ceiling Given  a requirement whose `implements:` members name three distinct files When   `gate` runs with member data Then   it reports a `file-spread` warning",
        "CASE-6 — — file-spread is silent without member data Given  a requirement whose members span three files, but `gate` runs with no member data When   `gate` runs Then   it reports no `file-spread` warning --------------------"
      ],
      "accept": "CASE-1 — ac-count-low fires under the minimum\n  Given  an Acceptance section holding two criteria, one under `LINT_AC_MIN`\n  When   `gate` runs\n  Then   it reports an `ac-count-low` warning\n\nCASE-2 — ac-count-high fires over the maximum, either counting style\n  Given  an Acceptance section holding eight `AC-N` blocks, one over `LINT_AC_MAX`\n  When   `gate` runs\n  Then   it reports `ac-count-high`; eight `- ` bullets trigger the same warning\n\nCASE-3 — over-scoped fires only when both ceilings are crossed\n  Given  a Contract with eleven scope units and an Acceptance section with eight criteria\n  When   `gate` runs\n  Then   it reports `over-scoped`; crossing only one ceiling reports none\n\nCASE-4 — over-scoped counts bold groups, not raw clauses\n  Given  a Contract holding thirty clauses under three bold group labels\n  When   `gate` runs\n  Then   it reports no `over-scoped`, because the scope-unit count is three groups, not thirty\n\nCASE-5 — file-spread fires at the distinct-file ceiling\n  Given  a requirement whose `implements:` members name three distinct files\n  When   `gate` runs with member data\n  Then   it reports a `file-spread` warning\n\nCASE-6 — file-spread is silent without member data\n  Given  a requirement whose members span three files, but `gate` runs with no member data\n  When   `gate` runs\n  Then   it reports no `file-spread` warning\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-LINTCHECKS-025"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4591"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2846"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Verificări de scope: numărul acceptance, over-scoping, răspândirea pe fișiere",
          "intent": "O cerință poate fi sub-specificată (prea puține criterii ca să fie de încredere) sau un grab-bag (prea multe obligații, sau cod împrăștiat pe prea multe fișiere ca să fie un singur lucru). Aceste verificări limitează ambele capete: plafoane și praguri minime pe numărul acceptance, un semnal combinat over-scoped pentru contract+acceptance, și un semnal de răspândire pe fișiere pentru implementări difuzate pe prea multe fișiere.",
          "contract": "> O cerință poate fi sub-specificată (prea puține criterii ca să fie de încredere) sau\n> un grab-bag (prea multe obligații, sau cod împrăștiat pe prea multe fișiere ca să fie un\n> singur lucru). Aceste verificări limitează ambele capete: plafoane și praguri minime pe\n> numărul acceptance, un semnal combinat over-scoped pentru contract+acceptance, și un\n> semnal de răspândire pe fișiere pentru implementări difuzate pe prea multe fișiere.\n\nFiecare bullet de mai jos este obligatoriu.\n- Verificarea `ac-count-low` avertizează pe o secțiune Acceptance care conține mai puțin de\n  `LINT_AC_MIN`, implicit 3, criterii.\n- Verificarea `ac-count-high` avertizează pe mai mult de `LINT_AC_MAX`, implicit 7, criterii,\n  numărate ca bullets `- ` sau blocuri etichetate `AC-N`.\n- Verificarea `over-scoped` avertizează pe o cerință care depășește ambele plafoane deodată: mai mult\n  de `LINT_CONTRACT_MAX` unități de scope, implicit 10, cu mai mult de `LINT_AC_MAX` criterii.\n- `over-scoped` numără grupurile de clauze când Contract-ul poartă etichete de grup bold, și numără\n  clauzele când nu poartă. Scrierea unei obligații per bullet multiplică clauzele fără a lărgi\n  scope-ul, deci a număra doar clauzele ar pedepsi vocea de redactare.\n- Verificarea `file-spread` avertizează pe o cerință ai cărei membri `implements` se întind pe cel puțin\n  `LINT_FILE_SPREAD_MAX`, implicit 3, fișiere distincte.\n- `file-spread` este un semnal de difuzitate arhitecturală și este omisă când nu sunt furnizate date de membri.",
          "acceptance": "CASE-1 — ac-count-low se declanșează sub minim\n  Given  o secțiune Acceptance care conține două criterii, unul sub `LINT_AC_MIN`\n  When   rulează `gate`\n  Then   raportează un avertisment `ac-count-low`\n\nCASE-2 — ac-count-high se declanșează peste maxim, în oricare stil de numărare\n  Given  o secțiune Acceptance care conține opt blocuri `AC-N`, unul peste `LINT_AC_MAX`\n  When   rulează `gate`\n  Then   raportează `ac-count-high`; opt bullets `- ` declanșează același avertisment\n\nCASE-3 — over-scoped se declanșează doar când ambele plafoane sunt depășite\n  Given  un Contract cu unsprezece unități de scope și o secțiune Acceptance cu opt criterii\n  When   rulează `gate`\n  Then   raportează `over-scoped`; depășirea doar unui plafon nu raportează niciunul\n\nCASE-4 — over-scoped numără grupurile bold, nu clauzele brute\n  Given  un Contract care conține treizeci de clauze sub trei etichete de grup bold\n  When   rulează `gate`\n  Then   nu raportează niciun `over-scoped`, deoarece numărul de unități de scope este trei grupuri, nu treizeci\n\nCASE-5 — file-spread se declanșează la plafonul de fișiere distincte\n  Given  o cerință ai cărei membri `implements:` numesc trei fișiere distincte\n  When   rulează `gate` cu date de membri\n  Then   raportează un avertisment `file-spread`\n\nCASE-6 — file-spread este tăcut fără date de membri\n  Given  o cerință ai cărei membri se întind pe trei fișiere, dar `gate` rulează fără date de membri\n  When   rulează `gate`\n  Then   nu raportează niciun avertisment `file-spread`\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-LINTCHECKS-867",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Atomic-form parity and layer-mismatch checks",
      "intent": "An atomic-form story quote is a promise that its facts and its Scenario's `Then` lines agree, and a `bus` requirement is a promise that something actually depends on it. These checks catch both promises breaking: a story that lists more facts than its Scenario proves (or simply lists too many), and a `bus`-layer requirement with fan-out but no fan-in.",
      "contract": [
        "The `atomic-bullet-then-mismatch` check warns on an atomic-form story quote that lists more than one `- ` fact when the count of `Then` lines in the Scenario does not equal the bullet count.",
        "The `atomic-story-overlong` check warns on an atomic-form story quote listing more than `LINT_ATOMIC_STORY_BULLETS_MAX`, default 3, `- ` facts — beyond that it is no longer one obligation.",
        "Both checks are promoted to error under `lint --strict`, the same mechanism as `ac-count-high` and `over-scoped`.",
        "The `layer-mismatch` check warns on a `layer: bus` requirement that nothing depends on and that itself depends on at least `LINT_BUS_FANOUT_MIN`, default 3, requirements.",
        "`layer-mismatch` is skipped when no fan-in data is supplied."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — atomic-bullet-then-mismatch fires when bullets and Then lines disagree Given  an atomic-form story quote listing 3 `- ` facts and a Scenario with 1 `Then` line When   `gate` runs Then   it reports an `atomic-bullet-then-mismatch` warning; the same story with 3 matching `Then` lines reports none",
        "CASE-2 — — atomic-story-overlong fires over the ceiling regardless of Then count Given  an atomic-form story quote listing 4 `- ` facts, one over the ceiling When   `gate` runs Then   it reports `atomic-story-overlong`, not `atomic-bullet-then-mismatch`, even when the Scenario carries 4 matching `Then` lines",
        "CASE-3 — — layer-mismatch fires on a fan-out bus with no dependents Given  a `layer: bus` requirement nothing depends on, itself depending on three others When   `gate` runs with fan-in data Then   it reports a `layer-mismatch` warning",
        "CASE-4 — — layer-mismatch is silent without fan-in data Given  the same fan-out bus requirement, but `gate` runs with no fan-in data supplied When   `gate` runs Then   it reports no `layer-mismatch` warning --------------------"
      ],
      "accept": "CASE-1 — atomic-bullet-then-mismatch fires when bullets and Then lines disagree\n  Given  an atomic-form story quote listing 3 `- ` facts and a Scenario with 1 `Then` line\n  When   `gate` runs\n  Then   it reports an `atomic-bullet-then-mismatch` warning; the same story with 3 matching\n         `Then` lines reports none\n\nCASE-2 — atomic-story-overlong fires over the ceiling regardless of Then count\n  Given  an atomic-form story quote listing 4 `- ` facts, one over the ceiling\n  When   `gate` runs\n  Then   it reports `atomic-story-overlong`, not `atomic-bullet-then-mismatch`, even when\n         the Scenario carries 4 matching `Then` lines\n\nCASE-3 — layer-mismatch fires on a fan-out bus with no dependents\n  Given  a `layer: bus` requirement nothing depends on, itself depending on three others\n  When   `gate` runs with fan-in data\n  Then   it reports a `layer-mismatch` warning\n\nCASE-4 — layer-mismatch is silent without fan-in data\n  Given  the same fan-out bus requirement, but `gate` runs with no fan-in data supplied\n  When   `gate` runs\n  Then   it reports no `layer-mismatch` warning\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-LINTCHECKS-025"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4591"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4437"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Paritatea formei atomice și verificări de layer-mismatch",
          "intent": "Un citat de poveste în formă atomică este o promisiune că faptele sale și liniile `Then` din Scenario-ul său sunt de acord, iar o cerință `bus` este o promisiune că ceva chiar depinde de ea. Aceste verificări prind ambele promisiuni când se rup: o poveste care listează mai multe fapte decât dovedește Scenario-ul ei (sau pur și simplu listează prea multe), și o cerință de layer `bus` cu fan-out dar fără fan-in.",
          "contract": "> Un citat de poveste în formă atomică este o promisiune că faptele sale și liniile\n> `Then` din Scenario-ul său sunt de acord, iar o cerință `bus` este o promisiune că ceva\n> chiar depinde de ea. Aceste verificări prind ambele promisiuni când se rup: o poveste\n> care listează mai multe fapte decât dovedește Scenario-ul ei (sau pur și simplu\n> listează prea multe), și o cerință de layer `bus` cu fan-out dar fără fan-in.\n\nFiecare bullet de mai jos este obligatoriu.\n- Verificarea `atomic-bullet-then-mismatch` avertizează pe un citat de poveste în formă atomică ce\n  listează mai mult de un fapt `- ` când numărul de linii `Then` din Scenario nu este egal cu\n  numărul de bullets.\n- Verificarea `atomic-story-overlong` avertizează pe un citat de poveste în formă atomică ce listează\n  mai mult de `LINT_ATOMIC_STORY_BULLETS_MAX`, implicit 3, fapte `- ` — dincolo de asta nu mai este\n  o singură obligație.\n- Ambele verificări sunt promovate la error sub `lint --strict`, același mecanism ca\n  `ac-count-high` și `over-scoped`.\n- Verificarea `layer-mismatch` avertizează pe o cerință `layer: bus` de care nimic nu depinde și\n  care ea însăși depinde de cel puțin `LINT_BUS_FANOUT_MIN`, implicit 3, cerințe.\n- `layer-mismatch` este omisă când nu sunt furnizate date de fan-in.",
          "acceptance": "CASE-1 — atomic-bullet-then-mismatch se declanșează când bullets și liniile Then nu sunt de acord\n  Given  un citat de poveste în formă atomică listând 3 fapte `- ` și un Scenario cu 1 linie `Then`\n  When   rulează `gate`\n  Then   raportează un avertisment `atomic-bullet-then-mismatch`; aceeași poveste cu 3 linii\n         `Then` care se potrivesc nu raportează niciunul\n\nCASE-2 — atomic-story-overlong se declanșează peste plafon indiferent de numărul de Then\n  Given  un citat de poveste în formă atomică listând 4 fapte `- `, unul peste plafon\n  When   rulează `gate`\n  Then   raportează `atomic-story-overlong`, nu `atomic-bullet-then-mismatch`, chiar dacă\n         Scenario-ul poartă 4 linii `Then` care se potrivesc\n\nCASE-3 — layer-mismatch se declanșează pe un bus cu fan-out fără dependenți\n  Given  o cerință `layer: bus` de care nimic nu depinde, ea însăși depinzând de alte trei\n  When   rulează `gate` cu date de fan-in\n  Then   raportează un avertisment `layer-mismatch`\n\nCASE-4 — layer-mismatch este tăcut fără date de fan-in\n  Given  aceeași cerință bus cu fan-out, dar `gate` rulează fără date de fan-in furnizate\n  When   rulează `gate`\n  Then   nu raportează niciun avertisment `layer-mismatch`\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-LINTCHECKS-868",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The vague-term check",
      "intent": "\"Appropriate\", \"robust\", \"user-friendly\" — words that sound like a requirement but commit to nothing testable. `vague-term` flags any Contract bullet using one from a closed word list, so a reviewer catches the sentence that reads fine and means nothing before it ships as a contract.",
      "contract": [
        "The `vague-term` check warns on a Contract bullet using a word from the closed `LINT_VAGUE_TERMS` set — untestable quality words such as `appropriate`.",
        "Backticked code spans are stripped before the `vague-term` scan runs.",
        "`vague-term` emits one finding per distinct term."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — vague-term fires on a closed-set word Given  a Contract bullet reading \"logs an appropriate message\" When   `gate` runs Then   it reports a `vague-term` warning naming \"appropriate\"",
        "CASE-2 — — vague-term ignores a backticked span Given  a Contract bullet containing only the backticked identifier `` `appropriate_flag` `` When   `gate` runs Then   it reports no `vague-term` warning",
        "CASE-3 — — vague-term reports each distinct term once Given  a Contract bullet using \"appropriate\" twice and \"user-friendly\" once When   `gate` runs Then   it reports two `vague-term` findings, one per distinct term --------------------"
      ],
      "accept": "CASE-1 — vague-term fires on a closed-set word\n  Given  a Contract bullet reading \"logs an appropriate message\"\n  When   `gate` runs\n  Then   it reports a `vague-term` warning naming \"appropriate\"\n\nCASE-2 — vague-term ignores a backticked span\n  Given  a Contract bullet containing only the backticked identifier `` `appropriate_flag` ``\n  When   `gate` runs\n  Then   it reports no `vague-term` warning\n\nCASE-3 — vague-term reports each distinct term once\n  Given  a Contract bullet using \"appropriate\" twice and \"user-friendly\" once\n  When   `gate` runs\n  Then   it reports two `vague-term` findings, one per distinct term\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-LINTCHECKS-025"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4591"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2846"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Verificarea vague-term",
          "intent": "\"Appropriate\", \"robust\", \"user-friendly\" — cuvinte care sună ca o cerință dar nu se angajează la nimic testabil. `vague-term` semnalează orice bullet de Contract care folosește unul dintr-o listă închisă de cuvinte, astfel încât un reviewer prinde propoziția care se citește bine și nu înseamnă nimic înainte să fie livrată ca un contract.",
          "contract": "> \"Appropriate\", \"robust\", \"user-friendly\" — cuvinte care sună ca o cerință dar nu se\n> angajează la nimic testabil. `vague-term` semnalează orice bullet de Contract care\n> folosește unul dintr-o listă închisă de cuvinte, astfel încât un reviewer prinde\n> propoziția care se citește bine și nu înseamnă nimic înainte să fie livrată ca un\n> contract.\n\nFiecare bullet de mai jos este obligatoriu.\n- Verificarea `vague-term` avertizează pe un bullet de Contract care folosește un cuvânt din setul\n  închis `LINT_VAGUE_TERMS` — cuvinte de calitate netestabile precum `appropriate`.\n- Blocurile de cod cu backtick sunt eliminate înainte să ruleze scanarea `vague-term`.\n- `vague-term` emite un finding per termen distinct.",
          "acceptance": "CASE-1 — vague-term se declanșează pe un cuvânt din setul închis\n  Given  un bullet de Contract care se citește \"logs an appropriate message\"\n  When   rulează `gate`\n  Then   raportează un avertisment `vague-term` care numește \"appropriate\"\n\nCASE-2 — vague-term ignoră un span cu backtick\n  Given  un bullet de Contract conținând doar identificatorul cu backtick `` `appropriate_flag` ``\n  When   rulează `gate`\n  Then   nu raportează niciun avertisment `vague-term`\n\nCASE-3 — vague-term raportează fiecare termen distinct o singură dată\n  Given  un bullet de Contract care folosește \"appropriate\" de două ori și \"user-friendly\" o dată\n  When   rulează `gate`\n  Then   raportează două findings `vague-term`, unul per termen distinct\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-LINTCHECKS-869",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The redundant-modal check",
      "intent": "A Contract section is binding by construction — the heading already says so — so \"shall\" and \"must\" inside it add nothing but legalese. `redundant-modal` flags either word so a bullet reads as a plain, present-tense statement of fact instead of a contract clause pretending to be a legal document.",
      "contract": [
        "The `redundant-modal` check warns on a Contract bullet using `shall` or `must` — the closed `LINT_MODAL_WORDS` set.",
        "Backticked code spans are stripped before the `redundant-modal` scan runs, same as `vague-term`.",
        "`redundant-modal` emits one finding per distinct term."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — redundant-modal fires on shall or must Given  a Contract bullet reading \"The system shall retry once.\" When   `gate` runs Then   it reports a `redundant-modal` warning naming \"shall\"",
        "CASE-2 — — redundant-modal ignores a backticked span Given  a Contract bullet containing only the backticked identifier `` `shall_retry` `` When   `gate` runs Then   it reports no `redundant-modal` warning",
        "CASE-3 — — redundant-modal reports each distinct term once Given  a Contract bullet reading \"The system shall log the event and must retry once.\" When   `gate` runs Then   it reports two `redundant-modal` findings, one for \"shall\" and one for \"must\""
      ],
      "accept": "CASE-1 — redundant-modal fires on shall or must\n  Given  a Contract bullet reading \"The system shall retry once.\"\n  When   `gate` runs\n  Then   it reports a `redundant-modal` warning naming \"shall\"\n\nCASE-2 — redundant-modal ignores a backticked span\n  Given  a Contract bullet containing only the backticked identifier `` `shall_retry` ``\n  When   `gate` runs\n  Then   it reports no `redundant-modal` warning\n\nCASE-3 — redundant-modal reports each distinct term once\n  Given  a Contract bullet reading \"The system shall log the event and must retry once.\"\n  When   `gate` runs\n  Then   it reports two `redundant-modal` findings, one for \"shall\" and one for \"must\"",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-LINTCHECKS-025"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4591"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2846"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Verificarea redundant-modal",
          "intent": "O secțiune Contract este obligatorie prin construcție — titlul deja o spune — deci \"shall\" și \"must\" în interiorul ei nu adaugă nimic altceva decât jargon juridic. `redundant-modal` semnalează oricare din cele două cuvinte astfel încât un bullet se citește ca o afirmație simplă, la timpul prezent, în loc de o clauză de contract care se preface a fi un document legal.",
          "contract": "> O secțiune Contract este obligatorie prin construcție — titlul deja o spune — deci\n> \"shall\" și \"must\" în interiorul ei nu adaugă nimic altceva decât jargon juridic.\n> `redundant-modal` semnalează oricare din cele două cuvinte astfel încât un bullet se\n> citește ca o afirmație simplă, la timpul prezent, în loc de o clauză de contract care\n> se preface a fi un document legal.\n\nFiecare bullet de mai jos este obligatoriu.\n- Verificarea `redundant-modal` avertizează pe un bullet de Contract care folosește `shall` sau\n  `must` — setul închis `LINT_MODAL_WORDS`.\n- Blocurile de cod cu backtick sunt eliminate înainte să ruleze scanarea `redundant-modal`, la fel\n  ca la `vague-term`.\n- `redundant-modal` emite un finding per termen distinct.",
          "acceptance": "CASE-1 — redundant-modal se declanșează pe shall sau must\n  Given  un bullet de Contract care se citește \"The system shall retry once.\"\n  When   rulează `gate`\n  Then   raportează un avertisment `redundant-modal` care numește \"shall\"\n\nCASE-2 — redundant-modal ignoră un span cu backtick\n  Given  un bullet de Contract conținând doar identificatorul cu backtick `` `shall_retry` ``\n  When   rulează `gate`\n  Then   nu raportează niciun avertisment `redundant-modal`\n\nCASE-3 — redundant-modal raportează fiecare termen distinct o singură dată\n  Given  un bullet de Contract care se citește \"The system shall log the event and must retry once.\"\n  When   rulează `gate`\n  Then   raportează două findings `redundant-modal`, unul pentru \"shall\" și unul pentru \"must\""
        }
      }
    },
    {
      "id": "ARCH-MAP-007",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Requirement graph (_map.json)",
      "intent": "A list of requirement files tells you what exists but not how it all connects. This builds the graph: one node per requirement, one edge per dependency, each node carrying what the requirement says, what code backs it and what is at risk. Everything that draws or publishes a picture reads this file; nothing draws from the corpus directly.",
      "contract": [
        "`sync` generates `_map.json` under `requirements/`, one node per requirement and one edge per `depends_on`. [[REQ-MAP-870]]",
        "`_map.json` carries top-level `repo`, `engine_version` and `todos` fields; `repo`/`engine_version` are excluded from the freshness diff since each varies with the build environment, not the corpus. [[REQ-MAP-871]]",
        "Reading a requirement's clauses folds a wrapped line back into the clause above it, so a multi-line clause is never truncated to its first physical line. [[REQ-MAP-872]]",
        "The `intent` field carries a requirement's first blockquote, joined into one line, and is empty when that quote just repeats the Contract. [[REQ-MAP-873]]"
      ],
      "verify": [],
      "notes": [
        "`_map.json` is the committed source of the map's data; the diagrams in `_map.md` and the inlined viewer are derived from it and regenerated by `map`.",
        "`map` writes `_map.json` even when the corpus carries a character that has no UTF-8 encoding, replacing that character with U+FFFD.",
        "Split out of this requirement on 2026-09-02: the four Mermaid diagrams, which are a rendering of this graph rather than a property of it ([[ARCH-MAPDIAGRAMS-055]]). The HTML viewer ([[ARCH-VIEWER-007]]) and the Pages publish/gate ([[ARCH-PAGES-021]]) were already separate and consume `_map.json` and `_map.html`."
      ],
      "current_impl": [
        "`cmd_map`, `_assemble_map_data`, `_build_map_data`, `render_json`, `_build_json_text`, `_repo_name`, `_utf8_safe`, `_map_check`, `_stale_artifacts` and the body readers (`_title`, `_first_quote`, `_section`, `_bullets`, `_acc_items`) in `reqmap.py`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  the corpus When   `map` runs Then   the generated graph contains one node per requirement and one edge per `depends_on`",
        "CASE-2 — Given  the generated `_map.json` When   it is parsed Then   it yields `{engine_version, repo, nodes, edges, todos}` with one node per requirement carrying its members and risk signals. The `repo` field is the project's `owner/repo` (or directory name, or null) and is omitted from the freshness comparison.",
        "CASE-3 — Given  a requirement id/title containing a quote, `</script>`, or a lone surrogate When   `map` runs Then   it round-trips through `_map.json` as data (no injection), a lone surrogate is written as U+FFFD rather than failing the write, and a node with no members reports an empty member list",
        "CASE-4 — Given  an atomic requirement whose quote and single Contract clause are the same text When   `map` runs Then   that node's `intent` is empty and its `contract` still carries the clause",
        "CASE-5 — Given  a sectioned requirement whose quote is rationale distinct from its clauses When   `map` runs Then   that node's `intent` carries the quote unchanged"
      ],
      "accept": "CASE-1\n  Given  the corpus\n  When   `map` runs\n  Then   the generated graph contains one node per requirement and one edge per `depends_on`\n\nCASE-2\n  Given  the generated `_map.json`\n  When   it is parsed\n  Then   it yields `{engine_version, repo, nodes, edges, todos}` with one node per requirement carrying\n         its members and risk signals. The `repo` field is the project's `owner/repo` (or\n         directory name, or null) and is omitted from the freshness comparison.\n\nCASE-3\n  Given  a requirement id/title containing a quote, `</script>`, or a lone surrogate\n  When   `map` runs\n  Then   it round-trips through `_map.json` as data (no injection), a lone surrogate is\n         written as U+FFFD rather than failing the write, and a node with no members\n         reports an empty member list\n\nCASE-4\n  Given  an atomic requirement whose quote and single Contract clause are the same text\n  When   `map` runs\n  Then   that node's `intent` is empty and its `contract` still carries the clause\n\nCASE-5\n  Given  a sectioned requirement whose quote is rationale distinct from its clauses\n  When   `map` runs\n  Then   that node's `intent` carries the quote unchanged",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001",
        "ARCH-SCAN-002"
      ],
      "used_by": [
        "ARCH-CONTEXT-048",
        "ARCH-INIT-012",
        "ARCH-MAPDIAGRAMS-055",
        "ARCH-NEXT-013",
        "ARCH-PAGES-021",
        "ARCH-SITE-026",
        "ARCH-TRANSLATE-044",
        "ARCH-VIEWER-007"
      ],
      "satisfies": [
        "SYS-VISUAL-106"
      ],
      "satisfied_by": [
        "REQ-MAP-870",
        "REQ-MAP-871",
        "REQ-MAP-872",
        "REQ-MAP-873"
      ],
      "members": [
        {
          "role": "generated-from",
          "loc": "docs/full_architecture.html:4"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1810"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2829"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3960"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4206"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4235"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6163"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6218"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6233"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6241"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6261"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6284"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6306"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6327"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6343"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6480"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6486"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6845"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7335"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7354"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7375"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1379"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1425"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1678"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2052"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2240"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3973"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4144"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:5423"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:5781"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6338"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6749"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8809"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.04",
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Graful de cerințe (_map.json)",
          "intent": "O listă de fișiere de cerințe îți spune ce există, dar nu și cum se leagă totul. Aceasta construiește graful: un nod per cerință, o muchie per dependență, fiecare nod purtând ce spune cerința, ce cod o susține și ce este în pericol. Tot ce desenează sau publică o imagine citește acest fișier; nimic nu desenează direct din corpus.",
          "contract": "> O listă de fișiere de cerințe îți spune ce există, dar nu și cum se leagă totul.\n> Aceasta construiește graful: un nod per cerință, o muchie per dependență, fiecare nod\n> purtând ce spune cerința, ce cod o susține și ce este în pericol. Tot ce desenează sau\n> publică o imagine citește acest fișier; nimic nu desenează direct din corpus.\n\nFiecare punct de mai jos este obligatoriu.\n- `sync` generează `_map.json` sub `requirements/`, un nod per cerință și o muchie per `depends_on`. [[REQ-MAP-870]]\n- `_map.json` poartă câmpurile de nivel superior `repo`, `engine_version` și `todos`; `repo`/`engine_version` sunt excluse din diff-ul de prospețime deoarece fiecare variază cu mediul de build, nu cu corpusul. [[REQ-MAP-871]]\n- Citirea clauzelor unei cerințe pliază o linie înfășurată înapoi în clauza de deasupra ei, astfel încât o clauză pe mai multe linii nu este niciodată trunchiată la prima ei linie fizică. [[REQ-MAP-872]]\n- Câmpul `intent` poartă primul citat al unei cerințe, unit pe o singură linie, și este gol atunci când acel citat doar repetă Contractul. [[REQ-MAP-873]]",
          "acceptance": "CASE-1\n  Given  corpusul\n  When   `map` rulează\n  Then   graful generat conține un nod per cerință și o muchie per `depends_on`\n\nCASE-2\n  Given  `_map.json` generat\n  When   este analizat\n  Then   produce `{engine_version, repo, nodes, edges, todos}` cu un nod per cerință care poartă\n         membrii și semnalele de risc. Câmpul `repo` este `owner/repo` al proiectului (sau\n         numele directorului, sau null) și este omis din comparația de prospețime.\n\nCASE-3\n  Given  un id/titlu de cerință care conține un ghilimele, `</script>`, sau un surogat izolat\n  When   `map` rulează\n  Then   face round-trip prin `_map.json` ca date (fără injecție), un surogat izolat este\n         scris ca U+FFFD în loc să facă scrierea să eșueze, iar un nod fără membri\n         raportează o listă de membri goală\n\nCASE-4\n  Given  o cerință atomică al cărei citat și a cărei singură clauză Contract sunt același text\n  When   `map` rulează\n  Then   `intent`-ul acelui nod este gol iar `contract`-ul său poartă în continuare clauza\n\nCASE-5\n  Given  o cerință secționată al cărei citat este o motivație distinctă de clauzele sale\n  When   `map` rulează\n  Then   `intent`-ul acelui nod poartă citatul neschimbat"
        }
      }
    },
    {
      "id": "REQ-MAP-870",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Generating _map.json's node graph",
      "intent": "Everything that draws or publishes a picture of the corpus reads `_map.json`; nothing draws from the requirement files directly. `map` writes it alongside the diagrams and viewer.",
      "contract": [
        "`map` generates `_map.json` under `requirements/`.",
        "`_map.json` is a derived view. It is regenerated, never edited.",
        "`_map.json` carries one node per requirement and one edge per `depends_on`.",
        "Each node carries its requirement's id, layer, status, area, title, intent, Contract/Verify-intent/Notes bullets, acceptance, members (`role`/`loc`), `deps`, `used_by`, and risk signals.",
        "All requirement-derived text is JSON-encoded, which neutralizes a hostile id, title or body by construction — there is no markup context for it to break out of."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — map produces _map.json Given  a corpus of requirements When   `map` runs Then   it writes `requirements/_map.json`",
        "CASE-2 — — the graph has one node per requirement and one edge per dependency Given  a corpus of N requirements carrying a total of M `depends_on` links When   `map` runs Then   `_map.json` holds exactly N nodes and M edges",
        "CASE-3 — — a node carries its full requirement metadata Given  a requirement with an id, layer, status, area, title, Contract clauses and members When   `map` runs Then   its node carries all of those fields plus `deps`, `used_by` and any risk signals",
        "CASE-4 — — acc lists one entry per criterion in either acceptance form Given  one requirement with labelled `CASE-N` blocks and another with bulleted acceptance When   `map` runs Then   each node's `acc` list has exactly one entry per criterion, in both requirements",
        "CASE-5 — — repo resolves to the git remote's owner/repo Given  a git checkout whose remote `origin` points at `owner/repo` When   `map` runs Then   `_map.json`'s top-level `repo` field reads `owner/repo`, identifying the project for display in the viewer header",
        "CASE-6 — — a hostile id or title round-trips as inert data Given  a requirement whose id contains `</script>` and whose title contains a raw quote When   `map` runs Then   `_map.json` stores both values unchanged as ordinary JSON strings, with no markup break-out --------------------"
      ],
      "accept": "CASE-1 — map produces _map.json\n  Given  a corpus of requirements\n  When   `map` runs\n  Then   it writes `requirements/_map.json`\n\nCASE-2 — the graph has one node per requirement and one edge per dependency\n  Given  a corpus of N requirements carrying a total of M `depends_on` links\n  When   `map` runs\n  Then   `_map.json` holds exactly N nodes and M edges\n\nCASE-3 — a node carries its full requirement metadata\n  Given  a requirement with an id, layer, status, area, title, Contract clauses and members\n  When   `map` runs\n  Then   its node carries all of those fields plus `deps`, `used_by` and any risk signals\n\nCASE-4 — acc lists one entry per criterion in either acceptance form\n  Given  one requirement with labelled `CASE-N` blocks and another with bulleted acceptance\n  When   `map` runs\n  Then   each node's `acc` list has exactly one entry per criterion, in both requirements\n\nCASE-5 — repo resolves to the git remote's owner/repo\n  Given  a git checkout whose remote `origin` points at `owner/repo`\n  When   `map` runs\n  Then   `_map.json`'s top-level `repo` field reads `owner/repo`, identifying the project\n         for display in the viewer header\n\nCASE-6 — a hostile id or title round-trips as inert data\n  Given  a requirement whose id contains `</script>` and whose title contains a raw quote\n  When   `map` runs\n  Then   `_map.json` stores both values unchanged as ordinary JSON strings, with no markup\n         break-out\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-MAP-007"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3960"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4206"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4235"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7335"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7354"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7375"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1379"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:5781"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Generarea grafului de noduri din _map.json",
          "intent": "Tot ce desenează sau publică o imagine a corpusului citește `_map.json`; nimic nu desenează direct din fișierele de cerințe. `map` îl scrie alături de diagrame și de viewer.",
          "contract": "> Tot ce desenează sau publică o imagine a corpusului citește `_map.json`; nimic nu desenează\n> direct din fișierele de cerințe. `map` îl scrie alături de diagrame și de viewer.\n\nFiecare punct de mai jos este obligatoriu.\n- `map` generează `_map.json` sub `requirements/`.\n- `_map.json` este o vedere derivată. Este regenerat, niciodată editat.\n- `_map.json` poartă un nod per cerință și o muchie per `depends_on`.\n- Fiecare nod poartă id-ul cerinței sale, layer, status, area, titlu, intenție,\n  punctele Contract/Verify-intent/Notes, acceptanța, membrii (`role`/`loc`), `deps`,\n  `used_by` și semnalele de risc.\n- Tot textul derivat din cerințe este codificat JSON, ceea ce neutralizează prin construcție un\n  id, un titlu sau un corp ostil — nu există niciun context de markup din care să evadeze.",
          "acceptance": "CASE-1 — map produce _map.json\n  Given  un corpus de cerințe\n  When   rulează `map`\n  Then   scrie `requirements/_map.json`\n\nCASE-2 — graful are un nod per cerință și o muchie per dependență\n  Given  un corpus de N cerințe purtând în total M legături `depends_on`\n  When   rulează `map`\n  Then   `_map.json` conține exact N noduri și M muchii\n\nCASE-3 — un nod poartă metadatele complete ale cerinței sale\n  Given  o cerință cu id, layer, status, area, titlu, clauze de Contract și membri\n  When   rulează `map`\n  Then   nodul ei poartă toate aceste câmpuri plus `deps`, `used_by` și orice semnale de risc\n\nCASE-4 — acc listează o intrare per criteriu în oricare formă de acceptanță\n  Given  o cerință cu blocuri `CASE-N` etichetate și alta cu acceptanță sub formă de listă\n  When   rulează `map`\n  Then   lista `acc` a fiecărui nod are exact o intrare per criteriu, în ambele cerințe\n\nCASE-5 — repo se rezolvă la owner/repo din remote-ul git\n  Given  un checkout git al cărui remote `origin` indică spre `owner/repo`\n  When   rulează `map`\n  Then   câmpul de nivel superior `repo` din `_map.json` citește `owner/repo`, identificând proiectul\n         pentru afișare în antetul viewer-ului\n\nCASE-6 — un id sau un titlu ostil face drumul dus-întors ca date inerte\n  Given  o cerință al cărei id conține `</script>` și al cărei titlu conține un ghilimele brut\n  When   rulează `map`\n  Then   `_map.json` stochează ambele valori neschimbate ca șiruri JSON obișnuite, fără\n         evadare în markup\n         break-out\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-MAP-871",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Repo, engine version, todos, and freshness checking",
      "intent": "`repo` and `engine_version` describe the environment the map was built in, not the corpus itself — so both are excluded from the freshness diff, or a plain clone into a different fork, or bumping the vendored engine, would make every committed map falsely look stale. `todos` rides alongside for the same reason a picture needs planned work next to what already exists. `map --check` is the freshness gate that reads all of this: a committed file that no longer matches a fresh render is stale, and the gate surfaces that as a warning so a commit prepared with `gate` alone is never surprised by it later in CI.",
      "contract": [
        "`_map.json` carries a top-level `repo` field: a best-effort `owner/repo`, else the repo directory name, else null.",
        "`repo` identifies the project the map describes, for display in the viewer header.",
        "`repo` is derived from the git remote, so it differs across forks and clones. It is therefore excluded from the `map --check` freshness diff.",
        "Resolving `repo` never raises and never blocks map generation, because git may be absent or the tree may not be a checkout.",
        "`engine_version` is likewise excluded from the `map --check` freshness diff. Updating the vendored engine alone never makes a committed map stale; the next `sync` refreshes it.",
        "`_map.json` carries a top-level `todos` array, derived from `TODO.md` via `_parse_todos`, so the viewer's Roadmap tab can show planned work alongside requirements.",
        "`map --check` fails and names the stale file when a committed generated file differs from a fresh render.",
        "The gate reports that same staleness as a warning, without failing the commit, and never regenerates the map itself — only `sync` or `map` does that."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a changed repo field never trips map --check Given  a committed `_map.json` and a fresh render whose only difference is the `repo` field When   `map --check` runs Then   it reports no staleness",
        "CASE-2 — — map succeeds when the tree has no git remote Given  a working tree with no `.git` directory When   `map` runs Then   it completes without raising, and `_map.json`'s `repo` field is null",
        "CASE-3 — — a changed engine_version never trips map --check Given  a committed `_map.json` and a fresh render whose only difference is `engine_version` When   `map --check` runs Then   it reports no staleness",
        "CASE-4 — — map derives the todos array from TODO.md Given  a `TODO.md` with one open item under a milestone section When   `map` runs Then   `_map.json`'s top-level `todos` array contains that item",
        "CASE-5 — — map --check fails on a genuinely stale committed map Given  a committed `_map.json` generated from one requirement title, then that title changed When   `map --check` runs Then   it exits non-zero and reports the file as stale",
        "CASE-6 — — the gate warns about a stale map without failing the commit Given  a committed `_map.json` that no longer matches a fresh render When   `gate` runs Then   it prints a staleness warning naming `_map.json`, and never regenerates the file itself --------------------"
      ],
      "accept": "CASE-1 — a changed repo field never trips map --check\n  Given  a committed `_map.json` and a fresh render whose only difference is the `repo` field\n  When   `map --check` runs\n  Then   it reports no staleness\n\nCASE-2 — map succeeds when the tree has no git remote\n  Given  a working tree with no `.git` directory\n  When   `map` runs\n  Then   it completes without raising, and `_map.json`'s `repo` field is null\n\nCASE-3 — a changed engine_version never trips map --check\n  Given  a committed `_map.json` and a fresh render whose only difference is `engine_version`\n  When   `map --check` runs\n  Then   it reports no staleness\n\nCASE-4 — map derives the todos array from TODO.md\n  Given  a `TODO.md` with one open item under a milestone section\n  When   `map` runs\n  Then   `_map.json`'s top-level `todos` array contains that item\n\nCASE-5 — map --check fails on a genuinely stale committed map\n  Given  a committed `_map.json` generated from one requirement title, then that title changed\n  When   `map --check` runs\n  Then   it exits non-zero and reports the file as stale\n\nCASE-6 — the gate warns about a stale map without failing the commit\n  Given  a committed `_map.json` that no longer matches a fresh render\n  When   `gate` runs\n  Then   it prints a staleness warning naming `_map.json`, and never regenerates the file itself\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-MAP-007"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4096"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6163"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6218"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6845"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1425"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2240"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4144"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6749"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Repo, versiunea engine-ului, todos, și verificarea prospețimii",
          "intent": "`repo` și `engine_version` descriu mediul în care a fost construit map-ul, nu corpusul în sine — deci ambele sunt excluse din diff-ul de prospețime, altfel o simplă clonare într-un alt fork, sau actualizarea engine-ului vendored, ar face fiecare map committed să pară fals perimat. `todos` merge alături din același motiv pentru care o imagine are nevoie de munca planificată lângă ce există deja. `map --check` este gate-ul de prospețime care citește toate acestea: un fișier committed care nu mai corespunde unei randări proaspete este perimat, iar gate-ul afișează asta ca avertisment astfel încât un commit pregătit doar cu `gate` nu este niciodată surprins de asta mai târziu în CI.",
          "contract": "> `repo` și `engine_version` descriu mediul în care a fost construit map-ul, nu corpusul\n> în sine — deci ambele sunt excluse din diff-ul de prospețime, altfel o simplă clonare\n> într-un alt fork, sau actualizarea engine-ului vendored, ar face fiecare map committed\n> să pară fals perimat. `todos` merge alături din același motiv pentru care o imagine are\n> nevoie de munca planificată lângă ce există deja. `map --check` este gate-ul de\n> prospețime care citește toate acestea: un fișier committed care nu mai corespunde unei\n> randări proaspete este perimat, iar gate-ul afișează asta ca avertisment astfel încât un\n> commit pregătit doar cu `gate` nu este niciodată surprins de asta mai târziu în CI.\n\nFiecare bullet de mai jos este obligatoriu.\n- `_map.json` poartă un câmp de top-level `repo`: un `owner/repo` best-effort, altfel\n  numele directorului repo-ului, altfel null.\n- `repo` identifică proiectul pe care map-ul îl descrie, pentru afișare în header-ul viewer-ului.\n- `repo` este derivat din remote-ul git, deci diferă între fork-uri și clone. Este de aceea\n  exclus din diff-ul de prospețime al `map --check`.\n- Rezolvarea `repo` nu ridică niciodată o excepție și nu blochează niciodată generarea map-ului,\n  deoarece git poate lipsi sau arborele poate să nu fie un checkout.\n- `engine_version` este de asemenea exclus din diff-ul de prospețime al `map --check`. Actualizarea\n  singură a engine-ului vendored nu face niciodată un map committed perimat; următorul `sync`\n  îl reîmprospătează.\n- `_map.json` poartă un array de top-level `todos`, derivat din `TODO.md` prin\n  `_parse_todos`, astfel încât tab-ul Roadmap al viewer-ului poate arăta munca planificată alături de\n  cerințe.\n- `map --check` eșuează și numește fișierul perimat când un fișier generat committed diferă de\n  o randare proaspătă.\n- Gate-ul raportează aceeași perimare ca avertisment, fără să eșueze commit-ul, și nu regenerează\n  niciodată singur map-ul — doar `sync` sau `map` fac asta.",
          "acceptance": "CASE-1 — un câmp repo schimbat nu declanșează niciodată map --check\n  Given  un `_map.json` committed și o randare proaspătă a cărei singură diferență este câmpul `repo`\n  When   rulează `map --check`\n  Then   nu raportează nicio perimare\n\nCASE-2 — map reușește când arborele nu are remote git\n  Given  un working tree fără director `.git`\n  When   rulează `map`\n  Then   se termină fără să ridice o excepție, iar câmpul `repo` din `_map.json` este null\n\nCASE-3 — un engine_version schimbat nu declanșează niciodată map --check\n  Given  un `_map.json` committed și o randare proaspătă a cărei singură diferență este `engine_version`\n  When   rulează `map --check`\n  Then   nu raportează nicio perimare\n\nCASE-4 — map derivă array-ul todos din TODO.md\n  Given  un `TODO.md` cu un item deschis sub o secțiune de milestone\n  When   rulează `map`\n  Then   array-ul de top-level `todos` din `_map.json` conține acel item\n\nCASE-5 — map --check eșuează pe un map committed cu adevărat perimat\n  Given  un `_map.json` committed generat dintr-un titlu de cerință, apoi acel titlu s-a schimbat\n  When   rulează `map --check`\n  Then   iese cu cod non-zero și raportează fișierul ca perimat\n\nCASE-6 — gate-ul avertizează despre un map perimat fără să eșueze commit-ul\n  Given  un `_map.json` committed care nu mai corespunde unei randări proaspete\n  When   rulează `gate`\n  Then   afișează un avertisment de perimare care numește `_map.json`, și nu regenerează niciodată\n         singur fișierul\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-MAP-872",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Reading contract clauses across wrapped lines",
      "intent": "A contract clause is authored to read naturally, which means it sometimes wraps onto a second physical line, and a bold-only label line groups several clauses under one heading. Reading naively would truncate a wrapped clause to its first line, or worse, swallow a group label as if it were a clause of its own.",
      "contract": [
        "Reading a requirement's clauses folds a wrapped line back into the clause above it, so a multi-line clause is never truncated to its first physical line.",
        "A clause-group label groups the clauses below it: a bold-only line written flush left. A label is a heading, not a clause, and never folds into the clause above.",
        "Position decides a label, not the bold markers alone. An indented wrapped line folds even when it opens and closes on bold spans, so a two-part clause keeps both halves."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a wrapped clause line folds into the clause above it Given  a Contract clause written across two physical lines, the second indented When   the requirement is parsed Then   the clause reads as one unbroken sentence, never truncated to its first line",
        "CASE-2 — — a flush-left bold line reads as a group label, not a clause Given  a Contract section with a bold-only line flush left, followed by indented clauses When   the requirement is parsed Then   the bold line neither folds into the clause above it nor appears itself as a clause",
        "CASE-3 — — an indented bold-opening continuation folds by position Given  a clause whose wrapped continuation line is indented and both opens and closes on bold text When   the requirement is parsed Then   the continuation folds into the clause above, keeping both halves in one clause --------------------"
      ],
      "accept": "CASE-1 — a wrapped clause line folds into the clause above it\n  Given  a Contract clause written across two physical lines, the second indented\n  When   the requirement is parsed\n  Then   the clause reads as one unbroken sentence, never truncated to its first line\n\nCASE-2 — a flush-left bold line reads as a group label, not a clause\n  Given  a Contract section with a bold-only line flush left, followed by indented clauses\n  When   the requirement is parsed\n  Then   the bold line neither folds into the clause above it nor appears itself as a clause\n\nCASE-3 — an indented bold-opening continuation folds by position\n  Given  a clause whose wrapped continuation line is indented and both opens and closes on bold\n         text\n  When   the requirement is parsed\n  Then   the continuation folds into the clause above, keeping both halves in one clause\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-MAP-007"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6327"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6343"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2052"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Citirea clauzelor de contract peste linii wrapped",
          "intent": "O clauză de contract este redactată să se citească natural, ceea ce înseamnă că uneori se întinde pe o a doua linie fizică, iar o linie de etichetă doar-bold grupează mai multe clauze sub un singur titlu. O citire naivă ar trunchia o clauză wrapped la prima ei linie, sau mai rău, ar înghiți o etichetă de grup de parcă ar fi ea însăși o clauză.",
          "contract": "> O clauză de contract este redactată să se citească natural, ceea ce înseamnă că uneori\n> se întinde pe o a doua linie fizică, iar o linie de etichetă doar-bold grupează mai multe\n> clauze sub un singur titlu. O citire naivă ar trunchia o clauză wrapped la prima ei\n> linie, sau mai rău, ar înghiți o etichetă de grup de parcă ar fi ea însăși o clauză.\n\nFiecare bullet de mai jos este obligatoriu.\n- Citirea clauzelor unei cerințe pliază o linie wrapped înapoi în clauza de deasupra ei, astfel\n  încât o clauză pe mai multe linii nu este niciodată trunchiată la prima ei linie fizică.\n- O etichetă de grup de clauze grupează clauzele de dedesubt: o linie doar-bold scrisă aliniată la\n  stânga. O etichetă este un titlu, nu o clauză, și nu se pliază niciodată în clauza de deasupra.\n- Poziția decide o etichetă, nu markerii bold singuri. O linie wrapped indentată se pliază\n  chiar și atunci când se deschide și se închide pe spanuri bold, astfel încât o clauză în două părți\n  păstrează ambele jumătăți.",
          "acceptance": "CASE-1 — o linie de clauză wrapped se pliază în clauza de deasupra ei\n  Given  o clauză de Contract scrisă pe două linii fizice, a doua indentată\n  When   cerința este parsată\n  Then   clauza se citește ca o singură propoziție neîntreruptă, niciodată trunchiată la prima ei linie\n\nCASE-2 — o linie bold aliniată la stânga se citește ca o etichetă de grup, nu o clauză\n  Given  o secțiune Contract cu o linie doar-bold aliniată la stânga, urmată de clauze indentate\n  When   cerința este parsată\n  Then   linia bold nici nu se pliază în clauza de deasupra ei, nici nu apare ea însăși ca o clauză\n\nCASE-3 — o continuare cu deschidere bold indentată se pliază după poziție\n  Given  o clauză a cărei linie de continuare wrapped este indentată și se deschide și se închide\n         pe text bold\n  When   cerința este parsată\n  Then   continuarea se pliază în clauza de deasupra, păstrând ambele jumătăți într-o singură clauză\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-MAP-873",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Deduping intent against the contract",
      "intent": "In the atomic form the quote IS the single contract clause, so printing both `intent` and `contract` showed the exact same sentence twice — measured at 588 of 646 nodes, 91% of this corpus, before this existed. Deduping collapses that duplicate while leaving a sectioned requirement's real rationale, which says something the clauses do not, untouched.",
      "contract": [
        "The `intent` field carries a requirement's first blockquote, joined into one line.",
        "The `intent` field is empty when that quote and the Contract say the same thing.",
        "An atomic requirement is that case: its quote is its single obligation, so the map emits the text once, under `contract`.",
        "The comparison ignores whitespace-only differences, so a quote re-wrapped across lines still matches its clause and still dedupes.",
        "`show` follows the same rule, printing no intent line above a Contract that repeats it."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — an atomic requirement's intent collapses into its contract Given  an atomic requirement whose quote and single Contract clause are the same text When   `_distinct_intent` runs Then   it returns \"\", while the clause still survives under `contract`",
        "CASE-2 — — a sectioned requirement's intent survives as real rationale Given  a sectioned requirement whose quote is rationale distinct from its Contract clauses When   `_distinct_intent` runs Then   it returns the quote unchanged",
        "CASE-3 — — intent dedup ignores only whitespace differences Given  an atomic requirement whose quote is re-wrapped across two physical lines but is otherwise identical to its single Contract clause When   `_distinct_intent` runs Then   it still returns \"\", because the comparison collapses whitespace before comparing"
      ],
      "accept": "CASE-1 — an atomic requirement's intent collapses into its contract\n  Given  an atomic requirement whose quote and single Contract clause are the same text\n  When   `_distinct_intent` runs\n  Then   it returns \"\", while the clause still survives under `contract`\n\nCASE-2 — a sectioned requirement's intent survives as real rationale\n  Given  a sectioned requirement whose quote is rationale distinct from its Contract clauses\n  When   `_distinct_intent` runs\n  Then   it returns the quote unchanged\n\nCASE-3 — intent dedup ignores only whitespace differences\n  Given  an atomic requirement whose quote is re-wrapped across two physical lines but is\n         otherwise identical to its single Contract clause\n  When   `_distinct_intent` runs\n  Then   it still returns \"\", because the comparison collapses whitespace before comparing",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-MAP-007"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6241"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6261"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1678"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Deduplicarea intent-ului față de contract",
          "intent": "În forma atomică citatul ESTE unica clauză de contract, deci afișarea atât a `intent`-ului cât și a `contract`-ului arăta exact aceeași propoziție de două ori — măsurat la 588 din 646 noduri, 91% din acest corpus, înainte ca asta să existe. Deduplicarea colapsează acea duplicare lăsând neatinsă rațiunea reală a unei cerințe sectioned, care spune ceva ce clauzele nu spun.",
          "contract": "> În forma atomică citatul ESTE unica clauză de contract, deci afișarea atât a `intent`-ului\n> cât și a `contract`-ului arăta exact aceeași propoziție de două ori — măsurat la 588 din\n> 646 noduri, 91% din acest corpus, înainte ca asta să existe. Deduplicarea colapsează acea\n> duplicare lăsând neatinsă rațiunea reală a unei cerințe sectioned, care spune ceva ce\n> clauzele nu spun.\n\nFiecare bullet de mai jos este obligatoriu.\n- Câmpul `intent` poartă primul blockquote al unei cerințe, unit într-o singură linie.\n- Câmpul `intent` este gol când acel citat și Contract-ul spun același lucru.\n- O cerință atomică este exact acest caz: citatul ei este unica ei obligație, deci map-ul\n  emite textul o singură dată, sub `contract`.\n- Comparația ignoră diferențele doar-whitespace, astfel încât un citat re-wrapped pe mai multe\n  linii tot se potrivește cu clauza sa și tot se deduplică.\n- `show` urmează aceeași regulă, neafișând nicio linie de intent deasupra unui Contract care o repetă.",
          "acceptance": "CASE-1 — intent-ul unei cerințe atomice colapsează în contractul ei\n  Given  o cerință atomică al cărei citat și unică clauză de Contract sunt același text\n  When   rulează `_distinct_intent`\n  Then   returnează \"\", în timp ce clauza tot supraviețuiește sub `contract`\n\nCASE-2 — intent-ul unei cerințe sectioned supraviețuiește ca rațiune reală\n  Given  o cerință sectioned al cărei citat este rațiune distinctă de clauzele ei de Contract\n  When   rulează `_distinct_intent`\n  Then   returnează citatul neschimbat\n\nCASE-3 — deduplicarea intent-ului ignoră doar diferențele de whitespace\n  Given  o cerință atomică al cărei citat este re-wrapped pe două linii fizice dar este altfel\n         identic cu unica ei clauză de Contract\n  When   rulează `_distinct_intent`\n  Then   tot returnează \"\", deoarece comparația colapsează whitespace-ul înainte de a compara"
        }
      }
    },
    {
      "id": "ARCH-MAPDIAGRAMS-055",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Mermaid diagrams (_map.md)",
      "intent": "The graph is data; this is the picture. Four Mermaid diagrams that render on GitHub with no tooling, so someone reviewing a pull request can see the shape of the system without cloning anything or running the engine.",
      "contract": [
        "`map` generates `_map.md` under `requirements/` — exactly 5 legended Mermaid blocks, rendered from the graph and never hand-edited. [[REQ-MAPDIAGRAMS-874]]",
        "The Specification Hierarchy is drawn from the `satisfies:` edges, never from `depends_on:`, folding each code requirement into its parent's count instead of drawing it. [[REQ-MAPDIAGRAMS-875]]",
        "The System Map groups nodes into per-area subgraphs, an area falling back to the node's id prefix when `area:` is absent, and omits edges into a bus or high-fan-in node. [[REQ-MAPDIAGRAMS-876]]",
        "The Dependency Map is area-level: one counted node per area, one edge per cross-area dependency. Req→Code draws the system/architecture tiers only, coloured by link status, with same-file members collapsed to a line range. [[REQ-MAPDIAGRAMS-877]]",
        "The Risk diagram shows only requirements with at least one risk signal (confirmed with zero members; `draft`/`baseline`; ≥3 dependents), each paired with a scripted recommendation. [[REQ-MAPDIAGRAMS-878]]"
      ],
      "verify": [],
      "notes": [
        "The risk-signal dedup lives in `_risk_signals`, shared with the `next` worklist, so the diagram and the worklist cannot disagree about what is at risk.",
        "Split out of [[ARCH-MAP-007]] on 2026-09-02. That requirement held 35 Contract clauses covering two capabilities — the graph and its rendering — and was the only requirement over twenty clauses carrying no `lint_exempt:`, i.e. the only one whose size had never been judged acceptable in writing."
      ],
      "current_impl": [
        "`_mermaid_hierarchy`, `_grouped_areas`, `_mermaid_system`, `_mermaid_deps`, `_mermaid_req_to_code`, `_mermaid_risk`, `_build_md_text` and `render_md` in `reqmap.py`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  the generated `_map.md` When   it is inspected Then   it contains exactly 5 Mermaid code blocks, each with a legend",
        "CASE-2 — Given  a corpus carrying system, architecture and code requirements When   the Specification Hierarchy is rendered Then   it draws the system and architecture nodes joined by their `satisfies:` edges, draws no code node, and shows each architecture's code count on its box",
        "CASE-3 — Given  the System Map diagram When   it is rendered Then   nodes group into per-area subgraphs, single-node areas collapse into `misc`, and edges whose target is a bus node are omitted",
        "CASE-4 — Given  the Dependency Map diagram When   it is rendered Then   it is area-level: one node per area (with a count), an edge A→B when some capability in A depends on one in B; per-capability hub edges are not drawn",
        "CASE-5 — Given  the Risk diagram When   it is rendered Then   it shows only requirements with at least one risk signal, each with a scripted recommendation, and a draft's open question is not shown twice"
      ],
      "accept": "CASE-1\n  Given  the generated `_map.md`\n  When   it is inspected\n  Then   it contains exactly 5 Mermaid code blocks, each with a legend\n\nCASE-2\n  Given  a corpus carrying system, architecture and code requirements\n  When   the Specification Hierarchy is rendered\n  Then   it draws the system and architecture nodes joined by their `satisfies:` edges,\n         draws no code node, and shows each architecture's code count on its box\n\nCASE-3\n  Given  the System Map diagram\n  When   it is rendered\n  Then   nodes group into per-area subgraphs, single-node areas collapse into `misc`, and edges\n         whose target is a bus node are omitted\n\nCASE-4\n  Given  the Dependency Map diagram\n  When   it is rendered\n  Then   it is area-level: one node per area (with a count), an edge A→B when some capability\n         in A depends on one in B; per-capability hub edges are not drawn\n\nCASE-5\n  Given  the Risk diagram\n  When   it is rendered\n  Then   it shows only requirements with at least one risk signal, each with a scripted\n         recommendation, and a draft's open question is not shown twice",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-MAP-007"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-VISUAL-106"
      ],
      "satisfied_by": [
        "REQ-MAPDIAGRAMS-874",
        "REQ-MAPDIAGRAMS-875",
        "REQ-MAPDIAGRAMS-876",
        "REQ-MAPDIAGRAMS-877",
        "REQ-MAPDIAGRAMS-878"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6493"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6545"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6560"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6601"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6642"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6736"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6777"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6836"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:882"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7479"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9662"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "should-have",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Diagrame Mermaid (_map.md)",
          "intent": "Graful este date; aceasta este imaginea. Patru diagrame Mermaid care se randează pe GitHub fără nicio unealtă, astfel încât cineva care revizuiește un pull request poate vedea forma sistemului fără să claseze nimic sau să ruleze motorul.",
          "contract": "> Graful este date; aceasta este imaginea. Patru diagrame Mermaid care se randează pe\n> GitHub fără nicio unealtă, astfel încât cineva care revizuiește un pull request poate\n> vedea forma sistemului fără să claseze nimic sau să ruleze motorul.\n\nFiecare punct de mai jos este obligatoriu.\n- `map` generează `_map.md` sub `requirements/` — exact 5 blocuri Mermaid cu legendă, randate din graf și niciodată editate manual. [[REQ-MAPDIAGRAMS-874]]\n- Ierarhia de Specificație este desenată din muchiile `satisfies:`, niciodată din `depends_on:`, pliind fiecare cerință de cod în numărătoarea părintelui ei în loc să o deseneze. [[REQ-MAPDIAGRAMS-875]]\n- Harta Sistemului grupează nodurile în subgrafuri per-zonă, o zonă revenind la prefixul id-ului nodului atunci când `area:` lipsește, și omite muchiile către un nod bus sau cu fan-in ridicat. [[REQ-MAPDIAGRAMS-876]]\n- Harta de Dependențe este la nivel de zonă: un nod numărat per zonă, o muchie per dependență inter-zonă. Req→Code desenează doar nivelurile system/architecture, colorate după statusul legăturii, cu membrii din același fișier pliați într-un interval de linii. [[REQ-MAPDIAGRAMS-877]]\n- Diagrama de Risc arată doar cerințele cu cel puțin un semnal de risc (confirmed cu zero membri; `draft`/`baseline`; ≥3 dependenți), fiecare împerecheată cu o recomandare scriptată. [[REQ-MAPDIAGRAMS-878]]",
          "acceptance": "CASE-1\n  Given  `_map.md` generat\n  When   este inspectat\n  Then   conține exact 5 blocuri de cod Mermaid, fiecare cu o legendă\n\nCASE-2\n  Given  un corpus care poartă cerințe system, architecture și code\n  When   Ierarhia de Specificație este randată\n  Then   desenează nodurile system și architecture unite prin muchiile lor `satisfies:`,\n         nu desenează niciun nod code, și arată numărul de cod al fiecărei arhitecturi pe caseta ei\n\nCASE-3\n  Given  diagrama Hărții Sistemului\n  When   este randată\n  Then   nodurile se grupează în subgrafuri per-zonă, zonele cu un singur nod se pliază în `misc`, iar muchiile\n         a căror țintă este un nod bus sunt omise\n\nCASE-4\n  Given  diagrama Hărții de Dependențe\n  When   este randată\n  Then   este la nivel de zonă: un nod per zonă (cu o numărătoare), o muchie A→B când o capabilitate\n         din A depinde de una din B; muchiile hub per-capabilitate nu sunt desenate\n\nCASE-5\n  Given  diagrama de Risc\n  When   este randată\n  Then   arată doar cerințele cu cel puțin un semnal de risc, fiecare cu o recomandare\n         scriptată, iar întrebarea deschisă a unui draft nu este arătată de două ori"
        }
      }
    },
    {
      "id": "REQ-MAPDIAGRAMS-874",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "_map.md: five legended, always-regenerated Mermaid blocks",
      "intent": "`_map.md` is the picture that goes with the graph — Specification Hierarchy, System Map, Req→Code, Dependencies, Risk — rendered fresh from the registry on every `map` run so it can never drift into a hand-edited state. It renders on GitHub with no tooling, so a pull request reviewer sees the shape of the system without cloning anything.",
      "contract": [
        "`map` generates `_map.md` under `requirements/`, rendered from the graph and never edited.",
        "`_map.md` contains exactly 5 Mermaid code blocks: Specification Hierarchy, System Map, Req→Code, Dependencies and Risk.",
        "Each of those 5 blocks carries a legend."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — map writes _map.md from the graph, overwriting any manual edit Given  a `requirements/_map.md` a human hand-edited since the last run When   `map` runs Then   the file is fully regenerated from the graph, discarding the manual edit",
        "CASE-2 — — _map.md carries exactly the five named diagrams Given  the generated `_map.md` When   its Mermaid code blocks are counted Then   there are exactly 5: Specification Hierarchy, System Map, Req→Code, Dependencies, Risk",
        "CASE-3 — — every diagram block ships its own legend Given  the generated `_map.md` When   each of the 5 Mermaid blocks is inspected Then   each one carries a legend describing its symbols --------------------"
      ],
      "accept": "CASE-1 — map writes _map.md from the graph, overwriting any manual edit\n  Given  a `requirements/_map.md` a human hand-edited since the last run\n  When   `map` runs\n  Then   the file is fully regenerated from the graph, discarding the manual edit\n\nCASE-2 — _map.md carries exactly the five named diagrams\n  Given  the generated `_map.md`\n  When   its Mermaid code blocks are counted\n  Then   there are exactly 5: Specification Hierarchy, System Map, Req→Code, Dependencies, Risk\n\nCASE-3 — every diagram block ships its own legend\n  Given  the generated `_map.md`\n  When   each of the 5 Mermaid blocks is inspected\n  Then   each one carries a legend describing its symbols\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-MAPDIAGRAMS-055"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6777"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6836"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:882"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7479"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9662"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "_map.md: cinci blocuri Mermaid cu legendă, regenerate mereu",
          "intent": "`_map.md` este imaginea care merge alături de graf — Specification Hierarchy, System Map, Req→Code, Dependencies, Risk — randată proaspăt din registry la fiecare rulare `map` astfel încât nu poate niciodată să derive-ze într-o stare editată de mână. Se randează pe GitHub fără tooling, astfel încât un reviewer de pull request vede forma sistemului fără să cloneze nimic.",
          "contract": "> `_map.md` este imaginea care merge alături de graf — Specification Hierarchy, System\n> Map, Req→Code, Dependencies, Risk — randată proaspăt din registry la fiecare rulare\n> `map` astfel încât nu poate niciodată să derive-ze într-o stare editată de mână. Se\n> randează pe GitHub fără tooling, astfel încât un reviewer de pull request vede forma\n> sistemului fără să cloneze nimic.\n\nFiecare bullet de mai jos este obligatoriu.\n- `map` generează `_map.md` sub `requirements/`, randat din graf și niciodată editat.\n- `_map.md` conține exact 5 blocuri de cod Mermaid: Specification Hierarchy, System Map,\n  Req→Code, Dependencies și Risk.\n- Fiecare din acele 5 blocuri poartă o legendă.",
          "acceptance": "CASE-1 — map scrie _map.md din graf, suprascriind orice editare manuală\n  Given  un `requirements/_map.md` pe care un om l-a editat de mână de la ultima rulare\n  When   rulează `map`\n  Then   fișierul este complet regenerat din graf, aruncând editarea manuală\n\nCASE-2 — _map.md poartă exact cele cinci diagrame numite\n  Given  `_map.md`-ul generat\n  When   îi sunt numărate blocurile de cod Mermaid\n  Then   sunt exact 5: Specification Hierarchy, System Map, Req→Code, Dependencies, Risk\n\nCASE-3 — fiecare bloc de diagramă își livrează propria legendă\n  Given  `_map.md`-ul generat\n  When   fiecare din cele 5 blocuri Mermaid este inspectat\n  Then   fiecare poartă o legendă care descrie simbolurile sale\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-MAPDIAGRAMS-875",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Specification Hierarchy: satisfies edges only, code counted not drawn",
      "intent": "The Hierarchy is the pyramid `level:` implies — system requirements over architecture ones — and it reads only `satisfies:` edges, never `depends_on:`, because those are different axes (one hierarchy, one composition graph). A `code`-level requirement never gets its own box; it is folded into its architecture parent's count so the diagram stays readable on a corpus with hundreds of them.",
      "contract": [
        "The Specification Hierarchy is drawn from the `satisfies:` edges, never from `depends_on:`.",
        "The Hierarchy draws a node for each `system` and each `architecture` requirement.",
        "The Hierarchy counts a `code` requirement against its parent and never draws it.",
        "An architecture box shows how many code requirements sit under it."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the Hierarchy follows satisfies edges, not depends_on Given  a system and an architecture requirement linked by `satisfies:` and by an unrelated `depends_on:` When   the Specification Hierarchy renders Then   it draws the `satisfies:` edge and omits the `depends_on:` edge",
        "CASE-2 — — the Hierarchy draws a node per system and architecture requirement Given  a corpus with one system and two architecture requirements When   the Specification Hierarchy renders Then   it draws three nodes, one for the system and one for each architecture requirement",
        "CASE-3 — — the Hierarchy folds code requirements into their parent's count Given  an architecture requirement with three `satisfies:`-linked code requirements When   the Specification Hierarchy renders Then   it draws no node for any code requirement, and the parent box shows a count of 3 code requirements sitting under it --------------------"
      ],
      "accept": "CASE-1 — the Hierarchy follows satisfies edges, not depends_on\n  Given  a system and an architecture requirement linked by `satisfies:` and by an unrelated `depends_on:`\n  When   the Specification Hierarchy renders\n  Then   it draws the `satisfies:` edge and omits the `depends_on:` edge\n\nCASE-2 — the Hierarchy draws a node per system and architecture requirement\n  Given  a corpus with one system and two architecture requirements\n  When   the Specification Hierarchy renders\n  Then   it draws three nodes, one for the system and one for each architecture requirement\n\nCASE-3 — the Hierarchy folds code requirements into their parent's count\n  Given  an architecture requirement with three `satisfies:`-linked code requirements\n  When   the Specification Hierarchy renders\n  Then   it draws no node for any code requirement, and the parent box shows a count of 3\n         code requirements sitting under it\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-MAPDIAGRAMS-055"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6560"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:882"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7479"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Specification Hierarchy: doar muchii satisfies, cod numărat nu desenat",
          "intent": "Hierarchy este piramida pe care `level:` o implică — cerințe system deasupra celor architecture — și citește doar muchii `satisfies:`, niciodată `depends_on:`, deoarece acestea sunt axe diferite (una ierarhie, una graf de compoziție). O cerință de nivel `code` nu primește niciodată propria ei cutie; este plutuită în numărul părintelui ei architecture astfel încât diagrama rămâne lizibilă pe un corpus cu sute de asemenea cerințe.",
          "contract": "> Hierarchy este piramida pe care `level:` o implică — cerințe system deasupra celor\n> architecture — și citește doar muchii `satisfies:`, niciodată `depends_on:`, deoarece\n> acestea sunt axe diferite (una ierarhie, una graf de compoziție). O cerință de nivel\n> `code` nu primește niciodată propria ei cutie; este plutuită în numărul părintelui ei\n> architecture astfel încât diagrama rămâne lizibilă pe un corpus cu sute de asemenea\n> cerințe.\n\nFiecare bullet de mai jos este obligatoriu.\n- Specification Hierarchy este desenată din muchiile `satisfies:`, niciodată din `depends_on:`.\n- Hierarchy desenează un nod pentru fiecare cerință `system` și fiecare cerință `architecture`.\n- Hierarchy numără o cerință `code` în contul părintelui ei și nu o desenează niciodată.\n- O cutie architecture arată câte cerințe code stau dedesubtul ei.",
          "acceptance": "CASE-1 — Hierarchy urmează muchii satisfies, nu depends_on\n  Given  o cerință system și una architecture legate printr-un `satisfies:` și printr-un `depends_on:` neînrudit\n  When   se randează Specification Hierarchy\n  Then   desenează muchia `satisfies:` și omite muchia `depends_on:`\n\nCASE-2 — Hierarchy desenează un nod per cerință system și architecture\n  Given  un corpus cu o cerință system și două cerințe architecture\n  When   se randează Specification Hierarchy\n  Then   desenează trei noduri, unul pentru system și câte unul pentru fiecare cerință architecture\n\nCASE-3 — Hierarchy plutuiește cerințele code în numărul părintelui lor\n  Given  o cerință architecture cu trei cerințe code legate prin `satisfies:`\n  When   se randează Specification Hierarchy\n  Then   nu desenează niciun nod pentru vreo cerință code, iar cutia părinte arată un număr de 3\n         cerințe code stând dedesubt\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-MAPDIAGRAMS-876",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "System Map: per-area subgraphs, bus edges hidden",
      "intent": "The System Map groups every node under its area — the `area:` field, or the id prefix when that field is absent — so a reviewer sees the system in the shape a team already thinks in. A single-node area collapses into `misc` rather than cluttering the diagram with one-box subgraphs, and an edge into a foundational (`bus`) or high-fan-in node is omitted, since drawing it would turn every diagram into a hairball centred on that one node.",
      "contract": [
        "A node's area is its `area:` field, or its id prefix when that field is absent.",
        "The System Map groups nodes into per-area subgraphs, and collapses a single-node area into a `misc` box.",
        "The System Map omits a `depends_on` edge whose target is a bus node or a high-fan-in hub."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a node's area falls back to its id prefix Given  one node carrying `area: payments` and one node with no `area:` field, id `AUTH-LOGIN-001` When   the area for each node is computed Then   the first node's area is `payments` and the second's is `AUTH`",
        "CASE-2 — — the System Map collapses a lone node into misc Given  three nodes sharing one area and one node alone in its own area When   the System Map renders Then   the three group into one subgraph and the lone node lands in a `misc` box",
        "CASE-3 — — the System Map hides edges into a bus node Given  a node whose `depends_on:` targets both a `layer: bus` requirement and a normal one When   the System Map renders Then   it draws the edge to the normal requirement and omits the edge to the bus node --------------------"
      ],
      "accept": "CASE-1 — a node's area falls back to its id prefix\n  Given  one node carrying `area: payments` and one node with no `area:` field, id `AUTH-LOGIN-001`\n  When   the area for each node is computed\n  Then   the first node's area is `payments` and the second's is `AUTH`\n\nCASE-2 — the System Map collapses a lone node into misc\n  Given  three nodes sharing one area and one node alone in its own area\n  When   the System Map renders\n  Then   the three group into one subgraph and the lone node lands in a `misc` box\n\nCASE-3 — the System Map hides edges into a bus node\n  Given  a node whose `depends_on:` targets both a `layer: bus` requirement and a normal one\n  When   the System Map renders\n  Then   it draws the edge to the normal requirement and omits the edge to the bus node\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-MAPDIAGRAMS-055"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6493"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6545"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:882"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7479"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "System Map: subgraf-uri per arie, muchiile bus ascunse",
          "intent": "System Map grupează fiecare nod sub aria sa — câmpul `area:`, sau prefixul id-ului când acel câmp lipsește — astfel încât un reviewer vede sistemul în forma în care o echipă deja gândește. O arie cu un singur nod colapsează în `misc` în loc să aglomereze diagrama cu subgraf-uri de o singură cutie, iar o muchie către un nod fundamental (`bus`) sau de fan-in ridicat este omisă, deoarece desenarea ei ar transforma fiecare diagramă într-un ghem centrat pe acel nod.",
          "contract": "> System Map grupează fiecare nod sub aria sa — câmpul `area:`, sau prefixul id-ului\n> când acel câmp lipsește — astfel încât un reviewer vede sistemul în forma în care o\n> echipă deja gândește. O arie cu un singur nod colapsează în `misc` în loc să aglomereze\n> diagrama cu subgraf-uri de o singură cutie, iar o muchie către un nod fundamental\n> (`bus`) sau de fan-in ridicat este omisă, deoarece desenarea ei ar transforma fiecare\n> diagramă într-un ghem centrat pe acel nod.\n\nFiecare bullet de mai jos este obligatoriu.\n- Aria unui nod este câmpul său `area:`, sau prefixul id-ului său când acel câmp lipsește.\n- System Map grupează nodurile în subgraf-uri per arie, și colapsează o arie cu un singur nod\n  într-o cutie `misc`.\n- System Map omite o muchie `depends_on` a cărei țintă este un nod bus sau un hub de fan-in ridicat.",
          "acceptance": "CASE-1 — aria unui nod cade înapoi la prefixul id-ului său\n  Given  un nod purtând `area: payments` și un nod fără câmp `area:`, cu id `AUTH-LOGIN-001`\n  When   se calculează aria pentru fiecare nod\n  Then   aria primului nod este `payments` iar cea a celui de-al doilea este `AUTH`\n\nCASE-2 — System Map colapsează un nod solitar în misc\n  Given  trei noduri care împart o arie și un nod singur în aria sa proprie\n  When   se randează System Map\n  Then   cele trei se grupează într-un subgraf iar nodul solitar aterizează într-o cutie `misc`\n\nCASE-3 — System Map ascunde muchiile către un nod bus\n  Given  un nod ale cărui ținte `depends_on:` includ atât o cerință `layer: bus` cât și una normală\n  When   se randează System Map\n  Then   desenează muchia către cerința normală și omite muchia către nodul bus\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-MAPDIAGRAMS-877",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Dependency Map is area-level; Req→Code colors and collapses lines",
      "intent": "The Dependency Map trades per-capability detail for one node per area (with a count) and one edge per cross-area dependency, so it stays readable at corpus scale where the System Map would not. Req→Code, the other per-file diagram in this group, colors a node by its link status and collapses several members in one file to a single line range instead of listing each one.",
      "contract": [
        "The Dependency Map is area-level: one node per area, carrying a capability count.",
        "The Dependency Map draws an edge A→B when some capability in A depends on one in B. Per-capability hub edges are not drawn.",
        "Req→Code colors an enforced-but-unlinked requirement red, and a baseline or draft not-yet-linked one muted grey.",
        "Req→Code collapses multiple members in one file to a min–max line range.",
        "Req→Code omits `level: code` requirements: they are counted in the hierarchy diagram and drawn in the viewer, and drawing them here with their function-level members pushed the block past the 50,000 characters GitHub renders."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the Dependency Map draws one counted node per area Given  an area holding four requirements When   the Dependency Map renders Then   it draws a single node for that area labelled with a count of 4",
        "CASE-2 — — the Dependency Map draws one area edge per cross-area dependency Given  two requirements in area A each depending on a requirement in area B When   the Dependency Map renders Then   it draws exactly one A→B edge, not one per capability pair",
        "CASE-3 — — Req→Code colors nodes by link status Given  a `confirmed` requirement with no `tested-by:` member and a `draft` requirement with no member When   Req→Code renders Then   the confirmed node is red and the draft node is muted grey",
        "CASE-4 — — Req→Code collapses same-file members to a line range Given  a requirement with three members in one file at lines 10, 25 and 40 When   Req→Code renders Then   that file shows a single `10–40` range, not three separate entries",
        "CASE-5 — — code-level nodes stay out of Req→Code Given  an architecture node and a code node, both with members When   the Req→Code block is rendered Then   the architecture node appears and the code node does not --------------------"
      ],
      "accept": "CASE-1 — the Dependency Map draws one counted node per area\n  Given  an area holding four requirements\n  When   the Dependency Map renders\n  Then   it draws a single node for that area labelled with a count of 4\n\nCASE-2 — the Dependency Map draws one area edge per cross-area dependency\n  Given  two requirements in area A each depending on a requirement in area B\n  When   the Dependency Map renders\n  Then   it draws exactly one A→B edge, not one per capability pair\n\nCASE-3 — Req→Code colors nodes by link status\n  Given  a `confirmed` requirement with no `tested-by:` member and a `draft` requirement with no member\n  When   Req→Code renders\n  Then   the confirmed node is red and the draft node is muted grey\n\nCASE-4 — Req→Code collapses same-file members to a line range\n  Given  a requirement with three members in one file at lines 10, 25 and 40\n  When   Req→Code renders\n  Then   that file shows a single `10–40` range, not three separate entries\n\nCASE-5 — code-level nodes stay out of Req→Code\n  Given  an architecture node and a code node, both with members\n  When   the Req→Code block is rendered\n  Then   the architecture node appears and the code node does not\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-MAPDIAGRAMS-055"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6601"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6642"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:882"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7479"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7936"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Dependency Map este la nivel de arie; Req→Code colorează și colapsează linii",
          "intent": "Dependency Map schimbă detaliul per-capabilitate pentru un nod per arie (cu un număr) și o muchie per dependență cross-area, astfel încât rămâne lizibilă la scara corpusului acolo unde System Map nu ar rămâne. Req→Code, cealaltă diagramă per-fișier din acest grup, colorează un nod după statusul său de legătură și colapsează mai mulți membri dintr-un fișier la un singur interval de linii în loc să-i listeze pe fiecare.",
          "contract": "> Dependency Map schimbă detaliul per-capabilitate pentru un nod per arie (cu un\n> număr) și o muchie per dependență cross-area, astfel încât rămâne lizibilă la scara\n> corpusului acolo unde System Map nu ar rămâne. Req→Code, cealaltă diagramă per-fișier\n> din acest grup, colorează un nod după statusul său de legătură și colapsează mai mulți\n> membri dintr-un fișier la un singur interval de linii în loc să-i listeze pe fiecare.\n\nFiecare bullet de mai jos este obligatoriu.\n- Dependency Map este la nivel de arie: un nod per arie, purtând un număr de capabilități.\n- Dependency Map desenează o muchie A→B când o capabilitate din A depinde de una din B.\n  Muchiile per-capabilitate hub nu sunt desenate.\n- Req→Code colorează cu roșu o cerință enforced dar nelegată, iar una baseline sau draft\n  încă nelegată cu gri estompat.\n- Req→Code colapsează mai mulți membri dintr-un fișier la un interval min–max de linii.\n- Req→Code omite cerințele `level: code`: sunt numărate în diagrama de ierarhie și desenate\n  în viewer, iar desenarea lor aici cu membrii lor la nivel de funcție a împins blocul peste\n  cele 50,000 de caractere pe care GitHub le randează.",
          "acceptance": "CASE-1 — Dependency Map desenează un singur nod numărat per arie\n  Given  o arie care conține patru cerințe\n  When   se randează Dependency Map\n  Then   desenează un singur nod pentru acea arie etichetat cu un număr de 4\n\nCASE-2 — Dependency Map desenează o muchie de arie per dependență cross-area\n  Given  două cerințe în aria A care depind fiecare de o cerință din aria B\n  When   se randează Dependency Map\n  Then   desenează exact o muchie A→B, nu una per pereche de capabilități\n\nCASE-3 — Req→Code colorează nodurile după statusul de legătură\n  Given  o cerință `confirmed` fără membru `tested-by:` și o cerință `draft` fără membru\n  When   se randează Req→Code\n  Then   nodul confirmed este roșu iar nodul draft este gri estompat\n\nCASE-4 — Req→Code colapsează membrii din același fișier la un interval de linii\n  Given  o cerință cu trei membri într-un fișier la liniile 10, 25 și 40\n  When   se randează Req→Code\n  Then   acel fișier arată un singur interval `10–40`, nu trei intrări separate\n\nCASE-5 — nodurile de nivel code rămân în afara Req→Code\n  Given  un nod architecture și un nod code, ambele cu membri\n  When   se randează blocul Req→Code\n  Then   nodul architecture apare iar nodul code nu\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-MAPDIAGRAMS-878",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Risk diagram: only flagged requirements, each with advice",
      "intent": "The Risk diagram is deliberately not a census — it shows only requirements carrying at least one open risk signal, each paired with a scripted recommendation instead of a bare label, so a reader knows what to do about it. It shares its dedup with the `next` worklist's source, so a draft's open question is never shown twice under two different names for the same underlying problem.",
      "contract": [
        "The Risk diagram shows only requirements with at least one risk signal (confirmed with zero members; `draft`/`baseline`; ≥3 dependents).",
        "The Risk diagram pairs each of them with a scripted recommendation.",
        "A `draft`'s open verify-intent question is suppressed, subsumed by its `unreviewed` signal, so a draft is not double-flagged."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the Risk diagram omits a requirement with no risk signal Given  a `confirmed` requirement with members and few dependents, alongside a `confirmed` requirement with zero members When   the Risk diagram renders Then   only the zero-member requirement appears",
        "CASE-2 — — every Risk node carries a recommendation Given  a `draft` requirement shown in the Risk diagram When   the diagram renders Then   its box carries a scripted recommendation string, not just the risk label",
        "CASE-3 — — a draft's verify-intent question does not double-flag it Given  a `draft` requirement carrying both an open verify-intent question and the `unreviewed` signal When   the Risk diagram renders Then   it shows the `unreviewed` signal once and suppresses the separate verify-intent flag"
      ],
      "accept": "CASE-1 — the Risk diagram omits a requirement with no risk signal\n  Given  a `confirmed` requirement with members and few dependents, alongside a `confirmed` requirement with zero members\n  When   the Risk diagram renders\n  Then   only the zero-member requirement appears\n\nCASE-2 — every Risk node carries a recommendation\n  Given  a `draft` requirement shown in the Risk diagram\n  When   the diagram renders\n  Then   its box carries a scripted recommendation string, not just the risk label\n\nCASE-3 — a draft's verify-intent question does not double-flag it\n  Given  a `draft` requirement carrying both an open verify-intent question and the `unreviewed` signal\n  When   the Risk diagram renders\n  Then   it shows the `unreviewed` signal once and suppresses the separate verify-intent flag",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-MAPDIAGRAMS-055"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6736"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:882"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7479"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9662"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Diagrama Risk: doar cerințe semnalate, fiecare cu un sfat",
          "intent": "Diagrama Risk în mod deliberat nu este un recensământ — arată doar cerințele care poartă cel puțin un semnal de risc deschis, fiecare asociat cu o recomandare scriptată în loc de o etichetă goală, astfel încât un cititor știe ce să facă în privința ei. Împarte deduplicarea cu sursa listei de lucru a `next`, astfel încât întrebarea deschisă a unui draft nu este arătată niciodată de două ori sub două nume diferite pentru aceeași problemă de bază.",
          "contract": "> Diagrama Risk în mod deliberat nu este un recensământ — arată doar cerințele care\n> poartă cel puțin un semnal de risc deschis, fiecare asociat cu o recomandare scriptată\n> în loc de o etichetă goală, astfel încât un cititor știe ce să facă în privința ei.\n> Împarte deduplicarea cu sursa listei de lucru a `next`, astfel încât întrebarea deschisă\n> a unui draft nu este arătată niciodată de două ori sub două nume diferite pentru aceeași\n> problemă de bază.\n\nFiecare bullet de mai jos este obligatoriu.\n- Diagrama Risk arată doar cerințele cu cel puțin un semnal de risc\n  (confirmed cu zero membri; `draft`/`baseline`; ≥3 dependenți).\n- Diagrama Risk asociază fiecare din ele cu o recomandare scriptată.\n- Întrebarea deschisă verify-intent a unui `draft` este suprimată, subsumată de semnalul lui\n  `unreviewed`, astfel încât un draft nu este semnalat dublu.",
          "acceptance": "CASE-1 — diagrama Risk omite o cerință fără semnal de risc\n  Given  o cerință `confirmed` cu membri și puțini dependenți, alături de o cerință `confirmed` cu zero membri\n  When   se randează diagrama Risk\n  Then   doar cerința cu zero membri apare\n\nCASE-2 — fiecare nod Risk poartă o recomandare\n  Given  o cerință `draft` arătată în diagrama Risk\n  When   se randează diagrama\n  Then   cutia sa poartă un string de recomandare scriptată, nu doar eticheta de risc\n\nCASE-3 — întrebarea verify-intent a unui draft nu îl semnalează dublu\n  Given  o cerință `draft` purtând atât o întrebare deschisă verify-intent cât și semnalul `unreviewed`\n  When   se randează diagrama Risk\n  Then   arată semnalul `unreviewed` o singură dată și suprimă steag-ul separat verify-intent"
        }
      }
    },
    {
      "id": "ARCH-MEMBERDRIFT-027",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Reverse-direction member drift",
      "intent": "Drift detection has one eye open. `_reqlock.json` stores one hash per requirement — the contract — so it fires when the *prose* moves ahead of the code. The reverse is invisible: a member (the code or doc that realises a requirement) can change while the contract sits untouched, so behaviour ships and the spec quietly goes stale. This is the mechanical half of intent-sync: hash the dedicated members too, and warn when one moves while its requirement does not. Without it, \"confirmed\" can silently mean \"confirmed six versions ago\", and only a human re-reading both sides would ever notice.",
      "contract": [
        "Member content hashes live in a separate, versioned sidecar `_memberlock.json` (`{\"_schema\": N, \"members\": {id: {relfile: sha}}}`) computed only for single-requirement member files, on line-ending-normalized bytes, and it fails open when absent, corrupt or newer-schema. [[REQ-MEMBERDRIFT-879]]",
        "The gate warns for each confirmed requirement whose dedicated member changed since the sidecar while the requirement's own contract hash did not; a requirement whose contract also drifted is skipped, since forward drift already owns it. [[REQ-MEMBERDRIFT-880]]"
      ],
      "verify": [],
      "notes": [
        "Why `ac-count-high` is exempt: the eight criteria are the branch table of one decision — which (requirement, file) pair is reported as member drift — not eight behaviours that can break apart. Six of them (CASE-2 through CASE-6, CASE-8) are the suppression rules of that same decision. A split would produce two requirements sharing one contract.",
        "File-level granularity with a mono-requirement filter trades reach for silence: drift in a file shared by many requirements (e.g. a single engine file) is not attributed, by design. Repos with one file per capability get the most value.",
        "It asserts a member's bytes changed, not that the change is behaviourally meaningful — a reformat re-baselines like any edit. Re-running sync acknowledges and clears it."
      ],
      "current_impl": [
        "`compute_member_hashes`, `member_drift`, `load_memberlock`, `save_memberlock`, `_memberlock_path`, `MEMBERLOCK_SCHEMA` in `reqmap.py`, consumed by `cmd_check` — `compute_member_hashes` hashes mono-requirement member files, `member_drift` compares them to the sidecar while skipping forward-drifted requirements, and `cmd_check` emits a strict-promotable warn per result and re-baselines the sidecar on `--update-lock`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a file that is a member of one requirement and a file shared by two When   member hashes are computed Then   only the dedicated file is recorded",
        "CASE-2 — Given  a sidecar that is absent, corrupt, or of a newer schema When   it is loaded Then   it reads as empty (fail open)",
        "CASE-3 — Given  a confirmed requirement whose dedicated member changed but whose contract did not When   member drift is computed Then   that (requirement, file) pair is reported",
        "CASE-4 — Given  a requirement whose contract also drifted since the lock When   member drift is computed Then   it is not reported (forward drift owns it)",
        "CASE-5 — Given  a non-confirmed requirement whose member changed When   member drift is computed Then   it is not reported",
        "CASE-6 — Given  a member file with no recorded baseline in the sidecar When   member drift is computed Then   it is not reported",
        "CASE-7 — Given  a baselined dedicated member that is then edited When   the gate runs Then   it warns (exit 0) and, under `--strict`, exits non-zero",
        "CASE-8 — Given  the same member content saved once with LF and once with CRLF line endings When   the member hash is computed for each Then   the two hashes are identical (line endings are normalized before hashing)"
      ],
      "accept": "CASE-1\n  Given  a file that is a member of one requirement and a file shared by two\n  When   member hashes are computed\n  Then   only the dedicated file is recorded\nCASE-2\n  Given  a sidecar that is absent, corrupt, or of a newer schema\n  When   it is loaded\n  Then   it reads as empty (fail open)\nCASE-3\n  Given  a confirmed requirement whose dedicated member changed but whose contract did not\n  When   member drift is computed\n  Then   that (requirement, file) pair is reported\nCASE-4\n  Given  a requirement whose contract also drifted since the lock\n  When   member drift is computed\n  Then   it is not reported (forward drift owns it)\nCASE-5\n  Given  a non-confirmed requirement whose member changed\n  When   member drift is computed\n  Then   it is not reported\nCASE-6\n  Given  a member file with no recorded baseline in the sidecar\n  When   member drift is computed\n  Then   it is not reported\nCASE-7\n  Given  a baselined dedicated member that is then edited\n  When   the gate runs\n  Then   it warns (exit 0) and, under `--strict`, exits non-zero\nCASE-8\n  Given  the same member content saved once with LF and once with CRLF line endings\n  When   the member hash is computed for each\n  Then   the two hashes are identical (line endings are normalized before hashing)",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-DRIFT-003",
        "ARCH-SCAN-002",
        "ARCH-CHECK-006"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-GATE-102"
      ],
      "satisfied_by": [
        "REQ-MEMBERDRIFT-879",
        "REQ-MEMBERDRIFT-880"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2049"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2053"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2069"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2152"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2167"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2185"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2746"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:641"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.18",
      "priority": "should-have",
      "risks": [],
      "clauses": 8,
      "covered": 8,
      "i18n": {
        "ro": {
          "title": "Drift de membru în direcție inversă",
          "intent": "Detecția de drift are un singur ochi deschis. `_reqlock.json` stochează un hash per cerință — contractul — astfel încât se declanșează atunci când *proza* o ia înaintea codului. Inversul este invizibil: un membru (codul sau documentul care realizează o cerință) se poate schimba în timp ce contractul rămâne neatins, astfel încât comportamentul este livrat iar specificația devine în tăcere învechită. Aceasta este jumătatea mecanică a sincronizării de intenție: hash-uiește și membrii dedicați, și avertizează atunci când unul se mișcă în timp ce cerința lui nu. Fără aceasta, \"confirmed\" poate însemna în tăcere \"confirmat cu șase versiuni în urmă\", și doar un om care recitește ambele părți ar observa vreodată.",
          "contract": "> Detecția de drift are un singur ochi deschis. `_reqlock.json` stochează un hash per\n> cerință — contractul — astfel încât se declanșează atunci când *proza* o ia înaintea\n> codului. Inversul este invizibil: un membru (codul sau documentul care realizează o\n> cerință) se poate schimba în timp ce contractul rămâne neatins, astfel încât\n> comportamentul este livrat iar specificația devine în tăcere învechită. Aceasta este\n> jumătatea mecanică a sincronizării de intenție: hash-uiește și membrii dedicați, și\n> avertizează atunci când unul se mișcă în timp ce cerința lui nu. Fără aceasta,\n> \"confirmed\" poate însemna în tăcere \"confirmat cu șase versiuni în urmă\", și doar un\n> om care recitește ambele părți ar observa vreodată.\n\nFiecare punct de mai jos este obligatoriu.\n- Hash-urile de conținut ale membrilor trăiesc într-un sidecar separat, versionat, `_memberlock.json` (`{\"_schema\": N, \"members\": {id: {relfile: sha}}}`), calculate doar pentru fișierele de membru cu o singură cerință, pe octeți cu terminații de linie normalizate, și eșuează deschis atunci când lipsește, este corupt sau are o schemă mai nouă. [[REQ-MEMBERDRIFT-879]]\n- Gate-ul avertizează pentru fiecare cerință confirmată al cărei membru dedicat s-a schimbat de la sidecar în timp ce hash-ul propriu de contract al cerinței nu s-a schimbat; o cerință al cărei contract a suferit de asemenea drift este sărită, deoarece drift-ul înainte deja o deține. [[REQ-MEMBERDRIFT-880]]",
          "acceptance": "CASE-1\n  Given  un fișier care este membru al unei cerințe și un fișier partajat de două\n  When   hash-urile de membru sunt calculate\n  Then   doar fișierul dedicat este înregistrat\nCASE-2\n  Given  un sidecar lipsă, corupt, sau de o schemă mai nouă\n  When   este încărcat\n  Then   se citește ca gol (eșuează deschis)\nCASE-3\n  Given  o cerință confirmată al cărei membru dedicat s-a schimbat dar al cărei contract nu\n  When   drift-ul de membru este calculat\n  Then   acea pereche (cerință, fișier) este raportată\nCASE-4\n  Given  o cerință al cărei contract a suferit de asemenea drift de la lock\n  When   drift-ul de membru este calculat\n  Then   nu este raportată (drift-ul înainte o deține)\nCASE-5\n  Given  o cerință neconfirmată al cărei membru s-a schimbat\n  When   drift-ul de membru este calculat\n  Then   nu este raportată\nCASE-6\n  Given  un fișier de membru fără niciun baseline înregistrat în sidecar\n  When   drift-ul de membru este calculat\n  Then   nu este raportat\nCASE-7\n  Given  un membru dedicat cu baseline stabilit care este apoi editat\n  When   gate-ul rulează\n  Then   avertizează (exit 0) și, sub `--strict`, iese cu cod diferit de zero\nCASE-8\n  Given  același conținut de membru salvat o dată cu terminații de linie LF și o dată cu CRLF\n  When   hash-ul de membru este calculat pentru fiecare\n  Then   cele două hash-uri sunt identice (terminațiile de linie sunt normalizate înainte de hashing)"
        }
      }
    },
    {
      "id": "REQ-MEMBERDRIFT-879",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The member-hash sidecar",
      "intent": "Member hashes are kept in their own file, `_memberlock.json`, rather than folded into `_reqlock.json` — that keeps the requirement lock a byte-stable cross-repo contract an older seeded engine can still read, while giving reverse-direction drift its own versioned, fail-open storage. Only files dedicated to one requirement get a hash, because a shared file's change cannot be attributed to any single spec without noise.",
      "contract": [
        "Member content hashes live in a separate, versioned sidecar `_memberlock.json` (`{\"_schema\": N, \"members\": {id: {relfile: sha}}}`), so `_reqlock.json` stays a byte-stable cross-repo contract that an older seeded engine reads unchanged.",
        "The sidecar fails open (treated as empty) when absent, corrupt, or written by a newer `_schema` than the engine knows — degrading to \"reverse-drift off this run\" rather than crashing or mis-comparing.",
        "Member hashes are recorded only for files dedicated to ONE requirement (an `implements:`/`generated-from:` member of exactly one id); a file shared by several requirements is excluded, because a change there cannot be attributed without noise.",
        "Member hashes are computed on line-ending-normalized bytes (CRLF and lone CR folded to LF), so a lock generated on a CRLF working tree (Windows `core.autocrlf=true`) matches one verified on LF (Linux/CI). Without this every member shows spurious cross-platform drift, which `--strict` escalates to errors — mirrors the contract hash, already LF-normalized via the text-mode body parse."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — member hashes live in a separate versioned sidecar Given  a repo where `sync --update-lock` has run When   the requirements directory is inspected Then   `_memberlock.json` exists separately from `_reqlock.json`, holding `{\"_schema\": N, \"members\": {...}}`",
        "CASE-2 — — an absent, corrupt or newer-schema sidecar loads as empty Given  `_memberlock.json` is missing, and separately a copy whose `_schema` is above the engine's known value When   the sidecar is loaded Then   both cases yield an empty member map rather than an error",
        "CASE-3 — — only single-requirement member files get a hash Given  a file that is the sole `implements:` member of one requirement, and a file shared by two requirements When   member hashes are computed Then   the sidecar records the dedicated file's hash and omits the shared file",
        "CASE-4 — — member hashes are stable across line-ending styles Given  the same member content saved once with LF endings and once with CRLF endings When   the member hash is computed for each Then   both hashes are identical --------------------"
      ],
      "accept": "CASE-1 — member hashes live in a separate versioned sidecar\n  Given  a repo where `sync --update-lock` has run\n  When   the requirements directory is inspected\n  Then   `_memberlock.json` exists separately from `_reqlock.json`, holding `{\"_schema\": N,\n         \"members\": {...}}`\n\nCASE-2 — an absent, corrupt or newer-schema sidecar loads as empty\n  Given  `_memberlock.json` is missing, and separately a copy whose `_schema` is above the\n         engine's known value\n  When   the sidecar is loaded\n  Then   both cases yield an empty member map rather than an error\n\nCASE-3 — only single-requirement member files get a hash\n  Given  a file that is the sole `implements:` member of one requirement, and a file shared by\n         two requirements\n  When   member hashes are computed\n  Then   the sidecar records the dedicated file's hash and omits the shared file\n\nCASE-4 — member hashes are stable across line-ending styles\n  Given  the same member content saved once with LF endings and once with CRLF endings\n  When   the member hash is computed for each\n  Then   both hashes are identical\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-MEMBERDRIFT-027"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2049"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2053"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2069"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2167"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:641"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Sidecar-ul de hash de membri",
          "intent": "Hash-urile de membri sunt păstrate în propriul lor fișier, `_memberlock.json`, mai degrabă decât plutuite în `_reqlock.json` — asta păstrează lock-ul de cerințe un contract byte-stabil cross-repo pe care un engine vendored mai vechi îl poate încă citi, oferind în același timp drift-ului în direcție inversă propria sa stocare versionată, fail-open. Doar fișierele dedicate unei singure cerințe primesc un hash, deoarece schimbarea unui fișier partajat nu poate fi atribuită unei singure specificații fără zgomot.",
          "contract": "> Hash-urile de membri sunt păstrate în propriul lor fișier, `_memberlock.json`, mai\n> degrabă decât plutuite în `_reqlock.json` — asta păstrează lock-ul de cerințe un contract\n> byte-stabil cross-repo pe care un engine vendored mai vechi îl poate încă citi, oferind\n> în același timp drift-ului în direcție inversă propria sa stocare versionată, fail-open.\n> Doar fișierele dedicate unei singure cerințe primesc un hash, deoarece schimbarea unui\n> fișier partajat nu poate fi atribuită unei singure specificații fără zgomot.\n\nFiecare bullet de mai jos este obligatoriu.\n- Hash-urile de conținut ale membrilor trăiesc într-un sidecar separat, versionat, `_memberlock.json`\n  (`{\"_schema\": N, \"members\": {id: {relfile: sha}}}`), astfel încât `_reqlock.json` rămâne un\n  contract byte-stabil cross-repo pe care un engine vendored mai vechi îl citește neschimbat.\n- Sidecar-ul eșuează deschis (tratat ca gol) când lipsește, este corupt, sau a fost scris de\n  un `_schema` mai nou decât cel pe care engine-ul îl cunoaște — degradând la \"reverse-drift dezactivat\n  în această rulare\" în loc să se blocheze sau să comparare greșit.\n- Hash-urile de membri sunt înregistrate doar pentru fișiere dedicate UNEI singure cerințe (un\n  membru `implements:`/`generated-from:` al exact unui id); un fișier partajat de mai multe\n  cerințe este exclus, deoarece o schimbare acolo nu poate fi atribuită fără zgomot.\n- Hash-urile de membri sunt calculate pe bytes normalizați la nivelul terminatorilor de linie\n  (CRLF și CR solitar pliate la LF), astfel încât un lock generat pe un working tree CRLF\n  (Windows `core.autocrlf=true`) se potrivește cu unul verificat pe LF (Linux/CI). Fără asta\n  fiecare membru arată drift spuriu cross-platform, pe care `--strict` îl escaladează la erori —\n  oglindește hash-ul de contract, deja normalizat LF prin parsarea corpului în mod text.",
          "acceptance": "CASE-1 — hash-urile de membri trăiesc într-un sidecar separat, versionat\n  Given  un repo unde a rulat `sync --update-lock`\n  When   este inspectat directorul de cerințe\n  Then   `_memberlock.json` există separat de `_reqlock.json`, conținând `{\"_schema\": N,\n         \"members\": {...}}`\n\nCASE-2 — un sidecar absent, corupt sau cu schema mai nouă se încarcă gol\n  Given  `_memberlock.json` lipsește, și separat o copie al cărei `_schema` este peste valoarea\n         cunoscută de engine\n  When   sidecar-ul este încărcat\n  Then   ambele cazuri produc o hartă de membri goală, nu o eroare\n\nCASE-3 — doar fișierele de membri ale unei singure cerințe primesc un hash\n  Given  un fișier care este singurul membru `implements:` al unei cerințe, și un fișier partajat de\n         două cerințe\n  When   sunt calculate hash-urile de membri\n  Then   sidecar-ul înregistrează hash-ul fișierului dedicat și omite fișierul partajat\n\nCASE-4 — hash-urile de membri sunt stabile peste stiluri de terminator de linie\n  Given  același conținut de membru salvat o dată cu terminatori LF și o dată cu CRLF\n  When   este calculat hash-ul de membru pentru fiecare\n  Then   ambele hash-uri sunt identice\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-MEMBERDRIFT-880",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Warning when code moves ahead of its spec",
      "intent": "A confirmed requirement's contract can sit untouched while the code implementing it quietly changes underneath — \"confirmed\" then silently means \"confirmed several versions ago\". The gate warns for exactly that case, skipping a requirement whose contract also drifted (forward drift already owns that one) and a member with no recorded baseline yet (it gets baselined on the next sync instead of nagged on first sight).",
      "contract": [
        "The gate warns for each confirmed requirement whose dedicated member changed since the sidecar while the requirement's own contract hash did not. A requirement whose contract also drifted is skipped (forward drift already owns it).",
        "A non-confirmed requirement's member drift is never reported; the check only applies once a requirement has been confirmed.",
        "A member with no recorded baseline does not warn, so a freshly-tagged file is baselined on the next sync rather than nagged on first sight.",
        "The check is warn-only by default and is promoted to an error under `--strict` (it joins the same strict set as contract drift).",
        "`--update-lock` re-baselines the sidecar in lockstep with `_reqlock.json`."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the gate warns only when the member drifted and the contract did not Given  a confirmed requirement whose dedicated member changed while its contract hash is unchanged, and a second requirement where both drifted When   member drift is computed Then   the first pair is reported and the second is skipped",
        "CASE-2 — — a non-confirmed requirement's member drift is never reported Given  a `baseline` (not yet confirmed) requirement whose dedicated member changed When   member drift is computed Then   it produces no warning",
        "CASE-3 — — a member with no sidecar baseline is silent Given  a freshly-tagged member file absent from `_memberlock.json` When   member drift is computed Then   it produces no warning",
        "CASE-4 — — member drift is a warning by default and an error under --strict Given  a baselined dedicated member that was then edited When   `gate` runs, then `gate --strict` runs Then   the first exits 0 with a warning and the second exits non-zero",
        "CASE-5 — — --update-lock re-baselines both lock files together Given  a dedicated member edited since the last sidecar baseline When   `sync --update-lock` runs Then   `_memberlock.json`'s hash for that file matches its current content, alongside `_reqlock.json`'s refresh"
      ],
      "accept": "CASE-1 — the gate warns only when the member drifted and the contract did not\n  Given  a confirmed requirement whose dedicated member changed while its contract hash is\n         unchanged, and a second requirement where both drifted\n  When   member drift is computed\n  Then   the first pair is reported and the second is skipped\n\nCASE-2 — a non-confirmed requirement's member drift is never reported\n  Given  a `baseline` (not yet confirmed) requirement whose dedicated member changed\n  When   member drift is computed\n  Then   it produces no warning\n\nCASE-3 — a member with no sidecar baseline is silent\n  Given  a freshly-tagged member file absent from `_memberlock.json`\n  When   member drift is computed\n  Then   it produces no warning\n\nCASE-4 — member drift is a warning by default and an error under --strict\n  Given  a baselined dedicated member that was then edited\n  When   `gate` runs, then `gate --strict` runs\n  Then   the first exits 0 with a warning and the second exits non-zero\n\nCASE-5 — --update-lock re-baselines both lock files together\n  Given  a dedicated member edited since the last sidecar baseline\n  When   `sync --update-lock` runs\n  Then   `_memberlock.json`'s hash for that file matches its current content, alongside\n         `_reqlock.json`'s refresh",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-MEMBERDRIFT-027"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2185"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:641"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Avertizare când codul avansează înaintea specificației sale",
          "intent": "Contractul unei cerințe confirmed poate sta neatins în timp ce codul care îl implementează se schimbă tacit dedesubt — \"confirmed\" atunci înseamnă tacit \"confirmat cu câteva versiuni în urmă\". Gate-ul avertizează exact pentru acel caz, omițând o cerință al cărei contract a suferit de asemenea drift (forward drift deja deține acel caz) și un membru fără o baseline încă înregistrată (este baseline-uit la următorul sync, nu deranjat la prima vedere).",
          "contract": "> Contractul unei cerințe confirmed poate sta neatins în timp ce codul care îl\n> implementează se schimbă tacit dedesubt — \"confirmed\" atunci înseamnă tacit \"confirmat\n> cu câteva versiuni în urmă\". Gate-ul avertizează exact pentru acel caz, omițând o\n> cerință al cărei contract a suferit de asemenea drift (forward drift deja deține acel\n> caz) și un membru fără o baseline încă înregistrată (este baseline-uit la următorul sync,\n> nu deranjat la prima vedere).\n\nFiecare bullet de mai jos este obligatoriu.\n- Gate-ul avertizează pentru fiecare cerință confirmed al cărei membru dedicat s-a schimbat de la\n  sidecar în timp ce hash-ul propriu de contract al cerinței nu s-a schimbat. O cerință al cărei\n  contract a suferit de asemenea drift este omisă (forward drift deja o deține).\n- Drift-ul de membru al unei cerințe non-confirmed nu este niciodată raportat; verificarea se\n  aplică doar odată ce o cerință a fost confirmată.\n- Un membru fără baseline înregistrată nu avertizează, astfel încât un fișier proaspăt tagat este\n  baseline-uit la următorul sync în loc să fie deranjat la prima vedere.\n- Verificarea este warn-only implicit și este promovată la eroare sub\n  `--strict` (se alătură aceluiași set strict ca drift-ul de contract).\n- `--update-lock` re-baseline-uiește sidecar-ul în pas cu `_reqlock.json`.",
          "acceptance": "CASE-1 — gate-ul avertizează doar când membrul a suferit drift iar contractul nu\n  Given  o cerință confirmed al cărei membru dedicat s-a schimbat în timp ce hash-ul contractului este\n         neschimbat, și o a doua cerință unde amândouă au suferit drift\n  When   este calculat drift-ul de membru\n  Then   prima pereche este raportată iar a doua este omisă\n\nCASE-2 — drift-ul de membru al unei cerințe non-confirmed nu este niciodată raportat\n  Given  o cerință `baseline` (neconfirmată încă) al cărei membru dedicat s-a schimbat\n  When   este calculat drift-ul de membru\n  Then   nu produce niciun avertisment\n\nCASE-3 — un membru fără baseline în sidecar este tăcut\n  Given  un fișier membru proaspăt tagat, absent din `_memberlock.json`\n  When   este calculat drift-ul de membru\n  Then   nu produce niciun avertisment\n\nCASE-4 — drift-ul de membru este un avertisment implicit și o eroare sub --strict\n  Given  un membru dedicat baseline-uit care a fost apoi editat\n  When   rulează `gate`, apoi rulează `gate --strict`\n  Then   primul iese cu 0 cu un avertisment iar al doilea iese cu cod non-zero\n\nCASE-5 — --update-lock re-baseline-uiește ambele fișiere lock împreună\n  Given  un membru dedicat editat de la ultima baseline a sidecar-ului\n  When   rulează `sync --update-lock`\n  Then   hash-ul din `_memberlock.json` pentru acel fișier se potrivește cu conținutul său curent, alături de\n         reîmprospătarea lui `_reqlock.json`"
        }
      }
    },
    {
      "id": "ARCH-MODULEFILE-056",
      "layer": "bus",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Several requirements in one file",
      "intent": "A capability and the detailed design beneath it belong in one document, the way a requirements module holds many objects, instead of scattering one file per requirement across the folder — 618 of them, before this.",
      "contract": [
        "A block starts at a `---` line immediately followed by `id:`; each block loads as its own requirement.",
        "A file holding a single block is read exactly as before — unchanged for every pre-existing one-requirement file.",
        "A bare `---` used as a horizontal rule, not followed by `id:`, starts no new block.",
        "Confirming one requirement in a multi-block file changes that block's status alone.",
        "Only the first block in a file may fall back to the filename for its id; a later block with no `id:` does not."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — each id: block becomes its own requirement Given  a file whose text holds three `---`/`id:` blocks When   the engine loads the corpus Then   each block is loaded as its own requirement, keeping its own body and block index",
        "CASE-2 — — a single-block file is read exactly as before Given  a file holding one requirement, no second `---`/`id:` block When   `split_requirement_blocks` runs on its text Then   it returns the whole text unchanged, byte for byte",
        "CASE-3 — — a horizontal rule starts no new block Given  a file whose body contains a bare `---` not followed by `id:` When   the engine loads the corpus Then   the file still yields exactly one requirement, and the text after the rule stays part of that requirement's body",
        "CASE-4 — — confirming one block changes only that block's status Given  a two-block file, the second block carrying an `implements:` member When   `confirm` runs on the second block's id Then   only the second block's status becomes `confirmed`; the first block's status and body are unchanged",
        "CASE-5 — — only the first block may fall back to the filename for its id Given  a file whose first block carries no `id:` (so it falls back to the filename) and whose second block carries its own explicit `id:` When   the engine loads the corpus Then   the first block resolves to the filename's id and the second block keeps its own distinct id — the fallback claims the filename once, not per block"
      ],
      "accept": "CASE-1 — each id: block becomes its own requirement\n  Given  a file whose text holds three `---`/`id:` blocks\n  When   the engine loads the corpus\n  Then   each block is loaded as its own requirement, keeping its own body and block index\n\nCASE-2 — a single-block file is read exactly as before\n  Given  a file holding one requirement, no second `---`/`id:` block\n  When   `split_requirement_blocks` runs on its text\n  Then   it returns the whole text unchanged, byte for byte\n\nCASE-3 — a horizontal rule starts no new block\n  Given  a file whose body contains a bare `---` not followed by `id:`\n  When   the engine loads the corpus\n  Then   the file still yields exactly one requirement, and the text after the rule stays\n         part of that requirement's body\n\nCASE-4 — confirming one block changes only that block's status\n  Given  a two-block file, the second block carrying an `implements:` member\n  When   `confirm` runs on the second block's id\n  Then   only the second block's status becomes `confirmed`; the first block's status and\n         body are unchanged\n\nCASE-5 — only the first block may fall back to the filename for its id\n  Given  a file whose first block carries no `id:` (so it falls back to the filename) and\n         whose second block carries its own explicit `id:`\n  When   the engine loads the corpus\n  Then   the first block resolves to the filename's id and the second block keeps its own\n         distinct id — the fallback claims the filename once, not per block",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001"
      ],
      "used_by": [
        "ARCH-RETIRE-064"
      ],
      "satisfies": [
        "SYS-READ-103"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:984"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:987"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1003"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3295"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4291"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7559"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "should-have",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Mai multe cerințe într-un singur fișier",
          "intent": "O capabilitate și designul detaliat de dedesubt aparțin unui singur document, în felul în care un modul de cerințe conține multe obiecte, în loc să împrăștie câte un fișier per cerință în tot folderul — 618 dintre ele, înainte de aceasta.",
          "contract": "> O capabilitate și designul detaliat de dedesubt aparțin unui singur document, în felul\n> în care un modul de cerințe conține multe obiecte, în loc să împrăștie câte un fișier\n> per cerință în tot folderul — 618 dintre ele, înainte de aceasta.\n\nFiecare punct de mai jos este obligatoriu.\n- Un bloc începe la o linie `---` urmată imediat de `id:`; fiecare bloc se încarcă ca propria sa cerință.\n- Un fișier care conține un singur bloc este citit exact ca înainte — neschimbat pentru fiecare fișier preexistent cu o singură cerință.\n- Un `---` simplu folosit ca linie orizontală, neurmat de `id:`, nu începe niciun bloc nou.\n- Confirmarea unei cerințe într-un fișier cu mai multe blocuri schimbă doar statusul acelui bloc.\n- Doar primul bloc dintr-un fișier poate reveni la numele fișierului pentru id-ul său; un bloc ulterior fără `id:` nu.",
          "acceptance": "CASE-1 — fiecare bloc id: devine propria sa cerință\n  Given  un fișier al cărui text conține trei blocuri `---`/`id:`\n  When   motorul încarcă corpusul\n  Then   fiecare bloc este încărcat ca propria sa cerință, păstrându-și propriul corp și index de bloc\n\nCASE-2 — un fișier cu un singur bloc este citit exact ca înainte\n  Given  un fișier care conține o cerință, fără un al doilea bloc `---`/`id:`\n  When   `split_requirement_blocks` rulează pe textul său\n  Then   returnează întregul text neschimbat, octet cu octet\n\nCASE-3 — o linie orizontală nu începe niciun bloc nou\n  Given  un fișier al cărui corp conține un `---` simplu neurmat de `id:`\n  When   motorul încarcă corpusul\n  Then   fișierul produce în continuare exact o cerință, iar textul de după linie rămâne\n         parte din corpul acelei cerințe\n\nCASE-4 — confirmarea unui bloc schimbă doar statusul acelui bloc\n  Given  un fișier cu două blocuri, al doilea bloc purtând un membru `implements:`\n  When   `confirm` rulează pe id-ul celui de-al doilea bloc\n  Then   doar statusul celui de-al doilea bloc devine `confirmed`; statusul și corpul\n         primului bloc rămân neschimbate\n\nCASE-5 — doar primul bloc poate reveni la numele fișierului pentru id-ul său\n  Given  un fișier al cărui prim bloc nu poartă niciun `id:` (deci revine la numele fișierului) și\n         al cărui al doilea bloc poartă propriul `id:` explicit\n  When   motorul încarcă corpusul\n  Then   primul bloc se rezolvă la id-ul numelui fișierului iar al doilea bloc își păstrează\n         propriul id distinct — fallback-ul revendică numele fișierului o singură dată, nu per bloc"
        }
      }
    },
    {
      "id": "ARCH-NEW-004",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Scaffold a requirement",
      "intent": "When you want to document a new capability, you need a fresh requirement file with all the right sections in place. This command stamps one out from a standard template, so every requirement starts in the same predictable shape instead of being hand-typed from memory. Without it, files drift into different layouts and the tools that read them start to break.",
      "contract": [
        "Given a capability id, `new` writes `requirements/<ID>.md`, stamped from the scaffold (an on-disk `templates/requirement.md` if present, else the built-in template) with the placeholder `AREA-NAME-NNN` replaced by that id, creating the requirements directory if needed. [[REQ-NEW-881]]",
        "`new` refuses to overwrite an existing file, exiting non-zero and writing nothing; it warns but still succeeds on a same-area number collision, and its scaffold is pre-shaped to pass the linter's own authoring rules. [[REQ-NEW-882]]"
      ],
      "verify": [],
      "notes": [
        "The built-in template is the single source of the new-requirement shape; no `templates/` directory is required for `new` to work."
      ],
      "current_impl": [
        "`cmd_new` and `REQUIREMENT_TEMPLATE` in `reqmap.py`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  an empty registry When   `new FOO-NEW-099` runs Then   `requirements/FOO-NEW-099.md` is created with that id",
        "CASE-2 — Given  an id that already exists When   `new` runs for it Then   it exits non-zero and writes nothing",
        "CASE-3 — Given  a file created by `new` When   it is compared to the template Then   it matches with `AREA-NAME-NNN` replaced by the given id",
        "CASE-4 — Given  no on-disk template When   `new` runs Then   the built-in scaffold (Contract + Acceptance sections) is used",
        "CASE-5 — Given  `ARCH-MAP-007.md` already in the registry When   `new ARCH-VIEWER-007` runs Then   the file is created, the exit code is 0, and a warning names `ARCH-MAP-007`"
      ],
      "accept": "CASE-1\n  Given  an empty registry\n  When   `new FOO-NEW-099` runs\n  Then   `requirements/FOO-NEW-099.md` is created with that id\n\nCASE-2\n  Given  an id that already exists\n  When   `new` runs for it\n  Then   it exits non-zero and writes nothing\n\nCASE-3\n  Given  a file created by `new`\n  When   it is compared to the template\n  Then   it matches with `AREA-NAME-NNN` replaced by the given id\n\nCASE-4\n  Given  no on-disk template\n  When   `new` runs\n  Then   the built-in scaffold (Contract + Acceptance sections) is used\n\nCASE-5\n  Given  `ARCH-MAP-007.md` already in the registry\n  When   `new ARCH-VIEWER-007` runs\n  Then   the file is created, the exit code is 0, and a warning names `ARCH-MAP-007`",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001"
      ],
      "used_by": [
        "ARCH-DECOMPOSE-050",
        "ARCH-PROMOTE-TODO-001"
      ],
      "satisfies": [
        "SYS-AUTHOR-101"
      ],
      "satisfied_by": [
        "REQ-NEW-881",
        "REQ-NEW-882"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3116"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3138"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1134"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6670"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.02",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Schelărie pentru o cerință",
          "intent": "Când vrei să documentezi o capabilitate nouă, ai nevoie de un fișier de cerință proaspăt cu toate secțiunile corecte deja puse la locul lor. Această comandă ștanțează unul dintr-un șablon standard, astfel încât fiecare cerință începe în aceeași formă previzibilă în loc să fie tastată manual din memorie. Fără aceasta, fișierele alunecă spre aspecte diferite, iar instrumentele care le citesc încep să se strice.",
          "contract": "> Când vrei să documentezi o capabilitate nouă, ai nevoie de un fișier de cerință\n> proaspăt cu toate secțiunile corecte deja puse la locul lor. Această comandă ștanțează\n> unul dintr-un șablon standard, astfel încât fiecare cerință începe în aceeași formă\n> previzibilă în loc să fie tastată manual din memorie. Fără aceasta, fișierele alunecă\n> spre aspecte diferite, iar instrumentele care le citesc încep să se strice.\n\nFiecare punct de mai jos este obligatoriu.\n- Dat fiind un id de capabilitate, `new` scrie `requirements/<ID>.md`, ștanțat din schelărie (un `templates/requirement.md` de pe disc, dacă există, altfel șablonul încorporat) cu placeholder-ul `AREA-NAME-NNN` înlocuit de acel id, creând directorul de cerințe dacă este nevoie. [[REQ-NEW-881]]\n- `new` refuză să suprascrie un fișier existent, ieșind cu cod diferit de zero și nescriind nimic; avertizează dar tot reușește la o coliziune de număr în aceeași zonă, iar schelăria sa este preformată pentru a trece regulile proprii de redactare ale linter-ului. [[REQ-NEW-882]]",
          "acceptance": "CASE-1\n  Given  un registru gol\n  When   `new FOO-NEW-099` rulează\n  Then   `requirements/FOO-NEW-099.md` este creat cu acel id\n\nCASE-2\n  Given  un id care există deja\n  When   `new` rulează pentru el\n  Then   iese cu cod diferit de zero și nu scrie nimic\n\nCASE-3\n  Given  un fișier creat de `new`\n  When   este comparat cu șablonul\n  Then   se potrivește cu `AREA-NAME-NNN` înlocuit de id-ul dat\n\nCASE-4\n  Given  niciun șablon pe disc\n  When   `new` rulează\n  Then   este folosită schelăria încorporată (secțiunile Contract + Acceptance)\n\nCASE-5\n  Given  `ARCH-MAP-007.md` deja în registru\n  When   `new ARCH-VIEWER-007` rulează\n  Then   fișierul este creat, codul de ieșire este 0, iar un avertisment numește `ARCH-MAP-007`"
        }
      }
    },
    {
      "id": "REQ-NEW-881",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Stamping a fresh requirement file from a template",
      "intent": "A hand-typed requirement file drifts into its own layout, and the tools that read requirements start to break on it. `new` stamps `requirements/<ID>.md` from a template — the built-in one, or an on-disk `templates/requirement.md` when a repo wants to customize it — with the id placeholder filled in, so every requirement starts in the same predictable shape.",
      "contract": [
        "Given a capability id, `new` writes `requirements/<ID>.md`, stamped from the scaffold with the placeholder `AREA-NAME-NNN` replaced by that id.",
        "`new` creates the requirements directory if it is absent.",
        "The scaffold is the engine's built-in template.",
        "An on-disk `templates/requirement.md`, when present, overrides the built-in template."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — new stamps the scaffold with the given id Given  a template containing the placeholder `AREA-NAME-NNN` When   `new CORE-FOO-001` runs Then   `requirements/CORE-FOO-001.md` exists, contains \"CORE-FOO-001\", and no longer contains \"AREA-NAME-NNN\"",
        "CASE-2 — — new creates a missing requirements directory Given  a `reqs_dir` path that does not exist yet When   `new CORE-FOO-001` runs Then   the directory is created and `requirements/CORE-FOO-001.md` is written inside it",
        "CASE-3 — — new falls back to REQUIREMENT_TEMPLATE with no on-disk template Given  no `templates/requirement.md` on disk When   `new CORE-FOO-001` runs with `tmpl_path=None` Then   the written file carries the built-in scaffold's sections — \"## Description\", \"## Cases\", and \"CASE-1\"",
        "CASE-4 — — an on-disk template wins over the built-in scaffold Given  an on-disk template file distinct from `REQUIREMENT_TEMPLATE` When   `new CORE-FOO-001` runs with that template's path Then   the written file is stamped from the on-disk template's content, not the built-in one --------------------"
      ],
      "accept": "CASE-1 — new stamps the scaffold with the given id\n  Given  a template containing the placeholder `AREA-NAME-NNN`\n  When   `new CORE-FOO-001` runs\n  Then   `requirements/CORE-FOO-001.md` exists, contains \"CORE-FOO-001\", and no longer\n         contains \"AREA-NAME-NNN\"\n\nCASE-2 — new creates a missing requirements directory\n  Given  a `reqs_dir` path that does not exist yet\n  When   `new CORE-FOO-001` runs\n  Then   the directory is created and `requirements/CORE-FOO-001.md` is written inside it\n\nCASE-3 — new falls back to REQUIREMENT_TEMPLATE with no on-disk template\n  Given  no `templates/requirement.md` on disk\n  When   `new CORE-FOO-001` runs with `tmpl_path=None`\n  Then   the written file carries the built-in scaffold's sections — \"## Description\",\n         \"## Cases\", and \"CASE-1\"\n\nCASE-4 — an on-disk template wins over the built-in scaffold\n  Given  an on-disk template file distinct from `REQUIREMENT_TEMPLATE`\n  When   `new CORE-FOO-001` runs with that template's path\n  Then   the written file is stamped from the on-disk template's content, not the\n         built-in one\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-NEW-004"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3138"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1134"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Ștanțarea unui fișier de cerință nou dintr-un template",
          "intent": "Un fișier de cerință scris de mână deviază spre propriul lui layout, iar tool-urile care citesc cerințe încep să se strice pe el. `new` ștanțează `requirements/<ID>.md` dintr-un template — cel built-in, sau un `templates/requirement.md` pe disc când un repo vrea să-l personalizeze — cu placeholder-ul id-ului completat, astfel încât fiecare cerință începe în aceeași formă previzibilă.",
          "contract": "> Un fișier de cerință scris de mână deviază spre propriul lui layout, iar tool-urile\n> care citesc cerințe încep să se strice pe el. `new` ștanțează `requirements/<ID>.md`\n> dintr-un template — cel built-in, sau un `templates/requirement.md` pe disc când un repo\n> vrea să-l personalizeze — cu placeholder-ul id-ului completat, astfel încât fiecare\n> cerință începe în aceeași formă previzibilă.\n\nFiecare bullet de mai jos este obligatoriu.\n- Dat fiind un id de capabilitate, `new` scrie `requirements/<ID>.md`, ștanțat din scaffold\n  cu placeholder-ul `AREA-NAME-NNN` înlocuit de acel id.\n- `new` creează directorul de cerințe dacă lipsește.\n- Scaffold-ul este template-ul built-in al engine-ului.\n- Un `templates/requirement.md` pe disc, când este prezent, are prioritate față de template-ul\n  built-in.",
          "acceptance": "CASE-1 — new ștanțează scaffold-ul cu id-ul dat\n  Given  un template conținând placeholder-ul `AREA-NAME-NNN`\n  When   rulează `new CORE-FOO-001`\n  Then   `requirements/CORE-FOO-001.md` există, conține \"CORE-FOO-001\", și nu mai\n         conține \"AREA-NAME-NNN\"\n\nCASE-2 — new creează un director de cerințe lipsă\n  Given  o cale `reqs_dir` care nu există încă\n  When   rulează `new CORE-FOO-001`\n  Then   directorul este creat iar `requirements/CORE-FOO-001.md` este scris în interiorul lui\n\nCASE-3 — new cade înapoi la REQUIREMENT_TEMPLATE fără template pe disc\n  Given  niciun `templates/requirement.md` pe disc\n  When   rulează `new CORE-FOO-001` cu `tmpl_path=None`\n  Then   fișierul scris poartă secțiunile scaffold-ului built-in — \"## Description\",\n         \"## Cases\", și \"CASE-1\"\n\nCASE-4 — un template pe disc câștigă în fața scaffold-ului built-in\n  Given  un fișier template pe disc distinct de `REQUIREMENT_TEMPLATE`\n  When   rulează `new CORE-FOO-001` cu calea acelui template\n  Then   fișierul scris este ștanțat din conținutul template-ului de pe disc, nu din cel\n         built-in\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-NEW-882",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Refusing to clobber, and a scaffold that lints clean",
      "intent": "Running `new` twice by accident must never destroy half-finished work, so an existing file is left untouched and the command exits non-zero instead. A same-area number collision is a softer problem — ids stay unique by their full text, so it only warns — and the scaffold itself is written to already satisfy the linter's authoring rules, so a file built from it starts clean rather than immediately flagged.",
      "contract": [
        "`new` refuses to overwrite an existing file. It exits non-zero and writes nothing.",
        "The emitted Contract section opens with \"Every bullet below is binding.\", so the author writes clauses in present tense without a `shall` or `must` on each line.",
        "The scaffold's guidance names the authoring rules the linter enforces, so a file written from it starts clean.",
        "`new` warns, and still exits zero, when another requirement in the same area already uses the same `NNN` number. Ids stay unique by their full text; the warning keeps numbers unambiguous in conversation."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — new refuses to clobber an existing requirement file Given  `requirements/CORE-FOO-001.md` already exists with content \"existing\\n\" When   `new CORE-FOO-001` runs Then   it exits non-zero and the file's content is unchanged",
        "CASE-2 — — the template's Description opens with the binding line and no modal verbs Given  `REQUIREMENT_TEMPLATE`, comments stripped When   its Description clauses are extracted with `_lint_prose` Then   the template contains \"Every bullet below is binding.\" and no clause contains \"shall\" or \"must\"",
        "CASE-3 — — the shipped template body passes its own linter Given  a requirement built from `REQUIREMENT_TEMPLATE`'s body, status `confirmed` When   `lint_requirement` runs on it Then   the findings carry no `anonymous-subject`, `statement-too-long`, or `statement-size` check",
        "CASE-4 — — a same-area number collision warns but still creates the file Given  `ARCH-MAP-007.md` already in the registry When   `new ARCH-VIEWER-007` runs Then   `ARCH-VIEWER-007.md` is created, the exit code is 0, and the output contains \"WARN\" naming \"ARCH-MAP-007\""
      ],
      "accept": "CASE-1 — new refuses to clobber an existing requirement file\n  Given  `requirements/CORE-FOO-001.md` already exists with content \"existing\\n\"\n  When   `new CORE-FOO-001` runs\n  Then   it exits non-zero and the file's content is unchanged\n\nCASE-2 — the template's Description opens with the binding line and no modal verbs\n  Given  `REQUIREMENT_TEMPLATE`, comments stripped\n  When   its Description clauses are extracted with `_lint_prose`\n  Then   the template contains \"Every bullet below is binding.\" and no clause contains\n         \"shall\" or \"must\"\n\nCASE-3 — the shipped template body passes its own linter\n  Given  a requirement built from `REQUIREMENT_TEMPLATE`'s body, status `confirmed`\n  When   `lint_requirement` runs on it\n  Then   the findings carry no `anonymous-subject`, `statement-too-long`, or\n         `statement-size` check\n\nCASE-4 — a same-area number collision warns but still creates the file\n  Given  `ARCH-MAP-007.md` already in the registry\n  When   `new ARCH-VIEWER-007` runs\n  Then   `ARCH-VIEWER-007.md` is created, the exit code is 0, and the output contains\n         \"WARN\" naming \"ARCH-MAP-007\"",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-NEW-004"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3138"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1134"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6670"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Refuzul de a suprascrie, și un scaffold care trece lint curat",
          "intent": "Rularea `new` de două ori din greșeală nu trebuie niciodată să distrugă o muncă pe jumătate terminată, deci un fișier existent este lăsat neatins iar comanda iese cu cod non-zero în loc. O coliziune de număr în aceeași arie este o problemă mai blândă — id-urile rămân unice prin textul lor complet, deci doar avertizează — iar scaffold-ul în sine este scris să satisfacă deja regulile de redactare ale linter-ului, astfel încât un fișier construit din el pornește curat în loc să fie semnalat imediat.",
          "contract": "> Rularea `new` de două ori din greșeală nu trebuie niciodată să distrugă o muncă pe\n> jumătate terminată, deci un fișier existent este lăsat neatins iar comanda iese cu cod\n> non-zero în loc. O coliziune de număr în aceeași arie este o problemă mai blândă —\n> id-urile rămân unice prin textul lor complet, deci doar avertizează — iar scaffold-ul în\n> sine este scris să satisfacă deja regulile de redactare ale linter-ului, astfel încât un\n> fișier construit din el pornește curat în loc să fie semnalat imediat.\n\nFiecare bullet de mai jos este obligatoriu.\n- `new` refuză să suprascrie un fișier existent. Iese cu cod non-zero și nu scrie nimic.\n- Secțiunea Contract emisă începe cu \"Every bullet below is binding.\", astfel încât\n  autorul scrie clauze la timpul prezent fără un `shall` sau `must` pe fiecare linie.\n- Ghidajul scaffold-ului numește regulile de redactare pe care linter-ul le impune, astfel încât\n  un fișier scris din el pornește curat.\n- `new` avertizează, și tot iese zero, când o altă cerință din aceeași arie folosește deja\n  același număr `NNN`. Id-urile rămân unice prin textul lor complet; avertismentul păstrează\n  numerele lipsite de ambiguitate în conversație.",
          "acceptance": "CASE-1 — new refuză să suprascrie un fișier de cerință existent\n  Given  `requirements/CORE-FOO-001.md` există deja cu conținutul \"existing\\n\"\n  When   rulează `new CORE-FOO-001`\n  Then   iese cu cod non-zero iar conținutul fișierului este neschimbat\n\nCASE-2 — Description-ul template-ului începe cu linia obligatorie și fără verbe modale\n  Given  `REQUIREMENT_TEMPLATE`, cu comentariile eliminate\n  When   clauzele sale Description sunt extrase cu `_lint_prose`\n  Then   template-ul conține \"Every bullet below is binding.\" și nicio clauză nu conține\n         \"shall\" sau \"must\"\n\nCASE-3 — corpul template-ului livrat trece propriul lui linter\n  Given  o cerință construită din corpul lui `REQUIREMENT_TEMPLATE`, status `confirmed`\n  When   rulează `lint_requirement` pe ea\n  Then   findings-urile nu poartă niciun `anonymous-subject`, `statement-too-long`, sau\n         verificare `statement-size`\n\nCASE-4 — o coliziune de număr în aceeași arie avertizează dar tot creează fișierul\n  Given  `ARCH-MAP-007.md` deja în registry\n  When   rulează `new ARCH-VIEWER-007`\n  Then   `ARCH-VIEWER-007.md` este creat, codul de ieșire este 0, iar ieșirea conține\n         \"WARN\" numind \"ARCH-MAP-007\""
        }
      }
    },
    {
      "id": "ARCH-NEXT-013",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "What-should-I-do-next report",
      "intent": "Faced with a whole project of requirements, it's hard to know what to work on first. This command looks at every requirement, spots the gaps — ones with no code, no tests, or unanswered questions — and prints them as a short, prioritised to-do list right in the terminal. It puts the most urgent work at the top so you can just start. Without it, you'd have to open the visual map and eyeball the risk yourself to figure out where to begin.",
      "contract": [
        "`next` groups every requirement's open risk signals — read from the same `_risk_signals`/`RISK_ADVICE` source the Risk tab uses — into action buckets, behind a progress header. [[REQ-NEXT-883]]",
        "`next` surfaces exactly the actionable buckets, most urgent first: `unimplemented` (Orphans), `untested` (Needs tests), `unverified-intent` (Needs intent review), `unreviewed` (Drafts to review), plus advisory Granularity/Redundancy buckets and an Untagged-files bucket. [[REQ-NEXT-884]]",
        "Within a bucket, `next` orders items by `priority` rank, then by descending extract `risk:` score, then by id, and names the file to open. [[REQ-NEXT-885]]",
        "By default `next` shows at most the top few items of a bucket, truncating each independently with a `... N more` line; `--all` lists everything. [[REQ-NEXT-886]]",
        "With no requirements at all, `next` prints a distinct message pointing at `init`/`new`; otherwise it prints the all-clear line when nothing is open. Either way it writes no file and always exits 0. [[REQ-NEXT-887]]"
      ],
      "verify": [],
      "notes": [
        "`lint_exempt: ac-count-high` — the twelve cases are twelve buckets of ONE report, not twelve capabilities. `next` prints a single output and each case pins one line of it; splitting it would mint a requirement per bucket for a command that has one behaviour.",
        "Each bucket is truncated independently, so a higher-priority bucket is never hidden below a longer lower-priority one.",
        "The dedup of a draft's intent question lives inside the shared `_risk_signals` source, which is why `next` and the Risk tab report the same signals for the same requirement.",
        "The untagged-files scan is skipped when no `code_root` is supplied — the usual case for unit-test callers.",
        "A reviewed requirement may legitimately appear in more than one bucket (e.g. a confirmed requirement both `untested` and `unverified-intent`) — these are two distinct actions, by design. A `draft` never double-lists, because its `unverified-intent` is suppressed at the source (subsumed by `unreviewed`).",
        "`next` is the prioritized worklist; `findings` remains the exhaustive raw list of every open verify-intent bullet (including drafts). The two answer different questions — this divergence is intentional and documented, not drift.",
        "`risk:` ordering only discriminates extract-authored drafts (hand-authored requirements have no `risk:` field → score 0, ordered by id).",
        "`next` has printed Granularity and Redundancy since ADR-0020 shipped; this contract omitted both until 2026-09-03, when the gap was fixed alongside unifying Granularity's threshold with lint's `LINT_AC_MAX` — previously `next` used its own unscoped 5-AC threshold with no `lint_exempt` honoring, so `next` and `lint` could report different sets for the same corpus (`ARCH-DECOMPOSE-050`'s notes record the matching fix on lint's side)."
      ],
      "current_impl": [
        "`cmd_next`, `_risk_score`, and `_scan_untagged` in `reqmap.py` — prints the header, builds a minimal node per requirement, collects `_risk_signals`, orders each bucket by `risk:` score then id, truncates to the top-N unless `--all`, and prints `RISK_ADVICE` text. The draft intent-dedup is in `_risk_signals` (shared with the Risk tab). `_scan_untagged` adds the untagged-files bucket using the same walk as `scan_members`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  any corpus When   `next` runs Then   the output starts with a progress header carrying the confirmed/tested/draft counts",
        "CASE-2 — Given  a confirmed requirement with implementing code but no `tested-by` When   `next` runs Then   it is listed under \"Needs tests\" with `requirements/<ID>.md`",
        "CASE-3 — Given  a draft requirement with an open verify bullet When   `next` runs Then   it is listed under \"Drafts to review\" and NOT under \"Needs intent review\" (source dedup)",
        "CASE-4 — Given  a confirmed requirement with an open Verify-intent bullet When   `next` runs Then   it is listed under \"Needs intent review\"",
        "CASE-5 — Given  a bucket with more items than the top-N When   `next` runs Then   the default view truncates and prints a `... N more` line; `--all` lists every item",
        "CASE-6 — Given  a draft with `risk: 2` (REVIEW) and a `risk: 0` draft in one bucket When   `next` runs Then   the REVIEW draft is ordered first and tagged `[REVIEW]`",
        "CASE-7 — Given  a `must-have`, a `should-have`, and a no-`priority` requirement in one bucket When   `next` runs Then   they are ordered must-have, should-have, then no-priority",
        "CASE-8 — Given  an empty registry, or one with no open signals When   `next` runs Then   it prints the \"no requirements yet\" message or the all-clear line respectively, writes no files, and returns 0",
        "CASE-9 — Given  scannable files in the repo with no membership tag When   `next` runs with a code_root Then   they are listed under \"Untagged files\" with a `reqmap.py init` suggestion",
        "CASE-10 — Given  a confirmed requirement with no scanned member, whose node in the committed `_map.json` records one When   `next` runs with a `reqs_dir` Then   the Orphans bucket carries a note naming that member and `--code`",
        "CASE-11 — Given  a confirmed requirement with more acceptance criteria than `LINT_AC_MAX`, not exempt When   `next` runs Then   it is listed under \"Granularity\" with its AC count and `requirements/<ID>.md`",
        "CASE-12 — Given  two confirmed requirements whose Description states the same obligation, word for word When   `next` runs Then   they are listed together under \"Redundancy\" as one group"
      ],
      "accept": "CASE-1\n  Given  any corpus\n  When   `next` runs\n  Then   the output starts with a progress header carrying the confirmed/tested/draft counts\n\nCASE-2\n  Given  a confirmed requirement with implementing code but no `tested-by`\n  When   `next` runs\n  Then   it is listed under \"Needs tests\" with `requirements/<ID>.md`\n\nCASE-3\n  Given  a draft requirement with an open verify bullet\n  When   `next` runs\n  Then   it is listed under \"Drafts to review\" and NOT under \"Needs intent review\" (source dedup)\n\nCASE-4\n  Given  a confirmed requirement with an open Verify-intent bullet\n  When   `next` runs\n  Then   it is listed under \"Needs intent review\"\n\nCASE-5\n  Given  a bucket with more items than the top-N\n  When   `next` runs\n  Then   the default view truncates and prints a `... N more` line; `--all` lists every item\n\nCASE-6\n  Given  a draft with `risk: 2` (REVIEW) and a `risk: 0` draft in one bucket\n  When   `next` runs\n  Then   the REVIEW draft is ordered first and tagged `[REVIEW]`\n\nCASE-7\n  Given  a `must-have`, a `should-have`, and a no-`priority` requirement in one bucket\n  When   `next` runs\n  Then   they are ordered must-have, should-have, then no-priority\n\nCASE-8\n  Given  an empty registry, or one with no open signals\n  When   `next` runs\n  Then   it prints the \"no requirements yet\" message or the all-clear line respectively,\n         writes no files, and returns 0\n\nCASE-9\n  Given  scannable files in the repo with no membership tag\n  When   `next` runs with a code_root\n  Then   they are listed under \"Untagged files\" with a `reqmap.py init` suggestion\n\nCASE-10\n  Given  a confirmed requirement with no scanned member, whose node in the committed `_map.json` records one\n  When   `next` runs with a `reqs_dir`\n  Then   the Orphans bucket carries a note naming that member and `--code`\n\nCASE-11\n  Given  a confirmed requirement with more acceptance criteria than `LINT_AC_MAX`, not exempt\n  When   `next` runs\n  Then   it is listed under \"Granularity\" with its AC count and `requirements/<ID>.md`\n\nCASE-12\n  Given  two confirmed requirements whose Description states the same obligation, word for word\n  When   `next` runs\n  Then   they are listed together under \"Redundancy\" as one group",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-MAP-007"
      ],
      "used_by": [
        "ARCH-AUDIT-065",
        "ARCH-COVERAGE-029",
        "ARCH-REDUNDANCY-058"
      ],
      "satisfies": [
        "SYS-REPORT-105"
      ],
      "satisfied_by": [
        "REQ-NEXT-883",
        "REQ-NEXT-884",
        "REQ-NEXT-885",
        "REQ-NEXT-886",
        "REQ-NEXT-887"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1592"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2340"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4259"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4272"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4300"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2399"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2507"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2516"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2522"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6284"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6630"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6735"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7115"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8574"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.12",
      "priority": "",
      "risks": [],
      "clauses": 12,
      "covered": 12
    },
    {
      "id": "REQ-NEXT-883",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "next reads the same risk signals the Risk tab reads",
      "intent": "`next` does not run a second analysis: it reads `_risk_signals` and `RISK_ADVICE`, the exact source the map's Risk diagram already uses, and groups the results into buckets behind a one-line progress header. Sharing the source is what keeps the terminal report and the visual map from ever disagreeing about what is wrong with a requirement.",
      "contract": [
        "`next` groups every requirement's open risk signals into action buckets.",
        "`next` reads those signals from `_risk_signals` and their wording from `RISK_ADVICE`, the same two sources that drive the Risk tab. There is never a second signal path.",
        "`next` prints a progress header `N requirement(s) · X confirmed · Y tested · Z unreviewed (draft + baseline, the population of the Drafts bucket)` before the buckets.",
        "In that header, `tested` counts the requirements that have a `tested-by` member."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — an untested confirmed requirement lands in the Needs-tests bucket Given  a confirmed requirement with an `implements` member but no `tested-by` member When   `cmd_next` runs Then   its output carries a \"Needs tests\" bucket naming that requirement",
        "CASE-2 — — a bucket's advice line matches RISK_ADVICE verbatim Given  a confirmed requirement carrying the `untested` signal When   `cmd_next` runs Then   the \"Needs tests\" bucket's `-> ...` line equals `RISK_ADVICE[\"untested\"]` exactly",
        "CASE-3 — — the header names all four counts Given  one confirmed-and-tested requirement and one draft requirement When   `cmd_next` runs Then   its first line reads \"2 requirement(s) · 1 confirmed · 1 tested · 1 unreviewed\"",
        "CASE-4 — — implements-only members do not count as tested Given  two confirmed requirements: one with a `tested-by` member, one with only an `implements` member When   `cmd_next` runs Then   the header reads \"1 tested\", not \"2 tested\" --------------------"
      ],
      "accept": "CASE-1 — an untested confirmed requirement lands in the Needs-tests bucket\n  Given  a confirmed requirement with an `implements` member but no `tested-by` member\n  When   `cmd_next` runs\n  Then   its output carries a \"Needs tests\" bucket naming that requirement\n\nCASE-2 — a bucket's advice line matches RISK_ADVICE verbatim\n  Given  a confirmed requirement carrying the `untested` signal\n  When   `cmd_next` runs\n  Then   the \"Needs tests\" bucket's `-> ...` line equals `RISK_ADVICE[\"untested\"]` exactly\n\nCASE-3 — the header names all four counts\n  Given  one confirmed-and-tested requirement and one draft requirement\n  When   `cmd_next` runs\n  Then   its first line reads \"2 requirement(s) · 1 confirmed · 1 tested · 1 unreviewed\"\n\nCASE-4 — implements-only members do not count as tested\n  Given  two confirmed requirements: one with a `tested-by` member, one with only an `implements` member\n  When   `cmd_next` runs\n  Then   the header reads \"1 tested\", not \"2 tested\"\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-NEXT-013"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4300"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2399"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8574"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "next citește aceleași semnale de risc pe care le citește tab-ul Risk",
          "intent": "`next` nu rulează o a doua analiză: citește `_risk_signals` și `RISK_ADVICE`, exact sursa pe care diagrama Risk a map-ului o folosește deja, și grupează rezultatele în bucket-uri în spatele unui header de progres pe o singură linie. Împărțirea sursei este ce păstrează raportul din terminal și map-ul vizual să nu fie niciodată în dezacord despre ce e greșit la o cerință.",
          "contract": "> `next` nu rulează o a doua analiză: citește `_risk_signals` și `RISK_ADVICE`, exact\n> sursa pe care diagrama Risk a map-ului o folosește deja, și grupează rezultatele în\n> bucket-uri în spatele unui header de progres pe o singură linie. Împărțirea sursei este\n> ce păstrează raportul din terminal și map-ul vizual să nu fie niciodată în dezacord\n> despre ce e greșit la o cerință.\n\nFiecare bullet de mai jos este obligatoriu.\n- `next` grupează semnalele de risc deschise ale fiecărei cerințe în bucket-uri de acțiune.\n- `next` citește acele semnale din `_risk_signals` și formularea lor din `RISK_ADVICE`, aceleași\n  două surse care conduc tab-ul Risk. Nu există niciodată o a doua cale de semnal.\n- `next` afișează un header de progres `N requirement(s) · X confirmed · Y tested · Z unreviewed (draft + baseline, the population of the Drafts bucket)`\n  înainte de bucket-uri.\n- În acel header, `tested` numără cerințele care au un membru `tested-by`.",
          "acceptance": "CASE-1 — o cerință confirmed netestată aterizează în bucket-ul Needs-tests\n  Given  o cerință confirmed cu un membru `implements` dar fără membru `tested-by`\n  When   rulează `cmd_next`\n  Then   ieșirea sa poartă un bucket \"Needs tests\" care numește acea cerință\n\nCASE-2 — linia de sfat a unui bucket se potrivește cu RISK_ADVICE verbatim\n  Given  o cerință confirmed purtând semnalul `untested`\n  When   rulează `cmd_next`\n  Then   linia `-> ...` a bucket-ului \"Needs tests\" este egală exact cu `RISK_ADVICE[\"untested\"]`\n\nCASE-3 — header-ul numește toate cele patru numărători\n  Given  o cerință confirmed-și-tested și o cerință draft\n  When   rulează `cmd_next`\n  Then   prima sa linie se citește \"2 requirement(s) · 1 confirmed · 1 tested · 1 unreviewed\"\n\nCASE-4 — membrii doar-implements nu se numără ca tested\n  Given  două cerințe confirmed: una cu un membru `tested-by`, una doar cu un membru `implements`\n  When   rulează `cmd_next`\n  Then   header-ul se citește \"1 tested\", nu \"2 tested\"\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-NEXT-884",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Four action buckets, two advisory ones, and untagged files",
      "intent": "The four action buckets — Orphans, Needs tests, Needs intent review, Drafts to review — print most urgent first, because that order is the point of the whole command: start at the top. Granularity and Redundancy print below them, sharing their exact thresholds with `lint` and `dupes` so the three commands never disagree about which requirement is oversize or duplicated. Untagged files ranks lowest of all.",
      "contract": [
        "`next` surfaces exactly the actionable buckets: `unimplemented` (Orphans), `untested` (Needs tests), `unverified-intent` (Needs intent review), `unreviewed` (Drafts to review).",
        "`next` prints those four buckets in that order, most urgent first.",
        "`next` also prints two advisory buckets below the four action buckets: `Granularity` (a requirement with more acceptance criteria than lint's `LINT_AC_MAX`) and `Redundancy` (requirements whose Description states the same obligation, byte-identical once normalized).",
        "Granularity's set comes from `_oversize`, the one predicate `lint_requirement`'s `ac-count-high` check also calls — the same threshold, the same `LINT_STATUSES` scope, and the same `lint_exempt: [ac-count-high]` honoring, so `next` and `lint` never disagree on which requirement is oversize.",
        "Redundancy's set comes from `_redundant_groups`, the exact-match floor `dupes` also uses.",
        "Neither advisory bucket changes `next`'s exit code.",
        "`next` omits `blast-radius`, because that signal is a caution, not a task.",
        "`next` surfaces every scannable file that carries no membership tag as an \"Untagged files\" bucket, ranked lowest of all.",
        "That bucket omits prose in the auto-draft \"ignore\" bucket (`CLAUDE.md`, `TODO.md`, `CHANGELOG.md`, `LICENSE`, `_`-prefixed files): those are invisible to reqmap by contract.",
        "That bucket also omits repository boilerplate that never carries a tag by design: decision records under an `adr/` or `decisions/` directory, issue and pull-request templates, `SECURITY.md`, `CODE_OF_CONDUCT.md` and dependabot configuration (`_UNTAGGED_NOISE`).",
        "`next` skips that untagged scan when the caller gives no `code_root`.",
        "An Orphans item may have members recorded in the committed `_map.json` that this scan did not find. Then `next` adds a note naming one such member and suggesting `--code <dir>`. The note is advice; the item stays in the bucket."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — all four action bucket labels appear for their matching signal Given  one confirmed requirement with no members, one confirmed-untested, one confirmed with an open Verify-intent bullet, one draft When   `cmd_next` runs Then   its output contains \"Orphans\", \"Needs tests\", \"Needs intent review\" and \"Drafts to review\"",
        "CASE-2 — — Orphans prints before Needs tests, before Needs intent review, before Drafts Given  one requirement triggering each of the four action signals When   `cmd_next` runs Then   \"Orphans\" appears before \"Needs tests\", which appears before \"Needs intent review\", which appears before \"Drafts to review\"",
        "CASE-3 — — a high-fan-in requirement's blast-radius signal never prints Given  a confirmed, tested requirement with three dependents (triggers `blast-radius`) When   `cmd_next` runs Then   its output never contains the word \"blast-radius\"",
        "CASE-4 — — Untagged files prints after every action bucket Given  a draft requirement (triggers \"Drafts to review\") plus an untagged `orphan.py` in the code root When   `cmd_next` runs with that code root Then   \"Untagged files\" appears after \"Drafts to review\" in the output",
        "CASE-5 — — CLAUDE.md and TODO.md never appear as untagged Given  an untagged `CLAUDE.md`, `TODO.md`, `README.md` and `a.py` in the code root When   `_scan_untagged` runs Then   it lists `a.py` and `README.md` but neither `CLAUDE.md` nor `TODO.md`",
        "CASE-6 — — no code_root means no Untagged files section at all Given  a draft requirement, called without a `code_root` argument When   `cmd_next` runs Then   its output never contains \"Untagged files\"",
        "CASE-7 — — an Orphans note names the map-recorded member and --code Given  a confirmed requirement with no locally scanned member, whose `_map.json` node records `src/foo.py` as a member When   `cmd_next` runs with that `reqs_dir` Then   the Orphans bucket still lists the requirement, plus a note naming `src/foo.py` and suggesting `--code <dir>` --------------------"
      ],
      "accept": "CASE-1 — all four action bucket labels appear for their matching signal\n  Given  one confirmed requirement with no members, one confirmed-untested, one confirmed with an open Verify-intent bullet, one draft\n  When   `cmd_next` runs\n  Then   its output contains \"Orphans\", \"Needs tests\", \"Needs intent review\" and \"Drafts to review\"\n\nCASE-2 — Orphans prints before Needs tests, before Needs intent review, before Drafts\n  Given  one requirement triggering each of the four action signals\n  When   `cmd_next` runs\n  Then   \"Orphans\" appears before \"Needs tests\", which appears before \"Needs intent review\", which appears before \"Drafts to review\"\n\nCASE-3 — a high-fan-in requirement's blast-radius signal never prints\n  Given  a confirmed, tested requirement with three dependents (triggers `blast-radius`)\n  When   `cmd_next` runs\n  Then   its output never contains the word \"blast-radius\"\n\nCASE-4 — Untagged files prints after every action bucket\n  Given  a draft requirement (triggers \"Drafts to review\") plus an untagged `orphan.py` in the code root\n  When   `cmd_next` runs with that code root\n  Then   \"Untagged files\" appears after \"Drafts to review\" in the output\n\nCASE-5 — CLAUDE.md and TODO.md never appear as untagged\n  Given  an untagged `CLAUDE.md`, `TODO.md`, `README.md` and `a.py` in the code root\n  When   `_scan_untagged` runs\n  Then   it lists `a.py` and `README.md` but neither `CLAUDE.md` nor `TODO.md`\n\nCASE-6 — no code_root means no Untagged files section at all\n  Given  a draft requirement, called without a `code_root` argument\n  When   `cmd_next` runs\n  Then   its output never contains \"Untagged files\"\n\nCASE-7 — an Orphans note names the map-recorded member and --code\n  Given  a confirmed requirement with no locally scanned member, whose `_map.json` node records `src/foo.py` as a member\n  When   `cmd_next` runs with that `reqs_dir`\n  Then   the Orphans bucket still lists the requirement, plus a note naming `src/foo.py` and suggesting `--code <dir>`\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-NEXT-013"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4300"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2399"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8574"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 7,
      "covered": 7,
      "i18n": {
        "ro": {
          "title": "Patru bucket-uri de acțiune, două consultative, și fișiere netagate",
          "intent": "Cele patru bucket-uri de acțiune — Orphans, Needs tests, Needs intent review, Drafts to review — se afișează cele mai urgente primele, deoarece acea ordine este exact scopul întregii comenzi: începe de sus. Granularity și Redundancy se afișează dedesubt, împărțind exact pragurile cu `lint` și `dupes` astfel încât cele trei comenzi nu sunt niciodată în dezacord despre care cerință este oversize sau duplicată. Untagged files se clasează cel mai jos dintre toate.",
          "contract": "> Cele patru bucket-uri de acțiune — Orphans, Needs tests, Needs intent review, Drafts\n> to review — se afișează cele mai urgente primele, deoarece acea ordine este exact scopul\n> întregii comenzi: începe de sus. Granularity și Redundancy se afișează dedesubt,\n> împărțind exact pragurile cu `lint` și `dupes` astfel încât cele trei comenzi nu sunt\n> niciodată în dezacord despre care cerință este oversize sau duplicată. Untagged files se\n> clasează cel mai jos dintre toate.\n\nFiecare bullet de mai jos este obligatoriu.\n- `next` scoate la iveală exact bucket-urile acționabile: `unimplemented` (Orphans), `untested`\n  (Needs tests), `unverified-intent` (Needs intent review), `unreviewed` (Drafts to review).\n- `next` afișează acele patru bucket-uri în acea ordine, cele mai urgente primele.\n- `next` afișează de asemenea două bucket-uri consultative sub cele patru bucket-uri de acțiune:\n  `Granularity` (o cerință cu mai multe criterii de acceptance decât `LINT_AC_MAX`-ul lui lint) și\n  `Redundancy` (cerințe a căror Description afirmă aceeași obligație, byte-identică odată\n  normalizată).\n- Setul lui Granularity vine din `_oversize`, predicatul unic pe care îl apelează și verificarea\n  `ac-count-high` a lui `lint_requirement` — același prag, același scope `LINT_STATUSES`, și\n  aceeași respectare a `lint_exempt: [ac-count-high]`, astfel încât `next` și `lint` nu sunt\n  niciodată în dezacord despre care cerință este oversize.\n- Setul lui Redundancy vine din `_redundant_groups`, pragul de potrivire exactă pe care îl folosește\n  și `dupes`.\n- Niciun bucket consultativ nu schimbă codul de ieșire al lui `next`.\n- `next` omite `blast-radius`, deoarece acel semnal este o precauție, nu o sarcină.\n- `next` scoate la iveală fiecare fișier scanabil care nu poartă niciun tag de membership ca un\n  bucket \"Untagged files\", clasat cel mai jos dintre toate.\n- Acel bucket omite proza din bucket-ul auto-draft \"ignore\" (`CLAUDE.md`, `TODO.md`,\n  `CHANGELOG.md`, `LICENSE`, fișiere prefixate cu `_`): acestea sunt invizibile pentru reqmap prin\n  contract.\n- Acel bucket omite de asemenea boilerplate-ul de repository care nu poartă niciodată un tag prin\n  design: rapoarte de decizie sub un director `adr/` sau `decisions/`, template-uri de issue și\n  pull-request, `SECURITY.md`, `CODE_OF_CONDUCT.md` și configurarea dependabot (`_UNTAGGED_NOISE`).\n- `next` omite acea scanare de netagate când apelantul nu dă niciun `code_root`.\n- Un item Orphans poate avea membri înregistrați în `_map.json`-ul committed pe care această\n  scanare nu i-a găsit. Atunci `next` adaugă o notă care numește un asemenea membru și sugerează\n  `--code <dir>`. Nota este un sfat; item-ul rămâne în bucket.",
          "acceptance": "CASE-1 — toate cele patru etichete de bucket de acțiune apar pentru semnalul lor corespunzător\n  Given  o cerință confirmed fără membri, una confirmed-untested, una confirmed cu un bullet Verify-intent deschis, una draft\n  When   rulează `cmd_next`\n  Then   ieșirea sa conține \"Orphans\", \"Needs tests\", \"Needs intent review\" și \"Drafts to review\"\n\nCASE-2 — Orphans se afișează înainte de Needs tests, înainte de Needs intent review, înainte de Drafts\n  Given  o cerință care declanșează fiecare din cele patru semnale de acțiune\n  When   rulează `cmd_next`\n  Then   \"Orphans\" apare înaintea lui \"Needs tests\", care apare înaintea lui \"Needs intent review\", care apare\n         înaintea lui \"Drafts to review\"\n\nCASE-3 — semnalul blast-radius al unei cerințe cu fan-in ridicat nu se afișează niciodată\n  Given  o cerință confirmed, tested cu trei dependenți (declanșează `blast-radius`)\n  When   rulează `cmd_next`\n  Then   ieșirea sa nu conține niciodată cuvântul \"blast-radius\"\n\nCASE-4 — Untagged files se afișează după fiecare bucket de acțiune\n  Given  o cerință draft (declanșează \"Drafts to review\") plus un `orphan.py` netagat în code root\n  When   rulează `cmd_next` cu acel code root\n  Then   \"Untagged files\" apare după \"Drafts to review\" în ieșire\n\nCASE-5 — CLAUDE.md și TODO.md nu apar niciodată ca netagate\n  Given  un `CLAUDE.md`, `TODO.md`, `README.md` și `a.py` netagate în code root\n  When   rulează `_scan_untagged`\n  Then   listează `a.py` și `README.md` dar niciunul din `CLAUDE.md` sau `TODO.md`\n\nCASE-6 — fără code_root înseamnă nicio secțiune Untagged files deloc\n  Given  o cerință draft, apelată fără un argument `code_root`\n  When   rulează `cmd_next`\n  Then   ieșirea sa nu conține niciodată \"Untagged files\"\n\nCASE-7 — o notă Orphans numește membrul înregistrat în map și --code\n  Given  o cerință confirmed fără niciun membru scanat local, al cărei nod `_map.json` înregistrează\n         `src/foo.py` ca membru\n  When   rulează `cmd_next` cu acel `reqs_dir`\n  Then   bucket-ul Orphans tot listează cerința, plus o notă care numește `src/foo.py` și sugerează `--code <dir>`\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-NEXT-885",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Priority, then risk score, then id decide bucket order",
      "intent": "A bucket is only useful if the top item really is the most urgent one. `next` sorts by declared `priority` first (`must-have` before `should-have` before `could-have` before `wont-have`, no-priority last), breaks ties by a draft's extract-authored `risk:` score, and falls back to id — and tags a `risk: 2`-or-higher item `[REVIEW]` so it stands out.",
      "contract": [
        "Within a bucket, `next` orders items by `priority` rank, then by descending extract `risk:` score, then by id.",
        "Priority rank runs `must-have` < `should-have` < `could-have` < `wont-have`. A requirement with no `priority` ranks last.",
        "`next` tags an item whose `risk:` is 2 or more with `[REVIEW]`.",
        "`next` names the requirement file to open, as `requirements/<ID>.md`."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — equal priority and risk falls back to id order Given  two `must-have`, `risk: 0` drafts, \"ZZZ-B-002\" and \"AAA-A-001\", in the same bucket When   `cmd_next` runs Then   \"AAA-A-001\" is printed before \"ZZZ-B-002\"",
        "CASE-2 — — a no-priority item sorts after a could-have item Given  a `could-have` draft and a draft with no `priority` field, in the same bucket When   `cmd_next` runs Then   the `could-have` draft is printed before the no-priority draft",
        "CASE-3 — — a risk: 2 draft is ordered first and tagged REVIEW Given  a `risk: 0` draft and a `risk: 2` draft in the same bucket When   `cmd_next` runs Then   the `risk: 2` draft prints first and its line carries `[REVIEW]`",
        "CASE-4 — — an item's line names its requirement file Given  a confirmed, untested requirement `CORE-FOO-001` When   `cmd_next` runs Then   its \"Needs tests\" line contains \"requirements/CORE-FOO-001.md\" --------------------"
      ],
      "accept": "CASE-1 — equal priority and risk falls back to id order\n  Given  two `must-have`, `risk: 0` drafts, \"ZZZ-B-002\" and \"AAA-A-001\", in the same bucket\n  When   `cmd_next` runs\n  Then   \"AAA-A-001\" is printed before \"ZZZ-B-002\"\n\nCASE-2 — a no-priority item sorts after a could-have item\n  Given  a `could-have` draft and a draft with no `priority` field, in the same bucket\n  When   `cmd_next` runs\n  Then   the `could-have` draft is printed before the no-priority draft\n\nCASE-3 — a risk: 2 draft is ordered first and tagged REVIEW\n  Given  a `risk: 0` draft and a `risk: 2` draft in the same bucket\n  When   `cmd_next` runs\n  Then   the `risk: 2` draft prints first and its line carries `[REVIEW]`\n\nCASE-4 — an item's line names its requirement file\n  Given  a confirmed, untested requirement `CORE-FOO-001`\n  When   `cmd_next` runs\n  Then   its \"Needs tests\" line contains \"requirements/CORE-FOO-001.md\"\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-NEXT-013"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4259"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4300"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2399"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8574"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Priority, apoi scorul de risc, apoi id decid ordinea bucket-ului",
          "intent": "Un bucket este util doar dacă item-ul de sus chiar este cel mai urgent. `next` sortează întâi după `priority`-ul declarat (`must-have` înainte de `should-have` înainte de `could-have` înainte de `wont-have`, fără-prioritate ultimul), rupe egalitățile după scorul `risk:` autohat la extract al unui draft, și cade înapoi la id — și etichetează un item cu `risk: 2`-sau-mai-mult cu `[REVIEW]` astfel încât iese în evidență.",
          "contract": "> Un bucket este util doar dacă item-ul de sus chiar este cel mai urgent. `next`\n> sortează întâi după `priority`-ul declarat (`must-have` înainte de `should-have`\n> înainte de `could-have` înainte de `wont-have`, fără-prioritate ultimul), rupe\n> egalitățile după scorul `risk:` autohat la extract al unui draft, și cade înapoi la id\n> — și etichetează un item cu `risk: 2`-sau-mai-mult cu `[REVIEW]` astfel încât iese în\n> evidență.\n\nFiecare bullet de mai jos este obligatoriu.\n- Într-un bucket, `next` ordonează item-urile după rangul `priority`, apoi după scorul `risk:` de\n  extract descrescător, apoi după id.\n- Rangul de priority merge `must-have` < `should-have` < `could-have` < `wont-have`. O cerință\n  fără `priority` se clasează ultima.\n- `next` etichetează un item al cărui `risk:` este 2 sau mai mult cu `[REVIEW]`.\n- `next` numește fișierul de cerință de deschis, ca `requirements/<ID>.md`.",
          "acceptance": "CASE-1 — priority și risk egale cad înapoi la ordinea id\n  Given  două draft-uri `must-have`, `risk: 0`, \"ZZZ-B-002\" și \"AAA-A-001\", în același bucket\n  When   rulează `cmd_next`\n  Then   \"AAA-A-001\" este afișat înaintea lui \"ZZZ-B-002\"\n\nCASE-2 — un item fără prioritate se sortează după un item could-have\n  Given  un draft `could-have` și un draft fără câmp `priority`, în același bucket\n  When   rulează `cmd_next`\n  Then   draft-ul `could-have` este afișat înaintea draft-ului fără prioritate\n\nCASE-3 — un draft risk: 2 este ordonat primul și etichetat REVIEW\n  Given  un draft `risk: 0` și un draft `risk: 2` în același bucket\n  When   rulează `cmd_next`\n  Then   draft-ul `risk: 2` se afișează primul iar linia sa poartă `[REVIEW]`\n\nCASE-4 — linia unui item numește fișierul cerinței sale\n  Given  o cerință confirmed, netestată `CORE-FOO-001`\n  When   rulează `cmd_next`\n  Then   linia sa \"Needs tests\" conține \"requirements/CORE-FOO-001.md\"\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-NEXT-886",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Each bucket truncates to a top few, --all shows everything",
      "intent": "A worklist that dumps hundreds of items is not a worklist. `next` caps each bucket independently at a small default count, so a long lower-priority bucket can never push a shorter, more urgent one off the screen, and prints a `... N more` line naming the escape hatch (`--all`) rather than silently hiding items.",
      "contract": [
        "By default `next` shows at most the top few items of a bucket.",
        "`next` prints a `... N more` line when a bucket holds more items than it showed.",
        "With `--all`, `next` lists every item.",
        "The \"Untagged files\" bucket truncates the same way as the others."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — only the top 3 of 5 items print without --all Given  5 drafts in one bucket When   `cmd_next` runs with default `top_n=3` Then   exactly 3 of the 5 requirement ids appear in the output",
        "CASE-2 — — a truncated bucket prints a \"... N more\" line Given  5 drafts in one bucket, shown with default `top_n=3` When   `cmd_next` runs Then   its output contains \"more — run `reqmap.py gate --risk --all`\"",
        "CASE-3 — — --all prints every item and drops the \"more\" line Given  5 drafts in one bucket When   `cmd_next` runs with `show_all=True` Then   all 5 requirement ids appear and no \"more — run\" line is printed",
        "CASE-4 — — 5 untagged files show only the top 3 by default Given  one clean confirmed-tested requirement and a code root holding 5 unrelated untagged scannable files When   `cmd_next` runs with that code root and default `top_n=3` Then   the \"Untagged files\" section lists 3 files and a \"... 2 more\" line",
        "CASE-5 — — repository boilerplate is not an untagged file Given  an untagged `a.py`, an ADR under `docs/adr/`, an issue template and a `SECURITY.md` When   `next` scans for untagged files Then   only `a.py` is listed --------------------"
      ],
      "accept": "CASE-1 — only the top 3 of 5 items print without --all\n  Given  5 drafts in one bucket\n  When   `cmd_next` runs with default `top_n=3`\n  Then   exactly 3 of the 5 requirement ids appear in the output\n\nCASE-2 — a truncated bucket prints a \"... N more\" line\n  Given  5 drafts in one bucket, shown with default `top_n=3`\n  When   `cmd_next` runs\n  Then   its output contains \"more — run `reqmap.py gate --risk --all`\"\n\nCASE-3 — --all prints every item and drops the \"more\" line\n  Given  5 drafts in one bucket\n  When   `cmd_next` runs with `show_all=True`\n  Then   all 5 requirement ids appear and no \"more — run\" line is printed\n\nCASE-4 — 5 untagged files show only the top 3 by default\n  Given  one clean confirmed-tested requirement and a code root holding 5 unrelated untagged scannable files\n  When   `cmd_next` runs with that code root and default `top_n=3`\n  Then   the \"Untagged files\" section lists 3 files and a \"... 2 more\" line\n\n\nCASE-5 — repository boilerplate is not an untagged file\n  Given  an untagged `a.py`, an ADR under `docs/adr/`, an issue template and a `SECURITY.md`\n  When   `next` scans for untagged files\n  Then   only `a.py` is listed\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-NEXT-013"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1592"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4300"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2399"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7936"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8574"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5
    },
    {
      "id": "REQ-NEXT-887",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "An empty registry and a clean one get different messages",
      "intent": "\"Nothing to show\" has two different causes, and `next` says which: a registry with no requirements at all points the reader at `init`/`new`, while a registry that is simply clean — nothing open — prints the all-clear line instead. Neither case writes a file, and `next` always exits 0: it is advice, not a gate.",
      "contract": [
        "With a registry that holds no requirements, `next` prints a distinct \"no requirements yet\" message pointing at `init`/`new`. `next` never prints the all-clear line in that case.",
        "With requirements but no open signal, `next` prints the all-clear line.",
        "`next` is deterministic and writes no file.",
        "`next` always exits zero. The report is advice, not a gate."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — an empty registry gets its own message, never the all-clear line Given  an empty requirements registry When   `cmd_next` runs Then   its output contains \"No requirements yet\" and \"reqmap.py init\", but never \"Nothing pending\"",
        "CASE-2 — — a fully clean registry prints the all-clear line Given  one confirmed requirement with both `implements` and `tested-by` members and no open questions When   `cmd_next` runs Then   its output contains \"Nothing pending\"",
        "CASE-3 — — two runs on the same input print byte-identical output Given  a fixed corpus with a mix of confirmed, untested and draft requirements When   `cmd_next` runs on it twice Then   both runs' captured output are identical strings, and no file was created",
        "CASE-4 — — a corpus full of orphans and drafts still exits 0 Given  three unimplemented confirmed requirements and two drafts with open verify-intent bullets When   `cmd_next` runs Then   it returns exit code 0"
      ],
      "accept": "CASE-1 — an empty registry gets its own message, never the all-clear line\n  Given  an empty requirements registry\n  When   `cmd_next` runs\n  Then   its output contains \"No requirements yet\" and \"reqmap.py init\", but never \"Nothing pending\"\n\nCASE-2 — a fully clean registry prints the all-clear line\n  Given  one confirmed requirement with both `implements` and `tested-by` members and no open questions\n  When   `cmd_next` runs\n  Then   its output contains \"Nothing pending\"\n\nCASE-3 — two runs on the same input print byte-identical output\n  Given  a fixed corpus with a mix of confirmed, untested and draft requirements\n  When   `cmd_next` runs on it twice\n  Then   both runs' captured output are identical strings, and no file was created\n\nCASE-4 — a corpus full of orphans and drafts still exits 0\n  Given  three unimplemented confirmed requirements and two drafts with open verify-intent bullets\n  When   `cmd_next` runs\n  Then   it returns exit code 0",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-NEXT-013"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4300"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2399"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8574"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Un registry gol și unul curat primesc mesaje diferite",
          "intent": "\"Nimic de arătat\" are două cauze diferite, iar `next` spune care: un registry fără nicio cerință deloc îndreaptă cititorul spre `init`/`new`, în timp ce un registry care e pur și simplu curat — nimic deschis — afișează în loc linia all-clear. Niciun caz nu scrie un fișier, iar `next` iese mereu cu 0: este sfat, nu un gate.",
          "contract": "> \"Nimic de arătat\" are două cauze diferite, iar `next` spune care: un registry fără\n> nicio cerință deloc îndreaptă cititorul spre `init`/`new`, în timp ce un registry care e\n> pur și simplu curat — nimic deschis — afișează în loc linia all-clear. Niciun caz nu\n> scrie un fișier, iar `next` iese mereu cu 0: este sfat, nu un gate.\n\nFiecare bullet de mai jos este obligatoriu.\n- Cu un registry care nu conține nicio cerință, `next` afișează un mesaj distinct \"no requirements yet\"\n  care indică spre `init`/`new`. `next` nu afișează niciodată linia all-clear în acel caz.\n- Cu cerințe dar fără niciun semnal deschis, `next` afișează linia all-clear.\n- `next` este determinist și nu scrie niciun fișier.\n- `next` iese mereu cu zero. Raportul este sfat, nu un gate.",
          "acceptance": "CASE-1 — un registry gol primește propriul mesaj, niciodată linia all-clear\n  Given  un registry de cerințe gol\n  When   rulează `cmd_next`\n  Then   ieșirea sa conține \"No requirements yet\" și \"reqmap.py init\", dar niciodată \"Nothing pending\"\n\nCASE-2 — un registry complet curat afișează linia all-clear\n  Given  o cerință confirmed cu atât membri `implements` cât și `tested-by` și fără întrebări deschise\n  When   rulează `cmd_next`\n  Then   ieșirea sa conține \"Nothing pending\"\n\nCASE-3 — două rulări pe aceeași intrare afișează ieșire byte-identică\n  Given  un corpus fix cu un amestec de cerințe confirmed, netestate și draft\n  When   rulează `cmd_next` de două ori pe el\n  Then   ieșirea capturată a ambelor rulări sunt string-uri identice, și niciun fișier nu a fost creat\n\nCASE-4 — un corpus plin de orfani și draft-uri tot iese cu 0\n  Given  trei cerințe confirmed neimplementate și două draft-uri cu bullets verify-intent deschise\n  When   rulează `cmd_next`\n  Then   returnează codul de ieșire 0"
        }
      }
    },
    {
      "id": "ARCH-ORPHANCODE-034",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Orphan-code warning",
      "intent": "A substantial new source file with no membership tag is invisible to every drift signal: it implements behavior no requirement describes, so \"the spec covers the code\" quietly stops being true as the repo grows. `health` counts untagged files ([[ARCH-COVERAGE-029]]) but nothing says it at the moment it matters — the commit gate. This warns, at gate time, about each sizeable program file that carries no tag, so the author links it, drafts a requirement for it, or marks it out of scope — the same nudge-not-block pattern as the untagged doc-bundle warning ([[ARCH-DOCBUNDLE-026]]).",
      "contract": [
        "The gate warns, without affecting its exit code, for each program file at least `ORPHAN_CODE_MIN_LOC` lines long carrying no membership tag and no `verifies:` tag — silenced by tagging the file or listing it in `.reqmapignore`. [[REQ-ORPHANCODE-888]] details the behaviour."
      ],
      "verify": [],
      "notes": [
        "The LOC threshold is a deterministic proxy for \"substantial\", not a semantic one: a 149-line untagged file stays silent. Tune `ORPHAN_CODE_MIN_LOC` in the engine, not per repo.",
        "Like [[ARCH-COVERAGE-029]] this measures tag PRESENCE, not quality: one hollow tag silences the file. Accepted — the signal is advisory precisely because presence is gameable.",
        "Granularity is per file: a tagged file with three untagged capabilities inside it is silent. `dupes`/`draft` remain the tools for that."
      ],
      "current_impl": [
        "`orphan_code_files`, `ORPHAN_CODE_MIN_LOC`, `ORPHAN_CODE_EXTS` in `reqmap.py`, consumed by `cmd_check` — the covered set is derived from the already-scanned member map plus the `verifies:` coverage map, so the check adds no second tag scan; `cmd_check` emits one warn-only line per result.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  an untagged program file at or above the LOC threshold When   the scan runs Then   the file is reported as orphan code",
        "CASE-2 — Given  an untagged program file below the LOC threshold When   the scan runs Then   it is not reported",
        "CASE-3 — Given  a program file at or above the threshold that carries a membership tag or a `verifies:` tag When   the scan runs Then   it is not reported",
        "CASE-4 — Given  a large untagged non-program file (e.g. `.md`, `.html`) When   the scan runs Then   it is not reported",
        "CASE-5 — Given  a large untagged program file matched by a `.reqmapignore` pattern When   the scan runs Then   it is not reported",
        "CASE-6 — Given  a large untagged program file When   the gate runs (with and without `--strict`) Then   its output names the file with a warn line and the exit code is unchanged by this check"
      ],
      "accept": "CASE-1\n  Given  an untagged program file at or above the LOC threshold\n  When   the scan runs\n  Then   the file is reported as orphan code\nCASE-2\n  Given  an untagged program file below the LOC threshold\n  When   the scan runs\n  Then   it is not reported\nCASE-3\n  Given  a program file at or above the threshold that carries a membership\n         tag or a `verifies:` tag\n  When   the scan runs\n  Then   it is not reported\nCASE-4\n  Given  a large untagged non-program file (e.g. `.md`, `.html`)\n  When   the scan runs\n  Then   it is not reported\nCASE-5\n  Given  a large untagged program file matched by a `.reqmapignore` pattern\n  When   the scan runs\n  Then   it is not reported\nCASE-6\n  Given  a large untagged program file\n  When   the gate runs (with and without `--strict`)\n  Then   its output names the file with a warn line and the exit code is\n         unchanged by this check",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-CHECK-006",
        "ARCH-SCAN-002"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-GATE-102"
      ],
      "satisfied_by": [
        "REQ-ORPHANCODE-888"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1616"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2793"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:741"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9652"
        }
      ],
      "test_exempt": null,
      "milestone": "v2.10",
      "priority": "should-have",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Avertisment de cod orfan",
          "intent": "Un fișier sursă nou, substanțial, fără nicio etichetă de apartenență, este invizibil pentru orice semnal de drift: implementează un comportament pe care nicio cerință nu îl descrie, astfel încât \"specificația acoperă codul\" încetează în tăcere să fie adevărat pe măsură ce repo-ul crește. `health` numără fișierele neetichetate ([[ARCH-COVERAGE-029]]), dar nimic nu spune asta în momentul în care contează — gate-ul de commit. Aceasta avertizează, la momentul gate-ului, despre fiecare fișier de program de dimensiune considerabilă care nu poartă nicio etichetă, astfel încât autorul îl leagă, schițează o cerință pentru el, sau îl marchează ca fiind în afara domeniului — același tipar de îmbrânzire-nu-blocare ca avertismentul pentru pachetul de documentație neetichetat ([[ARCH-DOCBUNDLE-026]]).",
          "contract": "> Un fișier sursă nou, substanțial, fără nicio etichetă de apartenență, este invizibil\n> pentru orice semnal de drift: implementează un comportament pe care nicio cerință nu\n> îl descrie, astfel încât \"specificația acoperă codul\" încetează în tăcere să fie\n> adevărat pe măsură ce repo-ul crește. `health` numără fișierele neetichetate\n> ([[ARCH-COVERAGE-029]]), dar nimic nu spune asta în momentul în care contează — gate-ul\n> de commit. Aceasta avertizează, la momentul gate-ului, despre fiecare fișier de\n> program de dimensiune considerabilă care nu poartă nicio etichetă, astfel încât\n> autorul îl leagă, schițează o cerință pentru el, sau îl marchează ca fiind în afara\n> domeniului — același tipar de îmbrânzire-nu-blocare ca avertismentul pentru pachetul\n> de documentație neetichetat ([[ARCH-DOCBUNDLE-026]]).\n\nFiecare punct de mai jos este obligatoriu.\n- Gate-ul avertizează, fără a-i afecta codul de ieșire, pentru fiecare fișier de program de cel puțin `ORPHAN_CODE_MIN_LOC` linii care nu poartă nicio etichetă de apartenență și nicio etichetă `verifies:` — redus la tăcere prin etichetarea fișierului sau listarea lui în `.reqmapignore`. [[REQ-ORPHANCODE-888]] detaliază comportamentul.",
          "acceptance": "CASE-1\n  Given  un fișier de program neetichetat la sau peste pragul LOC\n  When   scanarea rulează\n  Then   fișierul este raportat ca cod orfan\nCASE-2\n  Given  un fișier de program neetichetat sub pragul LOC\n  When   scanarea rulează\n  Then   nu este raportat\nCASE-3\n  Given  un fișier de program la sau peste prag care poartă o etichetă\n         de apartenență sau o etichetă `verifies:`\n  When   scanarea rulează\n  Then   nu este raportat\nCASE-4\n  Given  un fișier mare, neetichetat, care nu este de program (de ex. `.md`, `.html`)\n  When   scanarea rulează\n  Then   nu este raportat\nCASE-5\n  Given  un fișier de program mare, neetichetat, potrivit de un tipar `.reqmapignore`\n  When   scanarea rulează\n  Then   nu este raportat\nCASE-6\n  Given  un fișier de program mare, neetichetat\n  When   gate-ul rulează (cu și fără `--strict`)\n  Then   ieșirea sa numește fișierul cu o linie warn iar codul de ieșire rămâne\n         neschimbat de această verificare"
        }
      }
    },
    {
      "id": "REQ-ORPHANCODE-888",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Warning on a sizeable file with no requirement link",
      "intent": "A big new source file with no membership tag implements behavior no requirement describes, and nothing said so at commit time — only `health` counted it later. The gate now warns on each program file past a line-count threshold with no membership or `verifies:` tag, so the author links it, drafts a requirement, or marks it out of scope; it never fails the build, at any flag combination, because tag presence alone is gameable.",
      "contract": [
        "The gate warns for each program file that carries no membership tag and no `verifies:` tag, once that file is at least `ORPHAN_CODE_MIN_LOC` physical lines long.",
        "A program file is one ending in `.py .js .ts .tsx .jsx .c .cc .cpp .h .hpp .java .go .rs`.",
        "A membership tag is one of `implements`, `tested-by`, `generated-from` and `validated-against`.",
        "The gate does not consider the prose, styling and config extensions (`.md .html .css .sql .yaml .yml`). Prose coverage is [[ARCH-DOCBUNDLE-026]]'s concern.",
        "The check honors `.reqmapignore` and the standard scan walk, so a repo can mark generated or vendored code out of scope rather than tag it.",
        "The walk itself — what it prunes, what it ignores, and how it treats a file it cannot read — is [[ARCH-SCAN-002]]'s contract, not restated here.",
        "The check is warn-only and never changes the gate's exit code, including under `--strict`. <!-- Rationale: the 2026-06-21 Senate audit on ARCH-COVERAGE-029 rejected coverage as a hard gate (hollow tags become the rational way to pass CI); this stays advisory at any flag combination. -->",
        "An author silences a file by tagging it or by adding it to `.reqmapignore`.",
        "There is no separate exemption mechanism."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — an untagged file at the LOC threshold triggers the warning Given  an untagged 400-line `.py` file with no `implements`/`verifies` tag When   `gate` runs Then   it prints a warn line naming that file as orphan code, and the gate's exit code is unchanged, including under `--strict`",
        "CASE-2 — — only files on the program-extension list are considered Given  an untagged 400-line `.go` file and an untagged 400-line `.txt` file When   `gate` runs Then   the `.go` file is reported as orphan code and the `.txt` file is not",
        "CASE-3 — — any of the four membership tags silences the warning Given  four large untagged files, each carrying one of `implements`, `tested-by`, `generated-from`, `validated-against` When   `gate` runs Then   none of the four is reported as orphan code",
        "CASE-4 — — prose and config extensions are never checked for orphan code Given  a large untagged `.md` file and a large untagged `.yaml` file When   `gate` runs Then   neither is reported as orphan code",
        "CASE-5 — — a .reqmapignore pattern exempts a large untagged file Given  a large untagged `.py` file matched by a `.reqmapignore` pattern When   `gate` runs Then   it is not reported as orphan code, with no separate exemption mechanism beyond tagging or `.reqmapignore`"
      ],
      "accept": "CASE-1 — an untagged file at the LOC threshold triggers the warning\n  Given  an untagged 400-line `.py` file with no `implements`/`verifies` tag\n  When   `gate` runs\n  Then   it prints a warn line naming that file as orphan code, and the gate's exit code is unchanged, including under `--strict`\n\nCASE-2 — only files on the program-extension list are considered\n  Given  an untagged 400-line `.go` file and an untagged 400-line `.txt` file\n  When   `gate` runs\n  Then   the `.go` file is reported as orphan code and the `.txt` file is not\n\nCASE-3 — any of the four membership tags silences the warning\n  Given  four large untagged files, each carrying one of `implements`, `tested-by`,\n         `generated-from`, `validated-against`\n  When   `gate` runs\n  Then   none of the four is reported as orphan code\n\nCASE-4 — prose and config extensions are never checked for orphan code\n  Given  a large untagged `.md` file and a large untagged `.yaml` file\n  When   `gate` runs\n  Then   neither is reported as orphan code\n\nCASE-5 — a .reqmapignore pattern exempts a large untagged file\n  Given  a large untagged `.py` file matched by a `.reqmapignore` pattern\n  When   `gate` runs\n  Then   it is not reported as orphan code, with no separate exemption mechanism beyond tagging or `.reqmapignore`",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-ORPHANCODE-034"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1616"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:741"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9652"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Avertizare pe un fișier considerabil fără nicio legătură de cerință",
          "intent": "Un fișier sursă nou, mare, fără niciun tag de membership implementează un comportament pe care nicio cerință nu îl descrie, iar nimic nu a spus asta la momentul commit-ului — doar `health` l-a numărat mai târziu. Gate-ul acum avertizează pe fiecare fișier de program peste un prag de număr de linii fără niciun tag de membership sau `verifies:`, astfel încât autorul îl leagă, redactează o cerință, sau îl marchează out of scope; nu eșuează niciodată build-ul, la nicio combinație de flag-uri, deoarece prezența unui tag singură este gameable.",
          "contract": "> Un fișier sursă nou, mare, fără niciun tag de membership implementează un\n> comportament pe care nicio cerință nu îl descrie, iar nimic nu a spus asta la momentul\n> commit-ului — doar `health` l-a numărat mai târziu. Gate-ul acum avertizează pe fiecare\n> fișier de program peste un prag de număr de linii fără niciun tag de membership sau\n> `verifies:`, astfel încât autorul îl leagă, redactează o cerință, sau îl marchează out\n> of scope; nu eșuează niciodată build-ul, la nicio combinație de flag-uri, deoarece\n> prezența unui tag singură este gameable.\n\nFiecare bullet de mai jos este obligatoriu.\n- Gate-ul avertizează pentru fiecare fișier de program care nu poartă niciun tag de membership și\n  niciun tag `verifies:`, odată ce acel fișier are cel puțin `ORPHAN_CODE_MIN_LOC`\n  linii fizice lungime.\n- Un fișier de program este unul care se termină în `.py .js .ts .tsx .jsx .c .cc .cpp .h .hpp\n  .java .go .rs`.\n- Un tag de membership este unul din `implements`, `tested-by`, `generated-from` și\n  `validated-against`.\n- Gate-ul nu ia în considerare extensiile de proză, stilizare și config\n  (`.md .html .css .sql .yaml .yml`). Acoperirea de proză este\n  preocuparea lui [[ARCH-DOCBUNDLE-026]].\n- Verificarea respectă `.reqmapignore` și walk-ul standard de scanare, astfel încât un repo poate\n  marca cod generat sau vendored ca out of scope în loc să-l tageze.\n- Walk-ul în sine — ce elimină, ce ignoră, și cum tratează un fișier pe care nu-l poate\n  citi — este contractul lui [[ARCH-SCAN-002]], nu restatat aici.\n- Verificarea este warn-only și nu schimbă niciodată codul de ieșire al gate-ului, inclusiv\n  sub `--strict`. <!-- Rationale: 2026-06-21 Senate audit on\n  ARCH-COVERAGE-029 rejected coverage as a hard gate (hollow tags become the\n  rational way to pass CI); this stays advisory at any flag combination. -->\n- Un autor amuțește un fișier tagându-l sau adăugându-l la `.reqmapignore`.\n- Nu există niciun mecanism separat de exemptare.",
          "acceptance": "CASE-1 — un fișier netagat la pragul LOC declanșează avertismentul\n  Given  un fișier `.py` netagat de 400 linii fără tag `implements`/`verifies`\n  When   rulează `gate`\n  Then   afișează o linie warn care numește acel fișier drept orphan code, iar codul de ieșire al gate-ului este neschimbat, inclusiv sub `--strict`\n\nCASE-2 — sunt considerate doar fișierele din lista de extensii de program\n  Given  un fișier `.go` netagat de 400 linii și un fișier `.txt` netagat de 400 linii\n  When   rulează `gate`\n  Then   fișierul `.go` este raportat ca orphan code iar fișierul `.txt` nu este\n\nCASE-3 — oricare din cele patru tag-uri de membership amuțește avertismentul\n  Given  patru fișiere mari netagate, fiecare purtând unul din `implements`, `tested-by`,\n         `generated-from`, `validated-against`\n  When   rulează `gate`\n  Then   niciunul din cele patru nu este raportat ca orphan code\n\nCASE-4 — extensiile de proză și config nu sunt niciodată verificate pentru orphan code\n  Given  un fișier `.md` mare netagat și un fișier `.yaml` mare netagat\n  When   rulează `gate`\n  Then   niciunul nu este raportat ca orphan code\n\nCASE-5 — un pattern .reqmapignore exemptează un fișier mare netagat\n  Given  un fișier `.py` mare netagat, potrivit de un pattern `.reqmapignore`\n  When   rulează `gate`\n  Then   nu este raportat ca orphan code, fără niciun mecanism separat de exemptare dincolo de tagging sau `.reqmapignore`"
        }
      }
    },
    {
      "id": "ARCH-PAGES-021",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Publish & gate the GitHub Pages map copy",
      "intent": "The map viewer is most useful when it is published — a URL anyone can open, no checkout required. This capability copies the freshly-built viewer into a repo's GitHub Pages folder and then guards that published copy, so the page people actually read can never quietly fall behind the registry it claims to show.",
      "contract": [
        "`map` copies `_map.html` to `docs/map.html` whenever a `docs/` directory at the git root carries a GitHub Pages signal (`.nojekyll` or `index.html`), and `map --check` then guards that published copy against drifting from a fresh render. [[REQ-PAGES-889]]"
      ],
      "verify": [],
      "notes": [
        "The Pages signal is detected at the git root (so the engine running from a sub-directory such as `plugin/` still finds a project-root `docs/`); it falls back to the engine's own root when git is absent or the tree is not a checkout.",
        "This depends on ARCH-MAP-007: the published copy and its freshness check both operate on the `_map.html` that the map capability renders. It adds no new rendering — only publish + gate."
      ],
      "current_impl": [
        "`_docs_publish_path` (Pages-signal resolution) and the `docs/map.html` branches of `cmd_map` (publish copy) and `_map_check` (freshness gate) in `reqmap.py`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a `docs/` at the git root carrying `.nojekyll` or `index.html` When   `map` runs Then   it also writes `docs/map.html` (same content as `_map.html`); absent the signal, `docs/map.html` is not written",
        "CASE-2 — Given  no Pages signal in `docs/` (or git absent) When   `map` runs Then   it still succeeds and writes only the standard outputs",
        "CASE-3 — Given  a completed `map` When   `docs/map.html` is edited to differ from a fresh render Then   `map --check` exits non-zero and names `map.html` (it exits 0 before the edit)",
        "CASE-4 — Given  the Pages signal present but no `docs/map.html` (never generated, or removed) When   `map --check` runs Then   it does not flag the file stale",
        "CASE-5 — Given  a change to only the git-derived `repo` value inside `docs/map.html` When   `map --check` runs Then   it stays fresh (the field is excluded from the diff)"
      ],
      "accept": "CASE-1\n  Given  a `docs/` at the git root carrying `.nojekyll` or `index.html`\n  When   `map` runs\n  Then   it also writes `docs/map.html` (same content as `_map.html`); absent the signal,\n         `docs/map.html` is not written\n\nCASE-2\n  Given  no Pages signal in `docs/` (or git absent)\n  When   `map` runs\n  Then   it still succeeds and writes only the standard outputs\n\nCASE-3\n  Given  a completed `map`\n  When   `docs/map.html` is edited to differ from a fresh render\n  Then   `map --check` exits non-zero and names `map.html` (it exits 0 before the edit)\n\nCASE-4\n  Given  the Pages signal present but no `docs/map.html` (never generated, or removed)\n  When   `map --check` runs\n  Then   it does not flag the file stale\n\nCASE-5\n  Given  a change to only the git-derived `repo` value inside `docs/map.html`\n  When   `map --check` runs\n  Then   it stays fresh (the field is excluded from the diff)",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-MAP-007"
      ],
      "used_by": [
        "ARCH-SITE-026"
      ],
      "satisfies": [
        "SYS-VISUAL-106"
      ],
      "satisfied_by": [
        "REQ-PAGES-889"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4224"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6193"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7393"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1483"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2241"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.04",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Publică și gate-uiește copia hărții pentru GitHub Pages",
          "intent": "Viewer-ul hărții este cel mai util atunci când este publicat — un URL pe care oricine îl poate deschide, fără niciun checkout necesar. Această capabilitate copiază viewer-ul proaspăt construit în folderul GitHub Pages al unui repo și apoi păzește acea copie publicată, astfel încât pagina pe care oamenii chiar o citesc să nu poată niciodată rămâne în urmă în tăcere față de registrul pe care pretinde că îl arată.",
          "contract": "> Viewer-ul hărții este cel mai util atunci când este publicat — un URL pe care oricine\n> îl poate deschide, fără niciun checkout necesar. Această capabilitate copiază\n> viewer-ul proaspăt construit în folderul GitHub Pages al unui repo și apoi păzește\n> acea copie publicată, astfel încât pagina pe care oamenii chiar o citesc să nu poată\n> niciodată rămâne în urmă în tăcere față de registrul pe care pretinde că îl arată.\n\nFiecare punct de mai jos este obligatoriu.\n- `map` copiază `_map.html` în `docs/map.html` de fiecare dată când un director `docs/` la rădăcina git poartă un semnal GitHub Pages (`.nojekyll` sau `index.html`), iar `map --check` păzește apoi acea copie publicată împotriva devierii de la o randare proaspătă. [[REQ-PAGES-889]]",
          "acceptance": "CASE-1\n  Given  un `docs/` la rădăcina git care poartă `.nojekyll` sau `index.html`\n  When   `map` rulează\n  Then   scrie de asemenea `docs/map.html` (același conținut ca `_map.html`); în absența semnalului,\n         `docs/map.html` nu este scris\n\nCASE-2\n  Given  niciun semnal Pages în `docs/` (sau git absent)\n  When   `map` rulează\n  Then   tot reușește și scrie doar ieșirile standard\n\nCASE-3\n  Given  un `map` finalizat\n  When   `docs/map.html` este editat pentru a diferi de o randare proaspătă\n  Then   `map --check` iese cu cod diferit de zero și numește `map.html` (iese cu 0 înainte de editare)\n\nCASE-4\n  Given  semnalul Pages prezent dar fără niciun `docs/map.html` (niciodată generat, sau eliminat)\n  When   `map --check` rulează\n  Then   nu marchează fișierul ca învechit\n\nCASE-5\n  Given  o schimbare doar a valorii `repo` derivate din git din interiorul `docs/map.html`\n  When   `map --check` rulează\n  Then   rămâne proaspăt (câmpul este exclus din diff)"
        }
      }
    },
    {
      "id": "REQ-PAGES-889",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Publish docs/map.html and flag it when it drifts",
      "intent": "Anyone can open a URL; almost nobody clones a repo just to double-click a file. Copying `_map.html` into `docs/map.html` on every `map` run turns the viewer into a page GitHub Pages serves directly. Without the paired freshness check in `map --check`, that published copy could be hand-edited or simply forgotten and would silently drift from the registry it claims to show.",
      "contract": [
        "When `_map.html` is generated AND a `docs/` directory at the git root carries a GitHub Pages signal (a `.nojekyll` or `index.html` file present), `map` also copies `_map.html` to `docs/map.html`. When no signal is present, or git is absent, `docs/map.html` is not written — the behaviour is opt-in by folder contents, so a repo that does not publish a page is unaffected.",
        "`map --check` (the no-write freshness gate) additionally flags `docs/map.html` as stale when it differs from a fresh viewer render of the current registry. This applies only when the Pages signal and the viewer template `_map_viewer.html` are both present and `docs/map.html` already exists. A copy that was never generated is not stale (the same absent-file rule the gate applies to `_map.*`).",
        "The freshness comparison reads the on-disk copy as text so a platform newline difference (CRLF vs LF) never raises a false positive. It excludes the git-derived `repo` field (as the `_map.json` check does), so a fork or clone with a different remote is not spuriously flagged; the rest of the injected data carries no wall-clock value, so the comparison is deterministic."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — map copies the viewer to docs/map.html only when a Pages signal exists Given  a `docs/` directory at the git root containing a `.nojekyll` file When   `map` runs and writes `_map.html` Then   `docs/map.html` exists with the same bytes; without the signal it is not written",
        "CASE-2 — — check flags an edited docs/map.html as stale, absent copy as fresh Given  a generated `docs/map.html` that is then overwritten with different HTML When   `map --check` runs Then   it exits 1 naming `map.html`; if the file is missing instead, it exits 0",
        "CASE-3 — — a swapped repo field alone does not mark docs/map.html stale Given  a fresh `docs/map.html` whose embedded `\"repo\"` value is edited to a different slug When   `map --check` runs Then   it still exits 0 and reports the file fresh"
      ],
      "accept": "CASE-1 — map copies the viewer to docs/map.html only when a Pages signal exists\n  Given  a `docs/` directory at the git root containing a `.nojekyll` file\n  When   `map` runs and writes `_map.html`\n  Then   `docs/map.html` exists with the same bytes; without the signal it is not written\n\nCASE-2 — check flags an edited docs/map.html as stale, absent copy as fresh\n  Given  a generated `docs/map.html` that is then overwritten with different HTML\n  When   `map --check` runs\n  Then   it exits 1 naming `map.html`; if the file is missing instead, it exits 0\n\nCASE-3 — a swapped repo field alone does not mark docs/map.html stale\n  Given  a fresh `docs/map.html` whose embedded `\"repo\"` value is edited to a different slug\n  When   `map --check` runs\n  Then   it still exits 0 and reports the file fresh",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-PAGES-021"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7393"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1483"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Publică docs/map.html și semnalează-l când suferă drift",
          "intent": "Oricine poate deschide un URL; aproape nimeni nu clonează un repo doar ca să dea dublu-click pe un fișier. Copierea `_map.html` în `docs/map.html` la fiecare rulare `map` transformă viewer-ul într-o pagină pe care GitHub Pages o servește direct. Fără verificarea de prospețime asociată în `map --check`, acea copie publicată ar putea fi editată de mână sau pur și simplu uitată și ar suferi tacit drift față de registry-ul pe care pretinde că îl arată.",
          "contract": "> Oricine poate deschide un URL; aproape nimeni nu clonează un repo doar ca să dea\n> dublu-click pe un fișier. Copierea `_map.html` în `docs/map.html` la fiecare rulare\n> `map` transformă viewer-ul într-o pagină pe care GitHub Pages o servește direct. Fără\n> verificarea de prospețime asociată în `map --check`, acea copie publicată ar putea fi\n> editată de mână sau pur și simplu uitată și ar suferi tacit drift față de registry-ul\n> pe care pretinde că îl arată.\n\nFiecare bullet de mai jos este obligatoriu.\n- Când `_map.html` este generat ȘI un director `docs/` la rădăcina git poartă un semnal de\n  GitHub Pages (un fișier `.nojekyll` sau `index.html` prezent), `map` copiază de asemenea\n  `_map.html` în `docs/map.html`. Când niciun semnal nu este prezent, sau git lipsește,\n  `docs/map.html` nu este scris — comportamentul este opt-in după conținutul folderului, astfel\n  încât un repo care nu publică o pagină rămâne neafectat.\n- `map --check` (gate-ul de prospețime fără scriere) marchează în plus `docs/map.html` ca\n  perimat când diferă de o randare proaspătă a viewer-ului pentru registry-ul curent. Asta se\n  aplică doar când semnalul Pages și template-ul viewer-ului `_map_viewer.html` sunt ambele\n  prezente iar `docs/map.html` există deja. O copie care nu a fost niciodată generată nu este\n  perimată (aceeași regulă de fișier-absent pe care gate-ul o aplică pentru `_map.*`).\n- Comparația de prospețime citește copia de pe disc ca text astfel încât o diferență de\n  newline la nivel de platformă (CRLF vs LF) nu ridică niciodată un fals-pozitiv. Exclude câmpul\n  `repo` derivat din git (așa cum face verificarea `_map.json`), astfel încât un fork sau o\n  clonă cu un remote diferit nu este semnalat spuriu; restul datelor injectate nu poartă nicio\n  valoare de tip wall-clock, deci comparația este determinist.",
          "acceptance": "CASE-1 — map copiază viewer-ul în docs/map.html doar când există un semnal Pages\n  Given  un director `docs/` la rădăcina git conținând un fișier `.nojekyll`\n  When   rulează `map` și scrie `_map.html`\n  Then   `docs/map.html` există cu aceiași bytes; fără semnal nu este scris\n\nCASE-2 — check marchează un docs/map.html editat ca perimat, o copie absentă ca proaspătă\n  Given  un `docs/map.html` generat care este apoi suprascris cu HTML diferit\n  When   rulează `map --check`\n  Then   iese cu 1 numind `map.html`; dacă fișierul lipsește în loc, iese cu 0\n\nCASE-3 — un câmp repo schimbat singur nu marchează docs/map.html ca perimat\n  Given  un `docs/map.html` proaspăt a cărui valoare embedată `\"repo\"` este editată la un alt slug\n  When   rulează `map --check`\n  Then   tot iese cu 0 și raportează fișierul ca proaspăt"
        }
      }
    },
    {
      "id": "ARCH-PARSE-001",
      "layer": "bus",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Requirement reading",
      "intent": "Every command in the tool — the gate, the map, the next-steps list — needs the requirement files as structured data, not raw text. This is the part that reads each Markdown file and hands the rest of the tool clean records to work with. If it breaks, nothing downstream can run.",
      "contract": [
        "`load_requirements` parses each `requirements/*.md` file into a record `{meta, body, path}`, keyed by the frontmatter `id:` or the filename stem. [[REQ-PARSE-890]]",
        "The hand-rolled frontmatter grammar accepts scalars, inline `[a, b]` lists, and block-style `key:` / indented `- item` lists — no external YAML library. [[REQ-PARSE-891]]",
        "A file with no leading `---` block, an underscore-prefixed filename, or a leading UTF-8 BOM are all handled without raising. [[REQ-PARSE-892]]"
      ],
      "verify": [],
      "notes": [
        "Deliberately minimal (no YAML library) to keep the engine stdlib-only: nested mappings and multi-line scalars are not supported."
      ],
      "current_impl": [
        "`parse_frontmatter` (with `_clean_item`) and `load_requirements` in `reqmap.py`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a file with valid frontmatter When   it is parsed Then   its scalar and list fields appear in `meta`",
        "CASE-2 — Given  a frontmatter line carrying a trailing `# comment` When   it is parsed Then   the comment is stripped from the value",
        "CASE-3 — Given  a file without a leading `---` block When   it is parsed Then   `meta` is empty and the whole text is the body",
        "CASE-4 — Given  a requirements directory containing files starting with `_` When   requirements are loaded Then   those files are excluded from the result",
        "CASE-5 — Given  a block-style list and an unclosed inline list When   they are parsed Then   both yield the intended list"
      ],
      "accept": "CASE-1\n  Given  a file with valid frontmatter\n  When   it is parsed\n  Then   its scalar and list fields appear in `meta`\n\nCASE-2\n  Given  a frontmatter line carrying a trailing `# comment`\n  When   it is parsed\n  Then   the comment is stripped from the value\n\nCASE-3\n  Given  a file without a leading `---` block\n  When   it is parsed\n  Then   `meta` is empty and the whole text is the body\n\nCASE-4\n  Given  a requirements directory containing files starting with `_`\n  When   requirements are loaded\n  Then   those files are excluded from the result\n\nCASE-5\n  Given  a block-style list and an unclosed inline list\n  When   they are parsed\n  Then   both yield the intended list",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [
        "ARCH-ATOMICFORM-053",
        "ARCH-ATOMICITY-049",
        "ARCH-CHECK-006",
        "ARCH-CLARIFY-062",
        "ARCH-CMDREGISTRY-033",
        "ARCH-CONFIG-060",
        "ARCH-DESCRIPTION-057",
        "ARCH-FANOUT-052",
        "ARCH-FINDINGS-010",
        "ARCH-HEALTH-017",
        "ARCH-IMPLEMENT-063",
        "ARCH-LEVEL-051",
        "ARCH-LINT-014",
        "ARCH-LINTCHECKS-025",
        "ARCH-MAP-007",
        "ARCH-MODULEFILE-056",
        "ARCH-NEW-004",
        "ARCH-PROMOTE-011",
        "ARCH-RETIRE-064",
        "ARCH-REVIEW-022",
        "ARCH-RULES-059",
        "ARCH-SEARCH-036",
        "ARCH-SHOW-015",
        "ARCH-SIMILAR-016",
        "ARCH-TRACE-020",
        "ARCH-TRANSLATE-044"
      ],
      "satisfies": [
        "SYS-READ-103"
      ],
      "satisfied_by": [
        "REQ-PARSE-890",
        "REQ-PARSE-891",
        "REQ-PARSE-892"
      ],
      "members": [
        {
          "role": "generated-from",
          "loc": "docs/full_architecture.html:4"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:804"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:908"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:918"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:931"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:939"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1003"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:51"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1651"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6173"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.00",
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Citirea cerințelor",
          "intent": "Fiecare comandă din instrument — gate-ul, harta, lista de pași următori — are nevoie de fișierele de cerințe ca date structurate, nu ca text brut. Aceasta este partea care citește fiecare fișier Markdown și predă restului instrumentului înregistrări curate cu care să lucreze. Dacă se strică, nimic din aval nu mai poate rula.",
          "contract": "> Fiecare comandă din instrument — gate-ul, harta, lista de pași următori — are nevoie\n> de fișierele de cerințe ca date structurate, nu ca text brut. Aceasta este partea care\n> citește fiecare fișier Markdown și predă restului instrumentului înregistrări curate\n> cu care să lucreze. Dacă se strică, nimic din aval nu mai poate rula.\n\nFiecare punct de mai jos este obligatoriu.\n- `load_requirements` analizează fiecare fișier `requirements/*.md` într-o înregistrare `{meta, body, path}`, cheiată după `id:`-ul din frontmatter sau după numele fișierului. [[REQ-PARSE-890]]\n- Gramatica de frontmatter scrisă manual acceptă scalari, liste inline `[a, b]`, și liste de tip bloc `key:` / `- item` indentate — fără nicio bibliotecă YAML externă. [[REQ-PARSE-891]]\n- Un fișier fără niciun bloc `---` la început, un nume de fișier cu prefix underscore, sau un BOM UTF-8 la început sunt toate gestionate fără a ridica excepții. [[REQ-PARSE-892]]",
          "acceptance": "CASE-1\n  Given  un fișier cu frontmatter valid\n  When   este analizat\n  Then   câmpurile sale scalare și de listă apar în `meta`\n\nCASE-2\n  Given  o linie de frontmatter care poartă un `# comment` final\n  When   este analizată\n  Then   comentariul este eliminat din valoare\n\nCASE-3\n  Given  un fișier fără niciun bloc `---` la început\n  When   este analizat\n  Then   `meta` este gol iar întregul text este corpul\n\nCASE-4\n  Given  un director de cerințe care conține fișiere care încep cu `_`\n  When   cerințele sunt încărcate\n  Then   acele fișiere sunt excluse din rezultat\n\nCASE-5\n  Given  o listă de tip bloc și o listă inline neînchisă\n  When   sunt analizate\n  Then   ambele produc lista intenționată"
        }
      }
    },
    {
      "id": "REQ-PARSE-890",
      "layer": "bus",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "load_requirements returns one meta/body/path record per file",
      "intent": "Every other command — gate, map, next — works from an in-memory dict of requirement records, not the raw Markdown. `load_requirements` builds that dict once: one entry per file, keyed by id, holding the parsed frontmatter (`meta`), the prose after it (`body`), and the file's path. Everything downstream assumes this shape.",
      "contract": [
        "`load_requirements` parses each `requirements/*.md` file into a record `{meta, body, path}`.",
        "`meta` is the parsed frontmatter, and `body` is the markdown after the frontmatter block.",
        "The `id` comes from the frontmatter `id:` field, falling back to the filename stem."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — load_requirements returns a meta/body/path record per file Given  a `requirements/` directory with one valid `REQ-A-001.md` When   `load_requirements(dir)` runs Then   the returned dict's entry carries `meta`, `body`, and `path` keys",
        "CASE-2 — — parse_frontmatter splits the header fields from the markdown that follows Given  `\"---\\nid: REQ-A-001\\nstatus: draft\\n---\\nbody text\\n\"` When   `parse_frontmatter(text)` runs Then   `meta[\"id\"] == \"REQ-A-001\"` and `body == \"body text\\n\"`, with no `---` markers in either",
        "CASE-3 — — a file with no id: field is keyed by its filename stem Given  `REQ-A-001.md` whose frontmatter carries no `id:` line When   `load_requirements(dir)` runs Then   the returned dict has the key `\"REQ-A-001\"`, taken from the filename --------------------"
      ],
      "accept": "CASE-1 — load_requirements returns a meta/body/path record per file\n  Given  a `requirements/` directory with one valid `REQ-A-001.md`\n  When   `load_requirements(dir)` runs\n  Then   the returned dict's entry carries `meta`, `body`, and `path` keys\n\nCASE-2 — parse_frontmatter splits the header fields from the markdown that follows\n  Given  `\"---\\nid: REQ-A-001\\nstatus: draft\\n---\\nbody text\\n\"`\n  When   `parse_frontmatter(text)` runs\n  Then   `meta[\"id\"] == \"REQ-A-001\"` and `body == \"body text\\n\"`, with no `---` markers in either\n\nCASE-3 — a file with no id: field is keyed by its filename stem\n  Given  `REQ-A-001.md` whose frontmatter carries no `id:` line\n  When   `load_requirements(dir)` runs\n  Then   the returned dict has the key `\"REQ-A-001\"`, taken from the filename\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-PARSE-001"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1003"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:51"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10033"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "load_requirements returnează o înregistrare meta/body/path per fișier",
          "intent": "Fiecare altă comandă — gate, map, next — lucrează dintr-un dicționar în memorie de înregistrări de cerință, nu din Markdown-ul brut. `load_requirements` construiește acel dicționar o dată: o intrare per fișier, cheiată după id, purtând frontmatter-ul parsat (`meta`), proza de după el (`body`), și calea fișierului. Tot ce urmează presupune această formă.",
          "contract": "> Fiecare altă comandă — gate, map, next — lucrează dintr-un dicționar în memorie de\n> înregistrări de cerință, nu din Markdown-ul brut. `load_requirements` construiește acel\n> dicționar o dată: o intrare per fișier, cheiată după id, purtând frontmatter-ul parsat\n> (`meta`), proza de după el (`body`), și calea fișierului. Tot ce urmează presupune\n> această formă.\n\nFiecare bullet de mai jos este obligatoriu.\n- `load_requirements` parsează fiecare fișier `requirements/*.md` într-o înregistrare\n  `{meta, body, path}`.\n- `meta` este frontmatter-ul parsat, iar `body` este markdown-ul de după blocul de\n  frontmatter.\n- `id`-ul vine din câmpul frontmatter `id:`, căzând înapoi la stem-ul numelui de fișier.",
          "acceptance": "CASE-1 — load_requirements returnează o înregistrare meta/body/path per fișier\n  Given  un director `requirements/` cu un `REQ-A-001.md` valid\n  When   rulează `load_requirements(dir)`\n  Then   intrarea dicționarului returnat poartă cheile `meta`, `body`, și `path`\n\nCASE-2 — parse_frontmatter desparte câmpurile de header de markdown-ul care urmează\n  Given  `\"---\\nid: REQ-A-001\\nstatus: draft\\n---\\nbody text\\n\"`\n  When   rulează `parse_frontmatter(text)`\n  Then   `meta[\"id\"] == \"REQ-A-001\"` și `body == \"body text\\n\"`, fără niciun marcator `---` în niciunul\n\nCASE-3 — un fișier fără câmp id: este cheiat după stem-ul numelui de fișier\n  Given  `REQ-A-001.md` al cărui frontmatter nu poartă nicio linie `id:`\n  When   rulează `load_requirements(dir)`\n  Then   dicționarul returnat are cheia `\"REQ-A-001\"`, luată din numele fișierului\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-PARSE-891",
      "layer": "bus",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The hand-rolled frontmatter grammar: scalars and lists only",
      "intent": "`parse_frontmatter` reads the `---`-delimited header without a YAML library, so the engine stays stdlib-only. It supports what requirement files actually use — scalars, inline `[a, b]` lists, block-style lists — and nothing more: no nested mappings, no multi-line scalars. Comments and quotes are stripped so a value is clean either way.",
      "contract": [
        "The grammar supports scalars, inline `[a, b]` lists, and block-style lists written as `key:` then indented `- item`.",
        "A trailing `# comment` is stripped from a value.",
        "Matching surrounding quotes are removed from a scalar.",
        "An inline list missing its closing `]` is parsed leniently, rather than kept as a literal string."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a block-style key: / indented - item list parses to a Python list Given  `\"---\\ndepends_on:\\n  - A-B-001\\n  - C-D-002\\n---\\n\"` When   `parse_frontmatter(text)` runs Then   `meta[\"depends_on\"] == [\"A-B-001\", \"C-D-002\"]`",
        "CASE-2 — — a trailing # comment does not leak into the parsed value Given  `\"---\\nstatus: draft  # not enforced\\n---\\n\"` When   `parse_frontmatter(text)` runs Then   `meta[\"status\"] == \"draft\"`, with the comment text absent",
        "CASE-3 — — matching quote characters are stripped from a scalar value Given  `'---\\nid: \"REQ-X-001\"\\nstatus: \\'draft\\'\\n---\\n'` When   `parse_frontmatter(text)` runs Then   `meta[\"id\"] == \"REQ-X-001\"` and `meta[\"status\"] == \"draft\"`, quotes removed",
        "CASE-4 — — an unclosed inline list still parses as a list, not a literal string Given  `\"---\\ntags: [x, y\\n---\\n\"` (no closing `]`) When   `parse_frontmatter(text)` runs Then   `meta[\"tags\"] == [\"x\", \"y\"]`, not the literal string `\"[x, y\"` --------------------"
      ],
      "accept": "CASE-1 — a block-style key: / indented - item list parses to a Python list\n  Given  `\"---\\ndepends_on:\\n  - A-B-001\\n  - C-D-002\\n---\\n\"`\n  When   `parse_frontmatter(text)` runs\n  Then   `meta[\"depends_on\"] == [\"A-B-001\", \"C-D-002\"]`\n\nCASE-2 — a trailing # comment does not leak into the parsed value\n  Given  `\"---\\nstatus: draft  # not enforced\\n---\\n\"`\n  When   `parse_frontmatter(text)` runs\n  Then   `meta[\"status\"] == \"draft\"`, with the comment text absent\n\nCASE-3 — matching quote characters are stripped from a scalar value\n  Given  `'---\\nid: \"REQ-X-001\"\\nstatus: \\'draft\\'\\n---\\n'`\n  When   `parse_frontmatter(text)` runs\n  Then   `meta[\"id\"] == \"REQ-X-001\"` and `meta[\"status\"] == \"draft\"`, quotes removed\n\nCASE-4 — an unclosed inline list still parses as a list, not a literal string\n  Given  `\"---\\ntags: [x, y\\n---\\n\"` (no closing `]`)\n  When   `parse_frontmatter(text)` runs\n  Then   `meta[\"tags\"] == [\"x\", \"y\"]`, not the literal string `\"[x, y\"`\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-PARSE-001"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:931"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:939"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:51"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Gramatica hand-rolled a frontmatter-ului: doar scalari și liste",
          "intent": "`parse_frontmatter` citește header-ul delimitat prin `---` fără o bibliotecă YAML, astfel încât engine-ul rămâne doar-stdlib. Suportă ce fișierele de cerință chiar folosesc — scalari, liste inline `[a, b]`, liste în stil de bloc — și nimic mai mult: fără mappings imbricate, fără scalari pe mai multe linii. Comentariile și ghilimelele sunt eliminate astfel încât o valoare este curată oricum ar fi.",
          "contract": "> `parse_frontmatter` citește header-ul delimitat prin `---` fără o bibliotecă YAML,\n> astfel încât engine-ul rămâne doar-stdlib. Suportă ce fișierele de cerință chiar\n> folosesc — scalari, liste inline `[a, b]`, liste în stil de bloc — și nimic mai mult:\n> fără mappings imbricate, fără scalari pe mai multe linii. Comentariile și ghilimelele\n> sunt eliminate astfel încât o valoare este curată oricum ar fi.\n\nFiecare bullet de mai jos este obligatoriu.\n- Gramatica suportă scalari, liste inline `[a, b]`, și liste în stil de bloc scrise ca\n  `key:` apoi `- item` indentat.\n- Un `# comment` final este eliminat dintr-o valoare.\n- Ghilimelele care se potrivesc împrejurul unui scalar sunt eliminate.\n- O listă inline căreia îi lipsește `]`-ul de închidere este parsată permisiv, în loc să fie\n  păstrată ca string literal.",
          "acceptance": "CASE-1 — o listă în stil de bloc key: / - item indentat se parsează într-o listă Python\n  Given  `\"---\\ndepends_on:\\n  - A-B-001\\n  - C-D-002\\n---\\n\"`\n  When   rulează `parse_frontmatter(text)`\n  Then   `meta[\"depends_on\"] == [\"A-B-001\", \"C-D-002\"]`\n\nCASE-2 — un # comment final nu se scurge în valoarea parsată\n  Given  `\"---\\nstatus: draft  # not enforced\\n---\\n\"`\n  When   rulează `parse_frontmatter(text)`\n  Then   `meta[\"status\"] == \"draft\"`, cu textul comentariului absent\n\nCASE-3 — caracterele de ghilimele care se potrivesc sunt eliminate dintr-o valoare scalară\n  Given  `'---\\nid: \"REQ-X-001\"\\nstatus: \\'draft\\'\\n---\\n'`\n  When   rulează `parse_frontmatter(text)`\n  Then   `meta[\"id\"] == \"REQ-X-001\"` și `meta[\"status\"] == \"draft\"`, ghilimele eliminate\n\nCASE-4 — o listă inline neînchisă tot se parsează ca o listă, nu ca un string literal\n  Given  `\"---\\ntags: [x, y\\n---\\n\"` (fără `]` de închidere)\n  When   rulează `parse_frontmatter(text)`\n  Then   `meta[\"tags\"] == [\"x\", \"y\"]`, nu string-ul literal `\"[x, y\"`\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-PARSE-892",
      "layer": "bus",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Missing frontmatter, underscore files, and a BOM never crash the reader",
      "intent": "A requirements directory is not always full of well-formed files: `_reqlock.json` and other generated `_`-prefixed files sit next to real requirements, some files may be plain prose with no frontmatter block, and an editor may have saved one with a leading byte-order mark. `load_requirements` treats all three as ordinary inputs, not errors.",
      "contract": [
        "A file with no leading `---` block yields empty `meta` and the whole text as `body`.",
        "A file whose name starts with `_` (a lock, the generated map) is excluded.",
        "A leading UTF-8 BOM is tolerated."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a file with no frontmatter block yields empty meta and the full text as body Given  `\"# Title\\njust text\\n\"` (no leading `---`) When   `parse_frontmatter(text)` runs Then   `meta == {}` and `body == \"# Title\\njust text\\n\"`",
        "CASE-2 — — an underscore-prefixed file is excluded from load_requirements Given  a `requirements/` directory holding `_draft.md` and `REQ-A-001.md` When   `load_requirements(dir)` runs Then   the result contains only `\"REQ-A-001\"`, not any id from `_draft.md`",
        "CASE-3 — — a leading UTF-8 BOM does not break frontmatter parsing Given  a requirement file saved with a leading BOM before `---\\nid: REQ-A-001\\n...` When   `load_requirements(dir)` runs Then   `\"REQ-A-001\"` is a key in the result, with `meta[\"status\"]` read correctly"
      ],
      "accept": "CASE-1 — a file with no frontmatter block yields empty meta and the full text as body\n  Given  `\"# Title\\njust text\\n\"` (no leading `---`)\n  When   `parse_frontmatter(text)` runs\n  Then   `meta == {}` and `body == \"# Title\\njust text\\n\"`\n\nCASE-2 — an underscore-prefixed file is excluded from load_requirements\n  Given  a `requirements/` directory holding `_draft.md` and `REQ-A-001.md`\n  When   `load_requirements(dir)` runs\n  Then   the result contains only `\"REQ-A-001\"`, not any id from `_draft.md`\n\nCASE-3 — a leading UTF-8 BOM does not break frontmatter parsing\n  Given  a requirement file saved with a leading BOM before `---\\nid: REQ-A-001\\n...`\n  When   `load_requirements(dir)` runs\n  Then   `\"REQ-A-001\"` is a key in the result, with `meta[\"status\"]` read correctly",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-PARSE-001"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:939"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1003"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:51"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Frontmatter lipsă, fișiere cu underscore, și un BOM nu blochează niciodată cititorul",
          "intent": "Un director de cerințe nu este mereu plin de fișiere bine formate: `_reqlock.json` și alte fișiere generate prefixate cu `_` stau alături de cerințe reale, unele fișiere pot fi proză simplă fără niciun bloc de frontmatter, iar un editor poate fi salvat unul cu un byte-order mark inițial. `load_requirements` tratează toate trei ca intrări obișnuite, nu erori.",
          "contract": "> Un director de cerințe nu este mereu plin de fișiere bine formate: `_reqlock.json` și\n> alte fișiere generate prefixate cu `_` stau alături de cerințe reale, unele fișiere pot\n> fi proză simplă fără niciun bloc de frontmatter, iar un editor poate fi salvat unul cu\n> un byte-order mark inițial. `load_requirements` tratează toate trei ca intrări\n> obișnuite, nu erori.\n\nFiecare bullet de mai jos este obligatoriu.\n- Un fișier fără bloc `---` inițial produce `meta` gol și textul întreg ca `body`.\n- Un fișier al cărui nume începe cu `_` (un lock, map-ul generat) este exclus.\n- Un BOM UTF-8 inițial este tolerat.",
          "acceptance": "CASE-1 — un fișier fără bloc de frontmatter produce meta gol și textul complet ca body\n  Given  `\"# Title\\njust text\\n\"` (fără `---` inițial)\n  When   rulează `parse_frontmatter(text)`\n  Then   `meta == {}` și `body == \"# Title\\njust text\\n\"`\n\nCASE-2 — un fișier prefixat cu underscore este exclus din load_requirements\n  Given  un director `requirements/` care conține `_draft.md` și `REQ-A-001.md`\n  When   rulează `load_requirements(dir)`\n  Then   rezultatul conține doar `\"REQ-A-001\"`, niciun id din `_draft.md`\n\nCASE-3 — un BOM UTF-8 inițial nu strică parsarea frontmatter-ului\n  Given  un fișier de cerință salvat cu un BOM inițial înainte de `---\\nid: REQ-A-001\\n...`\n  When   rulează `load_requirements(dir)`\n  Then   `\"REQ-A-001\"` este o cheie în rezultat, cu `meta[\"status\"]` citit corect"
        }
      }
    },
    {
      "id": "ARCH-PIPE-046",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "A closed output pipe ends a command quietly",
      "intent": "`reqmap.py gate --dupes | head` is the natural way to look at a long report. On Windows there is no SIGPIPE, so when `head` stops reading, the next `print` raises `OSError` and the command dies with a traceback the reader had nothing to do with. The first C/C++ evidence run hit it on `curl` (2,055 pairs). A closed reader is not an error.",
      "contract": [
        "`_run_cli` ends a command with exit code 0 and no traceback when its standard output turns out to be a closed pipe. [[REQ-PIPE-893]]"
      ],
      "verify": [],
      "notes": [
        "Exit 0 on purpose: `dupes | head -12` ended early because the reader had seen enough, and a non-zero code there would fail a shell pipeline that did exactly what was asked.",
        "After the pipe closes, standard output is pointed at the null device and the process ends with `os._exit(0)`: measured on Windows, the interpreter's own shutdown flush still raised `EINVAL` after the redirect and the command exited 120 with an \"Exception ignored\" line."
      ],
      "current_impl": [
        "`_run_cli()` in `reqmap.py`, the `__main__` entry."
      ],
      "acc": [
        "CASE-1 — Given  a command whose `print` raises `BrokenPipeError`, or `OSError` with `EPIPE`/`EINVAL` When   `_run_cli` runs it Then   it returns 0 and raises nothing",
        "CASE-2 — Given  a command whose `print` raises any other `OSError` When   `_run_cli` runs it Then   that error propagates unchanged",
        "CASE-3 — Given  a command that completes normally When   `_run_cli` runs it Then   the command's own exit code is returned"
      ],
      "accept": "CASE-1\n  Given  a command whose `print` raises `BrokenPipeError`, or `OSError` with `EPIPE`/`EINVAL`\n  When   `_run_cli` runs it\n  Then   it returns 0 and raises nothing\n\nCASE-2\n  Given  a command whose `print` raises any other `OSError`\n  When   `_run_cli` runs it\n  Then   that error propagates unchanged\n\nCASE-3\n  Given  a command that completes normally\n  When   `_run_cli` runs it\n  Then   the command's own exit code is returned",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-CMDREGISTRY-033"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-QUALITY-104"
      ],
      "satisfied_by": [
        "REQ-PIPE-893"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:9336"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:9355"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6930"
        }
      ],
      "test_exempt": null,
      "milestone": "v2.9",
      "priority": "",
      "risks": []
    },
    {
      "id": "REQ-PIPE-893",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "A closed reader ends the command with exit 0",
      "intent": "`reqmap.py gate --dupes | head` closes the pipe as soon as `head` has enough. On Windows that surfaces as `OSError` rather than `SIGPIPE`, and the command used to die with a traceback the reader had nothing to do with. `_run_cli` catches that specific failure and turns it into a quiet exit 0, so a normal shell pipeline never looks like a crash.",
      "contract": [
        "When the command's standard output turns out to be a closed pipe, `reqmap.py` ends the command, prints no traceback, and exits with code 0.",
        "`reqmap.py` treats `BrokenPipeError` and the Windows form of the same event (`OSError` with `EPIPE` or `EINVAL`) alike.",
        "Any other `OSError` still propagates unchanged. The rule covers a closed reader, not disk or permission errors.",
        "The rule lives in the command-line entry point (`_run_cli`), so every subcommand gets it and none re-implements it."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — BrokenPipeError and its Windows OSError form both exit quietly Given  one run whose `print` raises `BrokenPipeError` and another whose `print` raises `OSError` with `errno` `EINVAL` When   `_run_cli` runs each Then   both return 0 without printing a traceback",
        "CASE-2 — — an unrelated OSError still propagates Given  a command whose `print` raises `OSError` with `errno` `ENOENT` (a missing file, not a closed pipe) When   `_run_cli` runs it Then   that `OSError` propagates instead of being swallowed",
        "CASE-3 — — a command that finishes normally returns its own exit code Given  a command function that returns 3, and one that returns `None` When   `_run_cli` runs each Then   it returns 3 and 0 respectively, unchanged by the pipe handling"
      ],
      "accept": "CASE-1 — BrokenPipeError and its Windows OSError form both exit quietly\n  Given  one run whose `print` raises `BrokenPipeError` and another whose `print` raises `OSError` with `errno` `EINVAL`\n  When   `_run_cli` runs each\n  Then   both return 0 without printing a traceback\n\nCASE-2 — an unrelated OSError still propagates\n  Given  a command whose `print` raises `OSError` with `errno` `ENOENT` (a missing file, not a closed pipe)\n  When   `_run_cli` runs it\n  Then   that `OSError` propagates instead of being swallowed\n\nCASE-3 — a command that finishes normally returns its own exit code\n  Given  a command function that returns 3, and one that returns `None`\n  When   `_run_cli` runs each\n  Then   it returns 3 and 0 respectively, unchanged by the pipe handling",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-PIPE-046"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:9355"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6930"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3
    },
    {
      "id": "ARCH-PROMOTE-011",
      "layer": "feature",
      "level": "architecture",
      "status": "draft",
      "area": "ARCH",
      "title": "Confirmation is a human's answer, and an edit takes it back",
      "intent": "A requirement is `confirmed` when a human has read it and said so. That was a command for a long time, and a command is the wrong shape for it: `confirm <ID>` let anything that could type — a script, an agent, a habit — record a human's judgement. Worse, the judgement then outlived the thing it was about. A contract could be rewritten line by line and keep the `confirmed` its old text earned, because nothing connected editing to re-validating. Confirmation is now asked for and written by hand, and an edit takes it back automatically.",
      "contract": [
        "There is **no `confirm` command.** A status is set by editing the frontmatter, which is what a human sign-off is: someone read it and wrote it down. The engine still owns the edit mechanics, so nothing else has to get them right. [[REQ-PROMOTE-894]] details the behaviour.",
        "**An edited confirmed contract goes back to `draft`.** When `sync` finds that a `confirmed` or `implemented` requirement's binding content no longer matches the lock, it writes `draft` into that requirement and advances the baseline. [[REQ-PROMOTE-974]] details the behaviour.",
        "**`--accept-drift` is the escape hatch, and it is a human saying so:** it keeps the status and advances the baseline. It exists because \"I edited it and it is still valid\" is a real answer — it just has to be given, not assumed.",
        "**The invariant that a confirmed requirement points at code is enforced by the gate, not by a command.** `RM006` is an error, so a status written by hand with no `implements:` member fails at the next run."
      ],
      "verify": [],
      "notes": [
        "**What this costs, said plainly:** a demoted requirement stops being enforced. The gate no longer requires an `implements:` member for it and no longer drift-checks it, until a human confirms it again. That is why the demotion is printed loudly, by name, with the sentence \"These no longer gate\" — the danger is not the demotion, it is a demotion nobody noticed.",
        "`confirm` was removed on 2026-09-05 at the user's request, together with its two guard requirements (REQ-PROMOTE-895, REQ-PROMOTE-896), which described refusals a command can no longer make. The gate covers the first; the second was advisory.",
        "The demotion writes through the same surgical status edit the command used, so a CRLF-committed file stays CRLF and a `status:` line's inline comment survives."
      ],
      "current_impl": [],
      "acc": [
        "CASE-1 — Given  a `confirmed` requirement whose contract has just been edited When   `sync` runs without `--accept-drift` Then   its frontmatter reads `status: draft`, the run says so by name, and the lock advances",
        "CASE-2 — Given  the same requirement When   `sync --accept-drift` runs instead Then   the status stays `confirmed` and the lock advances",
        "CASE-3 — Given  a requirement whose status was set to `confirmed` by hand with no `implements:` member When   `gate` runs Then   it reports `RM006` as an error"
      ],
      "accept": "CASE-1\n  Given  a `confirmed` requirement whose contract has just been edited\n  When   `sync` runs without `--accept-drift`\n  Then   its frontmatter reads `status: draft`, the run says so by name, and the lock\n         advances\n\nCASE-2\n  Given  the same requirement\n  When   `sync --accept-drift` runs instead\n  Then   the status stays `confirmed` and the lock advances\n\nCASE-3\n  Given  a requirement whose status was set to `confirmed` by hand with no `implements:`\n         member\n  When   `gate` runs\n  Then   it reports `RM006` as an error",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001",
        "ARCH-DRIFT-003"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-AUTHOR-101"
      ],
      "satisfied_by": [
        "REQ-PROMOTE-894",
        "REQ-PROMOTE-974"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3253"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3279"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2344"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4052"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:5423"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6284"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9506"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10633"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.08",
      "priority": "",
      "risks": [
        {
          "signal": "unreviewed",
          "advice": "Draft/baseline, not yet validated: review the contract, wire its `tested-by` tests, then promote to `confirmed`. Until then it is tracked, not enforced."
        }
      ],
      "clauses": 3,
      "covered": 2,
      "gap": "no `verifies:` tag for CASE-3"
    },
    {
      "id": "REQ-PROMOTE-894",
      "layer": "feature",
      "level": "code",
      "status": "draft",
      "area": "REQ",
      "title": "A surgical edit to the status line",
      "intent": "Rewriting `status: confirmed` to `status: draft` risks touching the wrong line, losing a trailing comment, or nudging the file's formatting — and on Windows, silently flipping a CRLF file to LF. The engine finds and rewrites only the frontmatter's first `status:` line, so the rest of the file is guaranteed untouched.",
      "contract": [
        "`_write_frontmatter_status` edits only the value of the first `status:` line in the leading frontmatter.",
        "It preserves that line's indentation and any trailing inline comment.",
        "It leaves the body untouched, byte for byte.",
        "It preserves the file's own line endings, per line, so a file with mixed endings keeps every line it did not touch exactly as it was.",
        "In a module file holding several requirements, it edits the block of the requirement it was given, not the first block in the file."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — only the frontmatter's first status line changes Given  a requirement whose body also contains the word \"status:\" in prose When   the status is rewritten Then   only the frontmatter's first `status:` line changes; the prose text is untouched",
        "CASE-2 — — the status line's formatting survives Given  a frontmatter line `status: confirmed  # reviewed by A` When   the status is rewritten to `draft` Then   the line becomes `status: draft  # reviewed by A`, comment and spacing intact",
        "CASE-3 — — the body is never touched Given  a requirement with a body When   the status is rewritten Then   every byte after the frontmatter stays identical",
        "CASE-4 — — the right block in a module file Given  a file holding several requirements, one of the later ones targeted When   the status is rewritten Then   that block's status changes and the others are untouched --------------------"
      ],
      "accept": "CASE-1 — only the frontmatter's first status line changes\n  Given  a requirement whose body also contains the word \"status:\" in prose\n  When   the status is rewritten\n  Then   only the frontmatter's first `status:` line changes; the prose text is untouched\n\nCASE-2 — the status line's formatting survives\n  Given  a frontmatter line `status: confirmed  # reviewed by A`\n  When   the status is rewritten to `draft`\n  Then   the line becomes `status: draft  # reviewed by A`, comment and spacing intact\n\nCASE-3 — the body is never touched\n  Given  a requirement with a body\n  When   the status is rewritten\n  Then   every byte after the frontmatter stays identical\n\nCASE-4 — the right block in a module file\n  Given  a file holding several requirements, one of the later ones targeted\n  When   the status is rewritten\n  Then   that block's status changes and the others are untouched\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-PROMOTE-011"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3253"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8306"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:2344"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9506"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [
        {
          "signal": "unreviewed",
          "advice": "Draft/baseline, not yet validated: review the contract, wire its `tested-by` tests, then promote to `confirmed`. Until then it is tracked, not enforced."
        }
      ],
      "clauses": 4,
      "covered": 4
    },
    {
      "id": "REQ-PROMOTE-974",
      "layer": "feature",
      "level": "code",
      "status": "draft",
      "area": "REQ",
      "title": "An edited contract loses its confirmation",
      "intent": "A `confirmed` status is a claim that a human read THIS text. Once the text changes, the claim is about something that no longer exists. Before this, `sync` refused to advance the baseline and told the caller to pass `--accept-drift`, which made accepting the edit the easy path and re-reading it the hard one. Now the safe outcome is the default.",
      "contract": [
        "When `sync` updates the lock and finds a `confirmed` or `implemented` requirement whose binding hash differs from the committed one, it writes `status: draft` into that requirement.",
        "**A brand-new requirement is not drift.** A requirement absent from the lock has no previous text to have changed, so it is never demoted.",
        "The run **names every demoted requirement**, with its previous status, and states that they no longer gate.",
        "After demoting, the lock and the member lock advance in the same run. The next run is clean, so the demotion happens once, not on every sync.",
        "`--accept-drift` skips the demotion entirely: the status stays and the baseline advances.",
        "The in-memory status is updated too, so the same run's own summary counts the requirement as a draft rather than reporting a status it has just changed."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — an edited confirmed contract is demoted, and the lock advances Given  a `confirmed` requirement, synced once, whose contract is then edited When   `sync` runs without `--accept-drift` Then   the file reads `status: draft`, the output names it, and a second `sync` reports no drift",
        "CASE-2 — — a new requirement is not demoted Given  a `confirmed` requirement that has never been in the lock When   `sync` runs Then   its status is unchanged and nothing is reported as demoted",
        "CASE-3 — — accept-drift keeps the status Given  a `confirmed` requirement whose contract was edited When   `sync --accept-drift` runs Then   the status stays `confirmed` and the lock advances"
      ],
      "accept": "CASE-1 — an edited confirmed contract is demoted, and the lock advances\n  Given  a `confirmed` requirement, synced once, whose contract is then edited\n  When   `sync` runs without `--accept-drift`\n  Then   the file reads `status: draft`, the output names it, and a second `sync` reports\n         no drift\n\nCASE-2 — a new requirement is not demoted\n  Given  a `confirmed` requirement that has never been in the lock\n  When   `sync` runs\n  Then   its status is unchanged and nothing is reported as demoted\n\nCASE-3 — accept-drift keeps the status\n  Given  a `confirmed` requirement whose contract was edited\n  When   `sync --accept-drift` runs\n  Then   the status stays `confirmed` and the lock advances",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-PROMOTE-011"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2942"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10633"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [
        {
          "signal": "unreviewed",
          "advice": "Draft/baseline, not yet validated: review the contract, wire its `tested-by` tests, then promote to `confirmed`. Until then it is tracked, not enforced."
        }
      ],
      "clauses": 3,
      "covered": 3
    },
    {
      "id": "ARCH-PROMOTE-TODO-001",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Promote a TODO item into a requirement draft",
      "intent": "You plan features as checkbox items in `TODO.md`, grouped by milestone. When you decide to build one, you have to turn that line into a real requirement file with a contract and acceptance criteria. This command does the scaffolding step: it finds the TODO item, creates a requirement seeded with its name, milestone and lane, and (optionally) checks the item off — so the roadmap item and its requirement stay connected instead of being retyped by hand.",
      "contract": [
        "`new --from-todo` scaffolds a new draft requirement from an unfinished `TODO.md` item, seeded with its title, milestone and lane. [[REQ-PROMOTE-TODO-897]]",
        "`new --from-todo` refuses with a non-zero exit and writes no file when `--id` is absent, the id is taken, the name matches no open item, or the name is ambiguous. [[REQ-PROMOTE-TODO-898]]",
        "`new --from-todo` does not modify `TODO.md` by default; `--mark-done` checks off the matched item, best-effort. [[REQ-PROMOTE-TODO-899]]"
      ],
      "verify": [],
      "notes": [
        "Selection is by exact name within the unfinished items; rename a TODO to disambiguate rather than adding index syntax. Batch promotion and an undo command are intentionally out of scope.",
        "This is orthogonal to [[ARCH-PROMOTE-011]] (`confirm`, which lifts a baseline requirement to confirmed): this one creates a *new draft* from a TODO line."
      ],
      "current_impl": [
        "`cmd_promote_todo`, `_mark_todo_done` in `reqmap.py`; reuses `_parse_todos` (TODO.md parsing) and the `REQUIREMENT_TEMPLATE` scaffold ([[ARCH-NEW-004]]), dispatched from `main()` under `new --from-todo`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  an unfinished TODO item When   `new --from-todo \"<name>\" --id REQ-X-001` runs Then   `requirements/REQ-X-001.md` is created as a `draft` seeded with the item's title, milestone and layer, and `TODO.md` is left unchanged",
        "CASE-2 — Given  a TODO `lane: ops` When   the requirement is scaffolded Then   its `layer:` is `feature`",
        "CASE-3 — Given  `--mark-done` When   the command runs Then   the matched `TODO.md` line becomes `- [x] <name>` and no other line changes",
        "CASE-4 — Given  a missing `--id`, an already-taken id, a name with no open match, or an ambiguous name When   the command runs Then   it exits non-zero and writes no requirement file"
      ],
      "accept": "CASE-1\n  Given  an unfinished TODO item\n  When   `new --from-todo \"<name>\" --id REQ-X-001` runs\n  Then   `requirements/REQ-X-001.md` is created as a `draft` seeded with the item's title,\n         milestone and layer, and `TODO.md` is left unchanged\n\nCASE-2\n  Given  a TODO `lane: ops`\n  When   the requirement is scaffolded\n  Then   its `layer:` is `feature`\n\nCASE-3\n  Given  `--mark-done`\n  When   the command runs\n  Then   the matched `TODO.md` line becomes `- [x] <name>` and no other line changes\n\nCASE-4\n  Given  a missing `--id`, an already-taken id, a name with no open match, or an ambiguous name\n  When   the command runs\n  Then   it exits non-zero and writes no requirement file",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-NEW-004"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-AUTHOR-101"
      ],
      "satisfied_by": [
        "REQ-PROMOTE-TODO-897",
        "REQ-PROMOTE-TODO-898",
        "REQ-PROMOTE-TODO-899"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3162"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3221"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4593"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6284"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9516"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.14",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Promovarea unui element TODO într-un draft de cerință",
          "intent": "Planifici funcționalități ca elemente checkbox în `TODO.md`, grupate pe milestone. Când decizi să construiești una, trebuie să transformi acea linie într-un fișier de cerință real, cu un contract și criterii de acceptare. Această comandă face pasul de schelărie: găsește elementul TODO, creează o cerință însămânțată cu numele, milestone-ul și lane-ul lui, și (opțional) bifează elementul — astfel încât elementul din roadmap și cerința sa rămân conectate în loc să fie retastate manual.",
          "contract": "> Planifici funcționalități ca elemente checkbox în `TODO.md`, grupate pe milestone.\n> Când decizi să construiești una, trebuie să transformi acea linie într-un fișier de\n> cerință real, cu un contract și criterii de acceptare. Această comandă face pasul de\n> schelărie: găsește elementul TODO, creează o cerință însămânțată cu numele,\n> milestone-ul și lane-ul lui, și (opțional) bifează elementul — astfel încât elementul\n> din roadmap și cerința sa rămân conectate în loc să fie retastate manual.\n\nFiecare punct de mai jos este obligatoriu.\n- `new --from-todo` schițează o nouă cerință draft dintr-un element neterminat din `TODO.md`, însămânțată cu titlul, milestone-ul și lane-ul lui. [[REQ-PROMOTE-TODO-897]]\n- `new --from-todo` refuză cu un exit diferit de zero și nu scrie niciun fișier atunci când `--id` lipsește, id-ul este deja luat, numele nu se potrivește cu niciun element deschis, sau numele este ambiguu. [[REQ-PROMOTE-TODO-898]]\n- `new --from-todo` nu modifică `TODO.md` implicit; `--mark-done` bifează elementul potrivit, pe bază de best-effort. [[REQ-PROMOTE-TODO-899]]",
          "acceptance": "CASE-1\n  Given  un element TODO neterminat\n  When   `new --from-todo \"<name>\" --id REQ-X-001` rulează\n  Then   `requirements/REQ-X-001.md` este creat ca `draft` însămânțat cu titlul, milestone-ul\n         și layer-ul elementului, iar `TODO.md` rămâne neschimbat\n\nCASE-2\n  Given  un `lane: ops` din TODO\n  When   cerința este schițată\n  Then   `layer:`-ul ei este `feature`\n\nCASE-3\n  Given  `--mark-done`\n  When   comanda rulează\n  Then   linia potrivită din `TODO.md` devine `- [x] <name>` iar nicio altă linie nu se schimbă\n\nCASE-4\n  Given  un `--id` lipsă, un id deja luat, un nume fără nicio potrivire deschisă, sau un nume ambiguu\n  When   comanda rulează\n  Then   iese cu cod diferit de zero și nu scrie niciun fișier de cerință"
        }
      }
    },
    {
      "id": "REQ-PROMOTE-TODO-897",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Scaffolding a draft from a matched TODO item",
      "intent": "`new --from-todo` turns a `TODO.md` checkbox line into a real requirement file, so a planned feature and its eventual contract stay one traceable thing instead of being retyped by hand. It requires an explicit `--id` rather than prompting, because the engine also runs headless in CI and git hooks.",
      "contract": [
        "`new --from-todo` scaffolds a new requirement file from an unfinished `TODO.md` item.",
        "The item is selected by exact name, trimmed and compared case-insensitively.",
        "`new --from-todo` requires an explicit `--id AREA-NAME-NNN`. There is no interactive prompt, because the engine runs headless.",
        "`new --from-todo` seeds the new requirement from the matched item: the title from the TODO name, `milestone:` from the item's `## vX.Y` section, `layer:` from the item's `lane:`.",
        "A `lane: ops` maps to `layer: feature`.",
        "The new requirement's status is `draft`, so the author reviews it and then promotes it."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — new --from-todo scaffolds a requirement from an unfinished item Given  an unfinished TODO.md item named \"Add export command\" When   `new --from-todo \"Add export command\" --id REQ-X-001` runs Then   `requirements/REQ-X-001.md` is created",
        "CASE-2 — — matching is case-insensitive and trims whitespace Given  a TODO item named \"Add Export Command\" When   `new --from-todo \"  add export command  \" --id REQ-X-001` runs Then   the item is matched and the requirement is scaffolded",
        "CASE-3 — — the scaffolded requirement is seeded from the matched item Given  a TODO item \"Add export command\" under `## v2.0` with `lane: cli` When   `new --from-todo \"Add export command\" --id REQ-X-001` runs Then   the new file's title comes from the item name, `milestone: v2.0`, and `layer:` reflects the `cli` lane",
        "CASE-4 — — a lane of ops maps to layer feature Given  a TODO item with `lane: ops` When   the requirement is scaffolded from it Then   the new file's `layer:` reads `feature`",
        "CASE-5 — — a scaffolded requirement starts as draft Given  an unfinished TODO item When   `new --from-todo` scaffolds it Then   the new file's `status:` reads `draft` --------------------"
      ],
      "accept": "CASE-1 — new --from-todo scaffolds a requirement from an unfinished item\n  Given  an unfinished TODO.md item named \"Add export command\"\n  When   `new --from-todo \"Add export command\" --id REQ-X-001` runs\n  Then   `requirements/REQ-X-001.md` is created\n\nCASE-2 — matching is case-insensitive and trims whitespace\n  Given  a TODO item named \"Add Export Command\"\n  When   `new --from-todo \"  add export command  \" --id REQ-X-001` runs\n  Then   the item is matched and the requirement is scaffolded\n\nCASE-3 — the scaffolded requirement is seeded from the matched item\n  Given  a TODO item \"Add export command\" under `## v2.0` with `lane: cli`\n  When   `new --from-todo \"Add export command\" --id REQ-X-001` runs\n  Then   the new file's title comes from the item name, `milestone: v2.0`, and `layer:` reflects\n         the `cli` lane\n\nCASE-4 — a lane of ops maps to layer feature\n  Given  a TODO item with `lane: ops`\n  When   the requirement is scaffolded from it\n  Then   the new file's `layer:` reads `feature`\n\nCASE-5 — a scaffolded requirement starts as draft\n  Given  an unfinished TODO item\n  When   `new --from-todo` scaffolds it\n  Then   the new file's `status:` reads `draft`\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-PROMOTE-TODO-001"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3162"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4593"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9516"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Ștanțarea unui draft dintr-un item TODO potrivit",
          "intent": "`new --from-todo` transformă o linie de checkbox din `TODO.md` într-un fișier de cerință real, astfel încât o funcționalitate planificată și contractul ei eventual rămân un singur lucru trasabil în loc să fie retastate de mână. Cere un `--id` explicit în loc să întrebe interactiv, deoarece engine-ul rulează de asemenea headless în CI și git hooks.",
          "contract": "> `new --from-todo` transformă o linie de checkbox din `TODO.md` într-un fișier de\n> cerință real, astfel încât o funcționalitate planificată și contractul ei eventual\n> rămân un singur lucru trasabil în loc să fie retastate de mână. Cere un `--id` explicit\n> în loc să întrebe interactiv, deoarece engine-ul rulează de asemenea headless în CI și\n> git hooks.\n\nFiecare bullet de mai jos este obligatoriu.\n- `new --from-todo` ștanțează un fișier de cerință nou dintr-un item `TODO.md` neterminat.\n- Item-ul este selectat după numele exact, trimuit și comparat case-insensitive.\n- `new --from-todo` cere un `--id AREA-NAME-NNN` explicit. Nu există niciun prompt\n  interactiv, deoarece engine-ul rulează headless.\n- `new --from-todo` seedează cerința nouă din item-ul potrivit: titlul din numele\n  TODO-ului, `milestone:` din secțiunea `## vX.Y` a item-ului, `layer:` din `lane:`-ul item-ului.\n- Un `lane: ops` se mapează la `layer: feature`.\n- Statusul cerinței noi este `draft`, astfel încât autorul o revizuiește și apoi o promovează.",
          "acceptance": "CASE-1 — new --from-todo ștanțează o cerință dintr-un item neterminat\n  Given  un item TODO.md neterminat numit \"Add export command\"\n  When   rulează `new --from-todo \"Add export command\" --id REQ-X-001`\n  Then   `requirements/REQ-X-001.md` este creat\n\nCASE-2 — potrivirea este case-insensitive și trimuie whitespace\n  Given  un item TODO numit \"Add Export Command\"\n  When   rulează `new --from-todo \"  add export command  \" --id REQ-X-001`\n  Then   item-ul este potrivit iar cerința este ștanțată\n\nCASE-3 — cerința ștanțată este seedată din item-ul potrivit\n  Given  un item TODO \"Add export command\" sub `## v2.0` cu `lane: cli`\n  When   rulează `new --from-todo \"Add export command\" --id REQ-X-001`\n  Then   titlul fișierului nou vine din numele item-ului, `milestone: v2.0`, iar `layer:` reflectă\n         lane-ul `cli`\n\nCASE-4 — un lane de ops se mapează la layer feature\n  Given  un item TODO cu `lane: ops`\n  When   cerința este ștanțată din el\n  Then   `layer:`-ul fișierului nou se citește `feature`\n\nCASE-5 — o cerință ștanțată pornește ca draft\n  Given  un item TODO neterminat\n  When   `new --from-todo` îl ștanțează\n  Then   `status:`-ul fișierului nou se citește `draft`\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-PROMOTE-TODO-898",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Refusing an unresolvable promotion",
      "intent": "A promotion that can't be pinned to exactly one TODO item and one free id must not silently guess. `new --from-todo` refuses outright — no partial requirement file, no half-checked `TODO.md` — whenever the id is missing or taken, or the name matches zero or more than one open item.",
      "contract": [
        "`new --from-todo` refuses with a non-zero exit and no file written when the `--id` is absent.",
        "The command refuses when the target id already exists.",
        "The command refuses when no open TODO matches the name.",
        "The command refuses when the name is ambiguous, meaning more than one open item matches.",
        "Each refusal prints a clear message. For a name with no match, that message lists the open items."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a missing --id refuses the command Given  an unfinished TODO item and no `--id` flag When   `new --from-todo \"<name>\"` runs Then   it exits non-zero and writes no requirement file, never prompting for the missing value — the engine runs headless",
        "CASE-2 — — an already-taken id refuses the command Given  `--id REQ-X-001` naming a file that already exists When   `new --from-todo \"<name>\" --id REQ-X-001` runs Then   it exits non-zero and the existing file is left unchanged",
        "CASE-3 — — an unmatched name refuses the command Given  a name matching no open TODO item When   `new --from-todo \"<name>\" --id REQ-X-001` runs Then   it exits non-zero and writes no requirement file",
        "CASE-4 — — an ambiguous name refuses the command Given  two open TODO items whose names both match the given name When   `new --from-todo \"<name>\" --id REQ-X-001` runs Then   it exits non-zero and writes no requirement file",
        "CASE-5 — — a no-match refusal lists the open items Given  a name matching no open TODO item When   `new --from-todo \"<name>\" --id REQ-X-001` runs Then   the printed error message lists the currently open TODO items --------------------"
      ],
      "accept": "CASE-1 — a missing --id refuses the command\n  Given  an unfinished TODO item and no `--id` flag\n  When   `new --from-todo \"<name>\"` runs\n  Then   it exits non-zero and writes no requirement file, never prompting for the missing\n         value — the engine runs headless\n\nCASE-2 — an already-taken id refuses the command\n  Given  `--id REQ-X-001` naming a file that already exists\n  When   `new --from-todo \"<name>\" --id REQ-X-001` runs\n  Then   it exits non-zero and the existing file is left unchanged\n\nCASE-3 — an unmatched name refuses the command\n  Given  a name matching no open TODO item\n  When   `new --from-todo \"<name>\" --id REQ-X-001` runs\n  Then   it exits non-zero and writes no requirement file\n\nCASE-4 — an ambiguous name refuses the command\n  Given  two open TODO items whose names both match the given name\n  When   `new --from-todo \"<name>\" --id REQ-X-001` runs\n  Then   it exits non-zero and writes no requirement file\n\nCASE-5 — a no-match refusal lists the open items\n  Given  a name matching no open TODO item\n  When   `new --from-todo \"<name>\" --id REQ-X-001` runs\n  Then   the printed error message lists the currently open TODO items\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-PROMOTE-TODO-001"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3162"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4593"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9516"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Refuzul unei promovări irezolvabile",
          "intent": "O promovare care nu poate fi fixată pe exact un item TODO și un id liber nu trebuie niciodată să ghicească în tăcere. `new --from-todo` refuză categoric — niciun fișier de cerință parțial, niciun `TODO.md` pe jumătate bifat — ori de câte ori id-ul lipsește sau este ocupat, sau numele se potrivește cu zero sau mai multe itemi deschise.",
          "contract": "> O promovare care nu poate fi fixată pe exact un item TODO și un id liber nu trebuie\n> niciodată să ghicească în tăcere. `new --from-todo` refuză categoric — niciun fișier de\n> cerință parțial, niciun `TODO.md` pe jumătate bifat — ori de câte ori id-ul lipsește sau\n> este ocupat, sau numele se potrivește cu zero sau mai multe itemi deschise.\n\nFiecare bullet de mai jos este obligatoriu.\n- `new --from-todo` refuză cu un cod de ieșire non-zero și niciun fișier scris când `--id`-ul\n  lipsește.\n- Comanda refuză când id-ul țintă există deja.\n- Comanda refuză când niciun TODO deschis nu se potrivește cu numele.\n- Comanda refuză când numele este ambiguu, adică mai mult de un item deschis\n  se potrivește.\n- Fiecare refuz afișează un mesaj clar. Pentru un nume fără nicio potrivire, acel mesaj listează\n  itemii deschise.",
          "acceptance": "CASE-1 — un --id lipsă refuză comanda\n  Given  un item TODO neterminat și niciun flag `--id`\n  When   rulează `new --from-todo \"<name>\"`\n  Then   iese cu cod non-zero și nu scrie niciun fișier de cerință, fără să întrebe niciodată\n         pentru valoarea lipsă — engine-ul rulează headless\n\nCASE-2 — un id deja ocupat refuză comanda\n  Given  `--id REQ-X-001` numind un fișier care există deja\n  When   rulează `new --from-todo \"<name>\" --id REQ-X-001`\n  Then   iese cu cod non-zero iar fișierul existent este lăsat neschimbat\n\nCASE-3 — un nume nepotrivit refuză comanda\n  Given  un nume care nu se potrivește cu niciun item TODO deschis\n  When   rulează `new --from-todo \"<name>\" --id REQ-X-001`\n  Then   iese cu cod non-zero și nu scrie niciun fișier de cerință\n\nCASE-4 — un nume ambiguu refuză comanda\n  Given  două itemi TODO deschise ale căror nume se potrivesc amândouă cu numele dat\n  When   rulează `new --from-todo \"<name>\" --id REQ-X-001`\n  Then   iese cu cod non-zero și nu scrie niciun fișier de cerință\n\nCASE-5 — un refuz de tip fără-potrivire listează itemii deschise\n  Given  un nume care nu se potrivește cu niciun item TODO deschis\n  When   rulează `new --from-todo \"<name>\" --id REQ-X-001`\n  Then   mesajul de eroare afișat listează itemii TODO deschise în prezent\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-PROMOTE-TODO-899",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "TODO.md stays untouched unless --mark-done asks otherwise",
      "intent": "Scaffolding a requirement and checking off its TODO item are two different intents, so the default run leaves `TODO.md` byte-identical and only `--mark-done` flips the checkbox. The flip is best-effort: a roadmap file the process cannot write to must not turn a successful scaffold into a failed command.",
      "contract": [
        "`new --from-todo` does not modify `TODO.md` by default.",
        "With `--mark-done` it flips the matched item's checkbox `[ ]` → `[x]`.",
        "That flip is best-effort: a write failure warns and does not fail the command."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — scaffolding leaves TODO.md untouched by default Given  an unfinished TODO item When   `new --from-todo \"<name>\" --id REQ-X-001` runs without `--mark-done` Then   `TODO.md` is byte-identical to before the run",
        "CASE-2 — — --mark-done checks off the matched item Given  an unfinished TODO item \"Add export command\" When   `new --from-todo \"Add export command\" --id REQ-X-001 --mark-done` runs Then   that line in `TODO.md` reads `- [x] Add export command`",
        "CASE-3 — — a TODO.md write failure warns but does not fail the command Given  `--mark-done` and a `TODO.md` the process cannot write to When   `new --from-todo \"<name>\" --id REQ-X-001 --mark-done` runs Then   it prints a warning, still exits zero, and the requirement file is still created"
      ],
      "accept": "CASE-1 — scaffolding leaves TODO.md untouched by default\n  Given  an unfinished TODO item\n  When   `new --from-todo \"<name>\" --id REQ-X-001` runs without `--mark-done`\n  Then   `TODO.md` is byte-identical to before the run\n\nCASE-2 — --mark-done checks off the matched item\n  Given  an unfinished TODO item \"Add export command\"\n  When   `new --from-todo \"Add export command\" --id REQ-X-001 --mark-done` runs\n  Then   that line in `TODO.md` reads `- [x] Add export command`\n\nCASE-3 — a TODO.md write failure warns but does not fail the command\n  Given  `--mark-done` and a `TODO.md` the process cannot write to\n  When   `new --from-todo \"<name>\" --id REQ-X-001 --mark-done` runs\n  Then   it prints a warning, still exits zero, and the requirement file is still created",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-PROMOTE-TODO-001"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3162"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3221"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4593"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9516"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "TODO.md rămâne neatins cu excepția cazului în care --mark-done cere altfel",
          "intent": "Ștanțarea unei cerințe și bifarea item-ului ei TODO sunt două intenții diferite, deci rularea implicită lasă `TODO.md` byte-identic iar doar `--mark-done` flip-uiește checkbox-ul. Flip-ul este best-effort: un fișier de roadmap în care procesul nu poate scrie nu trebuie să transforme un scaffold reușit într-o comandă eșuată.",
          "contract": "> Ștanțarea unei cerințe și bifarea item-ului ei TODO sunt două intenții diferite, deci\n> rularea implicită lasă `TODO.md` byte-identic iar doar `--mark-done` flip-uiește\n> checkbox-ul. Flip-ul este best-effort: un fișier de roadmap în care procesul nu poate\n> scrie nu trebuie să transforme un scaffold reușit într-o comandă eșuată.\n\nFiecare bullet de mai jos este obligatoriu.\n- `new --from-todo` nu modifică implicit `TODO.md`.\n- Cu `--mark-done` flip-uiește checkbox-ul item-ului potrivit `[ ]` → `[x]`.\n- Acel flip este best-effort: un eșec de scriere avertizează și nu eșuează comanda.",
          "acceptance": "CASE-1 — ștanțarea lasă TODO.md neatins implicit\n  Given  un item TODO neterminat\n  When   rulează `new --from-todo \"<name>\" --id REQ-X-001` fără `--mark-done`\n  Then   `TODO.md` este byte-identic cu înainte de rulare\n\nCASE-2 — --mark-done bifiază item-ul potrivit\n  Given  un item TODO neterminat \"Add export command\"\n  When   rulează `new --from-todo \"Add export command\" --id REQ-X-001 --mark-done`\n  Then   acea linie din `TODO.md` se citește `- [x] Add export command`\n\nCASE-3 — un eșec de scriere pentru TODO.md avertizează dar nu eșuează comanda\n  Given  `--mark-done` și un `TODO.md` în care procesul nu poate scrie\n  When   rulează `new --from-todo \"<name>\" --id REQ-X-001 --mark-done`\n  Then   afișează un avertisment, tot iese cu zero, iar fișierul de cerință este totuși creat"
        }
      }
    },
    {
      "id": "ARCH-PROSE-024",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Prose capability classification & drafting",
      "intent": "Specs and prompts written as text documents can be capabilities too, not just code. When `draft` runs, this part decides which text files deserve a draft requirement, which are only kept in sync, and which are ignored as boilerplate — then writes the drafts. Without it, prompt and spec files would stay invisible to the registry, or every README would pollute it with junk drafts.",
      "contract": [
        "`classify_prose` sorts every untagged prose file (`.md`/`.html`) into one of three buckets — ignore, sync_only, or capability — and `draft` auto-drafts only the capability bucket. [[REQ-PROSE-900]] details the behaviour.",
        "The buckets govern auto-drafting only; an explicit tag on any file is always honored regardless of bucket, and a capability draft is scaffolded from the file's title and section headings. [[REQ-PROSE-901]] details the behaviour."
      ],
      "verify": [],
      "notes": [
        "The bucket lists are fixed in the engine, not configurable; `.reqmapignore` remains the per-repo escape hatch for anything the buckets misjudge."
      ],
      "current_impl": [
        "`classify_prose` (the three-bucket router) and `_prose_facts` (title + heading scaffold) in `reqmap.py`; the prose walk lives inside `cmd_extract`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a `prompts/foo.md` with no member tag When   `draft` runs Then   a `draft`-status requirement is written for it",
        "CASE-2 — Given  `README.md`, a file under `docs/`, a `*.html` file, `CLAUDE.md`, or `CHANGELOG.md` When   `draft` runs Then   no draft is written for it",
        "CASE-3 — Given  a capability-bucket prose file already tagged `# implements: <ID>` When   `draft` runs Then   it is skipped (no duplicate draft)",
        "CASE-4 — Given  a file tagged `generated-from: <ID>` inside an HTML comment (``) When   the scanner runs Then   that file is a member of `<ID>`"
      ],
      "accept": "CASE-1\n  Given  a `prompts/foo.md` with no member tag\n  When   `draft` runs\n  Then   a `draft`-status requirement is written for it\n\nCASE-2\n  Given  `README.md`, a file under `docs/`, a `*.html` file, `CLAUDE.md`, or `CHANGELOG.md`\n  When   `draft` runs\n  Then   no draft is written for it\n\nCASE-3\n  Given  a capability-bucket prose file already tagged `# implements: <ID>`\n  When   `draft` runs\n  Then   it is skipped (no duplicate draft)\n\nCASE-4\n  Given  a file tagged `generated-from: <ID>` inside an HTML comment (`<!-- generated-from: <ID> -->`)\n  When   the scanner runs\n  Then   that file is a member of `<ID>`",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-SCAN-002",
        "ARCH-EXTRACT-008"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-READ-103"
      ],
      "satisfied_by": [
        "REQ-PROSE-900",
        "REQ-PROSE-901"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3323"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3348"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3382"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:839"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:857"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1004"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1055"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6828"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8895"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.06",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Clasificarea și schițarea capabilităților de proză",
          "intent": "Specificațiile și prompt-urile scrise ca documente de text pot fi și ele capabilități, nu doar codul. Când `draft` rulează, această parte decide ce fișiere de text merită un draft de cerință, care sunt doar ținute în sincronizare, și care sunt ignorate ca boilerplate — apoi scrie drafturile. Fără aceasta, fișierele de prompt și specificație ar rămâne invizibile pentru registru, sau fiecare README l-ar polua cu drafturi inutile.",
          "contract": "> Specificațiile și prompt-urile scrise ca documente de text pot fi și ele\n> capabilități, nu doar codul. Când `draft` rulează, această parte decide ce fișiere de\n> text merită un draft de cerință, care sunt doar ținute în sincronizare, și care sunt\n> ignorate ca boilerplate — apoi scrie drafturile. Fără aceasta, fișierele de prompt și\n> specificație ar rămâne invizibile pentru registru, sau fiecare README l-ar polua cu\n> drafturi inutile.\n\nFiecare punct de mai jos este obligatoriu.\n- `classify_prose` sortează fiecare fișier de proză neetichetat (`.md`/`.html`) în unul din trei bucket-uri — ignore, sync_only, sau capability — iar `draft` auto-schițează doar bucket-ul capability. [[REQ-PROSE-900]] detaliază comportamentul.\n- Bucket-urile guvernează doar auto-schițarea; o etichetă explicită pe orice fișier este întotdeauna respectată indiferent de bucket, iar un draft de capabilitate este schelărit din titlul fișierului și titlurile secțiunilor. [[REQ-PROSE-901]] detaliază comportamentul.",
          "acceptance": "CASE-1\n  Given  un `prompts/foo.md` fără nicio etichetă de membru\n  When   `draft` rulează\n  Then   o cerință cu status `draft` este scrisă pentru el\n\nCASE-2\n  Given  `README.md`, un fișier sub `docs/`, un fișier `*.html`, `CLAUDE.md`, sau `CHANGELOG.md`\n  When   `draft` rulează\n  Then   niciun draft nu este scris pentru el\n\nCASE-3\n  Given  un fișier de proză din bucket-ul capability deja etichetat `# implements: <ID>`\n  When   `draft` rulează\n  Then   este sărit (niciun draft duplicat)\n\nCASE-4\n  Given  un fișier etichetat `generated-from: <ID>` în interiorul unui comentariu HTML (`<!-- generated-from: <ID> -->`)\n  When   scanner-ul rulează\n  Then   acel fișier este membru al `<ID>`"
        }
      }
    },
    {
      "id": "REQ-PROSE-900",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Sorting prose files into three drafting buckets",
      "intent": "Not every text file is a capability — a README and a prompt spec need different treatment. `classify_prose` sorts each untagged prose file into ignore (boilerplate), sync_only (README/docs/HTML, drift-checked only once hand-tagged), or capability (auto-drafted), so `draft` neither misses real specs nor floods the registry with junk.",
      "contract": [
        "`draft` also produces `draft`-status requirements from untagged **prose** files (`.md`/`.html`) — prose meaning human-readable spec/prompt text, as opposed to source code.",
        "Each prose file is classified into one of three buckets before drafting:",
        "**ignore** — meta/boilerplate that is never a capability: `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`, `CONTRIBUTING.md`, `SKILL.md`, `TODO.md`, `CHANGELOG.md`, `LICENSE`/`LICENSE.*`, and any `_`-prefixed generated file (`_map.html`, `_findings.md`, …).",
        "**sync_only** — `README`/`README.*` in any letter case, everything under `docs/`, and every `*.html`. These are never drafted as their own requirement, but become a member (and are drift-checked) when a human tags them `generated-from: <ID>`.",
        "**capability** — everything else (e.g. `prompts/`, `specs/`, `modes/` prose). These are auto-drafted."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — draft writes a requirement for an untagged capability-bucket prose file Given  `prompts/foo.md` with no `# implements:` tag When   `draft` runs Then   a new `draft`-status requirement is written scaffolded from that file",
        "CASE-2 — — classify_prose buckets meta/boilerplate files as ignore Given  the paths `CLAUDE.md`, `LICENSE`, and `_map.html` When   `classify_prose` runs on each Then   each returns `\"ignore\"`",
        "CASE-3 — — classify_prose buckets README/docs/html paths as sync_only Given  the paths `README.md`, `docs/sub/guide.md`, and `x.html` When   `classify_prose` runs on each Then   each returns `\"sync_only\"`",
        "CASE-4 — — classify_prose buckets prompt/spec prose as capability Given  the paths `prompts/senators/aurelius.md`, `specs/foo.md`, and `notes.md` When   `classify_prose` runs on each Then   each returns `\"capability\"` --------------------"
      ],
      "accept": "CASE-1 — draft writes a requirement for an untagged capability-bucket prose file\n  Given  `prompts/foo.md` with no `# implements:` tag\n  When   `draft` runs\n  Then   a new `draft`-status requirement is written scaffolded from that file\n\nCASE-2 — classify_prose buckets meta/boilerplate files as ignore\n  Given  the paths `CLAUDE.md`, `LICENSE`, and `_map.html`\n  When   `classify_prose` runs on each\n  Then   each returns `\"ignore\"`\n\nCASE-3 — classify_prose buckets README/docs/html paths as sync_only\n  Given  the paths `README.md`, `docs/sub/guide.md`, and `x.html`\n  When   `classify_prose` runs on each\n  Then   each returns `\"sync_only\"`\n\nCASE-4 — classify_prose buckets prompt/spec prose as capability\n  Given  the paths `prompts/senators/aurelius.md`, `specs/foo.md`, and `notes.md`\n  When   `classify_prose` runs on each\n  Then   each returns `\"capability\"`\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-PROSE-024"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3323"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:839"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1004"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Sortarea fișierelor de proză în trei bucket-uri de redactare",
          "intent": "Nu orice fișier de text este o capabilitate — un README și o specificație de prompt au nevoie de tratament diferit. `classify_prose` sortează fiecare fișier de proză netagat în ignore (boilerplate), sync_only (README/docs/HTML, verificat pentru drift doar odată tagat de mână), sau capability (auto-redactat), astfel încât `draft` nici nu ratează specificații reale, nici nu inundă registry-ul cu junk.",
          "contract": "> Nu orice fișier de text este o capabilitate — un README și o specificație de prompt\n> au nevoie de tratament diferit. `classify_prose` sortează fiecare fișier de proză\n> netagat în ignore (boilerplate), sync_only (README/docs/HTML, verificat pentru drift\n> doar odată tagat de mână), sau capability (auto-redactat), astfel încât `draft` nici nu\n> ratează specificații reale, nici nu inundă registry-ul cu junk.\n\nFiecare bullet de mai jos este obligatoriu.\n- `draft` produce de asemenea cerințe cu status `draft` din fișiere de **proză** netagate\n  (`.md`/`.html`) — proză însemnând text de specificație/prompt lizibil pentru om, spre deosebire\n  de cod sursă.\n- Fiecare fișier de proză este clasificat într-unul din trei bucket-uri înainte de redactare:\n  - **ignore** — meta/boilerplate care nu este niciodată o capabilitate: `CLAUDE.md`, `AGENTS.md`,\n    `GEMINI.md`, `CONTRIBUTING.md`, `SKILL.md`, `TODO.md`, `CHANGELOG.md`,\n    `LICENSE`/`LICENSE.*`, și orice fișier generat prefixat cu `_` (`_map.html`, `_findings.md`, …).\n  - **sync_only** — `README`/`README.*` în orice caz de litere, tot ce se află sub `docs/`, și\n    orice `*.html`.\n    Acestea nu sunt niciodată redactate ca propria lor cerință, dar devin un membru (și sunt\n    verificate pentru drift) când un om le tagă `generated-from: <ID>`.\n  - **capability** — orice altceva (de ex. proza din `prompts/`, `specs/`, `modes/`). Acestea\n    sunt auto-redactate.",
          "acceptance": "CASE-1 — draft scrie o cerință pentru un fișier de proză netagat din bucket-ul capability\n  Given  `prompts/foo.md` fără niciun tag `# implements:`\n  When   rulează `draft`\n  Then   o nouă cerință cu status `draft` este scrisă, ștanțată din acel fișier\n\nCASE-2 — classify_prose bucketuiește fișiere meta/boilerplate ca ignore\n  Given  căile `CLAUDE.md`, `LICENSE`, și `_map.html`\n  When   rulează `classify_prose` pe fiecare\n  Then   fiecare returnează `\"ignore\"`\n\nCASE-3 — classify_prose bucketuiește căi README/docs/html ca sync_only\n  Given  căile `README.md`, `docs/sub/guide.md`, și `x.html`\n  When   rulează `classify_prose` pe fiecare\n  Then   fiecare returnează `\"sync_only\"`\n\nCASE-4 — classify_prose bucketuiește proza de prompt/spec ca capability\n  Given  căile `prompts/senators/aurelius.md`, `specs/foo.md`, și `notes.md`\n  When   rulează `classify_prose` pe fiecare\n  Then   fiecare returnează `\"capability\"`\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-PROSE-901",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Scaffolding a draft from a prose file's own headings",
      "intent": "A bucket decides only whether to auto-draft; a hand-added tag always wins, so a hand-tagged README still counts as a member even though it is never auto-drafted. When a capability file is drafted, its title and section headings become the scaffold — and because the drift hash anchors on the authored contract, not the source prose, editing the original file afterward never falsely reports drift.",
      "contract": [
        "The buckets govern auto-drafting ONLY; an explicit tag on any file is always honored by the scanner regardless of bucket (so a hand-tagged README is still a member).",
        "A prose draft is scaffolded from the file's title (frontmatter `title:`, else the first `#` heading, else the HTML `<title>`/`<h1>`) plus its `##` section headings, recorded as an authoring hint.",
        "When a file has no `##` heading at all, its later `#` headings are the sections instead, so flat single-level prose (a prompt library) still yields a hint.",
        "The source prose is never the contract: the drift hash anchors on the authored Contract + Acceptance, so the prose may later drift freely from the authored requirement."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — an explicit generated-from tag registers a sync_only file as a member Given  a `README.md` carrying a `` comment When   `scan_members` runs Then   the file appears as a member of `REQ-DOCS-001`, despite being sync_only",
        "CASE-2 — — _prose_facts prefers frontmatter title, then collects the H2 headings Given  a source with frontmatter `title: Senator Aurelius` and two `## ` headings When   `_prose_facts(src)` runs Then   it returns `(\"Senator Aurelius\", [\"Role\", \"Specialty\"])`",
        "CASE-3 — — later H1 headings become the section hint when no H2 exists Given  `\"# IDENTITY and PURPOSE\\n\\ntext\\n\\n# STEPS\\n\\n# OUTPUT INSTRUCTIONS\\n\"` (no `##` anywhere) When   `_prose_facts(src)` runs Then   the headings list is `[\"STEPS\", \"OUTPUT INSTRUCTIONS\"]`, drawn from the later H1s",
        "CASE-4 — — editing the source prose file does not change the requirement's binding_hash Given  a capability-bucket prose file drafted into a requirement, then edited afterward When   `binding_hash` is computed on the requirement body before and after the edit Then   the two hashes are equal — the prose edit produced no drift"
      ],
      "accept": "CASE-1 — an explicit generated-from tag registers a sync_only file as a member\n  Given  a `README.md` carrying a `<!-- generated-from: REQ-DOCS-001 -->` comment\n  When   `scan_members` runs\n  Then   the file appears as a member of `REQ-DOCS-001`, despite being sync_only\n\nCASE-2 — _prose_facts prefers frontmatter title, then collects the H2 headings\n  Given  a source with frontmatter `title: Senator Aurelius` and two `## ` headings\n  When   `_prose_facts(src)` runs\n  Then   it returns `(\"Senator Aurelius\", [\"Role\", \"Specialty\"])`\n\nCASE-3 — later H1 headings become the section hint when no H2 exists\n  Given  `\"# IDENTITY and PURPOSE\\n\\ntext\\n\\n# STEPS\\n\\n# OUTPUT INSTRUCTIONS\\n\"` (no `##` anywhere)\n  When   `_prose_facts(src)` runs\n  Then   the headings list is `[\"STEPS\", \"OUTPUT INSTRUCTIONS\"]`, drawn from the later H1s\n\nCASE-4 — editing the source prose file does not change the requirement's binding_hash\n  Given  a capability-bucket prose file drafted into a requirement, then edited afterward\n  When   `binding_hash` is computed on the requirement body before and after the edit\n  Then   the two hashes are equal — the prose edit produced no drift",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-PROSE-024"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3348"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:857"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1004"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8895"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Ștanțarea unui draft din titlurile proprii ale unui fișier de proză",
          "intent": "Un bucket decide doar dacă se auto-redactează; un tag adăugat de mână câștigă mereu, deci un README tagat de mână tot contează ca membru chiar dacă nu este niciodată auto-redactat. Când un fișier capability este redactat, titlul lui și titlurile de secțiune devin scaffold-ul — și deoarece hash-ul de drift se ancorează pe contractul redactat, nu pe proza sursă, editarea fișierului original ulterior nu raportează niciodată fals drift.",
          "contract": "> Un bucket decide doar dacă se auto-redactează; un tag adăugat de mână câștigă mereu,\n> deci un README tagat de mână tot contează ca membru chiar dacă nu este niciodată\n> auto-redactat. Când un fișier capability este redactat, titlul lui și titlurile de\n> secțiune devin scaffold-ul — și deoarece hash-ul de drift se ancorează pe contractul\n> redactat, nu pe proza sursă, editarea fișierului original ulterior nu raportează\n> niciodată fals drift.\n\nFiecare bullet de mai jos este obligatoriu.\n- Bucket-urile guvernează auto-redactarea DOAR; un tag explicit pe orice fișier este mereu\n  respectat de scanner indiferent de bucket (astfel un README tagat de mână tot este un membru).\n- Un draft de proză este ștanțat din titlul fișierului (frontmatter `title:`, altfel primul\n  titlu `#`, altfel `<title>`/`<h1>` din HTML) plus titlurile sale de secțiune `##`,\n  înregistrate ca indiciu de redactare.\n- Când un fișier nu are deloc niciun titlu `##`, titlurile sale ulterioare `#` devin secțiunile\n  în loc, astfel încât proza plată, cu un singur nivel (o bibliotecă de prompt-uri) tot produce\n  un indiciu.\n- Proza sursă nu este niciodată contractul: hash-ul de drift se ancorează pe Contract-ul și\n  Acceptance-ul redactate, astfel încât proza poate deriva ulterior liber de la cerința redactată.",
          "acceptance": "CASE-1 — un tag explicit generated-from înregistrează un fișier sync_only ca membru\n  Given  un `README.md` purtând un comentariu `<!-- generated-from: REQ-DOCS-001 -->`\n  When   rulează `scan_members`\n  Then   fișierul apare ca membru al `REQ-DOCS-001`, deși este sync_only\n\nCASE-2 — _prose_facts preferă titlul frontmatter, apoi colectează titlurile H2\n  Given  o sursă cu frontmatter `title: Senator Aurelius` și două titluri `## `\n  When   rulează `_prose_facts(src)`\n  Then   returnează `(\"Senator Aurelius\", [\"Role\", \"Specialty\"])`\n\nCASE-3 — titlurile H1 ulterioare devin indiciul de secțiune când nu există niciun H2\n  Given  `\"# IDENTITY and PURPOSE\\n\\ntext\\n\\n# STEPS\\n\\n# OUTPUT INSTRUCTIONS\\n\"` (fără niciun `##` nicăieri)\n  When   rulează `_prose_facts(src)`\n  Then   lista de titluri este `[\"STEPS\", \"OUTPUT INSTRUCTIONS\"]`, desprinsă din H1-urile ulterioare\n\nCASE-4 — editarea fișierului de proză sursă nu schimbă binding_hash-ul cerinței\n  Given  un fișier de proză din bucket-ul capability redactat într-o cerință, apoi editat ulterior\n  When   `binding_hash` este calculat pe corpul cerinței înainte și după editare\n  Then   cele două hash-uri sunt egale — editarea de proză nu a produs niciun drift"
        }
      }
    },
    {
      "id": "ARCH-PYFLOOR-040",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Declared Python support floor",
      "intent": "The engine's only promise to a new repo is \"copy one file, run it with Python\" — but which Python was never stated. It happened to work on 3.7 by accident, nothing tested that, and a user on an older interpreter met a stdlib AttributeError from deep inside a command instead of a sentence telling them what to do.",
      "contract": [
        "`MIN_PYTHON` names the oldest interpreter version the engine supports, and `reqmap.py` refuses to run below it with a readable message and exit code 2. [[REQ-PYFLOOR-902]]"
      ],
      "verify": [],
      "notes": [
        "The check cannot catch an interpreter below 3.6: the module uses f-strings, so such an interpreter fails at compile time and never reaches the guard. The covered range is 3.6 through the version below `MIN_PYTHON`.",
        "The message is ASCII only, because a legacy Windows codepage is exactly where an outdated interpreter turns up.",
        "Raising the floor is a breaking change for a consumer repo and moves with the matrix, never ahead of it."
      ],
      "current_impl": [
        "`plugin/scripts/reqmap.py` — `MIN_PYTHON`, `_python_floor_error`, and the guard at the top of `main`.",
        "`.github/workflows/ci.yml` — the `tests` matrix that proves the floor on ubuntu-latest and windows-latest.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  an interpreter older than `MIN_PYTHON` When   any `reqmap.py` command runs Then   it prints the required version, the running version and the fix, and exits 2",
        "CASE-2 — Given  an interpreter at or above `MIN_PYTHON` When   any `reqmap.py` command runs Then   the floor check reports nothing and the command runs normally",
        "CASE-3 — Given  the CI test matrix When   the `tests` job runs Then   its oldest Python entry equals `MIN_PYTHON`, on both Linux and Windows"
      ],
      "accept": "CASE-1\n  Given  an interpreter older than `MIN_PYTHON`\n  When   any `reqmap.py` command runs\n  Then   it prints the required version, the running version and the fix, and exits 2\n\nCASE-2\n  Given  an interpreter at or above `MIN_PYTHON`\n  When   any `reqmap.py` command runs\n  Then   the floor check reports nothing and the command runs normally\n\nCASE-3\n  Given  the CI test matrix\n  When   the `tests` job runs\n  Then   its oldest Python entry equals `MIN_PYTHON`, on both Linux and Windows",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "SYS-SHIP-108"
      ],
      "satisfied_by": [
        "REQ-PYFLOOR-902"
      ],
      "members": [
        {
          "role": "implements",
          "loc": ".github/workflows/ci.yml:3"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:236"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:5840"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9310"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Prag declarat de suport Python",
          "intent": "Singura promisiune a motorului către un repo nou este \"copiază un fișier, rulează-l cu Python\" — dar care Python nu a fost niciodată precizat. S-a întâmplat să funcționeze pe 3.7 din întâmplare, nimic nu testa asta, iar un utilizator cu un interpretor mai vechi întâlnea un AttributeError din stdlib venit din adâncul unei comenzi, în loc de o propoziție care să îi spună ce să facă.",
          "contract": "> Singura promisiune a motorului către un repo nou este \"copiază un fișier, rulează-l\n> cu Python\" — dar care Python nu a fost niciodată precizat. S-a întâmplat să\n> funcționeze pe 3.7 din întâmplare, nimic nu testa asta, iar un utilizator cu un\n> interpretor mai vechi întâlnea un AttributeError din stdlib venit din adâncul unei\n> comenzi, în loc de o propoziție care să îi spună ce să facă.\n\nFiecare punct de mai jos este obligatoriu.\n- `MIN_PYTHON` numește cea mai veche versiune de interpretor pe care motorul o suportă, iar `reqmap.py` refuză să ruleze sub ea cu un mesaj lizibil și codul de ieșire 2. [[REQ-PYFLOOR-902]]",
          "acceptance": "CASE-1\n  Given  un interpretor mai vechi decât `MIN_PYTHON`\n  When   orice comandă `reqmap.py` rulează\n  Then   afișează versiunea necesară, versiunea care rulează și remediul, și iese cu 2\n\nCASE-2\n  Given  un interpretor la sau peste `MIN_PYTHON`\n  When   orice comandă `reqmap.py` rulează\n  Then   verificarea de prag nu raportează nimic iar comanda rulează normal\n\nCASE-3\n  Given  matricea de teste CI\n  When   job-ul `tests` rulează\n  Then   cea mai veche intrare Python a lui este egală cu `MIN_PYTHON`, atât pe Linux cât și pe Windows"
        }
      }
    },
    {
      "id": "REQ-PYFLOOR-902",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Refusing an interpreter below the declared floor",
      "intent": "A floor nothing tests is a claim, not a guarantee: `MIN_PYTHON` is deliberately pinned to the oldest interpreter CI actually runs, not the oldest one the code happens to work on. The refusal fires before any command dispatches, so an old interpreter fails with one readable line naming the fix instead of a stdlib `AttributeError` buried deep in some command.",
      "contract": [
        "`MIN_PYTHON` names the oldest interpreter version the engine supports.",
        "`MIN_PYTHON` equals the oldest version the CI test matrix runs, so the declared floor and the proven floor are the same number.",
        "`reqmap.py` refuses to run on an interpreter below `MIN_PYTHON`, before any command executes.",
        "`reqmap.py` exits 2 on refusal and prints one line naming the required version, the running version, and the fix.",
        "`_python_floor_error` reports the refusal message for a caller-supplied version, so a test pins the floor without spawning an old interpreter."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — MIN_PYTHON names a concrete required version Given  the `reqmap.py` module When   `MIN_PYTHON` is inspected Then   it holds a specific version tuple such as `(3, 9)`, not a placeholder",
        "CASE-2 — — MIN_PYTHON matches the CI matrix's oldest entry Given  `MIN_PYTHON` and `.github/workflows/ci.yml`'s `tests` job matrix When   both are compared Then   the matrix's lowest Python version equals `MIN_PYTHON`",
        "CASE-3 — — an interpreter below the floor is refused before any command runs Given  `reqmap.py` started under an interpreter version below `MIN_PYTHON` When   `main()` runs Then   it refuses and returns before dispatching to any command",
        "CASE-4 — — the refusal exits 2 and names both versions and the fix Given  an interpreter below `MIN_PYTHON` When   `reqmap.py` starts Then   it prints one line naming `MIN_PYTHON`, the running version and the upgrade fix, then exits 2",
        "CASE-5 — — _python_floor_error is testable without an old interpreter Given  a caller-supplied version tuple below `MIN_PYTHON` When   `_python_floor_error(version)` is called directly Then   it returns the same refusal message `reqmap.py` would print, with no interpreter spawned"
      ],
      "accept": "CASE-1 — MIN_PYTHON names a concrete required version\n  Given  the `reqmap.py` module\n  When   `MIN_PYTHON` is inspected\n  Then   it holds a specific version tuple such as `(3, 9)`, not a placeholder\n\nCASE-2 — MIN_PYTHON matches the CI matrix's oldest entry\n  Given  `MIN_PYTHON` and `.github/workflows/ci.yml`'s `tests` job matrix\n  When   both are compared\n  Then   the matrix's lowest Python version equals `MIN_PYTHON`\n\nCASE-3 — an interpreter below the floor is refused before any command runs\n  Given  `reqmap.py` started under an interpreter version below `MIN_PYTHON`\n  When   `main()` runs\n  Then   it refuses and returns before dispatching to any command\n\nCASE-4 — the refusal exits 2 and names both versions and the fix\n  Given  an interpreter below `MIN_PYTHON`\n  When   `reqmap.py` starts\n  Then   it prints one line naming `MIN_PYTHON`, the running version and the upgrade fix, then\n         exits 2\n\nCASE-5 — _python_floor_error is testable without an old interpreter\n  Given  a caller-supplied version tuple below `MIN_PYTHON`\n  When   `_python_floor_error(version)` is called directly\n  Then   it returns the same refusal message `reqmap.py` would print, with no interpreter\n         spawned",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-PYFLOOR-040"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": ".github/workflows/ci.yml:117"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:233"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:236"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:9050"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:5840"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9310"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Refuzul unui interpreter sub pragul declarat",
          "intent": "Un prag pe care nimic nu-l testează este o pretenție, nu o garanție: `MIN_PYTHON` este fixat deliberat la cel mai vechi interpreter pe care CI chiar îl rulează, nu cel mai vechi cu care codul se întâmplă să funcționeze. Refuzul se declanșează înainte ca vreo comandă să se execute, astfel încât un interpreter vechi eșuează cu o singură linie lizibilă care numește fix-ul, în loc de un `AttributeError` de stdlib îngropat adânc într-o comandă oarecare.",
          "contract": "> Un prag pe care nimic nu-l testează este o pretenție, nu o garanție: `MIN_PYTHON`\n> este fixat deliberat la cel mai vechi interpreter pe care CI chiar îl rulează, nu cel\n> mai vechi cu care codul se întâmplă să funcționeze. Refuzul se declanșează înainte ca\n> vreo comandă să se execute, astfel încât un interpreter vechi eșuează cu o singură\n> linie lizibilă care numește fix-ul, în loc de un `AttributeError` de stdlib îngropat\n> adânc într-o comandă oarecare.\n\nFiecare bullet de mai jos este obligatoriu.\n- `MIN_PYTHON` numește cel mai vechi interpreter pe care engine-ul îl suportă.\n- `MIN_PYTHON` este egal cu cea mai veche versiune pe care matricea de test CI o rulează, astfel\n  încât pragul declarat și pragul dovedit sunt același număr.\n- `reqmap.py` refuză să ruleze pe un interpreter sub `MIN_PYTHON`, înainte ca vreo comandă\n  să se execute.\n- `reqmap.py` iese cu 2 la refuz și afișează o linie care numește versiunea cerută, versiunea\n  curentă, și fix-ul.\n- `_python_floor_error` raportează mesajul de refuz pentru o versiune dată de apelant, astfel\n  încât un test fixează pragul fără să lanseze un interpreter vechi.",
          "acceptance": "CASE-1 — MIN_PYTHON numește o versiune concretă cerută\n  Given  modulul `reqmap.py`\n  When   este inspectat `MIN_PYTHON`\n  Then   conține un tuple de versiune concret precum `(3, 9)`, nu un placeholder\n\nCASE-2 — MIN_PYTHON se potrivește cu cea mai veche intrare din matricea CI\n  Given  `MIN_PYTHON` și matricea job-ului `tests` din `.github/workflows/ci.yml`\n  When   ambele sunt comparate\n  Then   cea mai joasă versiune Python din matrice este egală cu `MIN_PYTHON`\n\nCASE-3 — un interpreter sub prag este refuzat înainte ca vreo comandă să ruleze\n  Given  `reqmap.py` pornit sub o versiune de interpreter sub `MIN_PYTHON`\n  When   rulează `main()`\n  Then   refuză și se întoarce înainte de a trimite spre orice comandă\n\nCASE-4 — refuzul iese cu 2 și numește ambele versiuni și fix-ul\n  Given  un interpreter sub `MIN_PYTHON`\n  When   pornește `reqmap.py`\n  Then   afișează o linie care numește `MIN_PYTHON`, versiunea curentă și fix-ul de upgrade, apoi\n         iese cu 2\n\nCASE-5 — _python_floor_error este testabil fără un interpreter vechi\n  Given  un tuple de versiune dat de apelant sub `MIN_PYTHON`\n  When   `_python_floor_error(version)` este apelat direct\n  Then   returnează același mesaj de refuz pe care `reqmap.py` l-ar afișa, fără niciun interpreter\n         lansat"
        }
      }
    },
    {
      "id": "ARCH-REDUNDANCY-058",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Requirements that say the same thing",
      "intent": "Decomposing several architecture requirements can mint the same obligation twice, and nothing in the engine noticed — a corpus that only ever grows covers the same code with more requirements each time. `_redundant_groups` reports requirements whose Description clauses are byte-identical once case and whitespace are normalised, so the corpus can be folded back down instead of growing without bound.",
      "contract": [
        "Two or more requirements whose Description clauses are identical once case and whitespace are normalised form one duplicate group.",
        "Every scaffolded draft carries the same `TODO:` placeholder text, so draft-status requirements are excluded, or every draft would report as a duplicate of every other.",
        "`next` reports each group once, naming its ids and how many could be folded away.",
        "`gate` says nothing about redundancy — it runs on every commit, and corpus shape is not a commit-time concern.",
        "The check is read-only: it never writes a file and never merges anything itself."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — identical Description clauses group together Given  two requirements whose Description clause reads exactly \"`x` does the thing.\" and a third with a different clause When   `_redundant_groups` runs Then   it returns one group holding only the two matching ids",
        "CASE-2 — — case and whitespace differences do not hide a duplicate Given  one requirement's clause reading \"`x` does   the thing.\" and another reading \"`X` DOES the thing.\" When   `_redundant_groups` runs Then   the two are still grouped as one duplicate",
        "CASE-3 — — a genuinely different clause is not flagged Given  two requirements whose clauses differ by more than case or whitespace When   `_redundant_groups` runs Then   it returns no group for them",
        "CASE-4 — — draft placeholders sharing the same TODO text are not duplicates of each other Given  two `status: draft` requirements both carrying the identical scaffolded `TODO: the observed behavior.` clause When   `_redundant_groups` runs Then   it returns no group for them",
        "CASE-5 — — next reports each group once and writes nothing; gate stays silent Given  two confirmed requirements sharing one identical Description clause When   `next` runs, and separately `gate` runs on the same corpus Then   `next`'s output names both ids under one \"Redundancy\" finding and the directory's file listing is unchanged afterward; `gate`'s output says nothing about redundancy"
      ],
      "accept": "CASE-1 — identical Description clauses group together\n  Given  two requirements whose Description clause reads exactly \"`x` does the thing.\" and a\n         third with a different clause\n  When   `_redundant_groups` runs\n  Then   it returns one group holding only the two matching ids\n\nCASE-2 — case and whitespace differences do not hide a duplicate\n  Given  one requirement's clause reading \"`x` does   the thing.\" and another reading\n         \"`X` DOES the thing.\"\n  When   `_redundant_groups` runs\n  Then   the two are still grouped as one duplicate\n\nCASE-3 — a genuinely different clause is not flagged\n  Given  two requirements whose clauses differ by more than case or whitespace\n  When   `_redundant_groups` runs\n  Then   it returns no group for them\n\nCASE-4 — draft placeholders sharing the same TODO text are not duplicates of each other\n  Given  two `status: draft` requirements both carrying the identical scaffolded\n         `TODO: the observed behavior.` clause\n  When   `_redundant_groups` runs\n  Then   it returns no group for them\n\nCASE-5 — next reports each group once and writes nothing; gate stays silent\n  Given  two confirmed requirements sharing one identical Description clause\n  When   `next` runs, and separately `gate` runs on the same corpus\n  Then   `next`'s output names both ids under one \"Redundancy\" finding and the directory's\n         file listing is unchanged afterward; `gate`'s output says nothing about redundancy",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-NEXT-013",
        "ARCH-SIMILAR-016"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-QUALITY-104"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5191"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:9308"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7712"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "should-have",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Cerințe care spun același lucru",
          "intent": "Descompunerea mai multor cerințe de arhitectură poate crea aceeași obligație de două ori, iar nimic în motor nu a observat asta — un corpus care doar crește acoperă același cod cu tot mai multe cerințe de fiecare dată. `_redundant_groups` raportează cerințele ale căror clauze Description sunt identice octet-cu-octet odată ce litera mare/mică și spațiile albe sunt normalizate, astfel încât corpusul poate fi restrâns în loc să crească fără limită.",
          "contract": "> Descompunerea mai multor cerințe de arhitectură poate crea aceeași obligație de două\n> ori, iar nimic în motor nu a observat asta — un corpus care doar crește acoperă\n> același cod cu tot mai multe cerințe de fiecare dată. `_redundant_groups` raportează\n> cerințele ale căror clauze Description sunt identice octet-cu-octet odată ce litera\n> mare/mică și spațiile albe sunt normalizate, astfel încât corpusul poate fi restrâns\n> în loc să crească fără limită.\n\nFiecare punct de mai jos este obligatoriu.\n- Două sau mai multe cerințe ale căror clauze Description sunt identice odată ce litera mare/mică și spațiile albe sunt normalizate formează un singur grup duplicat.\n- Fiecare draft schelărit poartă același text placeholder `TODO:`, astfel încât cerințele cu status draft sunt excluse, altfel fiecare draft ar raporta ca duplicat al oricărui alt draft.\n- `next` raportează fiecare grup o singură dată, numind id-urile sale și câte ar putea fi restrânse.\n- `gate` nu spune nimic despre redundanță — rulează la fiecare commit, iar forma corpusului nu este o preocupare la momentul commit-ului.\n- Verificarea este doar-citire: nu scrie niciodată un fișier și nu fuzionează niciodată nimic ea însăși.",
          "acceptance": "CASE-1 — clauzele Description identice se grupează împreună\n  Given  două cerințe a căror clauză Description citește exact \"`x` does the thing.\" și o\n         a treia cu o clauză diferită\n  When   `_redundant_groups` rulează\n  Then   returnează un singur grup care conține doar cele două id-uri care se potrivesc\n\nCASE-2 — diferențele de literă mare/mică și spații albe nu ascund un duplicat\n  Given  clauza unei cerințe care citește \"`x` does   the thing.\" și alta care citește\n         \"`X` DOES the thing.\"\n  When   `_redundant_groups` rulează\n  Then   cele două sunt totuși grupate ca un singur duplicat\n\nCASE-3 — o clauză cu adevărat diferită nu este marcată\n  Given  două cerințe ale căror clauze diferă prin mai mult decât litera mare/mică sau spațiile albe\n  When   `_redundant_groups` rulează\n  Then   nu returnează niciun grup pentru ele\n\nCASE-4 — placeholder-ele de draft care partajează același text TODO nu sunt duplicate unul al celuilalt\n  Given  două cerințe `status: draft` care poartă amândouă clauza schelărită identică\n         `TODO: the observed behavior.`\n  When   `_redundant_groups` rulează\n  Then   nu returnează niciun grup pentru ele\n\nCASE-5 — next raportează fiecare grup o dată și nu scrie nimic; gate rămâne tăcut\n  Given  două cerințe confirmate care partajează o clauză Description identică\n  When   `next` rulează, și separat `gate` rulează pe același corpus\n  Then   ieșirea lui `next` numește ambele id-uri sub o singură constatare \"Redundancy\" iar\n         listarea de fișiere a directorului rămâne neschimbată după aceea; ieșirea lui `gate` nu spune nimic despre redundanță"
        }
      }
    },
    {
      "id": "ARCH-REGISTRYLAG-035",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Registry-lag signal — commits since the requirements dir was last touched",
      "intent": "`health` reports whether the requirements that exist are coherent, but not whether the registry as a whole has gone stale while code moved on. A downstream consumer's registry sat frozen for 18 days across ~40 code commits while a money value drifted with no requirement update — nothing in `health` surfaced that the spec had stopped tracking reality. This adds a read-only count of how many commits have landed since the requirements dir was last touched, so a reviewer sees at a glance that code is racing ahead of the spec. It is the temporal complement to the untagged-code coverage signal ([[ARCH-COVERAGE-029]]): coverage answers \"is this code traced?\", lag answers \"has the registry moved lately at all?\". Per the Senate audit that governs the coverage signal (2026-06-21), advisory visibility only — never a hard gate.",
      "contract": [
        "Registry lag is the number of commits on `HEAD` since the most recent commit that touched `reqs_dir`, computed from git alone, never from requirement contents. [[REQ-REGISTRYLAG-903]] details the behaviour.",
        "`health --json` reports the count as a `commits_since_req_touch` integer key — present only when measurable, and never affecting the score or exit code. [[REQ-REGISTRYLAG-904]] details the behaviour."
      ],
      "verify": [],
      "notes": [
        "It measures RECENCY of any touch, not QUALITY of the update: a whitespace edit to one requirement resets the lag to 0. Accepted — like coverage, this is an advisory nudge, not proof the spec is current.",
        "Granularity is whole-repo, not per requirement; it answers whether the registry as a body has moved, not which requirement is stale.",
        "A shallow clone or a repo whose first commit already contained `reqs_dir` still reports correctly; only a genuinely absent git history or an untracked `reqs_dir` yields the absent (None) reading."
      ],
      "current_impl": [
        "`_commits_since_reqs_touch(code_root, reqs_dir)` in `reqmap.py`, wired into `cmd_health` alongside the untagged block; surfaced read-only in `health` text output and `--json`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a git repo whose requirements dir was committed, then two later commits touched only code When   `health --json` runs with that code root Then   `commits_since_req_touch` equals 2 and the score is unchanged by it",
        "CASE-2 — Given  a git repo whose most recent commit touched the requirements dir When   `health --json` runs with that code root Then   `commits_since_req_touch` is present and equals 0",
        "CASE-3 — Given  a code root that is not a git worktree When   `health --json` runs with that code root Then   the output carries no `commits_since_req_touch` key"
      ],
      "accept": "CASE-1\n  Given  a git repo whose requirements dir was committed, then two later commits touched only code\n  When   `health --json` runs with that code root\n  Then   `commits_since_req_touch` equals 2 and the score is unchanged by it\n\nCASE-2\n  Given  a git repo whose most recent commit touched the requirements dir\n  When   `health --json` runs with that code root\n  Then   `commits_since_req_touch` is present and equals 0\n\nCASE-3\n  Given  a code root that is not a git worktree\n  When   `health --json` runs with that code root\n  Then   the output carries no `commits_since_req_touch` key",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-HEALTH-017"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-REPORT-105"
      ],
      "satisfied_by": [
        "REQ-REGISTRYLAG-903",
        "REQ-REGISTRYLAG-904"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5747"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5901"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3600"
        }
      ],
      "test_exempt": null,
      "milestone": "v2.12",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Semnal de întârziere a registrului — commit-uri de la ultima atingere a directorului de cerințe",
          "intent": "`health` raportează dacă cerințele care există sunt coerente, dar nu dacă registrul în ansamblu a devenit învechit în timp ce codul a avansat. Registrul unui consumator din aval a stat înghețat 18 zile de-a lungul a ~40 de commit-uri de cod, în timp ce o valoare monetară a suferit drift fără nicio actualizare de cerință — nimic în `health` nu a scos la suprafață faptul că specificația încetase să urmărească realitatea. Aceasta adaugă o numărătoare doar-citire a câte commit-uri au ajuns de la ultima atingere a directorului de cerințe, astfel încât un revizor vede dintr-o privire că codul o ia înaintea specificației. Este complementul temporal al semnalului de acoperire pentru codul neetichetat ([[ARCH-COVERAGE-029]]): acoperirea răspunde la \"este acest cod trasat?\", întârzierea răspunde la \"s-a mișcat registrul deloc în ultima vreme?\". Conform auditului Senate care guvernează semnalul de acoperire (2026-06-21), doar vizibilitate consultativă — niciodată un gate ferm.",
          "contract": "> `health` raportează dacă cerințele care există sunt coerente, dar nu dacă registrul\n> în ansamblu a devenit învechit în timp ce codul a avansat. Registrul unui\n> consumator din aval a stat înghețat 18 zile de-a lungul a ~40 de commit-uri de\n> cod, în timp ce o valoare monetară a suferit drift fără nicio actualizare de\n> cerință — nimic în `health` nu a scos la suprafață faptul că specificația\n> încetase să urmărească realitatea. Aceasta adaugă o numărătoare doar-citire a câte\n> commit-uri au ajuns de la ultima atingere a directorului de cerințe, astfel încât\n> un revizor vede dintr-o privire că codul o ia înaintea specificației. Este\n> complementul temporal al semnalului de acoperire pentru codul neetichetat\n> ([[ARCH-COVERAGE-029]]): acoperirea răspunde la \"este acest cod trasat?\", întârzierea\n> răspunde la \"s-a mișcat registrul deloc în ultima vreme?\". Conform auditului Senate\n> care guvernează semnalul de acoperire (2026-06-21), doar vizibilitate consultativă\n> — niciodată un gate ferm.\n\nFiecare punct de mai jos este obligatoriu.\n- Întârzierea registrului este numărul de commit-uri de pe `HEAD` de la cel mai recent commit care a atins `reqs_dir`, calculat doar din git, niciodată din conținutul cerințelor. [[REQ-REGISTRYLAG-903]] detaliază comportamentul.\n- `health --json` raportează numărătoarea ca o cheie întreagă `commits_since_req_touch` — prezentă doar când este măsurabilă, și nu afectează niciodată scorul sau codul de ieșire. [[REQ-REGISTRYLAG-904]] detaliază comportamentul.",
          "acceptance": "CASE-1\n  Given  un repo git al cărui director de cerințe a fost commis, apoi două commit-uri ulterioare au atins doar cod\n  When   `health --json` rulează cu acea rădăcină de cod\n  Then   `commits_since_req_touch` este egal cu 2 iar scorul rămâne neschimbat de aceasta\n\nCASE-2\n  Given  un repo git al cărui commit cel mai recent a atins directorul de cerințe\n  When   `health --json` rulează cu acea rădăcină de cod\n  Then   `commits_since_req_touch` este prezent și egal cu 0\n\nCASE-3\n  Given  o rădăcină de cod care nu este un git worktree\n  When   `health --json` rulează cu acea rădăcină de cod\n  Then   ieșirea nu poartă nicio cheie `commits_since_req_touch`"
        }
      }
    },
    {
      "id": "REQ-REGISTRYLAG-903",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Counting commits since the registry last moved",
      "intent": "A registry can sit frozen while code races ahead of it, with nothing in `health` noticing. Registry lag asks git directly — the last commit that touched `reqs_dir`, then how many commits have landed on `HEAD` since — so it never needs to parse a requirement file, and a malformed one can never break the count.",
      "contract": [
        "Registry lag is the number of commits on `HEAD` since the most recent commit that touched `reqs_dir`.",
        "The count comes from git alone: the last commit touching `reqs_dir` (`git log -1 -- <reqs_dir>`), then the commit count from there to `HEAD` (`git rev-list --count`).",
        "The capability never parses requirement contents."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — lag counts commits landed after the requirements dir's last touch Given  a commit that touches `requirements/`, then two commits that touch only code When   `health --json` runs with that repo as the code root Then   `commits_since_req_touch` equals 2",
        "CASE-2 — — lag counts from the most recent touch, not the first Given  `requirements/` touched by commit A, then two code commits, then touched again by commit B, then one more code commit When   `_commits_since_reqs_touch` runs Then   it returns 1 (commits since B), not 3 (commits since A)",
        "CASE-3 — — a malformed requirement file does not break the count Given  `requirements/` holds a file with unparseable YAML frontmatter, committed, then two code commits When   `_commits_since_reqs_touch` runs Then   it returns 2 without raising, because it only asks git about the path, never opens the file --------------------"
      ],
      "accept": "CASE-1 — lag counts commits landed after the requirements dir's last touch\n  Given  a commit that touches `requirements/`, then two commits that touch only code\n  When   `health --json` runs with that repo as the code root\n  Then   `commits_since_req_touch` equals 2\n\nCASE-2 — lag counts from the most recent touch, not the first\n  Given  `requirements/` touched by commit A, then two code commits, then touched again by commit B, then one more code commit\n  When   `_commits_since_reqs_touch` runs\n  Then   it returns 1 (commits since B), not 3 (commits since A)\n\nCASE-3 — a malformed requirement file does not break the count\n  Given  `requirements/` holds a file with unparseable YAML frontmatter, committed, then two code commits\n  When   `_commits_since_reqs_touch` runs\n  Then   it returns 2 without raising, because it only asks git about the path, never opens the file\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-REGISTRYLAG-035"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5747"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3612"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9824"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Numărarea commit-urilor de când registry-ul s-a mișcat ultima oară",
          "intent": "Un registry poate sta înghețat în timp ce codul avansează rapid, fără ca nimic din `health` să observe. Registry lag întreabă direct git — ultimul commit care a atins `reqs_dir`, apoi câte commit-uri au aterizat pe `HEAD` de atunci — astfel încât nu are niciodată nevoie să parseze un fișier de cerință, iar unul malformat nu poate niciodată să strice numărătoarea.",
          "contract": "> Un registry poate sta înghețat în timp ce codul avansează rapid, fără ca nimic din\n> `health` să observe. Registry lag întreabă direct git — ultimul commit care a atins\n> `reqs_dir`, apoi câte commit-uri au aterizat pe `HEAD` de atunci — astfel încât nu are\n> niciodată nevoie să parseze un fișier de cerință, iar unul malformat nu poate niciodată\n> să strice numărătoarea.\n\nFiecare bullet de mai jos este obligatoriu.\n- Registry lag este numărul de commit-uri pe `HEAD` de la cel mai recent commit care a atins\n  `reqs_dir`.\n- Numărătoarea vine doar din git: ultimul commit care a atins `reqs_dir`\n  (`git log -1 -- <reqs_dir>`), apoi numărul de commit-uri de acolo până la `HEAD`\n  (`git rev-list --count`).\n- Capabilitatea nu parsează niciodată conținutul cerințelor.",
          "acceptance": "CASE-1 — lag numără commit-urile aterizate după ultima atingere a directorului de cerințe\n  Given  un commit care atinge `requirements/`, apoi două commit-uri care ating doar cod\n  When   rulează `health --json` cu acel repo ca code root\n  Then   `commits_since_req_touch` este egal cu 2\n\nCASE-2 — lag numără de la cea mai recentă atingere, nu de la prima\n  Given  `requirements/` atins de commit-ul A, apoi două commit-uri de cod, apoi atins din nou de\n         commit-ul B, apoi încă un commit de cod\n  When   rulează `_commits_since_reqs_touch`\n  Then   returnează 1 (commit-uri de la B), nu 3 (commit-uri de la A)\n\nCASE-3 — un fișier de cerință malformat nu strică numărătoarea\n  Given  `requirements/` conține un fișier cu frontmatter YAML neparsabil, committed, apoi două commit-uri de cod\n  When   rulează `_commits_since_reqs_touch`\n  Then   returnează 2 fără să ridice o excepție, deoarece doar întreabă git despre cale, niciodată nu deschide fișierul\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-REGISTRYLAG-904",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Reporting lag without ever gating on it",
      "intent": "Like the untagged-code signal, registry lag is visibility only — a repo-wide temporal fact, not a per-requirement axis, so it never touches the score or exit code. Absence, not zero, means the value could not be measured (no git worktree, no code root, no commit history for `reqs_dir`), so a missing reading is never mistaken for a healthy, just-touched registry.",
      "contract": [
        "`health --json` includes the count as a `commits_since_req_touch` integer key.",
        "Text output carries a labelled line only when the count is above zero. A lag of zero is the healthy case and needs no line.",
        "The signal is read-only and never a gate. It changes no exit code.",
        "The signal never lowers the health score, because it is a repo-wide temporal fact rather than a per-requirement axis.",
        "The `commits_since_req_touch` key is absent, not zero, whenever the value is unmeasurable.",
        "Unmeasurable means no code root was supplied, `code_root` is not a git worktree, git is unavailable, or `reqs_dir` has no commit in history.",
        "Absence rather than zero preserves the `--json` schema, so a missing reading is never mistaken for a fresh registry."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — --json exposes the count as an integer key Given  a repo with two commits landed after the last commit touching `requirements/` When   `health --json` runs Then   the parsed JSON object's `commits_since_req_touch` key equals the integer `2`",
        "CASE-2 — — the lag line appears only for a nonzero count Given  one repo where the last commit touched `requirements/` (lag 0) and another with two later code commits (lag 2) When   `health` runs in text mode on each Then   the \"commits since requirements touched\" line is absent for the first and present for the second",
        "CASE-3 — — a nonzero lag leaves the health score unchanged Given  one green requirement and two commits landed after `requirements/` was last touched (`commits_since_req_touch` = 2) When   `health --json` runs Then   `score` still reads 100 and the exit code stays unchanged, unaffected by the nonzero lag",
        "CASE-4 — — an unmeasurable lag omits the key entirely Given  a code root that is not a git worktree When   `health --json` runs with that code root Then   the parsed JSON object has no `commits_since_req_touch` key, preserving the `--json` schema so a missing reading is never mistaken for a fresh registry",
        "CASE-5 — — a git worktree whose reqs_dir has no commits reads as unmeasurable Given  a git repo with commits, where `requirements/` was never committed When   `_commits_since_reqs_touch` runs against that repo Then   it returns `None`, not `0` or an exception"
      ],
      "accept": "CASE-1 — --json exposes the count as an integer key\n  Given  a repo with two commits landed after the last commit touching `requirements/`\n  When   `health --json` runs\n  Then   the parsed JSON object's `commits_since_req_touch` key equals the integer `2`\n\nCASE-2 — the lag line appears only for a nonzero count\n  Given  one repo where the last commit touched `requirements/` (lag 0) and another with two later code commits (lag 2)\n  When   `health` runs in text mode on each\n  Then   the \"commits since requirements touched\" line is absent for the first and present for the second\n\nCASE-3 — a nonzero lag leaves the health score unchanged\n  Given  one green requirement and two commits landed after `requirements/` was last touched (`commits_since_req_touch` = 2)\n  When   `health --json` runs\n  Then   `score` still reads 100 and the exit code stays unchanged, unaffected by the nonzero lag\n\nCASE-4 — an unmeasurable lag omits the key entirely\n  Given  a code root that is not a git worktree\n  When   `health --json` runs with that code root\n  Then   the parsed JSON object has no `commits_since_req_touch` key, preserving the `--json` schema so a missing reading is never mistaken for a fresh registry\n\nCASE-5 — a git worktree whose reqs_dir has no commits reads as unmeasurable\n  Given  a git repo with commits, where `requirements/` was never committed\n  When   `_commits_since_reqs_touch` runs against that repo\n  Then   it returns `None`, not `0` or an exception",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-REGISTRYLAG-035"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5904"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3612"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9824"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Raportarea lag-ului fără să facă niciodată gate pe el",
          "intent": "La fel ca semnalul de cod netagat, registry lag este doar vizibilitate — un fapt temporal la nivel de repo, nu o axă per-cerință, deci nu atinge niciodată scorul sau codul de ieșire. Absența, nu zero, înseamnă că valoarea nu a putut fi măsurată (niciun git worktree, niciun code root, niciun istoric de commit pentru `reqs_dir`), astfel încât o citire lipsă nu este niciodată confundată cu un registry sănătos, tocmai atins.",
          "contract": "> La fel ca semnalul de cod netagat, registry lag este doar vizibilitate — un fapt\n> temporal la nivel de repo, nu o axă per-cerință, deci nu atinge niciodată scorul sau\n> codul de ieșire. Absența, nu zero, înseamnă că valoarea nu a putut fi măsurată (niciun\n> git worktree, niciun code root, niciun istoric de commit pentru `reqs_dir`), astfel\n> încât o citire lipsă nu este niciodată confundată cu un registry sănătos, tocmai atins.\n\nFiecare bullet de mai jos este obligatoriu.\n- `health --json` include numărătoarea ca o cheie întreagă `commits_since_req_touch`.\n- Ieșirea text poartă o linie etichetată doar când numărătoarea este peste zero. Un lag de zero\n  este cazul sănătos și nu are nevoie de nicio linie.\n- Semnalul este read-only și nu este niciodată un gate. Nu schimbă niciun cod de ieșire.\n- Semnalul nu scade niciodată scorul de health, deoarece este un fapt temporal la nivel de repo\n  mai degrabă decât o axă per-cerință.\n- Cheia `commits_since_req_touch` este absentă, nu zero, ori de câte ori valoarea este\n  nemăsurabilă.\n- Nemăsurabil înseamnă că niciun code root nu a fost furnizat, `code_root` nu este un git\n  worktree, git este indisponibil, sau `reqs_dir` nu are niciun commit în istoric.\n- Absența în loc de zero păstrează schema `--json`, astfel încât o citire lipsă nu este niciodată\n  confundată cu un registry proaspăt.",
          "acceptance": "CASE-1 — --json expune numărătoarea ca o cheie întreagă\n  Given  un repo cu două commit-uri aterizate după ultimul commit care a atins `requirements/`\n  When   rulează `health --json`\n  Then   cheia `commits_since_req_touch` a obiectului JSON parsat este egală cu întregul `2`\n\nCASE-2 — linia de lag apare doar pentru o numărătoare diferită de zero\n  Given  un repo unde ultimul commit a atins `requirements/` (lag 0) și altul cu două commit-uri de cod ulterioare (lag 2)\n  When   rulează `health` în mod text pe fiecare\n  Then   linia \"commits since requirements touched\" este absentă pentru primul și prezentă pentru al doilea\n\nCASE-3 — un lag diferit de zero lasă scorul de health neschimbat\n  Given  o cerință verde și două commit-uri aterizate după ce `requirements/` a fost atins ultima oară (`commits_since_req_touch` = 2)\n  When   rulează `health --json`\n  Then   `score` tot se citește 100 iar codul de ieșire rămâne neschimbat, neafectat de lag-ul diferit de zero\n\nCASE-4 — un lag nemăsurabil omite cheia complet\n  Given  un code root care nu este un git worktree\n  When   rulează `health --json` cu acel code root\n  Then   obiectul JSON parsat nu are nicio cheie `commits_since_req_touch`, păstrând schema `--json` astfel încât o citire lipsă nu este niciodată confundată cu un registry proaspăt\n\nCASE-5 — un git worktree al cărui reqs_dir nu are commit-uri se citește ca nemăsurabil\n  Given  un repo git cu commit-uri, unde `requirements/` nu a fost niciodată committed\n  When   rulează `_commits_since_reqs_touch` pe acel repo\n  Then   returnează `None`, nu `0` sau o excepție"
        }
      }
    },
    {
      "id": "ARCH-REPRO-041",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Committed build artifacts stay re-derivable",
      "intent": "Two files here are build output that is committed anyway, because the whole promise is that a consumer gets them without installing a toolchain. Nothing checked either one against the source it came from, so an edit to the viewer app or to a diagram generator could ship an artifact that no longer matched its own code, with the repo still green.",
      "contract": [
        "`plugin/scripts/_map_viewer.html` derives from `app/`, built by `npm run build:viewer`; the `artifacts` CI job rebuilds every covered artifact and fails the build when a rebuild differs from what is committed. [[REQ-REPRO-905]]"
      ],
      "verify": [],
      "notes": [
        "`docs/architecture.html` stays out of scope: a human authored it, and `map --check` already covers the regions the engine owns inside it.",
        "Byte-for-byte comparison requires the artifacts' line endings to be a property of the repository, not of the machine that built them: `.gitattributes` pins `app/viewer.html`, the vendored viewer and the published diagram to LF. Without that the Windows and Linux builds of the same source disagree, and `core.autocrlf` hides the disagreement locally.",
        "The check assumes both builds stay byte-reproducible. A Node major bump or a Vite upgrade can break that, and the job reports it as a stale artifact — re-vendor the viewer in the same commit that moves the toolchain.",
        "The diagram half compares against a temp directory rather than regenerating in place, because the generator writes a sibling `.excalidraw` and `.gitignore` blocks `docs/*.excalidraw`."
      ],
      "current_impl": [
        "`.github/workflows/ci.yml` — the `artifacts` job, and `release`'s dependency on it.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a commit that changes `app/` without re-committing the built viewer When   the `artifacts` job runs Then   it fails and names `plugin/scripts/_map_viewer.html` and the rebuild command",
        "CASE-2 — Given  a commit that changes the architecture generator without re-committing its output When   the `artifacts` job runs Then   it fails and names `docs/full_architecture.html` and the rebuild command",
        "CASE-3 — Given  a tree whose committed artifacts match their sources When   the `artifacts` job runs Then   it passes and `release` becomes eligible to run"
      ],
      "accept": "CASE-1\n  Given  a commit that changes `app/` without re-committing the built viewer\n  When   the `artifacts` job runs\n  Then   it fails and names `plugin/scripts/_map_viewer.html` and the rebuild command\n\nCASE-2\n  Given  a commit that changes the architecture generator without re-committing its output\n  When   the `artifacts` job runs\n  Then   it fails and names `docs/full_architecture.html` and the rebuild command\n\nCASE-3\n  Given  a tree whose committed artifacts match their sources\n  When   the `artifacts` job runs\n  Then   it passes and `release` becomes eligible to run",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-SELFGATE-039"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-SHIP-108"
      ],
      "satisfied_by": [
        "REQ-REPRO-905"
      ],
      "members": [
        {
          "role": "implements",
          "loc": ".github/workflows/ci.yml:4"
        }
      ],
      "test_exempt": "pipeline wiring (a CI job that rebuilds and compares) — the behavior IS the comparison, observed by CI running it; a unit test would only re-assert that a byte compare compares",
      "milestone": null,
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Artefactele de build commise rămân re-derivabile",
          "intent": "Două fișiere de aici sunt ieșiri de build care sunt totuși commise, deoarece întreaga promisiune este că un consumator le primește fără să instaleze un toolchain. Nimic nu verifica vreunul dintre ele față de sursa din care provenea, astfel încât o editare a aplicației viewer sau a unui generator de diagrame putea livra un artefact care nu mai corespundea propriului cod, iar repo-ul rămânea totuși verde.",
          "contract": "> Două fișiere de aici sunt ieșiri de build care sunt totuși commise, deoarece\n> întreaga promisiune este că un consumator le primește fără să instaleze un\n> toolchain. Nimic nu verifica vreunul dintre ele față de sursa din care provenea,\n> astfel încât o editare a aplicației viewer sau a unui generator de diagrame putea\n> livra un artefact care nu mai corespundea propriului cod, iar repo-ul rămânea\n> totuși verde.\n\nFiecare punct de mai jos este obligatoriu.\n- `plugin/scripts/_map_viewer.html` derivă din `app/`, construit prin `npm run build:viewer`; job-ul CI `artifacts` reconstruiește fiecare artefact acoperit și face build-ul să eșueze atunci când o reconstrucție diferă de ce este commis. [[REQ-REPRO-905]]",
          "acceptance": "CASE-1\n  Given  un commit care schimbă `app/` fără a re-commite viewer-ul construit\n  When   job-ul `artifacts` rulează\n  Then   eșuează și numește `plugin/scripts/_map_viewer.html` și comanda de reconstrucție\n\nCASE-2\n  Given  un commit care schimbă generatorul de arhitectură fără a re-commite ieșirea lui\n  When   job-ul `artifacts` rulează\n  Then   eșuează și numește `docs/full_architecture.html` și comanda de reconstrucție\n\nCASE-3\n  Given  un tree ale cărui artefacte commise se potrivesc cu sursele lor\n  When   job-ul `artifacts` rulează\n  Then   trece iar `release` devine eligibil să ruleze"
        }
      }
    },
    {
      "id": "REQ-REPRO-905",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Rebuilding and diffing each committed artifact in CI",
      "intent": "A committed build artifact is a promise that a consumer never has to run the toolchain that produced it — but that promise silently rots the moment source changes without a rebuild. Rebuilding each artifact fresh on every push and diffing it against the committed copy turns \"probably still matches\" into a hard CI failure, at the cost of running a Node and a Python build on every push.",
      "contract": [
        "`plugin/scripts/_map_viewer.html` derives from `app/`, built by `npm run build:viewer`.",
        "`docs/full_architecture.html` derives from `plugin/skills/excalidraw-diagram/examples/make_full_architecture.py`.",
        "The `artifacts` CI job rebuilds each covered artifact from its source on every push and pull request.",
        "The job fails the build when a rebuilt artifact differs from the committed one.",
        "The failure message names the stale file and the command that regenerates it.",
        "The `release` job runs only after `artifacts` passes, so a stale artifact never reaches a published tag."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a stale vendored viewer fails the job and names the fix Given  a commit that changes `app/` without re-committing `plugin/scripts/_map_viewer.html` When   the \"Vendored viewer matches app/\" step runs Then   `git diff --exit-code` on that file is non-zero, the step fails, and the error names the file and `npm run build:viewer`",
        "CASE-2 — — the published architecture diagram matches its generator Given  `make_full_architecture.py` regenerated into a temporary directory When   its output is compared byte-for-byte against the committed `docs/full_architecture.html` Then   the two files are identical",
        "CASE-3 — — the SSR smoke runs against this repo's real registry, not a stand-in Given  a fresh checkout whose `app/public/data.json` is gitignored and absent When   the \"Viewer SSR smoke\" step runs Then   `npm run sync` first builds that fixture from the committed `plugin/requirements/_map.json` before `npm run smoke` runs against it",
        "CASE-4 — — the vendored-viewer check is the rebuild's own diff, not a second comparison Given  the \"Rebuild the vendored viewer\" step has just overwritten `plugin/scripts/_map_viewer.html` in place When   the next step runs Then   it inspects the working-tree diff on that one file — the rebuild writing over the committed copy IS the check, with no separate comparison logic",
        "CASE-5 — — the diagram check builds into a temp directory, never in place Given  `.gitignore` hard-blocks `docs/*.excalidraw`, a sibling file the generator writes When   the \"Published architecture diagram matches its generator\" step runs Then   `make_full_architecture.py` writes into a `mktemp -d` directory, and only that output is compared against the committed `docs/full_architecture.html`",
        "CASE-6 — — the release job depends on the artifacts job Given  `.github/workflows/ci.yml` When   the `release` job's `needs:` list is inspected Then   it names `artifacts` alongside `gate-and-tests` and `tests`"
      ],
      "accept": "CASE-1 — a stale vendored viewer fails the job and names the fix\n  Given  a commit that changes `app/` without re-committing `plugin/scripts/_map_viewer.html`\n  When   the \"Vendored viewer matches app/\" step runs\n  Then   `git diff --exit-code` on that file is non-zero, the step fails, and the error\n         names the file and `npm run build:viewer`\n\nCASE-2 — the published architecture diagram matches its generator\n  Given  `make_full_architecture.py` regenerated into a temporary directory\n  When   its output is compared byte-for-byte against the committed\n         `docs/full_architecture.html`\n  Then   the two files are identical\n\nCASE-3 — the SSR smoke runs against this repo's real registry, not a stand-in\n  Given  a fresh checkout whose `app/public/data.json` is gitignored and absent\n  When   the \"Viewer SSR smoke\" step runs\n  Then   `npm run sync` first builds that fixture from the committed\n         `plugin/requirements/_map.json` before `npm run smoke` runs against it\n\nCASE-4 — the vendored-viewer check is the rebuild's own diff, not a second comparison\n  Given  the \"Rebuild the vendored viewer\" step has just overwritten\n         `plugin/scripts/_map_viewer.html` in place\n  When   the next step runs\n  Then   it inspects the working-tree diff on that one file — the rebuild writing over the\n         committed copy IS the check, with no separate comparison logic\n\nCASE-5 — the diagram check builds into a temp directory, never in place\n  Given  `.gitignore` hard-blocks `docs/*.excalidraw`, a sibling file the generator writes\n  When   the \"Published architecture diagram matches its generator\" step runs\n  Then   `make_full_architecture.py` writes into a `mktemp -d` directory, and only that\n         output is compared against the committed `docs/full_architecture.html`\n\nCASE-6 — the release job depends on the artifacts job\n  Given  `.github/workflows/ci.yml`\n  When   the `release` job's `needs:` list is inspected\n  Then   it names `artifacts` alongside `gate-and-tests` and `tests`",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-REPRO-041"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": ".github/workflows/ci.yml:180"
        },
        {
          "role": "implements",
          "loc": ".github/workflows/ci.yml:183"
        },
        {
          "role": "implements",
          "loc": ".github/workflows/ci.yml:187"
        },
        {
          "role": "implements",
          "loc": ".github/workflows/ci.yml:196"
        },
        {
          "role": "implements",
          "loc": ".github/workflows/ci.yml:317"
        }
      ],
      "test_exempt": "pipeline wiring observed by the CI artifacts job, not by a unit test",
      "milestone": null,
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Reconstruirea și diff-uirea fiecărui artefact committed în CI",
          "intent": "Un artefact de build committed este o promisiune că un consumator nu trebuie niciodată să ruleze toolchain-ul care l-a produs — dar acea promisiune putrezește tacit de îndată ce sursa se schimbă fără o reconstrucție. Reconstruirea fiecărui artefact proaspăt la fiecare push și diff-uirea lui față de copia committed transformă \"probabil tot se potrivește\" într-un eșec CI ferm, cu costul rulării unui build Node și unul Python la fiecare push.",
          "contract": "> Un artefact de build committed este o promisiune că un consumator nu trebuie\n> niciodată să ruleze toolchain-ul care l-a produs — dar acea promisiune putrezește\n> tacit de îndată ce sursa se schimbă fără o reconstrucție. Reconstruirea fiecărui\n> artefact proaspăt la fiecare push și diff-uirea lui față de copia committed transformă\n> \"probabil tot se potrivește\" într-un eșec CI ferm, cu costul rulării unui build Node și\n> unul Python la fiecare push.\n\nFiecare bullet de mai jos este obligatoriu.\n- `plugin/scripts/_map_viewer.html` derivă din `app/`, construit de `npm run build:viewer`.\n- `docs/full_architecture.html` derivă din\n  `plugin/skills/excalidraw-diagram/examples/make_full_architecture.py`.\n- Job-ul CI `artifacts` reconstruiește fiecare artefact acoperit din sursa lui la fiecare push și\n  pull request.\n- Job-ul eșuează build-ul când un artefact reconstruit diferă de cel committed.\n- Mesajul de eșec numește fișierul perimat și comanda care îl regenerează.\n- Job-ul `release` rulează doar după ce `artifacts` trece, astfel încât un artefact perimat nu\n  ajunge niciodată la un tag publicat.",
          "acceptance": "CASE-1 — un viewer vendored perimat eșuează job-ul și numește fix-ul\n  Given  un commit care schimbă `app/` fără să re-commit-eze `plugin/scripts/_map_viewer.html`\n  When   rulează pasul \"Vendored viewer matches app/\"\n  Then   `git diff --exit-code` pe acel fișier este non-zero, pasul eșuează, iar eroarea\n         numește fișierul și `npm run build:viewer`\n\nCASE-2 — diagrama de arhitectură publicată se potrivește cu generatorul ei\n  Given  `make_full_architecture.py` regenerat într-un director temporar\n  When   ieșirea sa este comparată byte-cu-byte cu\n         `docs/full_architecture.html`-ul committed\n  Then   cele două fișiere sunt identice\n\nCASE-3 — smoke test-ul SSR rulează pe registry-ul real al acestui repo, nu pe un substitut\n  Given  un checkout proaspăt al cărui `app/public/data.json` este gitignored și absent\n  When   rulează pasul \"Viewer SSR smoke\"\n  Then   `npm run sync` construiește mai întâi acel fixture din\n         `plugin/requirements/_map.json`-ul committed înainte ca `npm run smoke` să ruleze pe el\n\nCASE-4 — verificarea viewer-ului vendored este diff-ul propriu al reconstrucției, nu o a doua comparație\n  Given  pasul \"Rebuild the vendored viewer\" tocmai a suprascris\n  `plugin/scripts/_map_viewer.html` pe loc\n  When   rulează pasul următor\n  Then   inspectează diff-ul working-tree pe acel fișier — reconstrucția care suprascrie copia\n         committed ESTE verificarea, fără nicio logică de comparație separată\n\nCASE-5 — verificarea diagramei se construiește într-un director temp, niciodată pe loc\n  Given  `.gitignore` blochează dur `docs/*.excalidraw`, un fișier frate pe care generatorul îl scrie\n  When   rulează pasul \"Published architecture diagram matches its generator\"\n  Then   `make_full_architecture.py` scrie într-un director `mktemp -d`, și doar acea\n         ieșire este comparată cu `docs/full_architecture.html`-ul committed\n\nCASE-6 — job-ul release depinde de job-ul artifacts\n  Given  `.github/workflows/ci.yml`\n  When   este inspectată lista `needs:` a job-ului `release`\n  Then   numește `artifacts` alături de `gate-and-tests` și `tests`"
        }
      }
    },
    {
      "id": "ARCH-RETIRE-064",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Taking a requirement out of service",
      "intent": "Deleting a requirement is the operation nobody documents and everybody does badly: the file goes, the tags stay, and the next gate reports dangling tags in code whose purpose is now unrecorded. `retire` inverts that order — it states the blast radius first, refuses while anything still depends on the requirement, and only then removes what it can remove deterministically. Deprecating is the default because it is reversible.",
      "contract": [
        "`retire` reports everything that points at the requirement before anything is changed: dependents, children, members, prose references, and the files where it is the only tagged requirement. [[REQ-RETIRE-960]]",
        "`retire` deprecates by default, refuses while dependents or children exist unless forced, and never writes without `--apply`. [[REQ-RETIRE-961]]",
        "`retire --delete` removes the requirement block, its lock entries and its membership tags — never a function body. [[REQ-RETIRE-962]]"
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the plan comes before the change Given  a requirement with members When   `retire` runs without `--apply` Then   the blast radius is printed and no file changes",
        "CASE-2 — — a dependent stops the operation Given  a requirement another one depends on When   `retire --apply` runs without `--force` Then   it refuses, names the dependent, and exits 1",
        "CASE-3 — — deprecating keeps the code working Given  a confirmed requirement with an implementing member and no dependents When   `retire --apply` runs Then   its status becomes deprecated and its tags are untouched"
      ],
      "accept": "CASE-1 — the plan comes before the change\n  Given  a requirement with members\n  When   `retire` runs without `--apply`\n  Then   the blast radius is printed and no file changes\n\nCASE-2 — a dependent stops the operation\n  Given  a requirement another one depends on\n  When   `retire --apply` runs without `--force`\n  Then   it refuses, names the dependent, and exits 1\n\nCASE-3 — deprecating keeps the code working\n  Given  a confirmed requirement with an implementing member and no dependents\n  When   `retire --apply` runs\n  Then   its status becomes deprecated and its tags are untouched",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001",
        "ARCH-SCAN-002",
        "ARCH-DRIFT-003",
        "ARCH-MODULEFILE-056"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-GATE-102"
      ],
      "satisfied_by": [
        "REQ-RETIRE-960",
        "REQ-RETIRE-961",
        "REQ-RETIRE-962"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8058"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8109"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7789"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10324"
        }
      ],
      "test_exempt": null,
      "milestone": "v4.0",
      "priority": "should-have",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Scoaterea din uz a unei cerințe",
          "intent": "Ștergerea unei cerințe este operația pe care nimeni nu o documentează și pe care toată lumea o face prost: fișierul dispare, etichetele rămân, iar următorul gate raportează etichete suspendate în cod al cărui scop nu mai este înregistrat. `retire` inversează acea ordine — afirmă mai întâi raza de impact, refuză cât timp ceva încă depinde de cerință, și doar atunci elimină ce poate elimina determinist. Deprecierea este implicită deoarece este reversibilă.",
          "contract": "> Ștergerea unei cerințe este operația pe care nimeni nu o documentează și pe care\n> toată lumea o face prost: fișierul dispare, etichetele rămân, iar următorul gate\n> raportează etichete suspendate în cod al cărui scop nu mai este înregistrat.\n> `retire` inversează acea ordine — afirmă mai întâi raza de impact, refuză cât timp\n> ceva încă depinde de cerință, și doar atunci elimină ce poate elimina determinist.\n> Deprecierea este implicită deoarece este reversibilă.\n\nFiecare punct de mai jos este obligatoriu.\n- `retire` raportează tot ce indică spre cerință înainte ca ceva să fie schimbat: dependenți, copii, membri, referințe de proză, și fișierele unde este singura cerință etichetată. [[REQ-RETIRE-960]]\n- `retire` deprecă implicit, refuză cât timp există dependenți sau copii cu excepția forțării, și nu scrie niciodată fără `--apply`. [[REQ-RETIRE-961]]\n- `retire --delete` elimină blocul cerinței, intrările sale din lock și etichetele sale de apartenență — niciodată corpul unei funcții. [[REQ-RETIRE-962]]",
          "acceptance": "CASE-1 — planul vine înaintea schimbării\n  Given  o cerință cu membri\n  When   `retire` rulează fără `--apply`\n  Then   raza de impact este afișată iar niciun fișier nu se schimbă\n\nCASE-2 — un dependent oprește operația\n  Given  o cerință de care depinde alta\n  When   `retire --apply` rulează fără `--force`\n  Then   refuză, numește dependentul, și iese cu 1\n\nCASE-3 — deprecierea păstrează codul funcțional\n  Given  o cerință confirmată cu un membru de implementare și niciun dependent\n  When   `retire --apply` rulează\n  Then   statusul ei devine deprecated iar etichetele sale rămân neatinse"
        }
      }
    },
    {
      "id": "REQ-RETIRE-960",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The blast radius of a retirement",
      "intent": "What makes a deletion safe is knowing, before it happens, what was pointing at the thing. The engine can compute every one of those pointers exactly, and separating exclusive files from shared ones is what turns \"delete the code too\" from a guess into a list.",
      "contract": [
        "The plan names the requirements that declare `depends_on` on this id, and those that declare `satisfies` it.",
        "The plan lists every member of the requirement with its role and location.",
        "The plan separates files where this was the only tagged requirement from files shared with other requirements.",
        "The plan names the requirements whose prose carries a `[[<id>]]` reference to it.",
        "The plan names the dependencies this requirement was the last consumer of, since `depends_on` runs consumer to foundation and nothing else reports a capability that has just lost its only caller.",
        "Computing the plan changes nothing."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — dependents and children are named Given  requirement B depending on A and requirement C satisfying A When   the plan for A is computed Then   it names B as a dependent and C as a child",
        "CASE-2 — — exclusive and shared files are separated Given  A tagged in one file alone and in one file it shares with B When   the plan for A is computed Then   the first file is listed as exclusive and the second as shared",
        "CASE-3 — — a prose cross-reference is found Given  requirement D whose Description carries `[[A]]` When   the plan for A is computed Then   D is named as a referencing requirement",
        "CASE-4 — — a dependency left with no consumer is named Given  A depends on E, and no other requirement depends on E When   the plan for A is computed Then   E is named as left with no consumer"
      ],
      "accept": "CASE-1 — dependents and children are named\n  Given  requirement B depending on A and requirement C satisfying A\n  When   the plan for A is computed\n  Then   it names B as a dependent and C as a child\n\nCASE-2 — exclusive and shared files are separated\n  Given  A tagged in one file alone and in one file it shares with B\n  When   the plan for A is computed\n  Then   the first file is listed as exclusive and the second as shared\n\nCASE-3 — a prose cross-reference is found\n  Given  requirement D whose Description carries `[[A]]`\n  When   the plan for A is computed\n  Then   D is named as a referencing requirement\n\nCASE-4 — a dependency left with no consumer is named\n  Given  A depends on E, and no other requirement depends on E\n  When   the plan for A is computed\n  Then   E is named as left with no consumer",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-RETIRE-064"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8058"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10324"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Raza de explozie a unei retrageri",
          "intent": "Ceea ce face o ștergere sigură este să știi, înainte să se întâmple, ce indica spre lucrul respectiv. Engine-ul poate calcula exact fiecare dintre acei indicatori, iar separarea fișierelor exclusive de cele partajate este ce transformă \"șterge și codul\" dintr-o presupunere într-o listă.",
          "contract": "> Ceea ce face o ștergere sigură este să știi, înainte să se întâmple, ce indica spre\n> lucrul respectiv. Engine-ul poate calcula exact fiecare dintre acei indicatori, iar\n> separarea fișierelor exclusive de cele partajate este ce transformă \"șterge și codul\"\n> dintr-o presupunere într-o listă.\n\nFiecare bullet de mai jos este obligatoriu.\n- Planul numește cerințele care declară `depends_on` pe acest id, și pe cele care declară\n  `satisfies` pentru el.\n- Planul listează fiecare membru al cerinței cu rolul și locația lui.\n- Planul separă fișierele unde aceasta era singura cerință tagată de fișierele partajate cu\n  alte cerințe.\n- Planul numește cerințele a căror proză poartă o referință `[[<id>]]` către el.\n- Planul numește dependențele al căror ultim consumator a fost această cerință, deoarece\n  `depends_on` merge de la consumator spre fundație și nimic altceva nu raportează o capabilitate\n  care tocmai și-a pierdut singurul apelant.\n- Calcularea planului nu schimbă nimic.",
          "acceptance": "CASE-1 — dependenții și copiii sunt numiți\n  Given  cerința B depinzând de A și cerința C satisfăcând A\n  When   este calculat planul pentru A\n  Then   numește B ca dependent și C ca copil\n\nCASE-2 — fișierele exclusive și cele partajate sunt separate\n  Given  A tagat într-un fișier singur și într-un fișier pe care îl împarte cu B\n  When   este calculat planul pentru A\n  Then   primul fișier este listat ca exclusiv iar al doilea ca partajat\n\nCASE-3 — o referință de proză este găsită\n  Given  cerința D a cărei Description poartă `[[A]]`\n  When   este calculat planul pentru A\n  Then   D este numită ca cerință care face referire\n\nCASE-4 — o dependență rămasă fără consumator este numită\n  Given  A depinde de E, și nicio altă cerință nu depinde de E\n  When   este calculat planul pentru A\n  Then   E este numit ca rămas fără consumator"
        }
      }
    },
    {
      "id": "REQ-RETIRE-961",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Deprecating, refusing, and never writing by accident",
      "intent": "The safe default matters more here than anywhere else in the engine: this is the one command whose mistake is measured in lost work. So it reports by default, refuses while anything still points at the requirement, and prefers the reversible half of the operation.",
      "contract": [
        "Without `--apply` nothing is written, and the output says so.",
        "With dependents or children present, `retire` refuses and exits 1 unless `--force` is given.",
        "`--apply` without `--delete` sets the requirement's status to deprecated and leaves its code and tags untouched.",
        "`--apply` refuses on a working tree with uncommitted changes unless `--force`, so the retirement is one reviewable diff.",
        "An unknown id exits 1."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the default run writes nothing Given  any requirement When   `retire <ID>` runs Then   the requirement file is byte-identical afterwards",
        "CASE-2 — — force overrides the dependent refusal Given  a requirement with one dependent When   `retire --apply --force` runs Then   the retirement proceeds",
        "CASE-3 — — deprecation is a status change and nothing else Given  a confirmed requirement with members and no dependents When   `retire --apply --force` runs Then   its frontmatter reads `status: deprecated` and its member tags are unchanged"
      ],
      "accept": "CASE-1 — the default run writes nothing\n  Given  any requirement\n  When   `retire <ID>` runs\n  Then   the requirement file is byte-identical afterwards\n\nCASE-2 — force overrides the dependent refusal\n  Given  a requirement with one dependent\n  When   `retire --apply --force` runs\n  Then   the retirement proceeds\n\nCASE-3 — deprecation is a status change and nothing else\n  Given  a confirmed requirement with members and no dependents\n  When   `retire --apply --force` runs\n  Then   its frontmatter reads `status: deprecated` and its member tags are unchanged",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-RETIRE-064"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8109"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8207"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8306"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10324"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Deprecierea, refuzul, și fără să scrie niciodată din greșeală",
          "intent": "Implicitul sigur contează mai mult aici decât oriunde altundeva în engine: aceasta este singura comandă a cărei greșeală este măsurată în muncă pierdută. Deci raportează implicit, refuză cât timp ceva încă indică spre cerință, și preferă jumătatea reversibilă a operației.",
          "contract": "> Implicitul sigur contează mai mult aici decât oriunde altundeva în engine: aceasta\n> este singura comandă a cărei greșeală este măsurată în muncă pierdută. Deci raportează\n> implicit, refuză cât timp ceva încă indică spre cerință, și preferă jumătatea\n> reversibilă a operației.\n\nFiecare bullet de mai jos este obligatoriu.\n- Fără `--apply` nimic nu este scris, iar ieșirea spune asta.\n- Cu dependenți sau copii prezenți, `retire` refuză și iese cu 1 dacă `--force` nu este dat.\n- `--apply` fără `--delete` setează statusul cerinței la deprecated și lasă codul și tag-urile\n  ei neatinse.\n- `--apply` refuză pe un working tree cu schimbări necommitted decât dacă `--force`, astfel\n  încât retragerea este un singur diff revizuibil.\n- Un id necunoscut iese cu 1.",
          "acceptance": "CASE-1 — rularea implicită nu scrie nimic\n  Given  orice cerință\n  When   rulează `retire <ID>`\n  Then   fișierul cerinței este byte-identic ulterior\n\nCASE-2 — force suprascrie refuzul de dependent\n  Given  o cerință cu un dependent\n  When   rulează `retire --apply --force`\n  Then   retragerea continuă\n\nCASE-3 — deprecierea este o schimbare de status și nimic altceva\n  Given  o cerință confirmed cu membri și fără dependenți\n  When   rulează `retire --apply --force`\n  Then   frontmatter-ul ei se citește `status: deprecated` iar tag-urile ei de membru sunt neschimbate"
        }
      }
    },
    {
      "id": "REQ-RETIRE-962",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Deleting a requirement without deleting meaning",
      "intent": "A deletion that leaves tags behind produces dangling-tag errors; one that rips out code destroys work. The engine takes exactly the half it can do without understanding the code: the requirement, its baseline, and the tag tokens. The other half is a list, handed over.",
      "contract": [
        "`--delete` removes the requirement's block from its file, and removes the file itself when it held only that requirement.",
        "`--delete` removes the requirement's entry from the contract lock and from the member sidecar.",
        "`--delete` strips `implements:`, `tested-by:` and `verifies:` tokens for that id from the files that carry them; a comment line that held only that tag is removed, a line carrying other tags keeps them.",
        "No function body, class or file of source code is removed on the strength of a tag.",
        "The files where the tag was exclusive are reported as code nothing points at any more."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a module file keeps its other requirements Given  a file holding requirements A and B When   A is deleted Then   the file still holds B, byte-identical, and A is gone",
        "CASE-2 — — a tag line goes, a shared line survives Given  one line carrying only `# implements: A` and one carrying `# implements: A  # implements: B` When   A is deleted Then   the first line is removed and the second keeps `# implements: B`",
        "CASE-3 — — the lock forgets the requirement Given  a lock holding an entry for A When   A is deleted Then   the lock no longer carries that entry",
        "CASE-4 — — code is never removed on the strength of a tag Given  a file whose only tag was A's, carrying a function body When   A is deleted Then   the function body is still there and the file is named as now-unreferenced"
      ],
      "accept": "CASE-1 — a module file keeps its other requirements\n  Given  a file holding requirements A and B\n  When   A is deleted\n  Then   the file still holds B, byte-identical, and A is gone\n\nCASE-2 — a tag line goes, a shared line survives\n  Given  one line carrying only `# implements: A` and one carrying `# implements: A  # implements: B`\n  When   A is deleted\n  Then   the first line is removed and the second keeps `# implements: B`\n\nCASE-3 — the lock forgets the requirement\n  Given  a lock holding an entry for A\n  When   A is deleted\n  Then   the lock no longer carries that entry\n\nCASE-4 — code is never removed on the strength of a tag\n  Given  a file whose only tag was A's, carrying a function body\n  When   A is deleted\n  Then   the function body is still there and the file is named as now-unreferenced",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-RETIRE-064"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8219"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8260"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8293"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10324"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Ștergerea unei cerințe fără să șteargă înțelesul",
          "intent": "O ștergere care lasă tag-uri în urmă produce erori de tag dangling; una care smulge codul distruge muncă. Engine-ul preia exact jumătatea pe care o poate face fără să înțeleagă codul: cerința, baseline-ul ei, și token-urile de tag. Cealaltă jumătate este o listă, predată.",
          "contract": "> O ștergere care lasă tag-uri în urmă produce erori de tag dangling; una care smulge\n> codul distruge muncă. Engine-ul preia exact jumătatea pe care o poate face fără să\n> înțeleagă codul: cerința, baseline-ul ei, și token-urile de tag. Cealaltă jumătate este\n> o listă, predată.\n\nFiecare bullet de mai jos este obligatoriu.\n- `--delete` elimină blocul cerinței din fișierul ei, și elimină fișierul în sine când conținea\n  doar acea cerință.\n- `--delete` elimină intrarea cerinței din lock-ul de contract și din sidecar-ul de membri.\n- `--delete` elimină token-urile `implements:`, `tested-by:` și `verifies:` pentru acel id din\n  fișierele care le poartă; o linie de comentariu care purta doar acel tag este eliminată, o\n  linie care poartă alte tag-uri le păstrează.\n- Niciun corp de funcție, clasă sau fișier de cod sursă nu este eliminat pe baza unui tag.\n- Fișierele unde tag-ul era exclusiv sunt raportate ca cod pe care nimic nu mai indică.",
          "acceptance": "CASE-1 — un fișier de modul își păstrează celelalte cerințe\n  Given  un fișier care conține cerințele A și B\n  When   A este ștearsă\n  Then   fișierul tot conține B, byte-identic, iar A a dispărut\n\nCASE-2 — o linie de tag dispare, o linie partajată supraviețuiește\n  Given  o linie care poartă doar `# implements: A` și una care poartă `# implements: A  # implements: B`\n  When   A este ștearsă\n  Then   prima linie este eliminată iar a doua păstrează `# implements: B`\n\nCASE-3 — lock-ul uită cerința\n  Given  un lock care conține o intrare pentru A\n  When   A este ștearsă\n  Then   lock-ul nu mai poartă acea intrare\n\nCASE-4 — codul nu este niciodată eliminat pe baza unui tag\n  Given  un fișier al cărui singur tag era al lui A, purtând un corp de funcție\n  When   A este ștearsă\n  Then   corpul funcției este tot acolo iar fișierul este numit ca acum nereferențiat"
        }
      }
    },
    {
      "id": "ARCH-REVIEW-022",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "AI requirement-quality review (deterministic plan + advisory pass)",
      "intent": "Deterministic lint catches the SHAPE of a requirement (long sentences, vague words, size). It cannot judge MEANING — whether a contract clause is actually testable, whether the WHY explains intent or just restates the title, whether the acceptance criteria really exercise the contract. This emits a deterministic, machine-readable plan of each requirement's prose so an AI can make that semantic judgement out of band — advisory only, never the gate, never auto-applied. A human always decides; the engine never calls an LLM.",
      "contract": [
        "The `review` command emits a deterministic, read-only JSON plan to stdout and writes no file; it never invokes an LLM. `review` (no arg) covers the whole corpus, `review <ID>` covers one requirement, and an unknown id fails closed rather than emitting an empty plan. [[REQ-REVIEW-906]]"
      ],
      "verify": [],
      "notes": [
        "The `untestable-contract` / `acceptance-doesnt-cover-contract` categories are deliberately judged by the AI, not the engine: a deterministic clause↔acceptance mapping is ARCH-ACVERIFY-019's job (`# verifies: <id>#AC-N`), and a reliable semantic \"cover\" check has no deterministic form.",
        "`jargon-used-before-defined` was deliberately NOT included — ARCH-LINT-014 deferred it for lack of a domain dictionary, and an LLM does not dissolve that false-positive wall.",
        "The sidecar is its own channel (not folded into `_findings.md`) precisely to keep the deterministic findings surface reproducible; mixing a non-deterministic AI pass into it would break that property.",
        "`lint_exempt: file-spread` — the `implements:` members are the engine's `review` command, the advisory skill contract and its AI-agnostic variant: one capability whose deterministic half and advisory half deliberately live in different files, not a diffuse one."
      ],
      "current_impl": [
        "`cmd_review` in `reqmap.py` (the deterministic plan emitter), dispatched from `main()` for the `review` subcommand; the out-of-band consumer is the `requirement-quality-review` skill.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  the corpus When   `review` runs with no argument (or as `review <ID>`) Then   it emits JSON for every requirement (or that one); neither form writes a file nor invokes an LLM",
        "CASE-2 — Given  the emitted plan When   it is inspected Then   it carries a `coverage_summary` with `total_requirements` and `requirements_in_plan`, and names the three categories with the suggested_rewrite-required finding contract",
        "CASE-3 — Given  the same corpus When   `review` runs twice Then   the two outputs are byte-identical (deterministic)",
        "CASE-4 — Given  a `requirements/_ai_review.md` present or absent When   `gate` runs Then   its exit code and printed output are byte-identical — the gate never reads the AI sidecar"
      ],
      "accept": "CASE-1\n  Given  the corpus\n  When   `review` runs with no argument (or as `review <ID>`)\n  Then   it emits JSON for every requirement (or that one); neither form writes a file nor\n         invokes an LLM\n\nCASE-2\n  Given  the emitted plan\n  When   it is inspected\n  Then   it carries a `coverage_summary` with `total_requirements` and `requirements_in_plan`,\n         and names the three categories with the suggested_rewrite-required finding contract\n\nCASE-3\n  Given  the same corpus\n  When   `review` runs twice\n  Then   the two outputs are byte-identical (deterministic)\n\nCASE-4\n  Given  a `requirements/_ai_review.md` present or absent\n  When   `gate` runs\n  Then   its exit code and printed output are byte-identical — the gate never reads the AI sidecar",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-QUALITY-104"
      ],
      "satisfied_by": [
        "REQ-REVIEW-906"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8340"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4679"
        },
        {
          "role": "implements",
          "loc": "plugin/skills/requirement-quality-review/SKILL.md:6"
        },
        {
          "role": "implements",
          "loc": "plugin/skills/requirement-quality-review/SKILL.universal.md:9"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.14",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Revizuire AI a calității cerințelor (plan determinist + trecere consultativă)",
          "intent": "Lint-ul determinist prinde FORMA unei cerințe (propoziții lungi, cuvinte vagi, dimensiune). Nu poate judeca SENSUL — dacă o clauză de contract este chiar testabilă, dacă WHY explică intenția sau doar reformulează titlul, dacă criteriile de acceptare chiar exercită contractul. Aceasta emite un plan determinist, citibil de mașină, al prozei fiecărei cerințe, astfel încât un AI poate face acea judecată semantică în afara benzii — doar consultativ, niciodată gate-ul, niciodată aplicat automat. Un om decide întotdeauna; motorul nu apelează niciodată un LLM.",
          "contract": "> Lint-ul determinist prinde FORMA unei cerințe (propoziții lungi, cuvinte vagi,\n> dimensiune). Nu poate judeca SENSUL — dacă o clauză de contract este chiar\n> testabilă, dacă WHY explică intenția sau doar reformulează titlul, dacă criteriile\n> de acceptare chiar exercită contractul. Aceasta emite un plan determinist, citibil\n> de mașină, al prozei fiecărei cerințe, astfel încât un AI poate face acea judecată\n> semantică în afara benzii — doar consultativ, niciodată gate-ul, niciodată aplicat\n> automat. Un om decide întotdeauna; motorul nu apelează niciodată un LLM.\n\nFiecare punct de mai jos este obligatoriu.\n- Comanda `review` emite un plan JSON determinist, doar-citire, către stdout și nu scrie niciun fișier; nu invocă niciodată un LLM. `review` (fără argument) acoperă întregul corpus, `review <ID>` acoperă o cerință, iar un id necunoscut eșuează închis în loc să emită un plan gol. [[REQ-REVIEW-906]]",
          "acceptance": "CASE-1\n  Given  corpusul\n  When   `review` rulează fără argument (sau ca `review <ID>`)\n  Then   emite JSON pentru fiecare cerință (sau pentru aceea); niciuna dintre forme nu scrie un fișier și nici\n         nu invocă un LLM\n\nCASE-2\n  Given  planul emis\n  When   este inspectat\n  Then   poartă un `coverage_summary` cu `total_requirements` și `requirements_in_plan`,\n         și numește cele trei categorii cu contractul de constatare suggested_rewrite-required\n\nCASE-3\n  Given  același corpus\n  When   `review` rulează de două ori\n  Then   cele două ieșiri sunt identice octet-cu-octet (determinist)\n\nCASE-4\n  Given  un `requirements/_ai_review.md` prezent sau absent\n  When   `gate` rulează\n  Then   codul său de ieșire și ieșirea afișată sunt identice octet-cu-octet — gate-ul nu citește niciodată sidecar-ul AI"
        }
      }
    },
    {
      "id": "REQ-REVIEW-906",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "A deterministic plan for an out-of-band AI review",
      "intent": "Lint can catch a requirement's shape but not its meaning — whether a clause is really testable, or the WHY just restates the title. `review` emits a byte-reproducible JSON plan naming three semantic categories an AI can judge out of band; the engine itself never calls an LLM, and no gate path ever reads the resulting advisory findings, so the deterministic corpus stays deterministic.",
      "contract": [
        "The `review` command emits a DETERMINISTIC, read-only JSON plan to stdout and writes no file; it never invokes an LLM. `review` (no arg) covers the whole corpus; `review <ID>` covers one requirement.",
        "The plan carries, per requirement, its prose (title, intent/WHY, contract, acceptance, verify-intent) plus cheap STRUCTURAL anchors that are deterministic facts, not judgements (contract-clause count, acceptance count, intent word count, intent-terse, more-contract-than- acceptance), and a corpus `coverage_summary` (`total_requirements`, `requirements_in_plan`).",
        "The plan names exactly three AI categories — `untestable-contract`, `why-restates-title`, `acceptance-doesnt-cover-contract` — and a finding contract: every AI finding carries a concrete `suggested_rewrite`, only high-confidence findings are emitted, and severity is advisory-only (never `error`/`warn`, never carried in the engine's severity vocabulary).",
        "DETERMINISM WALL: the plan is byte-reproducible across runs (no clock, no randomness, no LLM). No gate path (`gate`, `map --check`, the pre-commit hook, the CI action) reads, writes, or regenerates the plan or any AI sidecar.",
        "`gate` behaves identically whether or not an AI sidecar (`requirements/_ai_review.md`) is present.",
        "The AI pass is non-deterministic and advisory: its findings carry an unbounded false-positive rate. The near-zero-false-positive property comes from MANDATORY human review of each `suggested_rewrite`, not from any LLM confidence value (which is uncalibrated triage only).",
        "The AI consumer (the `requirement-quality-review` skill) writes findings to a separate, clearly-labelled \"AI — advisory (non-deterministic)\" sidecar — never `_findings.md` (which stays deterministic) and never the gate — and never auto-edits a requirement.",
        "`review` is distinct from `show`: `show` is a human single-requirement dossier; `review` is a machine corpus plan with structural anchors, a coverage summary and category guidance for an out-of-band AI consumer — a different audience and shape, so they are not merged.",
        "`review <ID>` with an unknown id fails closed: it exits 1 and prints an error, never a misleadingly empty plan that could be mistaken for \"nothing to review\"."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — review is deterministic, read-only and LLM-free Given  any corpus When   `review` runs twice with no edit between the runs Then   both runs print byte-identical JSON to stdout, create no file, and make no network call",
        "CASE-2 — — the plan carries prose, structural anchors and a coverage summary Given  one requirement in the corpus When   `review <ID>` runs Then   its JSON entry carries title, WHY, contract, acceptance, contract-clause count and acceptance count",
        "CASE-3 — — the plan names the three AI categories and the finding contract Given  the emitted review plan When   it is inspected Then   it names `untestable-contract`, `why-restates-title`, `acceptance-doesnt-cover-contract`, each requiring a `suggested_rewrite` and mandatory human review as its false-positive safeguard, never an LLM confidence value",
        "CASE-4 — — the plan is byte-identical across runs and gate never reads it Given  the same corpus and an absent `_ai_review.md` sidecar When   `review` runs twice, then `gate` runs Then   the two `review` outputs are byte-identical and `gate`'s output is unaffected by the sidecar's absence",
        "CASE-5 — — gate ignores the AI sidecar file entirely Given  `gate` run once with no `_ai_review.md` and once with one present When   both runs complete Then   their exit codes and printed output are byte-identical",
        "CASE-6 — — an unknown id fails closed instead of emitting an empty plan Given  `review <ID>` with an id that matches no requirement When   the command runs Then   it exits 1 and prints \"no requirement with id\", writing no JSON plan"
      ],
      "accept": "CASE-1 — review is deterministic, read-only and LLM-free\n  Given  any corpus\n  When   `review` runs twice with no edit between the runs\n  Then   both runs print byte-identical JSON to stdout, create no file, and make no network call\n\nCASE-2 — the plan carries prose, structural anchors and a coverage summary\n  Given  one requirement in the corpus\n  When   `review <ID>` runs\n  Then   its JSON entry carries title, WHY, contract, acceptance, contract-clause count and acceptance count\n\nCASE-3 — the plan names the three AI categories and the finding contract\n  Given  the emitted review plan\n  When   it is inspected\n  Then   it names `untestable-contract`, `why-restates-title`, `acceptance-doesnt-cover-contract`,\n         each requiring a `suggested_rewrite` and mandatory human review as its\n         false-positive safeguard, never an LLM confidence value\n\nCASE-4 — the plan is byte-identical across runs and gate never reads it\n  Given  the same corpus and an absent `_ai_review.md` sidecar\n  When   `review` runs twice, then `gate` runs\n  Then   the two `review` outputs are byte-identical and `gate`'s output is unaffected by the sidecar's absence\n\nCASE-5 — gate ignores the AI sidecar file entirely\n  Given  `gate` run once with no `_ai_review.md` and once with one present\n  When   both runs complete\n  Then   their exit codes and printed output are byte-identical\n\nCASE-6 — an unknown id fails closed instead of emitting an empty plan\n  Given  `review <ID>` with an id that matches no requirement\n  When   the command runs\n  Then   it exits 1 and prints \"no requirement with id\", writing no JSON plan",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-REVIEW-022"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:8340"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4679"
        },
        {
          "role": "implements",
          "loc": "plugin/skills/requirement-quality-review/SKILL.md:6"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Un plan determinist pentru o revizuire AI out-of-band",
          "intent": "Lint poate detecta forma unei cerințe, dar nu și sensul ei — dacă o clauză este cu adevărat testabilă, sau dacă WHY doar reformulează titlul. `review` emite un plan JSON reproductibil byte cu byte, care numește trei categorii semantice pe care o AI le poate evalua out-of-band; motorul însuși nu apelează niciodată un LLM, iar nicio cale de gate nu citește vreodată constatările consultative rezultate, astfel încât corpusul determinist rămâne determinist.",
          "contract": "> Lint poate detecta forma unei cerințe, dar nu și sensul ei — dacă o clauză este cu\n> adevărat testabilă, sau dacă WHY doar reformulează titlul. `review` emite un plan JSON\n> reproductibil byte cu byte, care numește trei categorii semantice pe care o AI le poate\n> evalua out-of-band; motorul însuși nu apelează niciodată un LLM, iar nicio cale de gate\n> nu citește vreodată constatările consultative rezultate, astfel încât corpusul\n> determinist rămâne determinist.\n\nFiecare punct de mai jos este obligatoriu.\n- Comanda `review` emite la stdout un plan JSON DETERMINIST, read-only, și nu scrie niciun\n  fișier; nu invocă niciodată un LLM. `review` (fără argument) acoperă tot corpusul; `review <ID>`\n  acoperă o singură cerință.\n- Planul poartă, pentru fiecare cerință, proza ei (titlu, intent/WHY, contract, acceptance,\n  verify-intent) plus repere STRUCTURALE ieftine care sunt fapte deterministe, nu judecăți\n  (numărul de clauze din contract, numărul de criterii de acceptance, numărul de cuvinte din intent, intent-terse, more-contract-than-\n  acceptance), și un `coverage_summary` la nivel de corpus (`total_requirements`, `requirements_in_plan`).\n- Planul numește exact trei categorii AI — `untestable-contract`, `why-restates-title`,\n  `acceptance-doesnt-cover-contract` — și un contract al constatării: fiecare constatare AI poartă un\n  `suggested_rewrite` concret, sunt emise doar constatări cu încredere ridicată, iar severitatea este\n  doar consultativă (niciodată `error`/`warn`, niciodată inclusă în vocabularul de severitate al motorului).\n- ZID AL DETERMINISMULUI: planul este reproductibil byte cu byte între rulări (fără ceas, fără\n  aleatoriu, fără LLM). Nicio cale de gate (`gate`, `map --check`, hook-ul pre-commit, acțiunea CI) nu citește, nu scrie,\n  și nu regenerează planul sau vreun sidecar AI.\n- `gate` se comportă identic indiferent dacă un sidecar AI (`requirements/_ai_review.md`) este\n  prezent sau nu.\n- Trecerea AI este non-deterministă și consultativă: constatările ei poartă o rată de fals-pozitiv\n  nemărginită. Proprietatea de rată de fals-pozitiv aproape-zero vine din revizuirea umană OBLIGATORIE a fiecărui\n  `suggested_rewrite`, nu dintr-o valoare de încredere a vreunui LLM (care este doar triaj necalibrat).\n- Consumatorul AI (skill-ul `requirement-quality-review`) scrie constatările într-un sidecar separat,\n  etichetat clar \"AI — advisory (non-deterministic)\" — niciodată `_findings.md` (care\n  rămâne determinist) și niciodată gate-ul — și nu editează niciodată automat o cerință.\n- `review` este distinct de `show`: `show` este un dosar uman pentru o singură cerință; `review` este un\n  plan de corpus destinat mașinii, cu repere structurale, un rezumat de acoperire și îndrumări de categorie pentru un\n  consumator AI out-of-band — un public și o formă diferite, deci nu sunt combinate.\n- `review <ID>` cu un id necunoscut eșuează închis: iese cu 1 și afișează o eroare, niciodată un\n  plan gol înșelător, care ar putea fi confundat cu \"nimic de revizuit\".",
          "acceptance": "CASE-1 — review este determinist, read-only și fără LLM\n  Given  orice corpus\n  When   `review` rulează de două ori fără nicio editare între rulări\n  Then   ambele rulări afișează la stdout un JSON identic byte cu byte, nu creează niciun fișier, și nu fac niciun apel de rețea\n\nCASE-2 — planul poartă proza, reperele structurale și un rezumat de acoperire\n  Given  o cerință în corpus\n  When   `review <ID>` rulează\n  Then   intrarea ei JSON poartă titlu, WHY, contract, acceptance, numărul de clauze din contract și numărul de criterii de acceptance\n\nCASE-3 — planul numește cele trei categorii AI și contractul constatării\n  Given  planul de review emis\n  When   este inspectat\n  Then   numește `untestable-contract`, `why-restates-title`, `acceptance-doesnt-cover-contract`,\n         fiecare cerând un `suggested_rewrite` și revizuire umană obligatorie ca\n         garanție împotriva falsului pozitiv, niciodată o valoare de încredere a unui LLM\n\nCASE-4 — planul este identic byte cu byte între rulări, iar gate nu îl citește niciodată\n  Given  același corpus și un sidecar `_ai_review.md` absent\n  When   `review` rulează de două ori, apoi `gate` rulează\n  Then   cele două ieșiri `review` sunt identice byte cu byte, iar ieșirea lui `gate` nu este afectată de absența sidecar-ului\n\nCASE-5 — gate ignoră complet fișierul sidecar AI\n  Given  `gate` rulat o dată fără `_ai_review.md` și o dată cu unul prezent\n  When   ambele rulări se termină\n  Then   codurile lor de ieșire și ieșirea afișată sunt identice byte cu byte\n\nCASE-6 — un id necunoscut eșuează închis în loc să emită un plan gol\n  Given  `review <ID>` cu un id care nu se potrivește cu nicio cerință\n  When   comanda rulează\n  Then   iese cu 1 și afișează \"no requirement with id\", fără să scrie niciun plan JSON"
        }
      }
    },
    {
      "id": "ARCH-REVIEWEDSCORE-109",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Reviewed-only health score",
      "intent": "The headline health score cannot tell \"nobody has reviewed this yet\" from \"this is rotting\". Both read as a low number, because the first axis of green is status `confirmed`, so a draft can never be green while it still counts in the total. A repo that drafts requirements from legacy code therefore reads near-zero health forever, and the one number meant to show decay shows only that reviewing has not finished. This adds a second number scored over the reviewed part alone, so the two readings separate.",
      "contract": [
        "The reviewed-only score is the percentage of green requirements among the reviewed ones.",
        "The count of green requirements is the same one the headline uses. Only the denominator differs.",
        "The denominator counts `confirmed` requirements only. A requirement at any other status is left out of both sides, so it can never depress the score without being able to lift it.",
        "The reviewed-only score is absent, rather than zero, when no requirement is confirmed. Zero of zero is not zero per cent.",
        "The reviewed-only score is absent when no requirement is a draft, because it would then restate the headline under a second name.",
        "`health` prints the score as a line of its own, and `--json` carries it as a key, under exactly those same conditions.",
        "The headline score keeps its existing definition, so a badge a consumer already publishes reads the number it always read.",
        "A reader who wants the old behaviour alone ignores the new key; its absence in the two cases above leaves an existing `--json` schema untouched."
      ],
      "verify": [],
      "notes": [
        "Measured on this repo at commit `1eea8f1`, when the signal was added: the headline read `10/100` while every one of the 71 confirmed requirements was green on every axis — the detailed-design drafts [[ARCH-DECOMPOSE-050]] seeded were the whole difference. The reviewed-only score read `100/100` there. This is a dated snapshot, not a live figure: the corpus only grows ([[ARCH-DECOMPOSE-050]], ADR-0021), so run `health` for today's.",
        "It shares the \"absent, not zero\" discipline with the untagged-code count ([[ARCH-COVERAGE-029]]), which is the same shape of problem: a signal printed by `health` that must not silently widen a consumer's JSON schema.",
        "Redefining the headline's denominator was rejected instead of this: [[ARCH-HEALTH-017]] CASE-2 binds an all-draft corpus to a score of zero, and consumer badges already read that key, so moving it would change every published number without notice.",
        "The score still says nothing about whether a draft *should* exist. A corpus can read `100/100` reviewed while carrying hundreds of drafts nobody intends to finish."
      ],
      "current_impl": [
        "The `reviewed_total` / `reviewed_score` block in `cmd_health` (`reqmap.py`), emitted into `data` and printed as the `reviewed only:` line under the conditions above."
      ],
      "acc": [
        "CASE-1 — Given  a corpus of one green confirmed requirement and three drafts When   `health` runs Then   the headline score is 25 and the reviewed-only score is 100",
        "CASE-2 — Given  an all-draft corpus When   `health --json` runs Then   the output carries no reviewed-only key, and the headline score is still zero",
        "CASE-3 — Given  a corpus holding no draft at all When   `health --json` runs Then   the output carries no reviewed-only key",
        "CASE-4 — Given  a corpus holding both drafts and reviewed requirements When   `health` runs Then   the printed line names the confirmed count and how many are not confirmed yet",
        "CASE-5 — Given  a corpus of one green confirmed requirement, one draft, and one requirement at `baseline`, `in-progress`, `implemented` or `deprecated` When   `health --json` runs Then   the reviewed-only score is 100 and its denominator is 1, because the fourth status is neither green nor counted against green"
      ],
      "accept": "CASE-1\n  Given  a corpus of one green confirmed requirement and three drafts\n  When   `health` runs\n  Then   the headline score is 25 and the reviewed-only score is 100\n\nCASE-2\n  Given  an all-draft corpus\n  When   `health --json` runs\n  Then   the output carries no reviewed-only key, and the headline score is still zero\n\nCASE-3\n  Given  a corpus holding no draft at all\n  When   `health --json` runs\n  Then   the output carries no reviewed-only key\n\nCASE-4\n  Given  a corpus holding both drafts and reviewed requirements\n  When   `health` runs\n  Then   the printed line names the confirmed count and how many are not confirmed yet\n\nCASE-5\n  Given  a corpus of one green confirmed requirement, one draft, and one requirement at\n         `baseline`, `in-progress`, `implemented` or `deprecated`\n  When   `health --json` runs\n  Then   the reviewed-only score is 100 and its denominator is 1, because the fourth\n         status is neither green nor counted against green",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-HEALTH-017"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-REPORT-105"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5839"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5850"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3448"
        }
      ],
      "test_exempt": null,
      "milestone": "v3.1",
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Scor de sănătate doar pentru cele revizuite",
          "intent": "Scorul principal de sănătate nu poate distinge \"nimeni nu a revizuit asta încă\" de \"asta putrezește\". Ambele se citesc ca un număr scăzut, deoarece prima axă a verdelui este statusul `confirmed`, astfel încât un draft nu poate fi niciodată verde cât timp încă este numărat în total. Un repo care schițează cerințe din cod moștenit citește astfel o sănătate aproape zero pentru totdeauna, iar singurul număr menit să arate degradarea arată doar că revizuirea nu s-a terminat. Aceasta adaugă un al doilea număr calculat doar peste partea revizuită, astfel încât cele două citiri se separă.",
          "contract": "> Scorul principal de sănătate nu poate distinge \"nimeni nu a revizuit asta încă\" de\n> \"asta putrezește\". Ambele se citesc ca un număr scăzut, deoarece prima axă a\n> verdelui este statusul `confirmed`, astfel încât un draft nu poate fi niciodată verde\n> cât timp încă este numărat în total. Un repo care schițează cerințe din cod moștenit\n> citește astfel o sănătate aproape zero pentru totdeauna, iar singurul număr menit să\n> arate degradarea arată doar că revizuirea nu s-a terminat. Aceasta adaugă un al\n> doilea număr calculat doar peste partea revizuită, astfel încât cele două citiri se\n> separă.\n\nFiecare punct de mai jos este obligatoriu.\n\n**Ce calculează**\n- Scorul doar-revizuite este procentul de cerințe verzi dintre cele revizuite.\n- Numărul de cerințe verzi este același folosit de scorul principal. Doar numitorul\n  diferă.\n- Numitorul numără doar cerințele `confirmed`. O cerință aflată în orice alt\n  status este lăsată în afara ambelor părți, astfel încât nu poate niciodată coborî scorul fără\n  a putea să îl și ridice.\n\n**Când este absent**\n- Scorul doar-revizuite este absent, în loc de zero, atunci când nicio cerință nu este confirmată.\n  Zero din zero nu este zero la sută.\n- Scorul doar-revizuite este absent atunci când nicio cerință nu este draft, deoarece ar\n  reformula atunci scorul principal sub un al doilea nume.\n- `health` afișează scorul ca o linie proprie, iar `--json` îl poartă ca o cheie, exact în\n  aceleași condiții.\n\n**Ce nu schimbă**\n- Scorul principal își păstrează definiția existentă, astfel încât o insignă pe care un consumator o\n  publică deja citește numărul pe care l-a citit întotdeauna.\n- Un cititor care vrea doar comportamentul vechi ignoră noua cheie; absența ei în cele două\n  cazuri de mai sus lasă o schemă `--json` existentă neatinsă.",
          "acceptance": "CASE-1\n  Given  un corpus cu o cerință confirmată verde și trei drafturi\n  When   `health` rulează\n  Then   scorul principal este 25 iar scorul doar-revizuite este 100\n\nCASE-2\n  Given  un corpus numai-draft\n  When   `health --json` rulează\n  Then   ieșirea nu poartă nicio cheie doar-revizuite, iar scorul principal este tot zero\n\nCASE-3\n  Given  un corpus care nu conține niciun draft\n  When   `health --json` rulează\n  Then   ieșirea nu poartă nicio cheie doar-revizuite\n\nCASE-4\n  Given  un corpus care conține atât drafturi cât și cerințe revizuite\n  When   `health` rulează\n  Then   linia afișată numește numărul de confirmate și câte nu sunt încă confirmate\n\nCASE-5\n  Given  un corpus cu o cerință confirmată verde, un draft, și o cerință aflată la\n         `baseline`, `in-progress`, `implemented` sau `deprecated`\n  When   `health --json` rulează\n  Then   scorul doar-revizuite este 100 iar numitorul său este 1, deoarece al patrulea\n         status nu este nici verde, nici numărat împotriva verdelui"
        }
      }
    },
    {
      "id": "ARCH-ROADMAP-038",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Roadmap coherence signals",
      "intent": "`TODO.md` is this project's roadmap and decision log, and nothing checks that it still matches reality. Twice it has fallen behind — once by seven milestones — and once a cosmetic heading rename silently re-filed the only open item under the wrong version, because a heading that is not a version leaves the previous one in force. These two read-only signals surface both, so the roadmap stops drifting unnoticed between manual clean-ups.",
      "contract": [
        "`health --json` reads `TODO.md` from the code root, or its parent when absent there, and reports two read-only signals. [[REQ-ROADMAP-907]]",
        "The first signal fires when the roadmap's newest milestone falls behind the newest requirement `milestone:`. The second lists a `## ` heading whose first token is not a version, which silently re-files items under the wrong milestone. [[REQ-ROADMAP-907]]"
      ],
      "verify": [],
      "notes": [
        "The behind-signal compares against requirement `milestone:` fields, not against a package version. The engine owns the former in every repo; the latter is project-specific. A repo that leaves `milestone:` unset gets no behind-signal.",
        "Neither signal is a gate. The v1.35 roadmap-hygiene note chose manual upkeep over automation when demand was n=1; this is the read-only middle ground after n=2."
      ],
      "current_impl": [
        "`_roadmap_signals` and `_version_key` in `reqmap.py`, read by `cmd_health`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a roadmap whose newest milestone is older than the newest requirement milestone When   `health --json` runs Then   it reports both versions as the behind-signal",
        "CASE-2 — Given  a roadmap at or ahead of the newest requirement milestone When   `health --json` runs Then   it reports no behind-signal",
        "CASE-3 — Given  a roadmap holding a `## ` heading that does not start with a version When   `health --json` runs Then   that heading is listed, because items below it are filed under the section above",
        "CASE-4 — Given  a repo with no `TODO.md` When   `health --json` runs Then   it reports neither roadmap signal",
        "CASE-5 — Given  milestones `v2.9` and `v2.10` When   the versions are compared Then   `v2.10` ranks above `v2.9`, which a string compare would reverse"
      ],
      "accept": "CASE-1\n  Given  a roadmap whose newest milestone is older than the newest requirement milestone\n  When   `health --json` runs\n  Then   it reports both versions as the behind-signal\n\nCASE-2\n  Given  a roadmap at or ahead of the newest requirement milestone\n  When   `health --json` runs\n  Then   it reports no behind-signal\n\nCASE-3\n  Given  a roadmap holding a `## ` heading that does not start with a version\n  When   `health --json` runs\n  Then   that heading is listed, because items below it are filed under the section above\n\nCASE-4\n  Given  a repo with no `TODO.md`\n  When   `health --json` runs\n  Then   it reports neither roadmap signal\n\nCASE-5\n  Given  milestones `v2.9` and `v2.10`\n  When   the versions are compared\n  Then   `v2.10` ranks above `v2.9`, which a string compare would reverse",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-HEALTH-017"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-REPORT-105"
      ],
      "satisfied_by": [
        "REQ-ROADMAP-907"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4029"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4063"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5907"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6424"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9593"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Semnale de coerență a roadmap-ului",
          "intent": "`TODO.md` este roadmap-ul și jurnalul de decizii al acestui proiect, iar nimic nu verifică dacă mai corespunde realității. De două ori a rămas în urmă — o dată cu șapte milestone-uri — și o dată o redenumire cosmetică de titlu a re-arhivat în tăcere singurul element deschis sub versiunea greșită, deoarece un titlu care nu este o versiune lasă în vigoare versiunea precedentă. Aceste două semnale doar-citire le scot pe amândouă la suprafață, astfel încât roadmap-ul nu mai suferă drift neobservat între curățenii manuale.",
          "contract": "> `TODO.md` este roadmap-ul și jurnalul de decizii al acestui proiect, iar nimic nu\n> verifică dacă mai corespunde realității. De două ori a rămas în urmă — o dată cu\n> șapte milestone-uri — și o dată o redenumire cosmetică de titlu a re-arhivat în\n> tăcere singurul element deschis sub versiunea greșită, deoarece un titlu care nu\n> este o versiune lasă în vigoare versiunea precedentă. Aceste două semnale\n> doar-citire le scot pe amândouă la suprafață, astfel încât roadmap-ul nu mai suferă\n> drift neobservat între curățenii manuale.\n\nFiecare punct de mai jos este obligatoriu.\n- `health --json` citește `TODO.md` din rădăcina de cod, sau din părintele ei atunci când lipsește acolo, și raportează două semnale doar-citire. [[REQ-ROADMAP-907]]\n- Primul semnal se declanșează atunci când cel mai nou milestone al roadmap-ului rămâne în urma celui mai nou `milestone:` de cerință. Al doilea listează un titlu `## ` al cărui prim token nu este o versiune, ceea ce re-arhivează în tăcere elementele sub milestone-ul greșit. [[REQ-ROADMAP-907]]",
          "acceptance": "CASE-1\n  Given  un roadmap al cărui milestone cel mai nou este mai vechi decât cel mai nou milestone de cerință\n  When   `health --json` rulează\n  Then   raportează ambele versiuni ca semnalul de rămânere în urmă\n\nCASE-2\n  Given  un roadmap la sau înaintea celui mai nou milestone de cerință\n  When   `health --json` rulează\n  Then   nu raportează niciun semnal de rămânere în urmă\n\nCASE-3\n  Given  un roadmap care conține un titlu `## ` care nu începe cu o versiune\n  When   `health --json` rulează\n  Then   acel titlu este listat, deoarece elementele de sub el sunt arhivate sub secțiunea de deasupra\n\nCASE-4\n  Given  un repo fără niciun `TODO.md`\n  When   `health --json` rulează\n  Then   nu raportează niciun semnal de roadmap\n\nCASE-5\n  Given  milestone-urile `v2.9` și `v2.10`\n  When   versiunile sunt comparate\n  Then   `v2.10` se clasează deasupra lui `v2.9`, ceea ce o comparație de tip string ar inversa"
        }
      }
    },
    {
      "id": "REQ-ROADMAP-907",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "A behind roadmap and an unversioned heading, both read-only",
      "intent": "`TODO.md` is this project's roadmap, and nothing used to check that it still matched reality: it has fallen seven milestones behind once, and a cosmetic heading rename once silently re-filed the only open item under the wrong version, because a heading that is not a version leaves the previous one in force. `health --json` now surfaces both as read-only signals, neither a gate, so the roadmap stops drifting unnoticed between manual clean-ups.",
      "contract": [
        "`health` reads `TODO.md` from the code root, or from its parent when absent there.",
        "`health --json` reports nothing about the roadmap when no `TODO.md` exists, so a repo that keeps none sees no new output.",
        "`health --json` reports the newest milestone in the roadmap against the newest `milestone:` recorded on any requirement.",
        "`health --json` reports the pair only when the roadmap is the older of the two.",
        "Versions compare segment by segment as numbers, so `v2.10` ranks above `v2.9`.",
        "`health --json` lists every `## ` heading in the roadmap whose first token is not a version.",
        "Such a heading leaves the previous milestone in force, so items below it are filed under the section above instead of their own.",
        "Both signals are read-only. Neither changes an exit code, and neither lowers the health score."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — _roadmap_signals falls back to the parent directory for TODO.md Given  no `TODO.md` in the code root but one in its parent directory When   `_roadmap_signals(root)` runs Then   it reads the parent's `TODO.md` and returns its milestone data",
        "CASE-2 — — no TODO.md means no roadmap keys in health --json Given  a repo with no `TODO.md` anywhere When   `health --json` runs Then   the payload has no `roadmap_behind` and no `roadmap_unversioned_headings` key",
        "CASE-3 — — behind-signal names both the roadmap's and requirements' newest milestone Given  a `TODO.md` newest heading `## v2.8` and a requirement with `milestone: v2.13` When   `health --json` runs Then   `roadmap_behind` equals `{\"todo\": \"v2.8\", \"requirements\": \"v2.13\"}`, `health` exits 0, and the health score is unchanged — the signal is read-only, like its unversioned-heading counterpart",
        "CASE-4 — — no behind-signal when the roadmap is current or ahead Given  a `TODO.md` newest heading `## v2.16` and a requirement with `milestone: v2.13` When   `health --json` runs Then   the payload carries no `roadmap_behind` key",
        "CASE-5 — — v2.10 sorts above v2.9 under _version_key Given  the strings `\"v2.10\"` and `\"v2.9\"` When   `_version_key` is applied to each and compared Then   `_version_key(\"v2.10\") > _version_key(\"v2.9\")`, unlike a plain string compare",
        "CASE-6 — — a non-version heading is listed under roadmap_unversioned_headings Given  a `TODO.md` with `## v2.16` followed later by `## Deferred work` When   `health --json` runs Then   `roadmap_unversioned_headings` equals `[\"Deferred work\"]`",
        "CASE-7 — — an item under a non-version heading is filed under the prior milestone Given  `## v2.16` followed by `## Deferred work` followed by one checklist item When   `_parse_todos_from_text` parses the text Then   that item's `milestone` reads `\"v2.16\"`, not `\"Deferred work\"`"
      ],
      "accept": "CASE-1 — _roadmap_signals falls back to the parent directory for TODO.md\n  Given  no `TODO.md` in the code root but one in its parent directory\n  When   `_roadmap_signals(root)` runs\n  Then   it reads the parent's `TODO.md` and returns its milestone data\n\nCASE-2 — no TODO.md means no roadmap keys in health --json\n  Given  a repo with no `TODO.md` anywhere\n  When   `health --json` runs\n  Then   the payload has no `roadmap_behind` and no `roadmap_unversioned_headings` key\n\nCASE-3 — behind-signal names both the roadmap's and requirements' newest milestone\n  Given  a `TODO.md` newest heading `## v2.8` and a requirement with `milestone: v2.13`\n  When   `health --json` runs\n  Then   `roadmap_behind` equals `{\"todo\": \"v2.8\", \"requirements\": \"v2.13\"}`, `health`\n         exits 0, and the health score is unchanged — the signal is read-only, like its\n         unversioned-heading counterpart\n\nCASE-4 — no behind-signal when the roadmap is current or ahead\n  Given  a `TODO.md` newest heading `## v2.16` and a requirement with `milestone: v2.13`\n  When   `health --json` runs\n  Then   the payload carries no `roadmap_behind` key\n\nCASE-5 — v2.10 sorts above v2.9 under _version_key\n  Given  the strings `\"v2.10\"` and `\"v2.9\"`\n  When   `_version_key` is applied to each and compared\n  Then   `_version_key(\"v2.10\") > _version_key(\"v2.9\")`, unlike a plain string compare\n\nCASE-6 — a non-version heading is listed under roadmap_unversioned_headings\n  Given  a `TODO.md` with `## v2.16` followed later by `## Deferred work`\n  When   `health --json` runs\n  Then   `roadmap_unversioned_headings` equals `[\"Deferred work\"]`\n\nCASE-7 — an item under a non-version heading is filed under the prior milestone\n  Given  `## v2.16` followed by `## Deferred work` followed by one checklist item\n  When   `_parse_todos_from_text` parses the text\n  Then   that item's `milestone` reads `\"v2.16\"`, not `\"Deferred work\"`",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-ROADMAP-038"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4029"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4063"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6424"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9593"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 7,
      "covered": 7,
      "i18n": {
        "ro": {
          "title": "O foaie de parcurs rămasă în urmă și un titlu neversionat, ambele read-only",
          "intent": "`TODO.md` este foaia de parcurs a acestui proiect, și nimic nu verifica dacă ea mai corespunde încă realității: a rămas în urmă cu șapte milestone-uri o dată, iar redenumirea cosmetică a unui titlu a re-clasificat o dată în tăcere singurul item deschis sub versiunea greșită, pentru că un titlu care nu este o versiune lasă în vigoare versiunea anterioară. `health --json` acum arată ambele ca semnale read-only, niciunul un gate, astfel încât foaia de parcurs nu mai deriva neobservată între curățările manuale.",
          "contract": "> `TODO.md` este foaia de parcurs a acestui proiect, și nimic nu verifica dacă ea mai\n> corespunde încă realității: a rămas în urmă cu șapte milestone-uri o dată, iar\n> redenumirea cosmetică a unui titlu a re-clasificat o dată în tăcere singurul item\n> deschis sub versiunea greșită, pentru că un titlu care nu este o versiune lasă\n> în vigoare versiunea anterioară. `health --json` acum arată ambele ca semnale\n> read-only, niciunul un gate, astfel încât foaia de parcurs nu mai deriva neobservată\n> între curățările manuale.\n\nFiecare punct de mai jos este obligatoriu.\n- `health` citește `TODO.md` din code root, sau din părintele lui când lipsește acolo.\n- `health --json` nu raportează nimic despre foaia de parcurs când nu există niciun\n  `TODO.md`, astfel un repo care nu păstrează niciunul nu vede nicio ieșire nouă.\n- `health --json` raportează cel mai nou milestone din foaia de parcurs comparativ cu cel mai nou\n  `milestone:` înregistrat pe orice cerință.\n- `health --json` raportează perechea doar când foaia de parcurs este cea mai veche dintre cele două.\n- Versiunile se compară segment cu segment ca numere, astfel `v2.10` se clasează peste `v2.9`.\n- `health --json` listează fiecare titlu `## ` din foaia de parcurs al cărui prim token nu este o\n  versiune.\n- Un astfel de titlu lasă în vigoare milestone-ul anterior, astfel elementele de sub el sunt clasificate\n  sub secțiunea de deasupra, în loc de a lor proprie.\n- Ambele semnale sunt read-only. Niciunul nu schimbă un cod de ieșire, și niciunul nu scade\n  scorul de health.",
          "acceptance": "CASE-1 — _roadmap_signals recurge la directorul părinte pentru TODO.md\n  Given  niciun `TODO.md` în code root, dar unul în directorul lui părinte\n  When   `_roadmap_signals(root)` rulează\n  Then   citește `TODO.md`-ul părintelui și returnează datele lui de milestone\n\nCASE-2 — fără TODO.md înseamnă fără chei de roadmap în health --json\n  Given  un repo fără niciun `TODO.md` nicăieri\n  When   `health --json` rulează\n  Then   payload-ul nu are nicio cheie `roadmap_behind` și nicio cheie `roadmap_unversioned_headings`\n\nCASE-3 — semnalul behind numește atât cel mai nou milestone al roadmap-ului cât și al cerințelor\n  Given  un titlu cel mai nou din `TODO.md` `## v2.8` și o cerință cu `milestone: v2.13`\n  When   `health --json` rulează\n  Then   `roadmap_behind` este egal cu `{\"todo\": \"v2.8\", \"requirements\": \"v2.13\"}`, `health`\n         iese cu 0, iar scorul de health rămâne neschimbat — semnalul este read-only, la fel ca\n         omologul lui pentru titlu neversionat\n\nCASE-4 — fără semnal behind când foaia de parcurs este curentă sau înainte\n  Given  un titlu cel mai nou din `TODO.md` `## v2.16` și o cerință cu `milestone: v2.13`\n  When   `health --json` rulează\n  Then   payload-ul nu poartă nicio cheie `roadmap_behind`\n\nCASE-5 — v2.10 se clasează peste v2.9 sub _version_key\n  Given  șirurile `\"v2.10\"` și `\"v2.9\"`\n  When   `_version_key` este aplicat fiecăruia și comparat\n  Then   `_version_key(\"v2.10\") > _version_key(\"v2.9\")`, spre deosebire de o comparație simplă de șiruri\n\nCASE-6 — un titlu non-versiune este listat sub roadmap_unversioned_headings\n  Given  un `TODO.md` cu `## v2.16` urmat mai târziu de `## Deferred work`\n  When   `health --json` rulează\n  Then   `roadmap_unversioned_headings` este egal cu `[\"Deferred work\"]`\n\nCASE-7 — un item aflat sub un titlu non-versiune este clasificat sub milestone-ul anterior\n  Given  `## v2.16` urmat de `## Deferred work` urmat de un item de checklist\n  When   `_parse_todos_from_text` parsează textul\n  Then   câmpul `milestone` al acelui item citește `\"v2.16\"`, nu `\"Deferred work\"`"
        }
      }
    },
    {
      "id": "ARCH-RULES-059",
      "layer": "bus",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "The gate rule registry",
      "intent": "`cmd_check` used to be four hundred lines of inline checks, and every other command that needed to know \"what is wrong with this corpus\" re-derived the answer its own way. That is how `gate`, `health`, `next` and `confirm` came to disagree about who is exempt (ADR-0015) and which requirement is oversize (v3.1.0). This makes the rule list one registry that every consumer reads, gives each rule a permanent code, and lets a requirement opt out of one rule by that code.",
      "contract": [
        "`GATE_RULES` holds every gate rule as a `Rule` with a unique `RMnnn` code, a default severity, a `--strict` promotion flag and the function that produces its findings. [[REQ-RULES-947]]",
        "Every printed gate line carries its rule code, `--json` carries the same findings as records, and `gate_exempt: [RMnnn]` in a requirement's frontmatter silences that one rule for that one requirement. [[REQ-RULES-948]]"
      ],
      "verify": [],
      "notes": [
        "A retired rule keeps its number: a consumer may already write it in `gate_exempt:`.",
        "`RM000` is reserved for the `--since` fallback notice, which is not a rule.",
        "-------------------"
      ],
      "current_impl": [],
      "acc": [
        "CASE-1 — Given  the engine module is imported When   `GATE_RULES` is read Then   every rule has a distinct `RM` code and a severity of `error` or `warn`",
        "CASE-2 — Given  a corpus with a dangling tag and a confirmed requirement without code When   `gate` and `health` run over it Then   `health`'s `gate_errors` count equals the number of RM001 and RM006 findings the gate prints",
        "CASE-3 — Given  a requirement whose `gate_exempt:` names `RM013` When   `gate` runs Then   no RM013 line names that requirement, and every other rule still applies to it"
      ],
      "accept": "CASE-1\n  Given  the engine module is imported\n  When   `GATE_RULES` is read\n  Then   every rule has a distinct `RM` code and a severity of `error` or `warn`\n\nCASE-2\n  Given  a corpus with a dangling tag and a confirmed requirement without code\n  When   `gate` and `health` run over it\n  Then   `health`'s `gate_errors` count equals the number of RM001 and RM006 findings the gate prints\n\nCASE-3\n  Given  a requirement whose `gate_exempt:` names `RM013`\n  When   `gate` runs\n  Then   no RM013 line names that requirement, and every other rule still applies to it",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-CHECK-006",
        "ARCH-PARSE-001"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-GATE-102"
      ],
      "satisfied_by": [
        "REQ-RULES-947",
        "REQ-RULES-948"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:858"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:872"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:887"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2393"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2434"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2844"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2887"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7832"
        }
      ],
      "test_exempt": null,
      "milestone": "v3.3",
      "priority": "must-have",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Registrul de reguli al gate-ului",
          "intent": "`cmd_check` obișnuia să fie patru sute de linii de verificări inline, iar fiecare altă comandă care avea nevoie să știe \"ce este în neregulă cu acest corpus\" re-deriva răspunsul în felul ei propriu. Așa au ajuns `gate`, `health`, `next` și `confirm` să nu fie de acord asupra cine este exceptat (ADR-0015) și care cerință este supradimensionată (v3.1.0). Aceasta face din lista de reguli un singur registru pe care îl citește fiecare consumator, dă fiecărei reguli un cod permanent, și permite unei cerințe să renunțe la o regulă prin acel cod.",
          "contract": "> `cmd_check` obișnuia să fie patru sute de linii de verificări inline, iar fiecare\n> altă comandă care avea nevoie să știe \"ce este în neregulă cu acest corpus\"\n> re-deriva răspunsul în felul ei propriu. Așa au ajuns `gate`, `health`, `next` și\n> `confirm` să nu fie de acord asupra cine este exceptat (ADR-0015) și care cerință\n> este supradimensionată (v3.1.0). Aceasta face din lista de reguli un singur\n> registru pe care îl citește fiecare consumator, dă fiecărei reguli un cod\n> permanent, și permite unei cerințe să renunțe la o regulă prin acel cod.\n\nFiecare punct de mai jos este obligatoriu.\n- `GATE_RULES` conține fiecare regulă de gate ca un `Rule` cu un cod unic `RMnnn`, o severitate implicită, un flag de promovare `--strict` și funcția care produce constatările sale. [[REQ-RULES-947]]\n- Fiecare linie de gate afișată poartă codul regulii sale, `--json` poartă aceleași constatări ca înregistrări, iar `gate_exempt: [RMnnn]` în frontmatter-ul unei cerințe reduce la tăcere acea regulă pentru acea cerință. [[REQ-RULES-948]]",
          "acceptance": "CASE-1\n  Given  modulul motorului este importat\n  When   `GATE_RULES` este citit\n  Then   fiecare regulă are un cod `RM` distinct și o severitate `error` sau `warn`\n\nCASE-2\n  Given  un corpus cu o etichetă suspendată și o cerință confirmată fără cod\n  When   `gate` și `health` rulează pe el\n  Then   `health` are un `gate_errors` egal cu numărul de constatări RM001 și RM006 pe care le afișează gate-ul\n\nCASE-3\n  Given  o cerință al cărei `gate_exempt:` numește `RM013`\n  When   `gate` rulează\n  Then   nicio linie RM013 nu numește acea cerință, iar fiecare altă regulă i se aplică în continuare"
        }
      }
    },
    {
      "id": "REQ-RULES-947",
      "layer": "bus",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "One registry of gate rules",
      "intent": "A rule is a function that yields `(requirement id, message)` pairs over a `GateContext`, registered with `@gate_rule(\"RMnnn\", severity)`. `cmd_check` is only the runner: it builds the context once, runs the registry in order, prints, and advances the lock. Without a registry, adding a check means editing the runner, and every other consumer of the same fact goes stale.",
      "contract": [
        "`gate_rule(code, severity, strict=False, only_source_repo=False)` registers a rule in `GATE_RULES`; registering a code twice raises `ValueError`.",
        "`run_gate_rules(ctx, strict)` runs every rule in registry order. It returns two `Finding` lists, errors then warnings; each finding carries `rule`, `severity`, `rid`, `msg`.",
        "A rule with `strict=True` has its findings promoted to errors under `--strict`, except RM012 (test-link integrity) on a requirement that is not `confirmed`, which stays a warning.",
        "A rule with `only_source_repo=True` runs only when `_is_source_repo(code_root)` is true, that is inside the requirement-manager repository itself.",
        "`_link_sync_errors`, which `health` reads, returns the messages of RM001 and RM006 taken from the registry, so `health` and `gate` cannot count link-sync errors differently.",
        "`GateContext` computes once what rules read: the id set, both reverse indexes (`satisfies`, `depends_on`), the lock with the fresh binding hashes, the coverage maps."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — codes are unique and severities valid Given  the engine module When   `GATE_RULES` is inspected Then   no two rules share a code and each severity is `error` or `warn`",
        "CASE-2 — — a duplicate code is refused Given  a rule already registered as `RM001` When   `gate_rule(\"RM001\", \"warn\")` decorates another function Then   it raises `ValueError`",
        "CASE-3 — — strict promotes a strict-flagged rule only Given  a corpus with a confirmed drifted contract and a milestone typo When   `run_gate_rules(ctx, strict=True)` runs Then   the DRIFT finding is an error and the milestone finding stays a warning",
        "CASE-4 — — health and gate agree on link-sync errors Given  a corpus with one dangling tag and one confirmed requirement without an implements member When   `_link_sync_errors` runs Then   it returns exactly the two messages RM001 and RM006 produce",
        "CASE-5 — — a source-repo-only rule never runs in a consumer repo Given  a temporary consumer repo with an `app/src/lib/data.js` fixture that disagrees with its corpus When   `gate` runs there Then   no RM017 finding is printed --------------------"
      ],
      "accept": "CASE-1 — codes are unique and severities valid\n  Given  the engine module\n  When   `GATE_RULES` is inspected\n  Then   no two rules share a code and each severity is `error` or `warn`\n\nCASE-2 — a duplicate code is refused\n  Given  a rule already registered as `RM001`\n  When   `gate_rule(\"RM001\", \"warn\")` decorates another function\n  Then   it raises `ValueError`\n\nCASE-3 — strict promotes a strict-flagged rule only\n  Given  a corpus with a confirmed drifted contract and a milestone typo\n  When   `run_gate_rules(ctx, strict=True)` runs\n  Then   the DRIFT finding is an error and the milestone finding stays a warning\n\nCASE-4 — health and gate agree on link-sync errors\n  Given  a corpus with one dangling tag and one confirmed requirement without an implements member\n  When   `_link_sync_errors` runs\n  Then   it returns exactly the two messages RM001 and RM006 produce\n\nCASE-5 — a source-repo-only rule never runs in a consumer repo\n  Given  a temporary consumer repo with an `app/src/lib/data.js` fixture that disagrees with its corpus\n  When   `gate` runs there\n  Then   no RM017 finding is printed\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-RULES-059"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:887"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2434"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2487"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2542"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2844"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2866"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7832"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Un singur registru de reguli de gate",
          "intent": "O regulă este o funcție care produce perechi `(requirement id, message)` peste un `GateContext`, înregistrată cu `@gate_rule(\"RMnnn\", severity)`. `cmd_check` este doar executorul: construiește contextul o singură dată, rulează registrul în ordine, afișează, și avansează lock-ul. Fără un registru, adăugarea unei verificări înseamnă editarea executorului, și orice alt consumator al aceluiași fapt devine învechit.",
          "contract": "> O regulă este o funcție care produce perechi `(requirement id, message)` peste un\n> `GateContext`, înregistrată cu `@gate_rule(\"RMnnn\", severity)`. `cmd_check` este doar\n> executorul: construiește contextul o singură dată, rulează registrul în ordine, afișează, și\n> avansează lock-ul. Fără un registru, adăugarea unei verificări înseamnă editarea executorului, și\n> orice alt consumator al aceluiași fapt devine învechit.\n\nFiecare punct de mai jos este obligatoriu.\n- `gate_rule(code, severity, strict=False, only_source_repo=False)` înregistrează o regulă în `GATE_RULES`; înregistrarea unui cod de două ori ridică `ValueError`.\n- `run_gate_rules(ctx, strict)` rulează fiecare regulă în ordinea din registru. Returnează două liste de `Finding`, erori apoi avertismente; fiecare constatare poartă `rule`, `severity`, `rid`, `msg`.\n- O regulă cu `strict=True` are constatările ei promovate la erori sub `--strict`, cu excepția RM012 (integritatea legăturii de test) pe o cerință care nu este `confirmed`, care rămâne un avertisment.\n- O regulă cu `only_source_repo=True` rulează doar când `_is_source_repo(code_root)` este adevărat, adică în interiorul repo-ului requirement-manager însuși.\n- `_link_sync_errors`, pe care `health` îl citește, returnează mesajele RM001 și RM006 preluate din registru, astfel `health` și `gate` nu pot număra diferit erorile de link-sync.\n- `GateContext` calculează o singură dată ce citesc regulile: setul de id-uri, ambii indecși inversi (`satisfies`, `depends_on`), lock-ul cu hash-urile de legare proaspete, hărțile de acoperire.",
          "acceptance": "CASE-1 — codurile sunt unice și severitățile valide\n  Given  modulul motorului\n  When   `GATE_RULES` este inspectat\n  Then   nicio două reguli nu partajează un cod, iar fiecare severitate este `error` sau `warn`\n\nCASE-2 — un cod duplicat este refuzat\n  Given  o regulă deja înregistrată ca `RM001`\n  When   `gate_rule(\"RM001\", \"warn\")` decorează o altă funcție\n  Then   ridică `ValueError`\n\nCASE-3 — strict promovează doar o regulă marcată strict\n  Given  un corpus cu un contract confirmat aflat în drift și o greșeală de tipar la milestone\n  When   `run_gate_rules(ctx, strict=True)` rulează\n  Then   constatarea DRIFT este o eroare, iar constatarea de milestone rămâne un avertisment\n\nCASE-4 — health și gate sunt de acord asupra erorilor de link-sync\n  Given  un corpus cu un tag suspendat și o cerință confirmată fără membru implements\n  When   `_link_sync_errors` rulează\n  Then   returnează exact cele două mesaje pe care RM001 și RM006 le produc\n\nCASE-5 — o regulă doar-pentru-source-repo nu rulează niciodată într-un repo consumator\n  Given  un repo consumator temporar cu un fixture `app/src/lib/data.js` care nu concordă cu corpusul lui\n  When   `gate` rulează acolo\n  Then   nicio constatare RM017 nu este afișată\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-RULES-948",
      "layer": "bus",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Codes on every finding, and per-requirement exemption",
      "intent": "A finding you can name is a finding you can silence, count, or document. Each gate line starts with its severity and its code, `WARN  RM013 <message>`, the JSON output carries the same finding as a record, and a requirement can declare `gate_exempt: [RM013]` to switch that one rule off for itself, the way `lint_exempt:` already works for the linter.",
      "contract": [
        "`gate` prints each finding as `WARN  RMnnn <message>` or `ERROR RMnnn <message>`; the message text is unchanged from before codes existed.",
        "`gate --json` emits `errors` and `warnings` as message lists, unchanged, plus `findings`, a list of `{rule, severity, rid, msg}` records.",
        "A requirement whose `gate_exempt:` list names a rule's code is skipped by that rule; a finding with no requirement id (a corpus-wide finding) cannot be exempted.",
        "An exemption is per requirement and per rule: naming `RM013` on one requirement changes nothing for any other requirement or rule.",
        "Rule codes are permanent: a retired rule's code is never reassigned."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the code is printed with the severity Given  a corpus with a malformed `milestone:` When   `gate` runs Then   the output contains `WARN  RM004` followed by the milestone message",
        "CASE-2 — — JSON carries findings as records Given  the same corpus When   `gate --json` runs Then  the object has a `findings` list whose entry for RM004 carries `severity` `warn` and the requirement's id, and `warnings` still lists the message string",
        "CASE-3 — — gate_exempt silences one rule for one requirement Given  two requirements with malformed milestones, one carrying `gate_exempt: [RM004]` When   `gate` runs Then   only the other requirement's milestone warning is printed",
        "CASE-4 — — an exemption does not reach another rule Given  a requirement carrying `gate_exempt: [RM004]` that is also confirmed without a `tested-by` member When   `gate` runs Then   its RM007 warning is still printed"
      ],
      "accept": "CASE-1 — the code is printed with the severity\n  Given  a corpus with a malformed `milestone:`\n  When   `gate` runs\n  Then   the output contains `WARN  RM004` followed by the milestone message\n\nCASE-2 — JSON carries findings as records\n  Given  the same corpus\n  When   `gate --json` runs\n  Then  the object has a `findings` list whose entry for RM004 carries `severity` `warn` and the requirement's id, and `warnings` still lists the message string\n\nCASE-3 — gate_exempt silences one rule for one requirement\n  Given  two requirements with malformed milestones, one carrying `gate_exempt: [RM004]`\n  When   `gate` runs\n  Then   only the other requirement's milestone warning is printed\n\nCASE-4 — an exemption does not reach another rule\n  Given  a requirement carrying `gate_exempt: [RM004]` that is also confirmed without a `tested-by` member\n  When   `gate` runs\n  Then   its RM007 warning is still printed",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-RULES-059"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:858"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2844"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2887"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7832"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Coduri pe fiecare constatare, și exceptare per cerință",
          "intent": "O constatare pe care o poți numi este o constatare pe care o poți reduce la tăcere, număra, sau documenta. Fiecare linie de gate începe cu severitatea și codul ei, `WARN  RM013 <message>`, ieșirea JSON poartă aceeași constatare ca înregistrare, iar o cerință poate declara `gate_exempt: [RM013]` pentru a dezactiva acea regulă pentru ea însăși, așa cum `lint_exempt:` funcționează deja pentru linter.",
          "contract": "> O constatare pe care o poți numi este o constatare pe care o poți reduce la tăcere, număra,\n> sau documenta. Fiecare linie de gate începe cu severitatea și codul ei, `WARN  RM013 <message>`,\n> ieșirea JSON poartă aceeași constatare ca înregistrare, iar o cerință poate declara\n> `gate_exempt: [RM013]` pentru a dezactiva acea regulă pentru ea însăși, așa cum `lint_exempt:`\n> funcționează deja pentru linter.\n\nFiecare punct de mai jos este obligatoriu.\n- `gate` afișează fiecare constatare ca `WARN  RMnnn <message>` sau `ERROR RMnnn <message>`; textul mesajului este neschimbat față de dinaintea existenței codurilor.\n- `gate --json` emite `errors` și `warnings` ca liste de mesaje, neschimbate, plus `findings`, o listă de înregistrări `{rule, severity, rid, msg}`.\n- O cerință a cărei listă `gate_exempt:` numește codul unei reguli este omisă de acea regulă; o constatare fără id de cerință (o constatare la nivel de corpus) nu poate fi exceptată.\n- O exceptare este per cerință și per regulă: numirea `RM013` pe o cerință nu schimbă nimic pentru nicio altă cerință sau regulă.\n- Codurile de reguli sunt permanente: codul unei reguli retrase nu este niciodată reatribuit.",
          "acceptance": "CASE-1 — codul este afișat împreună cu severitatea\n  Given  un corpus cu un `milestone:` malformat\n  When   `gate` rulează\n  Then   ieșirea conține `WARN  RM004` urmat de mesajul de milestone\n\nCASE-2 — JSON poartă constatările ca înregistrări\n  Given  același corpus\n  When   `gate --json` rulează\n  Then  obiectul are o listă `findings` a cărei intrare pentru RM004 poartă `severity` `warn` și id-ul cerinței, iar `warnings` listează încă șirul de mesaj\n\nCASE-3 — gate_exempt reduce la tăcere o regulă pentru o cerință\n  Given  două cerințe cu milestone-uri malformate, una purtând `gate_exempt: [RM004]`\n  When   `gate` rulează\n  Then   doar avertismentul de milestone al celeilalte cerințe este afișat\n\nCASE-4 — o exceptare nu ajunge la o altă regulă\n  Given  o cerință purtând `gate_exempt: [RM004]` care este de asemenea confirmată fără un membru `tested-by`\n  When   `gate` rulează\n  Then   avertismentul ei RM007 este afișat în continuare"
        }
      }
    },
    {
      "id": "ARCH-SCAN-002",
      "layer": "bus",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Member discovery",
      "intent": "Developers mark a piece of code as belonging to a capability by writing a short note in a comment, like \"implements: LOGIN-001.\" This reads through all the project's source files and collects every one of those notes, so the tool always knows which code answers to which requirement. Without it, the link between a written requirement and the real code behind it would have to be tracked by hand, and would quickly fall out of date.",
      "contract": [
        "`scan_members` walks a code root and, in every source file with a known extension, finds inline `role: <ID>` tags and returns `cap_id -> [(role, relative_file, line), ...]`. [[REQ-SCAN-908]]",
        "A single tag may bind several requirements through a comma-separated id list, written `role: <ID>, <ID>, ...`, and `scan_all` runs member, per-criterion and test-level scanning in one walk. [[REQ-SCAN-909]]"
      ],
      "verify": [],
      "notes": [
        "The member list is discovered, never hand-maintained — that is the first thing to rot.",
        "The realpath comparison for the SSOT directory runs only for a directory whose name matches it; resolving every directory was 62% of one consumer's gate time (4,900 upload folders). A directory that a `.reqmapignore` pattern ending in `/**` or `/*` covers is not descended at all — every file under it matched the pattern anyway, so results are identical.",
        "Scanned schema files and basenames grew from the first consumer evidence run (2026-08-25): `.prisma`, `.graphql`, `.proto`, `Caddyfile`, `Jenkinsfile`, `Procfile`, `Vagrantfile`, and any `Dockerfile.<variant>`; the five-repo matrix run the same day added `.scss/.sass/.less`, `.vue/.svelte`, `.mjs/.cjs/.mts/.cts`, `.cs/.php/.rb/.kt/.kts/.swift/.scala/.ex/.exs/.dart` and `.toml`. A tag in any other type is reported by [[ARCH-UNSCANNEDTAG-045]] rather than lost."
      ],
      "current_impl": [
        "`scan_members`, `_prune_dirs`, `load_ignore`, `TAG_RE`, `TAG_LIST_RE`, `_findall_tags` in `reqmap.py`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a file containing `# implements: <ID>` When   the scan runs Then   a member `(implements, file, line)` is recorded under `<ID>`",
        "CASE-2 — Given  tags using each of the four roles and one unknown role string When   the scan runs Then   all four roles are recognized and the unknown role produces no member",
        "CASE-3 — Given  files under `.git`, `node_modules`, `__pycache__` or the SSOT `requirements/` When   the scan runs Then   those directories are not scanned",
        "CASE-4 — Given  a duplicate tag on a single line When   the scan runs Then   it is deduplicated and member paths are POSIX-relative",
        "CASE-5 — Given  an unreadable file in the tree When   the scan runs Then   the file is skipped without aborting the scan",
        "CASE-6 — Given  a tag `generated-from: <ID-A>, <ID-B>` listing several ids When   the scan runs Then   the file is recorded as a member of both <ID-A> and <ID-B>",
        "CASE-7 — Given  any code tree When   `scan_all` runs Then   its three results equal those of `scan_members`, `scan_ac_verifies` and `scan_test_levels` run separately, and every file is opened once"
      ],
      "accept": "CASE-1\n  Given  a file containing `# implements: <ID>`\n  When   the scan runs\n  Then   a member `(implements, file, line)` is recorded under `<ID>`\n\nCASE-2\n  Given  tags using each of the four roles and one unknown role string\n  When   the scan runs\n  Then   all four roles are recognized and the unknown role produces no member\n\nCASE-3\n  Given  files under `.git`, `node_modules`, `__pycache__` or the SSOT `requirements/`\n  When   the scan runs\n  Then   those directories are not scanned\n\nCASE-4\n  Given  a duplicate tag on a single line\n  When   the scan runs\n  Then   it is deduplicated and member paths are POSIX-relative\n\nCASE-5\n  Given  an unreadable file in the tree\n  When   the scan runs\n  Then   the file is skipped without aborting the scan\n\nCASE-6\n  Given  a tag `generated-from: <ID-A>, <ID-B>` listing several ids\n  When   the scan runs\n  Then   the file is recorded as a member of both <ID-A> and <ID-B>\n\nCASE-7\n  Given  any code tree\n  When   `scan_all` runs\n  Then   its three results equal those of `scan_members`, `scan_ac_verifies` and\n         `scan_test_levels` run separately, and every file is opened once",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [
        "ARCH-ACVERIFY-019",
        "ARCH-CANDIDATES-009",
        "ARCH-CHECK-006",
        "ARCH-COVERAGE-029",
        "ARCH-DESIGN-061",
        "ARCH-DOCBUNDLE-026",
        "ARCH-EXTRACT-008",
        "ARCH-HEALTH-017",
        "ARCH-IMPLEMENT-063",
        "ARCH-MAP-007",
        "ARCH-MEMBERDRIFT-027",
        "ARCH-ORPHANCODE-034",
        "ARCH-PROSE-024",
        "ARCH-RETIRE-064",
        "ARCH-SCANCACHE-023",
        "ARCH-SHOW-015",
        "ARCH-SUGGESTVERIFIES-047",
        "ARCH-TRACKED-042",
        "ARCH-UNSCANNEDTAG-045",
        "ARCH-VLEVEL-037"
      ],
      "satisfies": [
        "SYS-READ-103"
      ],
      "satisfied_by": [
        "REQ-SCAN-908",
        "REQ-SCAN-909"
      ],
      "members": [
        {
          "role": "generated-from",
          "loc": "docs/full_architecture.html:4"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:120"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1044"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1077"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1146"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1261"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1311"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1368"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:341"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:547"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:5670"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6696"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6821"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.00",
      "priority": "",
      "risks": [],
      "clauses": 7,
      "covered": 7,
      "i18n": {
        "ro": {
          "title": "Descoperirea membrilor",
          "intent": "Dezvoltatorii marchează o bucată de cod ca aparținând unei capabilități scriind o notă scurtă într-un comentariu, precum \"implements: LOGIN-001.\" Aceasta citește prin toate fișierele sursă ale proiectului și colectează fiecare dintre acele note, astfel încât instrumentul știe întotdeauna ce cod răspunde la ce cerință. Fără aceasta, legătura dintre o cerință scrisă și codul real din spatele ei ar trebui urmărită manual, și ar deveni rapid învechită.",
          "contract": "> Dezvoltatorii marchează o bucată de cod ca aparținând unei capabilități scriind o\n> notă scurtă într-un comentariu, precum \"implements: LOGIN-001.\" Aceasta citește\n> prin toate fișierele sursă ale proiectului și colectează fiecare dintre acele note,\n> astfel încât instrumentul știe întotdeauna ce cod răspunde la ce cerință. Fără\n> aceasta, legătura dintre o cerință scrisă și codul real din spatele ei ar trebui\n> urmărită manual, și ar deveni rapid învechită.\n\nFiecare punct de mai jos este obligatoriu.\n- `scan_members` parcurge o rădăcină de cod și, în fiecare fișier sursă cu o extensie cunoscută, găsește etichete inline `role: <ID>` și returnează `cap_id -> [(role, relative_file, line), ...]`. [[REQ-SCAN-908]]\n- O singură etichetă poate lega mai multe cerințe printr-o listă de id-uri separate prin virgulă, scrisă `role: <ID>, <ID>, ...`, iar `scan_all` rulează scanarea de membru, per-criteriu și de nivel de test într-o singură parcurgere. [[REQ-SCAN-909]]",
          "acceptance": "CASE-1\n  Given  un fișier care conține `# implements: <ID>`\n  When   scanarea rulează\n  Then   un membru `(implements, file, line)` este înregistrat sub `<ID>`\n\nCASE-2\n  Given  etichete care folosesc fiecare dintre cele patru roluri și un șir de rol necunoscut\n  When   scanarea rulează\n  Then   toate cele patru roluri sunt recunoscute iar rolul necunoscut nu produce niciun membru\n\nCASE-3\n  Given  fișiere sub `.git`, `node_modules`, `__pycache__` sau SSOT `requirements/`\n  When   scanarea rulează\n  Then   acele directoare nu sunt scanate\n\nCASE-4\n  Given  o etichetă duplicată pe o singură linie\n  When   scanarea rulează\n  Then   este dedublată iar căile membrilor sunt POSIX-relative\n\nCASE-5\n  Given  un fișier ilizibil în arbore\n  When   scanarea rulează\n  Then   fișierul este sărit fără a întrerupe scanarea\n\nCASE-6\n  Given  o etichetă `generated-from: <ID-A>, <ID-B>` care listează mai multe id-uri\n  When   scanarea rulează\n  Then   fișierul este înregistrat ca membru al ambelor <ID-A> și <ID-B>\n\nCASE-7\n  Given  orice arbore de cod\n  When   `scan_all` rulează\n  Then   cele trei rezultate ale sale sunt egale cu cele ale `scan_members`, `scan_ac_verifies` și\n         `scan_test_levels` rulate separat, iar fiecare fișier este deschis o singură dată"
        }
      }
    },
    {
      "id": "REQ-SCAN-908",
      "layer": "bus",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "scan_members walks the tree and collects role: ID tags",
      "intent": "`scan_members` is how the engine learns which code answers to which requirement, without anyone hand-maintaining a list. It walks every source file with a known extension, finds comments like `# implements: LOGIN-001`, and returns them keyed by requirement id — a left-boundary guard keeps a stray `reimplements:` from being read as a real tag.",
      "contract": [
        "`scan_members` walks a code root and, in every source file with a known extension, finds the inline tags.",
        "`scan_members` returns `cap_id -> [(role, relative_file, line), ...]`.",
        "A role is one of `implements`, `generated-from`, `validated-against` and `tested-by`.",
        "A tag ID matches `[A-Z][A-Z0-9]*(-[A-Z0-9]+)+`.",
        "A left-boundary guard prevents a substring match such as `reimplements:` or `x-implements:` being read as a real tag.",
        "The same `(role, ID)` appearing twice on one line is recorded once.",
        "File paths are reported repo-root-relative, with POSIX separators."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — an implements: tag in a scanned file is picked up by the walk Given  `a.py` at the code root carrying `# implements: REQ-T-001` When   `scan_members(root, None)` runs Then   `\"REQ-T-001\"` is a key in the returned dict",
        "CASE-2 — — a member entry is a (role, relative_file, line) tuple Given  `a.py` at the code root carrying `# implements: REQ-T-001` on line 1 When   `scan_members(root, None)` runs Then   `members[\"REQ-T-001\"] == [(\"implements\", \"a.py\", 1)]`",
        "CASE-3 — — all four roles are recognized and an unknown role string is not Given  one file carrying the four real role tags plus a `refines:` tag, all for one id When   `scan_members` runs Then   the recorded roles are exactly `implements`, `generated-from`, `validated-against`, `tested-by` — `refines` produces no member",
        "CASE-4 — — TAG_RE only matches an uppercase, hyphenated id shape Given  the strings `\"implements: FOO-BAR-001\"` and `\"implements: foobar\"` When   `TAG_RE.findall` runs on each Then   the first yields `[(\"implements\", \"FOO-BAR-001\")]` and the second yields `[]`",
        "CASE-5 — — reimplements: and x-implements: are not read as real tags Given  the lines `\"# reimplements: FOO-BAR-001\"` and `\"auto-implements: AB-CD-001\"` When   `TAG_RE.findall` runs on each Then   both return `[]`",
        "CASE-6 — — a duplicated tag on one line is recorded only once Given  one line carrying `implements: FOO-BAR-001` twice When   `scan_members` runs Then   `len(members[\"FOO-BAR-001\"]) == 1`",
        "CASE-7 — — a nested file's member path uses forward slashes, root-relative Given  a tag inside `sub/dir/m.py`, discovered via `scan_members(root, None)` When   the recorded member is read Then   its path field equals `\"sub/dir/m.py\"` (POSIX separators, even on Windows) --------------------"
      ],
      "accept": "CASE-1 — an implements: tag in a scanned file is picked up by the walk\n  Given  `a.py` at the code root carrying `# implements: REQ-T-001`\n  When   `scan_members(root, None)` runs\n  Then   `\"REQ-T-001\"` is a key in the returned dict\n\nCASE-2 — a member entry is a (role, relative_file, line) tuple\n  Given  `a.py` at the code root carrying `# implements: REQ-T-001` on line 1\n  When   `scan_members(root, None)` runs\n  Then   `members[\"REQ-T-001\"] == [(\"implements\", \"a.py\", 1)]`\n\nCASE-3 — all four roles are recognized and an unknown role string is not\n  Given  one file carrying the four real role tags plus a `refines:` tag, all for one id\n  When   `scan_members` runs\n  Then   the recorded roles are exactly `implements`, `generated-from`, `validated-against`,\n         `tested-by` — `refines` produces no member\n\nCASE-4 — TAG_RE only matches an uppercase, hyphenated id shape\n  Given  the strings `\"implements: FOO-BAR-001\"` and `\"implements: foobar\"`\n  When   `TAG_RE.findall` runs on each\n  Then   the first yields `[(\"implements\", \"FOO-BAR-001\")]` and the second yields `[]`\n\nCASE-5 — reimplements: and x-implements: are not read as real tags\n  Given  the lines `\"# reimplements: FOO-BAR-001\"` and `\"auto-implements: AB-CD-001\"`\n  When   `TAG_RE.findall` runs on each\n  Then   both return `[]`\n\nCASE-6 — a duplicated tag on one line is recorded only once\n  Given  one line carrying `implements: FOO-BAR-001` twice\n  When   `scan_members` runs\n  Then   `len(members[\"FOO-BAR-001\"]) == 1`\n\nCASE-7 — a nested file's member path uses forward slashes, root-relative\n  Given  a tag inside `sub/dir/m.py`, discovered via `scan_members(root, None)`\n  When   the recorded member is read\n  Then   its path field equals `\"sub/dir/m.py\"` (POSIX separators, even on Windows)\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SCAN-002"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1146"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1311"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1368"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:341"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 7,
      "covered": 7,
      "i18n": {
        "ro": {
          "title": "scan_members parcurge arborele și colectează tag-uri role: ID",
          "intent": "`scan_members` este modul în care motorul află ce cod răspunde de ce cerință, fără ca nimeni să întrețină manual o listă. Parcurge fiecare fișier sursă cu o extensie cunoscută, găsește comentarii precum `# implements: LOGIN-001`, și le returnează grupate după id-ul cerinței — o gardă de graniță din stânga împiedică ca un `reimplements:` accidental să fie citit ca un tag real.",
          "contract": "> `scan_members` este modul în care motorul află ce cod răspunde de ce cerință, fără ca\n> nimeni să întrețină manual o listă. Parcurge fiecare fișier sursă cu o extensie cunoscută,\n> găsește comentarii precum `# implements: LOGIN-001`, și le returnează grupate după id-ul\n> cerinței — o gardă de graniță din stânga împiedică ca un `reimplements:` accidental să fie\n> citit ca un tag real.\n\nFiecare punct de mai jos este obligatoriu.\n- `scan_members` parcurge un code root și, în fiecare fișier sursă cu o extensie cunoscută,\n  găsește tag-urile inline.\n- `scan_members` returnează `cap_id -> [(role, relative_file, line), ...]`.\n- Un role este unul dintre `implements`, `generated-from`, `validated-against` și `tested-by`.\n- Un ID de tag se potrivește cu `[A-Z][A-Z0-9]*(-[A-Z0-9]+)+`.\n- O gardă de graniță din stânga previne ca o potrivire de substring precum `reimplements:` sau\n  `x-implements:` să fie citită ca un tag real.\n- Aceeași pereche `(role, ID)` apărând de două ori pe o linie este înregistrată o singură dată.\n- Căile fișierelor sunt raportate relativ la rădăcina repo-ului, cu separatori POSIX.",
          "acceptance": "CASE-1 — un tag implements: dintr-un fișier scanat este preluat de parcurgere\n  Given  `a.py` la code root purtând `# implements: REQ-T-001`\n  When   `scan_members(root, None)` rulează\n  Then   `\"REQ-T-001\"` este o cheie în dicționarul returnat\n\nCASE-2 — o intrare de membru este un tuplu (role, relative_file, line)\n  Given  `a.py` la code root purtând `# implements: REQ-T-001` pe linia 1\n  When   `scan_members(root, None)` rulează\n  Then   `members[\"REQ-T-001\"] == [(\"implements\", \"a.py\", 1)]`\n\nCASE-3 — toate cele patru role-uri sunt recunoscute, iar un string de role necunoscut nu este\n  Given  un fișier purtând cele patru tag-uri reale de role plus un tag `refines:`, toate pentru un id\n  When   `scan_members` rulează\n  Then   role-urile înregistrate sunt exact `implements`, `generated-from`, `validated-against`,\n         `tested-by` — `refines` nu produce niciun membru\n\nCASE-4 — TAG_RE se potrivește doar cu o formă de id majusculă, cu cratimă\n  Given  șirurile `\"implements: FOO-BAR-001\"` și `\"implements: foobar\"`\n  When   `TAG_RE.findall` rulează pe fiecare\n  Then   primul produce `[(\"implements\", \"FOO-BAR-001\")]`, iar al doilea produce `[]`\n\nCASE-5 — reimplements: și x-implements: nu sunt citite ca tag-uri reale\n  Given  liniile `\"# reimplements: FOO-BAR-001\"` și `\"auto-implements: AB-CD-001\"`\n  When   `TAG_RE.findall` rulează pe fiecare\n  Then   ambele returnează `[]`\n\nCASE-6 — un tag duplicat pe o linie este înregistrat o singură dată\n  Given  o linie purtând `implements: FOO-BAR-001` de două ori\n  When   `scan_members` rulează\n  Then   `len(members[\"FOO-BAR-001\"]) == 1`\n\nCASE-7 — calea unui membru dintr-un fișier imbricat folosește slash-uri directe, relativ la rădăcină\n  Given  un tag în interiorul `sub/dir/m.py`, descoperit via `scan_members(root, None)`\n  When   membrul înregistrat este citit\n  Then   câmpul lui de cale este egal cu `\"sub/dir/m.py\"` (separatori POSIX, chiar și pe Windows)\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-SCAN-909",
      "layer": "bus",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "One tag can bind several requirements, and directories are pruned",
      "intent": "A comma-separated tag (`generated-from: A, B, C`) lets one file — a whole-system doc, a shared fixture — answer to several requirements at once, each getting the same recorded location. The walk also has to know what NOT to scan: `.git`, `node_modules`, the SSOT `requirements/` dir itself, and anything `.reqmapignore` excludes, or every scan would waste time on generated noise and false members inside its own output.",
      "contract": [
        "A single tag may bind several requirements through a comma-separated id list, written `role: <ID>, <ID>, ...`.",
        "Each id in that list is recorded as a member of the same `(role, file, line)`.",
        "A whole-system doc generated from many requirements (`generated-from: A, B, C`) is therefore a member of each, and drifts when ANY of them changes.",
        "`.git`, `node_modules`, `__pycache__` and the SSOT `requirements/` directory are skipped.",
        "The SSOT directory is matched by realpath, so a source package merely named `requirements/` is still scanned.",
        "Paths matching `.reqmapignore` are excluded.",
        "An unreadable file is skipped without aborting the scan.",
        "`scan_all` returns the members, the per-criterion coverage and the verification levels from a single walk, and each result equals what the three separate scanners return."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a comma-separated tag registers members under every listed id Given  `` in `docs/arch.html` When   `scan_members` runs Then   both `\"REQ-MA-001\"` and `\"REQ-MB-002\"` are keys in the result",
        "CASE-2 — — every id in a multi-id tag carries the identical (role, file, line) triple Given  `` in `docs/arch.html` When   `scan_members` runs Then   `members[\"REQ-MA-001\"] == members[\"REQ-MB-002\"] == [(\"generated-from\", \"docs/arch.html\", 1)]`, so the doc drifts when either requirement's contract changes",
        "CASE-3 — — a tag inside the real SSOT requirements/ dir is not scanned Given  `ignored.py` inside the SSOT `requirements/` dir, tagged `SSOT-IGN-001` When   `scan_members(root, requirements_dir)` runs Then   `\"SSOT-IGN-001\"` is absent from the result",
        "CASE-4 — — a source package named requirements/ that is not the SSOT dir is still scanned Given  a tagged file under `pkg/requirements/impl.py`, distinct from the real SSOT dir When   `scan_members(root, ssot_dir)` runs Then   `\"pkg/requirements/impl.py\"` appears among the recorded member paths",
        "CASE-5 — — a file listed in .reqmapignore is excluded from the scan Given  `.reqmapignore` naming `scripts/reqmap.py`, which itself carries a tag When   `scan_members` runs Then   the tagged id from that file is absent, while an untouched file's id is present",
        "CASE-6 — — an unreadable file yields None instead of raising Given  a path under a directory that does not exist When   `_scan_file_tags(path)` runs Then   it returns `None` rather than raising an exception",
        "CASE-7 — — scan_all's triple equals the three scanners run separately Given  a mixed tree with `.py`, `.ts` and `.md` files carrying tags, `verifies:` and `tested-by:` comments When   `scan_all(root, reqs_dir)` runs Then   it equals `(scan_members(...), scan_ac_verifies(...), scan_test_levels(...))`"
      ],
      "accept": "CASE-1 — a comma-separated tag registers members under every listed id\n  Given  `<!-- generated-from: REQ-MA-001, REQ-MB-002 -->` in `docs/arch.html`\n  When   `scan_members` runs\n  Then   both `\"REQ-MA-001\"` and `\"REQ-MB-002\"` are keys in the result\n\nCASE-2 — every id in a multi-id tag carries the identical (role, file, line) triple\n  Given  `<!-- generated-from: REQ-MA-001, REQ-MB-002 -->` in `docs/arch.html`\n  When   `scan_members` runs\n  Then   `members[\"REQ-MA-001\"] == members[\"REQ-MB-002\"] == [(\"generated-from\",\n         \"docs/arch.html\", 1)]`, so the doc drifts when either requirement's contract changes\n\nCASE-3 — a tag inside the real SSOT requirements/ dir is not scanned\n  Given  `ignored.py` inside the SSOT `requirements/` dir, tagged `SSOT-IGN-001`\n  When   `scan_members(root, requirements_dir)` runs\n  Then   `\"SSOT-IGN-001\"` is absent from the result\n\nCASE-4 — a source package named requirements/ that is not the SSOT dir is still scanned\n  Given  a tagged file under `pkg/requirements/impl.py`, distinct from the real SSOT dir\n  When   `scan_members(root, ssot_dir)` runs\n  Then   `\"pkg/requirements/impl.py\"` appears among the recorded member paths\n\nCASE-5 — a file listed in .reqmapignore is excluded from the scan\n  Given  `.reqmapignore` naming `scripts/reqmap.py`, which itself carries a tag\n  When   `scan_members` runs\n  Then   the tagged id from that file is absent, while an untouched file's id is present\n\nCASE-6 — an unreadable file yields None instead of raising\n  Given  a path under a directory that does not exist\n  When   `_scan_file_tags(path)` runs\n  Then   it returns `None` rather than raising an exception\n\nCASE-7 — scan_all's triple equals the three scanners run separately\n  Given  a mixed tree with `.py`, `.ts` and `.md` files carrying tags, `verifies:` and\n         `tested-by:` comments\n  When   `scan_all(root, reqs_dir)` runs\n  Then   it equals `(scan_members(...), scan_ac_verifies(...), scan_test_levels(...))`",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SCAN-002"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1044"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1077"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:341"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:5670"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 7,
      "covered": 7,
      "i18n": {
        "ro": {
          "title": "Un tag poate lega mai multe cerințe, iar directoarele sunt eliminate din parcurgere",
          "intent": "Un tag separat prin virgule (`generated-from: A, B, C`) permite ca un singur fișier — un document pentru întregul sistem, un fixture partajat — să răspundă simultan de mai multe cerințe, fiecare primind aceeași locație înregistrată. Parcurgerea trebuie de asemenea să știe ce NU trebuie scanat: `.git`, `node_modules`, directorul SSOT `requirements/` însuși, și orice exclude `.reqmapignore`, altfel fiecare scanare ar pierde timp cu zgomot generat și membri falși în interiorul propriei ieșiri.",
          "contract": "> Un tag separat prin virgule (`generated-from: A, B, C`) permite ca un singur fișier — un\n> document pentru întregul sistem, un fixture partajat — să răspundă simultan de mai multe\n> cerințe, fiecare primind aceeași locație înregistrată. Parcurgerea trebuie de asemenea să\n> știe ce NU trebuie scanat: `.git`, `node_modules`, directorul SSOT `requirements/` însuși,\n> și orice exclude `.reqmapignore`, altfel fiecare scanare ar pierde timp cu zgomot generat\n> și membri falși în interiorul propriei ieșiri.\n\nFiecare punct de mai jos este obligatoriu.\n- Un singur tag poate lega mai multe cerințe printr-o listă de id-uri separată prin virgule,\n  scrisă `role: <ID>, <ID>, ...`.\n- Fiecare id din acea listă este înregistrat ca membru al aceluiași `(role, file, line)`.\n- Un document pentru întregul sistem, generat din multe cerințe (`generated-from: A, B, C`), este\n  deci membru al fiecăreia, și intră în drift când ORICARE dintre ele se schimbă.\n- `.git`, `node_modules`, `__pycache__` și directorul SSOT `requirements/` sunt\n  omise.\n- Directorul SSOT este potrivit după realpath, astfel un pachet sursă numit doar\n  `requirements/` este totuși scanat.\n- Căile care se potrivesc cu `.reqmapignore` sunt excluse.\n- Un fișier necitibil este omis fără a întrerupe scanarea.\n- `scan_all` returnează membrii, acoperirea per criteriu și nivelurile de verificare\n  dintr-o singură parcurgere, și fiecare rezultat este egal cu ce returnează cele trei scanere separate.",
          "acceptance": "CASE-1 — un tag separat prin virgule înregistrează membri sub fiecare id listat\n  Given  `<!-- generated-from: REQ-MA-001, REQ-MB-002 -->` în `docs/arch.html`\n  When   `scan_members` rulează\n  Then   atât `\"REQ-MA-001\"` cât și `\"REQ-MB-002\"` sunt chei în rezultat\n\nCASE-2 — fiecare id dintr-un tag multi-id poartă tripletul identic (role, file, line)\n  Given  `<!-- generated-from: REQ-MA-001, REQ-MB-002 -->` în `docs/arch.html`\n  When   `scan_members` rulează\n  Then   `members[\"REQ-MA-001\"] == members[\"REQ-MB-002\"] == [(\"generated-from\",\n         \"docs/arch.html\", 1)]`, astfel documentul intră în drift când oricare cerință își schimbă contractul\n\nCASE-3 — un tag din interiorul directorului real SSOT requirements/ nu este scanat\n  Given  `ignored.py` în interiorul directorului SSOT `requirements/`, etichetat `SSOT-IGN-001`\n  When   `scan_members(root, requirements_dir)` rulează\n  Then   `\"SSOT-IGN-001\"` este absent din rezultat\n\nCASE-4 — un pachet sursă numit requirements/ care nu este directorul SSOT este totuși scanat\n  Given  un fișier etichetat sub `pkg/requirements/impl.py`, distinct de directorul SSOT real\n  When   `scan_members(root, ssot_dir)` rulează\n  Then   `\"pkg/requirements/impl.py\"` apare printre căile de membri înregistrate\n\nCASE-5 — un fișier listat în .reqmapignore este exclus din scanare\n  Given  `.reqmapignore` numind `scripts/reqmap.py`, care poartă el însuși un tag\n  When   `scan_members` rulează\n  Then   id-ul etichetat din acel fișier este absent, în timp ce id-ul unui fișier neatins este prezent\n\nCASE-6 — un fișier necitibil produce None în loc să ridice o excepție\n  Given  o cale sub un director care nu există\n  When   `_scan_file_tags(path)` rulează\n  Then   returnează `None` în loc să ridice o excepție\n\nCASE-7 — tripletul lui scan_all este egal cu cele trei scanere rulate separat\n  Given  un arbore mixt cu fișiere `.py`, `.ts` și `.md` purtând tag-uri, comentarii `verifies:` și\n         `tested-by:`\n  When   `scan_all(root, reqs_dir)` rulează\n  Then   este egal cu `(scan_members(...), scan_ac_verifies(...), scan_test_levels(...))`"
        }
      }
    },
    {
      "id": "ARCH-SCANCACHE-023",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Opt-in scan cache",
      "intent": "Scanning re-reads and re-parses every source file on every run to find membership tags. On a large repo that can be the slow part. This is an opt-in cache: when you ask for it, an unchanged file is skipped on the next scan. It is off by default and changes nothing about the results — only the speed — so the gate stays exactly as deterministic as before.",
      "contract": [
        "The `--cache` flag (off by default) enables a per-file scan cache for the single walk `scan_all`, which also serves `scan_members`,, keyed by `(mtime_ns, size)`, returning results byte-identical to an uncached scan; the default path and the CI/gate path never read or write it unless `--cache` is given. [[REQ-SCANCACHE-911]] details the behaviour."
      ],
      "verify": [],
      "notes": [
        "`(mtime_ns, size)` keying can miss a same-second, same-size content change in theory; this is acceptable because the cache is opt-in and deletable (the default gate path never uses it), so the worst case is a manual `rm requirements/_scancache.json`. Content-hash keying was rejected: it would have to read every file, eliminating the speedup the cache exists to provide.",
        "Since v3.3.0 the one walk `scan_all` is cached, coverage maps included; before that only members were, and may be cached later if it ever becomes a measured bottleneck."
      ],
      "current_impl": [
        "`_scancache_path`, `_load_scancache`, `_save_scancache` in `reqmap.py`; `scan_members` (ARCH-SCAN-002) gains an opt-in `cache=` parameter that reads/writes the sidecar; `--cache` is wired through `main()`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a tagged tree When   `scan_members(cache=True)` runs Then   it equals `scan_members(cache=False)` (identical members) and a `_scancache.json` sidecar is written",
        "CASE-2 — Given  a tagged file's content changes When   a cached re-scan runs Then   it reflects the new tag and drops the old",
        "CASE-3 — Given  a tagged file is deleted When   a cached re-scan runs Then   it omits the file and prunes its cache entry",
        "CASE-4 — Given  no `--cache` When   `scan_members` runs Then   it writes no cache file (off by default)"
      ],
      "accept": "CASE-1\n  Given  a tagged tree\n  When   `scan_members(cache=True)` runs\n  Then   it equals `scan_members(cache=False)` (identical members) and a `_scancache.json`\n         sidecar is written\n\nCASE-2\n  Given  a tagged file's content changes\n  When   a cached re-scan runs\n  Then   it reflects the new tag and drops the old\n\nCASE-3\n  Given  a tagged file is deleted\n  When   a cached re-scan runs\n  Then   it omits the file and prunes its cache entry\n\nCASE-4\n  Given  no `--cache`\n  When   `scan_members` runs\n  Then   it writes no cache file (off by default)",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-SCAN-002"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-READ-103"
      ],
      "satisfied_by": [
        "REQ-SCANCACHE-911"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1238"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1242"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1252"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1311"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4740"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.15",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Cache de scanare opțional",
          "intent": "Scanarea recitește și reanalizează fiecare fișier sursă la fiecare rulare pentru a găsi etichete de apartenență. Pe un repo mare, aceasta poate fi partea lentă. Acesta este un cache opțional: când îl ceri, un fișier neschimbat este sărit la următoarea scanare. Este dezactivat implicit și nu schimbă nimic despre rezultate — doar viteza — astfel încât gate-ul rămâne exact la fel de determinist ca înainte.",
          "contract": "> Scanarea recitește și reanalizează fiecare fișier sursă la fiecare rulare pentru a\n> găsi etichete de apartenență. Pe un repo mare, aceasta poate fi partea lentă. Acesta\n> este un cache opțional: când îl ceri, un fișier neschimbat este sărit la următoarea\n> scanare. Este dezactivat implicit și nu schimbă nimic despre rezultate — doar\n> viteza — astfel încât gate-ul rămâne exact la fel de determinist ca înainte.\n\nFiecare punct de mai jos este obligatoriu.\n- Flag-ul `--cache` (dezactivat implicit) activează un cache de scanare per-fișier pentru parcurgerea unică `scan_all`, care servește de asemenea `scan_members`, cheiat după `(mtime_ns, size)`, returnând rezultate identice octet-cu-octet cu o scanare fără cache; calea implicită și calea CI/gate nu îl citesc și nu îl scriu niciodată decât dacă este dat `--cache`. [[REQ-SCANCACHE-911]] detaliază comportamentul.",
          "acceptance": "CASE-1\n  Given  un arbore etichetat\n  When   `scan_members(cache=True)` rulează\n  Then   este egal cu `scan_members(cache=False)` (membri identici) iar un sidecar\n         `_scancache.json` este scris\n\nCASE-2\n  Given  conținutul unui fișier etichetat se schimbă\n  When   o rescanare cu cache rulează\n  Then   reflectă noua etichetă și renunță la cea veche\n\nCASE-3\n  Given  un fișier etichetat este șters\n  When   o rescanare cu cache rulează\n  Then   omite fișierul și elimină intrarea sa din cache\n\nCASE-4\n  Given  fără niciun `--cache`\n  When   `scan_members` rulează\n  Then   nu scrie niciun fișier de cache (dezactivat implicit)"
        }
      }
    },
    {
      "id": "REQ-SCANCACHE-911",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Caching scan results without changing them",
      "intent": "Re-parsing every source file on every scan is the slow part on a large repo, but the gate must stay exactly as deterministic as before. `--cache` keys a sidecar by each file's `(mtime_ns, size)`, reusing tags for an unchanged file and re-parsing anything that changed or is new, so a cached scan returns results byte-identical to an uncached one — never on by default, and never read on the gate's own path.",
      "contract": [
        "The `--cache` flag (off by default) enables a per-file scan cache for the single walk `scan_all`, which also serves `scan_members`,. The default path and the CI/gate path never read or write the cache unless `--cache` is given.",
        "The cache is a sidecar `requirements/_scancache.json`, keyed per file by `(mtime_ns, size)` → the membership tags found in that file. It is machine-local and gitignored — never committed (mtimes differ across machines, so a committed cache would be meaningless/stale).",
        "With the cache on: an unchanged file (matching `mtime_ns` AND `size`) reuses its cached tags; a changed or new file is re-parsed and its entry refreshed; a file no longer present is pruned (absent from the rewritten cache).",
        "The cache is a PURE performance optimization: `scan_members(cache=True)` returns results byte-identical to `scan_members(cache=False)` for any consistent cache — same members, same order.",
        "The cache fails open and best-effort: an absent, unreadable, or corrupt cache yields an empty cache (full re-scan), and an unwritable cache directory never fails or alters the scan."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — cache sidecar is written only when --cache is passed Given  a tagged tree with no `_scancache.json` sidecar yet When   `scan_members(code_root, reqs_dir)` runs without `cache=True` Then   `requirements/_scancache.json` is not created",
        "CASE-2 — — rewriting a file's size invalidates its cache entry Given  a file already cached under `scan_members(..., cache=True)` When   the file is rewritten with different content and a different size Then   the next cached scan drops the old tag and reports only the new one",
        "CASE-3 — — a deleted file's entry is pruned from the rewritten cache Given  a file already cached by a prior `scan_members(..., cache=True)` run When   the file is removed and a cached scan runs again Then   its capability disappears from the results and its key is absent from `_scancache.json`",
        "CASE-4 — — cache=True and cache=False return identical members Given  the same tagged tree When   `scan_members` runs once with `cache=False` and twice with `cache=True` Then   all three calls return the same `{cap_id: [(role, file, line)]}` mapping",
        "CASE-5 — — a corrupt cache file falls back to a full re-scan Given  a `_scancache.json` sidecar containing invalid JSON (`\"{ not json\"`) When   `scan_members(..., cache=True)` runs Then   it returns the same members as `scan_members(..., cache=False)`, without raising",
        "CASE-6 — — the cache covers all three scan results Given  a tree with an `implements:` tag, a levelled `tested-by:` tag and a `verifies:` tag When   `scan_all(cache=True)` runs twice Then   members, per-case coverage and test levels equal the uncached run both times, and an entry lacking `ac`/`lv` is re-scanned"
      ],
      "accept": "CASE-1 — cache sidecar is written only when --cache is passed\n  Given  a tagged tree with no `_scancache.json` sidecar yet\n  When   `scan_members(code_root, reqs_dir)` runs without `cache=True`\n  Then   `requirements/_scancache.json` is not created\n\nCASE-2 — rewriting a file's size invalidates its cache entry\n  Given  a file already cached under `scan_members(..., cache=True)`\n  When   the file is rewritten with different content and a different size\n  Then   the next cached scan drops the old tag and reports only the new one\n\nCASE-3 — a deleted file's entry is pruned from the rewritten cache\n  Given  a file already cached by a prior `scan_members(..., cache=True)` run\n  When   the file is removed and a cached scan runs again\n  Then   its capability disappears from the results and its key is absent from\n         `_scancache.json`\n\nCASE-4 — cache=True and cache=False return identical members\n  Given  the same tagged tree\n  When   `scan_members` runs once with `cache=False` and twice with `cache=True`\n  Then   all three calls return the same `{cap_id: [(role, file, line)]}` mapping\n\nCASE-5 — a corrupt cache file falls back to a full re-scan\n  Given  a `_scancache.json` sidecar containing invalid JSON (`\"{ not json\"`)\n  When   `scan_members(..., cache=True)` runs\n  Then   it returns the same members as `scan_members(..., cache=False)`, without raising\n\n\n\nCASE-6 — the cache covers all three scan results\n  Given  a tree with an `implements:` tag, a levelled `tested-by:` tag and a `verifies:` tag\n  When   `scan_all(cache=True)` runs twice\n  Then   members, per-case coverage and test levels equal the uncached run both times, and an entry lacking `ac`/`lv` is re-scanned",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SCANCACHE-023"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1238"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1242"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1252"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1311"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4740"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7936"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Caching rezultatelor de scanare fără a le schimba",
          "intent": "Re-parsarea fiecărui fișier sursă la fiecare scanare este partea lentă pe un repo mare, dar gate-ul trebuie să rămână exact la fel de determinist ca înainte. `--cache` cheie un sidecar după `(mtime_ns, size)`-ul fiecărui fișier, refolosind tag-urile pentru un fișier neschimbat și re-parsând orice s-a schimbat sau este nou, astfel o scanare cu cache returnează rezultate identice byte cu byte cu una fără cache — niciodată activat implicit, și niciodată citit pe calea gate-ului însuși.",
          "contract": "> Re-parsarea fiecărui fișier sursă la fiecare scanare este partea lentă pe un repo mare, dar\n> gate-ul trebuie să rămână exact la fel de determinist ca înainte. `--cache` cheie un sidecar\n> după `(mtime_ns, size)`-ul fiecărui fișier, refolosind tag-urile pentru un fișier neschimbat\n> și re-parsând orice s-a schimbat sau este nou, astfel o scanare cu cache returnează rezultate\n> identice byte cu byte cu una fără cache — niciodată activat implicit, și niciodată citit pe\n> calea gate-ului însuși.\n\nFiecare punct de mai jos este obligatoriu.\n- Flag-ul `--cache` (dezactivat implicit) activează un cache de scanare per fișier pentru singura parcurgere `scan_all`, care servește de asemenea `scan_members`. Calea\n  implicită și calea CI/gate nu citesc și nu scriu niciodată cache-ul decât dacă `--cache` este dat.\n- Cache-ul este un sidecar `requirements/_scancache.json`, cheie per fișier de `(mtime_ns, size)`\n  → tag-urile de apartenență găsite în acel fișier. Este local mașinii și gitignored — niciodată commis\n  (mtime-urile diferă între mașini, astfel un cache commis ar fi lipsit de sens/învechit).\n- Cu cache-ul activat: un fișier neschimbat (potrivind `mtime_ns` ȘI `size`) refolosește tag-urile\n  lui din cache; un fișier schimbat sau nou este re-parsat și intrarea lui reîmprospătată; un fișier care nu mai este\n  prezent este eliminat (absent din cache-ul rescris).\n- Cache-ul este o optimizare PURĂ de performanță: `scan_members(cache=True)` returnează rezultate\n  identice byte cu byte cu `scan_members(cache=False)` pentru orice cache consistent — aceiași membri, aceeași ordine.\n- Cache-ul eșuează deschis și best-effort: un cache absent, necitibil, sau corupt produce un\n  cache gol (rescanare completă), iar un director de cache inscriptibil nu eșuează sau nu alterează niciodată scanarea.",
          "acceptance": "CASE-1 — sidecar-ul de cache este scris doar când --cache este dat\n  Given  un arbore etichetat fără niciun sidecar `_scancache.json` încă\n  When   `scan_members(code_root, reqs_dir)` rulează fără `cache=True`\n  Then   `requirements/_scancache.json` nu este creat\n\nCASE-2 — rescrierea dimensiunii unui fișier invalidează intrarea lui din cache\n  Given  un fișier deja cache-uit sub `scan_members(..., cache=True)`\n  When   fișierul este rescris cu conținut diferit și o dimensiune diferită\n  Then   următoarea scanare cu cache renunță la tag-ul vechi și raportează doar pe cel nou\n\nCASE-3 — intrarea unui fișier șters este eliminată din cache-ul rescris\n  Given  un fișier deja cache-uit de o rulare anterioară `scan_members(..., cache=True)`\n  When   fișierul este eliminat și o scanare cu cache rulează din nou\n  Then   capacitatea lui dispare din rezultate iar cheia lui este absentă din\n         `_scancache.json`\n\nCASE-4 — cache=True și cache=False returnează membri identici\n  Given  același arbore etichetat\n  When   `scan_members` rulează o dată cu `cache=False` și de două ori cu `cache=True`\n  Then   toate cele trei apeluri returnează aceeași mapare `{cap_id: [(role, file, line)]}`\n\nCASE-5 — un fișier de cache corupt recurge la o rescanare completă\n  Given  un sidecar `_scancache.json` conținând JSON invalid (`\"{ not json\"`)\n  When   `scan_members(..., cache=True)` rulează\n  Then   returnează aceiași membri ca `scan_members(..., cache=False)`, fără să ridice o excepție\n\n\n\nCASE-6 — cache-ul acoperă toate cele trei rezultate de scanare\n  Given  un arbore cu un tag `implements:`, un tag `tested-by:` cu nivel și un tag `verifies:`\n  When   `scan_all(cache=True)` rulează de două ori\n  Then   membrii, acoperirea per criteriu și nivelurile de test sunt egale cu rularea fără cache de ambele dăți, iar o intrare căreia îi lipsește `ac`/`lv` este rescanată"
        }
      }
    },
    {
      "id": "ARCH-SEARCH-036",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Free-text requirement search",
      "intent": "To find the requirement about a topic today you grep the requirements folder or open the map — grep needs the exact word the author used, and neither ranks results. This ranks every requirement by how well its wording matches a free-text query, most-relevant-first, so \"where is the drift thing?\" lands on the right file without knowing its id. It reuses the same lexical scoring `dupes` already trusts, so it adds a way in, not a new engine. Because the match is lexical it can miss a synonym; when nothing matches well it says so plainly, so a near-miss is never mistaken for the answer — the drift this whole tool exists to prevent.",
      "contract": [
        "`search \"<query>\"` ranks every requirement by how well its wording matches the query, most-relevant-first, reusing the exact model the viewer's search box ports. [[REQ-SEARCH-912]]",
        "`search` prints every match together with its cosine score, so a weak match never carries a strong match's authority. [[REQ-SEARCH-913]]",
        "`search` applies a relevance floor (`0.05` by default) and reports plainly, in a message distinct from an empty query, when nothing clears it. [[REQ-SEARCH-914]]",
        "`search` always exits zero from a well-formed invocation; only a usage error (a missing query) exits non-zero. [[REQ-SEARCH-915]]",
        "`search` answers a query that names an id, and a query that appears literally in a requirement's text, before it falls back to ranking wording. [[REQ-SEARCH-965]]"
      ],
      "verify": [],
      "notes": [
        "The match is purely lexical (token overlap). A query worded in synonyms of the requirement's text will score low or miss; this is why the floor and the \"not synonym-aware\" label exist, rather than a silent empty or spurious result.",
        "One relevance model, two surfaces: the CLI for headless/agent/CI callers, the viewer for a human browsing the map. The golden fixture is what keeps them from diverging.",
        "The `0.05` default was chosen because a short query is a sparse vector whose cosine against a full requirement runs far below the `dupes` pair threshold; on this corpus a correct top hit scores well above it while a no-lexical-overlap query stays below.",
        "The floor is a module constant, not a flag. It was calibrated on this corpus; a very different corpus could warrant a different value, which is a code change, not configuration — keeping the surface minimal.",
        "The query is folded into the corpus for the idf computation, so its terms participate in the document-frequency statistics exactly as the benchmark that validated the ranking did."
      ],
      "current_impl": [
        "`cmd_search` in `reqmap.py` — tokenizes the query with `_sim_tokens`, builds each requirement's bag with `_sim_text`, folds the query into the corpus, then reuses `_tfidf` and `_cosine` (ARCH-SIMILAR-016) to rank. It prints each hit's score via `_req_title`, guards the empty-query and no-strong-match branches against the `SEARCH_FLOOR` constant, and always returns zero.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a query whose terms clearly match one requirement's contract When   `search` runs Then   that requirement appears in the results with its cosine score, ranked first",
        "CASE-2 — Given  a query with no lexical overlap with any requirement When   `search` runs Then   it prints the explicit no-strong-match line (best score below the floor) and does not print a ranked result",
        "CASE-3 — Given  a query that is empty after tokenizing (only stopwords / short words) When   `search` runs Then   it prints the \"no searchable terms\" line, distinct from the no-strong-match case",
        "CASE-4 — Given  more matching requirements than `--top` When   `search` runs with that `--top` Then   at most `--top` matches are printed"
      ],
      "accept": "CASE-1\n  Given  a query whose terms clearly match one requirement's contract\n  When   `search` runs\n  Then   that requirement appears in the results with its cosine score, ranked first\n\nCASE-2\n  Given  a query with no lexical overlap with any requirement\n  When   `search` runs\n  Then   it prints the explicit no-strong-match line (best score below the floor) and\n         does not print a ranked result\n\nCASE-3\n  Given  a query that is empty after tokenizing (only stopwords / short words)\n  When   `search` runs\n  Then   it prints the \"no searchable terms\" line, distinct from the no-strong-match case\n\nCASE-4\n  Given  more matching requirements than `--top`\n  When   `search` runs with that `--top`\n  Then   at most `--top` matches are printed",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001",
        "ARCH-SIMILAR-016"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-REPORT-105"
      ],
      "satisfied_by": [
        "REQ-SEARCH-912",
        "REQ-SEARCH-913",
        "REQ-SEARCH-914",
        "REQ-SEARCH-915",
        "REQ-SEARCH-965"
      ],
      "members": [
        {
          "role": "tested-by",
          "loc": "app/scripts/ssr-smoke.jsx:2"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/search.js:1"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5368"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5416"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3368"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8918"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10499"
        }
      ],
      "test_exempt": null,
      "milestone": "v2.13",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Căutare în text liber a cerințelor",
          "intent": "Pentru a găsi astăzi cerința despre un anumit subiect faci grep în folderul de cerințe sau deschizi harta — grep are nevoie de cuvântul exact folosit de autor, iar niciuna dintre variante nu clasifică rezultatele. Aceasta clasifică fiecare cerință după cât de bine se potrivește formularea ei cu o interogare în text liber, cele mai relevante primele, astfel încât „unde e chestia cu drift-ul?\" ajunge la fișierul corect fără a-i cunoaște id-ul. Reutilizează același scor lexical în care `dupes` are deja încredere, deci adaugă o cale de acces, nu un motor nou. Fiindcă potrivirea este lexicală, poate rata un sinonim; când nimic nu se potrivește bine, o spune clar, astfel încât o potrivire aproximativă să nu fie niciodată confundată cu răspunsul — exact drift-ul pe care acest instrument există ca să-l prevină.",
          "contract": "> Pentru a găsi astăzi cerința despre un anumit subiect faci grep în folderul de cerințe sau\n> deschizi harta — grep are nevoie de cuvântul exact folosit de autor, iar niciuna dintre\n> variante nu clasifică rezultatele. Aceasta clasifică fiecare cerință după cât de bine se\n> potrivește formularea ei cu o interogare în text liber, cele mai relevante primele, astfel\n> încât „unde e chestia cu drift-ul?\" ajunge la fișierul corect fără a-i cunoaște id-ul.\n> Reutilizează același scor lexical în care `dupes` are deja încredere, deci adaugă o cale de\n> acces, nu un motor nou. Fiindcă potrivirea este lexicală, poate rata un sinonim; când nimic\n> nu se potrivește bine, o spune clar, astfel încât o potrivire aproximativă să nu fie\n> niciodată confundată cu răspunsul — exact drift-ul pe care acest instrument există ca să-l\n> prevină.\n\nFiecare punct de mai jos este obligatoriu.\n- `search \"<query>\"` clasifică fiecare cerință după cât de bine se potrivește formularea ei cu interogarea, cele mai relevante primele, reutilizând exact modelul pe care îl portează caseta de căutare a viewer-ului. [[REQ-SEARCH-912]]\n- `search` afișează fiecare potrivire împreună cu scorul ei cosinus, astfel încât o potrivire slabă să nu poarte niciodată autoritatea unei potriviri puternice. [[REQ-SEARCH-913]]\n- `search` aplică un prag de relevanță (`0.05` implicit) și raportează clar, printr-un mesaj distinct de cel pentru o interogare goală, când nimic nu îl depășește. [[REQ-SEARCH-914]]\n- `search` iese întotdeauna cu zero la o invocare bine formată; doar o eroare de utilizare (o interogare lipsă) iese cu cod diferit de zero. [[REQ-SEARCH-915]]\n- `search` răspunde la o interogare care numește un id și la o interogare care apare literal în textul unei cerințe, înainte de a recurge la clasificarea formulării. [[REQ-SEARCH-965]]",
          "acceptance": "CASE-1\n  Given  o interogare ai cărei termeni se potrivesc clar cu contractul unei cerințe\n  When   `search` rulează\n  Then   acea cerință apare în rezultate cu scorul ei cosinus, clasată prima\n\nCASE-2\n  Given  o interogare fără nicio suprapunere lexicală cu vreo cerință\n  When   `search` rulează\n  Then   afișează linia explicită de absență a potrivirilor puternice (cel mai bun scor sub\n         prag) și nu afișează niciun rezultat clasat\n\nCASE-3\n  Given  o interogare care este goală după tokenizare (doar stopwords / cuvinte scurte)\n  When   `search` rulează\n  Then   afișează linia „niciun termen de căutat\", distinctă de cazul absenței potrivirilor puternice\n\nCASE-4\n  Given  mai multe cerințe care se potrivesc decât `--top`\n  When   `search` rulează cu acel `--top`\n  Then   se afișează cel mult `--top` potriviri"
        }
      }
    },
    {
      "id": "REQ-SEARCH-912",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Ranking a query against the corpus",
      "intent": "`search` turns a free-text query into a ranked list of requirements by reusing the exact TF-IDF/cosine model `dupes` already uses to compare requirements to each other, so there is only one scoring engine in the codebase, not two that could drift apart. The map viewer's search box ports the same model, pinned to it by a shared golden fixture, so a developer gets the same ranking whether they search from the CLI or the browser.",
      "contract": [
        "`search \"<query>\"` ranks every requirement by how well its wording matches the query, then prints them most-relevant-first.",
        "`search` writes no file. It only reads and prints.",
        "`search` reuses the scoring machinery of `dupes` (ARCH-SIMILAR-016). There is never a second scoring path.",
        "The query and each requirement both reduce to the same bag of words: title, intent line, Contract bullets.",
        "`search` then compares those two bags by cosine over smoothed TF-IDF weights.",
        "The map viewer's search box (`app/src/lib/search.js`) ports this exact model, never a divergent one, so both surfaces agree on what matches in what order.",
        "A shared golden fixture pins the port to the engine: one fixed query scores identically in `app/scripts/ssr-smoke.jsx` and in the Python `Search` tests."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — search runs without creating or modifying any file Given  a populated requirements corpus and a query with no output-writing flag When   `search \"<query>\"` runs Then   no file in the corpus changes and only stdout carries output",
        "CASE-2 — — search scores a pair the same way dupes does Given  a query text identical to one requirement's full bag of words When   `search` and `dupes` score that same pair Then   both report the identical cosine score, since both call `_tfidf` and `_cosine`",
        "CASE-3 — — only title, intent line and Contract bullets feed the match Given  a requirement whose Notes section mentions a word absent from its title, intent and Contract When   a query using only that Notes-only word runs through `search` Then   that requirement does not score above the floor on that word alone",
        "CASE-4 — — a rarer shared term outweighs a common one Given  two requirements matching the query on one common word and one rare word respectively When   `search` runs Then   the requirement sharing the rare word ranks higher, reflecting its higher TF-IDF weight, and prints before the other match",
        "CASE-5 — — the viewer's ported model scores identically to the engine Given  the shared golden fixture used by both the Python `Search` tests and the JS SSR smoke test When   the same query runs through the engine's `search` and the viewer's ported scorer Then   both report the identical cosine score for the same requirement --------------------"
      ],
      "accept": "CASE-1 — search runs without creating or modifying any file\n  Given  a populated requirements corpus and a query with no output-writing flag\n  When   `search \"<query>\"` runs\n  Then   no file in the corpus changes and only stdout carries output\n\nCASE-2 — search scores a pair the same way dupes does\n  Given  a query text identical to one requirement's full bag of words\n  When   `search` and `dupes` score that same pair\n  Then   both report the identical cosine score, since both call `_tfidf` and `_cosine`\n\nCASE-3 — only title, intent line and Contract bullets feed the match\n  Given  a requirement whose Notes section mentions a word absent from its title, intent and Contract\n  When   a query using only that Notes-only word runs through `search`\n  Then   that requirement does not score above the floor on that word alone\n\nCASE-4 — a rarer shared term outweighs a common one\n  Given  two requirements matching the query on one common word and one rare word respectively\n  When   `search` runs\n  Then   the requirement sharing the rare word ranks higher, reflecting its higher TF-IDF\n         weight, and prints before the other match\n\nCASE-5 — the viewer's ported model scores identically to the engine\n  Given  the shared golden fixture used by both the Python `Search` tests and the JS SSR smoke test\n  When   the same query runs through the engine's `search` and the viewer's ported scorer\n  Then   both report the identical cosine score for the same requirement\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SEARCH-036"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "tested-by",
          "loc": "app/scripts/ssr-smoke.jsx:84"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/search.js:78"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5416"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3368"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8918"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Clasarea unei interogări față de corpus",
          "intent": "`search` transformă o interogare de text liber într-o listă clasată de cerințe, refolosind exact modelul TF-IDF/cosinus pe care `dupes` îl folosește deja pentru a compara cerințele între ele, astfel încât există un singur motor de scorare în baza de cod, nu două care ar putea deriva una față de alta. Caseta de căutare a viewerului de map portă același model, fixat de el printr-un fixture golden partajat, astfel un dezvoltator obține aceeași clasare fie că face căutarea din CLI, fie din browser.",
          "contract": "> `search` transformă o interogare de text liber într-o listă clasată de cerințe, refolosind\n> exact modelul TF-IDF/cosinus pe care `dupes` îl folosește deja pentru a compara cerințele\n> între ele, astfel încât există un singur motor de scorare în baza de cod, nu două care ar\n> putea deriva una față de alta. Caseta de căutare a viewerului de map portă același model,\n> fixat de el printr-un fixture golden partajat, astfel un dezvoltator obține aceeași clasare\n> fie că face căutarea din CLI, fie din browser.\n\nFiecare punct de mai jos este obligatoriu.\n- `search \"<query>\"` clasează fiecare cerință după cât de bine se potrivește formularea ei cu\n  interogarea, apoi le afișează cele-mai-relevante-primele.\n- `search` nu scrie niciun fișier. Doar citește și afișează.\n- `search` refolosește mecanismul de scorare al `dupes` (ARCH-SIMILAR-016). Nu există niciodată o a\n  doua cale de scorare.\n- Interogarea și fiecare cerință se reduc amândouă la același sac de cuvinte: titlu, linia de\n  intent, punctele din Contract.\n- `search` apoi compară acele două saci prin cosinus peste ponderi TF-IDF netezite.\n- Caseta de căutare a viewerului de map (`app/src/lib/search.js`) portă acest model exact, niciodată\n  unul divergent, astfel ambele suprafețe sunt de acord asupra a ce se potrivește în ce ordine.\n- Un fixture golden partajat fixează portul de motor: o interogare fixă scorează identic în\n  `app/scripts/ssr-smoke.jsx` și în testele Python `Search`.",
          "acceptance": "CASE-1 — search rulează fără a crea sau modifica niciun fișier\n  Given  un corpus populat de cerințe și o interogare fără flag de scriere a ieșirii\n  When   `search \"<query>\"` rulează\n  Then   niciun fișier din corpus nu se schimbă, iar doar stdout poartă ieșire\n\nCASE-2 — search scorează o pereche la fel cum face dupes\n  Given  un text de interogare identic cu sacul complet de cuvinte al unei cerințe\n  When   `search` și `dupes` scorează aceeași pereche\n  Then   ambele raportează scorul cosinus identic, deoarece ambele apelează `_tfidf` și `_cosine`\n\nCASE-3 — doar titlul, linia de intent și punctele din Contract alimentează potrivirea\n  Given  o cerință a cărei secțiune Notes menționează un cuvânt absent din titlul, intentul și Contractul ei\n  When   o interogare folosind doar acel cuvânt-doar-din-Notes rulează prin `search`\n  Then   acea cerință nu scorează peste prag pe acel singur cuvânt\n\nCASE-4 — un termen mai rar, partajat, depășește unul comun\n  Given  două cerințe potrivind interogarea pe un cuvânt comun și, respectiv, unul rar\n  When   `search` rulează\n  Then   cerința care partajează cuvântul rar se clasează mai sus, reflectând ponderea ei\n         TF-IDF mai mare, și se afișează înaintea celeilalte potriviri\n\nCASE-5 — modelul portat al viewerului scorează identic cu motorul\n  Given  fixture-ul golden partajat folosit atât de testele Python `Search`, cât și de testul SSR smoke din JS\n  When   aceeași interogare rulează prin `search`-ul motorului și prin scorerul portat al viewerului\n  Then   ambele raportează scorul cosinus identic pentru aceeași cerință\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-SEARCH-913",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Printing ranked matches with their score",
      "intent": "Each printed match carries the cosine score that earned it its place, so a weak match is visibly weak rather than looking as authoritative as a strong one. Without a visible score, a caller could mistake a barely-passing hit for a confident answer.",
      "contract": [
        "`search` prints every match it shows together with that match's cosine score. A weak match then looks weak, instead of carrying the authority of a strong one.",
        "`search` shows at most `--top` matches. `--top` defaults to five.",
        "A `--top` of zero or less counts as one.",
        "`search` applies a relevance floor and never prints a ranked list of below-floor results.",
        "When no requirement scores at or above the floor, `search` prints an explicit no-strong-match line reporting the best score and the floor."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — each printed result carries its cosine score Given  a query matching at least one requirement above the floor When   `search` runs Then   every printed match line shows that match's cosine score next to its id",
        "CASE-2 — — search defaults to five results Given  a query matching more than five requirements above the floor, and no `--top` flag When   `search` runs Then   it prints exactly five matches",
        "CASE-3 — — a non-positive --top still prints one match Given  a query with at least one match above the floor and `--top 0` When   `search` runs Then   it prints exactly one match, not zero",
        "CASE-4 — — below-floor results never appear as a ranked list Given  a query whose best score falls under the floor When   `search` runs Then   no ranked results print, only the no-strong-match line",
        "CASE-5 — — the no-strong-match line names the best score and the floor Given  a query with no requirement scoring at or above the floor When   `search` runs Then   the printed line reports both the best score found and the floor value --------------------"
      ],
      "accept": "CASE-1 — each printed result carries its cosine score\n  Given  a query matching at least one requirement above the floor\n  When   `search` runs\n  Then   every printed match line shows that match's cosine score next to its id\n\nCASE-2 — search defaults to five results\n  Given  a query matching more than five requirements above the floor, and no `--top` flag\n  When   `search` runs\n  Then   it prints exactly five matches\n\nCASE-3 — a non-positive --top still prints one match\n  Given  a query with at least one match above the floor and `--top 0`\n  When   `search` runs\n  Then   it prints exactly one match, not zero\n\nCASE-4 — below-floor results never appear as a ranked list\n  Given  a query whose best score falls under the floor\n  When   `search` runs\n  Then   no ranked results print, only the no-strong-match line\n\nCASE-5 — the no-strong-match line names the best score and the floor\n  Given  a query with no requirement scoring at or above the floor\n  When   `search` runs\n  Then   the printed line reports both the best score found and the floor value\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SEARCH-036"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5416"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3368"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8918"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Afișarea potrivirilor clasate cu scorul lor",
          "intent": "Fiecare potrivire afișată poartă scorul cosinus care i-a adus locul, astfel o potrivire slabă este vizibil slabă, în loc să arate la fel de autoritară ca una puternică. Fără un scor vizibil, un apelant ar putea confunda o potrivire abia-trecută cu un răspuns sigur.",
          "contract": "> Fiecare potrivire afișată poartă scorul cosinus care i-a adus locul, astfel o potrivire\n> slabă este vizibil slabă, în loc să arate la fel de autoritară ca una puternică. Fără un scor\n> vizibil, un apelant ar putea confunda o potrivire abia-trecută cu un răspuns sigur.\n\nFiecare punct de mai jos este obligatoriu.\n- `search` afișează fiecare potrivire pe care o arată împreună cu scorul cosinus al acelei potriviri. O potrivire\n  slabă atunci arată slabă, în loc să poarte autoritatea uneia puternice.\n- `search` arată cel mult `--top` potriviri. `--top` este implicit cinci.\n- Un `--top` de zero sau mai puțin contează ca unu.\n- `search` aplică un prag de relevanță și nu afișează niciodată o listă clasată de rezultate sub prag.\n- Când nicio cerință nu scorează la sau peste prag, `search` afișează o linie explicită de\n  fără-potrivire-puternică, raportând cel mai bun scor și pragul.",
          "acceptance": "CASE-1 — fiecare rezultat afișat poartă scorul lui cosinus\n  Given  o interogare care se potrivește cu cel puțin o cerință peste prag\n  When   `search` rulează\n  Then   fiecare linie de potrivire afișată arată scorul cosinus al acelei potriviri lângă id-ul ei\n\nCASE-2 — search este implicit cinci rezultate\n  Given  o interogare care se potrivește cu mai mult de cinci cerințe peste prag, și niciun flag `--top`\n  When   `search` rulează\n  Then   afișează exact cinci potriviri\n\nCASE-3 — un --top nepozitiv afișează totuși o potrivire\n  Given  o interogare cu cel puțin o potrivire peste prag și `--top 0`\n  When   `search` rulează\n  Then   afișează exact o potrivire, nu zero\n\nCASE-4 — rezultatele sub prag nu apar niciodată ca o listă clasată\n  Given  o interogare al cărei cel mai bun scor cade sub prag\n  When   `search` rulează\n  Then   niciun rezultat clasat nu se afișează, doar linia de fără-potrivire-puternică\n\nCASE-5 — linia de fără-potrivire-puternică numește cel mai bun scor și pragul\n  Given  o interogare fără nicio cerință care scorează la sau peste prag\n  When   `search` rulează\n  Then   linia afișată raportează atât cel mai bun scor găsit, cât și valoarea pragului\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-SEARCH-914",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The relevance floor and the empty-query message",
      "intent": "A query with no usable words (all stopwords, or too short) is not the same failure as a query that simply found nothing above the relevance floor — conflating the two would hide a typo'd or accidental empty query behind the same message as a genuine no-match, so `search` reports each with its own distinct line.",
      "contract": [
        "The floor defaults to `0.05`.",
        "When the query holds no searchable term, `search` says so and ranks nothing. That line is distinct from the no-strong-match line.",
        "Tokenizing drops short words, stopwords and pure numbers. A query holds no searchable term when nothing survives that.",
        "The output of `search` says that the search is lexical, not synonym-aware. A user who gets no hit then knows to try other words rather than conclude no requirement exists."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the floor is 0.05 absent an override Given  a query scoring just above 0.05 against one requirement and below against all others When   `search` runs Then   that one requirement is reported as a match",
        "CASE-2 — — an all-stopword query says so, not \"no strong match\" Given  a query made only of stopwords and pure numbers When   `search` runs Then   it prints the \"no searchable terms\" line, never the no-strong-match line",
        "CASE-3 — — short words and numbers never seed a match Given  a query of only two-letter words and digit-only tokens When   `search` tokenizes it Then   every token is dropped, so the query counts as holding no searchable term",
        "CASE-4 — — the no-strong-match line names the search as lexical Given  a query with no requirement scoring at or above the floor When   `search` prints its no-strong-match line Then   that line states the match is lexical, not synonym-aware --------------------"
      ],
      "accept": "CASE-1 — the floor is 0.05 absent an override\n  Given  a query scoring just above 0.05 against one requirement and below against all others\n  When   `search` runs\n  Then   that one requirement is reported as a match\n\nCASE-2 — an all-stopword query says so, not \"no strong match\"\n  Given  a query made only of stopwords and pure numbers\n  When   `search` runs\n  Then   it prints the \"no searchable terms\" line, never the no-strong-match line\n\nCASE-3 — short words and numbers never seed a match\n  Given  a query of only two-letter words and digit-only tokens\n  When   `search` tokenizes it\n  Then   every token is dropped, so the query counts as holding no searchable term\n\nCASE-4 — the no-strong-match line names the search as lexical\n  Given  a query with no requirement scoring at or above the floor\n  When   `search` prints its no-strong-match line\n  Then   that line states the match is lexical, not synonym-aware\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SEARCH-036"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5416"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3368"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8918"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Pragul de relevanță și mesajul de interogare goală",
          "intent": "O interogare fără cuvinte utilizabile (toate stopword-uri, sau prea scurte) nu este același eșec ca o interogare care pur și simplu nu a găsit nimic peste pragul de relevanță — combinarea celor două ar ascunde o interogare cu greșeală de tastare sau accidental goală în spatele aceluiași mesaj ca o lipsă-de-potrivire genuină, astfel `search` raportează fiecare cu propria linie distinctă.",
          "contract": "> O interogare fără cuvinte utilizabile (toate stopword-uri, sau prea scurte) nu este același\n> eșec ca o interogare care pur și simplu nu a găsit nimic peste pragul de relevanță —\n> combinarea celor două ar ascunde o interogare cu greșeală de tastare sau accidental goală în\n> spatele aceluiași mesaj ca o lipsă-de-potrivire genuină, astfel `search` raportează fiecare cu\n> propria linie distinctă.\n\nFiecare punct de mai jos este obligatoriu.\n- Pragul este implicit `0.05`.\n- Când interogarea nu conține niciun termen căutabil, `search` spune asta și nu clasează nimic. Acea linie este\n  distinctă de linia de fără-potrivire-puternică.\n- Tokenizarea elimină cuvintele scurte, stopword-urile și numerele pure. O interogare nu conține niciun termen căutabil\n  când nimic nu supraviețuiește acelui filtru.\n- Ieșirea lui `search` spune că această căutare este lexicală, nu sensibilă la sinonime. Un utilizator care nu obține nicio\n  potrivire știe atunci să încerce alte cuvinte, mai degrabă decât să concluzioneze că nu există nicio cerință.",
          "acceptance": "CASE-1 — pragul este 0.05 în absența unei suprascrieri\n  Given  o interogare care scorează chiar peste 0.05 față de o cerință și sub prag față de toate celelalte\n  When   `search` rulează\n  Then   acea unică cerință este raportată ca potrivire\n\nCASE-2 — o interogare din toate stopword-uri spune asta, nu \"no strong match\"\n  Given  o interogare formată doar din stopword-uri și numere pure\n  When   `search` rulează\n  Then   afișează linia \"no searchable terms\", niciodată linia de fără-potrivire-puternică\n\nCASE-3 — cuvintele scurte și numerele nu declanșează niciodată o potrivire\n  Given  o interogare formată doar din cuvinte de două litere și token-uri numai-cifre\n  When   `search` le tokenizează\n  Then   fiecare token este eliminat, astfel interogarea contează ca neconținând niciun termen căutabil\n\nCASE-4 — linia de fără-potrivire-puternică numește căutarea ca lexicală\n  Given  o interogare fără nicio cerință care scorează la sau peste prag\n  When   `search` afișează linia lui de fără-potrivire-puternică\n  Then   acea linie afirmă că potrivirea este lexicală, nu sensibilă la sinonime\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-SEARCH-915",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Search always exits zero on a well-formed query",
      "intent": "`search` is a read-only reporting command: whatever it finds — a strong match, a weak one, or nothing at all — is a normal result, not a failure, so the process exits 0. Only a genuine usage error, such as an omitted query, is a real failure and exits non-zero.",
      "contract": [
        "`search` always returns zero from a well-formed invocation, regardless of how well (or poorly) the query matched.",
        "A missing query argument is a usage error and returns a non-zero code.",
        "An empty corpus, or a corpus where no requirement carries contract text, still exits zero and prints an explanatory line rather than crashing."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a well-formed search always exits zero Given  a query, whether it matches strongly, weakly, or not at all When   `search` runs Then   the process exits 0 in every case",
        "CASE-2 — — a missing query argument fails with a nonzero exit Given  `search` invoked with no query argument When   the command runs Then   it prints a usage error and exits with a nonzero code",
        "CASE-3 — — an empty corpus still exits zero Given  no requirements carry any contract text to search When   `search` runs Then   it prints \"No requirements with contract text to search.\" and exits 0"
      ],
      "accept": "CASE-1 — a well-formed search always exits zero\n  Given  a query, whether it matches strongly, weakly, or not at all\n  When   `search` runs\n  Then   the process exits 0 in every case\n\nCASE-2 — a missing query argument fails with a nonzero exit\n  Given  `search` invoked with no query argument\n  When   the command runs\n  Then   it prints a usage error and exits with a nonzero code\n\nCASE-3 — an empty corpus still exits zero\n  Given  no requirements carry any contract text to search\n  When   `search` runs\n  Then   it prints \"No requirements with contract text to search.\" and exits 0",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SEARCH-036"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5416"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3368"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8918"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Search iese întotdeauna cu zero pe o interogare bine formată",
          "intent": "`search` este o comandă de raportare read-only: orice găsește — o potrivire puternică, una slabă, sau nimic — este un rezultat normal, nu un eșec, astfel procesul iese cu 0. Doar o eroare de utilizare genuină, precum o interogare omisă, este un eșec real și iese cu non-zero.",
          "contract": "> `search` este o comandă de raportare read-only: orice găsește — o potrivire puternică, una\n> slabă, sau nimic — este un rezultat normal, nu un eșec, astfel procesul iese cu 0. Doar o\n> eroare de utilizare genuină, precum o interogare omisă, este un eșec real și iese cu non-zero.\n\nFiecare punct de mai jos este obligatoriu.\n- `search` returnează întotdeauna zero dintr-o invocare bine formată, indiferent de cât de bine (sau\n  de slab) s-a potrivit interogarea.\n- Un argument de interogare lipsă este o eroare de utilizare și returnează un cod non-zero.\n- Un corpus gol, sau un corpus unde nicio cerință nu poartă text de contract, iese totuși cu zero\n  și afișează o linie explicativă în loc să se prăbușească.",
          "acceptance": "CASE-1 — o căutare bine formată iese întotdeauna cu zero\n  Given  o interogare, fie că se potrivește puternic, slab, sau deloc\n  When   `search` rulează\n  Then   procesul iese cu 0 în fiecare caz\n\nCASE-2 — un argument de interogare lipsă eșuează cu o ieșire nonzero\n  Given  `search` invocat fără niciun argument de interogare\n  When   comanda rulează\n  Then   afișează o eroare de utilizare și iese cu un cod nonzero\n\nCASE-3 — un corpus gol iese totuși cu zero\n  Given  nicio cerință nu poartă niciun text de contract de căutat\n  When   `search` rulează\n  Then   afișează \"No requirements with contract text to search.\" și iese cu 0"
        }
      }
    },
    {
      "id": "REQ-SEARCH-965",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Finding a requirement by its id, and by its literal text",
      "intent": "The bag of words a requirement is ranked on is its title, its intent and its clauses — its id is in none of them. So searching this corpus for `ARCH-CHECK-006` returned `REQ-ORPHANCODE-888` and not the requirement named, because \"arch\" and \"check\" were matched as ordinary prose. An id is the primary key here, and a phrase that appears verbatim in a requirement is stronger evidence than a partial token overlap with a different one — neither is a question about wording, and the ranking model cannot answer either.",
      "contract": [
        "A query equal to a requirement id returns that requirement, alone, ahead of every ranked result.",
        "A query that prefixes or occurs inside ids returns those requirements ahead of the ranked results, capped so a common word cannot crowd the ranking out.",
        "A query that occurs literally in a requirement's title, description or cases returns that requirement ahead of the ranked results. `## Context` is excluded, exactly as it is from the ranking bag: commentary is not what a requirement is about.",
        "The literal search also reads a requirement's cached translation, so a query in the language the reader is being shown finds the requirement even though the ranking model indexes one language.",
        "The ranking model itself is unchanged by all of this: the id and literal layers select requirements, and the ranked layer fills what they leave, so the score a hit carries still means exactly what it meant before."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — an exact id returns its own requirement first Given  a corpus holding `AREA-X-001` and requirements whose prose mentions \"area\" and \"x\" When   `search \"AREA-X-001\"` runs Then   `AREA-X-001` is the first result, marked as an id match",
        "CASE-2 — — a partial id returns the ids it names Given  a corpus holding `AREA-X-001` and `AREA-X-002` When   `search \"AREA-X\"` runs Then   both are returned ahead of any ranked result",
        "CASE-3 — — a phrase inside a case is found Given  a requirement whose only mention of \"ghost tag\" is inside one of its cases When   `search \"ghost tag\"` runs Then   that requirement is returned, marked as a text match",
        "CASE-6 — — a phrase living only in Context is not a match Given  a requirement whose only mention of a word is inside its `## Context` When   `search` runs on that word Then   the requirement is not returned by the literal layer",
        "CASE-4 — — a query in the cached translation's language finds the requirement Given  a requirement with a cached translation containing a phrase absent from its English text When   `search` runs on that phrase Then   the requirement is returned",
        "CASE-5 — — the ranked model is untouched Given  a query naming no id and appearing literally nowhere When   `search` runs Then   every result comes from the ranking model, with the same scores as before"
      ],
      "accept": "CASE-1 — an exact id returns its own requirement first\n  Given  a corpus holding `AREA-X-001` and requirements whose prose mentions \"area\" and \"x\"\n  When   `search \"AREA-X-001\"` runs\n  Then   `AREA-X-001` is the first result, marked as an id match\n\nCASE-2 — a partial id returns the ids it names\n  Given  a corpus holding `AREA-X-001` and `AREA-X-002`\n  When   `search \"AREA-X\"` runs\n  Then   both are returned ahead of any ranked result\n\nCASE-3 — a phrase inside a case is found\n  Given  a requirement whose only mention of \"ghost tag\" is inside one of its cases\n  When   `search \"ghost tag\"` runs\n  Then   that requirement is returned, marked as a text match\n\nCASE-6 — a phrase living only in Context is not a match\n  Given  a requirement whose only mention of a word is inside its `## Context`\n  When   `search` runs on that word\n  Then   the requirement is not returned by the literal layer\n\nCASE-4 — a query in the cached translation's language finds the requirement\n  Given  a requirement with a cached translation containing a phrase absent from its English text\n  When   `search` runs on that phrase\n  Then   the requirement is returned\n\nCASE-5 — the ranked model is untouched\n  Given  a query naming no id and appearing literally nowhere\n  When   `search` runs\n  Then   every result comes from the ranking model, with the same scores as before",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SEARCH-036"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "tested-by",
          "loc": "app/scripts/ssr-smoke.jsx:3"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/search.js:2"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/search.js:137"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5368"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5382"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10499"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Găsirea unei cerințe după id-ul ei și după textul ei literal",
          "intent": "Sacul de cuvinte după care este clasată o cerință este titlul ei, intenția ei și clauzele ei — id-ul ei nu se află în niciunul dintre acestea. Așa că o căutare în acest corpus după `ARCH-CHECK-006` a returnat `REQ-ORPHANCODE-888` și nu cerința numită, pentru că \"arch\" și \"check\" au fost potrivite ca proză obișnuită. Un id este cheia primară aici, iar o expresie care apare verbatim într-o cerință este o dovadă mai puternică decât o suprapunere parțială de tokenuri cu o alta — niciuna dintre ele nu este o chestiune de formulare, iar modelul de clasare nu poate răspunde la niciuna.",
          "contract": "> Sacul de cuvinte după care este clasată o cerință este titlul ei, intenția ei și clauzele ei — id-ul ei nu\n> se află în niciunul dintre acestea. Așa că o căutare în acest corpus după `ARCH-CHECK-006` a returnat `REQ-ORPHANCODE-888` și\n> nu cerința numită, pentru că \"arch\" și \"check\" au fost potrivite ca proză obișnuită. Un id este\n> cheia primară aici, iar o expresie care apare verbatim într-o cerință este o dovadă mai puternică\n> decât o suprapunere parțială de tokenuri cu o alta — niciuna dintre ele nu este o chestiune de formulare, iar\n> modelul de clasare nu poate răspunde la niciuna.\n\nFiecare punct de mai jos este obligatoriu.\n- O interogare egală cu id-ul unei cerințe returnează acea cerință, singură, înaintea oricărui rezultat clasat.\n- O interogare care prefixează sau apare în interiorul id-urilor returnează acele cerințe înaintea rezultatelor\n  clasate, plafonat astfel încât un cuvânt comun să nu poată înghesui clasarea afară.\n- O interogare care apare literal în titlul, descrierea sau cazurile unei cerințe returnează acea\n  cerință înaintea rezultatelor clasate. `## Context` este exclus, exact așa cum este și din\n  sacul de clasare: comentariile nu sunt despre ce anume este o cerință.\n- Căutarea literală citește și traducerea în cache a unei cerințe, astfel încât o interogare în limba în care\n  cititorul este servit găsește cerința chiar dacă modelul de clasare indexează o singură limbă.\n- Modelul de clasare în sine rămâne neschimbat de toate acestea: straturile de id și de literal selectează\n  cerințe, iar stratul clasat completează ce lasă ele, așa că scorul purtat de un rezultat înseamnă în continuare\n  exact ce însemna înainte.",
          "acceptance": "CASE-1 — un id exact returnează propria cerință prima\n  Given  un corpus care conține `AREA-X-001` și cerințe a căror proză menționează \"area\" și \"x\"\n  When   rulează `search \"AREA-X-001\"`\n  Then   `AREA-X-001` este primul rezultat, marcat ca potrivire de id\n\nCASE-2 — un id parțial returnează id-urile pe care le numește\n  Given  un corpus care conține `AREA-X-001` și `AREA-X-002`\n  When   rulează `search \"AREA-X\"`\n  Then   ambele sunt returnate înaintea oricărui rezultat clasat\n\nCASE-3 — o expresie din interiorul unui caz este găsită\n  Given  o cerință a cărei singură mențiune a lui \"ghost tag\" se află în interiorul unuia dintre cazurile ei\n  When   rulează `search \"ghost tag\"`\n  Then   acea cerință este returnată, marcată ca potrivire de text\n\nCASE-6 — o expresie care trăiește doar în Context nu este o potrivire\n  Given  o cerință a cărei singură mențiune a unui cuvânt se află în interiorul secțiunii ei `## Context`\n  When   `search` rulează pe acel cuvânt\n  Then   cerința nu este returnată de stratul literal\n\nCASE-4 — o interogare în limba traducerii din cache găsește cerința\n  Given  o cerință cu o traducere în cache care conține o expresie absentă din textul ei englezesc\n  When   `search` rulează pe acea expresie\n  Then   cerința este returnată\n\nCASE-5 — modelul clasat este neatins\n  Given  o interogare care nu numește niciun id și care nu apare literal nicăieri\n  When   `search` rulează\n  Then   fiecare rezultat provine din modelul de clasare, cu aceleași scoruri ca înainte"
        }
      }
    },
    {
      "id": "ARCH-SELFGATE-039",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "This repo's own gate wiring",
      "intent": "`reqmap.py` ships a gate other repos vendor and run — but until now, none of the files that actually WIRE the gate into this repo (CI, the GitHub Action, the git hooks, the cache-sync script) carried a single tag pointing back at the capability they invoke. \"Tag your own pipeline\" (v2.9 TODO.md) closes that gap.",
      "contract": [
        "Five repo-root files — `ci.yml`, `check/action.yml`, both dev git hooks, and `sync_reqmap.sh` — each wire the gate into a real entry point (CI, a consumer's Action, a local commit/push, the cache-sync script), and each carries a member tag pointing back at this requirement. [[REQ-SELFGATE-916]]"
      ],
      "verify": [],
      "notes": [
        "`lint_exempt: ac-count-high` — the capability is \"the gate runs at every entry point\", and each case is one of those entry points. They cannot fail independently of the obligation they share, which is the test for whether a clause deserves its own requirement.",
        "This requirement exists to give these 5 files a member tag, not to re-describe `gate`'s own behavior — that contract lives in [[ARCH-CHECK-006]].",
        "`lint_exempt: file-spread` — spanning CI, the composite action, both dev hooks and the sync script is the capability (the gate wired at every entry point), not a sign it is diffuse."
      ],
      "current_impl": [
        "`.github/workflows/ci.yml`, `check/action.yml`, `.githooks/pre-commit`, `.githooks/pre-push`, `sync_reqmap.sh` (all repo root).",
        "The alias axis is asserted by `scripts/check_versions.py` (`ACTION_REF_FILES`), covered by",
        "The alias major equals the plugin's major, and the same check refuses a release where they disagree. `scripts/test_check_versions.py`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a push or pull request to this repo When   `ci.yml`'s `gate-and-tests` job runs Then   `reqmap.py gate`, `lint --strict`, and `map --check` all exit 0 before any other job runs",
        "CASE-2 — Given  a local commit attempt with the dev hook enabled (`core.hooksPath .githooks`) When   `.githooks/pre-commit` runs Then   it fails the commit on the same errors CI would fail on, before the commit is created",
        "CASE-3 — Given  a consumer repo referencing `uses: alxmax/requirement-manager/check@v5` When   their own CI runs that step Then   `check/action.yml` invokes the same gate this repo runs on itself",
        "CASE-4 — Given  a local push attempt with the dev hook enabled and the target branch is `main` When   `.githooks/pre-push` runs Then   the push is blocked before it reaches the remote",
        "CASE-5 — Given  `sync_reqmap.sh` is run with zero or more consumer-repo paths as arguments When   it completes Then   `plugin/scripts/reqmap.py` (and the vendored viewer template, if present) in the local plugin cache and every named consumer repo matches this repo's current copy",
        "CASE-6 — Given  a push to `main`, whether or not it bumps `plugin.json` When   the `release` job's alias step runs Then   the major-alias tag read from `check/action.yml` points at the commit tagged with the current `plugin.json` version, so `check@vN` resolves to the latest released content",
        "CASE-7 — Given  `check/action.yml`, `README.md` and `CLAUDE.md` do not all name the same `check@vN` When   `scripts/check_versions.py` runs in the `gate-and-tests` job Then   it exits 1 and names the file that disagrees, before the alias can be published",
        "CASE-8 — Given  every documented `check@vN` agrees, but names a major other than `plugin.json`'s When   `scripts/check_versions.py` runs in the `gate-and-tests` job Then   it exits 1 and says the alias must track the plugin's major, so `v4.0.0` cannot ship advertised as `@v3`"
      ],
      "accept": "CASE-1  <!-- verifiable by: inspection -->\n  Given  a push or pull request to this repo\n  When   `ci.yml`'s `gate-and-tests` job runs\n  Then   `reqmap.py gate`, `lint --strict`, and `map --check` all exit 0 before any other job runs\n\nCASE-2  <!-- verifiable by: inspection -->\n  Given  a local commit attempt with the dev hook enabled (`core.hooksPath .githooks`)\n  When   `.githooks/pre-commit` runs\n  Then   it fails the commit on the same errors CI would fail on, before the commit is created\n\nCASE-3  <!-- verifiable by: inspection -->\n  Given  a consumer repo referencing `uses: alxmax/requirement-manager/check@v5`\n  When   their own CI runs that step\n  Then   `check/action.yml` invokes the same gate this repo runs on itself\n\nCASE-4  <!-- verifiable by: inspection -->\n  Given  a local push attempt with the dev hook enabled and the target branch is `main`\n  When   `.githooks/pre-push` runs\n  Then   the push is blocked before it reaches the remote\n\nCASE-5  <!-- verifiable by: inspection -->\n  Given  `sync_reqmap.sh` is run with zero or more consumer-repo paths as arguments\n  When   it completes\n  Then   `plugin/scripts/reqmap.py` (and the vendored viewer template, if present) in the local\n         plugin cache and every named consumer repo matches this repo's current copy\n\nCASE-6  <!-- verifiable by: inspection -->\n  Given  a push to `main`, whether or not it bumps `plugin.json`\n  When   the `release` job's alias step runs\n  Then   the major-alias tag read from `check/action.yml` points at the commit tagged with the\n         current `plugin.json` version, so `check@vN` resolves to the latest released content\n\nCASE-7\n  Given  `check/action.yml`, `README.md` and `CLAUDE.md` do not all name the same `check@vN`\n  When   `scripts/check_versions.py` runs in the `gate-and-tests` job\n  Then   it exits 1 and names the file that disagrees, before the alias can be published\n\nCASE-8\n  Given  every documented `check@vN` agrees, but names a major other than `plugin.json`'s\n  When   `scripts/check_versions.py` runs in the `gate-and-tests` job\n  Then   it exits 1 and says the alias must track the plugin's major, so `v4.0.0` cannot\n         ship advertised as `@v3`",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-CHECK-006"
      ],
      "used_by": [
        "ARCH-REPRO-041",
        "ARCH-STALEENGINE-043"
      ],
      "satisfies": [
        "SYS-SHIP-108"
      ],
      "satisfied_by": [
        "REQ-SELFGATE-916"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "sync_reqmap.sh:2"
        },
        {
          "role": "implements",
          "loc": ".githooks/pre-commit:2"
        },
        {
          "role": "implements",
          "loc": ".githooks/pre-push:2"
        },
        {
          "role": "implements",
          "loc": ".github/workflows/ci.yml:2"
        },
        {
          "role": "implements",
          "loc": "check/action.yml:2"
        },
        {
          "role": "implements",
          "loc": "scripts/check_versions.py:2"
        },
        {
          "role": "tested-by",
          "loc": "scripts/test_check_engine_bump.py:56"
        },
        {
          "role": "tested-by",
          "loc": "scripts/test_check_versions.py:93"
        },
        {
          "role": "tested-by",
          "loc": "scripts/test_check_versions.py:100"
        }
      ],
      "test_exempt": "pipeline wiring (YAML/shell config invoking the gate) — no unit-testable behavior of its own; correctness is observed by CI/the hook actually running, per CASE-1/CASE-2. CASE-7's alias-coherence check is the one exception and IS unit-tested, in scripts/test_check_versions.py (repo-local dev tooling, outside the scanned engine)",
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 2,
      "covered": 2
    },
    {
      "id": "REQ-SELFGATE-916",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Five files wire the gate into CI, hooks, and a consumer's Action",
      "intent": "`reqmap.py` ships a gate other repos vendor and run, but until \"tag your own pipeline\" (v2.9 TODO.md), none of the files that actually invoke it here — CI, the published Action, the local git hooks, the cache-sync script — carried a tag back to that fact. Without the tag, a change to any of these five could silently stop enforcing the gate and nothing in the requirement graph would show it.",
      "contract": [
        "`.github/workflows/ci.yml`'s `gate-and-tests` job invokes `reqmap.py gate` / `lint --strict` / `map --check` on every push and pull request.",
        "`check/action.yml` packages the same invocation as a reusable GitHub Action for consumer repos.",
        "`ci.yml`'s `release` job force-moves the action's major-alias tag — the `@vN` named by the `uses:` reference in `check/action.yml` — onto the commit the current `plugin.json` version is tagged at, on every push to `main`.",
        "`.githooks/pre-commit` mirrors the CI order locally, before a commit is created.",
        "`.githooks/pre-push` blocks a direct push to `main`.",
        "`sync_reqmap.sh` propagates `plugin/scripts/reqmap.py` (+ the vendored viewer template) into the local plugin cache and any consumer repos passed as arguments."
      ],
      "verify": [],
      "notes": [
        "`lint_exempt: file-spread` — the five files ARE this requirement. Spanning CI, the composite action, both dev hooks and the sync script is the capability, not a sign it is diffuse; a version of it that touched one file would assert nothing."
      ],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the CI job runs all three checks on both triggers Given  `.github/workflows/ci.yml` When   its `gate-and-tests` job is read Then   the job invokes `gate`, `lint --strict`, `map --check`; the workflow triggers on both push and pull_request",
        "CASE-2 — — the composite action runs the same three checks Given  `check/action.yml` When   its `runs.steps` are read Then   the composite action invokes `reqmap.py gate`, `map --check` and `lint --strict`, the same commands `ci.yml` runs on itself",
        "CASE-3 — — the release job moves the action's major-alias tag on every push to main Given  a push to `main`, whether or not `plugin.json`'s version changed When   `ci.yml`'s `release` job's alias step runs Then   the `@vN` tag named in `check/action.yml`'s `uses:` reference is force-moved onto the commit tagged with the current `plugin.json` version",
        "CASE-4 — — the hook runs checks in the same relative order as CI Given  `.githooks/pre-commit` When   its script body is read top to bottom Then   it runs `check_versions.py`, `check_engine_bump.py --staged`, `gate`, `lint --strict`, `map --check` in that order, matching `ci.yml`'s `gate-and-tests` job",
        "CASE-5 — — the pre-push hook blocks a direct push to main Given  `.githooks/pre-push` enabled and a local push whose target branch is `main` When   `git push` runs Then   the hook rejects the push before it reaches the remote",
        "CASE-6 — — sync_reqmap.sh propagates the engine to the local cache and named consumers Given  `sync_reqmap.sh` run with zero or more consumer-repo paths as arguments When   it completes Then   `plugin/scripts/reqmap.py` (and the vendored viewer template, if present) in the local plugin cache and every named consumer repo matches this repo's current copy"
      ],
      "accept": "CASE-1 — the CI job runs all three checks on both triggers\n  Given  `.github/workflows/ci.yml`\n  When   its `gate-and-tests` job is read\n  Then   the job invokes `gate`, `lint --strict`, `map --check`; the workflow triggers on\n         both push and pull_request\n\nCASE-2 — the composite action runs the same three checks\n  Given  `check/action.yml`\n  When   its `runs.steps` are read\n  Then   the composite action invokes `reqmap.py gate`, `map --check` and `lint --strict`, the same commands `ci.yml` runs on itself\n\nCASE-3 — the release job moves the action's major-alias tag on every push to main\n  Given  a push to `main`, whether or not `plugin.json`'s version changed\n  When   `ci.yml`'s `release` job's alias step runs\n  Then   the `@vN` tag named in `check/action.yml`'s `uses:` reference is force-moved onto the\n         commit tagged with the current `plugin.json` version\n\nCASE-4 — the hook runs checks in the same relative order as CI\n  Given  `.githooks/pre-commit`\n  When   its script body is read top to bottom\n  Then   it runs `check_versions.py`, `check_engine_bump.py --staged`, `gate`, `lint --strict`, `map --check` in that order, matching `ci.yml`'s `gate-and-tests` job\n\nCASE-5 — the pre-push hook blocks a direct push to main\n  Given  `.githooks/pre-push` enabled and a local push whose target branch is `main`\n  When   `git push` runs\n  Then   the hook rejects the push before it reaches the remote\n\nCASE-6 — sync_reqmap.sh propagates the engine to the local cache and named consumers\n  Given  `sync_reqmap.sh` run with zero or more consumer-repo paths as arguments\n  When   it completes\n  Then   `plugin/scripts/reqmap.py` (and the vendored viewer template, if present) in the local\n         plugin cache and every named consumer repo matches this repo's current copy",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SELFGATE-039"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "sync_reqmap.sh:2"
        },
        {
          "role": "implements",
          "loc": ".githooks/pre-commit:2"
        },
        {
          "role": "implements",
          "loc": ".githooks/pre-push:2"
        },
        {
          "role": "implements",
          "loc": ".github/workflows/ci.yml:2"
        },
        {
          "role": "implements",
          "loc": "check/action.yml:2"
        }
      ],
      "test_exempt": "pipeline wiring (CI, hooks, the Action) observed by running it, not by a unit test",
      "milestone": null,
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Cinci fișiere leagă gate-ul de CI, hooks, și Action-ul unui consumator",
          "intent": "`reqmap.py` livrează un gate pe care alte repo-uri îl vendorizează și îl rulează, dar până la \"tag your own pipeline\" (v2.9 TODO.md), niciunul dintre fișierele care de fapt îl invocă aici — CI, Action-ul publicat, hook-urile locale de git, scriptul de sincronizare-cache — nu purta un tag înapoi la acest fapt. Fără tag, o schimbare la oricare dintre aceste cinci ar putea opri în tăcere aplicarea gate-ului, și nimic din graful de cerințe nu ar arăta asta.",
          "contract": "> `reqmap.py` livrează un gate pe care alte repo-uri îl vendorizează și îl rulează, dar până\n> la \"tag your own pipeline\" (v2.9 TODO.md), niciunul dintre fișierele care de fapt îl invocă\n> aici — CI, Action-ul publicat, hook-urile locale de git, scriptul de sincronizare-cache — nu\n> purta un tag înapoi la acest fapt. Fără tag, o schimbare la oricare dintre aceste cinci ar\n> putea opri în tăcere aplicarea gate-ului, și nimic din graful de cerințe nu ar arăta asta.\n\nFiecare punct de mai jos este obligatoriu.\n- Job-ul `gate-and-tests` al lui `.github/workflows/ci.yml` invocă `reqmap.py gate` / `lint --strict`\n  / `map --check` la fiecare push și pull request.\n- `check/action.yml` împachetează aceeași invocare ca o GitHub Action reutilizabilă pentru repo-urile consumatoare.\n- Job-ul `release` al lui `ci.yml` forțează mutarea tag-ului alias major al action-ului — `@vN` numit de\n  referința `uses:` din `check/action.yml` — pe commit-ul unde este marcat versiunea curentă din `plugin.json`,\n  la fiecare push pe `main`.\n- `.githooks/pre-commit` oglindește ordinea CI local, înainte ca un commit să fie creat.\n- `.githooks/pre-push` blochează un push direct pe `main`.\n- `sync_reqmap.sh` propagă `plugin/scripts/reqmap.py` (+ șablonul de viewer vendorizat) în\n  cache-ul local al plugin-ului și în orice repo-uri consumatoare pasate ca argumente.",
          "acceptance": "CASE-1 — job-ul CI rulează toate cele trei verificări la ambele declanșatoare\n  Given  `.github/workflows/ci.yml`\n  When   job-ul lui `gate-and-tests` este citit\n  Then   job-ul invocă `gate`, `lint --strict`, `map --check`; workflow-ul se declanșează la\n         atât push cât și pull_request\n\nCASE-2 — action-ul compozit rulează aceleași trei verificări\n  Given  `check/action.yml`\n  When   `runs.steps`-urile lui sunt citite\n  Then   action-ul compozit invocă `reqmap.py gate`, `map --check` și `lint --strict`, aceleași comenzi pe care `ci.yml` le rulează asupra sa\n\nCASE-3 — job-ul release mută tag-ul alias major al action-ului la fiecare push pe main\n  Given  un push pe `main`, indiferent dacă versiunea din `plugin.json` s-a schimbat sau nu\n  When   pasul de alias al job-ului `release` din `ci.yml` rulează\n  Then   tag-ul `@vN` numit în referința `uses:` din `check/action.yml` este forțat mutat pe\n         commit-ul marcat cu versiunea curentă din `plugin.json`\n\nCASE-4 — hook-ul rulează verificările în aceeași ordine relativă ca CI\n  Given  `.githooks/pre-commit`\n  When   corpul scriptului lui este citit de sus în jos\n  Then   rulează `check_versions.py`, `check_engine_bump.py --staged`, `gate`, `lint --strict`, `map --check` în această ordine, corespunzând job-ului `gate-and-tests` din `ci.yml`\n\nCASE-5 — hook-ul pre-push blochează un push direct pe main\n  Given  `.githooks/pre-push` activat și un push local a cărui ramură țintă este `main`\n  When   `git push` rulează\n  Then   hook-ul respinge push-ul înainte să ajungă la remote\n\nCASE-6 — sync_reqmap.sh propagă motorul la cache-ul local și la consumatorii numiți\n  Given  `sync_reqmap.sh` rulat cu zero sau mai multe căi de repo-uri consumatoare ca argumente\n  When   se termină\n  Then   `plugin/scripts/reqmap.py` (și șablonul de viewer vendorizat, dacă este prezent) din cache-ul\n         local al plugin-ului și fiecare repo consumator numit se potrivesc cu copia curentă a acestui repo"
        }
      }
    },
    {
      "id": "ARCH-SHOW-015",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Single-requirement dossier",
      "intent": "To understand one requirement today you open its file, then hunt through other files to see what depends on it and which code carries it out. This gathers all of that onto one screen: the title and intent, what it promises, what it depends on and what depends on it, where it lives in the code, any open questions, and any risk signals. Without it, answering \"what does this do and where is it?\" means cross-referencing several files by hand.",
      "contract": [
        "`show <ID>` prints one consolidated, read-only, human-readable view of a single requirement: a header, the full intent, and the contract bullets. [[REQ-SHOW-917]]",
        "`show` prints dependencies in both directions (the `depends_on` ids and the reverse edges) and the code members grouped by role with their `file:line`. [[REQ-SHOW-918]]",
        "`show` lists the open `## Verify intent` questions and the risk signals with their advice, reusing the same sources as `findings` and `next`. [[REQ-SHOW-919]]",
        "`show` returns zero for a known id and a non-zero code with a clear message for an unknown one. [[REQ-SHOW-919]]"
      ],
      "verify": [],
      "notes": [
        "The view is read-only and never edits the requirement. To change a status, use `confirm`; to change the contract, edit the file.",
        "The reverse-dependency list is computed by scanning every requirement's `depends_on` on each call. The corpus is small, so this is not cached."
      ],
      "current_impl": [
        "`cmd_show` in `reqmap.py` — looks the id up in the loaded requirements, then prints the header, intent, contract, both dependency directions, members, open verify-intent, and risk signals. It reuses `_bullets`, `_req_title`, `_as_list`, `_risk_signals` and `RISK_ADVICE`, so the view agrees with `next`, `findings`, and the map.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a known id When   `show` runs Then   it prints the id, status, and layer on the header line and returns zero",
        "CASE-2 — Given  a requirement with a `priority` field When   `show` runs Then   the priority value appears on the header line; given none, the header carries no empty priority segment",
        "CASE-3 — Given  an unknown id When   `show` runs Then   it prints a \"no requirement with id\" message and returns a non-zero code",
        "CASE-4 — Given  a requirement that another requirement depends on When   `show` runs Then   the depender's id appears under \"Depended on by\"",
        "CASE-5 — Given  a requirement with a tagged member When   `show` runs Then   the member's role and `file:line` appear under members",
        "CASE-6 — Given  a requirement with an open verify-intent bullet When   `show` runs Then   that bullet appears and a \"None\" placeholder bullet does not"
      ],
      "accept": "CASE-1\n  Given  a known id\n  When   `show` runs\n  Then   it prints the id, status, and layer on the header line and returns zero\n\nCASE-2\n  Given  a requirement with a `priority` field\n  When   `show` runs\n  Then   the priority value appears on the header line; given none, the header carries no\n         empty priority segment\n\nCASE-3\n  Given  an unknown id\n  When   `show` runs\n  Then   it prints a \"no requirement with id\" message and returns a non-zero code\n\nCASE-4\n  Given  a requirement that another requirement depends on\n  When   `show` runs\n  Then   the depender's id appears under \"Depended on by\"\n\nCASE-5\n  Given  a requirement with a tagged member\n  When   `show` runs\n  Then   the member's role and `file:line` appear under members\n\nCASE-6\n  Given  a requirement with an open verify-intent bullet\n  When   `show` runs\n  Then   that bullet appears and a \"None\" placeholder bullet does not",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001",
        "ARCH-SCAN-002"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-REPORT-105"
      ],
      "satisfied_by": [
        "REQ-SHOW-917",
        "REQ-SHOW-918",
        "REQ-SHOW-919"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5033"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3205"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8748"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.14",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Dosar pentru o singură cerință",
          "intent": "Pentru a înțelege o cerință astăzi, deschizi fișierul ei, apoi cauți prin alte fișiere pentru a vedea ce depinde de ea și ce cod o duce la îndeplinire. Aceasta adună tot acest lucru pe un singur ecran: titlul și intenția, ce promite, de ce depinde și ce depinde de ea, unde trăiește în cod, orice întrebări deschise, și orice semnale de risc. Fără aceasta, a răspunde la \"ce face asta și unde este?\" înseamnă referențierea încrucișată a mai multor fișiere manual.",
          "contract": "> Pentru a înțelege o cerință astăzi, deschizi fișierul ei, apoi cauți prin alte\n> fișiere pentru a vedea ce depinde de ea și ce cod o duce la îndeplinire. Aceasta\n> adună tot acest lucru pe un singur ecran: titlul și intenția, ce promite, de ce\n> depinde și ce depinde de ea, unde trăiește în cod, orice întrebări deschise, și\n> orice semnale de risc. Fără aceasta, a răspunde la \"ce face asta și unde este?\"\n> înseamnă referențierea încrucișată a mai multor fișiere manual.\n\nFiecare punct de mai jos este obligatoriu.\n- `show <ID>` afișează o vizualizare consolidată, doar-citire, lizibilă pentru oameni, a unei singure cerințe: un antet, intenția completă, și punctele de contract. [[REQ-SHOW-917]]\n- `show` afișează dependențele în ambele direcții (id-urile `depends_on` și muchiile inverse) și membrii de cod grupați pe rol cu `file:line`-ul lor. [[REQ-SHOW-918]]\n- `show` listează întrebările deschise `## Verify intent` și semnalele de risc cu sfatul lor, refolosind aceleași surse ca `findings` și `next`. [[REQ-SHOW-919]]\n- `show` returnează zero pentru un id cunoscut și un cod diferit de zero cu un mesaj clar pentru unul necunoscut. [[REQ-SHOW-919]]",
          "acceptance": "CASE-1\n  Given  un id cunoscut\n  When   `show` rulează\n  Then   afișează id-ul, status-ul, și layer-ul pe linia de antet și returnează zero\n\nCASE-2\n  Given  o cerință cu un câmp `priority`\n  When   `show` rulează\n  Then   valoarea de prioritate apare pe linia de antet; în absența ei, antetul nu poartă\n         niciun segment gol de prioritate\n\nCASE-3\n  Given  un id necunoscut\n  When   `show` rulează\n  Then   afișează un mesaj \"no requirement with id\" și returnează un cod diferit de zero\n\nCASE-4\n  Given  o cerință de care depinde altă cerință\n  When   `show` rulează\n  Then   id-ul dependentului apare sub \"Depended on by\"\n\nCASE-5\n  Given  o cerință cu un membru etichetat\n  When   `show` rulează\n  Then   rolul membrului și `file:line` apar sub membri\n\nCASE-6\n  Given  o cerință cu un punct verify-intent deschis\n  When   `show` rulează\n  Then   acel punct apare iar un punct placeholder \"None\" nu"
        }
      }
    },
    {
      "id": "REQ-SHOW-917",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "A one-screen header, intent and contract",
      "intent": "Understanding one requirement used to mean opening its file, then hunting through other files for what depends on it and what code carries it out. `show <ID>` is read-only and puts the header (id, status, layer and optional fields), the full multi-line intent, and the contract bullets on one screen — or says plainly that no such requirement exists.",
      "contract": [
        "`show <ID>` prints one consolidated, human-readable view of a single requirement.",
        "`show` writes nothing. It only reads and prints.",
        "`show` prints a header line carrying the id, the status and the layer.",
        "The header appends `priority` after the layer when that field is present, then `milestone`.",
        "An absent optional field adds no empty segment to the header.",
        "`show` prints the title and the intent. A WHY spanning several `>` lines is gathered whole, never truncated to its first line.",
        "`show` lists the Contract bullets. When the requirement has no `## Description` section, `show` says so instead."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — one show call surfaces header, contract, deps, and members together Given  a requirement with a Contract bullet, a `depends_on` target, and a tagged member When   `show <ID>` runs once Then   the single output contains the header, \"Contract:\", \"Depends on:\", and \"Members in code\" — no second command needed",
        "CASE-2 — — show leaves the requirement file byte-identical on disk Given  a requirement file with known content, loaded and passed to `cmd_show` When   `show <ID>` runs Then   the file's on-disk bytes are unchanged afterward",
        "CASE-3 — — the header line carries id, status, and layer Given  a known requirement `REQ-X-001` with status `confirmed` and layer `feature` When   `show REQ-X-001` runs Then   its output contains \"REQ-X-001\", \"confirmed\", and \"feature\", and it returns 0",
        "CASE-4 — — a set priority appears on the header line Given  a requirement with `priority: must-have` When   `show` runs Then   \"must-have\" appears in the first line of the output",
        "CASE-5 — — no priority leaves no blank header segment Given  a requirement with no `priority` field When   `show` runs Then   the header line contains no empty \"·  ·\" segment",
        "CASE-6 — — a multi-line WHY blockquote prints in full, not just its first line Given  a requirement whose intent is three consecutive `>` lines When   `_first_quote` (called by `show`) gathers the intent Then   it returns all three lines joined, not only the first",
        "CASE-7 — — a requirement with no Description section says so instead of listing bullets Given  a requirement body with no `## Description`/`## Contract` section When   `show` runs Then   under \"Contract:\" it prints \"(none — no '## Description' section)\" --------------------"
      ],
      "accept": "CASE-1 — one show call surfaces header, contract, deps, and members together\n  Given  a requirement with a Contract bullet, a `depends_on` target, and a tagged member\n  When   `show <ID>` runs once\n  Then   the single output contains the header, \"Contract:\", \"Depends on:\", and \"Members\n         in code\" — no second command needed\n\nCASE-2 — show leaves the requirement file byte-identical on disk\n  Given  a requirement file with known content, loaded and passed to `cmd_show`\n  When   `show <ID>` runs\n  Then   the file's on-disk bytes are unchanged afterward\n\nCASE-3 — the header line carries id, status, and layer\n  Given  a known requirement `REQ-X-001` with status `confirmed` and layer `feature`\n  When   `show REQ-X-001` runs\n  Then   its output contains \"REQ-X-001\", \"confirmed\", and \"feature\", and it returns 0\n\nCASE-4 — a set priority appears on the header line\n  Given  a requirement with `priority: must-have`\n  When   `show` runs\n  Then   \"must-have\" appears in the first line of the output\n\nCASE-5 — no priority leaves no blank header segment\n  Given  a requirement with no `priority` field\n  When   `show` runs\n  Then   the header line contains no empty \"·  ·\" segment\n\nCASE-6 — a multi-line WHY blockquote prints in full, not just its first line\n  Given  a requirement whose intent is three consecutive `>` lines\n  When   `_first_quote` (called by `show`) gathers the intent\n  Then   it returns all three lines joined, not only the first\n\nCASE-7 — a requirement with no Description section says so instead of listing bullets\n  Given  a requirement body with no `## Description`/`## Contract` section\n  When   `show` runs\n  Then   under \"Contract:\" it prints \"(none — no '## Description' section)\"\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SHOW-015"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5033"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6261"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3205"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8748"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 7,
      "covered": 7,
      "i18n": {
        "ro": {
          "title": "Un antet pe un singur ecran, intent și contract",
          "intent": "A înțelege o cerință însemna altfel deschiderea fișierului ei, apoi căutarea prin alte fișiere pentru ce depinde de ea și ce cod o duce la îndeplinire. `show <ID>` este read-only și pune antetul (id, status, layer și câmpuri opționale), intentul complet pe mai multe linii, și punctele din contract pe un singur ecran — sau spune clar că nu există o astfel de cerință.",
          "contract": "> A înțelege o cerință însemna altfel deschiderea fișierului ei, apoi căutarea prin alte\n> fișiere pentru ce depinde de ea și ce cod o duce la îndeplinire. `show <ID>` este read-only și\n> pune antetul (id, status, layer și câmpuri opționale), intentul complet pe mai multe linii, și\n> punctele din contract pe un singur ecran — sau spune clar că nu există o astfel de cerință.\n\nFiecare punct de mai jos este obligatoriu.\n- `show <ID>` afișează o vizualizare consolidată, lizibilă pentru om, a unei singure cerințe.\n- `show` nu scrie nimic. Doar citește și afișează.\n- `show` afișează o linie de antet purtând id-ul, status-ul și layer-ul.\n- Antetul adaugă `priority` după layer când acel câmp este prezent, apoi `milestone`.\n- Un câmp opțional absent nu adaugă niciun segment gol la antet.\n- `show` afișează titlul și intentul. Un WHY care se întinde pe mai multe linii `>` este adunat integral,\n  niciodată trunchiat la prima lui linie.\n- `show` listează punctele din Contract. Când cerința nu are nicio secțiune `## Description`,\n  `show` spune asta în loc.",
          "acceptance": "CASE-1 — un singur apel show scoate la iveală antetul, contractul, dependențele și membrii împreună\n  Given  o cerință cu un punct de Contract, o țintă `depends_on`, și un membru etichetat\n  When   `show <ID>` rulează o singură dată\n  Then   singura ieșire conține antetul, \"Contract:\", \"Depends on:\", și \"Members\n         in code\" — fără să fie nevoie de o a doua comandă\n\nCASE-2 — show lasă fișierul de cerință identic byte cu byte pe disc\n  Given  un fișier de cerință cu conținut cunoscut, încărcat și pasat la `cmd_show`\n  When   `show <ID>` rulează\n  Then   octeții de pe disc ai fișierului sunt neschimbați ulterior\n\nCASE-3 — linia de antet poartă id, status și layer\n  Given  o cerință cunoscută `REQ-X-001` cu status `confirmed` și layer `feature`\n  When   `show REQ-X-001` rulează\n  Then   ieșirea ei conține \"REQ-X-001\", \"confirmed\", și \"feature\", și returnează 0\n\nCASE-4 — o prioritate setată apare pe linia de antet\n  Given  o cerință cu `priority: must-have`\n  When   `show` rulează\n  Then   \"must-have\" apare în prima linie a ieșirii\n\nCASE-5 — fără prioritate nu lasă niciun segment de antet gol\n  Given  o cerință fără câmp `priority`\n  When   `show` rulează\n  Then   linia de antet nu conține niciun segment gol \"·  ·\"\n\nCASE-6 — un blockquote WHY pe mai multe linii se afișează integral, nu doar prima lui linie\n  Given  o cerință al cărei intent este trei linii `>` consecutive\n  When   `_first_quote` (apelat de `show`) adună intentul\n  Then   returnează toate cele trei linii unite, nu doar prima\n\nCASE-7 — o cerință fără secțiune Description spune asta în loc să listeze punctele\n  Given  un corp de cerință fără secțiune `## Description`/`## Contract`\n  When   `show` rulează\n  Then   sub \"Contract:\" afișează \"(none — no '## Description' section)\"\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-SHOW-918",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Dependencies both ways, and the code members",
      "intent": "Knowing what a requirement will affect means seeing both what it depends on and what depends on it, plus exactly which code implements it. `show` computes the reverse edges by scanning every requirement's `depends_on` on each call — the corpus is small enough that this needs no cache — and lists members with their verification level when their `tested-by:` tag carries one.",
      "contract": [
        "`show` prints dependencies in both directions: the `depends_on` ids, and the reverse edges.",
        "`show` lists the code members grouped by role, each with its `file:line`. When no member is tagged, `show` says so.",
        "`show` prints the verification level beside a member whose `tested-by:` tag carries one."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — show lists a reverse dependency under Depended on by Given  `REQ-B-002` with `depends_on: [CORE-A-001]` When   `show CORE-A-001` runs Then   its output contains \"Depended on by\" and \"REQ-B-002\"",
        "CASE-2 — — a tagged member prints its role and file:line Given  a member `(\"implements\", \"src/foo.py\", 42)` for `REQ-X-001` When   `show REQ-X-001` runs Then   its output contains \"implements\" and \"src/foo.py:42\"",
        "CASE-3 — — a levelled tested-by member is annotated with its level Given  a `tested-by` member at `t.py:2` whose `levels` mapping marks it `integration` When   `show REQ-X-001` runs with that level data Then   its output contains \"@integration\" --------------------"
      ],
      "accept": "CASE-1 — show lists a reverse dependency under Depended on by\n  Given  `REQ-B-002` with `depends_on: [CORE-A-001]`\n  When   `show CORE-A-001` runs\n  Then   its output contains \"Depended on by\" and \"REQ-B-002\"\n\nCASE-2 — a tagged member prints its role and file:line\n  Given  a member `(\"implements\", \"src/foo.py\", 42)` for `REQ-X-001`\n  When   `show REQ-X-001` runs\n  Then   its output contains \"implements\" and \"src/foo.py:42\"\n\nCASE-3 — a levelled tested-by member is annotated with its level\n  Given  a `tested-by` member at `t.py:2` whose `levels` mapping marks it `integration`\n  When   `show REQ-X-001` runs with that level data\n  Then   its output contains \"@integration\"\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SHOW-015"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5033"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3205"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Dependențe în ambele sensuri, și membrii de cod",
          "intent": "A ști ce va afecta o cerință înseamnă a vedea atât de ce depinde, cât și ce depinde de ea, plus exact ce cod o implementează. `show` calculează muchiile inverse scanând `depends_on`-ul fiecărei cerințe la fiecare apel — corpusul este suficient de mic încât asta nu are nevoie de niciun cache — și listează membrii cu nivelul lor de verificare când tag-ul lor `tested-by:` poartă unul.",
          "contract": "> A ști ce va afecta o cerință înseamnă a vedea atât de ce depinde, cât și ce depinde de\n> ea, plus exact ce cod o implementează. `show` calculează muchiile inverse scanând\n> `depends_on`-ul fiecărei cerințe la fiecare apel — corpusul este suficient de mic încât\n> asta nu are nevoie de niciun cache — și listează membrii cu nivelul lor de verificare când\n> tag-ul lor `tested-by:` poartă unul.\n\nFiecare punct de mai jos este obligatoriu.\n- `show` afișează dependențele în ambele direcții: id-urile `depends_on`, și muchiile inverse.\n- `show` listează membrii de cod grupați după role, fiecare cu `file:line`-ul lui. Când niciun membru nu\n  este etichetat, `show` spune asta.\n- `show` afișează nivelul de verificare lângă un membru al cărui tag `tested-by:` poartă unul.",
          "acceptance": "CASE-1 — show listează o dependență inversă sub Depended on by\n  Given  `REQ-B-002` cu `depends_on: [CORE-A-001]`\n  When   `show CORE-A-001` rulează\n  Then   ieșirea lui conține \"Depended on by\" și \"REQ-B-002\"\n\nCASE-2 — un membru etichetat afișează role-ul lui și file:line\n  Given  un membru `(\"implements\", \"src/foo.py\", 42)` pentru `REQ-X-001`\n  When   `show REQ-X-001` rulează\n  Then   ieșirea lui conține \"implements\" și \"src/foo.py:42\"\n\nCASE-3 — un membru tested-by cu nivel este adnotat cu nivelul lui\n  Given  un membru `tested-by` la `t.py:2` a cărui mapare `levels` îl marchează `integration`\n  When   `show REQ-X-001` rulează cu acele date de nivel\n  Then   ieșirea lui conține \"@integration\"\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-SHOW-919",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Open questions, risk signals, and a caller-visible exit code",
      "intent": "A dossier is more useful when it also flags what still needs attention: open verify-intent questions and risk signals, both drawn from the same source `findings` and `next` use so the views can never disagree. And a typo'd id must be caught rather than silently produce an empty-looking report, so `show` exits non-zero with a clear message when the id does not exist.",
      "contract": [
        "`show` lists the open `## Verify intent` questions, using the same filter as `findings`, so the \"None\" placeholder is skipped.",
        "`show` lists the risk signals with their advice, reusing the same `_risk_signals` source as `next` and the Risk tab, so the three never disagree.",
        "`show` returns zero for a known id and a non-zero code for an unknown one, so a typo is visible to a caller or to CI."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — an open verify-intent bullet shows; the None placeholder does not Given  a body with one real verify-intent question and one \"None — doc is unambiguous\" placeholder bullet When   `show` runs Then   its output contains the real question and omits the placeholder bullet",
        "CASE-2 — — show prints a risk signal alongside its advice text Given  a requirement whose `_risk_signals(node)` yields at least one signal (e.g. `untested`) When   `show` runs Then   its output contains \"Risk signals:\" followed by that signal name and the matching `RISK_ADVICE` text",
        "CASE-3 — — exit code distinguishes a known id from an unknown one Given  a known id `REQ-X-001` and an unknown id `NOPE-000` When   `show` runs on each Then   the known id returns 0 and the unknown id returns 1 with \"no requirement with id NOPE-000\""
      ],
      "accept": "CASE-1 — an open verify-intent bullet shows; the None placeholder does not\n  Given  a body with one real verify-intent question and one \"None — doc is unambiguous\"\n         placeholder bullet\n  When   `show` runs\n  Then   its output contains the real question and omits the placeholder bullet\n\nCASE-2 — show prints a risk signal alongside its advice text\n  Given  a requirement whose `_risk_signals(node)` yields at least one signal (e.g.\n         `untested`)\n  When   `show` runs\n  Then   its output contains \"Risk signals:\" followed by that signal name and the matching\n         `RISK_ADVICE` text\n\nCASE-3 — exit code distinguishes a known id from an unknown one\n  Given  a known id `REQ-X-001` and an unknown id `NOPE-000`\n  When   `show` runs on each\n  Then   the known id returns 0 and the unknown id returns 1 with \"no requirement with id\n         NOPE-000\"",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SHOW-015"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5033"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3205"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:8748"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Întrebări deschise, semnale de risc, și un cod de ieșire vizibil apelantului",
          "intent": "Un dosar este mai util când semnalează de asemenea ce mai are nevoie de atenție: întrebări deschise de verify-intent și semnale de risc, ambele extrase din aceeași sursă pe care o folosesc `findings` și `next`, astfel încât vizualizările nu pot fi niciodată în dezacord. Și un id greșit trebuie prins, nu produs în tăcere ca un raport care pare gol, astfel `show` iese cu non-zero și un mesaj clar când id-ul nu există.",
          "contract": "> Un dosar este mai util când semnalează de asemenea ce mai are nevoie de atenție: întrebări\n> deschise de verify-intent și semnale de risc, ambele extrase din aceeași sursă pe care o\n> folosesc `findings` și `next`, astfel încât vizualizările nu pot fi niciodată în dezacord. Și\n> un id greșit trebuie prins, nu produs în tăcere ca un raport care pare gol, astfel `show`\n> iese cu non-zero și un mesaj clar când id-ul nu există.\n\nFiecare punct de mai jos este obligatoriu.\n- `show` listează întrebările deschise din `## Verify intent`, folosind același filtru ca\n  `findings`, astfel placeholder-ul \"None\" este omis.\n- `show` listează semnalele de risc cu sfaturile lor, refolosind aceeași sursă `_risk_signals` ca\n  `next` și tab-ul Risk, astfel cele trei nu pot fi niciodată în dezacord.\n- `show` returnează zero pentru un id cunoscut și un cod non-zero pentru unul necunoscut, astfel o greșeală de tastare este\n  vizibilă unui apelant sau unui CI.",
          "acceptance": "CASE-1 — un punct deschis de verify-intent apare; placeholder-ul None nu\n  Given  un corp cu o întrebare reală de verify-intent și un punct placeholder \"None — doc is unambiguous\"\n  When   `show` rulează\n  Then   ieșirea lui conține întrebarea reală și omite punctul placeholder\n\nCASE-2 — show afișează un semnal de risc alături de sfatul lui\n  Given  o cerință al cărei `_risk_signals(node)` produce cel puțin un semnal (de exemplu\n         `untested`)\n  When   `show` rulează\n  Then   ieșirea lui conține \"Risk signals:\" urmat de numele acelui semnal și textul corespunzător\n         `RISK_ADVICE`\n\nCASE-3 — codul de ieșire distinge un id cunoscut de unul necunoscut\n  Given  un id cunoscut `REQ-X-001` și un id necunoscut `NOPE-000`\n  When   `show` rulează pe fiecare\n  Then   id-ul cunoscut returnează 0, iar id-ul necunoscut returnează 1 cu \"no requirement with id\n         NOPE-000\""
        }
      }
    },
    {
      "id": "ARCH-SIMILAR-016",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Duplicate-capability detector",
      "intent": "When two requirements quietly describe the same capability, you do not notice until each has grown its own divergent implementation — and now there is a mess to untangle. This compares requirement pairs by their wording and flags the ones that overlap, so a reviewer can merge or sharpen them early. Without it, duplication stays invisible until it is expensive.",
      "contract": [
        "`dupes` reports pairs of requirements whose contracts overlap, and never writes any file. [[REQ-SIMILAR-920]]",
        "Each requirement's comparison text is a bag of words from its title, intent line and Contract bullets, tokenized to lowercase words of 3+ letters. [[REQ-SIMILAR-921]]",
        "An unauthored draft or a tested-by-linked pair is skipped and counted rather than compared. [[REQ-SIMILAR-921]]",
        "Terms are weighted by a smoothed TF-IDF and pairs are scored by cosine similarity in the range zero to one. [[REQ-SIMILAR-922]]",
        "Only pairs at or above the threshold (default `0.35`, overridable with `--threshold`) are reported, most-similar-first with their shared terms; `dupes` always exits zero. [[REQ-SIMILAR-923]]"
      ],
      "verify": [],
      "notes": [
        "The default threshold is a starting point, not a proven value. A reviewer tunes it with `--threshold` for the corpus at hand.",
        "Detection is lexical, not semantic. Two contracts that mean the same thing in different words may score low. The command surfaces likely duplicates; it does not prove duplication.",
        "Comparison is requirement-to-requirement only. Matching untagged code to an existing requirement is a separate, fuzzier problem left to `plan`."
      ],
      "current_impl": [
        "`cmd_similar`, `_sim_text`, `_sim_tokens`, `_tfidf`, `_cosine` and `_test_suite_pairs` in `reqmap.py` — `_sim_text` gathers the compared text, `_sim_tokens` builds the bag of words, `_tfidf` weights terms with smoothed inverse-document frequency, `_cosine` scores each pair, and `_test_suite_pairs` derives the tested-by-linked pairs from the member map. `cmd_similar` sorts the pairs and prints them with their shared terms.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  two requirements with near-identical Contract text When   `dupes` runs Then   the pair is reported with a high score",
        "CASE-2 — Given  two requirements about unrelated topics When   `dupes` runs at the default threshold Then   the pair is not reported",
        "CASE-3 — Given  a corpus with fewer than two requirements that have contract text When   `dupes` runs Then   it prints a \"need at least two\" message and returns zero",
        "CASE-4 — Given  a custom `--threshold` above a pair's score When   `dupes` runs Then   that pair is not reported",
        "CASE-5 — Given  any corpus When   `dupes` runs Then   it returns zero",
        "CASE-6 — Given  two fresh drafts whose Contract is the placeholder, and two authored requirements When   `dupes` runs Then   the drafts are skipped with a count line, and only the authored pair can be reported",
        "CASE-7 — Given  a requirement A whose `tested-by` file implements requirement B, with overlapping contracts When   `dupes` runs with the member map Then   the pair A–B is not reported and a \"linked by tested-by or satisfies\" count line is printed; without the member map it is reported as before"
      ],
      "accept": "CASE-1\n  Given  two requirements with near-identical Contract text\n  When   `dupes` runs\n  Then   the pair is reported with a high score\n\nCASE-2\n  Given  two requirements about unrelated topics\n  When   `dupes` runs at the default threshold\n  Then   the pair is not reported\n\nCASE-3\n  Given  a corpus with fewer than two requirements that have contract text\n  When   `dupes` runs\n  Then   it prints a \"need at least two\" message and returns zero\n\nCASE-4\n  Given  a custom `--threshold` above a pair's score\n  When   `dupes` runs\n  Then   that pair is not reported\n\nCASE-5\n  Given  any corpus\n  When   `dupes` runs\n  Then   it returns zero\n\nCASE-6\n  Given  two fresh drafts whose Contract is the placeholder, and two authored requirements\n  When   `dupes` runs\n  Then   the drafts are skipped with a count line, and only the authored pair can be reported\n\nCASE-7\n  Given  a requirement A whose `tested-by` file implements requirement B, with overlapping contracts\n  When   `dupes` runs with the member map\n  Then   the pair A–B is not reported and a \"linked by tested-by or satisfies\" count line is printed; without the member map it is reported as before",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001"
      ],
      "used_by": [
        "ARCH-AUDIT-065",
        "ARCH-IMPLEMENT-063",
        "ARCH-REDUNDANCY-058",
        "ARCH-SEARCH-036"
      ],
      "satisfies": [
        "SYS-QUALITY-104"
      ],
      "satisfied_by": [
        "REQ-SIMILAR-920",
        "REQ-SIMILAR-921",
        "REQ-SIMILAR-922",
        "REQ-SIMILAR-923"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5123"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5130"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5216"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5228"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5246"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5258"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5289"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3287"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6905"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9318"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.14",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Detector de capabilități duplicate",
          "intent": "Când două cerințe descriu în tăcere aceeași capabilitate, nu observi până când fiecare nu și-a dezvoltat propria implementare divergentă — și acum ai o încurcătură de descâlcit. Aceasta compară perechile de cerințe după formulare și marchează pe cele care se suprapun, astfel încât un recenzent le poate uni sau clarifica din timp. Fără ea, duplicarea rămâne invizibilă până devine costisitoare.",
          "contract": "> Când două cerințe descriu în tăcere aceeași capabilitate, nu observi până când fiecare\n> nu și-a dezvoltat propria implementare divergentă — și acum ai o încurcătură de\n> descâlcit. Aceasta compară perechile de cerințe după formulare și marchează pe cele care\n> se suprapun, astfel încât un recenzent le poate uni sau clarifica din timp. Fără ea, duplicarea rămâne invizibilă până devine costisitoare.\n\nFiecare punct de mai jos este obligatoriu.\n- `dupes` raportează perechi de cerințe ale căror contracte se suprapun și nu scrie niciodată vreun fișier. [[REQ-SIMILAR-920]]\n- Textul de comparație al fiecărei cerințe este un set de cuvinte din titlul, linia de intenție și punctele din Contract, tokenizate în cuvinte de 3+ litere, scrise cu minuscule. [[REQ-SIMILAR-921]]\n- Un draft fără conținut autorat sau o pereche legată prin tested-by este omisă și numărată, nu comparată. [[REQ-SIMILAR-921]]\n- Termenii sunt ponderați printr-un TF-IDF netezit, iar perechile sunt scorate prin similaritate cosinus în intervalul zero la unu. [[REQ-SIMILAR-922]]\n- Sunt raportate doar perechile la sau peste pragul (implicit `0.35`, care poate fi suprascris cu `--threshold`), în ordinea celei mai mari similarități, împreună cu termenii lor comuni; `dupes` iese întotdeauna cu zero. [[REQ-SIMILAR-923]]",
          "acceptance": "CASE-1\n  Given  două cerințe cu text de Contract aproape identic\n  When   rulează `dupes`\n  Then   perechea este raportată cu un scor ridicat\n\nCASE-2\n  Given  două cerințe despre subiecte fără legătură\n  When   `dupes` rulează la pragul implicit\n  Then   perechea nu este raportată\n\nCASE-3\n  Given  un corpus cu mai puțin de două cerințe care au text de contract\n  When   rulează `dupes`\n  Then   afișează un mesaj de tipul „are nevoie de cel puțin două” și returnează zero\n\nCASE-4\n  Given  un `--threshold` personalizat peste scorul unei perechi\n  When   rulează `dupes`\n  Then   acea pereche nu este raportată\n\nCASE-5\n  Given  orice corpus\n  When   rulează `dupes`\n  Then   returnează zero\n\nCASE-6\n  Given  două drafturi noi al căror Contract este placeholder-ul, și două cerințe autorate\n  When   rulează `dupes`\n  Then   drafturile sunt omise cu o linie de numărătoare, și doar perechea autorată poate fi raportată\n\nCASE-7\n  Given  o cerință A al cărei fișier `tested-by` implementează cerința B, cu contracte care se suprapun\n  When   `dupes` rulează cu harta membrilor\n  Then   perechea A–B nu este raportată și este afișată o linie de numărătoare „legată prin tested-by sau satisfies”; fără harta membrilor este raportată ca înainte"
        }
      }
    },
    {
      "id": "REQ-SIMILAR-920",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Reporting overlapping requirement pairs",
      "intent": "`dupes` exists so a reviewer finds two requirements that quietly describe the same capability before each grows its own divergent implementation. It only reads the corpus and prints — like `search`, it never edits a requirement file, so running it carries no risk of corrupting the corpus it is inspecting.",
      "contract": [
        "`dupes` reports pairs of requirements whose contracts overlap.",
        "`dupes` writes nothing. It only reads and prints."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — overlapping contracts are reported as a pair Given  two requirements whose Contract bullets share most of their wording, even with unrelated titles When   `dupes` runs Then   that pair appears in the printed report",
        "CASE-2 — — dupes runs without writing any file Given  a corpus with at least one flagged pair When   `dupes` runs Then   no requirement file changes and output goes only to stdout",
        "CASE-3 — — unrelated requirements are not reported Given  two requirements about unrelated topics When   `dupes` runs at the default threshold Then   neither appears in the report, which states no overlap was found --------------------"
      ],
      "accept": "CASE-1 — overlapping contracts are reported as a pair\n  Given  two requirements whose Contract bullets share most of their wording, even with\n         unrelated titles\n  When   `dupes` runs\n  Then   that pair appears in the printed report\n\nCASE-2 — dupes runs without writing any file\n  Given  a corpus with at least one flagged pair\n  When   `dupes` runs\n  Then   no requirement file changes and output goes only to stdout\n\nCASE-3 — unrelated requirements are not reported\n  Given  two requirements about unrelated topics\n  When   `dupes` runs at the default threshold\n  Then   neither appears in the report, which states no overlap was found\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SIMILAR-016"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5289"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3287"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9318"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Raportarea perechilor de cerințe care se suprapun",
          "intent": "`dupes` există astfel încât un revizor să găsească două cerințe care descriu în tăcere aceeași capacitate, înainte ca fiecare să-și dezvolte propria implementare divergentă. Doar citește corpusul și afișează — la fel ca `search`, nu editează niciodată un fișier de cerință, astfel rularea lui nu poartă niciun risc de a corupe corpusul pe care îl inspectează.",
          "contract": "> `dupes` există astfel încât un revizor să găsească două cerințe care descriu în tăcere\n> aceeași capacitate, înainte ca fiecare să-și dezvolte propria implementare divergentă. Doar\n> citește corpusul și afișează — la fel ca `search`, nu editează niciodată un fișier de\n> cerință, astfel rularea lui nu poartă niciun risc de a corupe corpusul pe care îl inspectează.\n\nFiecare punct de mai jos este obligatoriu.\n- `dupes` raportează perechi de cerințe ale căror contracte se suprapun.\n- `dupes` nu scrie nimic. Doar citește și afișează.",
          "acceptance": "CASE-1 — contractele care se suprapun sunt raportate ca o pereche\n  Given  două cerințe ale căror puncte din Contract partajează majoritatea formulării, chiar și cu\n         titluri fără legătură\n  When   `dupes` rulează\n  Then   acea pereche apare în raportul afișat\n\nCASE-2 — dupes rulează fără să scrie niciun fișier\n  Given  un corpus cu cel puțin o pereche semnalată\n  When   `dupes` rulează\n  Then   niciun fișier de cerință nu se schimbă, iar ieșirea merge doar la stdout\n\nCASE-3 — cerințele fără legătură nu sunt raportate\n  Given  două cerințe despre subiecte fără legătură\n  When   `dupes` rulează la pragul implicit\n  Then   niciuna nu apare în raport, care declară că nu a fost găsită nicio suprapunere\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-SIMILAR-921",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Building the comparison bag of words",
      "intent": "A fair comparison needs the same text for every requirement and needs to leave out text that would only add noise or produce a match nobody would call a real duplicate. So the bag of words is built from title, intent and Contract only (never the dense Notes section), stripped of stopwords, short tokens and pure numbers — and a requirement with nothing authored yet, or a pair already linked as a requirement and its own test suite, is skipped rather than compared.",
      "contract": [
        "`dupes` builds a bag of words for each requirement from its title, its intent line, and its Contract bullets.",
        "`dupes` leaves the \"Notes & limitations\" section out of that text, because that section is dense and would add noise.",
        "`dupes` tokenizes text into lowercase alphanumeric words of length three or more.",
        "`dupes` drops a small stopword set and pure numbers from those tokens.",
        "`dupes` skips a requirement whose Contract bullets are all still the draft placeholder (`TODO: …`), and prints how many it skipped. An unauthored contract has nothing to compare.",
        "`dupes` skips a pair linked by `tested-by` — one requirement's `tested-by` file is the other's `implements` file, so the second requirement IS the first one's test suite — and prints how many such pairs it skipped. Such a pair shares vocabulary by construction and is a known link, not a duplicate.",
        "`dupes` also skips a pair joined by a `satisfies:` edge: a child restates part of its parent's contract by construction, so a parent-child pair is a known link, not a duplicate."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the bag of words comes from title, intent and Contract only Given  two requirements sharing wording only in their titles and Contract bullets When   `dupes` builds each requirement's bag of words Then   that shared wording is included and drives the comparison",
        "CASE-2 — — text unique to the Notes section is left out of the bag Given  two unrelated requirements whose Notes sections happen to share several words When   `dupes` runs Then   that shared Notes wording does not raise their pair's score",
        "CASE-3 — — tokens shorter than three characters are dropped Given  two requirements whose only shared wording is two-letter tokens When   `dupes` tokenizes their text Then   those tokens are absent from either bag of words",
        "CASE-4 — — stopwords and pure numbers never count as shared terms Given  two unrelated requirements sharing only stopwords and a number like \"2026\" When   `dupes` runs Then   neither is listed among the pair's shared terms",
        "CASE-5 — — an unauthored draft is skipped and counted Given  a requirement whose Contract bullets are all still `TODO: …` When   `dupes` runs Then   that requirement is excluded from comparison and the skip count includes it",
        "CASE-6 — — a tested-by-linked pair is skipped and counted Given  requirement A whose `tested-by` file is requirement B's `implements` file, with overlapping wording When   `dupes` runs with the member map Then   the pair is excluded from the report and the linked-pairs count includes it",
        "CASE-7 — — a parent and its child are never a duplicate finding Given  two requirements with the same contract, one declaring `satisfies:` the other When   `dupes` runs Then   the pair is skipped and counted in the linked-pairs line --------------------"
      ],
      "accept": "CASE-1 — the bag of words comes from title, intent and Contract only\n  Given  two requirements sharing wording only in their titles and Contract bullets\n  When   `dupes` builds each requirement's bag of words\n  Then   that shared wording is included and drives the comparison\n\nCASE-2 — text unique to the Notes section is left out of the bag\n  Given  two unrelated requirements whose Notes sections happen to share several words\n  When   `dupes` runs\n  Then   that shared Notes wording does not raise their pair's score\n\nCASE-3 — tokens shorter than three characters are dropped\n  Given  two requirements whose only shared wording is two-letter tokens\n  When   `dupes` tokenizes their text\n  Then   those tokens are absent from either bag of words\n\nCASE-4 — stopwords and pure numbers never count as shared terms\n  Given  two unrelated requirements sharing only stopwords and a number like \"2026\"\n  When   `dupes` runs\n  Then   neither is listed among the pair's shared terms\n\nCASE-5 — an unauthored draft is skipped and counted\n  Given  a requirement whose Contract bullets are all still `TODO: …`\n  When   `dupes` runs\n  Then   that requirement is excluded from comparison and the skip count includes it\n\nCASE-6 — a tested-by-linked pair is skipped and counted\n  Given  requirement A whose `tested-by` file is requirement B's `implements` file, with overlapping wording\n  When   `dupes` runs with the member map\n  Then   the pair is excluded from the report and the linked-pairs count includes it\n\nCASE-7 — a parent and its child are never a duplicate finding\n  Given  two requirements with the same contract, one declaring `satisfies:` the other\n  When   `dupes` runs\n  Then   the pair is skipped and counted in the linked-pairs line\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SIMILAR-016"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5123"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5216"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5270"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3287"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6905"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7936"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9318"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 7,
      "covered": 7,
      "i18n": {
        "ro": {
          "title": "Construirea sacului de cuvinte pentru comparație",
          "intent": "O comparație corectă are nevoie de același text pentru fiecare cerință și trebuie să lase deoparte textul care ar adăuga doar zgomot sau ar produce o potrivire pe care nimeni nu ar numi-o o duplicare reală. Astfel sacul de cuvinte este construit doar din titlu, intent și Contract (niciodată din secțiunea densă Notes), curățat de stopword-uri, token-uri scurte și numere pure — iar o cerință fără nimic redactat încă, sau o pereche deja legată ca o cerință și propria ei suită de teste, este omisă în loc să fie comparată.",
          "contract": "> O comparație corectă are nevoie de același text pentru fiecare cerință și trebuie să lase\n> deoparte textul care ar adăuga doar zgomot sau ar produce o potrivire pe care nimeni nu ar\n> numi-o o duplicare reală. Astfel sacul de cuvinte este construit doar din titlu, intent și\n> Contract (niciodată din secțiunea densă Notes), curățat de stopword-uri, token-uri scurte și\n> numere pure — iar o cerință fără nimic redactat încă, sau o pereche deja legată ca o cerință\n> și propria ei suită de teste, este omisă în loc să fie comparată.\n\nFiecare punct de mai jos este obligatoriu.\n- `dupes` construiește un sac de cuvinte pentru fiecare cerință din titlul ei, linia ei de intent,\n  și punctele din Contract.\n- `dupes` lasă deoparte secțiunea \"Notes & limitations\" din acel text, pentru că acea\n  secțiune este densă și ar adăuga zgomot.\n- `dupes` tokenizează textul în cuvinte alfanumerice minuscule de lungime trei sau mai mult.\n- `dupes` elimină un mic set de stopword-uri și numere pure din acele token-uri.\n- `dupes` omite o cerință ale cărei puncte din Contract sunt încă toate placeholder-ul de draft\n  (`TODO: …`), și afișează câte a omis. Un contract neredactat nu are nimic de comparat.\n- `dupes` omite o pereche legată prin `tested-by` — fișierul `tested-by` al unei cerințe este\n  fișierul `implements` al celeilalte, adică a doua cerință CHIAR ESTE suita de teste a primei —\n  și afișează câte astfel de perechi a omis. O astfel de pereche partajează vocabular prin construcție\n  și este o legătură cunoscută, nu o duplicare.\n- `dupes` de asemenea omite o pereche unită printr-o muchie `satisfies:`: un copil reformulează o parte din contractul\n  părintelui prin construcție, astfel o pereche părinte-copil este o legătură cunoscută, nu o duplicare.",
          "acceptance": "CASE-1 — sacul de cuvinte provine doar din titlu, intent și Contract\n  Given  două cerințe care partajează formulare doar în titlurile și punctele din Contract\n  When   `dupes` construiește sacul de cuvinte al fiecărei cerințe\n  Then   acea formulare partajată este inclusă și conduce comparația\n\nCASE-2 — textul unic din secțiunea Notes este lăsat deoparte din sac\n  Given  două cerințe fără legătură ale căror secțiuni Notes întâmplă să partajeze mai multe cuvinte\n  When   `dupes` rulează\n  Then   acea formulare partajată din Notes nu ridică scorul perechii lor\n\nCASE-3 — token-urile mai scurte de trei caractere sunt eliminate\n  Given  două cerințe a căror singură formulare partajată sunt token-uri de două litere\n  When   `dupes` le tokenizează textul\n  Then   acele token-uri sunt absente din oricare sac de cuvinte\n\nCASE-4 — stopword-urile și numerele pure nu contează niciodată ca termeni partajați\n  Given  două cerințe fără legătură partajând doar stopword-uri și un număr precum \"2026\"\n  When   `dupes` rulează\n  Then   niciunul nu este listat printre termenii partajați ai perechii\n\nCASE-5 — un draft neredactat este omis și numărat\n  Given  o cerință ale cărei puncte din Contract sunt încă toate `TODO: …`\n  When   `dupes` rulează\n  Then   acea cerință este exclusă din comparație, iar numărătoarea de omise o include\n\nCASE-6 — o pereche legată prin tested-by este omisă și numărată\n  Given  cerința A al cărei fișier `tested-by` este fișierul `implements` al cerinței B, cu formulare suprapusă\n  When   `dupes` rulează cu harta de membri\n  Then   perechea este exclusă din raport iar numărătoarea de perechi legate o include\n\nCASE-7 — un părinte și copilul lui nu sunt niciodată o constatare de duplicare\n  Given  două cerințe cu același contract, una declarând `satisfies:` cealaltă\n  When   `dupes` rulează\n  Then   perechea este omisă și numărată în linia de perechi legate\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-SIMILAR-922",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Weighting terms and scoring a pair",
      "intent": "A raw shared-word count would treat \"the\" the same as a rare domain term, so each term is weighted by how distinctive it is across the corpus (TF-IDF) before two requirements' bags are compared by cosine similarity. The smoothing constant matters in a small corpus: without it, a two-requirement corpus's inverse-document-frequency term can collapse to zero and silently hide a real match.",
      "contract": [
        "`dupes` weights terms with a smoothed TF-IDF (`log((1 + N) / (1 + df)) + 1`).",
        "The smoothing keeps every weight positive, so a two-requirement corpus does not collapse to a zero score.",
        "`dupes` scores each pair with cosine similarity in the range zero to one."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a rarer term is weighted higher than a common one Given  a corpus where one term appears in nearly every requirement and another appears in only two When   `dupes` computes TF-IDF weights Then   the rare term's weight is higher than the common term's",
        "CASE-2 — — a two-requirement corpus still scores a shared term Given  exactly two requirements sharing one term present in both When   `dupes` scores that pair Then   the score is above zero, not collapsed by an unsmoothed weight of zero",
        "CASE-3 — — cosine score stays bounded between zero and one Given  one pair of near-identical requirements and one pair with no shared wording When   `dupes` scores both pairs Then   the identical pair scores near one and the unrelated pair scores near zero, neither outside that range --------------------"
      ],
      "accept": "CASE-1 — a rarer term is weighted higher than a common one\n  Given  a corpus where one term appears in nearly every requirement and another appears in only two\n  When   `dupes` computes TF-IDF weights\n  Then   the rare term's weight is higher than the common term's\n\nCASE-2 — a two-requirement corpus still scores a shared term\n  Given  exactly two requirements sharing one term present in both\n  When   `dupes` scores that pair\n  Then   the score is above zero, not collapsed by an unsmoothed weight of zero\n\nCASE-3 — cosine score stays bounded between zero and one\n  Given  one pair of near-identical requirements and one pair with no shared wording\n  When   `dupes` scores both pairs\n  Then   the identical pair scores near one and the unrelated pair scores near zero, neither outside that range\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SIMILAR-016"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5228"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5246"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3287"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9318"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Ponderarea termenilor și scorarea unei perechi",
          "intent": "O numărătoare brută de cuvinte partajate ar trata \"the\" la fel ca un termen rar de domeniu, astfel fiecare termen este ponderat după cât de distinctiv este în corpus (TF-IDF) înainte ca sacii a două cerințe să fie comparați prin similaritate cosinus. Constanta de netezire contează într-un corpus mic: fără ea, termenul de frecvență inversă de document al unui corpus de două cerințe se poate prăbuși la zero și ascunde în tăcere o potrivire reală.",
          "contract": "> O numărătoare brută de cuvinte partajate ar trata \"the\" la fel ca un termen rar de domeniu,\n> astfel fiecare termen este ponderat după cât de distinctiv este în corpus (TF-IDF) înainte\n> ca sacii a două cerințe să fie comparați prin similaritate cosinus. Constanta de netezire\n> contează într-un corpus mic: fără ea, termenul de frecvență inversă de document al unui\n> corpus de două cerințe se poate prăbuși la zero și ascunde în tăcere o potrivire reală.\n\nFiecare punct de mai jos este obligatoriu.\n- `dupes` ponderează termenii cu un TF-IDF netezit (`log((1 + N) / (1 + df)) + 1`).\n- Netezirea păstrează fiecare pondere pozitivă, astfel un corpus de două cerințe nu se\n  prăbușește la un scor zero.\n- `dupes` scorează fiecare pereche cu similaritate cosinus în intervalul zero până la unu.",
          "acceptance": "CASE-1 — un termen mai rar este ponderat mai sus decât unul comun\n  Given  un corpus unde un termen apare în aproape fiecare cerință și altul apare doar în două\n  When   `dupes` calculează ponderile TF-IDF\n  Then   ponderea termenului rar este mai mare decât cea a termenului comun\n\nCASE-2 — un corpus de două cerințe încă scorează un termen partajat\n  Given  exact două cerințe partajând un termen prezent în ambele\n  When   `dupes` scorează acea pereche\n  Then   scorul este peste zero, nu prăbușit de o pondere nenetezită de zero\n\nCASE-3 — scorul cosinus rămâne mărginit între zero și unu\n  Given  o pereche de cerințe aproape identice și o pereche fără nicio formulare partajată\n  When   `dupes` scorează ambele perechi\n  Then   perechea identică scorează aproape de unu, iar perechea fără legătură scorează aproape de zero, niciuna în afara acelui interval\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-SIMILAR-923",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The relevance threshold and report format",
      "intent": "Not every scored pair is worth a reviewer's attention, so `dupes` only reports pairs at or above a threshold (tunable, since no single cutoff fits every corpus), sorted so the strongest candidate leads. The command is advisory only — it always exits zero, because a human, not the tool, decides whether a flagged pair is a real duplicate.",
      "contract": [
        "`dupes` reports only the pairs at or above the threshold.",
        "The threshold defaults to `0.35`.",
        "`--threshold` overrides that default.",
        "`dupes` prints pairs most-similar-first, each with its score and up to five shared terms, so the reviewer can see why the pair was flagged.",
        "`dupes` always returns zero. The report is advisory: a human decides whether a flagged pair is a real duplicate.",
        "`dupes --top N` prints the N highest-scoring pairs and a `... M more pair(s)` line; without `--top` every pair is printed."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the default threshold is 0.35 Given  a pair scoring above 0.35 and a pair scoring below it, with no `--threshold` flag When   `dupes` runs Then   only the pair above 0.35 is reported",
        "CASE-2 — — --threshold replaces the default cutoff Given  a pair scoring 0.20, below the default threshold When   `dupes --threshold 0.1` runs Then   that pair is now reported",
        "CASE-3 — — reported pairs are sorted and show their shared terms Given  three flagged pairs with different scores, one sharing more than five terms When   `dupes` runs Then   they print highest-score-first, each with its score and at most five shared terms",
        "CASE-4 — — dupes exits zero regardless of what it finds Given  a corpus containing several flagged pairs When   `dupes` runs Then   the process still exits 0",
        "CASE-5 — — a below-threshold pair is left out of the report Given  two requirements whose score falls under the active threshold When   `dupes` runs Then   that pair is absent from the printed report",
        "CASE-6 — — --top truncates with a count Given  three mutually similar requirements and `--top 1` When   `dupes` runs Then   one pair is printed followed by `... 2 more pair(s)`"
      ],
      "accept": "CASE-1 — the default threshold is 0.35\n  Given  a pair scoring above 0.35 and a pair scoring below it, with no `--threshold` flag\n  When   `dupes` runs\n  Then   only the pair above 0.35 is reported\n\nCASE-2 — --threshold replaces the default cutoff\n  Given  a pair scoring 0.20, below the default threshold\n  When   `dupes --threshold 0.1` runs\n  Then   that pair is now reported\n\nCASE-3 — reported pairs are sorted and show their shared terms\n  Given  three flagged pairs with different scores, one sharing more than five terms\n  When   `dupes` runs\n  Then   they print highest-score-first, each with its score and at most five shared terms\n\nCASE-4 — dupes exits zero regardless of what it finds\n  Given  a corpus containing several flagged pairs\n  When   `dupes` runs\n  Then   the process still exits 0\n\nCASE-5 — a below-threshold pair is left out of the report\n  Given  two requirements whose score falls under the active threshold\n  When   `dupes` runs\n  Then   that pair is absent from the printed report\n\nCASE-6 — --top truncates with a count\n  Given  three mutually similar requirements and `--top 1`\n  When   `dupes` runs\n  Then   one pair is printed followed by `... 2 more pair(s)`",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SIMILAR-016"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5289"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3287"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9318"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Pragul de relevanță și formatul raportului",
          "intent": "Nu fiecare pereche scorată merită atenția unui revizor, astfel `dupes` raportează doar perechile la sau peste un prag (reglabil, întrucât niciun singur prag nu se potrivește fiecărui corpus), sortate astfel încât cel mai puternic candidat conduce. Comanda este doar consultativă — iese întotdeauna cu zero, pentru că un om, nu unealta, decide dacă o pereche semnalată este o duplicare reală.",
          "contract": "> Nu fiecare pereche scorată merită atenția unui revizor, astfel `dupes` raportează doar\n> perechile la sau peste un prag (reglabil, întrucât niciun singur prag nu se potrivește\n> fiecărui corpus), sortate astfel încât cel mai puternic candidat conduce. Comanda este doar\n> consultativă — iese întotdeauna cu zero, pentru că un om, nu unealta, decide dacă o pereche\n> semnalată este o duplicare reală.\n\nFiecare punct de mai jos este obligatoriu.\n- `dupes` raportează doar perechile la sau peste prag.\n- Pragul este implicit `0.35`.\n- `--threshold` suprascrie acea valoare implicită.\n- `dupes` afișează perechile cele-mai-similare-primele, fiecare cu scorul ei și până la cinci\n  termeni partajați, astfel revizorul poate vedea de ce a fost semnalată perechea.\n- `dupes` returnează întotdeauna zero. Raportul este consultativ: un om decide dacă o\n  pereche semnalată este o duplicare reală.\n- `dupes --top N` afișează cele N perechi cu scorul cel mai mare și o linie `... M more pair(s)`; fără\n  `--top`, fiecare pereche este afișată.",
          "acceptance": "CASE-1 — pragul implicit este 0.35\n  Given  o pereche care scorează peste 0.35 și o pereche care scorează sub el, fără niciun flag `--threshold`\n  When   `dupes` rulează\n  Then   doar perechea de peste 0.35 este raportată\n\nCASE-2 — --threshold înlocuiește pragul implicit\n  Given  o pereche care scorează 0.20, sub pragul implicit\n  When   `dupes --threshold 0.1` rulează\n  Then   acea pereche este acum raportată\n\nCASE-3 — perechile raportate sunt sortate și își arată termenii partajați\n  Given  trei perechi semnalate cu scoruri diferite, una partajând mai mult de cinci termeni\n  When   `dupes` rulează\n  Then   se afișează cel-mai-mare-scor-primul, fiecare cu scorul ei și cel mult cinci termeni partajați\n\nCASE-4 — dupes iese cu zero indiferent de ce găsește\n  Given  un corpus conținând mai multe perechi semnalate\n  When   `dupes` rulează\n  Then   procesul iese totuși cu 0\n\nCASE-5 — o pereche sub prag este lăsată în afara raportului\n  Given  două cerințe al căror scor cade sub pragul activ\n  When   `dupes` rulează\n  Then   acea pereche este absentă din raportul afișat\n\nCASE-6 — --top trunchiază cu o numărătoare\n  Given  trei cerințe similare reciproc și `--top 1`\n  When   `dupes` rulează\n  Then   se afișează o pereche urmată de `... 2 more pair(s)`"
        }
      }
    },
    {
      "id": "ARCH-SITE-026",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Generate & maintain a project presentation page",
      "intent": "A project is easier to grasp from one presentation page than from a folder of files. This capability lets the engine keep that page's links and key numbers current — injecting engine-owned regions into a page the author still controls, and scaffolding one when none exists — so the page never drifts from the registry.",
      "contract": [
        "`site --attach <page.html>` injects the requested marker-delimited regions (`nav`, `stats`; default `nav`) into the page idempotently, replacing only the bytes between each region's paired markers and preserving all other authored content. [[REQ-SITE-924]]"
      ],
      "verify": [],
      "notes": [
        "v1 engine-owns `nav` + `stats`; `commands`/`layers` render as authored content in the scaffold and may be promoted to engine-owned regions later.",
        "The interactive \"scan docs/ and ask which target + regions\" flow lives in the requirement-manager skill, not the engine (the engine is headless-safe)."
      ],
      "current_impl": [
        "`cmd_site`, `_render_region`, `_inject_region`/`_extract_region`, `_git_remote_web_url`, `_site_default_target`, `_site_pages_bootstrap`, the `cmd_init` hook, and the `_map_check` site branch in `reqmap.py`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a page with `nav`/`stats` markers When   `site --attach` runs twice Then   the second run leaves the file byte-identical and authored prose intact",
        "CASE-2 — Given  a repo with no git remote When   `site --attach` injects `nav` Then   it exits 0 and emits no GitHub link",
        "CASE-3 — Given  an absent `--attach` target When   `sync` runs Then   it scaffolds a full page with the regions + the placeholder-hero marker",
        "CASE-4 — Given  a generated site page When   its `stats` region is edited to differ from a fresh render Then   `map --check` exits non-zero and names the page (it exits 0 before the edit)",
        "CASE-5 — Given  `docs/` present and no `--no-site` When   `init` runs Then   `docs/architecture.html` exists with engine regions; `--no-site` skips it"
      ],
      "accept": "CASE-1\n  Given  a page with `nav`/`stats` markers\n  When   `site --attach` runs twice\n  Then   the second run leaves the file byte-identical and authored prose intact\n\nCASE-2\n  Given  a repo with no git remote\n  When   `site --attach` injects `nav`\n  Then   it exits 0 and emits no GitHub link\n\nCASE-3\n  Given  an absent `--attach` target\n  When   `sync` runs\n  Then   it scaffolds a full page with the regions + the placeholder-hero marker\n\nCASE-4\n  Given  a generated site page\n  When   its `stats` region is edited to differ from a fresh render\n  Then   `map --check` exits non-zero and names the page (it exits 0 before the edit)\n\nCASE-5\n  Given  `docs/` present and no `--no-site`\n  When   `init` runs\n  Then   `docs/architecture.html` exists with engine regions; `--no-site` skips it",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-MAP-007",
        "ARCH-VIEWER-007",
        "ARCH-PAGES-021"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-VISUAL-106"
      ],
      "satisfied_by": [
        "REQ-SITE-924"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6112"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6154"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6204"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6876"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6896"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6916"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6919"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6924"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6945"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6957"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6962"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6981"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7272"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7417"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7435"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7443"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7458"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:5462"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.35",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Generarea și menținerea unei pagini de prezentare a proiectului",
          "intent": "Un proiect este mai ușor de înțeles dintr-o singură pagină de prezentare decât dintr-un folder de fișiere. Această capabilitate permite motorului să mențină actualizate linkurile și cifrele cheie ale acelei pagini — injectând regiuni deținute de motor într-o pagină pe care autorul o controlează în continuare, și generând una de la zero când nu există niciuna — astfel încât pagina nu se abate niciodată de la registru.",
          "contract": "> Un proiect este mai ușor de înțeles dintr-o singură pagină de prezentare decât dintr-un\n> folder de fișiere. Această capabilitate permite motorului să mențină actualizate linkurile\n> și cifrele cheie ale acelei pagini — injectând regiuni deținute de motor într-o pagină pe\n> care autorul o controlează în continuare, și generând una de la zero când nu există niciuna — astfel încât pagina nu se abate niciodată de la registru.\n\nFiecare punct de mai jos este obligatoriu.\n- `site --attach <page.html>` injectează regiunile delimitate de markeri cerute (`nav`, `stats`; implicit `nav`) în pagină în mod idempotent, înlocuind doar octeții dintre markerii pereche ai fiecărei regiuni și păstrând tot restul conținutului autorat. [[REQ-SITE-924]]",
          "acceptance": "CASE-1\n  Given  o pagină cu markeri `nav`/`stats`\n  When   `site --attach` rulează de două ori\n  Then   a doua rulare lasă fișierul identic byte cu byte și proza autorată intactă\n\nCASE-2\n  Given  un repo fără remote git\n  When   `site --attach` injectează `nav`\n  Then   iese cu 0 și nu emite niciun link GitHub\n\nCASE-3\n  Given  o țintă `--attach` absentă\n  When   rulează `sync`\n  Then   generează o pagină completă cu regiunile + markerul placeholder-hero\n\nCASE-4\n  Given  o pagină de site generată\n  When   regiunea sa `stats` este editată astfel încât diferă de o randare nouă\n  Then   `map --check` iese cu cod diferit de zero și numește pagina (iese cu 0 înainte de editare)\n\nCASE-5\n  Given  `docs/` prezent și fără `--no-site`\n  When   rulează `init`\n  Then   `docs/architecture.html` există cu regiunile motorului; `--no-site` o omite"
        }
      }
    },
    {
      "id": "REQ-SITE-924",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Inject engine-owned regions into a presentation page",
      "intent": "`site --attach docs/architecture.html` writes the `nav` and `stats` regions into a page the author still edits by hand, touching only the bytes between each region's markers. Without it, keeping a hand-authored page's links and counts in sync with the registry would mean either regenerating the whole page (losing authored prose) or manually copying numbers every time the corpus changes.",
      "contract": [
        "`site --attach <page.html>` injects the requested marker-delimited regions (`nav`, `stats`; default `nav`) into the page, replacing only the bytes between each region's paired markers and preserving all other (authored) content. A re-run with no underlying change produces a byte-identical file (idempotent).",
        "When the `--attach` target does not exist, `sync` scaffolds a self-contained default page (the inline `SITE_TEMPLATE`) with the regions filled and an authored placeholder hero.",
        "The `nav` region emits a link only when its target resolves: Live Map when a sibling `map.html` exists, Diagram when `--diagram <rel>` names an existing file, GitHub when a git remote resolves. A missing git remote, missing artifact, or non-checkout never raises.",
        "The engine never imports or executes the excalidraw skill's builder; the Diagram entry is a link only.",
        "`init`, unless `--no-site` is given, runs a best-effort `sync` step after `map`: refreshes `nav`+`stats` in `docs/architecture.html` if it exists, else scaffolds it plus a Pages signal (`.nojekyll` + an `index.html` redirect). A failure in this step does not abort `init`.",
        "`map --check` flags the site page stale when its on-disk `stats` region differs from a fresh render. The `nav` region is excluded (it embeds the fork-specific repo URL). A page that was never generated, or that lacks a `stats` region, is not stale."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a second attach run with no change is byte-identical Given  a page carrying `<h1>Mine</h1>` and no underlying data change When   `cmd_site(..., attach=page, regions=[\"nav\", \"stats\"])` runs twice Then   the file's content after the second run equals the content after the first, and `<h1>Mine</h1>` is still present",
        "CASE-2 — — site scaffolds a full page when the attach target is absent Given  no file at `docs/architecture.html` When   `cmd_site(..., attach=target, regions=[\"nav\", \"stats\"])` runs Then   the written page contains both region markers and the \"\" placeholder hero",
        "CASE-3 — — an absent nav target is omitted, not an error Given  a rendering context with no repo URL, no map, and no diagram When   `_render_region(\"nav\", ctx)` runs Then   it returns markup with no `<a` link and raises nothing",
        "CASE-4 — — reqmap.py's own source never references the excalidraw builder Given  the `reqmap.py` source file When   its text is searched for \"excalidraw_builder\" Then   no occurrence is found",
        "CASE-5 — — init scaffolds the site page unless --no-site is passed Given  a `docs/` directory with no `architecture.html` When   `cmd_init(..., no_site=False)` runs, and separately `cmd_init(..., no_site=True)` Then   the first run creates `docs/architecture.html` with a `##REQMAP:NAV##` region; the second run creates no such file",
        "CASE-6 — — map --check fails only after the stats region is tampered with Given  a freshly generated site page with a `stats` region When   `_map_check` runs before and after the `stats` region is overwritten with \"TAMPERED\" Then   it exits 0 before the edit and exits 1 after it"
      ],
      "accept": "CASE-1 — a second attach run with no change is byte-identical\n  Given  a page carrying `<h1>Mine</h1>` and no underlying data change\n  When   `cmd_site(..., attach=page, regions=[\"nav\", \"stats\"])` runs twice\n  Then   the file's content after the second run equals the content after the first, and\n         `<h1>Mine</h1>` is still present\n\nCASE-2 — site scaffolds a full page when the attach target is absent\n  Given  no file at `docs/architecture.html`\n  When   `cmd_site(..., attach=target, regions=[\"nav\", \"stats\"])` runs\n  Then   the written page contains both region markers and the \"<!-- author me -->\"\n         placeholder hero\n\nCASE-3 — an absent nav target is omitted, not an error\n  Given  a rendering context with no repo URL, no map, and no diagram\n  When   `_render_region(\"nav\", ctx)` runs\n  Then   it returns markup with no `<a` link and raises nothing\n\nCASE-4 — reqmap.py's own source never references the excalidraw builder\n  Given  the `reqmap.py` source file\n  When   its text is searched for \"excalidraw_builder\"\n  Then   no occurrence is found\n\nCASE-5 — init scaffolds the site page unless --no-site is passed\n  Given  a `docs/` directory with no `architecture.html`\n  When   `cmd_init(..., no_site=False)` runs, and separately `cmd_init(..., no_site=True)`\n  Then   the first run creates `docs/architecture.html` with a `##REQMAP:NAV##` region;\n         the second run creates no such file\n\nCASE-6 — map --check fails only after the stats region is tampered with\n  Given  a freshly generated site page with a `stats` region\n  When   `_map_check` runs before and after the `stats` region is overwritten with\n         \"TAMPERED\"\n  Then   it exits 0 before the edit and exits 1 after it",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SITE-026"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6924"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:6981"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7417"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7457"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:5462"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Injectarea regiunilor deținute de motor într-o pagină de prezentare",
          "intent": "`site --attach docs/architecture.html` scrie regiunile `nav` și `stats` într-o pagină pe care autorul încă o editează manual, atingând doar octeții dintre markerii fiecărei regiuni. Fără el, menținerea link-urilor și numărătorilor unei pagini redactate manual sincronizate cu registrul ar însemna fie regenerarea întregii pagini (pierzând proza redactată), fie copierea manuală a numerelor de fiecare dată când corpusul se schimbă.",
          "contract": "> `site --attach docs/architecture.html` scrie regiunile `nav` și `stats` într-o pagină\n> pe care autorul încă o editează manual, atingând doar octeții dintre markerii fiecărei\n> regiuni. Fără el, menținerea link-urilor și numărătorilor unei pagini redactate manual\n> sincronizate cu registrul ar însemna fie regenerarea întregii pagini (pierzând proza\n> redactată), fie copierea manuală a numerelor de fiecare dată când corpusul se schimbă.\n\nFiecare punct de mai jos este obligatoriu.\n- `site --attach <page.html>` injectează regiunile delimitate-de-marker cerute\n  (`nav`, `stats`; implicit `nav`) în pagină, înlocuind doar octeții dintre markerii perechi ai fiecărei\n  regiuni și păstrând tot restul conținutului (redactat). O rerulare fără nicio schimbare\n  subiacentă produce un fișier identic byte cu byte (idempotent).\n- Când ținta `--attach` nu există, `sync` schelează o pagină implicită de sine stătătoare\n  (`SITE_TEMPLATE` inline) cu regiunile completate și un hero placeholder redactat.\n- Regiunea `nav` emite un link doar când ținta lui se rezolvă: Live Map când un `map.html`\n  frate există, Diagram când `--diagram <rel>` numește un fișier existent, GitHub când un remote git se rezolvă. Un remote git lipsă, un artefact lipsă, sau un non-checkout nu ridică niciodată o excepție.\n- Motorul nu importă și nu execută niciodată builder-ul skill-ului excalidraw; intrarea Diagram\n  este doar un link.\n- `init`, dacă `--no-site` nu este dat, rulează un pas `sync` best-effort după `map`:\n  reîmprospătează `nav`+`stats` în `docs/architecture.html` dacă există, altfel îl schelează plus un\n  semnal Pages (`.nojekyll` + un redirect `index.html`). Un eșec în acest pas nu\n  întrerupe `init`.\n- `map --check` semnalează pagina site ca învechită când regiunea ei `stats` de pe disc diferă de o\n  randare proaspătă. Regiunea `nav` este exclusă (înglobează URL-ul de repo specific fork-ului). O pagină\n  care nu a fost niciodată generată, sau care nu are o regiune `stats`, nu este învechită.",
          "acceptance": "CASE-1 — o a doua rulare de attach fără nicio schimbare este identică byte cu byte\n  Given  o pagină purtând `<h1>Mine</h1>` și nicio schimbare de date subiacentă\n  When   `cmd_site(..., attach=page, regions=[\"nav\", \"stats\"])` rulează de două ori\n  Then   conținutul fișierului după a doua rulare este egal cu conținutul de după prima, iar\n         `<h1>Mine</h1>` este încă prezent\n\nCASE-2 — site schelează o pagină completă când ținta attach este absentă\n  Given  niciun fișier la `docs/architecture.html`\n  When   `cmd_site(..., attach=target, regions=[\"nav\", \"stats\"])` rulează\n  Then   pagina scrisă conține atât markerii ambelor regiuni, cât și placeholder-ul\n         \"<!-- author me -->\"\n\nCASE-3 — o țintă nav absentă este omisă, nu o eroare\n  Given  un context de randare fără niciun URL de repo, nicio hartă, și nicio diagramă\n  When   `_render_region(\"nav\", ctx)` rulează\n  Then   returnează markup fără niciun link `<a` și nu ridică nimic\n\nCASE-4 — sursa proprie a lui reqmap.py nu referențiază niciodată builder-ul excalidraw\n  Given  fișierul sursă `reqmap.py`\n  When   textul lui este căutat pentru \"excalidraw_builder\"\n  Then   nicio apariție nu este găsită\n\nCASE-5 — init schelează pagina site cu excepția cazului în care --no-site este dat\n  Given  un director `docs/` fără niciun `architecture.html`\n  When   `cmd_init(..., no_site=False)` rulează, și separat `cmd_init(..., no_site=True)`\n  Then   prima rulare creează `docs/architecture.html` cu o regiune `##REQMAP:NAV##`;\n         a doua rulare nu creează un astfel de fișier\n\nCASE-6 — map --check eșuează doar după ce regiunea stats a fost falsificată\n  Given  o pagină site generată proaspăt cu o regiune `stats`\n  When   `_map_check` rulează înainte și după ce regiunea `stats` este suprascrisă cu\n         \"TAMPERED\"\n  Then   iese cu 0 înainte de editare și cu 1 după ea"
        }
      }
    },
    {
      "id": "ARCH-STALEENGINE-043",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Stale vendored engine, reported in CI",
      "intent": "Every consumer repo runs its own vendored copy of `reqmap.py`, and that copy only improves when someone re-seeds it. The engine's own staleness notice (`warn_if_stale`) is gated on `CLAUDE_PLUGIN_ROOT`, which exists in a Claude Code session and nowhere else — so it is silent in CI, the one place that runs on every push and the one place a months-old engine actually costs something: checks that shipped since are simply absent, and nothing says so.",
      "contract": [
        "The gate action's staleness probe compares a consumer's vendored `MAP_ENGINE_VERSION` against the reference engine's and reports the result per `--mode`. [[REQ-STALEENGINE-925]]",
        "The probe fails open. Nothing it can hit — `off` mode, an already-current engine, an unreadable version, an internal crash — produces a false staleness warning or a non-zero exit. [[REQ-STALEENGINE-926]]"
      ],
      "verify": [],
      "notes": [
        "Why this cannot live in the engine, next to [[ARCH-CHECK-006]]'s `warn_if_stale`: a stale vendored engine does not contain the check that would report it stale. The detector has to run from something the consumer does not vendor — the action, which is always current for whatever `@vN` they reference.",
        "The reference version is whatever the pinned action ref ships. A consumer pinned to an exact old SHA is compared against that SHA's engine, which is the honest answer: it is the engine they asked for.",
        "Default `warn`, not `error`: adding a step that can newly fail a green build is a breaking change for an existing `@v2` pin. A repo that wants the hard stop opts in."
      ],
      "current_impl": [
        "`check/engine_staleness.py`, invoked by the `reqmap engine staleness` step in `check/action.yml`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a vendored engine older than the reference engine When   the probe runs in `warn` mode Then   it prints one message naming both versions and exits 0",
        "CASE-2 — Given  the same pair of engines When   the probe runs in `error` mode Then   it exits 1",
        "CASE-3 — Given  any pair of engines When   the probe runs in `off` mode Then   it prints nothing and exits 0",
        "CASE-4 — Given  a vendored engine at or ahead of the reference version When   the probe runs in `warn` or `error` mode Then   no staleness message appears and it exits 0",
        "CASE-5 — Given  a vendored or reference file that is missing or carries no readable `MAP_ENGINE_VERSION`, or a probe that raises unexpectedly When   the probe runs in `error` mode Then   it reports the probe as skipped and exits 0",
        "CASE-6 — Given  `GITHUB_ACTIONS` is set in the environment When   the probe reports a stale engine Then   the message is a `::warning::` annotation; without that variable it is a plain `WARN` line"
      ],
      "accept": "CASE-1\n  Given  a vendored engine older than the reference engine\n  When   the probe runs in `warn` mode\n  Then   it prints one message naming both versions and exits 0\n\nCASE-2\n  Given  the same pair of engines\n  When   the probe runs in `error` mode\n  Then   it exits 1\n\nCASE-3\n  Given  any pair of engines\n  When   the probe runs in `off` mode\n  Then   it prints nothing and exits 0\n\nCASE-4\n  Given  a vendored engine at or ahead of the reference version\n  When   the probe runs in `warn` or `error` mode\n  Then   no staleness message appears and it exits 0\n\nCASE-5\n  Given  a vendored or reference file that is missing or carries no readable\n         `MAP_ENGINE_VERSION`, or a probe that raises unexpectedly\n  When   the probe runs in `error` mode\n  Then   it reports the probe as skipped and exits 0\n\nCASE-6\n  Given  `GITHUB_ACTIONS` is set in the environment\n  When   the probe reports a stale engine\n  Then   the message is a `::warning::` annotation; without that variable it is a plain\n         `WARN` line",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-CHECK-006",
        "ARCH-SELFGATE-039"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-SHIP-108"
      ],
      "satisfied_by": [
        "REQ-STALEENGINE-925",
        "REQ-STALEENGINE-926"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "check/action.yml:3"
        },
        {
          "role": "implements",
          "loc": "check/engine_staleness.py:2"
        },
        {
          "role": "tested-by",
          "loc": "scripts/test_engine_staleness.py:49"
        },
        {
          "role": "tested-by",
          "loc": "scripts/test_engine_staleness.py:154"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Motor vendorizat învechit, raportat în CI",
          "intent": "Fiecare repo consumator rulează propria copie vendorizată a `reqmap.py`, iar acea copie se îmbunătățește doar când cineva o re-seedează. Notificarea proprie de învechire a motorului (`warn_if_stale`) este condiționată de `CLAUDE_PLUGIN_ROOT`, care există într-o sesiune Claude Code și nicăieri altundeva — deci este tăcută în CI, singurul loc care rulează la fiecare push și singurul loc unde un motor vechi de luni de zile chiar costă ceva: verificările apărute de atunci sunt pur și simplu absente, și nimic nu semnalează asta.",
          "contract": "> Fiecare repo consumator rulează propria copie vendorizată a `reqmap.py`, iar acea copie\n> se îmbunătățește doar când cineva o re-seedează. Notificarea proprie de învechire a\n> motorului (`warn_if_stale`) este condiționată de `CLAUDE_PLUGIN_ROOT`, care există într-o\n> sesiune Claude Code și nicăieri altundeva — deci este tăcută în CI, singurul loc care\n> rulează la fiecare push și singurul loc unde un motor vechi de luni de zile chiar costă ceva: verificările apărute de atunci sunt pur și simplu absente, și nimic nu semnalează asta.\n\nFiecare punct de mai jos este obligatoriu.\n- Sonda de învechire a acțiunii gate compară `MAP_ENGINE_VERSION` vendorizat al consumatorului cu cel al motorului de referință și raportează rezultatul conform `--mode`. [[REQ-STALEENGINE-925]]\n- Sonda eșuează deschis (fail open). Nimic din ce poate întâlni — modul `off`, un motor deja actual, o versiune ilizibilă, un crash intern — nu produce un avertisment fals de învechire sau o ieșire diferită de zero. [[REQ-STALEENGINE-926]]",
          "acceptance": "CASE-1\n  Given  un motor vendorizat mai vechi decât motorul de referință\n  When   sonda rulează în modul `warn`\n  Then   afișează un mesaj numind ambele versiuni și iese cu 0\n\nCASE-2\n  Given  aceeași pereche de motoare\n  When   sonda rulează în modul `error`\n  Then   iese cu 1\n\nCASE-3\n  Given  orice pereche de motoare\n  When   sonda rulează în modul `off`\n  Then   nu afișează nimic și iese cu 0\n\nCASE-4\n  Given  un motor vendorizat egal sau mai nou decât versiunea de referință\n  When   sonda rulează în modul `warn` sau `error`\n  Then   nu apare niciun mesaj de învechire și iese cu 0\n\nCASE-5\n  Given  un fișier vendorizat sau de referință care lipsește sau nu conține un\n         `MAP_ENGINE_VERSION` lizibil, sau o sondă care ridică o eroare neașteptată\n  When   sonda rulează în modul `error`\n  Then   raportează sonda ca omisă (skipped) și iese cu 0\n\nCASE-6\n  Given  `GITHUB_ACTIONS` este setat în mediu\n  When   sonda raportează un motor învechit\n  Then   mesajul este o adnotare `::warning::`; fără acea variabilă este o linie simplă\n         `WARN`"
        }
      }
    },
    {
      "id": "REQ-STALEENGINE-925",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The gate action reports a stale vendored engine",
      "intent": "`check/action.yml` runs `check/engine_staleness.py` as a step of the published gate. It compares the consumer's vendored `reqmap.py` against the engine the action itself ships and, when the vendored copy is older, names both versions so a consumer learns their gate is missing checks that shipped since — without this, a stale copy stays silently behind.",
      "contract": [
        "The staleness probe compares the vendored engine's `MAP_ENGINE_VERSION` against the reference engine's.",
        "`check/action.yml` runs the probe as a step of the published gate action.",
        "The probe's `--mode` selects its behaviour: `warn`, `error`, or `off`.",
        "The action's `stale-engine` input sets that mode and defaults to `warn`.",
        "A vendored engine older than the reference produces one message naming both versions and the re-seed remedy.",
        "In `warn` the message is a warning and the exit code stays 0.",
        "In `error` the same condition exits 1.",
        "Under GitHub Actions the message is emitted as a `::warning::` workflow annotation, so it surfaces on the run rather than only in the log."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a vendored engine older than the reference one is named Given  a consumer's `scripts/reqmap.py` whose `MAP_ENGINE_VERSION` predates the action's own copy When   `engine_staleness.py` runs Then   it annotates the run, naming the vendored version and the reference version",
        "CASE-2 — — the gate action wires the staleness probe as its own step Given  `check/action.yml` When   its steps are inspected Then   one step invokes `engine_staleness.py` as part of the action's run",
        "CASE-3 — — the stale-engine input defaults to warn Given  `check/action.yml` with no `stale-engine` input set by the caller When   the action's inputs are read Then   `stale-engine` resolves to `warn`",
        "CASE-4 — — the stale message names the re-seed remedy Given  a vendored engine older than the reference engine When   the probe reports it Then   the message names the remedy of re-seeding the vendored copy",
        "CASE-5 — — warn mode reports and stays green Given  a stale vendored engine When   the probe runs with `--mode warn` Then   it prints a warning and the process exits 0",
        "CASE-6 — — error mode turns the same staleness into a failure Given  the same stale vendored engine When   the probe runs with `--mode error` Then   the process exits 1",
        "CASE-7 — — GitHub Actions gets an annotation, elsewhere a plain line Given  a stale vendored engine, once with `GITHUB_ACTIONS` set and once without When   the probe reports it Then   the first prints a `::warning::` annotation and the second a plain `WARN` line --------------------"
      ],
      "accept": "CASE-1 — a vendored engine older than the reference one is named\n  Given  a consumer's `scripts/reqmap.py` whose `MAP_ENGINE_VERSION` predates the action's own copy\n  When   `engine_staleness.py` runs\n  Then   it annotates the run, naming the vendored version and the reference version\n\nCASE-2 — the gate action wires the staleness probe as its own step\n  Given  `check/action.yml`\n  When   its steps are inspected\n  Then   one step invokes `engine_staleness.py` as part of the action's run\n\nCASE-3 — the stale-engine input defaults to warn\n  Given  `check/action.yml` with no `stale-engine` input set by the caller\n  When   the action's inputs are read\n  Then   `stale-engine` resolves to `warn`\n\nCASE-4 — the stale message names the re-seed remedy\n  Given  a vendored engine older than the reference engine\n  When   the probe reports it\n  Then   the message names the remedy of re-seeding the vendored copy\n\nCASE-5 — warn mode reports and stays green\n  Given  a stale vendored engine\n  When   the probe runs with `--mode warn`\n  Then   it prints a warning and the process exits 0\n\nCASE-6 — error mode turns the same staleness into a failure\n  Given  the same stale vendored engine\n  When   the probe runs with `--mode error`\n  Then   the process exits 1\n\nCASE-7 — GitHub Actions gets an annotation, elsewhere a plain line\n  Given  a stale vendored engine, once with `GITHUB_ACTIONS` set and once without\n  When   the probe reports it\n  Then   the first prints a `::warning::` annotation and the second a plain `WARN` line\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-STALEENGINE-043"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "check/action.yml:114"
        },
        {
          "role": "implements",
          "loc": "check/action.yml:153"
        },
        {
          "role": "implements",
          "loc": "check/engine_staleness.py:60"
        },
        {
          "role": "implements",
          "loc": "check/engine_staleness.py:81"
        },
        {
          "role": "tested-by",
          "loc": "scripts/test_engine_staleness.py:26"
        },
        {
          "role": "tested-by",
          "loc": "scripts/test_engine_staleness.py:154"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 7,
      "covered": 7,
      "i18n": {
        "ro": {
          "title": "Acțiunea de gate raportează un motor vendorizat învechit",
          "intent": "`check/action.yml` rulează `check/engine_staleness.py` ca un pas al gate-ului publicat. Compară `reqmap.py`-ul vendorizat al consumatorului cu motorul pe care action-ul însuși îl livrează, și, când copia vendorizată este mai veche, numește ambele versiuni astfel un consumator află că gate-ul lui îi lipsesc verificări livrate de atunci — fără asta, o copie învechită rămâne în tăcere în urmă.",
          "contract": "> `check/action.yml` rulează `check/engine_staleness.py` ca un pas al gate-ului publicat. Compară\n> `reqmap.py`-ul vendorizat al consumatorului cu motorul pe care action-ul însuși îl livrează\n> și, când copia vendorizată este mai veche, numește ambele versiuni astfel un consumator află\n> că gate-ul lui îi lipsesc verificări livrate de atunci — fără asta, o copie învechită rămâne\n> în tăcere în urmă.\n\nFiecare punct de mai jos este obligatoriu.\n- Sonda de învechire compară `MAP_ENGINE_VERSION`-ul motorului vendorizat cu cel al\n  motorului de referință.\n- `check/action.yml` rulează sonda ca un pas al acțiunii de gate publicate.\n- `--mode`-ul sondei selectează comportamentul ei: `warn`, `error`, sau `off`.\n- Intrarea `stale-engine` a acțiunii setează acel mod și este implicit `warn`.\n- Un motor vendorizat mai vechi decât referința produce un mesaj numind ambele versiuni și\n  remediul de re-seed.\n- În `warn` mesajul este un avertisment, iar codul de ieșire rămâne 0.\n- În `error` aceeași condiție iese cu 1.\n- Sub GitHub Actions mesajul este emis ca o adnotare de workflow `::warning::`, astfel\n  apare pe rulare, nu doar în jurnal.",
          "acceptance": "CASE-1 — un motor vendorizat mai vechi decât cel de referință este numit\n  Given  un `scripts/reqmap.py` al consumatorului al cărui `MAP_ENGINE_VERSION` precede copia proprie a action-ului\n  When   `engine_staleness.py` rulează\n  Then   adnotează rularea, numind versiunea vendorizată și versiunea de referință\n\nCASE-2 — acțiunea de gate conectează sonda de învechire ca propriul ei pas\n  Given  `check/action.yml`\n  When   pașii lui sunt inspectați\n  Then   un pas invocă `engine_staleness.py` ca parte din rularea acțiunii\n\nCASE-3 — intrarea stale-engine este implicit warn\n  Given  `check/action.yml` fără nicio intrare `stale-engine` setată de apelant\n  When   intrările acțiunii sunt citite\n  Then   `stale-engine` se rezolvă la `warn`\n\nCASE-4 — mesajul de învechire numește remediul de re-seed\n  Given  un motor vendorizat mai vechi decât motorul de referință\n  When   sonda îl raportează\n  Then   mesajul numește remediul de a re-seed-ui copia vendorizată\n\nCASE-5 — modul warn raportează și rămâne verde\n  Given  un motor vendorizat învechit\n  When   sonda rulează cu `--mode warn`\n  Then   afișează un avertisment, iar procesul iese cu 0\n\nCASE-6 — modul error transformă aceeași învechire într-un eșec\n  Given  același motor vendorizat învechit\n  When   sonda rulează cu `--mode error`\n  Then   procesul iese cu 1\n\nCASE-7 — GitHub Actions primește o adnotare, altundeva o linie simplă\n  Given  un motor vendorizat învechit, o dată cu `GITHUB_ACTIONS` setat și o dată fără\n  When   sonda îl raportează\n  Then   primul afișează o adnotare `::warning::`, iar al doilea o linie simplă `WARN`\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-STALEENGINE-926",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The staleness probe fails open, never the gate itself",
      "intent": "A version check that can crash or false-positive would make the gate less trustworthy, not more. So the probe treats `off` mode, an already-current engine, a missing or unreadable `MAP_ENGINE_VERSION`, and its own internal errors all the same way: no warning, exit 0. The probe is never itself the reason a run goes red.",
      "contract": [
        "`off` produces no output and exit 0.",
        "A vendored engine at or ahead of the reference version produces no warning.",
        "A version that cannot be read from either file — absent file, unparseable value — is reported as a skipped probe with exit 0, in `warn` and `error` mode, because an unreadable version is not evidence of staleness. `off` mode never reaches this check — it returns before the probe runs, so it reports nothing at all, skipped or otherwise.",
        "An unexpected internal failure of the probe is reported the same way: a skipped probe, exit 0. The probe is never itself the reason a gate run goes red."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — off mode is silent Given  a stale vendored engine When   the probe runs with `--mode off` Then   it prints nothing and exits 0",
        "CASE-2 — — an up-to-date or newer engine never warns Given  a vendored engine at the same version as the reference, or newer When   the probe runs in `warn` or `error` mode Then   no staleness message appears and it exits 0",
        "CASE-3 — — an unreadable version is skipped, not treated as stale Given  a vendored `reqmap.py` missing `MAP_ENGINE_VERSION` When   the probe runs in `error` mode Then   it reports the probe as skipped and exits 0",
        "CASE-4 — — an internal probe crash never fails the gate Given  a probe run that raises an unexpected exception while comparing versions When   the probe runs in `error` mode Then   it reports the probe as skipped and exits 0 rather than propagating the exception"
      ],
      "accept": "CASE-1 — off mode is silent\n  Given  a stale vendored engine\n  When   the probe runs with `--mode off`\n  Then   it prints nothing and exits 0\n\nCASE-2 — an up-to-date or newer engine never warns\n  Given  a vendored engine at the same version as the reference, or newer\n  When   the probe runs in `warn` or `error` mode\n  Then   no staleness message appears and it exits 0\n\nCASE-3 — an unreadable version is skipped, not treated as stale\n  Given  a vendored `reqmap.py` missing `MAP_ENGINE_VERSION`\n  When   the probe runs in `error` mode\n  Then   it reports the probe as skipped and exits 0\n\nCASE-4 — an internal probe crash never fails the gate\n  Given  a probe run that raises an unexpected exception while comparing versions\n  When   the probe runs in `error` mode\n  Then   it reports the probe as skipped and exits 0 rather than propagating the exception",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-STALEENGINE-043"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "check/engine_staleness.py:60"
        },
        {
          "role": "implements",
          "loc": "check/engine_staleness.py:81"
        },
        {
          "role": "tested-by",
          "loc": "scripts/test_engine_staleness.py:26"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Sonda de învechire eșuează deschis, niciodată gate-ul însuși",
          "intent": "O verificare de versiune care se poate prăbuși sau da fals-pozitiv ar face gate-ul mai puțin demn de încredere, nu mai mult. Astfel sonda tratează modul `off`, un motor deja actual, un `MAP_ENGINE_VERSION` lipsă sau necitibil, și propriile ei erori interne toate în același fel: fără avertisment, ieșire 0. Sonda nu este niciodată ea însăși motivul pentru care o rulare devine roșie.",
          "contract": "> O verificare de versiune care se poate prăbuși sau da fals-pozitiv ar face gate-ul mai puțin\n> demn de încredere, nu mai mult. Astfel sonda tratează modul `off`, un motor deja actual, un\n> `MAP_ENGINE_VERSION` lipsă sau necitibil, și propriile ei erori interne toate în același fel:\n> fără avertisment, ieșire 0. Sonda nu este niciodată ea însăși motivul pentru care o rulare\n> devine roșie.\n\nFiecare punct de mai jos este obligatoriu.\n- `off` nu produce nicio ieșire și iese cu 0.\n- Un motor vendorizat la sau înaintea versiunii de referință nu produce niciun avertisment.\n- O versiune care nu poate fi citită din niciun fișier — fișier absent, valoare neparsabilă — este\n  raportată ca o sondă omisă cu ieșire 0, în modul `warn` și `error`, pentru că o\n  versiune necitibilă nu este dovadă de învechire. Modul `off` nu ajunge niciodată la această verificare —\n  se întoarce înainte ca sonda să ruleze, astfel nu raportează nimic deloc, omis sau altfel.\n- Un eșec intern neașteptat al sondei este raportat în același fel: o sondă omisă,\n  ieșire 0. Sonda nu este niciodată ea însăși motivul pentru care o rulare de gate devine roșie.",
          "acceptance": "CASE-1 — modul off este tăcut\n  Given  un motor vendorizat învechit\n  When   sonda rulează cu `--mode off`\n  Then   nu afișează nimic și iese cu 0\n\nCASE-2 — un motor actualizat sau mai nou nu avertizează niciodată\n  Given  un motor vendorizat la aceeași versiune cu referința, sau mai nou\n  When   sonda rulează în modul `warn` sau `error`\n  Then   niciun mesaj de învechire nu apare, iar iese cu 0\n\nCASE-3 — o versiune necitibilă este omisă, nu tratată ca învechită\n  Given  un `reqmap.py` vendorizat căruia îi lipsește `MAP_ENGINE_VERSION`\n  When   sonda rulează în modul `error`\n  Then   raportează sonda ca omisă și iese cu 0\n\nCASE-4 — o prăbușire internă a sondei nu eșuează niciodată gate-ul\n  Given  o rulare a sondei care ridică o excepție neașteptată în timp ce compară versiunile\n  When   sonda rulează în modul `error`\n  Then   raportează sonda ca omisă și iese cu 0 în loc să propage excepția"
        }
      }
    },
    {
      "id": "ARCH-SUGGESTVERIFIES-047",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Suggest per-criterion `verifies:` tags",
      "intent": "Per-criterion coverage is easy to adopt on a new requirement and expensive to adopt on an old corpus: in one real repo, 205 criteria carried no `verifies:` tag while 110 of them already had a test *named* after the criterion (`test_ac3_auditor_refuzat`). The link existed; it just was not machine-readable. This command proposes those links, so each consumer does not have to write the same matching script — and re-discover, one wrong link at a time, the three ways a naive match goes wrong.",
      "contract": [
        "`suggest-verifies` proposes a `# verifies: <id>#CASE-N` tag for each untagged criterion whose requirement's tested-by file has a test named after it. [[REQ-SUGGESTVERIFIES-927]] details the behaviour.",
        "A match requires a whole token naming the criterion (and, in a shared file, a distinctive id token too); an ambiguous match proposes nothing. [[REQ-SUGGESTVERIFIES-928]] details the behaviour.",
        "`suggest-verifies` writes nothing by default, only printing proposals and ambiguities; `--apply` writes the tags and is safe to re-run. [[REQ-SUGGESTVERIFIES-929]] details the behaviour."
      ],
      "verify": [],
      "notes": [
        "The proposal is lexical: it asserts a test is NAMED after a criterion, never that its assertions exercise it. That is the same trust boundary as `tested-by` ([[ARCH-TESTLINK-018]]).",
        "A test named after nothing is invisible to this command. The tool closes the naming-to-tag gap only, which is exactly the gap measured (110 of 205)."
      ],
      "current_impl": [
        "`cmd_suggest_verifies`, `_verifies_proposals`, `_test_functions`, `_ac_name_re` and `_apply_verifies` in `reqmap.py`. `_verifies_proposals` is pure and returns `(proposals, ambiguous)`; `_apply_verifies` is the only part that writes.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a requirement with an untagged `CASE-1` and a `def test_ac1_x` in its tested-by file When   `suggest-verifies` runs Then   it prints one proposal naming the file, the line and the tag to add",
        "CASE-2 — Given  a tested-by file shared by two requirements and a test named `test_ac1_generic` When   `suggest-verifies` runs Then   no proposal is made for that criterion",
        "CASE-3 — Given  a test whose name carries another requirement's number, such as `test_ac1_083_x` When   `suggest-verifies` runs Then   it is not proposed for the requirement whose file it sits in",
        "CASE-4 — Given  two tests matching the same criterion When   `suggest-verifies --apply` runs Then   the criterion is reported as ambiguous and no tag is written",
        "CASE-5 — Given  a single matching test When   `suggest-verifies --apply` runs twice Then   the tag is written once and the second run finds nothing to propose"
      ],
      "accept": "CASE-1  <!-- verifiable by: automated test -->\n  Given  a requirement with an untagged `CASE-1` and a `def test_ac1_x` in its tested-by file\n  When   `suggest-verifies` runs\n  Then   it prints one proposal naming the file, the line and the tag to add\nCASE-2  <!-- verifiable by: automated test -->\n  Given  a tested-by file shared by two requirements and a test named `test_ac1_generic`\n  When   `suggest-verifies` runs\n  Then   no proposal is made for that criterion\nCASE-3  <!-- verifiable by: automated test -->\n  Given  a test whose name carries another requirement's number, such as `test_ac1_083_x`\n  When   `suggest-verifies` runs\n  Then   it is not proposed for the requirement whose file it sits in\nCASE-4  <!-- verifiable by: automated test -->\n  Given  two tests matching the same criterion\n  When   `suggest-verifies --apply` runs\n  Then   the criterion is reported as ambiguous and no tag is written\nCASE-5  <!-- verifiable by: automated test -->\n  Given  a single matching test\n  When   `suggest-verifies --apply` runs twice\n  Then   the tag is written once and the second run finds nothing to propose",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-ACVERIFY-019",
        "ARCH-SCAN-002"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-QUALITY-104"
      ],
      "satisfied_by": [
        "REQ-SUGGESTVERIFIES-927",
        "REQ-SUGGESTVERIFIES-928",
        "REQ-SUGGESTVERIFIES-929"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7563"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7584"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7595"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7656"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7694"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4187"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9188"
        }
      ],
      "test_exempt": null,
      "milestone": "v2.29",
      "priority": "should-have",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Sugerarea etichetelor `verifies:` per criteriu",
          "intent": "Acoperirea per criteriu este ușor de adoptat pe o cerință nouă și costisitoare de adoptat pe un corpus vechi: într-un repo real, 205 criterii nu purtau nicio etichetă `verifies:`, în timp ce 110 dintre ele aveau deja un test *denumit* după criteriu (`test_ac3_auditor_refuzat`). Legătura exista; pur și simplu nu era lizibilă de mașină. Această comandă propune acele legături, astfel încât fiecare consumator nu trebuie să scrie același script de potrivire — și să redescopere, o legătură greșită pe rând, cele trei moduri în care o potrivire naivă dă greș.",
          "contract": "> Acoperirea per criteriu este ușor de adoptat pe o cerință nouă și costisitoare de adoptat\n> pe un corpus vechi: într-un repo real, 205 criterii nu purtau nicio etichetă `verifies:`,\n> în timp ce 110 dintre ele aveau deja un test *denumit* după criteriu\n> (`test_ac3_auditor_refuzat`). Legătura exista; pur și simplu nu era lizibilă de mașină.\n> Această comandă propune acele legături, astfel încât fiecare consumator nu trebuie să\n> scrie același script de potrivire — și să redescopere, o legătură greșită pe rând, cele trei moduri în care o potrivire naivă dă greș.\n\nFiecare punct de mai jos este obligatoriu.\n- `suggest-verifies` propune o etichetă `# verifies: <id>#CASE-N` pentru fiecare criteriu neetichetat al cărui fișier tested-by al cerinței are un test denumit după el. [[REQ-SUGGESTVERIFIES-927]] detaliază comportamentul.\n- O potrivire necesită un token întreg care numește criteriul (și, într-un fișier comun, și un token distinctiv de id); o potrivire ambiguă nu propune nimic. [[REQ-SUGGESTVERIFIES-928]] detaliază comportamentul.\n- `suggest-verifies` nu scrie nimic implicit, doar afișează propuneri și ambiguități; `--apply` scrie etichetele și este sigur de rerulat. [[REQ-SUGGESTVERIFIES-929]] detaliază comportamentul.",
          "acceptance": "CASE-1  <!-- verifiable by: automated test -->\n  Given  o cerință cu un `CASE-1` neetichetat și un `def test_ac1_x` în fișierul ei tested-by\n  When   rulează `suggest-verifies`\n  Then   afișează o propunere care numește fișierul, linia și eticheta de adăugat\nCASE-2  <!-- verifiable by: automated test -->\n  Given  un fișier tested-by partajat de două cerințe și un test denumit `test_ac1_generic`\n  When   rulează `suggest-verifies`\n  Then   nu se face nicio propunere pentru acel criteriu\nCASE-3  <!-- verifiable by: automated test -->\n  Given  un test al cărui nume poartă numărul altei cerințe, precum `test_ac1_083_x`\n  When   rulează `suggest-verifies`\n  Then   nu este propus pentru cerința în al cărei fișier se află\nCASE-4  <!-- verifiable by: automated test -->\n  Given  două teste care se potrivesc cu același criteriu\n  When   rulează `suggest-verifies --apply`\n  Then   criteriul este raportat ca ambiguu și nu se scrie nicio etichetă\nCASE-5  <!-- verifiable by: automated test -->\n  Given  un singur test care se potrivește\n  When   `suggest-verifies --apply` rulează de două ori\n  Then   eticheta este scrisă o dată, iar a doua rulare nu găsește nimic de propus"
        }
      }
    },
    {
      "id": "REQ-SUGGESTVERIFIES-927",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Proposing a tag from a matching test name",
      "intent": "A test named `test_case1_x` already proves `CASE-1` — the link just is not machine-readable yet. `suggest-verifies` reads the tested-by files of the requirement that owns each untagged, machine-verifiable criterion, recognizing a test name from a `def`/`function`/`func` declaration or an `it(...)`/`test(...)` label, and proposes the tag that would make the link explicit.",
      "contract": [
        "`suggest-verifies` proposes a `# verifies: <id>#AC-N` tag for each untagged criterion whose requirement has a test named after it.",
        "`suggest-verifies` searches only the `tested-by` files of the requirement that owns the criterion.",
        "`suggest-verifies` reads a test name from a `def`, `function` or `func` declaration whose own name contains \"test\", or from an `it(...)`/`test(...)` label.",
        "A criterion already carrying a `verifies` tag is never proposed again.",
        "A criterion marked as not machine-verifiable is never proposed."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — an untagged criterion with a matching test yields a proposal Given  a requirement with untagged `CASE-1` and its tested-by file defining `def test_case1_x` When   `suggest-verifies` runs Then   it proposes the tag `# verifies: <id>#CASE-1` for that test",
        "CASE-2 — — matching is restricted to the owning requirement's tested-by files Given  a test named after `CASE-1` sitting in a file that is not this requirement's tested-by file When   `suggest-verifies` runs Then   that test is not proposed for the criterion",
        "CASE-3 — — a test name is read from a def, function, func or it/test label Given  a Python `def test_case1_x`, a JS `function test_case1_x()`, and a `it(\"test_case1_x\", ...)` call When   `suggest-verifies` runs Then   each is recognized as a test named `test_case1_x`",
        "CASE-4 — — an already-tagged criterion is never re-proposed Given  a `CASE-1` already carrying a `# verifies:` tag and a matching test name When   `suggest-verifies` runs Then   no proposal is printed for that criterion",
        "CASE-5 — — a criterion marked not machine-verifiable is skipped Given  a `CASE-1` marked not machine-verifiable and a matching test name When   `suggest-verifies` runs Then   no proposal is printed for that criterion --------------------"
      ],
      "accept": "CASE-1 — an untagged criterion with a matching test yields a proposal\n  Given  a requirement with untagged `CASE-1` and its tested-by file defining `def test_case1_x`\n  When   `suggest-verifies` runs\n  Then   it proposes the tag `# verifies: <id>#CASE-1` for that test\n\nCASE-2 — matching is restricted to the owning requirement's tested-by files\n  Given  a test named after `CASE-1` sitting in a file that is not this requirement's tested-by\n         file\n  When   `suggest-verifies` runs\n  Then   that test is not proposed for the criterion\n\nCASE-3 — a test name is read from a def, function, func or it/test label\n  Given  a Python `def test_case1_x`, a JS `function test_case1_x()`, and a `it(\"test_case1_x\",\n         ...)` call\n  When   `suggest-verifies` runs\n  Then   each is recognized as a test named `test_case1_x`\n\nCASE-4 — an already-tagged criterion is never re-proposed\n  Given  a `CASE-1` already carrying a `# verifies:` tag and a matching test name\n  When   `suggest-verifies` runs\n  Then   no proposal is printed for that criterion\n\nCASE-5 — a criterion marked not machine-verifiable is skipped\n  Given  a `CASE-1` marked not machine-verifiable and a matching test name\n  When   `suggest-verifies` runs\n  Then   no proposal is printed for that criterion\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SUGGESTVERIFIES-047"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7563"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7595"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4187"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9188"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Propunerea unui tag dintr-un nume de test potrivit",
          "intent": "Un test numit `test_case1_x` deja dovedește `CASE-1` — legătura pur și simplu nu este încă citibilă de mașină. `suggest-verifies` citește fișierele tested-by ale cerinței care deține fiecare criteriu neetichetat, verificabil-de-mașină, recunoscând un nume de test dintr-o declarație `def`/`function`/`func` sau o etichetă `it(...)`/`test(...)`, și propune tag-ul care ar face legătura explicită.",
          "contract": "> Un test numit `test_case1_x` deja dovedește `CASE-1` — legătura pur și simplu nu este\n> încă citibilă de mașină. `suggest-verifies` citește fișierele tested-by ale cerinței\n> care deține fiecare criteriu neetichetat, verificabil-de-mașină, recunoscând un nume de\n> test dintr-o declarație `def`/`function`/`func` sau o etichetă `it(...)`/`test(...)`, și\n> propune tag-ul care ar face legătura explicită.\n\nFiecare punct de mai jos este obligatoriu.\n- `suggest-verifies` propune un tag `# verifies: <id>#AC-N` pentru fiecare criteriu neetichetat a cărui\n  cerință are un test numit după el.\n- `suggest-verifies` caută doar fișierele `tested-by` ale cerinței care deține\n  criteriul.\n- `suggest-verifies` citește un nume de test dintr-o declarație `def`, `function` sau `func` al cărei\n  propriu nume conține \"test\", sau dintr-o etichetă `it(...)`/`test(...)`.\n- Un criteriu care deja poartă un tag `verifies` nu este niciodată propus din nou.\n- Un criteriu marcat ca neverificabil-de-mașină nu este niciodată propus.",
          "acceptance": "CASE-1 — un criteriu neetichetat cu un test potrivit produce o propunere\n  Given  o cerință cu `CASE-1` neetichetat, iar fișierul ei tested-by definind `def test_case1_x`\n  When   `suggest-verifies` rulează\n  Then   propune tag-ul `# verifies: <id>#CASE-1` pentru acel test\n\nCASE-2 — potrivirea este restrânsă la fișierele tested-by ale cerinței proprietare\n  Given  un test numit după `CASE-1` aflat într-un fișier care nu este fișierul tested-by al acestei cerințe\n  When   `suggest-verifies` rulează\n  Then   acel test nu este propus pentru criteriu\n\nCASE-3 — un nume de test este citit dintr-o etichetă def, function, func sau it/test\n  Given  un `def test_case1_x` Python, un `function test_case1_x()` JS, și un apel `it(\"test_case1_x\",\n         ...)`\n  When   `suggest-verifies` rulează\n  Then   fiecare este recunoscut ca un test numit `test_case1_x`\n\nCASE-4 — un criteriu deja etichetat nu este niciodată re-propus\n  Given  un `CASE-1` deja purtând un tag `# verifies:` și un nume de test potrivit\n  When   `suggest-verifies` rulează\n  Then   nicio propunere nu este afișată pentru acel criteriu\n\nCASE-5 — un criteriu marcat neverificabil-de-mașină este omis\n  Given  un `CASE-1` marcat neverificabil-de-mașină și un nume de test potrivit\n  When   `suggest-verifies` rulează\n  Then   nicio propunere nu este afișată pentru acel criteriu\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-SUGGESTVERIFIES-928",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Refusing a match that could be wrong",
      "intent": "A naive name match goes wrong in three specific ways: a substring match (`ac12` inside `CASE-1`), a generic name that fits any requirement sharing a test file, and a name that actually belongs to a different requirement's criterion. `suggest-verifies` checks for all three before proposing anything, and reports — rather than guesses at — a criterion matched by more than one test.",
      "contract": [
        "A name matches a criterion only as a whole token, so `ac12` never matches `CASE-1`.",
        "When the tested-by file belongs to more than one requirement, the test name also carries a distinctive token of this requirement's id, else no proposal is made.",
        "A test whose name carries another requirement's number belongs to that requirement, and is never proposed for this one.",
        "When two or more tests match one criterion, both are reported as ambiguous and neither is written."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — matching requires a whole token, not a substring Given  a test named `test_ac12_x` and an untagged `CASE-1` When   `suggest-verifies` runs Then   `test_ac12_x` is not proposed as a match for `CASE-1`",
        "CASE-2 — — a shared tested-by file needs a distinctive id token to match Given  a tested-by file shared by two requirements and a test `test_ac1_generic` naming neither id distinctly When   `suggest-verifies` runs Then   no proposal is made for that criterion in either requirement",
        "CASE-3 — — a test carrying another requirement's number belongs to that requirement Given  a test named `test_ac1_083_x` sitting in a file shared with requirement 083 When   `suggest-verifies` runs Then   the test is proposed for requirement 083's `CASE-1`, not for the requirement whose file it sits in",
        "CASE-4 — — two matching tests report the criterion as ambiguous Given  two tests both named after the same untagged `CASE-1` When   `suggest-verifies` runs Then   the criterion is reported as ambiguous and no tag is proposed --------------------"
      ],
      "accept": "CASE-1 — matching requires a whole token, not a substring\n  Given  a test named `test_ac12_x` and an untagged `CASE-1`\n  When   `suggest-verifies` runs\n  Then   `test_ac12_x` is not proposed as a match for `CASE-1`\n\nCASE-2 — a shared tested-by file needs a distinctive id token to match\n  Given  a tested-by file shared by two requirements and a test `test_ac1_generic` naming\n         neither id distinctly\n  When   `suggest-verifies` runs\n  Then   no proposal is made for that criterion in either requirement\n\nCASE-3 — a test carrying another requirement's number belongs to that requirement\n  Given  a test named `test_ac1_083_x` sitting in a file shared with requirement 083\n  When   `suggest-verifies` runs\n  Then   the test is proposed for requirement 083's `CASE-1`, not for the requirement whose file\n         it sits in\n\nCASE-4 — two matching tests report the criterion as ambiguous\n  Given  two tests both named after the same untagged `CASE-1`\n  When   `suggest-verifies` runs\n  Then   the criterion is reported as ambiguous and no tag is proposed\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SUGGESTVERIFIES-047"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7584"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7595"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4187"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Refuzarea unei potriviri care ar putea fi greșită",
          "intent": "O potrivire naivă de nume greșește în trei moduri specifice: o potrivire de substring (`ac12` în interiorul `CASE-1`), un nume generic care se potrivește oricărei cerințe care partajează un fișier de test, și un nume care de fapt aparține criteriului unei alte cerințe. `suggest-verifies` verifică toate trei înainte de a propune orice, și raportează — mai degrabă decât ghicește la — un criteriu potrivit de mai mult de un test.",
          "contract": "> O potrivire naivă de nume greșește în trei moduri specifice: o potrivire de substring\n> (`ac12` în interiorul `CASE-1`), un nume generic care se potrivește oricărei cerințe care\n> partajează un fișier de test, și un nume care de fapt aparține criteriului unei alte\n> cerințe. `suggest-verifies` verifică toate trei înainte de a propune orice, și raportează\n> — mai degrabă decât ghicește la — un criteriu potrivit de mai mult de un test.\n\nFiecare punct de mai jos este obligatoriu.\n- Un nume se potrivește cu un criteriu doar ca un token întreg, astfel `ac12` nu se potrivește niciodată cu `CASE-1`.\n- Când fișierul tested-by aparține mai multor cerințe, numele testului trebuie de asemenea să poarte un\n  token distinctiv al id-ului acestei cerințe, altfel nicio propunere nu este făcută.\n- Un test al cărui nume poartă numărul unei alte cerințe aparține acelei cerințe, și este\n  niciodată propus pentru aceasta.\n- Când două sau mai multe teste se potrivesc unui criteriu, ambele sunt raportate ca ambigue și niciunul nu este\n  scris.",
          "acceptance": "CASE-1 — potrivirea necesită un token întreg, nu un substring\n  Given  un test numit `test_ac12_x` și un `CASE-1` neetichetat\n  When   `suggest-verifies` rulează\n  Then   `test_ac12_x` nu este propus ca o potrivire pentru `CASE-1`\n\nCASE-2 — un fișier tested-by partajat are nevoie de un token distinctiv de id pentru a se potrivi\n  Given  un fișier tested-by partajat de două cerințe și un test `test_ac1_generic` care nu numește\n         distinctiv niciun id\n  When   `suggest-verifies` rulează\n  Then   nicio propunere nu este făcută pentru acel criteriu în oricare cerință\n\nCASE-3 — un test purtând numărul unei alte cerințe aparține acelei cerințe\n  Given  un test numit `test_ac1_083_x` aflat într-un fișier partajat cu cerința 083\n  When   `suggest-verifies` rulează\n  Then   testul este propus pentru `CASE-1`-ul cerinței 083, nu pentru cerința în al cărei fișier\n         se află\n\nCASE-4 — două teste potrivite raportează criteriul ca ambiguu\n  Given  două teste ambele numite după același `CASE-1` neetichetat\n  When   `suggest-verifies` rulează\n  Then   criteriul este raportat ca ambiguu și niciun tag nu este propus\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-SUGGESTVERIFIES-929",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "A dry run by default, --apply to write",
      "intent": "Proposing hundreds of tags across an old corpus should be reviewable before it is permanent, so `suggest-verifies` only prints by default. `--apply` appends each proposed tag to its test's declaration line in that file's own comment syntax, and leaves an already-tagged line untouched, so running it twice is safe.",
      "contract": [
        "`suggest-verifies` writes nothing by default. It prints the proposals and the ambiguities.",
        "`--apply` appends each proposed tag to its test's declaration line, in that file's comment syntax.",
        "`--apply` leaves a line that already carries the same tag unchanged, so re-running it is safe."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a dry run prints proposals without writing Given  an untagged criterion with a matching test When   `suggest-verifies` runs without `--apply` Then   it prints the proposal, and the test file on disk stays unchanged",
        "CASE-2 — — --apply appends the tag using the file's comment syntax Given  a Python test file with an untagged matching test When   `suggest-verifies --apply` runs Then   the test's declaration line gains a trailing `# verifies: <id>#CASE-N` comment",
        "CASE-3 — — re-running --apply is a no-op for an already-tagged test Given  a test whose declaration line already carries the proposed `verifies` tag When   `suggest-verifies --apply` runs again Then   that line is left unchanged and no duplicate tag is added"
      ],
      "accept": "CASE-1 — a dry run prints proposals without writing\n  Given  an untagged criterion with a matching test\n  When   `suggest-verifies` runs without `--apply`\n  Then   it prints the proposal, and the test file on disk stays unchanged\n\nCASE-2 — --apply appends the tag using the file's comment syntax\n  Given  a Python test file with an untagged matching test\n  When   `suggest-verifies --apply` runs\n  Then   the test's declaration line gains a trailing `# verifies: <id>#CASE-N` comment\n\nCASE-3 — re-running --apply is a no-op for an already-tagged test\n  Given  a test whose declaration line already carries the proposed `verifies` tag\n  When   `suggest-verifies --apply` runs again\n  Then   that line is left unchanged and no duplicate tag is added",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-SUGGESTVERIFIES-047"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7656"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7694"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4187"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9188"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Un dry run implicit, --apply pentru a scrie",
          "intent": "Propunerea a sute de tag-uri pe un corpus vechi ar trebui revizuibilă înainte de a fi permanentă, astfel `suggest-verifies` doar afișează implicit. `--apply` adaugă fiecare tag propus la linia de declarație a testului lui, în sintaxa de comentariu proprie a acelui fișier, și lasă neatinsă o linie deja etichetată, astfel rularea lui de două ori este sigură.",
          "contract": "> Propunerea a sute de tag-uri pe un corpus vechi ar trebui revizuibilă înainte de a fi\n> permanentă, astfel `suggest-verifies` doar afișează implicit. `--apply` adaugă fiecare\n> tag propus la linia de declarație a testului lui, în sintaxa de comentariu proprie a\n> acelui fișier, și lasă neatinsă o linie deja etichetată, astfel rularea lui de două ori\n> este sigură.\n\nFiecare punct de mai jos este obligatoriu.\n- `suggest-verifies` nu scrie nimic implicit. Afișează propunerile și ambiguitățile.\n- `--apply` adaugă fiecare tag propus la linia de declarație a testului lui, în sintaxa de comentariu\n  a acelui fișier.\n- `--apply` lasă neatinsă o linie care deja poartă același tag, astfel rerularea este\n  sigură.",
          "acceptance": "CASE-1 — un dry run afișează propunerile fără să scrie\n  Given  un criteriu neetichetat cu un test potrivit\n  When   `suggest-verifies` rulează fără `--apply`\n  Then   afișează propunerea, iar fișierul de test de pe disc rămâne neschimbat\n\nCASE-2 — --apply adaugă tag-ul folosind sintaxa de comentariu a fișierului\n  Given  un fișier de test Python cu un test potrivit neetichetat\n  When   `suggest-verifies --apply` rulează\n  Then   linia de declarație a testului câștigă un comentariu final `# verifies: <id>#CASE-N`\n\nCASE-3 — rerularea --apply este un no-op pentru un test deja etichetat\n  Given  un test a cărui linie de declarație deja poartă tag-ul `verifies` propus\n  When   `suggest-verifies --apply` rulează din nou\n  Then   acea linie este lăsată neschimbată și niciun tag duplicat nu este adăugat"
        }
      }
    },
    {
      "id": "ARCH-TESTLINK-018",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Test-link integrity check",
      "intent": "A requirement can claim it is tested by pointing at a test file — but if that file is gone or has no real test in it, the link is a lie and the safety net exists only on paper. This makes the gate check those links: it warns when a `tested-by` file is missing or holds no test, so a broken link cannot pose as coverage. Without it, a project can look fully tested while its tests have quietly disappeared.",
      "contract": [
        "The gate checks every `tested-by` link, at every status, as the deterministic half of behavior-sync. [[REQ-TESTLINK-930]]",
        "The gate recognizes a test function lexically, by language-specific patterns such as Python `def test...(` or a Jest `it(`/`test(` call. [[REQ-TESTLINK-931]]",
        "The gate also recognizes a Rust `#[test]`, a shell `test_x()`/bats `@test`, and a stdlib-style Python `run`/`main` entry point under `if __name__ == \"__main__\"`. [[REQ-TESTLINK-932]]",
        "When a `tested-by` file is missing, unreadable, or holds no test function, the gate adds one warning naming the requirement and the file. [[REQ-TESTLINK-933]]"
      ],
      "verify": [],
      "notes": [
        "The `run`/`main` entry-point form is accepted because stdlib-only suites commonly take that shape rather than a `def test...` per case, and a `tested-by` tag already declares the file a test.",
        "This does not prove that each acceptance criterion is tested. It proves only that real tests exist at the link target. Per-criterion mapping needs a per-criterion tag and is deferred.",
        "Detection is lexical, not a parse. A file that mentions a test-shaped call in a string could pass without real tests. The check targets the common failure: a renamed, deleted, or placeholder file, not a deliberate fake.",
        "Only confirmed requirements are checked. A draft or baseline requirement is exempt, matching the rest of the gate's enforcement scope."
      ],
      "current_impl": [
        "`_test_link_problem` in `reqmap.py`, called from `cmd_check` — `_test_link_problem` returns a short reason a file fails the check or an empty string when it passes; `cmd_check` resolves each distinct `tested-by` file against the scan root and appends one warning per failing link.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a confirmed requirement whose `tested-by` file is missing When   the gate runs Then   it adds a warning that names the requirement and the file",
        "CASE-2 — Given  a confirmed requirement whose `tested-by` file exists and contains a `def test...(` When   the gate runs Then   it adds no warning for that link",
        "CASE-3 — Given  a confirmed requirement whose `tested-by` file exists but contains no test function When   the gate runs Then   it adds a warning",
        "CASE-4 — Given  any of the above When   the gate runs Then   the error count is unchanged (the check is warn-only)",
        "CASE-5 — Given  a confirmed requirement whose `tested-by` is a `.py` file with no `def test...`, but which defines a `run()`/`main()` entry point under an `if __name__ == \"__main__\"` guard When   the gate runs Then   it adds no warning for that link",
        "CASE-6 — Given  a `tested-by` file that is a shell script holding a `test_x()` function When   the gate runs Then   it adds no warning for that link",
        "CASE-7 — Given  a draft requirement whose `tested-by` file holds no test function When   the gate runs with `--strict` Then   it adds a warning and the exit code stays 0"
      ],
      "accept": "CASE-1\n  Given  a confirmed requirement whose `tested-by` file is missing\n  When   the gate runs\n  Then   it adds a warning that names the requirement and the file\n\nCASE-2\n  Given  a confirmed requirement whose `tested-by` file exists and contains a `def test...(`\n  When   the gate runs\n  Then   it adds no warning for that link\n\nCASE-3\n  Given  a confirmed requirement whose `tested-by` file exists but contains no test function\n  When   the gate runs\n  Then   it adds a warning\n\nCASE-4\n  Given  any of the above\n  When   the gate runs\n  Then   the error count is unchanged (the check is warn-only)\n\nCASE-5\n  Given  a confirmed requirement whose `tested-by` is a `.py` file with no `def test...`,\n         but which defines a `run()`/`main()` entry point under an\n         `if __name__ == \"__main__\"` guard\n  When   the gate runs\n  Then   it adds no warning for that link\n\nCASE-6\n  Given  a `tested-by` file that is a shell script holding a `test_x()` function\n  When   the gate runs\n  Then   it adds no warning for that link\n\nCASE-7\n  Given  a draft requirement whose `tested-by` file holds no test function\n  When   the gate runs with `--strict`\n  Then   it adds a warning and the exit code stays 0",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-CHECK-006"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-GATE-102"
      ],
      "satisfied_by": [
        "REQ-TESTLINK-930",
        "REQ-TESTLINK-931",
        "REQ-TESTLINK-932",
        "REQ-TESTLINK-933"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2364"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2384"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2604"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3799"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4075"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4110"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9263"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.15",
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Verificarea integrității legăturilor de test",
          "intent": "O cerință poate pretinde că este testată indicând un fișier de test — dar dacă acel fișier a dispărut sau nu conține niciun test real, legătura este o minciună, iar plasa de siguranță există doar pe hârtie. Aceasta face ca gate să verifice acele legături: avertizează când un fișier `tested-by` lipsește sau nu conține niciun test, astfel încât o legătură ruptă nu se poate da drept acoperire. Fără ea, un proiect poate părea complet testat în timp ce testele lui au dispărut în tăcere.",
          "contract": "> O cerință poate pretinde că este testată indicând un fișier de test — dar dacă acel fișier\n> a dispărut sau nu conține niciun test real, legătura este o minciună, iar plasa de\n> siguranță există doar pe hârtie. Aceasta face ca gate să verifice acele legături: avertizează\n> când un fișier `tested-by` lipsește sau nu conține niciun test, astfel încât o legătură ruptă\n> nu se poate da drept acoperire. Fără ea, un proiect poate părea complet testat în timp ce testele lui au dispărut în tăcere.\n\nFiecare punct de mai jos este obligatoriu.\n- Gate verifică fiecare legătură `tested-by`, la fiecare status, ca jumătate deterministă a behavior-sync. [[REQ-TESTLINK-930]]\n- Gate recunoaște o funcție de test lexical, prin tipare specifice limbajului precum Python `def test...(` sau un apel Jest `it(`/`test(`. [[REQ-TESTLINK-931]]\n- Gate mai recunoaște un `#[test]` Rust, un `test_x()` shell/bats `@test`, și un punct de intrare `run`/`main` stil-stdlib Python sub `if __name__ == \"__main__\"`. [[REQ-TESTLINK-932]]\n- Când un fișier `tested-by` lipsește, este ilizibil, sau nu conține nicio funcție de test, gate adaugă un avertisment care numește cerința și fișierul. [[REQ-TESTLINK-933]]",
          "acceptance": "CASE-1\n  Given  o cerință confirmed al cărei fișier `tested-by` lipsește\n  When   rulează gate\n  Then   adaugă un avertisment care numește cerința și fișierul\n\nCASE-2\n  Given  o cerință confirmed al cărei fișier `tested-by` există și conține un `def test...(`\n  When   rulează gate\n  Then   nu adaugă niciun avertisment pentru acea legătură\n\nCASE-3\n  Given  o cerință confirmed al cărei fișier `tested-by` există dar nu conține nicio funcție de test\n  When   rulează gate\n  Then   adaugă un avertisment\n\nCASE-4\n  Given  oricare dintre cele de mai sus\n  When   rulează gate\n  Then   numărul de erori rămâne neschimbat (verificarea este doar avertisment)\n\nCASE-5\n  Given  o cerință confirmed al cărei `tested-by` este un fișier `.py` fără `def test...`,\n         dar care definește un punct de intrare `run()`/`main()` sub o gardă\n         `if __name__ == \"__main__\"`\n  When   rulează gate\n  Then   nu adaugă niciun avertisment pentru acea legătură\n\nCASE-6\n  Given  un fișier `tested-by` care este un script shell ce conține o funcție `test_x()`\n  When   rulează gate\n  Then   nu adaugă niciun avertisment pentru acea legătură\n\nCASE-7\n  Given  o cerință draft al cărei fișier `tested-by` nu conține nicio funcție de test\n  When   rulează gate cu `--strict`\n  Then   adaugă un avertisment și codul de ieșire rămâne 0"
        }
      }
    },
    {
      "id": "REQ-TESTLINK-930",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Checking every tested-by link, at every status",
      "intent": "A `tested-by` link is a claim of coverage, and that claim can go stale the moment a file is renamed or emptied out. This check runs at every status, not just `confirmed`, because a link pointing at the wrong file is wrong the day it is written — waiting for promotion would only audit the corpus once it is largest.",
      "contract": [
        "The gate checks every `tested-by` link, at every status. This is the deterministic half of behavior-sync.",
        "For each distinct `tested-by` file, the gate verifies that the file exists.",
        "For each such file the gate also verifies that it holds at least one test function."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a non-confirmed requirement's test link is still checked Given  a `baseline` requirement whose `tested-by:` file exists but holds no test function When   `gate` runs Then   it reports that link as a warning, because the check is not gated on status",
        "CASE-2 — — a missing tested-by target is reported as broken Given  a path with no file on disk When   `_test_link_problem` runs on that path Then   it returns a string containing \"does not exist\"",
        "CASE-3 — — a file with only a helper function fails the check Given  a `.py` file whose only function is `def helper(): return 1` When   `_test_link_problem` runs on that file Then   it returns a string containing \"no test function\" --------------------"
      ],
      "accept": "CASE-1 — a non-confirmed requirement's test link is still checked\n  Given  a `baseline` requirement whose `tested-by:` file exists but holds no test function\n  When   `gate` runs\n  Then   it reports that link as a warning, because the check is not gated on status\n\nCASE-2 — a missing tested-by target is reported as broken\n  Given  a path with no file on disk\n  When   `_test_link_problem` runs on that path\n  Then   it returns a string containing \"does not exist\"\n\nCASE-3 — a file with only a helper function fails the check\n  Given  a `.py` file whose only function is `def helper(): return 1`\n  When   `_test_link_problem` runs on that file\n  Then   it returns a string containing \"no test function\"\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-TESTLINK-018"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2364"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3799"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4110"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Verificarea fiecărei legături tested-by, la fiecare status",
          "intent": "O legătură `tested-by` este o afirmație de acoperire, iar acea afirmație poate deveni învechită în momentul în care un fișier este redenumit sau golit. Această verificare rulează la fiecare status, nu doar la `confirmed`, pentru că o legătură care indică fișierul greșit este greșită din ziua în care este scrisă — așteptarea promovării ar audita corpusul doar atunci când este cel mai mare.",
          "contract": "> O legătură `tested-by` este o afirmație de acoperire, iar acea afirmație poate deveni\n> învechită în momentul în care un fișier este redenumit sau golit. Această verificare rulează\n> la fiecare status, nu doar la `confirmed`, pentru că o legătură care indică fișierul greșit\n> este greșită din ziua în care este scrisă — așteptarea promovării ar audita corpusul doar\n> atunci când este cel mai mare.\n\nFiecare punct de mai jos este obligatoriu.\n- Gate-ul verifică fiecare legătură `tested-by`, la fiecare status. Aceasta este jumătatea\n  deterministă a sincronizării-de-comportament.\n- Pentru fiecare fișier `tested-by` distinct, gate-ul verifică că fișierul există.\n- Pentru fiecare astfel de fișier gate-ul de asemenea verifică că deține cel puțin o funcție de test.",
          "acceptance": "CASE-1 — legătura de test a unei cerințe non-confirmed este încă verificată\n  Given  o cerință `baseline` al cărei fișier `tested-by:` există dar nu deține nicio funcție de test\n  When   `gate` rulează\n  Then   raportează acea legătură ca un avertisment, pentru că verificarea nu este condiționată de status\n\nCASE-2 — o țintă tested-by lipsă este raportată ca ruptă\n  Given  o cale fără niciun fișier pe disc\n  When   `_test_link_problem` rulează pe acea cale\n  Then   returnează un șir conținând \"does not exist\"\n\nCASE-3 — un fișier cu doar o funcție ajutătoare eșuează verificarea\n  Given  un fișier `.py` a cărui unică funcție este `def helper(): return 1`\n  When   `_test_link_problem` rulează pe acel fișier\n  Then   returnează un șir conținând \"no test function\"\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-TESTLINK-931",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Recognizing a test function by shape, not by parsing",
      "intent": "The check has no parser for every language a `tested-by` file might be written in, so it looks for the textual shape a test function takes instead — `def test_foo(`, `it(...)`, `func TestX(`. This is cheap and language-agnostic, at the cost of trusting a string that merely looks like a test.",
      "contract": [
        "The gate recognizes a test function lexically.",
        "A Python `def test...(` counts.",
        "A JavaScript or TypeScript `function test...(` counts.",
        "An `it(` call or a `test(` call counts.",
        "A Go `func Test/Benchmark/Example/Fuzz(` counts."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a test-shaped string inside a docstring still passes Given  a `.py` file whose only content is a docstring containing the text `def test_foo():` When   `_test_link_problem` runs on that file Then   it returns \"\" — the pattern match is textual, with no AST parse behind it",
        "CASE-2 — — a file with def test_ passes the check Given  a `.py` file containing `def test_something():` When   `_test_link_problem` runs on that file Then   it returns \"\"",
        "CASE-3 — — a def-style JS test function passes the check Given  a `.ts` file containing `function testAdd() { assert(1 + 1 === 2); }` When   `_test_link_problem` runs on that file Then   it returns \"\"",
        "CASE-4 — — a Jest-style it() call passes the check Given  a `.test.js` file containing `it('works', () => { expect(1).toBe(1); });` When   `_test_link_problem` runs on that file Then   it returns \"\"",
        "CASE-5 — — a Go func TestX passes the check Given  a `.go` file containing `func TestScan(t *testing.T) {}` When   `_test_link_problem` runs on that file Then   it returns \"\" --------------------"
      ],
      "accept": "CASE-1 — a test-shaped string inside a docstring still passes\n  Given  a `.py` file whose only content is a docstring containing the text `def test_foo():`\n  When   `_test_link_problem` runs on that file\n  Then   it returns \"\" — the pattern match is textual, with no AST parse behind it\n\nCASE-2 — a file with def test_ passes the check\n  Given  a `.py` file containing `def test_something():`\n  When   `_test_link_problem` runs on that file\n  Then   it returns \"\"\n\nCASE-3 — a def-style JS test function passes the check\n  Given  a `.ts` file containing `function testAdd() { assert(1 + 1 === 2); }`\n  When   `_test_link_problem` runs on that file\n  Then   it returns \"\"\n\nCASE-4 — a Jest-style it() call passes the check\n  Given  a `.test.js` file containing `it('works', () => { expect(1).toBe(1); });`\n  When   `_test_link_problem` runs on that file\n  Then   it returns \"\"\n\nCASE-5 — a Go func TestX passes the check\n  Given  a `.go` file containing `func TestScan(t *testing.T) {}`\n  When   `_test_link_problem` runs on that file\n  Then   it returns \"\"\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-TESTLINK-018"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2364"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3799"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9263"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Recunoașterea unei funcții de test după formă, nu prin parsare",
          "intent": "Verificarea nu are un parser pentru fiecare limbaj în care ar putea fi scris un fișier `tested-by`, astfel caută în schimb forma textuală pe care o ia o funcție de test — `def test_foo(`, `it(...)`, `func TestX(`. Acest lucru este ieftin și agnostic de limbaj, cu prețul de a avea încredere într-un șir care doar arată ca un test.",
          "contract": "> Verificarea nu are un parser pentru fiecare limbaj în care ar putea fi scris un fișier\n> `tested-by`, astfel caută în schimb forma textuală pe care o ia o funcție de test —\n> `def test_foo(`, `it(...)`, `func TestX(`. Acest lucru este ieftin și agnostic de limbaj,\n> cu prețul de a avea încredere într-un șir care doar arată ca un test.\n\nFiecare punct de mai jos este obligatoriu.\n- Gate-ul recunoaște o funcție de test lexical.\n- Un `def test...(` Python contează.\n- Un `function test...(` JavaScript sau TypeScript contează.\n- Un apel `it(` sau un apel `test(` contează.\n- Un `func Test/Benchmark/Example/Fuzz(` Go contează.",
          "acceptance": "CASE-1 — un șir în formă de test în interiorul unui docstring încă trece\n  Given  un fișier `.py` al cărui unic conținut este un docstring conținând textul `def test_foo():`\n  When   `_test_link_problem` rulează pe acel fișier\n  Then   returnează \"\" — potrivirea de pattern este textuală, fără nicio parsare AST în spate\n\nCASE-2 — un fișier cu def test_ trece verificarea\n  Given  un fișier `.py` conținând `def test_something():`\n  When   `_test_link_problem` rulează pe acel fișier\n  Then   returnează \"\"\n\nCASE-3 — o funcție de test JS în stil def trece verificarea\n  Given  un fișier `.ts` conținând `function testAdd() { assert(1 + 1 === 2); }`\n  When   `_test_link_problem` rulează pe acel fișier\n  Then   returnează \"\"\n\nCASE-4 — un apel it() în stil Jest trece verificarea\n  Given  un fișier `.test.js` conținând `it('works', () => { expect(1).toBe(1); });`\n  When   `_test_link_problem` rulează pe acel fișier\n  Then   returnează \"\"\n\nCASE-5 — un func TestX Go trece verificarea\n  Given  un fișier `.go` conținând `func TestScan(t *testing.T) {}`\n  When   `_test_link_problem` rulează pe acel fișier\n  Then   returnează \"\"\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-TESTLINK-932",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Recognizing Rust, shell, and stdlib-style test entry points",
      "intent": "Not every suite writes a `def test...` — a stdlib-only Python suite commonly drives its checks from a `run()`/`main()` guarded by `if __name__ == \"__main__\"`, and shell suites use `test_x()` functions, bats `@test`, or just a `*.test.sh` filename. Each of these recognized shapes exists because a real project in this corpus already used it and would otherwise have warned forever.",
      "contract": [
        "A Rust `#[test]` counts.",
        "A `.py` file with no `def test...` also counts when it drives its checks from a `run`/`run_tests`/`main` entry point under an `if __name__ == \"__main__\"` guard.",
        "A shell `test_x()` function, a `function test_x` definition, or a bats `@test` counts.",
        "A shell file named by a test convention, such as `x.test.sh`, counts on its name alone."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a Rust #[test] attribute passes the check Given  a `.rs` file containing `#[test]\\n  fn checks() {}` When   `_test_link_problem` runs on that file Then   it returns \"\"",
        "CASE-2 — — a run()-under-__main__ Python file passes the check Given  a `.py` file with `def run(): return 0` and `if __name__ == \"__main__\": raise SystemExit(run())` When   `_test_link_problem` runs on that file Then   it returns \"\"",
        "CASE-3 — — a bash test_x() function and a bats @test case both pass Given  a `.sh` file with `test_backup_runs() { [ -f x ]; }` and a `.bats` file with `@test \"restore drill\" { run ./restore }` When   `_test_link_problem` runs on each file Then   both return \"\"",
        "CASE-4 — — a *.test.sh file with no test-shaped body still passes Given  a file named `backup-check.test.sh` whose only content is `./backup-check --dry-run || exit 1` When   `_test_link_problem` runs on that file Then   it returns \"\", recognized by filename alone --------------------"
      ],
      "accept": "CASE-1 — a Rust #[test] attribute passes the check\n  Given  a `.rs` file containing `#[test]\\n  fn checks() {}`\n  When   `_test_link_problem` runs on that file\n  Then   it returns \"\"\n\nCASE-2 — a run()-under-__main__ Python file passes the check\n  Given  a `.py` file with `def run(): return 0` and `if __name__ == \"__main__\": raise SystemExit(run())`\n  When   `_test_link_problem` runs on that file\n  Then   it returns \"\"\n\nCASE-3 — a bash test_x() function and a bats @test case both pass\n  Given  a `.sh` file with `test_backup_runs() { [ -f x ]; }` and a `.bats` file with `@test \"restore drill\" { run ./restore }`\n  When   `_test_link_problem` runs on each file\n  Then   both return \"\"\n\nCASE-4 — a *.test.sh file with no test-shaped body still passes\n  Given  a file named `backup-check.test.sh` whose only content is `./backup-check --dry-run || exit 1`\n  When   `_test_link_problem` runs on that file\n  Then   it returns \"\", recognized by filename alone\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-TESTLINK-018"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2364"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2384"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3799"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4075"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Recunoașterea punctelor de intrare de test Rust, shell și stil-stdlib",
          "intent": "Nu orice suită scrie un `def test...` — o suită Python doar-stdlib își conduce de obicei verificările dintr-un `run()`/`main()` protejat de `if __name__ == \"__main__\"`, iar suitele shell folosesc funcții `test_x()`, bats `@test`, sau doar un nume de fișier `*.test.sh`. Fiecare dintre aceste forme recunoscute există pentru că un proiect real din acest corpus a folosit-o deja și ar fi altfel avertizat la nesfârșit.",
          "contract": "> Nu orice suită scrie un `def test...` — o suită Python doar-stdlib își conduce de obicei\n> verificările dintr-un `run()`/`main()` protejat de `if __name__ == \"__main__\"`, iar suitele\n> shell folosesc funcții `test_x()`, bats `@test`, sau doar un nume de fișier `*.test.sh`.\n> Fiecare dintre aceste forme recunoscute există pentru că un proiect real din acest corpus\n> a folosit-o deja și ar fi altfel avertizat la nesfârșit.\n\nFiecare punct de mai jos este obligatoriu.\n- Un `#[test]` Rust contează.\n- Un fișier `.py` fără niciun `def test...` contează de asemenea când își conduce verificările dintr-un\n  punct de intrare `run`/`run_tests`/`main` sub o gardă `if __name__ == \"__main__\"`.\n- O funcție shell `test_x()`, o definiție `function test_x`, sau un `@test` bats contează.\n- Un fișier shell numit după o convenție de test, precum `x.test.sh`, contează doar după numele lui.",
          "acceptance": "CASE-1 — un atribut Rust #[test] trece verificarea\n  Given  un fișier `.rs` conținând `#[test]\\n  fn checks() {}`\n  When   `_test_link_problem` rulează pe acel fișier\n  Then   returnează \"\"\n\nCASE-2 — un fișier Python run()-sub-__main__ trece verificarea\n  Given  un fișier `.py` cu `def run(): return 0` și `if __name__ == \"__main__\": raise SystemExit(run())`\n  When   `_test_link_problem` rulează pe acel fișier\n  Then   returnează \"\"\n\nCASE-3 — o funcție bash test_x() și un caz bats @test trec ambele\n  Given  un fișier `.sh` cu `test_backup_runs() { [ -f x ]; }` și un fișier `.bats` cu `@test \"restore drill\" { run ./restore }`\n  When   `_test_link_problem` rulează pe fiecare fișier\n  Then   ambele returnează \"\"\n\nCASE-4 — un fișier *.test.sh fără niciun corp în formă de test trece totuși\n  Given  un fișier numit `backup-check.test.sh` al cărui unic conținut este `./backup-check --dry-run || exit 1`\n  When   `_test_link_problem` rulează pe acel fișier\n  Then   returnează \"\", recunoscut doar după nume\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-TESTLINK-933",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Reporting a broken link as a warning, never an error",
      "intent": "A broken `tested-by` link is a documentation problem, not proof the feature itself is broken — so it warns rather than blocking a commit. `--strict` promotes it to an error, but only for a `confirmed` requirement, so a draft-heavy corpus adopting `--strict` in CI does not suddenly start failing on links nobody has finished writing.",
      "contract": [
        "When a file is missing, unreadable, or holds no test function, the gate adds one warning.",
        "That warning names the requirement and the file.",
        "The check is warn-only. It never adds an error, and never changes whether the gate passes.",
        "Under `--strict` the warning becomes an error only for a confirmed requirement.",
        "The check stays silent on a well-formed corpus, so a green gate run gains no noise."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a broken tested-by link produces exactly one gate warning Given  a confirmed requirement whose `tested-by` names `tests/missing_test.py`, a path that does not exist When   `cmd_check` runs Then   its output carries exactly one warning line naming that broken link, adding no error — the gate's pass/fail stays unaffected",
        "CASE-2 — — the warning text names both the requirement id and the file Given  `REQ-A-001` whose `tested-by` names `tests/missing_test.py`, a path that does not exist When   `cmd_check` runs Then   its output contains both \"REQ-A-001\" and \"tests/missing_test.py\" in the same line",
        "CASE-3 — — strict promotion applies to confirmed, not to draft Given  one `draft` and one `confirmed` requirement, each with a `tested-by` link to a non-test file When   `cmd_check` runs with `strict=True` on each Then   the draft's gate still exits 0 while the confirmed one exits 1",
        "CASE-4 — — a valid tested-by link produces no test-link warning Given  a confirmed requirement whose `tested-by` file exists and contains `def test_it(): pass` When   `cmd_check` runs Then   its output contains no \"tested-by\" warning line for that requirement"
      ],
      "accept": "CASE-1 — a broken tested-by link produces exactly one gate warning\n  Given  a confirmed requirement whose `tested-by` names `tests/missing_test.py`, a path that does not exist\n  When   `cmd_check` runs\n  Then   its output carries exactly one warning line naming that broken link, adding no\n         error — the gate's pass/fail stays unaffected\n\nCASE-2 — the warning text names both the requirement id and the file\n  Given  `REQ-A-001` whose `tested-by` names `tests/missing_test.py`, a path that does not exist\n  When   `cmd_check` runs\n  Then   its output contains both \"REQ-A-001\" and \"tests/missing_test.py\" in the same line\n\nCASE-3 — strict promotion applies to confirmed, not to draft\n  Given  one `draft` and one `confirmed` requirement, each with a `tested-by` link to a non-test file\n  When   `cmd_check` runs with `strict=True` on each\n  Then   the draft's gate still exits 0 while the confirmed one exits 1\n\nCASE-4 — a valid tested-by link produces no test-link warning\n  Given  a confirmed requirement whose `tested-by` file exists and contains `def test_it(): pass`\n  When   `cmd_check` runs\n  Then   its output contains no \"tested-by\" warning line for that requirement",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-TESTLINK-018"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2604"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4110"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9263"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Raportarea unei legături rupte ca avertisment, niciodată ca eroare",
          "intent": "O legătură `tested-by` ruptă este o problemă de documentație, nu dovada că funcționalitatea în sine este ruptă — astfel avertizează, nu blochează un commit. `--strict` o promovează la eroare, dar doar pentru o cerință `confirmed`, astfel un corpus plin de draft-uri care adoptă `--strict` în CI nu începe brusc să eșueze pe legături pe care nimeni nu a terminat de scris.",
          "contract": "> O legătură `tested-by` ruptă este o problemă de documentație, nu dovada că funcționalitatea\n> în sine este ruptă — astfel avertizează, nu blochează un commit. `--strict` o promovează la\n> eroare, dar doar pentru o cerință `confirmed`, astfel un corpus plin de draft-uri care\n> adoptă `--strict` în CI nu începe brusc să eșueze pe legături pe care nimeni nu a terminat\n> de scris.\n\nFiecare punct de mai jos este obligatoriu.\n- Când un fișier lipsește, este necitibil, sau nu conține nicio funcție de test, gate-ul adaugă un avertisment.\n- Acel avertisment numește cerința și fișierul.\n- Verificarea este doar avertisment. Nu adaugă niciodată o eroare, și nu schimbă niciodată dacă gate-ul trece.\n- Sub `--strict` avertismentul devine eroare doar pentru o cerință confirmată.\n- Verificarea rămâne tăcută pe un corpus bine format, astfel o rulare de gate verde nu câștigă niciun zgomot.",
          "acceptance": "CASE-1 — o legătură tested-by ruptă produce exact un avertisment de gate\n  Given  o cerință confirmată al cărei `tested-by` numește `tests/missing_test.py`, o cale care nu există\n  When   `cmd_check` rulează\n  Then   ieșirea lui poartă exact o linie de avertisment numind acea legătură ruptă, fără să adauge nicio\n         eroare — trecerea/eșuarea gate-ului rămâne neafectată\n\nCASE-2 — textul avertismentului numește atât id-ul cerinței, cât și fișierul\n  Given  `REQ-A-001` al cărui `tested-by` numește `tests/missing_test.py`, o cale care nu există\n  When   `cmd_check` rulează\n  Then   ieșirea lui conține atât \"REQ-A-001\" cât și \"tests/missing_test.py\" pe aceeași linie\n\nCASE-3 — promovarea strict se aplică la confirmed, nu la draft\n  Given  o cerință `draft` și una `confirmed`, fiecare cu o legătură `tested-by` la un fișier care nu este de test\n  When   `cmd_check` rulează cu `strict=True` pe fiecare\n  Then   gate-ul draft-ului iese totuși cu 0, în timp ce cel confirmat iese cu 1\n\nCASE-4 — o legătură tested-by validă nu produce niciun avertisment de test-link\n  Given  o cerință confirmată al cărei fișier `tested-by` există și conține `def test_it(): pass`\n  When   `cmd_check` rulează\n  Then   ieșirea lui nu conține nicio linie de avertisment \"tested-by\" pentru acea cerință"
        }
      }
    },
    {
      "id": "ARCH-TRACE-020",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Upstream traceability",
      "intent": "A requirement file tells you what a capability does and which code builds it, but not *why it exists* — which stakeholder need it serves. This lets a requirement point upward with a `satisfies:` field, so you can trace from a stakeholder need down to every requirement that fulfils it, and back up from any requirement to the need behind it. Without it, requirements float free of the needs that justify them, and a need can quietly go unaddressed.",
      "contract": [
        "A requirement may declare a `satisfies:` frontmatter list naming the upstream ids it fulfils, and the gate warns (never errors) on a dangling link or an unaddressed confirmed need. [[REQ-TRACE-934]]",
        "The `need` and `aggregate` layers are exempt from the implements/tested-by checks; each is covered by edges — `satisfies:` or `depends_on` — instead of code. [[REQ-TRACE-935]]"
      ],
      "verify": [],
      "notes": [
        "A dangling `satisfies:` is a warning, not an error, so the field can hold an external reference (a ticket id) that the engine cannot resolve — at the cost of not catching a typo in an internal id.",
        "Traceability is opt-in: a requirement with no `satisfies:` is never flagged, matching the tool's subtle-guidance philosophy."
      ],
      "current_impl": [
        "`cmd_check` (satisfies validation + need exemptions + orphan-need warn), `cmd_show` (upstream lines), and `_build_map_data` (`satisfies`/`satisfied_by` node fields + `upstream_edges`) in `reqmap.py`, plus `VALID_LAYER` gaining `need`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a requirement with `satisfies: [GHOST-X-999]` and no such requirement When   the gate runs Then   it warns about the dangling upstream trace and does NOT raise an error",
        "CASE-2 — Given  a confirmed `need` that no requirement satisfies When   the gate runs Then   it warns that the need is unaddressed",
        "CASE-3 — Given  a confirmed `layer: need` requirement with no implements or tested-by tag When   the gate runs Then   it raises no implements/tested-by finding for that need",
        "CASE-4 — Given  a requirement that satisfies a need When   `show` runs on either the requirement or the need Then   the upstream/satisfied-by relationship is printed",
        "CASE-5 — Given  a confirmed `layer: aggregate` requirement with dependencies and no implements tag When   the gate runs Then   it raises no implements finding for that requirement"
      ],
      "accept": "CASE-1\n  Given  a requirement with `satisfies: [GHOST-X-999]` and no such requirement\n  When   the gate runs\n  Then   it warns about the dangling upstream trace and does NOT raise an error\nCASE-2\n  Given  a confirmed `need` that no requirement satisfies\n  When   the gate runs\n  Then   it warns that the need is unaddressed\nCASE-3\n  Given  a confirmed `layer: need` requirement with no implements or tested-by tag\n  When   the gate runs\n  Then   it raises no implements/tested-by finding for that need\nCASE-4\n  Given  a requirement that satisfies a need\n  When   `show` runs on either the requirement or the need\n  Then   the upstream/satisfied-by relationship is printed\nCASE-5\n  Given  a confirmed `layer: aggregate` requirement with dependencies and no implements tag\n  When   the gate runs\n  Then   it raises no implements finding for that requirement",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001",
        "ARCH-CHECK-006"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-VMODEL-107"
      ],
      "satisfied_by": [
        "REQ-TRACE-934",
        "REQ-TRACE-935"
      ],
      "members": [
        {
          "role": "generated-from",
          "loc": "docs/full_architecture.html:4"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2329"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2532"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2542"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2644"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3972"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4023"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5069"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4052"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4469"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.17",
      "priority": "should-have",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Trasabilitate în amonte",
          "intent": "Un fișier de cerință îți spune ce face o capabilitate și ce cod o construiește, dar nu *de ce există* — cărei nevoi a unui stakeholder îi servește. Aceasta permite unei cerințe să indice în amonte printr-un câmp `satisfies:`, astfel încât poți trasa de la o nevoie a unui stakeholder în jos până la fiecare cerință care o îndeplinește, și înapoi în sus de la orice cerință la nevoia din spatele ei. Fără aceasta, cerințele plutesc libere de nevoile care le justifică, iar o nevoie poate rămâne neadresată în tăcere.",
          "contract": "> Un fișier de cerință îți spune ce face o capabilitate și ce cod o construiește, dar nu *de\n> ce există* — cărei nevoi a unui stakeholder îi servește. Aceasta permite unei cerințe să\n> indice în amonte printr-un câmp `satisfies:`, astfel încât poți trasa de la o nevoie a unui\n> stakeholder în jos până la fiecare cerință care o îndeplinește, și înapoi în sus de la orice\n> cerință la nevoia din spatele ei. Fără aceasta, cerințele plutesc libere de nevoile care le justifică, iar o nevoie poate rămâne neadresată în tăcere.\n\nFiecare punct de mai jos este obligatoriu.\n- O cerință poate declara o listă `satisfies:` în frontmatter care numește id-urile în amonte pe care le îndeplinește, iar gate avertizează (niciodată erori) la o legătură ruptă sau o nevoie confirmed neadresată. [[REQ-TRACE-934]]\n- Straturile `need` și `aggregate` sunt scutite de verificările implements/tested-by; fiecare este acoperit prin muchii — `satisfies:` sau `depends_on` — în locul codului. [[REQ-TRACE-935]]",
          "acceptance": "CASE-1\n  Given  o cerință cu `satisfies: [GHOST-X-999]` și nicio astfel de cerință nu există\n  When   rulează gate\n  Then   avertizează despre trasarea în amonte ruptă și NU ridică o eroare\nCASE-2\n  Given  o `need` confirmed pe care nicio cerință nu o satisface\n  When   rulează gate\n  Then   avertizează că nevoia este neadresată\nCASE-3\n  Given  o cerință confirmed `layer: need` fără nicio etichetă implements sau tested-by\n  When   rulează gate\n  Then   nu ridică nicio constatare implements/tested-by pentru acea need\nCASE-4\n  Given  o cerință care satisface o need\n  When   rulează `show` fie pe cerință, fie pe need\n  Then   relația în amonte/satisfăcut-de este afișată\nCASE-5\n  Given  o cerință confirmed `layer: aggregate` cu dependențe și fără etichetă implements\n  When   rulează gate\n  Then   nu ridică nicio constatare implements pentru acea cerință"
        }
      }
    },
    {
      "id": "REQ-TRACE-934",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Declaring and checking a satisfies: link",
      "intent": "`satisfies:` is the one field that lets a requirement point at the stakeholder need or higher-level requirement behind it, so the corpus can be read both downward (what fulfils this need) and upward (why this exists). A dangling link or an unaddressed need only warns — the upstream anchor may be authored later, or tracked outside this repo entirely.",
      "contract": [
        "A requirement may declare a `satisfies:` frontmatter list of the upstream ids it fulfils.",
        "The gate warns, and never errors, when a `satisfies:` id resolves to no requirement. An upstream anchor may be authored later, or tracked outside this repo.",
        "The gate warns when a confirmed `need` has no requirement satisfying it, so an unaddressed stakeholder need is visible."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — satisfies: parses into a frontmatter list like any other id field Given  a requirement file with `satisfies: [SYS-SSOT-001]` in its frontmatter When   `parse_frontmatter` reads the file Then   `meta[\"satisfies\"]` equals `[\"SYS-SSOT-001\"]`",
        "CASE-2 — — a dangling satisfies id warns without failing the gate Given  a requirement with `satisfies: [GHOST-X-999]` and no such requirement in the corpus When   `gate` runs Then   it prints \"satisfies GHOST-X-999 but no such requirement\" and exits 0",
        "CASE-3 — — an unaddressed confirmed need warns at the gate Given  a confirmed `layer: need` requirement no other requirement satisfies When   `gate` runs Then   it warns \"need has no requirement that satisfies it (upstream trace unaddressed)\" --------------------"
      ],
      "accept": "CASE-1 — satisfies: parses into a frontmatter list like any other id field\n  Given  a requirement file with `satisfies: [SYS-SSOT-001]` in its frontmatter\n  When   `parse_frontmatter` reads the file\n  Then   `meta[\"satisfies\"]` equals `[\"SYS-SSOT-001\"]`\n\nCASE-2 — a dangling satisfies id warns without failing the gate\n  Given  a requirement with `satisfies: [GHOST-X-999]` and no such requirement in the corpus\n  When   `gate` runs\n  Then   it prints \"satisfies GHOST-X-999 but no such requirement\" and exits 0\n\nCASE-3 — an unaddressed confirmed need warns at the gate\n  Given  a confirmed `layer: need` requirement no other requirement satisfies\n  When   `gate` runs\n  Then   it warns \"need has no requirement that satisfies it (upstream trace unaddressed)\"\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-TRACE-020"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2532"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2644"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4469"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Declararea și verificarea unei legături satisfies:",
          "intent": "`satisfies:` este singurul câmp care permite unei cerințe să indice nevoia stakeholder-ului sau cerința de nivel superior din spatele ei, astfel corpusul poate fi citit atât în jos (ce împlinește această nevoie) cât și în sus (de ce există aceasta). O legătură suspendată sau o nevoie neadresată doar avertizează — ancora din amonte poate fi redactată mai târziu, sau urmărită în afara acestui repo cu totul.",
          "contract": "> `satisfies:` este singurul câmp care permite unei cerințe să indice nevoia\n> stakeholder-ului sau cerința de nivel superior din spatele ei, astfel corpusul poate fi citit\n> atât în jos (ce împlinește această nevoie) cât și în sus (de ce există aceasta). O legătură\n> suspendată sau o nevoie neadresată doar avertizează — ancora din amonte poate fi redactată\n> mai târziu, sau urmărită în afara acestui repo cu totul.\n\nFiecare punct de mai jos este obligatoriu.\n- O cerință poate declara o listă frontmatter `satisfies:` a id-urilor din amonte pe care le împlinește.\n- Gate-ul avertizează, și nu eșuează niciodată, când un id `satisfies:` se rezolvă la nicio cerință. O\n  ancoră din amonte poate fi redactată mai târziu, sau urmărită în afara acestui repo.\n- Gate-ul avertizează când o `need` confirmată nu are nicio cerință care să o satisfacă, astfel o\n  nevoie a stakeholder-ului neadresată este vizibilă.",
          "acceptance": "CASE-1 — satisfies: se parsează într-o listă frontmatter ca orice alt câmp de id\n  Given  un fișier de cerință cu `satisfies: [SYS-SSOT-001]` în frontmatter\n  When   `parse_frontmatter` citește fișierul\n  Then   `meta[\"satisfies\"]` este egal cu `[\"SYS-SSOT-001\"]`\n\nCASE-2 — un id satisfies suspendat avertizează fără să eșueze gate-ul\n  Given  o cerință cu `satisfies: [GHOST-X-999]` și nicio astfel de cerință în corpus\n  When   `gate` rulează\n  Then   afișează \"satisfies GHOST-X-999 but no such requirement\" și iese cu 0\n\nCASE-3 — o need confirmată neadresată avertizează la gate\n  Given  o cerință confirmată `layer: need` pe care nicio altă cerință nu o satisface\n  When   `gate` rulează\n  Then   avertizează \"need has no requirement that satisfies it (upstream trace unaddressed)\"\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-TRACE-935",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "How need and aggregate layers are exempt from code checks",
      "intent": "A `need` is fulfilled by other requirements and an `aggregate` by its dependencies — neither is implemented by code of its own, so forcing an `implements:`/`tested-by:` tag on either would be a false requirement the gate could never satisfy. The exemption is edge-based, not a free pass: an aggregate with an empty `depends_on` is an orphan, not an aggregate, and a confirmed need still warns when nothing satisfies it.",
      "contract": [
        "The `aggregate` layer is exempt from the implements check. Its implementation is the implementation of the requirements it depends on.",
        "An aggregate declares at least one `depends_on` id. An aggregate with no dependency is an orphan, not an aggregate.",
        "An aggregate adds no behaviour of its own. It asserts that its dependencies work together.",
        "The `need` layer is exempt from the implements and tested-by checks. A need is satisfied by other requirements, not implemented or tested by code.",
        "A need is still expected to carry a `validated-against:` link once the repo adopts that role, because being satisfied by requirements is not evidence the need was met (see [[ARCH-VLEVEL-037]]).",
        "`show` prints the upstream ids a requirement satisfies and the requirements that satisfy it, but only when that requirement takes part in traceability.",
        "The map data carries `satisfies` and `satisfied_by` on each node, plus a list of upstream edges, so a front-end can draw the trace."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a confirmed aggregate with no implements tag passes the gate Given  a confirmed `layer: aggregate` requirement with `depends_on` a code-backed requirement, and no `implements:` tag of its own When   `gate` runs Then   `_link_sync_errors` reports no missing-implements error for the aggregate, because it adds no behaviour of its own — it only asserts its dependencies work together",
        "CASE-2 — — confirm refuses an aggregate with an empty depends_on Given  a `layer: aggregate` requirement with `depends_on: []` When   `confirm <ID>` runs Then   it refuses with \"depends_on is empty\" and leaves the file's status unchanged",
        "CASE-3 — — a confirmed need raises no implements/tested-by finding Given  a confirmed `layer: need` requirement with no `implements:` or `tested-by:` tag, satisfied by another requirement so the orphan-need warn stays silent When   `gate` runs Then   it raises no missing-implements error and no missing-tested-by warning for that need",
        "CASE-4 — — a confirmed need with no validated-against tag warns once the repo opts in Given  a corpus where at least one member carries a `validated-against:` tag, and a confirmed `layer: need` requirement with no `validated-against:` member of its own When   `gate` runs Then   it warns \"confirmed need with no `validated-against:` tag\"",
        "CASE-5 — — show prints the trace in both directions Given  a need `NEED-X-001` and a feature `A-FOO-001` with `satisfies: [NEED-X-001]` When   `show A-FOO-001` and `show NEED-X-001` each run Then   the feature's output contains \"Satisfies (upstream): NEED-X-001\" and the need's output contains \"Satisfied by: A-FOO-001\"",
        "CASE-6 — — map data exposes satisfies, satisfied_by and upstream_edges Given  a need `NEED-X-001` and a feature `A-FOO-001` with `satisfies: [NEED-X-001]` When   `_build_map_data` builds the node graph Then   the feature node's `satisfies` is `[\"NEED-X-001\"]`, the need node's `satisfied_by` is `[\"A-FOO-001\"]`, and `[\"A-FOO-001\", \"NEED-X-001\"]` appears in `upstream_edges`"
      ],
      "accept": "CASE-1 — a confirmed aggregate with no implements tag passes the gate\n  Given  a confirmed `layer: aggregate` requirement with `depends_on` a code-backed requirement,\n         and no `implements:` tag of its own\n  When   `gate` runs\n  Then   `_link_sync_errors` reports no missing-implements error for the aggregate, because\n         it adds no behaviour of its own — it only asserts its dependencies work together\n\nCASE-2 — confirm refuses an aggregate with an empty depends_on\n  Given  a `layer: aggregate` requirement with `depends_on: []`\n  When   `confirm <ID>` runs\n  Then   it refuses with \"depends_on is empty\" and leaves the file's status unchanged\n\nCASE-3 — a confirmed need raises no implements/tested-by finding\n  Given  a confirmed `layer: need` requirement with no `implements:` or `tested-by:` tag,\n         satisfied by another requirement so the orphan-need warn stays silent\n  When   `gate` runs\n  Then   it raises no missing-implements error and no missing-tested-by warning for that need\n\nCASE-4 — a confirmed need with no validated-against tag warns once the repo opts in\n  Given  a corpus where at least one member carries a `validated-against:` tag, and a\n         confirmed `layer: need` requirement with no `validated-against:` member of its own\n  When   `gate` runs\n  Then   it warns \"confirmed need with no `validated-against:` tag\"\n\nCASE-5 — show prints the trace in both directions\n  Given  a need `NEED-X-001` and a feature `A-FOO-001` with `satisfies: [NEED-X-001]`\n  When   `show A-FOO-001` and `show NEED-X-001` each run\n  Then   the feature's output contains \"Satisfies (upstream): NEED-X-001\" and the need's\n         output contains \"Satisfied by: A-FOO-001\"\n\nCASE-6 — map data exposes satisfies, satisfied_by and upstream_edges\n  Given  a need `NEED-X-001` and a feature `A-FOO-001` with `satisfies: [NEED-X-001]`\n  When   `_build_map_data` builds the node graph\n  Then   the feature node's `satisfies` is `[\"NEED-X-001\"]`, the need node's `satisfied_by`\n         is `[\"A-FOO-001\"]`, and `[\"A-FOO-001\", \"NEED-X-001\"]` appears in `upstream_edges`",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-TRACE-020"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2329"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:3960"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5033"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4052"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:4469"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 5,
      "gap": "no `verifies:` tag for CASE-2",
      "i18n": {
        "ro": {
          "title": "Cum sunt exceptate layer-urile need și aggregate de la verificările de cod",
          "intent": "O `need` este împlinită de alte cerințe, iar un `aggregate` de dependențele lui — niciuna nu este implementată de propriul ei cod, astfel forțarea unui tag `implements:`/`tested-by:` pe oricare dintre ele ar fi o cerință falsă pe care gate-ul nu ar putea-o satisface niciodată. Exceptarea este bazată pe muchii, nu un liber-arbitru: un aggregate cu un `depends_on` gol este un orfan, nu un aggregate, iar o need confirmată încă avertizează când nimic nu o satisface.",
          "contract": "> O `need` este împlinită de alte cerințe, iar un `aggregate` de dependențele lui — niciuna\n> nu este implementată de propriul ei cod, astfel forțarea unui tag `implements:`/`tested-by:`\n> pe oricare dintre ele ar fi o cerință falsă pe care gate-ul nu ar putea-o satisface niciodată.\n> Exceptarea este bazată pe muchii, nu un liber-arbitru: un aggregate cu un `depends_on` gol\n> este un orfan, nu un aggregate, iar o need confirmată încă avertizează când nimic nu o satisface.\n\nFiecare punct de mai jos este obligatoriu.\n- Layer-ul `aggregate` este exceptat de la verificarea implements. Implementarea lui este\n  implementarea cerințelor de care depinde.\n- Un aggregate declară cel puțin un id `depends_on`. Un aggregate fără nicio dependență este un\n  orfan, nu un aggregate.\n- Un aggregate nu adaugă niciun comportament al lui propriu. Afirmă că dependențele lui funcționează împreună.\n- Layer-ul `need` este exceptat de la verificările implements și tested-by. O need este satisfăcută de\n  alte cerințe, nu implementată sau testată de cod.\n- Se așteaptă în continuare ca o need să poarte o legătură `validated-against:` odată ce repo-ul adoptă acel\n  rol, pentru că a fi satisfăcută de cerințe nu este dovada că need-ul a fost îndeplinit\n  (vezi [[ARCH-VLEVEL-037]]).\n- `show` afișează id-urile din amonte pe care le satisface o cerință și cerințele care o satisfac,\n  dar numai când acea cerință ia parte la trasabilitate.\n- Datele de map poartă `satisfies` și `satisfied_by` pe fiecare nod, plus o listă de\n  muchii din amonte, astfel un front-end poate desena traseul.",
          "acceptance": "CASE-1 — un aggregate confirmat fără niciun tag implements trece gate-ul\n  Given  o cerință `layer: aggregate` confirmată cu `depends_on` o cerință susținută de cod,\n         și niciun tag `implements:` al ei\n  When   `gate` rulează\n  Then   `_link_sync_errors` nu raportează nicio eroare de implements lipsă pentru aggregate, pentru că\n         nu adaugă niciun comportament al lui — doar afirmă că dependențele lui funcționează împreună\n\nCASE-2 — confirm refuză un aggregate cu un depends_on gol\n  Given  o cerință `layer: aggregate` cu `depends_on: []`\n  When   `confirm <ID>` rulează\n  Then   refuză cu \"depends_on is empty\" și lasă status-ul fișierului neschimbat\n\nCASE-3 — o need confirmată nu ridică nicio constatare implements/tested-by\n  Given  o cerință `layer: need` confirmată fără niciun tag `implements:` sau `tested-by:`,\n         satisfăcută de o altă cerință astfel avertismentul de need-orfan rămâne tăcut\n  When   `gate` rulează\n  Then   nu ridică nicio eroare implements lipsă și niciun avertisment tested-by lipsă pentru acea need\n\nCASE-4 — o need confirmată fără niciun tag validated-against avertizează odată ce repo-ul optează\n  Given  un corpus unde cel puțin un membru poartă un tag `validated-against:`, și o\n         cerință `layer: need` confirmată fără niciun membru `validated-against:` al ei\n  When   `gate` rulează\n  Then   avertizează \"confirmed need with no `validated-against:` tag\"\n\nCASE-5 — show afișează traseul în ambele direcții\n  Given  o need `NEED-X-001` și o feature `A-FOO-001` cu `satisfies: [NEED-X-001]`\n  When   `show A-FOO-001` și `show NEED-X-001` rulează fiecare\n  Then   ieșirea feature-ului conține \"Satisfies (upstream): NEED-X-001\", iar ieșirea need-ului\n         conține \"Satisfied by: A-FOO-001\"\n\nCASE-6 — datele de map expun satisfies, satisfied_by și upstream_edges\n  Given  o need `NEED-X-001` și o feature `A-FOO-001` cu `satisfies: [NEED-X-001]`\n  When   `_build_map_data` construiește graful de noduri\n  Then   `satisfies`-ul nodului feature este `[\"NEED-X-001\"]`, `satisfied_by`-ul nodului need\n         este `[\"A-FOO-001\"]`, iar `[\"A-FOO-001\", \"NEED-X-001\"]` apare în `upstream_edges`"
        }
      }
    },
    {
      "id": "ARCH-TRACKED-042",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Untracked members reported",
      "intent": "A generated artifact this tool commits must depend only on files git tracks. When a member lives in a gitignored directory, the map records something a fresh checkout has no copy of — so the committed map cannot be regenerated anywhere but the machine that wrote it, and the failure lands in CI as a mysterious staleness error about a file the reader cannot find.",
      "contract": [
        "`untracked_members` lists the member files git does not track under the scan root, and the gate warns (never errors) when any exist. [[REQ-TRACKED-936]]"
      ],
      "verify": [],
      "notes": [
        "Untracked is the property that matters, not ignored: a file that is merely uncommitted breaks reproducibility exactly as a gitignored one does, and one `git ls-files` call answers both.",
        "Warn-only on purpose. A consumer repo may tag an ignored file deliberately; this nudges that choice into the open rather than overruling it.",
        "The check reads the index, so a member staged but not committed counts as tracked."
      ],
      "current_impl": [
        "`untracked_members` in `reqmap.py`, called from `cmd_check`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  a member file that git does not track When   `gate` runs inside a git work tree Then   it warns, naming that file and the count of untracked members, and exits 0",
        "CASE-2 — Given  every member file is tracked When   `gate` runs Then   no untracked-member warning appears",
        "CASE-3 — Given  a scan root that is not a git work tree When   `untracked_members` runs Then   it returns nothing at all, rather than reporting every member as untracked"
      ],
      "accept": "CASE-1\n  Given  a member file that git does not track\n  When   `gate` runs inside a git work tree\n  Then   it warns, naming that file and the count of untracked members, and exits 0\n\nCASE-2\n  Given  every member file is tracked\n  When   `gate` runs\n  Then   no untracked-member warning appears\n\nCASE-3\n  Given  a scan root that is not a git work tree\n  When   `untracked_members` runs\n  Then   it returns nothing at all, rather than reporting every member as untracked",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-SCAN-002",
        "ARCH-CHECK-006"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-GATE-102"
      ],
      "satisfied_by": [
        "REQ-TRACKED-936"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1458"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2770"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:5728"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Membri neurmăriți de git, raportați",
          "intent": "Un artefact generat pe care acest tool îl comite trebuie să depindă doar de fișiere pe care git le urmărește. Când un membru se află într-un director gitignored, harta înregistrează ceva de care un checkout proaspăt nu are nicio copie — deci harta comisă nu poate fi regenerată nicăieri altundeva decât pe mașina care a scris-o, iar eșecul ajunge în CI ca o eroare misterioasă de învechire despre un fișier pe care cititorul nu-l poate găsi.",
          "contract": "> Un artefact generat pe care acest tool îl comite trebuie să depindă doar de fișiere pe care\n> git le urmărește. Când un membru se află într-un director gitignored, harta înregistrează\n> ceva de care un checkout proaspăt nu are nicio copie — deci harta comisă nu poate fi\n> regenerată nicăieri altundeva decât pe mașina care a scris-o, iar eșecul ajunge în CI ca o eroare misterioasă de învechire despre un fișier pe care cititorul nu-l poate găsi.\n\nFiecare punct de mai jos este obligatoriu.\n- `untracked_members` listează fișierele membre pe care git nu le urmărește sub rădăcina de scanare, iar gate avertizează (niciodată erori) când există vreunul. [[REQ-TRACKED-936]]",
          "acceptance": "CASE-1\n  Given  un fișier membru pe care git nu îl urmărește\n  When   `gate` rulează într-un work tree git\n  Then   avertizează, numind acel fișier și numărul de membri neurmăriți, și iese cu 0\n\nCASE-2\n  Given  fiecare fișier membru este urmărit\n  When   rulează `gate`\n  Then   nu apare niciun avertisment de membru neurmărit\n\nCASE-3\n  Given  o rădăcină de scanare care nu este un work tree git\n  When   rulează `untracked_members`\n  Then   nu returnează absolut nimic, în loc să raporteze fiecare membru ca neurmărit"
        }
      }
    },
    {
      "id": "REQ-TRACKED-936",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Warning when a member is not tracked by git",
      "intent": "A committed generated artifact — the map — must depend only on files a fresh checkout also has. When a member lives in a gitignored or merely-uncommitted file, the local scan can see it but CI never will, and the failure shows up later as a mystifying staleness error about a file the reader cannot find. This check catches the real cause at the source, before the map is even regenerated.",
      "contract": [
        "`untracked_members` lists the member files git does not track under the scan root.",
        "`gate` reports those untracked members in one warning, naming up to five paths and the total count.",
        "The warning names the two remedies: commit the files, or exclude them in `.reqmapignore`.",
        "`untracked_members` reports nothing and the gate stays silent when the scan root is not a git work tree, or git is unavailable.",
        "The warning never changes the exit code."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — untracked_members names a git-untracked member file Given  a member file that git does not track under the scan root When   `untracked_members` runs Then   it returns that file's path in its result list",
        "CASE-2 — — the gate warning caps the named files and reports the total Given  seven untracked member files When   `gate` runs inside a git work tree Then   its warning names five of them by path and states the total count of 7, and `gate` exits 0 — the warning never changes the exit code",
        "CASE-3 — — the untracked warning names both fixes Given  a gate run reporting an untracked member When   the warning text is inspected Then   it names committing the file and adding it to `.reqmapignore` as the two remedies",
        "CASE-4 — — the check stays silent outside a git work tree Given  a scan root that is not a git repository When   `untracked_members` runs Then   it returns nothing and `gate` prints no untracked-member warning"
      ],
      "accept": "CASE-1 — untracked_members names a git-untracked member file\n  Given  a member file that git does not track under the scan root\n  When   `untracked_members` runs\n  Then   it returns that file's path in its result list\n\nCASE-2 — the gate warning caps the named files and reports the total\n  Given  seven untracked member files\n  When   `gate` runs inside a git work tree\n  Then   its warning names five of them by path and states the total count of 7, and\n         `gate` exits 0 — the warning never changes the exit code\n\nCASE-3 — the untracked warning names both fixes\n  Given  a gate run reporting an untracked member\n  When   the warning text is inspected\n  Then   it names committing the file and adding it to `.reqmapignore` as the two remedies\n\nCASE-4 — the check stays silent outside a git work tree\n  Given  a scan root that is not a git repository\n  When   `untracked_members` runs\n  Then   it returns nothing and `gate` prints no untracked-member warning",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-TRACKED-042"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1458"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:5728"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9959"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Avertizare când un membru nu este urmărit de git",
          "intent": "Un artefact generat commis — map-ul — trebuie să depindă doar de fișiere pe care le are și un checkout proaspăt. Când un membru trăiește într-un fișier gitignored sau doar-necommis, scanarea locală îl poate vedea, dar CI nu îl va vedea niciodată, iar eșecul apare mai târziu ca o eroare de învechire derutantă despre un fișier pe care cititorul nu îl poate găsi. Această verificare prinde cauza reală la sursă, înainte ca map-ul să fie măcar regenerat.",
          "contract": "> Un artefact generat commis — map-ul — trebuie să depindă doar de fișiere pe care le are și\n> un checkout proaspăt. Când un membru trăiește într-un fișier gitignored sau doar-necommis,\n> scanarea locală îl poate vedea, dar CI nu îl va vedea niciodată, iar eșecul apare mai\n> târziu ca o eroare de învechire derutantă despre un fișier pe care cititorul nu îl poate\n> găsi. Această verificare prinde cauza reală la sursă, înainte ca map-ul să fie măcar\n> regenerat.\n\nFiecare punct de mai jos este obligatoriu.\n- `untracked_members` listează fișierele de membri pe care git nu le urmărește sub rădăcina de scanare.\n- `gate` raportează acei membri neurmăriți într-un singur avertisment, numind până la cinci căi și numărul total.\n- Avertismentul numește cele două remedii: commite-ți fișierele, sau exclude-le în `.reqmapignore`.\n- `untracked_members` nu raportează nimic, iar gate-ul rămâne tăcut când rădăcina de scanare nu este\n  un arbore de lucru git, sau git este indisponibil.\n- Avertismentul nu schimbă niciodată codul de ieșire.",
          "acceptance": "CASE-1 — untracked_members numește un fișier membru neurmărit de git\n  Given  un fișier membru pe care git nu îl urmărește sub rădăcina de scanare\n  When   `untracked_members` rulează\n  Then   returnează calea acelui fișier în lista lui de rezultate\n\nCASE-2 — avertismentul de gate limitează fișierele numite și raportează totalul\n  Given  șapte fișiere membru neurmărite\n  When   `gate` rulează în interiorul unui arbore de lucru git\n  Then   avertismentul lui numește cinci dintre ele după cale și afirmă numărul total de 7, iar\n         `gate` iese cu 0 — avertismentul nu schimbă niciodată codul de ieșire\n\nCASE-3 — avertismentul de neurmărit numește ambele remedii\n  Given  o rulare de gate raportând un membru neurmărit\n  When   textul avertismentului este inspectat\n  Then   numește commiterea fișierului și adăugarea lui la `.reqmapignore` ca cele două remedii\n\nCASE-4 — verificarea rămâne tăcută în afara unui arbore de lucru git\n  Given  o rădăcină de scanare care nu este un repo git\n  When   `untracked_members` rulează\n  Then   nu returnează nimic, iar `gate` nu afișează niciun avertisment de membru neurmărit"
        }
      }
    },
    {
      "id": "ARCH-TRANSLATE-044",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Reading a cached requirement translation into the map",
      "intent": "`i18n.jsx` (ARCH-VIEWER-007) draws a hard line: the locale toggle translates UI chrome only, never requirement content — a requirement's title, intent, contract and acceptance criteria stay in the author's language, because they are the artifact under review and translating them live would put words in the author's mouth. That line held until a corpus is written entirely in one language and a reader of the other language cannot use the viewer at all. This capability adds the one exception, and only the reading half of it: a translation cache produced out of band is inlined into the graph, always rendered with a visible \"machine-translated, unreviewed\" marker, never presented as the authored source.",
      "contract": [
        "The engine READS `requirements/_i18n/<locale>.json` and never writes it. No subcommand spawns an external process, so `gate`, `sync` and the pre-commit hook stay exactly as deterministic and offline as they were before this capability existed. [[REQ-TRANSLATE-937]]",
        "A cached entry is served only while its stored hash still matches the requirement's current content; a stale or malformed cache degrades to no translation, never to a wrong one. [[REQ-TRANSLATE-938]]",
        "The gate reports a cached translation that carries a field the requirement itself does not emit. [[REQ-TRANSLATE-967]]"
      ],
      "verify": [],
      "notes": [
        "**The `translate` command was removed on 2026-09-05**, at the user's request, together with everything that produced the cache: the `claude -p` call, the language detection, the corpus-majority vote and the structural-fidelity check. What survives is this reading half, which is what puts Romanian into the viewer. The consequence is stated plainly rather than hidden: the cache now decays. Every requirement edited from here on loses its cached translation, silently and by design (CASE-3), and the engine has no way to produce a new one. Refreshing a translation is a manual step — ask Claude for it and write the entry into `_i18n/<locale>.json` in the shape this contract reads.",
        "`TRANSLATOR_VERSION` stays folded into the cache key. It is now the only lever that invalidates every cached entry at once, which matters more without a producer, not less.",
        "The cache is one aggregate `_i18n/<locale>.json` per locale, not one file per requirement — the same shape as `_reqlock.json`/`_memberlock.json` (ARCH-DRIFT-003), not a new pattern.",
        "The reader is deliberately mechanical: it compares a hash and serves or drops. It cannot tell a fluent translation from a semantically wrong one. That is exactly why the badge exists — cached is not reviewed.",
        "Decision (2026-08-25), still standing: the language set stays `ro`/`en`. The cache is per-locale, so adding a language later is additive.",
        "`file-spread` is exempted: the three files are the engine side plus the two viewer consumers named in WHERE (`i18n.jsx`, `SpecView.jsx`) — one feature, two runtimes.",
        "`ac-count-high` is no longer exempted, and no longer needs to be: the contract went from eight criteria to three when the producing half left."
      ],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a title-only edit invalidates the cached translation Given  two requirement bodies identical except for the `# ` title line When   `translation_hash` runs on each, and `binding_hash` runs on each Then   the `translation_hash` values differ while the `binding_hash` values stay equal — proof the wider span is necessary",
        "CASE-2 — — the map attaches a fresh cache entry without ever calling an external tool Given  a `requirements/_i18n/en.json` entry whose hash matches the requirement's current content When   `_build_map_data` + `_attach_translations` run with `subprocess.run` mocked to raise on any call Then   the matching node carries `node.i18n.en` and no subprocess was started",
        "CASE-3 — — a stale cache entry is dropped, never served Given  a cache entry whose stored hash does NOT match the requirement's current content, because the source was edited since the cache was produced When   `_load_translations` runs Then   that entry is absent from the result — never served stale"
      ],
      "accept": "CASE-1 — a title-only edit invalidates the cached translation\n  Given  two requirement bodies identical except for the `# ` title line\n  When   `translation_hash` runs on each, and `binding_hash` runs on each\n  Then   the `translation_hash` values differ while the `binding_hash` values stay\n         equal — proof the wider span is necessary\n\nCASE-2 — the map attaches a fresh cache entry without ever calling an external tool\n  Given  a `requirements/_i18n/en.json` entry whose hash matches the requirement's\n         current content\n  When   `_build_map_data` + `_attach_translations` run with `subprocess.run`\n         mocked to raise on any call\n  Then   the matching node carries `node.i18n.en` and no subprocess was started\n\nCASE-3 — a stale cache entry is dropped, never served\n  Given  a cache entry whose stored hash does NOT match the requirement's current\n         content, because the source was edited since the cache was produced\n  When   `_load_translations` runs\n  Then   that entry is absent from the result — never served stale",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-PARSE-001",
        "ARCH-MAP-007",
        "ARCH-VIEWER-007"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-SHIP-108"
      ],
      "satisfied_by": [
        "REQ-TRANSLATE-937",
        "REQ-TRANSLATE-938",
        "REQ-TRANSLATE-967"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "app/src/lib/i18n.jsx:2"
        },
        {
          "role": "implements",
          "loc": "app/src/views/SpecView.jsx:2"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2681"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4110"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4138"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4149"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4159"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4195"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3125"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10578"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3
    },
    {
      "id": "REQ-TRANSLATE-937",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The cache key, and the promise that nothing shells out",
      "intent": "The cache key is deliberately wider than `binding_hash`: a translation covers the title too, so a title-only edit must invalidate it even though the same edit would not trip drift. And with the producing command gone, the offline promise is no longer a policy anyone can break by typing the wrong verb — it is a property of the engine.",
      "contract": [
        "**No subcommand invokes an external process.** `gate`, `sync`, `new`, `init`, `confirm` and `clarify` read and write files only. The `translate` command, which was the single exception, was removed on 2026-09-05.",
        "The cache key is `translation_hash(body, title)` — a hash over title + WHY + Contract + Acceptance, distinct from `binding_hash()` (Contract+Acceptance only, ARCH-DRIFT-003). A title-only edit also invalidates a cached translation, which reusing `binding_hash` would miss.",
        "`TRANSLATOR_VERSION` is folded into the key, so bumping it invalidates every cached entry in one step rather than file by file."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — no command spawns an external process Given  `subprocess.run` mocked to raise if it is invoked at all When   `gate`, `sync` and `map` run Then   none of them invoke `subprocess.run`",
        "CASE-2 — — a title-only edit invalidates the cached translation Given  two bodies identical except for the `# ` title line When   `translation_hash` runs on each, and `binding_hash` runs on each Then   the `translation_hash` values differ while the `binding_hash` values stay equal --------------------"
      ],
      "accept": "CASE-1 — no command spawns an external process\n  Given  `subprocess.run` mocked to raise if it is invoked at all\n  When   `gate`, `sync` and `map` run\n  Then   none of them invoke `subprocess.run`\n\nCASE-2 — a title-only edit invalidates the cached translation\n  Given  two bodies identical except for the `# ` title line\n  When   `translation_hash` runs on each, and `binding_hash` runs on each\n  Then   the `translation_hash` values differ while the `binding_hash` values stay\n         equal\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-TRANSLATE-044"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4149"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3125"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 2,
      "covered": 2
    },
    {
      "id": "REQ-TRANSLATE-938",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Reading the cache: fresh only, and failing open",
      "intent": "A cache read has two ways to go wrong and only one acceptable answer to both. An entry can be out of date, or the file can be unreadable. In either case the requirement renders in the author's own language, exactly as it did before this capability existed — never a stale translation, never a crash.",
      "contract": [
        "`map` and `export` read `requirements/_i18n/*.json` when present and attach `node.i18n[locale] = {title, intent, contract, acceptance}` for any node whose cached hash still matches its current content.",
        "**A stale entry is silently dropped, never served.** This is what keeps `map --check` deterministic: it only ever reads a file already on disk, and it never serves a translation known to be out of date.",
        "A malformed cache file — unreadable, not JSON, or not an object — yields no translations at all rather than an exception. The map still builds.",
        "The viewer consumes `node.i18n` ONLY through `translatedText()` (`i18n.jsx`), which reports `isTranslated` alongside the text. Every caller that renders `isTranslated` text renders the \"machine-translated, unreviewed\" badge next to it. Absent a cache entry, content renders in the author's own language exactly as before this capability existed."
      ],
      "verify": [],
      "notes": [
        "`lint_exempt: file-spread` — the cache is written out of band and read by the engine and the viewer, so this contract is the handshake across that boundary. Both halves have to be named for the obligation to mean anything.",
        "-------------------"
      ],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the map attaches a fresh cache entry without calling anything external Given  a `requirements/_i18n/en.json` entry whose hash matches the requirement's current content When   `_build_map_data` runs with `subprocess.run` mocked to raise on any call Then   the node carries `node.i18n.en` and no external call was made",
        "CASE-2 — — a stale cache entry is dropped, never served Given  a `requirements/_i18n/en.json` entry whose stored hash does not match the requirement's current content When   `_load_translations` runs Then   that entry is absent from the result, so `map`/`export` never attach it to the node"
      ],
      "accept": "CASE-1 — the map attaches a fresh cache entry without calling anything external\n  Given  a `requirements/_i18n/en.json` entry whose hash matches the requirement's\n         current content\n  When   `_build_map_data` runs with `subprocess.run` mocked to raise on any call\n  Then   the node carries `node.i18n.en` and no external call was made\n\nCASE-2 — a stale cache entry is dropped, never served\n  Given  a `requirements/_i18n/en.json` entry whose stored hash does not match the\n         requirement's current content\n  When   `_load_translations` runs\n  Then   that entry is absent from the result, so `map`/`export` never attach it to\n         the node",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-TRANSLATE-044"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "tested-by",
          "loc": "app/scripts/ssr-smoke.jsx:1"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/i18n.jsx:206"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/loadData.js:68"
        },
        {
          "role": "implements",
          "loc": "app/src/views/SpecView.jsx:14"
        },
        {
          "role": "implements",
          "loc": "app/src/views/SpecView.jsx:80"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4159"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:4195"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3125"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 2,
      "covered": 2
    },
    {
      "id": "REQ-TRANSLATE-967",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "A translation may not carry a field the requirement does not",
      "intent": "`translate` and the map both derive from the same requirement, and each was right about it: the map emits no intent when the quote IS the obligation, while the translator was handed the raw quote. Nothing compared the two, so a translated document showed a `Why — Intent` section the untranslated one hides — on eight requirements, invisible until a corpus carried both features at once. Two correct parts, wrong together, is a shape a gate can catch and a test of either part alone cannot.",
      "contract": [
        "The gate warns when a cached translation carries a field whose source in the requirement is empty, naming the requirement, the locale and the field.",
        "A field the requirement has and the translation does not is never reported: a partial or in-progress translation is a normal state, not a defect.",
        "The check reads the caches already on disk and calls nothing external, so it costs a file read and stays as deterministic as the rest of the gate.",
        "A repository with no translation cache raises nothing at all."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a translated field the requirement does not emit is reported Given  a requirement whose quote is its only clause, so the map emits no intent, and a cache entry for it carrying an `intent` When   `gate` runs Then   it warns once, naming that requirement, the locale and `intent`",
        "CASE-2 — — a field the translation has not reached yet is not a finding Given  a requirement with an intent and a cache entry whose `intent` is empty When   `gate` runs Then   no warning is raised for it",
        "CASE-3 — — a corpus with no cache raises nothing Given  a requirements directory with no `_i18n` at all When   `gate` runs Then   the check contributes no finding"
      ],
      "accept": "CASE-1 — a translated field the requirement does not emit is reported\n  Given  a requirement whose quote is its only clause, so the map emits no intent, and a cache\n         entry for it carrying an `intent`\n  When   `gate` runs\n  Then   it warns once, naming that requirement, the locale and `intent`\n\nCASE-2 — a field the translation has not reached yet is not a finding\n  Given  a requirement with an intent and a cache entry whose `intent` is empty\n  When   `gate` runs\n  Then   no warning is raised for it\n\nCASE-3 — a corpus with no cache raises nothing\n  Given  a requirements directory with no `_i18n` at all\n  When   `gate` runs\n  Then   the check contributes no finding",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-TRANSLATE-044"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2681"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10578"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "O traducere nu poate purta un câmp pe care cerința nu îl are",
          "intent": "`translate` și harta derivă amândouă din aceeași cerință și fiecare avea dreptate despre ea: harta nu emite niciun intent atunci când citatul ESTE obligația, în timp ce traducătorului i s-a dat citatul brut. Nimic nu compara cele două, așa că un document tradus arăta o secțiune `Why — Intent` pe care cel netradus o ascunde — pe opt cerințe, invizibil până când un corpus a purtat ambele funcționalități deodată. Două părți corecte, greșite împreună, este o formă pe care o poartă poate să o prindă și pe care un test al oricăreia dintre părți luată singură nu o poate prinde.",
          "contract": "> `translate` și harta derivă amândouă din aceeași cerință și fiecare avea dreptate despre ea:\n> harta nu emite niciun intent atunci când citatul ESTE obligația, în timp ce traducătorului i s-a\n> dat citatul brut. Nimic nu compara cele două, așa că un document tradus arăta o secțiune\n> `Why — Intent` pe care cel netradus o ascunde — pe opt cerințe, invizibil până când un corpus a\n> purtat ambele funcționalități deodată. Două părți corecte, greșite împreună, este o formă pe\n> care o poartă poate să o prindă și pe care un test al oricăreia dintre părți luată singură nu o\n> poate prinde.\n\nFiecare punct de mai jos este obligatoriu.\n- Poarta avertizează atunci când o traducere din cache poartă un câmp a cărui sursă în cerință\n  este goală, numind cerința, localizarea și câmpul.\n- Un câmp pe care cerința îl are și traducerea nu îl are nu este raportat niciodată: o traducere\n  parțială sau în curs este o stare normală, nu un defect.\n- Verificarea citește cache-urile deja aflate pe disc și nu apelează nimic extern, așa că are\n  costul unei citiri de fișier și rămâne la fel de deterministă ca restul porții.\n- Un repository fără niciun cache de traducere nu ridică absolut nimic.",
          "acceptance": "CASE-1 — un câmp tradus pe care cerința nu îl emite este raportat\n  Given  o cerință al cărei citat este singura ei clauză, astfel încât harta nu emite niciun\n         intent, și o intrare de cache pentru ea care poartă un `intent`\n  When   `gate` rulează\n  Then   avertizează o dată, numind acea cerință, localizarea și `intent`\n\nCASE-2 — un câmp la care traducerea nu a ajuns încă nu este o constatare\n  Given  o cerință cu un intent și o intrare de cache al cărei `intent` este gol\n  When   `gate` rulează\n  Then   nu se ridică nicio avertizare pentru ea\n\nCASE-3 — un corpus fără cache nu ridică nimic\n  Given  un director de cerințe fără niciun `_i18n`\n  When   `gate` rulează\n  Then   verificarea nu contribuie cu nicio constatare"
        }
      }
    },
    {
      "id": "ARCH-UNSCANNEDTAG-045",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Tags in unscanned file types reported",
      "intent": "A membership tag only counts when the scan reads the file it sits in. The scan reads a fixed list of extensions and basenames, so a tag in any other kind of file is silently invisible: the author believes the requirement has a member, the map says it has none, and nothing points at the gap. The first consumer run found two such files on the first try — a `Caddyfile` and a Prisma schema, both tagged, neither ever read.",
      "contract": [
        "`tagged_unscanned_files` lists the tracked, non-scannable files under the scan root that contain a membership tag, and the gate warns (never errors) when any exist. [[REQ-UNSCANNEDTAG-939]]"
      ],
      "verify": [],
      "notes": [
        "Tracked, not \"every file on disk\": the walk that discovers members is bounded by the extension list on purpose (a 135 MB checkout with 40k `node_modules` files must stay fast). One `git ls-files` call bounds this check the same way `ARCH-TRACKED-042` is bounded, and the two share the fail-open convention.",
        "The size cap and the UTF-8 requirement are what keep a stray lockfile or an image from costing anything: a tag can only live in text a human wrote.",
        "The remedy is deliberately \"ask for the type\": a consumer's vendored engine is replaced on update, so a local edit to `CODE_EXTS` would not survive. The evidence run that produced this requirement also added `.prisma`, `.graphql`, `.proto`, `Caddyfile`, `Jenkinsfile`, `Procfile` and `Vagrantfile` to the scan."
      ],
      "current_impl": [
        "`tagged_unscanned_files()` in `reqmap.py`, wired into `cmd_check` beside `untracked_members()`."
      ],
      "acc": [
        "CASE-1 — Given  a tracked file of a type the scan never reads, carrying `# implements: <ID>` When   `gate` runs inside a git work tree Then   it warns, naming that file and the count, and exits 0",
        "CASE-2 — Given  a tracked non-scannable file with no tag, a binary file, and a `_`-prefixed file whose text mentions a tag When   `tagged_unscanned_files` runs Then   none of them is listed",
        "CASE-3 — Given  a scan root that is not a git work tree When   `tagged_unscanned_files` runs Then   it returns None and `gate` prints no such warning"
      ],
      "accept": "CASE-1\n  Given  a tracked file of a type the scan never reads, carrying `# implements: <ID>`\n  When   `gate` runs inside a git work tree\n  Then   it warns, naming that file and the count, and exits 0\n\nCASE-2\n  Given  a tracked non-scannable file with no tag, a binary file, and a `_`-prefixed file\n         whose text mentions a tag\n  When   `tagged_unscanned_files` runs\n  Then   none of them is listed\n\nCASE-3\n  Given  a scan root that is not a git work tree\n  When   `tagged_unscanned_files` runs\n  Then   it returns None and `gate` prints no such warning",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-SCAN-002",
        "ARCH-CHECK-006"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-GATE-102"
      ],
      "satisfied_by": [
        "REQ-UNSCANNEDTAG-939"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1501"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2782"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6768"
        }
      ],
      "test_exempt": null,
      "milestone": "v2.9",
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Etichete în tipuri de fișiere nescanate, raportate",
          "intent": "O etichetă de membru contează doar când scanarea citește fișierul în care se află. Scanarea citește o listă fixă de extensii și nume de bază, deci o etichetă în orice alt tip de fișier este invizibilă în tăcere: autorul crede că cerința are un membru, harta spune că nu are niciunul, și nimic nu semnalează golul. Prima rulare a unui consumator a găsit două astfel de fișiere din prima încercare — un `Caddyfile` și o schemă Prisma, ambele etichetate, niciodată citite.",
          "contract": "> O etichetă de membru contează doar când scanarea citește fișierul în care se află.\n> Scanarea citește o listă fixă de extensii și nume de bază, deci o etichetă în orice alt\n> tip de fișier este invizibilă în tăcere: autorul crede că cerința are un membru, harta\n> spune că nu are niciunul, și nimic nu semnalează golul. Prima rulare a unui consumator a\n> găsit două astfel de fișiere din prima încercare — un `Caddyfile` și o schemă Prisma, ambele etichetate, niciodată citite.\n\nFiecare punct de mai jos este obligatoriu.\n- `tagged_unscanned_files` listează fișierele urmărite, nescanabile, sub rădăcina de scanare care conțin o etichetă de membru, iar gate avertizează (niciodată erori) când există vreunul. [[REQ-UNSCANNEDTAG-939]]",
          "acceptance": "CASE-1\n  Given  un fișier urmărit de un tip pe care scanarea nu îl citește niciodată, purtând `# implements: <ID>`\n  When   `gate` rulează într-un work tree git\n  Then   avertizează, numind acel fișier și numărul, și iese cu 0\n\nCASE-2\n  Given  un fișier urmărit, nescanabil, fără etichetă, un fișier binar, și un fișier cu prefix `_`\n         al cărui text menționează o etichetă\n  When   rulează `tagged_unscanned_files`\n  Then   niciunul dintre ele nu este listat\n\nCASE-3\n  Given  o rădăcină de scanare care nu este un work tree git\n  When   rulează `tagged_unscanned_files`\n  Then   returnează None și `gate` nu afișează niciun astfel de avertisment"
        }
      }
    },
    {
      "id": "REQ-UNSCANNEDTAG-939",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Warning about a tag the scan never reads",
      "intent": "The scan only reads a fixed list of extensions and basenames, so a membership tag in any other file type — a Caddyfile, a Prisma schema — is silently invisible: the author believes the requirement has a member, the map says it has none, and nothing points at the gap. This check names exactly those files so the mismatch is not discovered by accident.",
      "contract": [
        "`tagged_unscanned_files` lists the tracked, non-scannable files under the scan root that contain a membership tag.",
        "`gate` reports those unscanned-type files in one warning, naming up to five paths and the total count.",
        "The warning states that those files are not members, and names the remedy: move the tag into a scannable file, or ask for the file type to be added to the scan.",
        "The check skips paths matching `.reqmapignore`, files under the SSOT directory, files whose basename starts with `_`, `.git` or `.reqmap` (their comments quote tags as examples), and files larger than one megabyte. Any other dotfile, such as `.env`, is checked like any file.",
        "A file that is not valid UTF-8 text is skipped. Binary files never produce a warning.",
        "The check reports nothing and the gate stays silent when the scan root is not a git work tree, or git is unavailable.",
        "The warning never changes the exit code."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a tagged Caddyfile is listed as unscannable Given  a tracked `Caddyfile` containing `# implements: ARCH-EXAMPLE-001` When   `tagged_unscanned_files` runs Then   that file appears in its returned list",
        "CASE-2 — — the gate warning names up to five files and the total count Given  seven tracked non-scannable files each carrying a tag When   `gate` runs Then   it prints one warning naming five of them and stating the total of seven, and the exit code is unchanged",
        "CASE-3 — — the warning states the remedy, not just the file names Given  one tracked non-scannable file carrying a tag When   `gate` runs Then   the warning states the file counts as no member and names both remedies: move the tag, or ask for the type",
        "CASE-4 — — ignored, SSOT, underscore-prefixed and oversized files are skipped Given  a file matched by `.reqmapignore`, a file under `requirements/`, a `_scratch.txt` file, and a 2 MB file, each carrying a tag When   `tagged_unscanned_files` runs Then   none of the four appears in its returned list",
        "CASE-5 — — a non-UTF-8 file is skipped without warning Given  a tracked non-scannable binary file whose bytes are not valid UTF-8, containing a tag-like string When   `tagged_unscanned_files` runs Then   that file is absent from the returned list",
        "CASE-6 — — outside a git work tree, the check stays silent Given  a scan root that is not a git repository When   `gate` runs Then   it prints no unscanned-tag warning at all"
      ],
      "accept": "CASE-1 — a tagged Caddyfile is listed as unscannable\n  Given  a tracked `Caddyfile` containing `# implements: ARCH-EXAMPLE-001`\n  When   `tagged_unscanned_files` runs\n  Then   that file appears in its returned list\n\nCASE-2 — the gate warning names up to five files and the total count\n  Given  seven tracked non-scannable files each carrying a tag\n  When   `gate` runs\n  Then   it prints one warning naming five of them and stating the total of seven, and the\n         exit code is unchanged\n\nCASE-3 — the warning states the remedy, not just the file names\n  Given  one tracked non-scannable file carrying a tag\n  When   `gate` runs\n  Then   the warning states the file counts as no member and names both remedies: move the tag, or ask for the type\n\nCASE-4 — ignored, SSOT, underscore-prefixed and oversized files are skipped\n  Given  a file matched by `.reqmapignore`, a file under `requirements/`, a `_scratch.txt` file, and a 2 MB file, each carrying a tag\n  When   `tagged_unscanned_files` runs\n  Then   none of the four appears in its returned list\n\nCASE-5 — a non-UTF-8 file is skipped without warning\n  Given  a tracked non-scannable binary file whose bytes are not valid UTF-8, containing a tag-like string\n  When   `tagged_unscanned_files` runs\n  Then   that file is absent from the returned list\n\nCASE-6 — outside a git work tree, the check stays silent\n  Given  a scan root that is not a git repository\n  When   `gate` runs\n  Then   it prints no unscanned-tag warning at all",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-UNSCANNEDTAG-045"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1501"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6768"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9897"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Avertizare despre un tag pe care scanarea nu îl citește niciodată",
          "intent": "Scanarea citește doar o listă fixă de extensii și nume de bază, astfel un tag de apartenență în orice alt tip de fișier — un Caddyfile, o schemă Prisma — este invizibil în tăcere: autorul crede că cerința are un membru, map-ul spune că nu are niciunul, și nimic nu semnalează golul. Această verificare numește exact acele fișiere astfel încât nepotrivirea nu este descoperită din întâmplare.",
          "contract": "> Scanarea citește doar o listă fixă de extensii și nume de bază, astfel un tag de\n> apartenență în orice alt tip de fișier — un Caddyfile, o schemă Prisma — este invizibil în\n> tăcere: autorul crede că cerința are un membru, map-ul spune că nu are niciunul, și nimic\n> nu semnalează golul. Această verificare numește exact acele fișiere astfel încât\n> nepotrivirea nu este descoperită din întâmplare.\n\nFiecare punct de mai jos este obligatoriu.\n- `tagged_unscanned_files` listează fișierele urmărite, non-scanabile, sub rădăcina de scanare care\n  conțin un tag de apartenență.\n- `gate` raportează acele fișiere de tip nescanat într-un singur avertisment, numind până la cinci căi și numărul total.\n- Avertismentul afirmă că acele fișiere nu sunt membri, și numește remediul: mută tag-ul\n  într-un fișier scanabil, sau cere ca acel tip de fișier să fie adăugat la scanare.\n- Verificarea omite căile care se potrivesc cu `.reqmapignore`, fișierele sub directorul SSOT, fișierele\n  al căror nume de bază începe cu `_`, `.git` sau `.reqmap` (comentariile lor citează tag-uri ca\n  exemple), și fișierele mai mari de un megabyte. Orice alt fișier dotfile, precum `.env`, este\n  verificat ca orice fișier.\n- Un fișier care nu este text UTF-8 valid este omis. Fișierele binare nu produc niciodată un avertisment.\n- Verificarea nu raportează nimic, iar gate-ul rămâne tăcut când rădăcina de scanare nu este un arbore de lucru\n  git, sau git este indisponibil.\n- Avertismentul nu schimbă niciodată codul de ieșire.",
          "acceptance": "CASE-1 — un Caddyfile etichetat este listat ca non-scanabil\n  Given  un `Caddyfile` urmărit conținând `# implements: ARCH-EXAMPLE-001`\n  When   `tagged_unscanned_files` rulează\n  Then   acel fișier apare în lista lui returnată\n\nCASE-2 — avertismentul de gate numește până la cinci fișiere și numărul total\n  Given  șapte fișiere urmărite non-scanabile fiecare purtând un tag\n  When   `gate` rulează\n  Then   afișează un avertisment numind cinci dintre ele și afirmând totalul de șapte, iar\n         codul de ieșire este neschimbat\n\nCASE-3 — avertismentul afirmă remediul, nu doar numele fișierelor\n  Given  un fișier urmărit non-scanabil purtând un tag\n  When   `gate` rulează\n  Then   avertismentul afirmă că fișierul contează ca fără membru și numește ambele remedii: mută tag-ul, sau cere tipul\n\nCASE-4 — fișierele ignorate, SSOT, cu prefix underscore și supradimensionate sunt omise\n  Given  un fișier potrivit de `.reqmapignore`, un fișier sub `requirements/`, un fișier `_scratch.txt`, și un fișier de 2 MB, fiecare purtând un tag\n  When   `tagged_unscanned_files` rulează\n  Then   niciunul dintre cele patru nu apare în lista lui returnată\n\nCASE-5 — un fișier non-UTF-8 este omis fără avertisment\n  Given  un fișier binar urmărit non-scanabil ale cărui octeți nu sunt UTF-8 valid, conținând un șir asemănător unui tag\n  When   `tagged_unscanned_files` rulează\n  Then   acel fișier este absent din lista returnată\n\nCASE-6 — în afara unui arbore de lucru git, verificarea rămâne tăcută\n  Given  o rădăcină de scanare care nu este un repo git\n  When   `gate` rulează\n  Then   nu afișează niciun avertisment de tag nescanat deloc"
        }
      }
    },
    {
      "id": "ARCH-VIEWER-007",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Self-contained HTML map viewer",
      "intent": "The Mermaid diagrams render on GitHub, but a richer interactive view needs a browser app. This inlines the map's JSON graph into a single self-contained HTML file you open by double-click — no server, no install. It is optional: absent the vendored template, the engine still emits the diagrams and the JSON.",
      "contract": [
        "`map` generates `_map.html` when the template `_map_viewer.html` is vendored beside the engine, and skips it (no crash) when the template is absent. [[REQ-VIEWER-940]]",
        "`render_html` makes the injected graph HTML-safe for embedding inside `<script>` by applying three escapes in order, each a V8 no-op that reads the same after parsing. [[REQ-VIEWER-941]]",
        "The viewer ranks nodes by longest dependency path so `depends_on` edges flow one way, and renders a node's acceptance criteria as the author wrote them, not folded to one line. [[REQ-VIEWER-942]]",
        "The viewer renders its own UI chrome in a chosen language while requirement content and engine vocabulary stay exactly as authored. [[REQ-VIEWER-943]]",
        "The viewer turns a requirement's `[[ID]]` cross-references into navigation, and states only header fields the export actually carries. [[REQ-VIEWER-944]]",
        "The viewer's registry tally is the control that scopes its outline, and the scope it applies is always visible and clearable. [[REQ-VIEWER-945]]",
        "The viewer documents the engine's own commands, in the reader's language, from the list the map carries. [[REQ-VIEWER-964]]",
        "The viewer shows every open signal in one inbox, keeping what a human asked distinguishable from what the engine derived. [[REQ-VIEWER-966]]",
        "The viewer shows the engine's health and design readings as two rings in the rail, displaying the numbers it was given rather than computing its own. [[REQ-VIEWER-969]]",
        "The viewer lists the engine's code-review candidates in a tab of their own, kept out of the count of what is open about the corpus. [[REQ-VIEWER-977]]"
      ],
      "verify": [],
      "notes": [
        "`lint_exempt: file-spread`: the members are one engine function plus the viewer's source tree (`app/src/**`, its vendoring script and single-file build config). A UI is many files by construction; they are built into ONE artifact, so the spread is not diffuseness.",
        "`lint_exempt: [ac-count-high]`: the 8 criteria cover four independent surfaces of one artifact — data injection and its three escapes, node placement, UI language, and how the acceptance block is rendered. Splitting them would produce requirements that all point at the same single vendored file, which is the coupling the count is meant to detect, not a fix for it.",
        "The single-file build (`app/` → `npm run build:viewer`) is vendored beside the engine as `scripts/_map_viewer.html` with a `<!--REQMAP_DATA-->` marker; the stdlib engine swaps the marker for the inline data, so it ships a rich UI without itself depending on Node/npm.",
        "`_map.html` is a regenerable artifact (template + `_map.json`), not committed; rebuild with `map`. `_map.json` (owned by [[ARCH-MAP-007]]) is the committed source of its data.",
        "Publishing this viewer to a repo's GitHub Pages folder and gating that copy is a separate capability — see [[ARCH-PAGES-021]]."
      ],
      "current_impl": [
        "`render_html`, `_inject_viewer`, `_viewer_template_path` in `reqmap.py`; `render_html` is called by `cmd_map` after `_map.json`/`_map.md` are written.",
        "`check_viewer_data_sync` (+ `_vds_*` helpers) in `reqmap.py`, called from `cmd_check` (`gate`): a warn-only heuristic comparing `app/src/lib/data.js`'s hand-authored `BAKED` fallback fixture against the live registry, so a stale demo entry is flagged rather than silently shown forever. A fixture entry marked `demoOnly:true` is skipped: the demo dataset deliberately invents an orphan and a deprecated capability so the Risk and Problems tabs have signals with no engine present, and those ids cannot exist in any registry. An id left unmarked and absent from the registry is still reported — that is a requirement renamed out from under the fixture.",
        "The locale dictionary and provider live in `app/src/lib/i18n.jsx`; the toggle is part of the top bar in `app/src/App.jsx`. Both are outside the plugin scan root, like the rest of the app, so the SSR smoke (`npm run smoke`) is what holds them — it asserts both directions: that a header translates, and that requirement content and engine vocabulary do not.",
        "The Vite+React source lives in `app/src/views/` (repo root, outside the plugin scan root):",
        "`MapView.jsx` — force-graph rendering of the requirement graph",
        "`ProblemsView.jsx` — gate errors, drift items, and open risk inbox",
        "`RoadmapView.jsx` — milestone Gantt built from requirement `milestone:` fields",
        "`SpecView.jsx` — full requirement dossier (contract, ACs, members, deps)",
        "`app/src/lib/data.js` — loads `window.__REQMAP_DATA__` into the views Every file under `app/src/` carries an `implements:` tag (this requirement for the shell, views, styles and data loading; `ARCH-SEARCH-036` for `lib/search.js`; `ARCH-TRANSLATE-044` alongside on `lib/i18n.jsx` and `views/SpecView.jsx`), as do `app/scripts/install-viewer.mjs` and `app/vite.viewer.config.js` — the vendoring step and the single-file build are part of this contract. `app/scripts/ssr-smoke.jsx` is the `tested-by` member; `npm run smoke` (app/CLAUDE.md) runs it in CI's `artifacts` job, which also rebuilds the vendored viewer and fails when the committed copy differs. The build strips the comments, so the tags never reach `_map_viewer.html`; only `app/dist*`, `app/.vite` and the SSR bundle stay in `.reqmapignore`.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  the vendored `_map_viewer.html` template is present When   `map` runs Then   it writes `_map.html` and the `` marker is replaced with a `window.__REQMAP_DATA__` assignment carrying one node per requirement",
        "CASE-2 — Given  a requirement field containing `</script>` When   the viewer is rendered Then   the sequence is escaped (`<\\/`) so it cannot close the inline-data script early",
        "CASE-7 — Given  a registry whose `depends_on` edges form a cycle When   the layout is computed Then   every node is placed, no rank exceeds the node count, and the closing edge is drawn",
        "CASE-4 — Given  a requirement field containing `<!--` (e.g. a body that documents HTML injection) When   the viewer is rendered Then   `<!--` is escaped to `<\\!--` in the inlined blob so the HTML5 parser never enters \"script data escaped\" state, and `window.__REQMAP_DATA__` is always accessible from the deferred bundle (verified: file:// opens without error)",
        "CASE-3 — Given  no template is vendored When   `map` runs Then   `render_html` returns None and only `_map.md` + `_map.json` are written (no crash)",
        "CASE-5 — Given  a requirement whose title or contract contains U+2028 or U+2029 When   `map` writes `_map.html` Then   neither character appears raw in the file, and the inlined graph still parses to the original text",
        "CASE-6 — Given  the viewer rendered with a non-English locale selected When   a requirement's spec is shown Then   the section headers appear in that language while the requirement's own title, contract and acceptance text stay exactly as authored, and `status` / `layer` values stay literal",
        "CASE-8 — Given  a node carrying both a labelled `accept` block and the folded `acc` list When   its spec is rendered Then   the Given/When/Then lines appear as authored, one per line, and the folded one-line form is not what the reader sees"
      ],
      "accept": "CASE-1\n  Given  the vendored `_map_viewer.html` template is present\n  When   `map` runs\n  Then   it writes `_map.html` and the `<!--REQMAP_DATA-->` marker is replaced with a\n         `window.__REQMAP_DATA__` assignment carrying one node per requirement\n\nCASE-2\n  Given  a requirement field containing `</script>`\n  When   the viewer is rendered\n  Then   the sequence is escaped (`<\\/`) so it cannot close the inline-data script early\n\nCASE-7\n  Given  a registry whose `depends_on` edges form a cycle\n  When   the layout is computed\n  Then   every node is placed, no rank exceeds the node count, and the closing edge is drawn\n\nCASE-4\n  Given  a requirement field containing `<!--` (e.g. a body that documents HTML injection)\n  When   the viewer is rendered\n  Then   `<!--` is escaped to `<\\!--` in the inlined blob so the HTML5 parser never\n         enters \"script data escaped\" state, and `window.__REQMAP_DATA__` is always\n         accessible from the deferred bundle (verified: file:// opens without error)\n\nCASE-3\n  Given  no template is vendored\n  When   `map` runs\n  Then   `render_html` returns None and only `_map.md` + `_map.json` are written (no crash)\n\nCASE-5\n  Given  a requirement whose title or contract contains U+2028 or U+2029\n  When   `map` writes `_map.html`\n  Then   neither character appears raw in the file, and the inlined graph still parses to\n         the original text\n\nCASE-6\n  Given  the viewer rendered with a non-English locale selected\n  When   a requirement's spec is shown\n  Then   the section headers appear in that language while the requirement's own title,\n         contract and acceptance text stay exactly as authored, and `status` / `layer`\n         values stay literal\n\nCASE-8\n  Given  a node carrying both a labelled `accept` block and the folded `acc` list\n  When   its spec is rendered\n  Then   the Given/When/Then lines appear as authored, one per line, and the folded\n         one-line form is not what the reader sees",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-MAP-007"
      ],
      "used_by": [
        "ARCH-SITE-026",
        "ARCH-TRANSLATE-044"
      ],
      "satisfies": [
        "SYS-VISUAL-106"
      ],
      "satisfied_by": [
        "REQ-VIEWER-940",
        "REQ-VIEWER-941",
        "REQ-VIEWER-942",
        "REQ-VIEWER-943",
        "REQ-VIEWER-944",
        "REQ-VIEWER-945",
        "REQ-VIEWER-964",
        "REQ-VIEWER-966",
        "REQ-VIEWER-969",
        "REQ-VIEWER-977"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "app/vite.viewer.config.js:1"
        },
        {
          "role": "implements",
          "loc": "app/scripts/install-viewer.mjs:1"
        },
        {
          "role": "tested-by",
          "loc": "app/scripts/ssr-smoke.jsx:1"
        },
        {
          "role": "implements",
          "loc": "app/src/App.jsx:1"
        },
        {
          "role": "implements",
          "loc": "app/src/main.jsx:1"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/data.js:1"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/i18n.jsx:1"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/icons.jsx:1"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/layout.js:1"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/loadData.js:1"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/tree.js:1"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/ui.jsx:1"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/useDragPan.js:1"
        },
        {
          "role": "implements",
          "loc": "app/src/styles/app.css:1"
        },
        {
          "role": "implements",
          "loc": "app/src/styles/colors_and_type.css:1"
        },
        {
          "role": "implements",
          "loc": "app/src/views/CommandsView.jsx:1"
        },
        {
          "role": "implements",
          "loc": "app/src/views/ExplorerView.jsx:1"
        },
        {
          "role": "implements",
          "loc": "app/src/views/MapView.jsx:1"
        },
        {
          "role": "implements",
          "loc": "app/src/views/ProblemsView.jsx:1"
        },
        {
          "role": "implements",
          "loc": "app/src/views/RoadmapView.jsx:1"
        },
        {
          "role": "implements",
          "loc": "app/src/views/SpecView.jsx:1"
        },
        {
          "role": "generated-from",
          "loc": "docs/full_architecture.html:4"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1430"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2666"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7508"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7512"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7537"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1456"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:5799"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:6467"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9116"
        }
      ],
      "test_exempt": null,
      "milestone": "v1.04",
      "priority": "",
      "risks": []
    },
    {
      "id": "REQ-VIEWER-940",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Writing _map.html from the vendored template",
      "intent": "The viewer ships as one HTML file so it can be opened by double-click, with no server and no Node/npm dependency for the stdlib engine itself. When the template is not vendored, `map` still succeeds — it degrades to the two artifacts every consumer gets regardless of whether they built the viewer.",
      "contract": [
        "`map` generates `_map.html` when the template `_map_viewer.html` is vendored beside the engine.",
        "`_map.html` is a self-contained, single-file copy of the React viewer — the Vite + React app under `app/` — with this repo's graph inlined as `window.__REQMAP_DATA__`.",
        "`_map.html` opens by double-click, with no server.",
        "Absent the template, `render_html` emits nothing and returns None without failing.",
        "`map` then still writes `_map.md` and `_map.json`, so the stdlib engine works with no extra files.",
        "`render_html` replaces the template's `<!--REQMAP_DATA-->` marker with a single inline `<script>window.__REQMAP_DATA__=…</script>` assignment.",
        "That assignment carries the same `{nodes, edges}` graph [[ARCH-MAP-007]] builds."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — render_html writes _map.html when the vendored template exists Given  the vendored `_map_viewer.html` template beside the engine When   `render_html(data, reqs_dir)` runs Then   it returns the path to a written `_map.html` file",
        "CASE-2 — — the written _map.html carries the graph as window.__REQMAP_DATA__ Given  `render_html({\"nodes\": [...], \"edges\": []}, reqs_dir)` writing `_map.html` When   the file is read back Then   it contains `\"window.__REQMAP_DATA__=\"` and no leftover `` marker, and references no external file — opening by double-click with no server",
        "CASE-3 — — render_html returns None, not an error, when the template is missing Given  no `_map_viewer.html` template beside the engine When   `render_html(data, reqs_dir)` runs Then   it returns `None` and raises nothing",
        "CASE-4 — — map still writes _map.md and _map.json when the viewer template is absent Given  no vendored `_map_viewer.html` template When   `cmd_map` runs Then   `_map.md` and `_map.json` are written and `map` exits 0",
        "CASE-5 — — the marker is consumed and replaced by a script assignment Given  template text `\"<head></head>\"` and a graph with one node When   `_inject_viewer(template_text, data)` runs Then   the marker is gone and the output contains `\"window.__REQMAP_DATA__=\"`",
        "CASE-6 — — the inlined blob's node id matches the graph handed to _inject_viewer Given  a graph `{\"nodes\": [{\"id\": \"A-1\"}], \"edges\": []}` When   `_inject_viewer(template_text, data)` runs Then   the injected `<script>` text contains `\"A-1\"` --------------------"
      ],
      "accept": "CASE-1 — render_html writes _map.html when the vendored template exists\n  Given  the vendored `_map_viewer.html` template beside the engine\n  When   `render_html(data, reqs_dir)` runs\n  Then   it returns the path to a written `_map.html` file\n\nCASE-2 — the written _map.html carries the graph as window.__REQMAP_DATA__\n  Given  `render_html({\"nodes\": [...], \"edges\": []}, reqs_dir)` writing `_map.html`\n  When   the file is read back\n  Then   it contains `\"window.__REQMAP_DATA__=\"` and no leftover `<!--REQMAP_DATA-->`\n         marker, and references no external file — opening by double-click with no server\n\nCASE-3 — render_html returns None, not an error, when the template is missing\n  Given  no `_map_viewer.html` template beside the engine\n  When   `render_html(data, reqs_dir)` runs\n  Then   it returns `None` and raises nothing\n\nCASE-4 — map still writes _map.md and _map.json when the viewer template is absent\n  Given  no vendored `_map_viewer.html` template\n  When   `cmd_map` runs\n  Then   `_map.md` and `_map.json` are written and `map` exits 0\n\nCASE-5 — the marker is consumed and replaced by a script assignment\n  Given  template text `\"<head><!--REQMAP_DATA--></head>\"` and a graph with one node\n  When   `_inject_viewer(template_text, data)` runs\n  Then   the marker is gone and the output contains `\"window.__REQMAP_DATA__=\"`\n\nCASE-6 — the inlined blob's node id matches the graph handed to _inject_viewer\n  Given  a graph `{\"nodes\": [{\"id\": \"A-1\"}], \"edges\": []}`\n  When   `_inject_viewer(template_text, data)` runs\n  Then   the injected `<script>` text contains `\"A-1\"`\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-VIEWER-007"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7508"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7537"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1456"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9116"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Scrierea _map.html din șablonul vendorizat",
          "intent": "Viewerul se livrează ca un singur fișier HTML astfel poate fi deschis prin dublu-clic, fără server și fără dependență Node/npm pentru motorul stdlib însuși. Când șablonul nu este vendorizat, `map` reușește totuși — se degradează la cele două artefacte pe care fiecare consumator le obține indiferent dacă a construit viewerul.",
          "contract": "> Viewerul se livrează ca un singur fișier HTML astfel poate fi deschis prin dublu-clic, fără\n> server și fără dependență Node/npm pentru motorul stdlib însuși. Când șablonul nu este\n> vendorizat, `map` reușește totuși — se degradează la cele două artefacte pe care fiecare\n> consumator le obține indiferent dacă a construit viewerul.\n\nFiecare punct de mai jos este obligatoriu.\n- `map` generează `_map.html` când șablonul `_map_viewer.html` este vendorizat lângă\n  motor.\n- `_map.html` este o copie de sine stătătoare, într-un singur fișier, a viewerului React — aplicația Vite +\n  React din `app/` — cu graful acestui repo inclus ca `window.__REQMAP_DATA__`.\n- `_map.html` se deschide prin dublu-clic, fără server.\n- În absența șablonului, `render_html` nu emite nimic și returnează None fără să eșueze.\n- `map` scrie totuși `_map.md` și `_map.json`, astfel motorul stdlib funcționează fără\n  fișiere suplimentare.\n- `render_html` înlocuiește markerul `<!--REQMAP_DATA-->` al șablonului cu o singură\n  atribuire inline `<script>window.__REQMAP_DATA__=…</script>`.\n- Acea atribuire poartă același graf `{nodes, edges}` pe care îl construiește [[ARCH-MAP-007]].",
          "acceptance": "CASE-1 — render_html scrie _map.html când șablonul vendorizat există\n  Given  șablonul vendorizat `_map_viewer.html` lângă motor\n  When   `render_html(data, reqs_dir)` rulează\n  Then   returnează calea către un fișier `_map.html` scris\n\nCASE-2 — _map.html-ul scris poartă graful ca window.__REQMAP_DATA__\n  Given  `render_html({\"nodes\": [...], \"edges\": []}, reqs_dir)` scriind `_map.html`\n  When   fișierul este citit înapoi\n  Then   conține `\"window.__REQMAP_DATA__=\"` și niciun rest de marker `<!--REQMAP_DATA-->`,\n         și nu referențiază niciun fișier extern — deschidere prin dublu-clic fără server\n\nCASE-3 — render_html returnează None, nu o eroare, când șablonul lipsește\n  Given  niciun șablon `_map_viewer.html` lângă motor\n  When   `render_html(data, reqs_dir)` rulează\n  Then   returnează `None` și nu ridică nimic\n\nCASE-4 — map scrie totuși _map.md și _map.json când șablonul viewerului este absent\n  Given  niciun șablon vendorizat `_map_viewer.html`\n  When   `cmd_map` rulează\n  Then   `_map.md` și `_map.json` sunt scrise, iar `map` iese cu 0\n\nCASE-5 — markerul este consumat și înlocuit de o atribuire de script\n  Given  textul șablonului `\"<head><!--REQMAP_DATA--></head>\"` și un graf cu un nod\n  When   `_inject_viewer(template_text, data)` rulează\n  Then   markerul a dispărut, iar ieșirea conține `\"window.__REQMAP_DATA__=\"`\n\nCASE-6 — id-ul nodului din blob-ul inclus se potrivește cu graful pasat la _inject_viewer\n  Given  un graf `{\"nodes\": [{\"id\": \"A-1\"}], \"edges\": []}`\n  When   `_inject_viewer(template_text, data)` rulează\n  Then   textul `<script>` injectat conține `\"A-1\"`\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-VIEWER-941",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Escaping the inlined graph for embedded <script>",
      "intent": "The graph is embedded as a `<script>` assignment inside HTML the reader trusts, so any body a requirement author wrote — including one that discusses HTML injection and therefore contains `<!--` literally — must not be able to break out of that script or corrupt the page. Three escapes close three different ways a literal sequence in requirement text could do that, each added after a real failure was found.",
      "contract": [
        "`render_html` makes the injected graph HTML-safe for embedding inside `<script>` by applying three escapes in order. All three are V8 no-ops: a backslash is silently ignored before `/`, `!` and `-`.",
        "`</`   → `<\\/`   — prevents `</script>` from closing the element early",
        "`<!--` → `<\\!--` — prevents the HTML5 parser entering \"script data escaped\" state",
        "`-->`  → `-\\->`  — prevents prematurely closing that state if somehow entered",
        "The first guard alone was the original contract. The `<!--` and `-->` guards were added in v2.3.5 after a confirmed bug: requirement bodies that discuss HTML injection (and therefore contain literal `<!--`) broke `file://` opening by making `window.__REQMAP_DATA__` null.",
        "`render_html` also escapes U+2028 and U+2029 to their `\\\\u2028`/`\\\\u2029` forms. Those two characters end a line in JavaScript but not in JSON, so unescaped they turn the assignment into an unterminated string on any engine older than ES2019. The escaped forms denote the same characters in JSON, so the parsed graph is unchanged."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a field carrying all three dangerous sequences is fully escaped and still parses Given  a node title `\"</script>\"` When   `_inject_viewer(template_text, data)` runs Then   the output has none of `</`, `` raw, and re-parsing the blob as JSON yields the original title unchanged",
        "CASE-2 — — a </script> breakout attempt in a field is neutralized Given  a node id `\"a</script><img src=x>\"` When   `_inject_viewer(template_text, data)` runs Then   the output has no raw `\"</script><img\"` and contains `\"<\\\\/script>\"` instead",
        "CASE-3 — — a literal <!-- inside a field is escaped to <\\!-- Given  a node contract clause `\"discusses HTML injection via <!-- markers\"` When   `_inject_viewer(template_text, data)` runs Then   the output contains `\"<\\\\!--\"` and no raw `\"<!--\"` inside the injected blob, keeping `window.__REQMAP_DATA__` parseable instead of null",
        "CASE-4 — — a literal --> inside a field is escaped to -\\-> Given  a node title containing the literal sequence `\"-->\"` When   `_inject_viewer(template_text, data)` runs Then   the output contains `\"-\\\\->\"` and no raw `\"-->\"` inside the injected blob",
        "CASE-5 — — U+2028/U+2029 in a title are escaped and still round-trip through JSON Given  a node title containing U+2028 and a contract clause containing U+2029 When   `_inject_viewer(template_text, data)` runs Then   neither raw character appears in the output, the literal 6-character sequences \\u2028 and \\u2029 do, and re-parsing the blob as JSON yields the original title unchanged --------------------"
      ],
      "accept": "CASE-1 — a field carrying all three dangerous sequences is fully escaped and still parses\n  Given  a node title `\"</script><!--x-->\"`\n  When   `_inject_viewer(template_text, data)` runs\n  Then   the output has none of `</`, `<!--`, `-->` raw, and re-parsing the blob as JSON\n         yields the original title unchanged\n\nCASE-2 — a </script> breakout attempt in a field is neutralized\n  Given  a node id `\"a</script><img src=x>\"`\n  When   `_inject_viewer(template_text, data)` runs\n  Then   the output has no raw `\"</script><img\"` and contains `\"<\\\\/script>\"` instead\n\nCASE-3 — a literal <!-- inside a field is escaped to <\\!--\n  Given  a node contract clause `\"discusses HTML injection via <!-- markers\"`\n  When   `_inject_viewer(template_text, data)` runs\n  Then   the output contains `\"<\\\\!--\"` and no raw `\"<!--\"` inside the injected blob,\n         keeping `window.__REQMAP_DATA__` parseable instead of null\n\nCASE-4 — a literal --> inside a field is escaped to -\\->\n  Given  a node title containing the literal sequence `\"-->\"`\n  When   `_inject_viewer(template_text, data)` runs\n  Then   the output contains `\"-\\\\->\"` and no raw `\"-->\"` inside the injected blob\n\nCASE-5 — U+2028/U+2029 in a title are escaped and still round-trip through JSON\n  Given  a node title containing U+2028 and a contract clause containing U+2029\n  When   `_inject_viewer(template_text, data)` runs\n  Then   neither raw character appears in the output, the literal 6-character\n         sequences \\u2028 and \\u2029 do, and re-parsing the blob as\n         JSON yields the original title unchanged\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-VIEWER-007"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:7512"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:1456"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:5781"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:9116"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Escaparea grafului inclus pentru <script>-ul înglobat",
          "intent": "Graful este înglobat ca o atribuire `<script>` în interiorul HTML-ului în care cititorul are încredere, astfel orice corp pe care l-a scris un autor de cerință — inclusiv unul care discută injecția HTML și deci conține `<!--` literal — nu trebuie să poată ieși din acel script sau să corupă pagina. Trei escapări închid trei moduri diferite în care o secvență literală din textul cerinței ar putea face asta, fiecare adăugată după ce a fost găsit un eșec real.",
          "contract": "> Graful este înglobat ca o atribuire `<script>` în interiorul HTML-ului în care cititorul are\n> încredere, astfel orice corp pe care l-a scris un autor de cerință — inclusiv unul care\n> discută injecția HTML și deci conține `<!--` literal — nu trebuie să poată ieși din acel\n> script sau să corupă pagina. Trei escapări închid trei moduri diferite în care o secvență\n> literală din textul cerinței ar putea face asta, fiecare adăugată după ce a fost găsit un\n> eșec real.\n\nFiecare punct de mai jos este obligatoriu.\n- `render_html` face graful injectat sigur pentru HTML pentru includerea în interiorul `<script>` prin\n  aplicarea a trei escapări în ordine. Toate trei sunt no-op-uri V8: un backslash este ignorat în tăcere\n  înaintea lui `/`, `!` și `-`.\n  - `</`   → `<\\/`   — previne ca `</script>` să închidă elementul prematur\n  - `<!--` → `<\\!--` — previne ca parserul HTML5 să intre în starea \"script data escaped\"\n  - `-->`  → `-\\->`  — previne închiderea prematură a acelei stări dacă totuși a fost intrat în ea\n- Prima gardă singură a fost contractul original. Gărzile `<!--` și `-->` au fost adăugate\n  în v2.3.5 după un bug confirmat: corpurile de cerințe care discută injecția HTML (și\n  deci conțin `<!--` literal) au rupt deschiderea `file://` făcând\n  `window.__REQMAP_DATA__` null.\n- `render_html` de asemenea escapă U+2028 și U+2029 la formele lor `\\\\u2028`/`\\\\u2029`.\n  Acele două caractere termină o linie în JavaScript, dar nu în JSON, astfel neescapate ele\n  transformă atribuirea într-un șir nefinalizat pe orice engine mai vechi decât ES2019. Formele escapate\n  denotă aceleași caractere în JSON, astfel graful parsat rămâne neschimbat.",
          "acceptance": "CASE-1 — un câmp purtând toate cele trei secvențe periculoase este complet escapat și încă se parsează\n  Given  un titlu de nod `\"</script><!--x-->\"`\n  When   `_inject_viewer(template_text, data)` rulează\n  Then   ieșirea nu are niciunul dintre `</`, `<!--`, `-->` neescapate, iar reparsarea blob-ului ca JSON\n         produce titlul original neschimbat\n\nCASE-2 — o încercare de evadare </script> într-un câmp este neutralizată\n  Given  un id de nod `\"a</script><img src=x>\"`\n  When   `_inject_viewer(template_text, data)` rulează\n  Then   ieșirea nu are niciun `\"</script><img\"` brut și conține `\"<\\\\/script>\"` în loc\n\nCASE-3 — un <!-- literal într-un câmp este escapat la <\\!--\n  Given  o clauză de contract a nodului `\"discusses HTML injection via <!-- markers\"`\n  When   `_inject_viewer(template_text, data)` rulează\n  Then   ieșirea conține `\"<\\\\!--\"` și niciun `\"<!--\"` brut în interiorul blob-ului injectat,\n         păstrând `window.__REQMAP_DATA__` parsabil în loc de null\n\nCASE-4 — un --> literal într-un câmp este escapat la -\\->\n  Given  un titlu de nod conținând secvența literală `\"-->\"`\n  When   `_inject_viewer(template_text, data)` rulează\n  Then   ieșirea conține `\"-\\\\->\"` și niciun `\"-->\"` brut în interiorul blob-ului injectat\n\nCASE-5 — U+2028/U+2029 într-un titlu sunt escapate și încă se recuperează prin JSON\n  Given  un titlu de nod conținând U+2028 și o clauză de contract conținând U+2029\n  When   `_inject_viewer(template_text, data)` rulează\n  Then   niciun caracter brut nu apare în ieșire, secvențele literale de 6 caractere\n         \\u2028 și \\u2029 apar, iar reparsarea blob-ului ca\n         JSON produce titlul original neschimbat\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-VIEWER-942",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Ranking nodes and rendering acceptance criteria as authored",
      "intent": "A force-directed graph with no fixed coordinates would jump around on every reload and correlate nothing with the dependency structure it is meant to show. Ranking by longest path makes every `depends_on` edge point the same visual direction, so a reader learns the shape of the corpus from where a node sits, not only from its label. The acceptance block gets the same care: folding it to one line for search must not become what the reader is actually shown.",
      "contract": [
        "The viewer ranks nodes by longest dependency path, so `depends_on` edges flow one way.",
        "The viewer excludes a cycle-closing edge from that ranking, and still draws it.",
        "No node ranks higher than the number of nodes, whatever the registry's shape.",
        "A node carries the acceptance section twice: `accept`, the labelled Given/When/Then block as the author wrote it, and `acc`, the same criteria folded to one line each.",
        "The viewer renders `accept` — one line per line, as authored. `acc` is for search and counting, never the thing a reader is shown when the authored block exists."
      ],
      "verify": [],
      "notes": [
        "`lint_exempt: file-spread` — ranking and acceptance rendering are one obligation about what the map export hands the viewer, and it is enforced where the data is built and again where it is drawn. The spread is the trace of one rule, not several."
      ],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a deep dependency chain ranks by longest path Given  an honest 12-node `depends_on` chain with no cycle When   the layout is computed Then   the deepest node's rank equals 11, one more than each of its direct predecessors",
        "CASE-2 — — a cycle-closing edge is excluded from ranking but still drawn Given  a registry whose `depends_on` edges close a 3-node cycle, plus a fourth node depending on the cycle When   the layout is computed Then   the cycle-closing edge still appears among the drawn edges, and every node — cyclic or not — receives a position",
        "CASE-3 — — no rank exceeds the node count even on a cyclic registry Given  the same cyclic registry When   the layout is computed Then   the highest rank is at most the node count minus one, and the canvas width stays bounded rather than growing with the number of relaxation passes",
        "CASE-4 — — a map node carries both the raw accept block and the folded acc list Given  a confirmed requirement with a labelled `## Cases` block of two criteria When   `_build_map_data` builds the node Then   `node[\"acc\"]` has two folded entries and `node[\"accept\"]` holds the raw block",
        "CASE-5 — — the reader sees the authored Given/When/Then lines, not the folded one-liner Given  a node whose `accept` field holds a labelled multi-line Given/When/Then case and whose `acc` field holds the same case folded to one line When   its spec is rendered Then   the multi-line block appears as authored, and the folded one-line text is not what the reader sees --------------------"
      ],
      "accept": "CASE-1 — a deep dependency chain ranks by longest path\n  Given  an honest 12-node `depends_on` chain with no cycle\n  When   the layout is computed\n  Then   the deepest node's rank equals 11, one more than each of its direct predecessors\n\nCASE-2 — a cycle-closing edge is excluded from ranking but still drawn\n  Given  a registry whose `depends_on` edges close a 3-node cycle, plus a fourth node\n         depending on the cycle\n  When   the layout is computed\n  Then   the cycle-closing edge still appears among the drawn edges, and every node —\n         cyclic or not — receives a position\n\nCASE-3 — no rank exceeds the node count even on a cyclic registry\n  Given  the same cyclic registry\n  When   the layout is computed\n  Then   the highest rank is at most the node count minus one, and the canvas width stays\n         bounded rather than growing with the number of relaxation passes\n\nCASE-4 — a map node carries both the raw accept block and the folded acc list\n  Given  a confirmed requirement with a labelled `## Cases` block of two criteria\n  When   `_build_map_data` builds the node\n  Then   `node[\"acc\"]` has two folded entries and `node[\"accept\"]` holds the raw block\n\nCASE-5 — the reader sees the authored Given/When/Then lines, not the folded one-liner\n  Given  a node whose `accept` field holds a labelled multi-line Given/When/Then case and\n         whose `acc` field holds the same case folded to one line\n  When   its spec is rendered\n  Then   the multi-line block appears as authored, and the folded one-line text is not\n         what the reader sees\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-VIEWER-007"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "tested-by",
          "loc": "app/scripts/ssr-smoke.jsx:1"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/layout.js:35"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/layout.js:62"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/layout.js:81"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/loadData.js:50"
        },
        {
          "role": "implements",
          "loc": "app/src/views/SpecView.jsx:200"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5,
      "i18n": {
        "ro": {
          "title": "Clasarea nodurilor și randarea criteriilor de acceptance așa cum au fost redactate",
          "intent": "Un graf force-directed fără coordonate fixe ar sări în jur la fiecare reîncărcare și nu ar corela nimic cu structura de dependențe pe care este menit s-o arate. Clasarea după cel mai lung drum face ca fiecare muchie `depends_on` să indice aceeași direcție vizuală, astfel un cititor învață forma corpusului din locul unde stă un nod, nu doar din eticheta lui. Blocul de acceptance primește aceeași grijă: împăturirea lui la o singură linie pentru căutare nu trebuie să devină ceea ce cititorul este de fapt afișat.",
          "contract": "> Un graf force-directed fără coordonate fixe ar sări în jur la fiecare reîncărcare și nu ar\n> corela nimic cu structura de dependențe pe care este menit s-o arate. Clasarea după cel mai\n> lung drum face ca fiecare muchie `depends_on` să indice aceeași direcție vizuală, astfel un\n> cititor învață forma corpusului din locul unde stă un nod, nu doar din eticheta lui. Blocul\n> de acceptance primește aceeași grijă: împăturirea lui la o singură linie pentru căutare nu\n> trebuie să devină ceea ce cititorul este de fapt afișat.\n\nFiecare punct de mai jos este obligatoriu.\n- Viewerul clasează nodurile după cel mai lung drum de dependență, astfel muchiile `depends_on` curg într-un singur sens.\n- Viewerul exclude o muchie care închide un ciclu din acea clasare, și o desenează totuși.\n- Niciun nod nu clasează mai sus decât numărul de noduri, oricare ar fi forma registrului.\n- Un nod poartă secțiunea de acceptance de două ori: `accept`, blocul etichetat Given/When/Then\n  așa cum l-a scris autorul, și `acc`, aceleași criterii împăturite la o linie fiecare.\n- Viewerul randează `accept` — o linie pe linie, așa cum a fost redactat. `acc` este pentru căutare și\n  numărare, niciodată ceea ce vede un cititor când blocul redactat există.",
          "acceptance": "CASE-1 — un lanț de dependențe adânc se clasează după cel mai lung drum\n  Given  un lanț `depends_on` onest de 12 noduri fără niciun ciclu\n  When   layout-ul este calculat\n  Then   rangul celui mai adânc nod este egal cu 11, unul mai mult decât fiecare dintre predecesorii lui direcți\n\nCASE-2 — o muchie care închide un ciclu este exclusă din clasare, dar desenată totuși\n  Given  un registru ale cărui muchii `depends_on` închid un ciclu de 3 noduri, plus un al patrulea nod\n         care depinde de ciclu\n  When   layout-ul este calculat\n  Then   muchia care închide ciclul apare totuși printre muchiile desenate, iar fiecare nod —\n         ciclic sau nu — primește o poziție\n\nCASE-3 — niciun rang nu depășește numărul de noduri chiar și pe un registru ciclic\n  Given  același registru ciclic\n  When   layout-ul este calculat\n  Then   cel mai mare rang este cel mult numărul de noduri minus unu, iar lățimea canvas-ului rămâne\n         mărginită, nu crește odată cu numărul de treceri de relaxare\n\nCASE-4 — un nod de map poartă atât blocul accept brut, cât și lista acc împăturită\n  Given  o cerință confirmată cu un bloc etichetat `## Cases` de două criterii\n  When   `_build_map_data` construiește nodul\n  Then   `node[\"acc\"]` are două intrări împăturite, iar `node[\"accept\"]` deține blocul brut\n\nCASE-5 — cititorul vede liniile Given/When/Then redactate, nu one-liner-ul împăturit\n  Given  un nod al cărui câmp `accept` deține un caz Given/When/Then etichetat pe mai multe linii și\n         al cărui câmp `acc` deține același caz împăturit la o linie\n  When   spec-ul lui este randat\n  Then   blocul pe mai multe linii apare așa cum a fost redactat, iar textul împăturit pe o linie nu este\n         ce vede cititorul\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-VIEWER-943",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "UI chrome language, requirement content untranslated",
      "intent": "The viewer's own chrome — nav labels, buttons, section headers — is UI text the tool owns and can safely translate. A requirement's title, contract and acceptance criteria are the artifact under review; translating those live would put words in the author's mouth and silently diverge from the `.md` file on disk. The two must never be confused, so this draws the line and holds it under every locale.",
      "contract": [
        "The viewer renders its own UI chrome in English by default.",
        "A locale control in the viewer's top bar switches that chrome to another bundled language.",
        "Requirement content is never translated: id, title, intent, contract clauses, acceptance criteria and member paths stay in the language their author wrote them in.",
        "The engine's own vocabulary is never translated either: `status`, `layer`, tag-role and severity values stay the literal strings the requirement files and the gate use.",
        "A chrome string with no entry in the active locale falls back to its English text.",
        "The reader's chosen locale is remembered on their machine and is never written into the generated file, so `_map.html` stays byte-identical whatever anyone last selected."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the viewer defaults to English chrome Given  no locale has been selected before When   a requirement's spec is rendered Then   its section headers appear in English",
        "CASE-2 — — switching locale translates chrome section headers Given  the Romanian locale selected When   the same spec is rendered Then   its section headers appear in Romanian, and the English header text is gone",
        "CASE-3 — — an untranslated chrome string falls back to English Given  a chrome string with no entry in the active locale's dictionary When   it is looked up for display Then   it renders as the original English text, not blank",
        "CASE-4 — — chrome translations interpolate their placeholders Given  a chrome string containing a `{n}`-style placeholder When   it is rendered in a non-English locale Then   the placeholder's value is substituted into the translated sentence",
        "CASE-5 — — requirement content stays in the author's language under any locale Given  the Romanian locale selected and a requirement's spec rendered Then   the requirement's own title appears exactly as authored, untranslated",
        "CASE-6 — — engine vocabulary stays literal under any locale Given  the Romanian locale selected and a requirement's spec rendered Then   its `status` value appears as the literal engine string, not a translated word"
      ],
      "accept": "CASE-1 — the viewer defaults to English chrome\n  Given  no locale has been selected before\n  When   a requirement's spec is rendered\n  Then   its section headers appear in English\n\nCASE-2 — switching locale translates chrome section headers\n  Given  the Romanian locale selected\n  When   the same spec is rendered\n  Then   its section headers appear in Romanian, and the English header text is gone\n\nCASE-3 — an untranslated chrome string falls back to English\n  Given  a chrome string with no entry in the active locale's dictionary\n  When   it is looked up for display\n  Then   it renders as the original English text, not blank\n\nCASE-4 — chrome translations interpolate their placeholders\n  Given  a chrome string containing a `{n}`-style placeholder\n  When   it is rendered in a non-English locale\n  Then   the placeholder's value is substituted into the translated sentence\n\nCASE-5 — requirement content stays in the author's language under any locale\n  Given  the Romanian locale selected and a requirement's spec rendered\n  Then   the requirement's own title appears exactly as authored, untranslated\n\nCASE-6 — engine vocabulary stays literal under any locale\n  Given  the Romanian locale selected and a requirement's spec rendered\n  Then   its `status` value appears as the literal engine string, not a translated word",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-VIEWER-007"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "tested-by",
          "loc": "app/scripts/ssr-smoke.jsx:1"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/i18n.jsx:44"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/i18n.jsx:195"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/i18n.jsx:229"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 6,
      "covered": 6,
      "i18n": {
        "ro": {
          "title": "Limba de chrome UI, conținutul cerinței netradus",
          "intent": "Chrome-ul propriu al viewerului — etichete de navigare, butoane, titluri de secțiune — este text UI pe care unealta îl deține și poate traduce în siguranță. Titlul, contractul și criteriile de acceptance ale unei cerințe sunt artefactul aflat în revizuire; traducerea lor live ar pune cuvinte în gura autorului și ar diverge în tăcere de fișierul `.md` de pe disc. Cele două nu trebuie confundate niciodată, astfel aceasta trasează linia și o menține sub fiecare locale.",
          "contract": "> Chrome-ul propriu al viewerului — etichete de navigare, butoane, titluri de secțiune —\n> este text UI pe care unealta îl deține și poate traduce în siguranță. Titlul, contractul și\n> criteriile de acceptance ale unei cerințe sunt artefactul aflat în revizuire; traducerea lor\n> live ar pune cuvinte în gura autorului și ar diverge în tăcere de fișierul `.md` de pe disc.\n> Cele două nu trebuie confundate niciodată, astfel aceasta trasează linia și o menține sub\n> fiecare locale.\n\nFiecare punct de mai jos este obligatoriu.\n- Viewerul randează propriul lui chrome UI implicit în engleză.\n- Un control de locale în bara de sus a viewerului comută acel chrome la o altă limbă inclusă.\n- Conținutul cerinței nu este niciodată tradus: id, titlu, intent, clauze de contract, criterii de\n  acceptance și căi de membri rămân în limba în care autorul lor le-a scris.\n- Nici vocabularul propriu al motorului nu este tradus: valorile `status`, `layer`, role de tag și\n  severitate rămân șirurile literale pe care fișierele de cerință și gate-ul le folosesc.\n- Un șir de chrome fără nicio intrare în locale-ul activ recurge la textul lui în engleză.\n- Locale-ul ales de cititor este reținut pe mașina lui și nu este scris niciodată în\n  fișierul generat, astfel `_map.html` rămâne identic byte cu byte indiferent de ce a selectat ultima dată oricine.",
          "acceptance": "CASE-1 — viewerul implicit are chrome-ul în engleză\n  Given  niciun locale nu a fost selectat înainte\n  When   spec-ul unei cerințe este randat\n  Then   titlurile lui de secțiune apar în engleză\n\nCASE-2 — comutarea locale-ului traduce titlurile de secțiune de chrome\n  Given  locale-ul românesc selectat\n  When   același spec este randat\n  Then   titlurile lui de secțiune apar în română, iar textul de titlu englez a dispărut\n\nCASE-3 — un șir de chrome netradus recurge la engleză\n  Given  un șir de chrome fără nicio intrare în dicționarul locale-ului activ\n  When   este căutat pentru afișare\n  Then   se randează ca textul original englez, nu gol\n\nCASE-4 — traducerile de chrome interpolează placeholder-ele lor\n  Given  un șir de chrome conținând un placeholder în stil `{n}`\n  When   este randat într-un locale non-englez\n  Then   valoarea placeholder-ului este substituită în propoziția tradusă\n\nCASE-5 — conținutul cerinței rămâne în limba autorului sub orice locale\n  Given  locale-ul românesc selectat și spec-ul unei cerințe randat\n  Then   titlul propriu al cerinței apare exact așa cum a fost redactat, netradus\n\nCASE-6 — vocabularul motorului rămâne literal sub orice locale\n  Given  locale-ul românesc selectat și spec-ul unei cerințe randat\n  Then   valoarea lui `status` apare ca șirul literal al motorului, nu un cuvânt tradus"
        }
      }
    },
    {
      "id": "REQ-VIEWER-944",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Cross-references and header fields in a rendered spec",
      "intent": "Authors write `[[REQ-CHECK-828]]` in a clause to point at the requirement that details it. Rendered literally, that is a pair of brackets that leads nowhere — on this corpus every architecture requirement carries several. The same document also used to print a frontmatter block including `owner:`, a field the engine has never exported, so every repo but the one it was hard-coded from read someone else's name.",
      "contract": [
        "A `[[ID]]` cross-reference in requirement prose renders as the bare id, without its brackets.",
        "When the loaded map holds that id, the reference is an activatable control that opens that requirement; keyboard activation does the same thing as a click.",
        "When the loaded map does not hold it, the id renders as marked text that navigates nowhere — a dangling reference is reported, not hidden.",
        "Requirement prose is HTML-escaped before either transform runs, so no authored text can reach the DOM as markup.",
        "A rendered document states only header fields the export carries; a field the engine does not emit is absent rather than invented."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a resolvable cross-reference becomes a control Given  a clause containing `[[ID]]` and a registry holding that id When   the requirement's document is rendered Then   the id appears as an activatable link carrying that id, and the brackets are gone",
        "CASE-2 — — a dangling cross-reference is marked, not linked Given  a clause containing `[[ID]]` and a registry that does not hold that id When   the requirement's document is rendered Then   the id appears as marked text with no link control",
        "CASE-3 — — escaping still wins over both transforms Given  a clause containing HTML markup alongside a cross-reference When   the requirement's document is rendered Then   the markup appears escaped and only the cross-reference is a control",
        "CASE-4 — — the header invents no field Given  a requirement rendered from an engine export, which carries no `owner` When   its document header is rendered Then   no owner is shown"
      ],
      "accept": "CASE-1 — a resolvable cross-reference becomes a control\n  Given  a clause containing `[[ID]]` and a registry holding that id\n  When   the requirement's document is rendered\n  Then   the id appears as an activatable link carrying that id, and the brackets are gone\n\nCASE-2 — a dangling cross-reference is marked, not linked\n  Given  a clause containing `[[ID]]` and a registry that does not hold that id\n  When   the requirement's document is rendered\n  Then   the id appears as marked text with no link control\n\nCASE-3 — escaping still wins over both transforms\n  Given  a clause containing HTML markup alongside a cross-reference\n  When   the requirement's document is rendered\n  Then   the markup appears escaped and only the cross-reference is a control\n\nCASE-4 — the header invents no field\n  Given  a requirement rendered from an engine export, which carries no `owner`\n  When   its document header is rendered\n  Then   no owner is shown",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-VIEWER-007"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "tested-by",
          "loc": "app/scripts/ssr-smoke.jsx:2"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/ui.jsx:2"
        },
        {
          "role": "implements",
          "loc": "app/src/views/SpecView.jsx:3"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Referințe încrucișate și câmpuri de antet într-un spec randat",
          "intent": "Autorii scriu `[[REQ-CHECK-828]]` într-o clauză pentru a indica cerința care o detaliază. Randat literal, asta este o pereche de paranteze care nu duce nicăieri — pe acest corpus fiecare cerință de arhitectură poartă mai multe. Același document obișnuia de asemenea să afișeze un bloc frontmatter incluzând `owner:`, un câmp pe care motorul nu l-a exportat niciodată, astfel fiecare repo în afară de cel din care a fost hard-codat citea numele altcuiva.",
          "contract": "> Autorii scriu `[[REQ-CHECK-828]]` într-o clauză pentru a indica cerința care o detaliază.\n> Randat literal, asta este o pereche de paranteze care nu duce nicăieri — pe acest corpus\n> fiecare cerință de arhitectură poartă mai multe. Același document obișnuia de asemenea să\n> afișeze un bloc frontmatter incluzând `owner:`, un câmp pe care motorul nu l-a exportat\n> niciodată, astfel fiecare repo în afară de cel din care a fost hard-codat citea numele\n> altcuiva.\n\nFiecare punct de mai jos este obligatoriu.\n- O referință încrucișată `[[ID]]` în proza cerinței se randează ca id-ul gol, fără parantezele lui.\n- Când map-ul încărcat deține acel id, referința este un control activabil care deschide acea\n  cerință; activarea de la tastatură face același lucru ca un clic.\n- Când map-ul încărcat nu îl deține, id-ul se randează ca text marcat care nu navighează nicăieri —\n  o referință suspendată este raportată, nu ascunsă.\n- Proza cerinței este escapată HTML înainte ca oricare dintre transformări să ruleze, astfel niciun text redactat nu poate ajunge\n  în DOM ca markup.\n- Un document randat afirmă doar câmpurile de antet pe care le poartă export-ul; un câmp pe care motorul nu-l\n  emite este absent în loc de inventat.",
          "acceptance": "CASE-1 — o referință încrucișată rezolvabilă devine un control\n  Given  o clauză conținând `[[ID]]` și un registru deținând acel id\n  When   documentul cerinței este randat\n  Then   id-ul apare ca un link activabil purtând acel id, iar parantezele au dispărut\n\nCASE-2 — o referință încrucișată suspendată este marcată, nu legată\n  Given  o clauză conținând `[[ID]]` și un registru care nu deține acel id\n  When   documentul cerinței este randat\n  Then   id-ul apare ca text marcat fără niciun control de link\n\nCASE-3 — escaparea încă domină ambele transformări\n  Given  o clauză conținând markup HTML alături de o referință încrucișată\n  When   documentul cerinței este randat\n  Then   markup-ul apare escapat, și doar referința încrucișată este un control\n\nCASE-4 — antetul nu inventează niciun câmp\n  Given  o cerință randată dintr-un export al motorului, care nu poartă niciun `owner`\n  When   antetul documentului ei este randat\n  Then   niciun owner nu este afișat"
        }
      }
    },
    {
      "id": "REQ-VIEWER-945",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Scoping the outline from the registry tally",
      "intent": "The rail's registry tally answers \"how many are drafts?\" and then leaves you to find them by hand. The number and the rows behind it are the same query, so the number is the natural place to ask for them — as long as what it applied stays visible, or a reader is left with a filtered list and no idea why.",
      "contract": [
        "Each row of the registry tally scopes the outline to the slice it counts, and opens the outline if another surface was showing.",
        "The `orphan` row scopes to the gate's own error condition — an enforced requirement with no `implements:` member — which is a computed state, not a status value.",
        "An applied scope is rendered as an active filter chip, and clearing that chip, or clicking the same tally row again, restores the full outline.",
        "The scope applies to the first render, not only after one — the outline is never painted unfiltered when a scope was requested."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a tally row narrows the outline Given  the outline rendered with the `draft` slice requested When   its rows are counted Then   fewer rows are shown than with no slice requested",
        "CASE-2 — — the orphan row scopes to the gate's condition, not a status Given  a registry in which every enforced requirement has an `implements:` member When   the outline is rendered with the `orphan` slice requested Then   no row matches, and the empty state says so",
        "CASE-3 — — the applied scope is visible and clearable Given  the outline rendered with a slice requested When   its filter row is drawn Then   the chip naming that slice is drawn active"
      ],
      "accept": "CASE-1 — a tally row narrows the outline\n  Given  the outline rendered with the `draft` slice requested\n  When   its rows are counted\n  Then   fewer rows are shown than with no slice requested\n\nCASE-2 — the orphan row scopes to the gate's condition, not a status\n  Given  a registry in which every enforced requirement has an `implements:` member\n  When   the outline is rendered with the `orphan` slice requested\n  Then   no row matches, and the empty state says so\n\nCASE-3 — the applied scope is visible and clearable\n  Given  the outline rendered with a slice requested\n  When   its filter row is drawn\n  Then   the chip naming that slice is drawn active",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-VIEWER-007"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "tested-by",
          "loc": "app/scripts/ssr-smoke.jsx:2"
        },
        {
          "role": "implements",
          "loc": "app/src/App.jsx:2"
        },
        {
          "role": "implements",
          "loc": "app/src/views/ExplorerView.jsx:2"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Delimitarea outline-ului din numărătoarea registrului",
          "intent": "Numărătoarea registrului din rail răspunde \"câte sunt draft-uri?\" și apoi te lasă să le găsești manual. Numărul și rândurile din spatele lui sunt aceeași interogare, astfel numărul este locul natural pentru a le cere — atâta timp cât ceea ce s-a aplicat rămâne vizibil, altfel un cititor rămâne cu o listă filtrată și nicio idee de ce.",
          "contract": "> Numărătoarea registrului din rail răspunde \"câte sunt draft-uri?\" și apoi te lasă să le\n> găsești manual. Numărul și rândurile din spatele lui sunt aceeași interogare, astfel numărul\n> este locul natural pentru a le cere — atâta timp cât ceea ce s-a aplicat rămâne vizibil, altfel\n> un cititor rămâne cu o listă filtrată și nicio idee de ce.\n\nFiecare punct de mai jos este obligatoriu.\n- Fiecare rând al numărătorii registrului delimitează outline-ul la felia pe care o numără, și deschide outline-ul\n  dacă o altă suprafață era afișată.\n- Rândul `orphan` delimitează la condiția de eroare proprie a gate-ului — o cerință aplicată fără niciun\n  membru `implements:` — care este o stare calculată, nu o valoare de status.\n- O delimitare aplicată este randată ca un chip de filtru activ, iar ștergerea acelui chip, sau apăsarea din nou pe\n  același rând al numărătorii, restaurează outline-ul complet.\n- Delimitarea se aplică la prima randare, nu doar după una — outline-ul nu este niciodată pictat\n  nefiltrat când o delimitare a fost cerută.",
          "acceptance": "CASE-1 — un rând al numărătorii îngustează outline-ul\n  Given  outline-ul randat cu felia `draft` cerută\n  When   rândurile lui sunt numărate\n  Then   se afișează mai puține rânduri decât fără nicio felie cerută\n\nCASE-2 — rândul orphan se delimitează la condiția gate-ului, nu la un status\n  Given  un registru în care fiecare cerință aplicată are un membru `implements:`\n  When   outline-ul este randat cu felia `orphan` cerută\n  Then   niciun rând nu se potrivește, iar starea goală spune asta\n\nCASE-3 — delimitarea aplicată este vizibilă și ștergibilă\n  Given  outline-ul randat cu o felie cerută\n  When   rândul lui de filtru este desenat\n  Then   chip-ul care numește acea felie este desenat activ"
        }
      }
    },
    {
      "id": "REQ-VIEWER-964",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The command reference, in the reader's language",
      "intent": "Someone reading the map to understand a repository is one question away from \"how do I run this?\" — and the answer used to live only in a README they would have to go and find. The viewer shows the commands themselves, grouped by the moment of work they belong to.",
      "contract": [
        "The viewer renders one entry per command the map carries, grouped by authoring, building and reading, showing the invocation, its summary and each flag with its help text.",
        "A command's summary is shown in the reader's chosen language, falling back to the engine's own English when that language has no entry for it.",
        "Flag names are never translated: they are literals the reader types.",
        "A map carrying no command list renders a named empty state saying how to regenerate one, never a blank page."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the commands are listed with their flags Given  a map carrying a command list When   the command reference is rendered Then   each command appears with its invocation, its summary and one row per flag",
        "CASE-2 — — the summary follows the chosen language Given  the Romanian locale and a command with a Romanian summary When   the reference is rendered Then   the Romanian summary is shown and the English one is not",
        "CASE-3 — — an untranslated command falls back to English Given  a locale with no entry for one command When   the reference is rendered Then   that command shows the engine's English summary rather than blank",
        "CASE-4 — — a map with no command list says so Given  a map produced before the list existed When   the reference is rendered Then   a named empty state appears naming the command that regenerates it"
      ],
      "accept": "CASE-1 — the commands are listed with their flags\n  Given  a map carrying a command list\n  When   the command reference is rendered\n  Then   each command appears with its invocation, its summary and one row per flag\n\nCASE-2 — the summary follows the chosen language\n  Given  the Romanian locale and a command with a Romanian summary\n  When   the reference is rendered\n  Then   the Romanian summary is shown and the English one is not\n\nCASE-3 — an untranslated command falls back to English\n  Given  a locale with no entry for one command\n  When   the reference is rendered\n  Then   that command shows the engine's English summary rather than blank\n\nCASE-4 — a map with no command list says so\n  Given  a map produced before the list existed\n  When   the reference is rendered\n  Then   a named empty state appears naming the command that regenerates it",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-VIEWER-007"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "tested-by",
          "loc": "app/scripts/ssr-smoke.jsx:3"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/i18n.jsx:167"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/i18n.jsx:241"
        },
        {
          "role": "implements",
          "loc": "app/src/views/CommandsView.jsx:2"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "Referința de comenzi, în limba cititorului",
          "intent": "Cineva care citește map-ul pentru a înțelege un repository este la o întrebare distanță de \"cum rulez asta?\" — iar răspunsul obișnuia să trăiască doar într-un README pe care ar fi trebuit să-l caute. Viewerul afișează comenzile însele, grupate după momentul de lucru căruia îi aparțin.",
          "contract": "> Cineva care citește map-ul pentru a înțelege un repository este la o întrebare distanță de \"cum rulez asta?\"\n> — iar răspunsul obișnuia să trăiască doar într-un README pe care ar fi trebuit să-l caute. Viewerul afișează\n> comenzile însele, grupate după momentul de lucru căruia îi aparțin.\n\nFiecare punct de mai jos este obligatoriu.\n- Viewerul randează câte o intrare pentru fiecare comandă pe care o poartă map-ul, grupate după authoring, building și\n  reading, arătând invocarea, rezumatul ei și fiecare flag cu textul lui de ajutor.\n- Rezumatul unei comenzi este afișat în limba aleasă de cititor, recurgând la engleza proprie a\n  motorului când acea limbă nu are nicio intrare pentru el.\n- Numele flag-urilor nu sunt niciodată traduse: sunt literale pe care cititorul le tastează.\n- Un map fără nicio listă de comenzi randează o stare goală numită, spunând cum să regenerezi una, niciodată o pagină\n  goală.",
          "acceptance": "CASE-1 — comenzile sunt listate cu flag-urile lor\n  Given  un map purtând o listă de comenzi\n  When   referința de comenzi este randată\n  Then   fiecare comandă apare cu invocarea ei, rezumatul ei și câte un rând per flag\n\nCASE-2 — rezumatul urmează limba aleasă\n  Given  locale-ul românesc și o comandă cu un rezumat românesc\n  When   referința este randată\n  Then   rezumatul românesc este afișat, iar cel englez nu\n\nCASE-3 — o comandă netradusă recurge la engleză\n  Given  un locale fără nicio intrare pentru o comandă\n  When   referința este randată\n  Then   acea comandă arată rezumatul englez al motorului, în loc de gol\n\nCASE-4 — un map fără nicio listă de comenzi spune asta\n  Given  un map produs înainte ca lista să existe\n  When   referința este randată\n  Then   apare o stare goală numită, numind comanda care o regenerează"
        }
      }
    },
    {
      "id": "REQ-VIEWER-966",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "One inbox, with the origin of a signal as a tab",
      "intent": "Two inboxes stood side by side because one of them used to be six hundred rows of draft noise, and a question a human had written down was invisible in there. That corpus is gone, and the noise is collapsed where it occurs. What is worth keeping is not the second screen but the distinction it protected: a warning the engine computed and a question a person wrote are different news, and a reader should be able to ask for one without reading the other. See [ADR-0028](../../docs/adr/0028-one-inbox-for-every-open-signal.md).",
      "contract": [
        "A requirement's open `## Verify intent` questions appear in the problems inbox as rows of their own kind, carrying the question text and the step that closes it.",
        "The authored placeholder is not a question, exactly as it is not one for the engine's own digest: a requirement that recorded nothing contributes no row.",
        "The inbox offers the origin as a filter of its own, so \"what did a human ask?\" is one click and never a severity ranked among computed signals.",
        "A question sorts above an unreviewed draft and below an error or a warning.",
        "The rail badge for authored questions is hidden at zero rather than rendered as a proud `0`."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — an authored question is a row in the inbox Given  a requirement carrying one real Verify-intent question When   the problems inbox is computed Then   it holds a row for that requirement, of the question kind",
        "CASE-2 — — the placeholder is still not a question Given  a requirement whose Verify-intent section holds only the authored placeholder When   the inbox is computed Then   no question row is raised for it, whatever else its state raises",
        "CASE-3 — — authored questions are counted apart from computed signals Given  an inbox holding both kinds of row When   the questions are counted for the rail badge Then   only the authored ones are counted",
        "CASE-4 — — the origin is offered as a filter, with the question text shown Given  an inbox holding a question row When   it is rendered Then   a tab for questions is offered and the question's own text is displayed --------------------"
      ],
      "accept": "CASE-1 — an authored question is a row in the inbox\n  Given  a requirement carrying one real Verify-intent question\n  When   the problems inbox is computed\n  Then   it holds a row for that requirement, of the question kind\n\nCASE-2 — the placeholder is still not a question\n  Given  a requirement whose Verify-intent section holds only the authored placeholder\n  When   the inbox is computed\n  Then   no question row is raised for it, whatever else its state raises\n\nCASE-3 — authored questions are counted apart from computed signals\n  Given  an inbox holding both kinds of row\n  When   the questions are counted for the rail badge\n  Then   only the authored ones are counted\n\nCASE-4 — the origin is offered as a filter, with the question text shown\n  Given  an inbox holding a question row\n  When   it is rendered\n  Then   a tab for questions is offered and the question's own text is displayed\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-VIEWER-007"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "tested-by",
          "loc": "app/scripts/ssr-smoke.jsx:2"
        },
        {
          "role": "implements",
          "loc": "app/src/views/ProblemsView.jsx:2"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "O singură căsuță de intrare, cu originea unui semnal drept filă",
          "intent": "Două căsuțe de intrare stăteau una lângă alta pentru că una dintre ele era pe vremuri șase sute de rânduri de zgomot de ciornă, iar o întrebare pe care o scrisese un om era invizibilă acolo. Acel corpus nu mai există, iar zgomotul este restrâns acolo unde apare. Ceea ce merită păstrat nu este al doilea ecran, ci distincția pe care o proteja: o avertizare calculată de motor și o întrebare scrisă de o persoană sunt vești diferite, iar un cititor ar trebui să poată cere una fără a o citi pe cealaltă. Vezi [ADR-0028](../../docs/adr/0028-one-inbox-for-every-open-signal.md).",
          "contract": "> Două căsuțe de intrare stăteau una lângă alta pentru că una dintre ele era pe vremuri șase sute de rânduri de zgomot de ciornă,\n> iar o întrebare pe care o scrisese un om era invizibilă acolo. Acel corpus nu mai există, iar\n> zgomotul este restrâns acolo unde apare. Ceea ce merită păstrat nu este al doilea ecran, ci\n> distincția pe care o proteja: o avertizare calculată de motor și o întrebare scrisă de o persoană sunt\n> vești diferite, iar un cititor ar trebui să poată cere una fără a o citi pe cealaltă.\n> Vezi [ADR-0028](../../docs/adr/0028-one-inbox-for-every-open-signal.md).\n\nFiecare punct de mai jos este obligatoriu.\n- Întrebările deschise `## Verify intent` ale unei cerințe apar în căsuța de intrare a problemelor ca rânduri de\n  tipul lor propriu, purtând textul întrebării și pasul care o închide.\n- Substituentul autorat nu este o întrebare, exact așa cum nu este una nici pentru rezumatul propriu al motorului:\n  o cerință care nu a consemnat nimic nu contribuie cu niciun rând.\n- Căsuța de intrare oferă originea drept filtru de sine stătător, astfel încât „ce a întrebat un om?” este un singur clic și\n  niciodată o severitate clasată printre semnalele calculate.\n- O întrebare se sortează deasupra unei ciorne nerevizuite și dedesubtul unei erori sau al unei avertizări.\n- Insigna din bara laterală pentru întrebările autorate este ascunsă la zero, în loc să fie redată ca un `0` semeț.",
          "acceptance": "CASE-1 — o întrebare autorată este un rând în căsuța de intrare\n  Given  o cerință care poartă o întrebare Verify-intent reală\n  When   se calculează căsuța de intrare a problemelor\n  Then   aceasta conține un rând pentru acea cerință, de tipul întrebare\n\nCASE-2 — substituentul tot nu este o întrebare\n  Given  o cerință a cărei secțiune Verify-intent conține doar substituentul autorat\n  When   se calculează căsuța de intrare\n  Then   nu se ridică niciun rând de întrebare pentru ea, orice altceva ar ridica starea ei\n\nCASE-3 — întrebările autorate sunt numărate separat de semnalele calculate\n  Given  o căsuță de intrare care conține ambele tipuri de rând\n  When   întrebările sunt numărate pentru insigna din bara laterală\n  Then   sunt numărate doar cele autorate\n\nCASE-4 — originea este oferită drept filtru, cu textul întrebării afișat\n  Given  o căsuță de intrare care conține un rând de întrebare\n  When   aceasta este redată\n  Then   este oferită o filă pentru întrebări și este afișat textul propriu al întrebării"
        }
      }
    },
    {
      "id": "REQ-VIEWER-969",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "Two engine-emitted readings in the rail",
      "intent": "`reqmap.py gate --risk` opens with two numbers — how much of the corpus is green, and how much of the code is free of design candidates — and the viewer showed neither. They belong where a reader already looks for the shape of the repo: the rail, under the navigation. The viewer displays the records the map hands it; recomputing either one here is what would let the terminal and the browser report different repos.",
      "contract": [
        "The rail renders one ring per reading, showing the score, its label and the fraction behind it, from the `health` and `design` records the map carries.",
        "The health ring is coloured by band, since it is a verdict: green while at or above 90, amber down to 60, red below. The design ring stays in one neutral ink — that score is advice the gate never enforces, and a red ring would read as a failure the repo does not have.",
        "The health ring opens the Problems inbox, where the reasons behind the score are listed. The design ring is not a control, because there is no view to open.",
        "A map carrying neither record renders no ring at all. An older map has neither key, and a reading invented client-side would be worse than an absent one.",
        "Both labels and both captions follow the chosen interface language, like the rest of the chrome."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the rings show the numbers the engine emitted Given  a map carrying a health record of 39 of 50 and a design record of 7 of 30 When   the rail renders Then   both scores and both fractions appear as given, with nothing recomputed",
        "CASE-2 — — the health band follows the score Given  a health score of 78 When   the rail renders Then   the health ring is drawn in the partial tone, not the green one",
        "CASE-3 — — an older map renders no ring Given  a map carrying neither a health nor a design record When   the rail renders Then   no gauge is present in the output",
        "CASE-4 — — only the verdict is a control Given  both records are present When   the rail renders Then   the design row is marked static and carries no click target, while the health row does",
        "CASE-5 — — the labels follow the interface language Given  the interface language is Romanian When   a label and a caption are translated Then   both come back in Romanian, numbers interpolated unchanged"
      ],
      "accept": "CASE-1 — the rings show the numbers the engine emitted\n  Given  a map carrying a health record of 39 of 50 and a design record of 7 of 30\n  When   the rail renders\n  Then   both scores and both fractions appear as given, with nothing recomputed\n\nCASE-2 — the health band follows the score\n  Given  a health score of 78\n  When   the rail renders\n  Then   the health ring is drawn in the partial tone, not the green one\n\nCASE-3 — an older map renders no ring\n  Given  a map carrying neither a health nor a design record\n  When   the rail renders\n  Then   no gauge is present in the output\n\nCASE-4 — only the verdict is a control\n  Given  both records are present\n  When   the rail renders\n  Then   the design row is marked static and carries no click target, while the health row does\n\nCASE-5 — the labels follow the interface language\n  Given  the interface language is Romanian\n  When   a label and a caption are translated\n  Then   both come back in Romanian, numbers interpolated unchanged",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-VIEWER-007"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "tested-by",
          "loc": "app/scripts/ssr-smoke.jsx:3"
        },
        {
          "role": "implements",
          "loc": "app/src/App.jsx:32"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/data.js:232"
        },
        {
          "role": "implements",
          "loc": "app/src/lib/data.js:249"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 5,
      "covered": 5
    },
    {
      "id": "REQ-VIEWER-977",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The advisory design tab",
      "intent": "The rail already showed a design score, and a reader who wanted to know which shapes cost the missing points had to leave the viewer for a terminal. The candidates now travel in the map, so the Problems screen lists them — in a tab of their own, because they are the only rows here that are about a file rather than a requirement, and the only ones that gate nothing.",
      "contract": [
        "`ProblemsView` offers a `Design` tab, labelled with the number of candidates the map carries, and offers it only when the map carries at least one.",
        "The tab groups candidates by pillar, shows each one's kind, name, detail and `file:line`, and prints each kind's advice once per group — the same shape the CLI prints.",
        "Design candidates are absent from the `All` tab and from every severity count, so the inbox keeps reporting what is open about the corpus.",
        "The tab states that a candidate is advisory and never enters the gate, so a reader does not mistake the list for a build failure.",
        "A map written before the engine carried candidates leaves the tab unoffered rather than rendering an empty one."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — the tab appears with its count Given  a map whose `design` record carries two candidates When   `ProblemsView` renders Then   a `Design` tab is offered showing the count 2",
        "CASE-2 — — an older map offers nothing Given  a map whose `design` record carries a score but no candidates When   `ProblemsView` renders Then   no `Design` tab is offered",
        "CASE-3 — — the inbox is unchanged Given  a map carrying both corpus signals and design candidates When   the open signals are computed Then   no computed row carries a design severity, and the `All` count is the corpus count alone"
      ],
      "accept": "CASE-1 — the tab appears with its count\n  Given  a map whose `design` record carries two candidates\n  When   `ProblemsView` renders\n  Then   a `Design` tab is offered showing the count 2\n\nCASE-2 — an older map offers nothing\n  Given  a map whose `design` record carries a score but no candidates\n  When   `ProblemsView` renders\n  Then   no `Design` tab is offered\n\nCASE-3 — the inbox is unchanged\n  Given  a map carrying both corpus signals and design candidates\n  When   the open signals are computed\n  Then   no computed row carries a design severity, and the `All` count is the corpus count alone",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-VIEWER-007"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "app/src/views/ProblemsView.jsx:3"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [
        {
          "signal": "untested",
          "advice": "Implemented but no `tested-by` member: write an acceptance test and tag it `# tested-by: <ID>`, or set `test_exempt: <reason>` in the frontmatter to acknowledge it intentionally and silence this signal."
        }
      ],
      "clauses": 3,
      "covered": 3
    },
    {
      "id": "ARCH-VLEVEL-037",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Verification levels",
      "intent": "A `tested-by:` link says a test exists, not how close that test sits to the code — a whole-system run and a single-function check look identical to the tool. This lets a tag name its level, and lets a stakeholder need point at the evidence it was met. Without it the engine models only the left side of the V: you can ask what a requirement depends on, but not \"has anyone ever shown this need was actually satisfied?\".",
      "contract": [
        "A `tested-by:` tag may end with a verification level (`@unit`, `@integration`, `@system`), invisible to the ordinary tag parser so an older engine still resolves the id. [[REQ-VLEVEL-944]]",
        "The engine collects, per requirement, each level it is verified at with the `file:line` locations that declare it, skipping backticked and quoted examples so prose about tagging is never mistaken for coverage. [[REQ-VLEVEL-945]]",
        "The gate warns, warn-only, when a confirmed need carries no `validated-against:` member (once the repo has opted in) and when a confirmed `bus` requirement's only levelled coverage is `@system`. [[REQ-VLEVEL-946]]"
      ],
      "verify": [],
      "notes": [
        "The level suffix is invisible to the ordinary tag parser, so an older vendored engine reads a levelled tag, resolves the id, and ignores the level — the annotation is backwards compatible.",
        "The level is author-declared, not measured: nothing checks that a test tagged `@unit` really isolates one unit. Same lexical-trust limitation as `tested-by` itself (ARCH-TESTLINK-018).",
        "`validated-against:` had sat unused in the role list since the beginning; this requirement is the first consumer of it."
      ],
      "current_impl": [
        "`TEST_LEVELS`, `TEST_LEVEL_RE` and `scan_test_levels` in `reqmap.py` collect `{cap: {level: [(file, line)]}}`, masking Python strings and backticked spans first. `cmd_check` computes `any_validation` from the member roles and `level_cover` from the scan, then appends the two warn-only findings. `cmd_show` takes the level map as an optional fourth argument and annotates each member line.",
        "-------------------"
      ],
      "acc": [
        "CASE-1 — Given  tags declaring two levels for one id, a two-id list at `@integration`, one tag with no level and one carrying `@wrong` When   the level scan runs Then   the first id reports both levels with their `file:line`, both listed ids report `integration`, and the unlevelled and unrecognised ones are absent",
        "CASE-2 — Given  a `tested-by:` tag carrying a level suffix When   the ordinary tag scan reads it Then   it resolves to the same member as an unlevelled tag, so an engine that predates levels reads the id and ignores the suffix",
        "CASE-3 — Given  levelled tags that are not real claims — one inside backticks, one in a Python docstring, one in a string literal — alongside one real tag in a comment When   the level scan runs Then   only the real tag produces a level entry",
        "CASE-4 — Given  two confirmed needs, one carrying a `validated-against:` tag and one carrying none When   the gate runs Then   it warns about the unvalidated need only",
        "CASE-5 — Given  a confirmed need in a repo with no `validated-against:` tag anywhere When   the gate runs Then   it says nothing about validation",
        "CASE-6 — Given  a confirmed `bus` requirement whose only levelled link is `@system` When   the gate runs Then   it warns; adding a `@unit` link, dropping to an unlevelled link, or moving to the `feature` layer each silences it",
        "CASE-7 — Given  a member whose `tested-by:` tag carries `@integration` When   `show` runs Then   `@integration` appears beside that member; with no level data the output is unchanged"
      ],
      "accept": "CASE-1\n  Given  tags declaring two levels for one id, a two-id list at `@integration`, one tag\n         with no level and one carrying `@wrong`\n  When   the level scan runs\n  Then   the first id reports both levels with their `file:line`, both listed ids report\n         `integration`, and the unlevelled and unrecognised ones are absent\nCASE-2\n  Given  a `tested-by:` tag carrying a level suffix\n  When   the ordinary tag scan reads it\n  Then   it resolves to the same member as an unlevelled tag, so an engine that predates\n         levels reads the id and ignores the suffix\nCASE-3\n  Given  levelled tags that are not real claims — one inside backticks, one in a Python\n         docstring, one in a string literal — alongside one real tag in a comment\n  When   the level scan runs\n  Then   only the real tag produces a level entry\nCASE-4\n  Given  two confirmed needs, one carrying a `validated-against:` tag and one carrying none\n  When   the gate runs\n  Then   it warns about the unvalidated need only\nCASE-5\n  Given  a confirmed need in a repo with no `validated-against:` tag anywhere\n  When   the gate runs\n  Then   it says nothing about validation\nCASE-6\n  Given  a confirmed `bus` requirement whose only levelled link is `@system`\n  When   the gate runs\n  Then   it warns; adding a `@unit` link, dropping to an unlevelled link, or moving to the\n         `feature` layer each silences it\nCASE-7\n  Given  a member whose `tested-by:` tag carries `@integration`\n  When   `show` runs\n  Then   `@integration` appears beside that member; with no level data the output is unchanged",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-SCAN-002",
        "ARCH-CHECK-006"
      ],
      "used_by": [
        "ARCH-VRUNGS-054"
      ],
      "satisfies": [
        "SYS-VMODEL-107"
      ],
      "satisfied_by": [
        "REQ-VLEVEL-944",
        "REQ-VLEVEL-945",
        "REQ-VLEVEL-946"
      ],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1688"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2560"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2573"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5033"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:273"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:287"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:297"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:307"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:318"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:329"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:479"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:490"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:502"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:520"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:541"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3268"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:3277"
        },
        {
          "role": "tested-by",
          "loc": "scripts/test_cross_tool.py:84"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "should-have",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Niveluri de verificare",
          "intent": "O legătură `tested-by:` spune că un test există, nu cât de aproape stă acel test de cod — o rulare de sistem întreg și o verificare de funcție unică arată identic pentru tool. Aceasta permite unei etichete să își numească nivelul, și permite unei nevoi a unui stakeholder să indice dovada că a fost îndeplinită. Fără aceasta motorul modelează doar partea stângă a V-ului: poți întreba de ce depinde o cerință, dar nu „a arătat cineva vreodată că această nevoie a fost cu adevărat satisfăcută?”.",
          "contract": "> O legătură `tested-by:` spune că un test există, nu cât de aproape stă acel test de cod\n> — o rulare de sistem întreg și o verificare de funcție unică arată identic pentru tool.\n> Aceasta permite unei etichete să își numească nivelul, și permite unei nevoi a unui\n> stakeholder să indice dovada că a fost îndeplinită. Fără aceasta motorul modelează doar\n> partea stângă a V-ului: poți întreba de ce depinde o cerință, dar nu „a arătat cineva\n> vreodată că această nevoie a fost cu adevărat satisfăcută?”.\n\nFiecare punct de mai jos este obligatoriu.\n- O etichetă `tested-by:` poate să se termine cu un nivel de verificare (`@unit`, `@integration`, `@system`), invizibil pentru parserul obișnuit de etichete astfel încât un motor mai vechi tot rezolvă id-ul. [[REQ-VLEVEL-944]]\n- Motorul colectează, per cerință, fiecare nivel la care este verificată, împreună cu locațiile `file:line` care îl declară, omițând exemplele din backtick-uri și din ghilimele astfel încât proza despre etichetare nu este niciodată confundată cu acoperirea. [[REQ-VLEVEL-945]]\n- Gate avertizează, doar avertisment, când o need confirmed nu poartă niciun membru `validated-against:` (odată ce repo-ul a optat pentru asta) și când singura acoperire nivelată a unei cerințe `bus` confirmed este `@system`. [[REQ-VLEVEL-946]]",
          "acceptance": "CASE-1\n  Given  etichete care declară două niveluri pentru un id, o listă de două id-uri la\n         `@integration`, o etichetă fără niciun nivel și una purtând `@wrong`\n  When   rulează scanarea de nivel\n  Then   primul id raportează ambele niveluri cu `file:line`-urile lor, ambele id-uri listate\n         raportează `integration`, iar cele nenivelate și nerecunoscute sunt absente\nCASE-2\n  Given  o etichetă `tested-by:` care poartă un sufix de nivel\n  When   scanarea obișnuită de etichete o citește\n  Then   se rezolvă la același membru ca o etichetă nenivelată, astfel încât un motor\n         anterior nivelurilor tot citește id-ul și ignoră sufixul\nCASE-3\n  Given  etichete nivelate care nu sunt afirmații reale — una în interiorul unor\n         backtick-uri, una într-un docstring Python, una într-un literal string — alături\n         de o etichetă reală într-un comentariu\n  When   rulează scanarea de nivel\n  Then   doar eticheta reală produce o intrare de nivel\nCASE-4\n  Given  două needs confirmed, una purtând o etichetă `validated-against:` și una fără nicio\n         etichetă\n  When   rulează gate\n  Then   avertizează doar despre need-ul nevalidat\nCASE-5\n  Given  o need confirmed într-un repo fără nicio etichetă `validated-against:` nicăieri\n  When   rulează gate\n  Then   nu spune nimic despre validare\nCASE-6\n  Given  o cerință `bus` confirmed a cărei singură legătură nivelată este `@system`\n  When   rulează gate\n  Then   avertizează; adăugarea unei legături `@unit`, coborârea la o legătură nenivelată,\n         sau mutarea la stratul `feature` o face pe fiecare să tacă\nCASE-7\n  Given  un membru a cărui etichetă `tested-by:` poartă `@integration`\n  When   rulează `show`\n  Then   `@integration` apare lângă acel membru; fără date de nivel ieșirea rămâne neschimbată"
        }
      }
    },
    {
      "id": "REQ-VLEVEL-944",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "A tested-by tag may carry a level suffix",
      "intent": "`@unit`, `@integration` and `@system` let a `tested-by:` tag say not just that a test exists but how close it sits to the code — a whole-system run and a single-function check otherwise look identical to the tool. The suffix is invisible to the plain tag parser, so an engine that predates levels still resolves the id and simply ignores it.",
      "contract": [
        "A `tested-by:` tag may end with a verification level: `@unit`, `@integration` or `@system`.",
        "A level written on a tag applies to every id in that tag's comma-separated list.",
        "A `tested-by:` tag carrying no level, or an unrecognised one, stays an ordinary member link."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — scan_test_levels recognizes all three level suffixes Given  `# tested-by: REQ-A-001 @unit`, `# tested-by: REQ-A-001 @system`, and `# tested-by: REQ-B-002 @integration` in one file When   `scan_test_levels` runs Then   `REQ-A-001` reports levels `{\"unit\", \"system\"}` and `REQ-B-002` reports `{\"integration\"}`",
        "CASE-2 — — one level suffix applies to every id in the list Given  `# tested-by: REQ-A-001, REQ-B-002 @integration` When   `scan_test_levels` runs Then   both `REQ-A-001` and `REQ-B-002` report level `integration`",
        "CASE-3 — — a tag with no level, or an unrecognised one, still resolves as a plain member Given  `# tested-by: REQ-D-004 @wrong` (an unknown level suffix) When   `_findall_tags` parses the line Then   it returns `[(\"tested-by\", \"REQ-D-004\")]`, same as an unlevelled tag — and `scan_test_levels` collects no level for `REQ-D-004` --------------------"
      ],
      "accept": "CASE-1 — scan_test_levels recognizes all three level suffixes\n  Given  `# tested-by: REQ-A-001 @unit`, `# tested-by: REQ-A-001 @system`, and\n         `# tested-by: REQ-B-002 @integration` in one file\n  When   `scan_test_levels` runs\n  Then   `REQ-A-001` reports levels `{\"unit\", \"system\"}` and `REQ-B-002` reports\n         `{\"integration\"}`\n\nCASE-2 — one level suffix applies to every id in the list\n  Given  `# tested-by: REQ-A-001, REQ-B-002 @integration`\n  When   `scan_test_levels` runs\n  Then   both `REQ-A-001` and `REQ-B-002` report level `integration`\n\nCASE-3 — a tag with no level, or an unrecognised one, still resolves as a plain member\n  Given  `# tested-by: REQ-D-004 @wrong` (an unknown level suffix)\n  When   `_findall_tags` parses the line\n  Then   it returns `[(\"tested-by\", \"REQ-D-004\")]`, same as an unlevelled tag — and\n         `scan_test_levels` collects no level for `REQ-D-004`\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-VLEVEL-037"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:55"
        },
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1688"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:479"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Un tag tested-by poate purta un sufix de nivel",
          "intent": "`@unit`, `@integration` și `@system` permit unui tag `tested-by:` să afirme nu doar că există un test, ci și cât de aproape stă de cod — o rulare a întregului sistem și o verificare a unei singure funcții arată altfel identic pentru unealtă. Sufixul este invizibil pentru parserul simplu de tag, astfel un motor dinainte de niveluri încă rezolvă id-ul și pur și simplu îl ignoră.",
          "contract": "> `@unit`, `@integration` și `@system` permit unui tag `tested-by:` să afirme nu doar că\n> există un test, ci și cât de aproape stă de cod — o rulare a întregului sistem și o\n> verificare a unei singure funcții arată altfel identic pentru unealtă. Sufixul este\n> invizibil pentru parserul simplu de tag, astfel un motor dinainte de niveluri încă rezolvă\n> id-ul și pur și simplu îl ignoră.\n\nFiecare punct de mai jos este obligatoriu.\n- Un tag `tested-by:` poate se termina cu un nivel de verificare: `@unit`, `@integration` sau `@system`.\n- Un nivel scris pe un tag se aplică fiecărui id din lista separată prin virgule a acelui tag.\n- Un tag `tested-by:` fără niciun nivel, sau cu unul nerecunoscut, rămâne o legătură de membru obișnuită.",
          "acceptance": "CASE-1 — scan_test_levels recunoaște toate cele trei sufixe de nivel\n  Given  `# tested-by: REQ-A-001 @unit`, `# tested-by: REQ-A-001 @system`, și\n         `# tested-by: REQ-B-002 @integration` într-un fișier\n  When   `scan_test_levels` rulează\n  Then   `REQ-A-001` raportează nivelurile `{\"unit\", \"system\"}`, iar `REQ-B-002` raportează\n         `{\"integration\"}`\n\nCASE-2 — un sufix de nivel se aplică fiecărui id din listă\n  Given  `# tested-by: REQ-A-001, REQ-B-002 @integration`\n  When   `scan_test_levels` rulează\n  Then   atât `REQ-A-001` cât și `REQ-B-002` raportează nivelul `integration`\n\nCASE-3 — un tag fără nivel, sau cu unul nerecunoscut, încă se rezolvă ca membru simplu\n  Given  `# tested-by: REQ-D-004 @wrong` (un sufix de nivel necunoscut)\n  When   `_findall_tags` parsează linia\n  Then   returnează `[(\"tested-by\", \"REQ-D-004\")]`, la fel ca un tag fără nivel — iar\n         `scan_test_levels` nu colectează niciun nivel pentru `REQ-D-004`\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-VLEVEL-945",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "scan_test_levels collects real levels, not documented examples",
      "intent": "`scan_test_levels` runs as its own pass, separate from the ordinary member scan, so the member shape every existing consumer reads never changes. It masks backticked spans and, in Python files, string literals and docstrings — a line that only shows someone how to write a levelled tag must never be counted as if it were one.",
      "contract": [
        "The engine reports, per requirement, each level it is verified at with the `file:line` locations that declare it.",
        "The level scan stays separate from the member scan, so the member shape every consumer reads is unchanged.",
        "The engine skips a levelled tag written inside backticks, so a documented example never counts as real coverage.",
        "In a Python file the engine also skips a levelled tag inside a string literal or a docstring. Prose about how to tag is not a claim of coverage."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — scan_test_levels records the file:line that declares each level Given  `# tested-by: REQ-B-002 @integration` as line 3 of `t_one.py` When   `scan_test_levels` runs Then   `got[\"REQ-B-002\"][\"integration\"]` equals `[(\"t_one.py\", 3)]`",
        "CASE-2 — — a level suffix does not change the ordinary member tuple shape Given  `# tested-by: REQ-A-001 @unit` When   `_findall_tags` (the member scan) parses it Then   it returns the plain `[(\"tested-by\", \"REQ-A-001\")]` two-tuple, with no level field grafted on",
        "CASE-3 — — a backticked levelled-tag example does not count as coverage Given  a line \"write it as `# tested-by: REQ-A-001 @unit` in your test\" plus a real `# tested-by: REQ-B-002 @unit` tag When   `scan_test_levels` runs Then   `REQ-A-001` is absent from the result and `REQ-B-002` reports `{\"unit\"}`",
        "CASE-4 — — a levelled tag inside a docstring or string literal is not coverage Given  a `.py` file with `REQ-DOC-001 @unit` inside a docstring, `REQ-STR-001 @unit` inside a string literal, and a real `# tested-by: REQ-REAL-001 @unit` comment When   `scan_test_levels` runs Then   `REQ-DOC-001` and `REQ-STR-001` are absent; `REQ-REAL-001` reports `{\"unit\"}` --------------------"
      ],
      "accept": "CASE-1 — scan_test_levels records the file:line that declares each level\n  Given  `# tested-by: REQ-B-002 @integration` as line 3 of `t_one.py`\n  When   `scan_test_levels` runs\n  Then   `got[\"REQ-B-002\"][\"integration\"]` equals `[(\"t_one.py\", 3)]`\n\nCASE-2 — a level suffix does not change the ordinary member tuple shape\n  Given  `# tested-by: REQ-A-001 @unit`\n  When   `_findall_tags` (the member scan) parses it\n  Then   it returns the plain `[(\"tested-by\", \"REQ-A-001\")]` two-tuple, with no level\n         field grafted on\n\nCASE-3 — a backticked levelled-tag example does not count as coverage\n  Given  a line \"write it as `# tested-by: REQ-A-001 @unit` in your test\" plus a real\n         `# tested-by: REQ-B-002 @unit` tag\n  When   `scan_test_levels` runs\n  Then   `REQ-A-001` is absent from the result and `REQ-B-002` reports `{\"unit\"}`\n\nCASE-4 — a levelled tag inside a docstring or string literal is not coverage\n  Given  a `.py` file with `REQ-DOC-001 @unit` inside a docstring, `REQ-STR-001 @unit`\n         inside a string literal, and a real `# tested-by: REQ-REAL-001 @unit` comment\n  When   `scan_test_levels` runs\n  Then   `REQ-DOC-001` and `REQ-STR-001` are absent; `REQ-REAL-001` reports `{\"unit\"}`\n\n\n--------------------",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-VLEVEL-037"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:1688"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:520"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 4,
      "covered": 4,
      "i18n": {
        "ro": {
          "title": "scan_test_levels colectează niveluri reale, nu exemple documentate",
          "intent": "`scan_test_levels` rulează ca propria lui trecere, separată de scanarea obișnuită de membri, astfel forma de membru pe care o citește fiecare consumator existent nu se schimbă niciodată. Maschează span-uri backticked și, în fișierele Python, literale de șir și docstring-uri — o linie care doar arată cuiva cum să scrie un tag cu nivel nu trebuie niciodată numărată ca și cum ar fi unul real.",
          "contract": "> `scan_test_levels` rulează ca propria lui trecere, separată de scanarea obișnuită de\n> membri, astfel forma de membru pe care o citește fiecare consumator existent nu se schimbă\n> niciodată. Maschează span-uri backticked și, în fișierele Python, literale de șir și\n> docstring-uri — o linie care doar arată cuiva cum să scrie un tag cu nivel nu trebuie\n> niciodată numărată ca și cum ar fi unul real.\n\nFiecare punct de mai jos este obligatoriu.\n- Motorul raportează, pentru fiecare cerință, fiecare nivel la care este verificată cu locațiile\n  `file:line` care îl declară.\n- Scanarea de nivel rămâne separată de scanarea de membri, astfel forma de membru pe care o citește\n  fiecare consumator rămâne neschimbată.\n- Motorul omite un tag cu nivel scris în interiorul backtick-urilor, astfel un exemplu documentat nu\n  contează niciodată ca acoperire reală.\n- Într-un fișier Python motorul de asemenea omite un tag cu nivel din interiorul unui literal de șir sau al unui\n  docstring. Proza despre cum să etichetezi nu este o afirmație de acoperire.",
          "acceptance": "CASE-1 — scan_test_levels înregistrează file:line-ul care declară fiecare nivel\n  Given  `# tested-by: REQ-B-002 @integration` ca linia 3 din `t_one.py`\n  When   `scan_test_levels` rulează\n  Then   `got[\"REQ-B-002\"][\"integration\"]` este egal cu `[(\"t_one.py\", 3)]`\n\nCASE-2 — un sufix de nivel nu schimbă forma tuplului obișnuit de membru\n  Given  `# tested-by: REQ-A-001 @unit`\n  When   `_findall_tags` (scanarea de membri) îl parsează\n  Then   returnează tuplul simplu de doi `[(\"tested-by\", \"REQ-A-001\")]`, fără niciun câmp\n         de nivel adăugat\n\nCASE-3 — un exemplu backticked de tag cu nivel nu contează ca acoperire\n  Given  o linie \"write it as `# tested-by: REQ-A-001 @unit` in your test\" plus un\n         tag real `# tested-by: REQ-B-002 @unit`\n  When   `scan_test_levels` rulează\n  Then   `REQ-A-001` este absent din rezultat, iar `REQ-B-002` raportează `{\"unit\"}`\n\nCASE-4 — un tag cu nivel din interiorul unui docstring sau literal de șir nu este acoperire\n  Given  un fișier `.py` cu `REQ-DOC-001 @unit` în interiorul unui docstring, `REQ-STR-001 @unit`\n         în interiorul unui literal de șir, și un comentariu real `# tested-by: REQ-REAL-001 @unit`\n  When   `scan_test_levels` rulează\n  Then   `REQ-DOC-001` și `REQ-STR-001` sunt absente; `REQ-REAL-001` raportează `{\"unit\"}`\n\n\n--------------------"
        }
      }
    },
    {
      "id": "REQ-VLEVEL-946",
      "layer": "feature",
      "level": "code",
      "status": "confirmed",
      "area": "REQ",
      "title": "The gate reads levels: unvalidated needs, system-only bus code",
      "intent": "Levels only matter once the gate reads them. It warns on a confirmed need with no `validated-against:` evidence (opt-in: silent until the repo carries at least one such tag anywhere) and on a confirmed `bus` requirement covered only end-to-end — foundation code tested only at `@system` is slow to run and hard to localise a failure in. Both stay warn-only and both are silent on a requirement with no levelled link at all.",
      "contract": [
        "The gate warns when a confirmed `need` carries no `validated-against:` member.",
        "The gate holds that need warning back until the repo carries at least one `validated-against:` tag, so adopting the role is opt-in.",
        "The gate warns when a confirmed `bus` requirement's levelled `tested-by:` links are all `@system`.",
        "The gate judges no requirement that has no levelled link, because an unlevelled link is evidence of neither level.",
        "The gate applies the level-fit rule to the `bus` layer only. A feature may legitimately be covered end to end.",
        "Both rules are warn-only. Neither changes the gate's exit code.",
        "How `show` renders a levelled member is [[ARCH-SHOW-015]]'s contract, not restated here.",
        "`show` prints a member whose tag carries no level with no level marker."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — an unvalidated confirmed need is named once the repo has opted in Given  `NEED-A-001` carrying a `validated-against:` tag and `NEED-B-002` carrying none, both confirmed needs When   `gate` runs Then   its output names `NEED-B-002` alongside \"validated-against\"",
        "CASE-2 — — no validated-against tag anywhere keeps the rule silent Given  a confirmed need and no `validated-against:` tag anywhere in the tree When   `gate` runs Then   its output contains no mention of \"validated-against\"",
        "CASE-3 — — a bus requirement verified only at @system level warns Given  a confirmed `layer: bus` requirement whose only levelled `tested-by:` link is `@system` When   `gate` runs Then   its output contains \"@system\"",
        "CASE-4 — — a bus requirement with only an unlevelled tested-by link is never judged Given  a confirmed `layer: bus` requirement whose only `tested-by:` link carries no level suffix When   `gate` runs Then   its output contains no \"verified only at @system\" finding",
        "CASE-5 — — a feature requirement verified only at @system stays silent Given  a confirmed `layer: feature` requirement whose only levelled `tested-by:` link is `@system` When   `gate` runs Then   its output contains no \"verified only at @system\" finding",
        "CASE-6 — — the unvalidated-need and system-only-bus findings never bump the exit code Given  a confirmed need with no `validated-against:` tag, and separately a confirmed `bus` requirement verified only at `@system`, each in an opted-in repo When   `gate` runs on each Then   both runs warn and both return exit code 0",
        "CASE-7 — — show prints an unlevelled member with no level marker Given  a `tested-by` member at `t.py:2` and no `levels` argument passed to `cmd_show` When   `show REQ-X-001` runs with the old 3-argument call Then   its output contains \"t.py:2\" and no \"@\" marker after the members section"
      ],
      "accept": "CASE-1 — an unvalidated confirmed need is named once the repo has opted in\n  Given  `NEED-A-001` carrying a `validated-against:` tag and `NEED-B-002` carrying none,\n         both confirmed needs\n  When   `gate` runs\n  Then   its output names `NEED-B-002` alongside \"validated-against\"\n\nCASE-2 — no validated-against tag anywhere keeps the rule silent\n  Given  a confirmed need and no `validated-against:` tag anywhere in the tree\n  When   `gate` runs\n  Then   its output contains no mention of \"validated-against\"\n\nCASE-3 — a bus requirement verified only at @system level warns\n  Given  a confirmed `layer: bus` requirement whose only levelled `tested-by:` link is\n         `@system`\n  When   `gate` runs\n  Then   its output contains \"@system\"\n\nCASE-4 — a bus requirement with only an unlevelled tested-by link is never judged\n  Given  a confirmed `layer: bus` requirement whose only `tested-by:` link carries no\n         level suffix\n  When   `gate` runs\n  Then   its output contains no \"verified only at @system\" finding\n\nCASE-5 — a feature requirement verified only at @system stays silent\n  Given  a confirmed `layer: feature` requirement whose only levelled `tested-by:` link is\n         `@system`\n  When   `gate` runs\n  Then   its output contains no \"verified only at @system\" finding\n\nCASE-6 — the unvalidated-need and system-only-bus findings never bump the exit code\n  Given  a confirmed need with no `validated-against:` tag, and separately a confirmed\n         `bus` requirement verified only at `@system`, each in an opted-in repo\n  When   `gate` runs on each\n  Then   both runs warn and both return exit code 0\n\nCASE-7 — show prints an unlevelled member with no level marker\n  Given  a `tested-by` member at `t.py:2` and no `levels` argument passed to `cmd_show`\n  When   `show REQ-X-001` runs with the old 3-argument call\n  Then   its output contains \"t.py:2\" and no \"@\" marker after the members section",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "ARCH-VLEVEL-037"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:5033"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:273"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:10042"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "",
      "risks": [],
      "clauses": 7,
      "covered": 7,
      "i18n": {
        "ro": {
          "title": "Gate-ul citește nivelurile: need-uri nevalidate, cod bus doar-la-sistem",
          "intent": "Nivelurile contează doar odată ce gate-ul le citește. Avertizează pe o need confirmată fără nicio dovadă `validated-against:` (opt-in: tăcut până când repo-ul poartă cel puțin un astfel de tag oriunde) și pe o cerință `bus` confirmată acoperită doar end-to-end — codul de fundație testat doar la `@system` este lent de rulat și greu de localizat un eșec în el. Ambele rămân doar-avertisment și ambele sunt tăcute pe o cerință fără nicio legătură cu nivel deloc.",
          "contract": "> Nivelurile contează doar odată ce gate-ul le citește. Avertizează pe o need confirmată\n> fără nicio dovadă `validated-against:` (opt-in: tăcut până când repo-ul poartă cel puțin un\n> astfel de tag oriunde) și pe o cerință `bus` confirmată acoperită doar end-to-end —\n> codul de fundație testat doar la `@system` este lent de rulat și greu de localizat un eșec\n> în el. Ambele rămân doar-avertisment și ambele sunt tăcute pe o cerință fără nicio legătură\n> cu nivel deloc.\n\nFiecare punct de mai jos este obligatoriu.\n- Gate-ul avertizează când o `need` confirmată nu poartă niciun membru `validated-against:`.\n- Gate-ul ține în loc acel avertisment de need până când repo-ul poartă cel puțin un tag\n  `validated-against:`, astfel adoptarea rolului este opt-in.\n- Gate-ul avertizează când legăturile `tested-by:` cu nivel ale unei cerințe `bus` confirmate sunt toate\n  `@system`.\n- Gate-ul nu judecă nicio cerință care nu are nicio legătură cu nivel, pentru că o legătură fără nivel nu este\n  dovadă pentru niciun nivel.\n- Gate-ul aplică regula de potrivire-a-nivelului doar layer-ului `bus`. O feature poate fi legitim\n  acoperită end-to-end.\n- Ambele reguli sunt doar-avertisment. Niciuna nu schimbă codul de ieșire al gate-ului.\n- Cum randează `show` un membru cu nivel este contractul lui [[ARCH-SHOW-015]], nu repetat aici.\n- `show` afișează un membru al cărui tag nu poartă niciun nivel fără niciun marker de nivel.",
          "acceptance": "CASE-1 — o need confirmată nevalidată este numită odată ce repo-ul a optat-in\n  Given  `NEED-A-001` purtând un tag `validated-against:` și `NEED-B-002` purtând niciunul,\n         ambele need-uri confirmate\n  When   `gate` rulează\n  Then   ieșirea lui numește `NEED-B-002` alături de \"validated-against\"\n\nCASE-2 — niciun tag validated-against nicăieri ține regula tăcută\n  Given  o need confirmată și niciun tag `validated-against:` nicăieri în arbore\n  When   `gate` rulează\n  Then   ieșirea lui nu conține nicio mențiune de \"validated-against\"\n\nCASE-3 — o cerință bus verificată doar la nivelul @system avertizează\n  Given  o cerință `layer: bus` confirmată a cărei unică legătură `tested-by:` cu nivel este\n         `@system`\n  When   `gate` rulează\n  Then   ieșirea lui conține \"@system\"\n\nCASE-4 — o cerință bus cu doar o legătură tested-by fără nivel nu este niciodată judecată\n  Given  o cerință `layer: bus` confirmată a cărei unică legătură `tested-by:` nu poartă niciun\n         sufix de nivel\n  When   `gate` rulează\n  Then   ieșirea lui nu conține nicio constatare \"verified only at @system\"\n\nCASE-5 — o cerință feature verificată doar la @system rămâne tăcută\n  Given  o cerință `layer: feature` confirmată a cărei unică legătură `tested-by:` cu nivel este\n         `@system`\n  When   `gate` rulează\n  Then   ieșirea lui nu conține nicio constatare \"verified only at @system\"\n\nCASE-6 — constatările de need-nevalidată și bus-doar-system nu ridică niciodată codul de ieșire\n  Given  o need confirmată fără niciun tag `validated-against:`, și separat o cerință `bus`\n         confirmată verificată doar la `@system`, fiecare într-un repo opt-in\n  When   `gate` rulează pe fiecare\n  Then   ambele rulări avertizează, iar ambele returnează codul de ieșire 0\n\nCASE-7 — show afișează un membru fără nivel fără niciun marker de nivel\n  Given  un membru `tested-by` la `t.py:2` și niciun argument `levels` pasat la `cmd_show`\n  When   `show REQ-X-001` rulează cu vechiul apel cu 3 argumente\n  Then   ieșirea lui conține \"t.py:2\" și niciun marker \"@\" după secțiunea de membri"
        }
      }
    },
    {
      "id": "ARCH-VRUNGS-054",
      "layer": "feature",
      "level": "architecture",
      "status": "confirmed",
      "area": "ARCH",
      "title": "Level-to-verification correspondence",
      "intent": "A level that no verification level answers to is a label; pairing them is what makes the V a V rather than two lists.",
      "contract": [
        "As someone who has declared both a requirement's specification level and the level its tests run at, I want the gate to tell me when the two do not correspond, so that a system requirement answered only by unit tests stops looking verified."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "CASE-1 — — a level answered by the wrong depth of test warns Given  a confirmed requirement carrying `level: system` whose only levelled `tested-by:` link is `@unit` When   the gate runs Then   it warns, naming the level the requirement has and the `@system` link it lacks",
        "CASE-2 — — the paired level is silent Given  a confirmed requirement carrying `level: architecture` with an `@integration` `tested-by:` link — the pairing is `system` to `@system`, `architecture` to `@integration`, `code` to `@unit` When   the gate runs Then   it says nothing about that requirement, and the exit code is unchanged",
        "CASE-3 — — an unlevelled or undeclared requirement is never judged Given  either a requirement with no levelled `tested-by:` link at all, or one with no `level:` declared in its frontmatter When   the gate runs Then   it never reports that requirement as answered at the wrong depth"
      ],
      "accept": "CASE-1 — a level answered by the wrong depth of test warns\n  Given  a confirmed requirement carrying `level: system` whose only levelled `tested-by:`\n         link is `@unit`\n  When   the gate runs\n  Then   it warns, naming the level the requirement has and the `@system` link it lacks\n\nCASE-2 — the paired level is silent\n  Given  a confirmed requirement carrying `level: architecture` with an `@integration`\n         `tested-by:` link — the pairing is `system` to `@system`, `architecture` to\n         `@integration`, `code` to `@unit`\n  When   the gate runs\n  Then   it says nothing about that requirement, and the exit code is unchanged\n\nCASE-3 — an unlevelled or undeclared requirement is never judged\n  Given  either a requirement with no levelled `tested-by:` link at all, or one with no\n         `level:` declared in its frontmatter\n  When   the gate runs\n  Then   it never reports that requirement as answered at the wrong depth",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [
        "ARCH-LEVEL-051",
        "ARCH-VLEVEL-037",
        "ARCH-CHECK-006"
      ],
      "used_by": [],
      "satisfies": [
        "SYS-VMODEL-107"
      ],
      "satisfied_by": [],
      "members": [
        {
          "role": "implements",
          "loc": "plugin/scripts/reqmap.py:2583"
        },
        {
          "role": "tested-by",
          "loc": "plugin/scripts/test_reqmap.py:7436"
        }
      ],
      "test_exempt": null,
      "milestone": null,
      "priority": "could-have",
      "risks": [],
      "clauses": 3,
      "covered": 3,
      "i18n": {
        "ro": {
          "title": "Corespondența nivel-la-verificare",
          "intent": "Un nivel căruia niciun nivel de verificare nu îi răspunde este doar o etichetă; împerecherea lor este ceea ce face din V un V, nu doar două liste.",
          "contract": "> Un nivel căruia niciun nivel de verificare nu îi răspunde este doar o etichetă; împerecherea lor este ceea ce face din V un V, nu doar două liste.\n\nFiecare punct de mai jos este obligatoriu.\n- Ca cineva care a declarat atât nivelul de specificație al unei cerințe, cât și nivelul la care rulează testele ei, vreau ca gate să îmi spună când cele două nu corespund, astfel încât o cerință de sistem răspunsă doar de teste unitare să nu mai pară verificată.",
          "acceptance": "CASE-1 — un nivel răspuns de o adâncime greșită de test avertizează\n  Given  o cerință confirmed care poartă `level: system` a cărei singură legătură nivelată\n         `tested-by:` este `@unit`\n  When   rulează gate\n  Then   avertizează, numind nivelul pe care îl are cerința și legătura `@system` care îi lipsește\n\nCASE-2 — nivelul împerecheat este tăcut\n  Given  o cerință confirmed care poartă `level: architecture` cu o legătură `@integration`\n         de tip `tested-by:` — împerecherea este `system` cu `@system`, `architecture` cu\n         `@integration`, `code` cu `@unit`\n  When   rulează gate\n  Then   nu spune nimic despre acea cerință, iar codul de ieșire rămâne neschimbat\n\nCASE-3 — o cerință nenivelată sau nedeclarată nu este niciodată judecată\n  Given  fie o cerință fără nicio legătură nivelată `tested-by:`, fie una fără niciun\n         `level:` declarat în frontmatter-ul ei\n  When   rulează gate\n  Then   nu raportează niciodată acea cerință ca fiind răspunsă la adâncimea greșită"
        }
      }
    },
    {
      "id": "SYS-AUTHOR-101",
      "layer": "need",
      "level": "system",
      "status": "confirmed",
      "area": "SYS",
      "title": "Authoring and evolving a requirement",
      "intent": "",
      "contract": [
        "As someone writing down what the system must do, I want one command to scaffold a requirement and one to promote it once code backs it, so that the corpus grows in a shape the engine can read instead of by hand-copying a template."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "Scenario: a capability goes from idea to confirmed Given  a capability that has just been built When   an author scaffolds a requirement for it and confirms it Then   the file carries the schema the gate reads, and its status reflects that code now backs it"
      ],
      "accept": "",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "SYS-SSOT-001"
      ],
      "satisfied_by": [
        "ARCH-ATOMICFORM-053",
        "ARCH-ATOMICITY-049",
        "ARCH-CLARIFY-062",
        "ARCH-CONFIG-060",
        "ARCH-CONTEXT-048",
        "ARCH-DECOMPOSE-050",
        "ARCH-DESCRIPTION-057",
        "ARCH-NEW-004",
        "ARCH-PROMOTE-011",
        "ARCH-PROMOTE-TODO-001"
      ],
      "members": [],
      "test_exempt": null,
      "milestone": null,
      "priority": "must-have",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Redactarea și evoluția unei cerințe",
          "intent": "",
          "contract": "",
          "acceptance": ""
        }
      }
    },
    {
      "id": "SYS-GATE-102",
      "layer": "need",
      "level": "system",
      "status": "confirmed",
      "area": "SYS",
      "title": "Keeping code and specification in step",
      "intent": "",
      "contract": [
        "As someone committing a change, I want the engine to tell me when the code and the requirements have moved apart, so that a spec nobody honours stops passing for documentation."
      ],
      "verify": [],
      "notes": [
        "`lint_exempt: [fan-out]`: eleven architecture requirements satisfy this need since ARCH-RULES-059 joined (ADR-0023's `system` ceiling is ten). One over the band is not a bucket yet; splitting the gate need into link-sync and drift halves is deferred until a twelfth arrives."
      ],
      "current_impl": [],
      "acc": [
        "Scenario: a confirmed contract changes without its code Given  a confirmed requirement whose contract text has been edited When   the gate runs Then   it reports the drift and names the members and dependents to re-check, without advancing the baseline itself"
      ],
      "accept": "",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "SYS-SSOT-001"
      ],
      "satisfied_by": [
        "ARCH-ACVERIFY-019",
        "ARCH-CHECK-006",
        "ARCH-DOCBUNDLE-026",
        "ARCH-DRIFT-003",
        "ARCH-DRIFTIMPACT-035",
        "ARCH-IMPLEMENT-063",
        "ARCH-MEMBERDRIFT-027",
        "ARCH-ORPHANCODE-034",
        "ARCH-RETIRE-064",
        "ARCH-RULES-059",
        "ARCH-TESTLINK-018",
        "ARCH-TRACKED-042",
        "ARCH-UNSCANNEDTAG-045"
      ],
      "members": [],
      "test_exempt": null,
      "milestone": null,
      "priority": "must-have",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Menținerea codului și a specificației în pas",
          "intent": "",
          "contract": "",
          "acceptance": ""
        }
      }
    },
    {
      "id": "SYS-QUALITY-104",
      "layer": "need",
      "level": "system",
      "status": "confirmed",
      "area": "SYS",
      "title": "Keeping requirements readable",
      "intent": "",
      "contract": [
        "As someone who has to read the corpus a year from now, I want the engine to flag prose that is hard to follow or scope that has quietly grown, so that a requirement stays something a newcomer can act on."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "Scenario: an overloaded requirement is flagged Given  a requirement whose contract and criteria have both outgrown their ceilings When   the linter runs Then   it reports the overload without changing the exit code, and an author who disagrees can record an exemption in the file"
      ],
      "accept": "",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "SYS-SSOT-001"
      ],
      "satisfied_by": [
        "ARCH-LINT-014",
        "ARCH-LINTCHECKS-025",
        "ARCH-PIPE-046",
        "ARCH-REDUNDANCY-058",
        "ARCH-REVIEW-022",
        "ARCH-SIMILAR-016",
        "ARCH-SUGGESTVERIFIES-047"
      ],
      "members": [],
      "test_exempt": null,
      "milestone": null,
      "priority": "must-have",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Menținerea cerințelor lizibile",
          "intent": "",
          "contract": "",
          "acceptance": ""
        }
      }
    },
    {
      "id": "SYS-READ-103",
      "layer": "need",
      "level": "system",
      "status": "confirmed",
      "area": "SYS",
      "title": "Reading a repository",
      "intent": "",
      "contract": [
        "As the engine, I want to read a repository's requirements and find every place the code claims one, so that every other command works from one discovered picture rather than a hand-maintained list."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "Scenario: a repository is read end to end Given  a repository carrying requirement files and tagged source When   the engine scans it Then   every requirement is parsed and every tag is resolved to the requirement it names, with no list maintained by hand"
      ],
      "accept": "",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "SYS-SSOT-001"
      ],
      "satisfied_by": [
        "ARCH-CANDIDATES-009",
        "ARCH-DESIGN-061",
        "ARCH-EXTRACT-008",
        "ARCH-MODULEFILE-056",
        "ARCH-PARSE-001",
        "ARCH-PROSE-024",
        "ARCH-SCAN-002",
        "ARCH-SCANCACHE-023"
      ],
      "members": [],
      "test_exempt": null,
      "milestone": null,
      "priority": "must-have",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Citirea unui repository",
          "intent": "",
          "contract": "",
          "acceptance": ""
        }
      }
    },
    {
      "id": "SYS-REPORT-105",
      "layer": "need",
      "level": "system",
      "status": "confirmed",
      "area": "SYS",
      "title": "Answering what is here and what to do next",
      "intent": "",
      "contract": [
        "As someone picking up the project, I want to ask where a capability lives, what is unfinished and what deserves attention first, so that I can act without reading every file."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "Scenario: the corpus is interrogated Given  a corpus with confirmed, drafted and unbacked requirements When   the reporting commands run Then   each answers one question about the corpus from the same scan, and none of them writes to it"
      ],
      "accept": "",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "SYS-SSOT-001"
      ],
      "satisfied_by": [
        "ARCH-AUDIT-065",
        "ARCH-COVERAGE-029",
        "ARCH-FINDINGS-010",
        "ARCH-HEALTH-017",
        "ARCH-NEXT-013",
        "ARCH-REGISTRYLAG-035",
        "ARCH-REVIEWEDSCORE-109",
        "ARCH-ROADMAP-038",
        "ARCH-SEARCH-036",
        "ARCH-SHOW-015"
      ],
      "members": [],
      "test_exempt": null,
      "milestone": null,
      "priority": "must-have",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Răspunzând ce este aici și ce urmează",
          "intent": "",
          "contract": "",
          "acceptance": ""
        }
      }
    },
    {
      "id": "SYS-SHIP-108",
      "layer": "need",
      "level": "system",
      "status": "confirmed",
      "area": "SYS",
      "title": "Adopting and shipping the engine",
      "intent": "",
      "contract": [
        "As someone bringing the engine into a repository, or releasing a new version of it, I want the bootstrap, the version guards and the published artifacts to agree, so that a consumer picks up a change instead of silently keeping an old copy."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "Scenario: a release reaches a consumer Given  an engine change and a repository that vendors it When   the release is cut and the consumer updates Then   the version guards refuse a change that would ship unannounced, and the consumer is told when its vendored copy has fallen behind"
      ],
      "accept": "",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "SYS-SSOT-001"
      ],
      "satisfied_by": [
        "ARCH-CMDREGISTRY-033",
        "ARCH-INIT-012",
        "ARCH-PYFLOOR-040",
        "ARCH-REPRO-041",
        "ARCH-SELFGATE-039",
        "ARCH-STALEENGINE-043",
        "ARCH-TRANSLATE-044"
      ],
      "members": [],
      "test_exempt": null,
      "milestone": null,
      "priority": "must-have",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Adoptarea și livrarea motorului",
          "intent": "",
          "contract": "",
          "acceptance": ""
        }
      }
    },
    {
      "id": "SYS-SSOT-001",
      "layer": "need",
      "level": "system",
      "status": "confirmed",
      "area": "SYS",
      "title": "Stakeholder need — specs and code stay in sync",
      "intent": "On most projects the \"spec\" lives in someone's head, a stale wiki, or a ticket nobody reads, so over time the code says one thing and the docs say another. The people who own this project need the opposite: a single written source of truth for each capability that the build itself keeps honest. This is the underlying need the whole tool exists to serve — every feature below is here because it fulfils part of it.",
      "contract": [
        "The project keeps a single written source of truth for each capability, living next to the code.",
        "Drift between a capability's description and its code is caught before it ships, not after.",
        "A reader can navigate from any capability to the code that implements it and the tests that verify it."
      ],
      "verify": [],
      "notes": [
        "`lint_exempt: ac-count-low` — a stakeholder need is verified by the requirements that satisfy it, not by cases of its own, so the floor that catches an under-specified capability measures the wrong thing here.",
        "A `need` is a stakeholder requirement, not a capability: it is satisfied by other requirements (see \"Satisfied by\"), not implemented or tested by code directly, so the gate exempts it from the implements/tested-by checks."
      ],
      "current_impl": [
        "Not implemented by code — a stakeholder need is fulfilled by the requirements that declare `satisfies: SYS-SSOT-001` (the gate, the map, the dossier, and the traceability feature itself)."
      ],
      "acc": [
        "CASE-2 — Given  a confirmed capability When   the gate runs Then   it has linked, existing code and tests",
        "CASE-3 — Given  any capability When   a reader runs `show` Then   they see its code locations and what depends on it"
      ],
      "accept": "AC-1a\n  Given  a repo where a tag points to a non-existent requirement (dangling ref)\n         or an enforced requirement (in-progress, implemented, or confirmed) has\n         no implements: member (structural gap)\n  When   the gate runs\n  Then   the build fails (exit 1 — link-sync is an ERROR)\n\nAC-1b\n  Given  a repo where a confirmed requirement's contract was edited after the lock\n  When   the gate runs\n  Then   the drift is surfaced (WARN: \"DRIFT — contract changed since lock\") and\n         the gate exits 0 — drift is reported, not blocking, by design\n\nCASE-2\n  Given  a confirmed capability\n  When   the gate runs\n  Then   it has linked, existing code and tests\n\nCASE-3\n  Given  any capability\n  When   a reader runs `show`\n  Then   they see its code locations and what depends on it",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [],
      "satisfied_by": [
        "SYS-AUTHOR-101",
        "SYS-GATE-102",
        "SYS-QUALITY-104",
        "SYS-READ-103",
        "SYS-REPORT-105",
        "SYS-SHIP-108",
        "SYS-VISUAL-106",
        "SYS-VMODEL-107"
      ],
      "members": [],
      "test_exempt": null,
      "milestone": "v1.00",
      "priority": "must-have",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Nevoia stakeholder-ului — spec-urile și codul rămân sincronizate",
          "intent": "În majoritatea proiectelor \"spec-ul\" trăiește în capul cuiva, într-un wiki învechit, sau într-un tichet pe care nimeni nu-l citește, astfel încât în timp codul spune un lucru, iar documentația altul. Oamenii care dețin acest proiect au nevoie de opusul: o singură sursă de adevăr scrisă pentru fiecare capacitate, pe care build-ul însuși o menține onestă. Aceasta este nevoia subiacentă căreia întreaga unealtă îi servește — fiecare funcționalitate de mai jos este aici pentru că îndeplinește o parte din ea.",
          "contract": "> În majoritatea proiectelor \"spec-ul\" trăiește în capul cuiva, într-un wiki învechit, sau\n> într-un tichet pe care nimeni nu-l citește, astfel încât în timp codul spune un lucru, iar\n> documentația altul. Oamenii care dețin acest proiect au nevoie de opusul: o singură sursă\n> de adevăr scrisă pentru fiecare capacitate, pe care build-ul însuși o menține onestă. Aceasta\n> este nevoia subiacentă căreia întreaga unealtă îi servește — fiecare funcționalitate de mai\n> jos este aici pentru că îndeplinește o parte din ea.\n- Proiectul menține o singură sursă de adevăr scrisă pentru fiecare capacitate, aflată lângă cod.\n- Drift-ul dintre descrierea unei capacități și codul ei este prins înainte de a fi livrat, nu după.\n- Un cititor poate naviga de la orice capacitate la codul care o implementează și testele care o verifică.",
          "acceptance": "AC-1a\n  Given  un repo unde un tag indică o cerință inexistentă (referință suspendată)\n         sau o cerință aplicată (in-progress, implemented, sau confirmed) nu are\n         niciun membru implements: (gol structural)\n  When   gate-ul rulează\n  Then   build-ul eșuează (iese cu 1 — link-sync este o EROARE)\n\nAC-1b\n  Given  un repo unde contractul unei cerințe confirmate a fost editat după lock\n  When   gate-ul rulează\n  Then   drift-ul este semnalat (WARN: \"DRIFT — contract changed since lock\") iar\n         gate-ul iese cu 0 — drift-ul este raportat, nu blocant, prin design\n\nCASE-2\n  Given  o capacitate confirmată\n  When   gate-ul rulează\n  Then   are cod și teste legate, existente\n\nCASE-3\n  Given  orice capacitate\n  When   un cititor rulează `show`\n  Then   vede locațiile ei de cod și ce depinde de ea"
        }
      }
    },
    {
      "id": "SYS-VISUAL-106",
      "layer": "need",
      "level": "system",
      "status": "confirmed",
      "area": "SYS",
      "title": "Seeing the system at a glance",
      "intent": "",
      "contract": [
        "As someone reviewing a change, I want the shape of the system as a picture I can open without tooling, so that a dependency I would have missed in a list is obvious."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "Scenario: the system is rendered Given  the requirement graph When   the map is generated Then   the same graph is available as diagrams that render in a browser with no install, and as a data file any front-end can consume"
      ],
      "accept": "",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "SYS-SSOT-001"
      ],
      "satisfied_by": [
        "ARCH-EXCALIDRAW-030",
        "ARCH-EXCALIDRAW-031",
        "ARCH-EXCALIDRAW-032",
        "ARCH-MAP-007",
        "ARCH-MAPDIAGRAMS-055",
        "ARCH-PAGES-021",
        "ARCH-SITE-026",
        "ARCH-VIEWER-007"
      ],
      "members": [],
      "test_exempt": null,
      "milestone": null,
      "priority": "must-have",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Văzând sistemul dintr-o privire",
          "intent": "",
          "contract": "",
          "acceptance": ""
        }
      }
    },
    {
      "id": "SYS-VMODEL-107",
      "layer": "need",
      "level": "system",
      "status": "confirmed",
      "area": "SYS",
      "title": "Placing a requirement in the V",
      "intent": "",
      "contract": [
        "As someone who wants to know whether a requirement is verified at the right depth, I want each one to declare how abstract it is and which level of test discharges it, so that a system-level promise answered only by a unit test stops looking covered."
      ],
      "verify": [],
      "notes": [],
      "current_impl": [],
      "acc": [
        "Scenario: a level and its verification are compared Given  a confirmed requirement declaring its specification level and carrying levelled test links When   the gate runs Then   it reports a requirement whose tests sit at no level that discharges its own, and judges nothing when either declaration is absent"
      ],
      "accept": "",
      "input": "",
      "output": "",
      "desc": "",
      "deps": [],
      "used_by": [],
      "satisfies": [
        "SYS-SSOT-001"
      ],
      "satisfied_by": [
        "ARCH-FANOUT-052",
        "ARCH-LEVEL-051",
        "ARCH-TRACE-020",
        "ARCH-VLEVEL-037",
        "ARCH-VRUNGS-054"
      ],
      "members": [],
      "test_exempt": null,
      "milestone": null,
      "priority": "must-have",
      "risks": [],
      "i18n": {
        "ro": {
          "title": "Plasarea unei cerințe în V",
          "intent": "",
          "contract": "",
          "acceptance": ""
        }
      }
    }
  ],
  "edges": [
    [
      "ARCH-ACVERIFY-019",
      "ARCH-CHECK-006"
    ],
    [
      "ARCH-ACVERIFY-019",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-ATOMICFORM-053",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-ATOMICFORM-053",
      "ARCH-DRIFT-003"
    ],
    [
      "ARCH-ATOMICFORM-053",
      "ARCH-CHECK-006"
    ],
    [
      "ARCH-ATOMICITY-049",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-ATOMICITY-049",
      "ARCH-LINT-014"
    ],
    [
      "ARCH-AUDIT-065",
      "ARCH-CHECK-006"
    ],
    [
      "ARCH-AUDIT-065",
      "ARCH-NEXT-013"
    ],
    [
      "ARCH-AUDIT-065",
      "ARCH-SIMILAR-016"
    ],
    [
      "ARCH-AUDIT-065",
      "ARCH-COVERAGE-029"
    ],
    [
      "ARCH-AUDIT-065",
      "ARCH-DESIGN-061"
    ],
    [
      "ARCH-CANDIDATES-009",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-CHECK-006",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-CHECK-006",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-CHECK-006",
      "ARCH-DRIFT-003"
    ],
    [
      "ARCH-CLARIFY-062",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-CMDREGISTRY-033",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-CONFIG-060",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-CONTEXT-048",
      "ARCH-MAP-007"
    ],
    [
      "ARCH-COVERAGE-029",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-COVERAGE-029",
      "ARCH-NEXT-013"
    ],
    [
      "ARCH-DECOMPOSE-050",
      "ARCH-ATOMICITY-049"
    ],
    [
      "ARCH-DECOMPOSE-050",
      "ARCH-LINT-014"
    ],
    [
      "ARCH-DECOMPOSE-050",
      "ARCH-NEW-004"
    ],
    [
      "ARCH-DESCRIPTION-057",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-DESCRIPTION-057",
      "ARCH-DRIFT-003"
    ],
    [
      "ARCH-DESIGN-061",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-DESIGN-061",
      "ARCH-CONFIG-060"
    ],
    [
      "ARCH-DESIGN-061",
      "ARCH-CMDREGISTRY-033"
    ],
    [
      "ARCH-DOCBUNDLE-026",
      "ARCH-CHECK-006"
    ],
    [
      "ARCH-DOCBUNDLE-026",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-DRIFTIMPACT-035",
      "ARCH-CHECK-006"
    ],
    [
      "ARCH-DRIFTIMPACT-035",
      "ARCH-DRIFT-003"
    ],
    [
      "ARCH-EXCALIDRAW-031",
      "ARCH-EXCALIDRAW-030"
    ],
    [
      "ARCH-EXCALIDRAW-032",
      "ARCH-EXCALIDRAW-030"
    ],
    [
      "ARCH-EXTRACT-008",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-FANOUT-052",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-FANOUT-052",
      "ARCH-LINT-014"
    ],
    [
      "ARCH-FANOUT-052",
      "ARCH-LEVEL-051"
    ],
    [
      "ARCH-FINDINGS-010",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-HEALTH-017",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-HEALTH-017",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-HEALTH-017",
      "ARCH-DRIFT-003"
    ],
    [
      "ARCH-IMPLEMENT-063",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-IMPLEMENT-063",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-IMPLEMENT-063",
      "ARCH-SIMILAR-016"
    ],
    [
      "ARCH-IMPLEMENT-063",
      "ARCH-CLARIFY-062"
    ],
    [
      "ARCH-INIT-012",
      "ARCH-EXTRACT-008"
    ],
    [
      "ARCH-INIT-012",
      "ARCH-CHECK-006"
    ],
    [
      "ARCH-INIT-012",
      "ARCH-MAP-007"
    ],
    [
      "ARCH-LEVEL-051",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-LEVEL-051",
      "ARCH-CHECK-006"
    ],
    [
      "ARCH-LINT-014",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-LINTCHECKS-025",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-LINTCHECKS-025",
      "ARCH-LINT-014"
    ],
    [
      "ARCH-MAP-007",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-MAP-007",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-MAPDIAGRAMS-055",
      "ARCH-MAP-007"
    ],
    [
      "ARCH-MEMBERDRIFT-027",
      "ARCH-DRIFT-003"
    ],
    [
      "ARCH-MEMBERDRIFT-027",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-MEMBERDRIFT-027",
      "ARCH-CHECK-006"
    ],
    [
      "ARCH-MODULEFILE-056",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-NEW-004",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-NEXT-013",
      "ARCH-MAP-007"
    ],
    [
      "ARCH-ORPHANCODE-034",
      "ARCH-CHECK-006"
    ],
    [
      "ARCH-ORPHANCODE-034",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-PAGES-021",
      "ARCH-MAP-007"
    ],
    [
      "ARCH-PIPE-046",
      "ARCH-CMDREGISTRY-033"
    ],
    [
      "ARCH-PROMOTE-011",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-PROMOTE-011",
      "ARCH-DRIFT-003"
    ],
    [
      "ARCH-PROMOTE-TODO-001",
      "ARCH-NEW-004"
    ],
    [
      "ARCH-PROSE-024",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-PROSE-024",
      "ARCH-EXTRACT-008"
    ],
    [
      "ARCH-REDUNDANCY-058",
      "ARCH-NEXT-013"
    ],
    [
      "ARCH-REDUNDANCY-058",
      "ARCH-SIMILAR-016"
    ],
    [
      "ARCH-REGISTRYLAG-035",
      "ARCH-HEALTH-017"
    ],
    [
      "ARCH-REPRO-041",
      "ARCH-SELFGATE-039"
    ],
    [
      "ARCH-RETIRE-064",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-RETIRE-064",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-RETIRE-064",
      "ARCH-DRIFT-003"
    ],
    [
      "ARCH-RETIRE-064",
      "ARCH-MODULEFILE-056"
    ],
    [
      "ARCH-REVIEW-022",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-REVIEWEDSCORE-109",
      "ARCH-HEALTH-017"
    ],
    [
      "ARCH-ROADMAP-038",
      "ARCH-HEALTH-017"
    ],
    [
      "ARCH-RULES-059",
      "ARCH-CHECK-006"
    ],
    [
      "ARCH-RULES-059",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-SCANCACHE-023",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-SEARCH-036",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-SEARCH-036",
      "ARCH-SIMILAR-016"
    ],
    [
      "ARCH-SELFGATE-039",
      "ARCH-CHECK-006"
    ],
    [
      "ARCH-SHOW-015",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-SHOW-015",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-SIMILAR-016",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-SITE-026",
      "ARCH-MAP-007"
    ],
    [
      "ARCH-SITE-026",
      "ARCH-VIEWER-007"
    ],
    [
      "ARCH-SITE-026",
      "ARCH-PAGES-021"
    ],
    [
      "ARCH-STALEENGINE-043",
      "ARCH-CHECK-006"
    ],
    [
      "ARCH-STALEENGINE-043",
      "ARCH-SELFGATE-039"
    ],
    [
      "ARCH-SUGGESTVERIFIES-047",
      "ARCH-ACVERIFY-019"
    ],
    [
      "ARCH-SUGGESTVERIFIES-047",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-TESTLINK-018",
      "ARCH-CHECK-006"
    ],
    [
      "ARCH-TRACE-020",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-TRACE-020",
      "ARCH-CHECK-006"
    ],
    [
      "ARCH-TRACKED-042",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-TRACKED-042",
      "ARCH-CHECK-006"
    ],
    [
      "ARCH-TRANSLATE-044",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-TRANSLATE-044",
      "ARCH-MAP-007"
    ],
    [
      "ARCH-TRANSLATE-044",
      "ARCH-VIEWER-007"
    ],
    [
      "ARCH-UNSCANNEDTAG-045",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-UNSCANNEDTAG-045",
      "ARCH-CHECK-006"
    ],
    [
      "ARCH-VIEWER-007",
      "ARCH-MAP-007"
    ],
    [
      "ARCH-VLEVEL-037",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-VLEVEL-037",
      "ARCH-CHECK-006"
    ],
    [
      "ARCH-VRUNGS-054",
      "ARCH-LEVEL-051"
    ],
    [
      "ARCH-VRUNGS-054",
      "ARCH-VLEVEL-037"
    ],
    [
      "ARCH-VRUNGS-054",
      "ARCH-CHECK-006"
    ]
  ],
  "upstream_edges": [
    [
      "ARCH-ACVERIFY-019",
      "SYS-GATE-102"
    ],
    [
      "REQ-ACVERIFY-821",
      "ARCH-ACVERIFY-019"
    ],
    [
      "REQ-ACVERIFY-822",
      "ARCH-ACVERIFY-019"
    ],
    [
      "REQ-ACVERIFY-823",
      "ARCH-ACVERIFY-019"
    ],
    [
      "ARCH-ATOMICFORM-053",
      "SYS-AUTHOR-101"
    ],
    [
      "ARCH-ATOMICITY-049",
      "SYS-AUTHOR-101"
    ],
    [
      "REQ-ATOMICITY-824",
      "ARCH-ATOMICITY-049"
    ],
    [
      "REQ-ATOMICITY-825",
      "ARCH-ATOMICITY-049"
    ],
    [
      "ARCH-AUDIT-065",
      "SYS-REPORT-105"
    ],
    [
      "REQ-AUDIT-970",
      "ARCH-AUDIT-065"
    ],
    [
      "REQ-AUDIT-971",
      "ARCH-AUDIT-065"
    ],
    [
      "REQ-AUDIT-972",
      "ARCH-AUDIT-065"
    ],
    [
      "REQ-AUDIT-973",
      "ARCH-AUDIT-065"
    ],
    [
      "ARCH-CANDIDATES-009",
      "SYS-READ-103"
    ],
    [
      "REQ-CANDIDATES-826",
      "ARCH-CANDIDATES-009"
    ],
    [
      "REQ-CANDIDATES-827",
      "ARCH-CANDIDATES-009"
    ],
    [
      "ARCH-CHECK-006",
      "SYS-GATE-102"
    ],
    [
      "REQ-CHECK-828",
      "ARCH-CHECK-006"
    ],
    [
      "REQ-CHECK-829",
      "ARCH-CHECK-006"
    ],
    [
      "REQ-CHECK-830",
      "ARCH-CHECK-006"
    ],
    [
      "REQ-CHECK-831",
      "ARCH-CHECK-006"
    ],
    [
      "REQ-CHECK-832",
      "ARCH-CHECK-006"
    ],
    [
      "REQ-CHECK-833",
      "ARCH-CHECK-006"
    ],
    [
      "ARCH-CLARIFY-062",
      "SYS-AUTHOR-101"
    ],
    [
      "REQ-CLARIFY-956",
      "ARCH-CLARIFY-062"
    ],
    [
      "REQ-CLARIFY-957",
      "ARCH-CLARIFY-062"
    ],
    [
      "REQ-CLARIFY-975",
      "ARCH-CLARIFY-062"
    ],
    [
      "ARCH-CMDREGISTRY-033",
      "SYS-SHIP-108"
    ],
    [
      "REQ-CMDREGISTRY-834",
      "ARCH-CMDREGISTRY-033"
    ],
    [
      "REQ-CMDREGISTRY-963",
      "ARCH-CMDREGISTRY-033"
    ],
    [
      "ARCH-CONFIG-060",
      "SYS-AUTHOR-101"
    ],
    [
      "REQ-CONFIG-949",
      "ARCH-CONFIG-060"
    ],
    [
      "ARCH-CONTEXT-048",
      "SYS-AUTHOR-101"
    ],
    [
      "REQ-CONTEXT-835",
      "ARCH-CONTEXT-048"
    ],
    [
      "ARCH-COVERAGE-029",
      "SYS-REPORT-105"
    ],
    [
      "REQ-COVERAGE-836",
      "ARCH-COVERAGE-029"
    ],
    [
      "ARCH-DECOMPOSE-050",
      "SYS-AUTHOR-101"
    ],
    [
      "REQ-DECOMPOSE-837",
      "ARCH-DECOMPOSE-050"
    ],
    [
      "REQ-DECOMPOSE-838",
      "ARCH-DECOMPOSE-050"
    ],
    [
      "REQ-DECOMPOSE-839",
      "ARCH-DECOMPOSE-050"
    ],
    [
      "ARCH-DESCRIPTION-057",
      "SYS-AUTHOR-101"
    ],
    [
      "ARCH-DESIGN-061",
      "SYS-READ-103"
    ],
    [
      "REQ-DESIGN-950",
      "ARCH-DESIGN-061"
    ],
    [
      "REQ-DESIGN-951",
      "ARCH-DESIGN-061"
    ],
    [
      "REQ-DESIGN-952",
      "ARCH-DESIGN-061"
    ],
    [
      "REQ-DESIGN-953",
      "ARCH-DESIGN-061"
    ],
    [
      "REQ-DESIGN-954",
      "ARCH-DESIGN-061"
    ],
    [
      "REQ-DESIGN-955",
      "ARCH-DESIGN-061"
    ],
    [
      "REQ-DESIGN-976",
      "ARCH-DESIGN-061"
    ],
    [
      "REQ-DESIGN-978",
      "ARCH-DESIGN-061"
    ],
    [
      "ARCH-DOCBUNDLE-026",
      "SYS-GATE-102"
    ],
    [
      "REQ-DOCBUNDLE-840",
      "ARCH-DOCBUNDLE-026"
    ],
    [
      "ARCH-DRIFT-003",
      "SYS-GATE-102"
    ],
    [
      "REQ-DRIFT-841",
      "ARCH-DRIFT-003"
    ],
    [
      "REQ-DRIFT-842",
      "ARCH-DRIFT-003"
    ],
    [
      "ARCH-DRIFTIMPACT-035",
      "SYS-GATE-102"
    ],
    [
      "REQ-DRIFTIMPACT-843",
      "ARCH-DRIFTIMPACT-035"
    ],
    [
      "ARCH-EXCALIDRAW-030",
      "SYS-VISUAL-106"
    ],
    [
      "REQ-EXCALIDRAW-844",
      "ARCH-EXCALIDRAW-030"
    ],
    [
      "REQ-EXCALIDRAW-845",
      "ARCH-EXCALIDRAW-030"
    ],
    [
      "ARCH-EXCALIDRAW-031",
      "SYS-VISUAL-106"
    ],
    [
      "REQ-EXCALIDRAW-846",
      "ARCH-EXCALIDRAW-031"
    ],
    [
      "REQ-EXCALIDRAW-847",
      "ARCH-EXCALIDRAW-031"
    ],
    [
      "ARCH-EXCALIDRAW-032",
      "SYS-VISUAL-106"
    ],
    [
      "REQ-EXCALIDRAW-848",
      "ARCH-EXCALIDRAW-032"
    ],
    [
      "ARCH-EXTRACT-008",
      "SYS-READ-103"
    ],
    [
      "REQ-EXTRACT-849",
      "ARCH-EXTRACT-008"
    ],
    [
      "REQ-EXTRACT-850",
      "ARCH-EXTRACT-008"
    ],
    [
      "REQ-EXTRACT-851",
      "ARCH-EXTRACT-008"
    ],
    [
      "ARCH-FANOUT-052",
      "SYS-VMODEL-107"
    ],
    [
      "REQ-FANOUT-852",
      "ARCH-FANOUT-052"
    ],
    [
      "ARCH-FINDINGS-010",
      "SYS-REPORT-105"
    ],
    [
      "REQ-FINDINGS-853",
      "ARCH-FINDINGS-010"
    ],
    [
      "REQ-FINDINGS-854",
      "ARCH-FINDINGS-010"
    ],
    [
      "REQ-FINDINGS-855",
      "ARCH-FINDINGS-010"
    ],
    [
      "REQ-FINDINGS-856",
      "ARCH-FINDINGS-010"
    ],
    [
      "ARCH-HEALTH-017",
      "SYS-REPORT-105"
    ],
    [
      "REQ-HEALTH-857",
      "ARCH-HEALTH-017"
    ],
    [
      "REQ-HEALTH-858",
      "ARCH-HEALTH-017"
    ],
    [
      "REQ-HEALTH-859",
      "ARCH-HEALTH-017"
    ],
    [
      "REQ-HEALTH-968",
      "ARCH-HEALTH-017"
    ],
    [
      "ARCH-IMPLEMENT-063",
      "SYS-GATE-102"
    ],
    [
      "REQ-IMPLEMENT-958",
      "ARCH-IMPLEMENT-063"
    ],
    [
      "REQ-IMPLEMENT-959",
      "ARCH-IMPLEMENT-063"
    ],
    [
      "ARCH-INIT-012",
      "SYS-SHIP-108"
    ],
    [
      "REQ-INIT-860",
      "ARCH-INIT-012"
    ],
    [
      "REQ-INIT-861",
      "ARCH-INIT-012"
    ],
    [
      "ARCH-LEVEL-051",
      "SYS-VMODEL-107"
    ],
    [
      "REQ-LEVEL-862",
      "ARCH-LEVEL-051"
    ],
    [
      "ARCH-LINT-014",
      "SYS-QUALITY-104"
    ],
    [
      "REQ-LINT-863",
      "ARCH-LINT-014"
    ],
    [
      "REQ-LINT-864",
      "ARCH-LINT-014"
    ],
    [
      "ARCH-LINTCHECKS-025",
      "SYS-QUALITY-104"
    ],
    [
      "REQ-LINTCHECKS-865",
      "ARCH-LINTCHECKS-025"
    ],
    [
      "REQ-LINTCHECKS-866",
      "ARCH-LINTCHECKS-025"
    ],
    [
      "REQ-LINTCHECKS-867",
      "ARCH-LINTCHECKS-025"
    ],
    [
      "REQ-LINTCHECKS-868",
      "ARCH-LINTCHECKS-025"
    ],
    [
      "REQ-LINTCHECKS-869",
      "ARCH-LINTCHECKS-025"
    ],
    [
      "ARCH-MAP-007",
      "SYS-VISUAL-106"
    ],
    [
      "REQ-MAP-870",
      "ARCH-MAP-007"
    ],
    [
      "REQ-MAP-871",
      "ARCH-MAP-007"
    ],
    [
      "REQ-MAP-872",
      "ARCH-MAP-007"
    ],
    [
      "REQ-MAP-873",
      "ARCH-MAP-007"
    ],
    [
      "ARCH-MAPDIAGRAMS-055",
      "SYS-VISUAL-106"
    ],
    [
      "REQ-MAPDIAGRAMS-874",
      "ARCH-MAPDIAGRAMS-055"
    ],
    [
      "REQ-MAPDIAGRAMS-875",
      "ARCH-MAPDIAGRAMS-055"
    ],
    [
      "REQ-MAPDIAGRAMS-876",
      "ARCH-MAPDIAGRAMS-055"
    ],
    [
      "REQ-MAPDIAGRAMS-877",
      "ARCH-MAPDIAGRAMS-055"
    ],
    [
      "REQ-MAPDIAGRAMS-878",
      "ARCH-MAPDIAGRAMS-055"
    ],
    [
      "ARCH-MEMBERDRIFT-027",
      "SYS-GATE-102"
    ],
    [
      "REQ-MEMBERDRIFT-879",
      "ARCH-MEMBERDRIFT-027"
    ],
    [
      "REQ-MEMBERDRIFT-880",
      "ARCH-MEMBERDRIFT-027"
    ],
    [
      "ARCH-MODULEFILE-056",
      "SYS-READ-103"
    ],
    [
      "ARCH-NEW-004",
      "SYS-AUTHOR-101"
    ],
    [
      "REQ-NEW-881",
      "ARCH-NEW-004"
    ],
    [
      "REQ-NEW-882",
      "ARCH-NEW-004"
    ],
    [
      "ARCH-NEXT-013",
      "SYS-REPORT-105"
    ],
    [
      "REQ-NEXT-883",
      "ARCH-NEXT-013"
    ],
    [
      "REQ-NEXT-884",
      "ARCH-NEXT-013"
    ],
    [
      "REQ-NEXT-885",
      "ARCH-NEXT-013"
    ],
    [
      "REQ-NEXT-886",
      "ARCH-NEXT-013"
    ],
    [
      "REQ-NEXT-887",
      "ARCH-NEXT-013"
    ],
    [
      "ARCH-ORPHANCODE-034",
      "SYS-GATE-102"
    ],
    [
      "REQ-ORPHANCODE-888",
      "ARCH-ORPHANCODE-034"
    ],
    [
      "ARCH-PAGES-021",
      "SYS-VISUAL-106"
    ],
    [
      "REQ-PAGES-889",
      "ARCH-PAGES-021"
    ],
    [
      "ARCH-PARSE-001",
      "SYS-READ-103"
    ],
    [
      "REQ-PARSE-890",
      "ARCH-PARSE-001"
    ],
    [
      "REQ-PARSE-891",
      "ARCH-PARSE-001"
    ],
    [
      "REQ-PARSE-892",
      "ARCH-PARSE-001"
    ],
    [
      "ARCH-PIPE-046",
      "SYS-QUALITY-104"
    ],
    [
      "REQ-PIPE-893",
      "ARCH-PIPE-046"
    ],
    [
      "ARCH-PROMOTE-011",
      "SYS-AUTHOR-101"
    ],
    [
      "REQ-PROMOTE-894",
      "ARCH-PROMOTE-011"
    ],
    [
      "REQ-PROMOTE-974",
      "ARCH-PROMOTE-011"
    ],
    [
      "ARCH-PROMOTE-TODO-001",
      "SYS-AUTHOR-101"
    ],
    [
      "REQ-PROMOTE-TODO-897",
      "ARCH-PROMOTE-TODO-001"
    ],
    [
      "REQ-PROMOTE-TODO-898",
      "ARCH-PROMOTE-TODO-001"
    ],
    [
      "REQ-PROMOTE-TODO-899",
      "ARCH-PROMOTE-TODO-001"
    ],
    [
      "ARCH-PROSE-024",
      "SYS-READ-103"
    ],
    [
      "REQ-PROSE-900",
      "ARCH-PROSE-024"
    ],
    [
      "REQ-PROSE-901",
      "ARCH-PROSE-024"
    ],
    [
      "ARCH-PYFLOOR-040",
      "SYS-SHIP-108"
    ],
    [
      "REQ-PYFLOOR-902",
      "ARCH-PYFLOOR-040"
    ],
    [
      "ARCH-REDUNDANCY-058",
      "SYS-QUALITY-104"
    ],
    [
      "ARCH-REGISTRYLAG-035",
      "SYS-REPORT-105"
    ],
    [
      "REQ-REGISTRYLAG-903",
      "ARCH-REGISTRYLAG-035"
    ],
    [
      "REQ-REGISTRYLAG-904",
      "ARCH-REGISTRYLAG-035"
    ],
    [
      "ARCH-REPRO-041",
      "SYS-SHIP-108"
    ],
    [
      "REQ-REPRO-905",
      "ARCH-REPRO-041"
    ],
    [
      "ARCH-RETIRE-064",
      "SYS-GATE-102"
    ],
    [
      "REQ-RETIRE-960",
      "ARCH-RETIRE-064"
    ],
    [
      "REQ-RETIRE-961",
      "ARCH-RETIRE-064"
    ],
    [
      "REQ-RETIRE-962",
      "ARCH-RETIRE-064"
    ],
    [
      "ARCH-REVIEW-022",
      "SYS-QUALITY-104"
    ],
    [
      "REQ-REVIEW-906",
      "ARCH-REVIEW-022"
    ],
    [
      "ARCH-REVIEWEDSCORE-109",
      "SYS-REPORT-105"
    ],
    [
      "ARCH-ROADMAP-038",
      "SYS-REPORT-105"
    ],
    [
      "REQ-ROADMAP-907",
      "ARCH-ROADMAP-038"
    ],
    [
      "ARCH-RULES-059",
      "SYS-GATE-102"
    ],
    [
      "REQ-RULES-947",
      "ARCH-RULES-059"
    ],
    [
      "REQ-RULES-948",
      "ARCH-RULES-059"
    ],
    [
      "ARCH-SCAN-002",
      "SYS-READ-103"
    ],
    [
      "REQ-SCAN-908",
      "ARCH-SCAN-002"
    ],
    [
      "REQ-SCAN-909",
      "ARCH-SCAN-002"
    ],
    [
      "ARCH-SCANCACHE-023",
      "SYS-READ-103"
    ],
    [
      "REQ-SCANCACHE-911",
      "ARCH-SCANCACHE-023"
    ],
    [
      "ARCH-SEARCH-036",
      "SYS-REPORT-105"
    ],
    [
      "REQ-SEARCH-912",
      "ARCH-SEARCH-036"
    ],
    [
      "REQ-SEARCH-913",
      "ARCH-SEARCH-036"
    ],
    [
      "REQ-SEARCH-914",
      "ARCH-SEARCH-036"
    ],
    [
      "REQ-SEARCH-915",
      "ARCH-SEARCH-036"
    ],
    [
      "REQ-SEARCH-965",
      "ARCH-SEARCH-036"
    ],
    [
      "ARCH-SELFGATE-039",
      "SYS-SHIP-108"
    ],
    [
      "REQ-SELFGATE-916",
      "ARCH-SELFGATE-039"
    ],
    [
      "ARCH-SHOW-015",
      "SYS-REPORT-105"
    ],
    [
      "REQ-SHOW-917",
      "ARCH-SHOW-015"
    ],
    [
      "REQ-SHOW-918",
      "ARCH-SHOW-015"
    ],
    [
      "REQ-SHOW-919",
      "ARCH-SHOW-015"
    ],
    [
      "ARCH-SIMILAR-016",
      "SYS-QUALITY-104"
    ],
    [
      "REQ-SIMILAR-920",
      "ARCH-SIMILAR-016"
    ],
    [
      "REQ-SIMILAR-921",
      "ARCH-SIMILAR-016"
    ],
    [
      "REQ-SIMILAR-922",
      "ARCH-SIMILAR-016"
    ],
    [
      "REQ-SIMILAR-923",
      "ARCH-SIMILAR-016"
    ],
    [
      "ARCH-SITE-026",
      "SYS-VISUAL-106"
    ],
    [
      "REQ-SITE-924",
      "ARCH-SITE-026"
    ],
    [
      "ARCH-STALEENGINE-043",
      "SYS-SHIP-108"
    ],
    [
      "REQ-STALEENGINE-925",
      "ARCH-STALEENGINE-043"
    ],
    [
      "REQ-STALEENGINE-926",
      "ARCH-STALEENGINE-043"
    ],
    [
      "ARCH-SUGGESTVERIFIES-047",
      "SYS-QUALITY-104"
    ],
    [
      "REQ-SUGGESTVERIFIES-927",
      "ARCH-SUGGESTVERIFIES-047"
    ],
    [
      "REQ-SUGGESTVERIFIES-928",
      "ARCH-SUGGESTVERIFIES-047"
    ],
    [
      "REQ-SUGGESTVERIFIES-929",
      "ARCH-SUGGESTVERIFIES-047"
    ],
    [
      "ARCH-TESTLINK-018",
      "SYS-GATE-102"
    ],
    [
      "REQ-TESTLINK-930",
      "ARCH-TESTLINK-018"
    ],
    [
      "REQ-TESTLINK-931",
      "ARCH-TESTLINK-018"
    ],
    [
      "REQ-TESTLINK-932",
      "ARCH-TESTLINK-018"
    ],
    [
      "REQ-TESTLINK-933",
      "ARCH-TESTLINK-018"
    ],
    [
      "ARCH-TRACE-020",
      "SYS-VMODEL-107"
    ],
    [
      "REQ-TRACE-934",
      "ARCH-TRACE-020"
    ],
    [
      "REQ-TRACE-935",
      "ARCH-TRACE-020"
    ],
    [
      "ARCH-TRACKED-042",
      "SYS-GATE-102"
    ],
    [
      "REQ-TRACKED-936",
      "ARCH-TRACKED-042"
    ],
    [
      "ARCH-TRANSLATE-044",
      "SYS-SHIP-108"
    ],
    [
      "REQ-TRANSLATE-937",
      "ARCH-TRANSLATE-044"
    ],
    [
      "REQ-TRANSLATE-938",
      "ARCH-TRANSLATE-044"
    ],
    [
      "REQ-TRANSLATE-967",
      "ARCH-TRANSLATE-044"
    ],
    [
      "ARCH-UNSCANNEDTAG-045",
      "SYS-GATE-102"
    ],
    [
      "REQ-UNSCANNEDTAG-939",
      "ARCH-UNSCANNEDTAG-045"
    ],
    [
      "ARCH-VIEWER-007",
      "SYS-VISUAL-106"
    ],
    [
      "REQ-VIEWER-940",
      "ARCH-VIEWER-007"
    ],
    [
      "REQ-VIEWER-941",
      "ARCH-VIEWER-007"
    ],
    [
      "REQ-VIEWER-942",
      "ARCH-VIEWER-007"
    ],
    [
      "REQ-VIEWER-943",
      "ARCH-VIEWER-007"
    ],
    [
      "REQ-VIEWER-944",
      "ARCH-VIEWER-007"
    ],
    [
      "REQ-VIEWER-945",
      "ARCH-VIEWER-007"
    ],
    [
      "REQ-VIEWER-964",
      "ARCH-VIEWER-007"
    ],
    [
      "REQ-VIEWER-966",
      "ARCH-VIEWER-007"
    ],
    [
      "REQ-VIEWER-969",
      "ARCH-VIEWER-007"
    ],
    [
      "REQ-VIEWER-977",
      "ARCH-VIEWER-007"
    ],
    [
      "ARCH-VLEVEL-037",
      "SYS-VMODEL-107"
    ],
    [
      "REQ-VLEVEL-944",
      "ARCH-VLEVEL-037"
    ],
    [
      "REQ-VLEVEL-945",
      "ARCH-VLEVEL-037"
    ],
    [
      "REQ-VLEVEL-946",
      "ARCH-VLEVEL-037"
    ],
    [
      "ARCH-VRUNGS-054",
      "SYS-VMODEL-107"
    ],
    [
      "SYS-AUTHOR-101",
      "SYS-SSOT-001"
    ],
    [
      "SYS-GATE-102",
      "SYS-SSOT-001"
    ],
    [
      "SYS-QUALITY-104",
      "SYS-SSOT-001"
    ],
    [
      "SYS-READ-103",
      "SYS-SSOT-001"
    ],
    [
      "SYS-REPORT-105",
      "SYS-SSOT-001"
    ],
    [
      "SYS-SHIP-108",
      "SYS-SSOT-001"
    ],
    [
      "SYS-VISUAL-106",
      "SYS-SSOT-001"
    ],
    [
      "SYS-VMODEL-107",
      "SYS-SSOT-001"
    ]
  ],
  "todos": [
    {
      "name": "Roadmap tab (Gantt viewer)",
      "lane": "feature",
      "milestone": "v1.13",
      "done": true
    },
    {
      "name": "TODO.md template + engine parsing",
      "lane": "feature",
      "milestone": "v1.13",
      "done": true
    },
    {
      "name": "Requirement readability linter (lint)",
      "lane": "feature",
      "milestone": "v1.14",
      "done": true
    },
    {
      "name": "Single-requirement dossier (show)",
      "lane": "feature",
      "milestone": "v1.14",
      "done": true
    },
    {
      "name": "Duplicate-capability detector (similar)",
      "lane": "feature",
      "milestone": "v1.14",
      "done": true
    },
    {
      "name": "Corpus health snapshot (health)",
      "lane": "ops",
      "milestone": "v1.14",
      "done": true
    },
    {
      "name": "promote-todo command",
      "lane": "feature",
      "milestone": "v1.14",
      "done": true
    },
    {
      "name": "Gate validation for milestone IDs",
      "lane": "ops",
      "milestone": "v1.14",
      "done": true
    },
    {
      "name": "Requirement quality checker (AI)",
      "lane": "feature",
      "milestone": "v1.14",
      "done": true
    },
    {
      "name": "Test-link integrity gate check (testlink)",
      "lane": "ops",
      "milestone": "v1.15",
      "done": true
    },
    {
      "name": "Scan cache (opt-in `--cache` flag — ARCH-SCANCACHE-023)",
      "lane": "ops",
      "milestone": "v1.15",
      "done": true
    },
    {
      "name": "Phantom-member fix: context-aware tag scan (fence/backtick/indent + .py string state)",
      "lane": "bus",
      "milestone": "v1.16",
      "done": true
    },
    {
      "name": "Drift doc reconciliation (direction B): SKILL.md, CLAUDE.md, SYS-SSOT-001 AC-1 split + severity table",
      "lane": "ops",
      "milestone": "v1.16",
      "done": true
    },
    {
      "name": "check --strict (promotes test-link integrity + confirmed-drift to error)",
      "lane": "feature",
      "milestone": "v1.16",
      "done": true
    },
    {
      "name": "check --json (structured gate output, exit-code aligned)",
      "lane": "feature",
      "milestone": "v1.16",
      "done": true
    },
    {
      "name": "Frontmatter '#' scalar truncation fix",
      "lane": "bus",
      "milestone": "v1.16",
      "done": true
    },
    {
      "name": "Off-status drift blind spot fix",
      "lane": "bus",
      "milestone": "v1.16",
      "done": true
    },
    {
      "name": "check --since <ref> (git-scoped gate, full-scan fallback + WARN)",
      "lane": "feature",
      "milestone": "v1.16",
      "done": true
    },
    {
      "name": "`generated-from:` accepting >1 requirement ID — drift a system/explainer doc when ANY referenced requirement changes",
      "lane": "feature",
      "milestone": "v1.17",
      "done": true
    },
    {
      "name": "WARN when a large docs/ HTML/generated bundle carries no `generated-from:` tag at all (surface the doc-sync blind spot instead of silently ignoring it)",
      "lane": "ops",
      "milestone": "v1.17",
      "done": true
    },
    {
      "name": "Member-hash drift (reverse direction): record member content hashes in `_reqlock.json` and WARN when a `confirmed` requirement's member changed since lock but the requirement was not re-touched/re-confirmed — catches \"behavior shipped, spec not updated\". Default WARN (likely `--strict`-promotable) to bound the noise of every code edit nagging its requirement.",
      "lane": "feature",
      "milestone": "v1.18",
      "done": true
    },
    {
      "name": "~~Internal-consistency lint: flag a requirement whose structured fields moved to a new model while its prose Contract/AC still assert the superseded model.~~ **DROPPED**",
      "lane": "feature",
      "milestone": "v1.18",
      "done": true
    },
    {
      "name": "Project presentation page (`site`): inject/refresh engine-owned nav + stats regions into docs/architecture.html (scaffold if absent)",
      "lane": "feature",
      "milestone": "v1.35",
      "done": true
    },
    {
      "name": "Excalidraw scene builder — core stdlib API (.excalidraw scene + self-contained HTML viewer)",
      "lane": "feature",
      "milestone": "v2.4",
      "done": true
    },
    {
      "name": "Excalidraw quality gates at save() (overlap, crossing, legend, text-overflow, text-overlap; later: short-arrow, label-fit)",
      "lane": "feature",
      "milestone": "v2.4",
      "done": true
    },
    {
      "name": "Excalidraw builder CLI verbs (`render` a scene → viewer; `discover` a repo → generator stub)",
      "lane": "feature",
      "milestone": "v2.4",
      "done": true
    },
    {
      "name": "Untagged-code coverage signal: read-only `untagged` count in `health` (code traced to no requirement)",
      "lane": "feature",
      "milestone": "v2.6",
      "done": true
    },
    {
      "name": "Command registry: one declarative `COMMANDS` structure in reqmap.py; argparse choices derived from it (hand-written `choices=[...]` literal removed). Dispatch ladder left intact (de-risked); CLI behaviour byte-identical (TestCli unchanged).",
      "lane": "bus",
      "milestone": "v2.7",
      "done": true
    },
    {
      "name": "Generate `tool_definition.json` (function-calling schema) + the command-reference TABLE inside SKILL.universal.md (delimited region; curated WHY/WHEN prose stays hand-authored) from the registry.",
      "lane": "feature",
      "milestone": "v2.7",
      "done": true
    },
    {
      "name": "Drift-guard: `gate` (incl. `--json`) regenerates the artifacts in-memory and byte-compares vs committed → error (exit 1), mirroring `map --check`.",
      "lane": "ops",
      "milestone": "v2.7",
      "done": true
    },
    {
      "name": "Registry lag in `health`: commits since the requirements dir was last touched",
      "lane": "ops",
      "milestone": "v2.12",
      "done": true
    },
    {
      "name": "Ranked free-text requirement search (`search`), shared scoring model with the viewer",
      "lane": "feature",
      "milestone": "v2.13",
      "done": true
    },
    {
      "name": "Requirement clarity lint enforced in CI, the dev hook and the published action",
      "lane": "ops",
      "milestone": "v2.14",
      "done": true
    },
    {
      "name": "Plain present requirement voice: named subject, no `shall`, tightened lint ceilings",
      "lane": "feature",
      "milestone": "v2.15",
      "done": true
    },
    {
      "name": "V-model verification levels: `@unit`/`@integration`/`@system` on `tested-by:`, `validated-against:` activated as the validation link",
      "lane": "feature",
      "milestone": "v2.16",
      "done": true
    },
    {
      "name": "TO VERIFY: decide the system grouping — 5-8 needs instead of one",
      "lane": "ops",
      "milestone": "v2.32",
      "done": false
    },
    {
      "name": "TO VERIFY: satisfies from 11 to 54",
      "lane": "ops",
      "milestone": "v2.32",
      "done": false
    },
    {
      "name": "TO VERIFY: verifiable by: from 2 to 54",
      "lane": "ops",
      "milestone": "v2.32",
      "done": false
    },
    {
      "name": "TO VERIFY: rename by alias — need->system, aggregate->architecture",
      "lane": "feature",
      "milestone": "v2.32",
      "done": false
    },
    {
      "name": "TO VERIFY: decide the fate of the architecture layer — populate it or delete it",
      "lane": "ops",
      "milestone": "v2.32",
      "done": false
    },
    {
      "name": "TO VERIFY: fan-out check 5-20, warn-only",
      "lane": "feature",
      "milestone": "v2.32",
      "done": false
    },
    {
      "name": "TO VERIFY: split ARCH-MAP-007 (35 clauses)",
      "lane": "ops",
      "milestone": "v2.32",
      "done": false
    },
    {
      "name": "TO VERIFY: decide the unit for long-sentence / statement-too-long",
      "lane": "bus",
      "milestone": "v2.32",
      "done": false
    },
    {
      "name": "TO VERIFY: form: atomic — 54 -> ~665 nodes. Not now",
      "lane": "feature",
      "milestone": "v2.32",
      "done": false
    },
    {
      "name": "MCP server exposing reqmap commands as MCP tools — stdlib-only, generated from the command registry. Demand-gated; see conditions above.",
      "lane": "feature",
      "milestone": "v2.8",
      "done": false
    },
    {
      "name": "Decide the beachhead domain (4 named candidates) — tie-break on which gets one external user in 30 days; pipelines currently leads since this repo is already its own demo",
      "lane": "ops",
      "milestone": "v2.9",
      "done": false
    },
    {
      "name": "Rewrite the README lead around agent drift, not stale specs; fix the ~3700-vs-5,199 line-count claim; add TODO.md and test_reqmap.py to the layout tree",
      "lane": "ops",
      "milestone": "v2.9",
      "done": true
    },
    {
      "name": "Extend scan reach: add .sh/.tf to CODE_EXTS, extensionless basename matching for Dockerfile/Makefile, let the scan root reach the repo root instead of stopping at plugin/ — and cover docs/, not just .github/, so ARCH-DOCBUNDLE-026 finally sees its own bundle",
      "lane": "bus",
      "milestone": "v2.9",
      "done": true
    },
    {
      "name": "Tag this repo's own pipeline (ci.yml, check/action.yml, .githooks/, sync_reqmap.sh) — currently zero tags across all of them",
      "lane": "ops",
      "milestone": "v2.9",
      "done": true
    },
    {
      "name": "Ship one worked example end-to-end: one requirement, one agent session, one drift the gate caught, real terminal output — every claim on the README is currently architectural, none demonstrated",
      "lane": "ops",
      "milestone": "v2.9",
      "done": true
    },
    {
      "name": "app/src/lib/data.js's BAKED requirement data: add a CI/gate step that diffs it against the live registry",
      "lane": "feature",
      "milestone": "v2.9",
      "done": true
    },
    {
      "name": "Fix the .reqmapignore comment claiming the viewer is \"tested via npm build\" — there is no npm step in CI",
      "lane": "ops",
      "milestone": "v2.9",
      "done": true
    },
    {
      "name": "`lint` check for redundant \"shall\"/\"must\" on a Contract clause (ARCH-LINTCHECKS-025 AC-10)",
      "lane": "feature",
      "milestone": "v2.9",
      "done": true
    },
    {
      "name": "Opt-in requirement-content translation: `reqmap.py translate` + viewer badge (ARCH-TRANSLATE-044)",
      "lane": "feature",
      "milestone": "v2.9",
      "done": true
    },
    {
      "name": "Scan-evidence run 6 — Management_Dashboard (TS/TSX, SQL, shell, Dockerfile, Caddyfile, Prisma, YAML, JSON, Markdown): four engine defects found and fixed in v2.27.0",
      "lane": "ops",
      "milestone": "v2.9",
      "done": true
    },
    {
      "name": "Scan-evidence runs 1–5 — zlib+curl (C/C++), excalidraw (TS/React), awesome-compose (infra), gin+httpx (Go/Python), fabric (Markdown prompts): ten engine fixes in v2.28.0",
      "lane": "ops",
      "milestone": "v2.9",
      "done": true
    },
    {
      "name": "Bring app/src/ under the gate: real per-file `# implements:`/`generated-from:` tagging of app/src/**/*.jsx",
      "lane": "ops",
      "milestone": "v2.9",
      "done": true
    },
    {
      "name": "Fix check@v1: frozen at v2.1.0 while shipping v2.17.0 — re-point it or pull it from the README",
      "lane": "ops",
      "milestone": "v3",
      "done": true
    },
    {
      "name": "Scan vs `.gitignore`: a gitignored-but-tagged file silently changes committed generated artifacts — make the scan skip gitignored paths, or WARN when a member resolves to one",
      "lane": "bus",
      "milestone": "v3",
      "done": true
    },
    {
      "name": "Add an npm job asserting `git diff --exit-code` on plugin/scripts/_map_viewer.html, and the same check for regenerated Excalidraw diagrams — closes the committed-build-artifact hole for both",
      "lane": "ops",
      "milestone": "v3",
      "done": true
    },
    {
      "name": "Stop squashing main; land the next change as a visible PR — this also revives the changelog gate, which currently no-ops because HEAD~1 doesn't resolve",
      "lane": "ops",
      "milestone": "v3",
      "done": true
    },
    {
      "name": "Extract ADRs to docs/adr/ — ten decisions pulled out of changelog prose, including the deliberate V-model omission",
      "lane": "ops",
      "milestone": "v3",
      "done": true
    },
    {
      "name": "CI matrix: Python 3.9/3.12/3.13 x ubuntu/windows — prove the `-X utf8` usage actually holds",
      "lane": "ops",
      "milestone": "v3",
      "done": true
    },
    {
      "name": "Coverage + ruff on the engine, published in the job summary",
      "lane": "ops",
      "milestone": "v3",
      "done": true
    },
    {
      "name": "Declare and assert the Python floor (3.7 today, accidental)",
      "lane": "bus",
      "milestone": "v3",
      "done": true
    },
    {
      "name": "Fix the upgrade path: warn_if_stale is silent in CI, exactly where it matters",
      "lane": "bus",
      "milestone": "v3",
      "done": true
    },
    {
      "name": "Repo hygiene: CONTRIBUTING, SECURITY.md, issue and PR templates",
      "lane": "ops",
      "milestone": "v3",
      "done": true
    },
    {
      "name": "Adversarial tests on injected HTML (</script>, U+2028, lone surrogates) and a published benchmark on a 10k-file tree",
      "lane": "bus",
      "milestone": "v3",
      "done": true
    },
    {
      "name": "One tree walk instead of three: `gate` calls scan_members + scan_ac_verifies + scan_test_levels, each opening every file — 3.06s + 2.76s + 2.81s of its 8.49s on a 10k-file tree",
      "lane": "bus",
      "milestone": "v3",
      "done": true
    },
    {
      "name": "Settle the 5,199-line module: split with a concatenating build, or keep single-file with an ADR plus a CI line-count budget",
      "lane": "bus",
      "milestone": "v3",
      "done": true
    },
    {
      "name": "Get to n=2: one external repo running the gate",
      "lane": "ops",
      "milestone": "v3",
      "done": false
    },
    {
      "name": "Run the gate over a real C/C++ tree (headers, macros, generated code) — the evidence behind the automotive credibility line",
      "lane": "ops",
      "milestone": "v3",
      "done": true
    },
    {
      "name": "Revisit BSL once there's someone who'd contribute",
      "lane": "ops",
      "milestone": "v3",
      "done": false
    },
    {
      "name": "Viewer i18n: an EN/RO (or pluggable-locale) toggle button for the self-contained HTML viewer, translating static UI chrome only (nav, tab labels, section headers, buttons, badges) — never requirement content (titles/intent/contract/AC stay author-language)",
      "lane": "feature",
      "milestone": "v3",
      "done": true
    },
    {
      "name": "`binding_hash`'s `_NORMATIVE_HEADING_RE` (line 1741) omits \"cases\" from its keyword list, so a `## Cases` section is silently excluded from the drift hash -- editing a CASE-N criterion never trips DRIFT",
      "lane": "bus",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`load_requirements` (line 909): a file's prose preamble block (before its first `---` block) can fall back to the filename for its id same as block 0 proper -- if the file is named after its real first requirement's id, the preamble wins the `rid in reqs` collision and the real requirement is silently dropped, with only a stderr warning",
      "lane": "bus",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`_SH_TEST_NAME_RE` (line 2117, feeds `_test_link_problem`) requires a `._-` separator on both sides of \"test\", so a shell test file literally named `test.sh` or `tests.sh` fails the filename-convention check and gets a false test-link-integrity warning",
      "lane": "bus",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`cmd_check` under `--since` (lines 2259 and 2268): the implements/tested-by coverage check tests `rid in members` against the `--since`-FILTERED member dict instead of full coverage, so a confirmed requirement whose implements tag is untouched but whose tested-by file changed (or vice versa) gets a false ERROR (implements side, flips exit code) or false WARN (tested-by side)",
      "lane": "ops",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`cmd_promote_todo`'s `layer:` frontmatter substitution (line 2734) has no success check, unlike the milestone/title anchor handling right below it -- a custom template whose layer line doesn't literally match `layer: feature` silently keeps the wrong layer while the success message reports the intended one",
      "lane": "feature",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`cmd_promote` (line 2865): EOL detection is \"any CRLF anywhere in the file\" then blanket-converts every remaining bare `\\n` to `\\r\\n`, so a file with mixed line endings gets its bare-LF lines silently rewritten -- contradicts the adjacent comment's promise to preserve the file's own line endings verbatim",
      "lane": "feature",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`_py_facts` (lines 3104 and 3121) crashes with `IndexError` when a module/function/class docstring is non-empty but whitespace-only after `ast.get_docstring`'s cleanup -- aborts the whole `plan`/`candidates` run on one such .py file instead of degrading to empty facts as its own docstring promises",
      "lane": "feature",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`cmd_translate` (line 3901) and `_load_translations` (line 3953) both call `.get(...)`/`.items()` on a parsed i18n cache JSON without checking it's a dict first, unlike every other cache loader in the file (scancache, lock, memberlock, capmap all guard with `isinstance(data, dict)`) -- a malformed `_i18n/<locale>.json` crashes `translate`/`map`/`map --check`/`export` instead of failing open per entry as documented",
      "lane": "feature",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`cmd_next` (lines 4124 and 4166): the early \"Nothing pending\" return fires whenever the four risk buckets + untagged-files are empty, even though Granularity/Redundancy (computed later in the same function) may still be non-empty -- and when Granularity DOES print, it ignores `top_n` truncation that every sibling bucket applies",
      "lane": "feature",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`lint_requirement`'s vague-term and redundant-modal checks (lines 4496 and 4510) both call `_lint_prose(body, \"contract\")` with the literal legacy label instead of iterating `CONTRACT_LABELS` -- both checks are dead on every requirement using the current `## Description` heading",
      "lane": "feature",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`_already_decomposed` (line 4639): `except OSError` when reading sibling requirement files doesn't catch `UnicodeDecodeError` (a `ValueError` subclass), unlike the same read pattern elsewhere in the file that deliberately catches `(OSError, ValueError)` -- one invalid-UTF-8 sibling crashes `lint --decompose` instead of being skipped",
      "lane": "feature",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`_commits_since_reqs_touch` (line 5184) passes `reqs_dir` un-rooted to `git -C code_root log -- reqs_dir`, so under `--code ..` (this repo's own documented common invocation) the pathspec resolves relative to `code_root` instead of the original cwd and the registry-lag signal silently goes missing from `health`",
      "lane": "ops",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`cmd_health` (line 5252): the \"healthy on every axis\" tally waives the tested-by requirement for `layer: need` (`has_test or is_need`) but not for `layer: aggregate`, even though `_impl_exempt` -- the one predicate CLAUDE.md documents as shared by gate/health/map/confirm -- waives both",
      "lane": "ops",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`_wipe` (lines 5382 and 5386, `init --wipe`) reads and rewrites tagged source files without `newline=\"\"`, unlike `cmd_promote`/`_mark_todo_done`/`_apply_verifies` which all deliberately pass it -- stripping one tag comment silently normalizes the ENTIRE file's line endings to the host platform's `os.linesep`",
      "lane": "bus",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`_section` (line 5623) and `_bullets` (line 5668) use `lstrip(\"- \")` / `lstrip(\"> \")`, which strips a leading CHARACTER SET, not a two-character prefix, corrupting requirement content that itself starts with `-` or `>` (e.g. `- -1 means error`, `> >100 requests/sec`) -- feeds the committed `_map.json`'s `input`/`output`/`contract`/`desc` fields",
      "lane": "bus",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`_emit_area_subgraphs` (line 5816): the per-area Mermaid subgraph label embeds the raw `area:` frontmatter string directly instead of passing it through `_mlabel` like the sibling `_mermaid_deps` does -- an area name containing a `\"` breaks the generated Mermaid syntax in the committed `_map.md`",
      "lane": "feature",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`_mermaid_deps` (line 5902): area-level node ids (`a_{_safe_id(label)}`) have no collision guard, unlike `_emit_area_subgraphs` in the same file which explicitly disambiguates two area labels that sanitize to the same id -- two such areas collapse into one Mermaid node and a real inter-area dependency renders as a self-loop",
      "lane": "feature",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`_LEGEND_MD` (line 6030) has 4 entries but `_build_md_text` now renders 5 diagrams (Specification Hierarchy was added as a new first diagram without a matching legend entry) -- every legend caption is shifted one diagram off and the last diagram (Risk & Unknowns) gets an empty legend",
      "lane": "feature",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`cmd_site` scaffold (`_render_region`, lines 6248 and 6256): the injected `nav`/`stats` regions wrap their content in `reqmap-nav`/`reqmap-stats` divs that match no selector in `SITE_TEMPLATE`'s CSS -- on a freshly scaffolded page the nav links render unstyled/uncrammed and the 5 stat cards stack vertically instead of the intended 6-column grid",
      "lane": "feature",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`_apply_verifies` (line 6922): the idempotency check does substring containment (`if tag in line`) on the literal tag text, so an existing `CASE-11`/`AC-11` tag is mistaken for a proposed `CASE-1`/`AC-1` tag already being present -- the real, missing tag is silently never written",
      "lane": "feature",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`_write_region` (line 761, used by `gen-integration` to patch SKILL.universal.md's command table) reads and writes its target file in default text mode with no `newline=\"\"`, so on POSIX it universal-newline-translates the WHOLE file CRLF -> LF on read and writes it back with no re-translation -- contradicts its own docstring's promise that \"prose outside is untouched\"",
      "lane": "bus",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "`cmd_gen_integration` (line 768) writes `tool_definition.json` via the same bare text-mode open with no `newline` argument, so regenerating on a non-Windows box flips the whole committed CRLF file to LF even though the JSON content is unchanged",
      "lane": "ops",
      "milestone": "v3.1",
      "done": true
    },
    {
      "name": "Regenerate SKILL.md's command list from the COMMANDS registry",
      "lane": "ops",
      "milestone": "v4.1",
      "done": true
    },
    {
      "name": "Rank a Romanian query, not just match it literally",
      "lane": "feature",
      "milestone": "v4.1",
      "done": false
    },
    {
      "name": "Senate review: is the C&K metrics pillar the right shape?",
      "lane": "ops",
      "milestone": "v5.2",
      "done": false
    }
  ],
  "commands": [
    {
      "name": "init",
      "group": "author",
      "summary": "First-use bootstrap: scaffold requirements/ and .reqmapignore if missing, draft requirements from existing code and prose, build the lock and map, and print guided next steps. Idempotent — safe to re-run; never clobbers an existing .reqmapignore. --plan emits the extraction plan as JSON and writes no requirement files, for looking before authoring.",
      "arg": null,
      "flags": [
        {
          "flag": "--plan",
          "help": "Emit the extraction plan as JSON instead of writing requirement files."
        },
        {
          "flag": "--out",
          "help": "With --plan: write the plan JSON here ('-' or omitted = stdout)."
        },
        {
          "flag": "--md-glob",
          "help": "With --plan: also scan these non-code globs for capabilities (repeatable)."
        },
        {
          "flag": "--wipe",
          "help": "Hard-reset: delete all non-generated requirements and strip membership tags from source files before re-extracting."
        },
        {
          "flag": "--no-site",
          "help": "Skip the final site step (scaffolding docs/architecture.html)."
        }
      ]
    },
    {
      "name": "new",
      "group": "author",
      "summary": "Scaffold a new blank requirement from the built-in template. Use --from-todo and --id together to pre-fill from a TODO.md item instead.",
      "arg": "AREA-NAME-NNN",
      "flags": [
        {
          "flag": "--id",
          "help": "Requirement ID in AREA-NAME-NNN format (e.g. AUTH-LOGIN-001). Required when using --from-todo."
        },
        {
          "flag": "--from-todo",
          "help": "Scaffold the requirement from a TODO.md item matched by this name (use with --id; add --mark-done to flip the item to [x])."
        },
        {
          "flag": "--mark-done",
          "help": "Also flip the matched TODO.md item to [x] (off by default). Only used with --from-todo."
        }
      ]
    },
    {
      "name": "gate",
      "group": "read",
      "summary": "The commit/CI verdict, and every read-only question you can ask the corpus. Bare, it verifies that every code tag resolves to a real requirement, that every confirmed requirement has at least one implements: member, and that drift has not been introduced since the last sync, then checks requirement readability and map freshness. Exits non-zero on link-sync errors only. Never writes anything. The mode flags answer one question each instead of running the verdict: --audit for the whole problem report, --risk for what to do next, --show for one requirement's dossier, --search to rank by relevance, --dupes for overlapping contracts, --design for the code review, --review and --implement for the two machine-readable plans.",
      "arg": null,
      "flags": [
        {
          "flag": "--audit",
          "help": "Print every pass that discovers a problem as one report: the gate, corpus risk, duplicate contracts, design signals and tag coverage. The exit code still comes from the gate alone."
        },
        {
          "flag": "--risk",
          "help": "Print the corpus risk snapshot and the actionable signals, most urgent first."
        },
        {
          "flag": "--show",
          "help": "Print one requirement's dossier: intent, contract, dependencies both ways, code members with file:line, open questions and risk signals."
        },
        {
          "flag": "--search",
          "help": "Rank requirements by lexical relevance to a free-text query."
        },
        {
          "flag": "--dupes",
          "help": "Rank requirement pairs whose contracts overlap, most similar first."
        },
        {
          "flag": "--design",
          "help": "Print the advisory design review of the code. Never part of the verdict."
        },
        {
          "flag": "--review",
          "help": "Emit the deterministic review plan for one requirement, as JSON."
        },
        {
          "flag": "--implement",
          "help": "Emit the implementation brief for one requirement: obligations, required tags, similar existing code."
        },
        {
          "flag": "--all",
          "help": "With --risk: expand every bucket instead of the top few."
        },
        {
          "flag": "--untagged",
          "help": "With --risk: report membership-tag coverage per directory."
        },
        {
          "flag": "--badge",
          "help": "With --risk: print the coherence score as a badge string."
        },
        {
          "flag": "--threshold",
          "help": "With --dupes: override the similarity threshold."
        },
        {
          "flag": "--top",
          "help": "With --search or --dupes: how many results to print."
        },
        {
          "flag": "--strict",
          "help": "Promote drift and test-link integrity warnings to errors. Useful in CI when all requirements are confirmed."
        },
        {
          "flag": "--json",
          "help": "Emit structured JSON output instead of human-readable text."
        },
        {
          "flag": "--since",
          "help": "Scope the gate to requirements whose member files changed since this git ref (e.g. 'main', 'HEAD~1')."
        }
      ]
    },
    {
      "name": "sync",
      "group": "build",
      "summary": "The write path. Rescan code members, advance the drift baseline, and regenerate the map, the findings file and the generated integration artifacts in one step. Run after editing requirement files or tagging new code members. --accept-drift is required when a confirmed or implemented contract changed. --suggest-verifies proposes per-criterion verifies: tags, and writes them with --apply.",
      "arg": null,
      "flags": [
        {
          "flag": "--retire",
          "help": "Take this requirement out of service instead of confirming it. Prints the blast radius; writes nothing without --apply."
        },
        {
          "flag": "--delete",
          "help": "With --retire: also remove the block, its lock entries and its membership tags. Never a function body."
        },
        {
          "flag": "--apply",
          "help": "With --retire: actually write the change. Without it, the run is a dry report."
        },
        {
          "flag": "--force",
          "help": "With --retire: proceed even though dependents still point at this requirement."
        },
        {
          "flag": "--suggest-verifies",
          "help": "Propose per-criterion `verifies:` tags for tests already named after the criterion they check."
        },
        {
          "flag": "--apply",
          "help": "With --suggest-verifies: write the proposed tags into the test files."
        },
        {
          "flag": "--findings",
          "help": "Also regenerate the aggregated open-questions file."
        },
        {
          "flag": "--accept-drift",
          "help": "Explicitly advance the baseline when a confirmed or implemented contract changed. Required when those contracts differ from the lock; sync exits non-zero without it."
        },
        {
          "flag": "--strict",
          "help": "Promote drift and test-link integrity from warn to error."
        }
      ]
    },
    {
      "name": "clarify",
      "group": "author",
      "summary": "Ask what a requirement has not answered yet: vague terms with no threshold, numbers with no unit, unbounded quantities, clauses with no case, a missing failure path. Read-only, always exit 0, never a gate rule. --decompose is the write half of the same question: it scaffolds an over-scoped requirement's clauses into requirements of their own. Run it before implementing, so the ambiguity is resolved in the requirement instead of guessed in code.",
      "arg": "AREA-NAME-NNN",
      "flags": [
        {
          "flag": "--decompose",
          "help": "Scaffold an over-scoped requirement's clauses into requirements of their own."
        },
        {
          "flag": "--json",
          "help": "Emit the questions as JSON for an agent to answer."
        }
      ]
    }
  ],
  "design": {
    "files": 31,
    "clean_files": 9,
    "score": 29,
    "candidates": {
      "encapsulation": 47,
      "abstraction": 46,
      "inheritance": 0,
      "polymorphism": 0,
      "metrics": 3,
      "standards": 25
    },
    "findings": [
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "app/scripts/install-viewer.mjs",
        "line": 17,
        "name": "install-viewer.mjs",
        "detail": "1 line(s) wider than 100 columns, first at line 17"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "app/scripts/ssr-smoke.jsx",
        "line": 1,
        "name": "ssr-smoke.jsx",
        "detail": "47 line(s) wider than 100 columns, first at line 1"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "app/src/App.jsx",
        "line": 36,
        "name": "App.jsx",
        "detail": "20 line(s) wider than 100 columns, first at line 36"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "app/src/App.jsx",
        "line": 106,
        "name": "Rail",
        "detail": "`Rail` is 119 lines (over 80)"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "app/src/lib/data.js",
        "line": 7,
        "name": "data.js",
        "detail": "78 line(s) wider than 100 columns, first at line 7"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "app/src/lib/i18n.jsx",
        "line": 60,
        "name": "i18n.jsx",
        "detail": "20 line(s) wider than 100 columns, first at line 60"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "app/src/lib/icons.jsx",
        "line": 6,
        "name": "icons.jsx",
        "detail": "10 line(s) wider than 100 columns, first at line 6"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "app/src/lib/layout.js",
        "line": 20,
        "name": "layout.js",
        "detail": "9 line(s) wider than 100 columns, first at line 20"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "app/src/lib/loadData.js",
        "line": 50,
        "name": "loadData.js",
        "detail": "2 line(s) wider than 100 columns, first at line 50"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "app/src/lib/search.js",
        "line": 78,
        "name": "search.js",
        "detail": "1 line(s) wider than 100 columns, first at line 78"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "app/src/lib/useDragPan.js",
        "line": 21,
        "name": "useDragPan.js",
        "detail": "1 line(s) wider than 100 columns, first at line 21"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "app/src/views/CommandsView.jsx",
        "line": 23,
        "name": "CommandsView",
        "detail": "`CommandsView` nests 6 levels deep (over 4)"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "app/src/views/CommandsView.jsx",
        "line": 32,
        "name": "CommandsView.jsx",
        "detail": "2 line(s) wider than 100 columns, first at line 32"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "app/src/views/ExplorerView.jsx",
        "line": 76,
        "name": "ExplorerView.jsx",
        "detail": "11 line(s) wider than 100 columns, first at line 76"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "app/src/views/ExplorerView.jsx",
        "line": 102,
        "name": "ExplorerView",
        "detail": "`ExplorerView` is 163 lines (over 80)"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "app/src/views/MapView.jsx",
        "line": 13,
        "name": "NodeBox",
        "detail": "`NodeBox` takes 7 parameters (over 6)"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "app/src/views/MapView.jsx",
        "line": 91,
        "name": "MapView.jsx",
        "detail": "29 line(s) wider than 100 columns, first at line 91"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "app/src/views/MapView.jsx",
        "line": 141,
        "name": "MapView",
        "detail": "`MapView` nests 5 levels deep (over 4)"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "app/src/views/MapView.jsx",
        "line": 141,
        "name": "MapView",
        "detail": "`MapView` is 116 lines (over 80)"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "app/src/views/ProblemsView.jsx",
        "line": 36,
        "name": "ProblemsView.jsx",
        "detail": "13 line(s) wider than 100 columns, first at line 36"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "app/src/views/ProblemsView.jsx",
        "line": 90,
        "name": "ProblemsView",
        "detail": "`ProblemsView` nests 6 levels deep (over 4)"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "app/src/views/ProblemsView.jsx",
        "line": 90,
        "name": "ProblemsView",
        "detail": "`ProblemsView` is 117 lines (over 80)"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "app/src/views/RoadmapView.jsx",
        "line": 31,
        "name": "RoadmapView.jsx",
        "detail": "21 line(s) wider than 100 columns, first at line 31"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "app/src/views/RoadmapView.jsx",
        "line": 61,
        "name": "RoadmapView",
        "detail": "`RoadmapView` nests 6 levels deep (over 4)"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "app/src/views/RoadmapView.jsx",
        "line": 61,
        "name": "RoadmapView",
        "detail": "`RoadmapView` is 118 lines (over 80)"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "app/src/views/SpecView.jsx",
        "line": 16,
        "name": "SpecView.jsx",
        "detail": "21 line(s) wider than 100 columns, first at line 16"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "app/src/views/SpecView.jsx",
        "line": 80,
        "name": "TranslatedProse",
        "detail": "`TranslatedProse` nests 5 levels deep (over 4)"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "app/src/views/SpecView.jsx",
        "line": 129,
        "name": "SpecDoc",
        "detail": "`SpecDoc` is 121 lines (over 80)"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "check/engine_staleness.py",
        "line": 64,
        "name": "engine_staleness.py",
        "detail": "3 line(s) wider than 100 columns, first at line 64"
      },
      {
        "pillar": "standards",
        "kind": "file-too-long",
        "file": "plugin/scripts/reqmap.py",
        "line": 1,
        "name": "reqmap.py",
        "detail": "9372 lines (over 500)"
      },
      {
        "pillar": "standards",
        "kind": "too-many-definitions",
        "file": "plugin/scripts/reqmap.py",
        "line": 1,
        "name": "reqmap.py",
        "detail": "294 top-level definitions (over 30)"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "plugin/scripts/reqmap.py",
        "line": 7,
        "name": "reqmap.py",
        "detail": "311 line(s) wider than 100 columns, first at line 7"
      },
      {
        "pillar": "abstraction",
        "kind": "prefix-family",
        "file": "plugin/scripts/reqmap.py",
        "line": 747,
        "name": "cmd",
        "detail": "24 top-level functions start with `cmd`: cmd_audit, cmd_candidates, cmd_check, cmd_clarify, cmd_coverage, cmd_design"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "plugin/scripts/reqmap.py",
        "line": 939,
        "name": "parse_frontmatter",
        "detail": "`parse_frontmatter` nests 6 levels deep (over 4)"
      },
      {
        "pillar": "abstraction",
        "kind": "prefix-family",
        "file": "plugin/scripts/reqmap.py",
        "line": 1003,
        "name": "load",
        "detail": "9 top-level functions start with `load`: _load_capmap, _load_scancache, _load_translations, load_clarifylock, load_config, load_ignore"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "plugin/scripts/reqmap.py",
        "line": 1077,
        "name": "load_ignore",
        "detail": "`load_ignore` nests 5 levels deep (over 4)"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "plugin/scripts/reqmap.py",
        "line": 1097,
        "name": "_strip_py_strings",
        "detail": "`_strip_py_strings` nests 5 levels deep (over 4)"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "plugin/scripts/reqmap.py",
        "line": 1146,
        "name": "_scan_file_tags",
        "detail": "`_scan_file_tags` nests 5 levels deep (over 4)"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "plugin/scripts/reqmap.py",
        "line": 1146,
        "name": "_scan_file_tags",
        "detail": "`_scan_file_tags` is 90 lines (over 80)"
      },
      {
        "pillar": "abstraction",
        "kind": "prefix-family",
        "file": "plugin/scripts/reqmap.py",
        "line": 1146,
        "name": "scan",
        "detail": "6 top-level functions start with `scan`: _scan_file_tags, _scan_untagged, scan_ac_verifies, scan_all, scan_members, scan_test_levels"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/scripts/reqmap.py",
        "line": 1311,
        "name": "scan_all",
        "detail": "cache, code_root, reqs_dir travel together through 3 functions: load, scan_all, scan_members"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "plugin/scripts/reqmap.py",
        "line": 1390,
        "name": "_vds_scan_array_body",
        "detail": "`_vds_scan_array_body` nests 6 levels deep (over 4)"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/scripts/reqmap.py",
        "line": 1552,
        "name": "untagged_doc_bundles",
        "detail": "code_root, members, reqs_dir travel together through 3 functions: __init__, _audit_summary, untagged_doc_bundles"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "plugin/scripts/reqmap.py",
        "line": 1645,
        "name": "scan_ac_verifies",
        "detail": "`scan_ac_verifies` nests 6 levels deep (over 4)"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "plugin/scripts/reqmap.py",
        "line": 1688,
        "name": "scan_test_levels",
        "detail": "`scan_test_levels` nests 6 levels deep (over 4)"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/scripts/reqmap.py",
        "line": 2185,
        "name": "member_drift",
        "detail": "code_root, members, reqs travel together through 4 functions: __init__, _audit_summary, _verifies_proposals, member_drift"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/scripts/reqmap.py",
        "line": 2411,
        "name": "__init__",
        "detail": "ac_cover, members, reqs travel together through 4 functions: __init__, _assemble_map_data, _build_map_data, _verifies_proposals"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/scripts/reqmap.py",
        "line": 2411,
        "name": "__init__",
        "detail": "members, reqs, reqs_dir travel together through 4 functions: __init__, _assemble_map_data, _audit_summary, _health_record"
      },
      {
        "pillar": "abstraction",
        "kind": "prefix-family",
        "file": "plugin/scripts/reqmap.py",
        "line": 2487,
        "name": "rule",
        "detail": "29 top-level functions start with `rule`: _rule_bus_only_system_level, _rule_case_coverage, _rule_confirmed_sections, _rule_corrupt_lock, _rule_dangling_tag, _rule_depends_on_cycle"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "plugin/scripts/reqmap.py",
        "line": 2886,
        "name": "cmd_check",
        "detail": "`cmd_check` nests 5 levels deep (over 4)"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "plugin/scripts/reqmap.py",
        "line": 2886,
        "name": "cmd_check",
        "detail": "`cmd_check` is 146 lines (over 80)"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "plugin/scripts/reqmap.py",
        "line": 3382,
        "name": "cmd_extract",
        "detail": "`cmd_extract` is 93 lines (over 80)"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "plugin/scripts/reqmap.py",
        "line": 3528,
        "name": "_py_facts",
        "detail": "`_py_facts` nests 5 levels deep (over 4)"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "plugin/scripts/reqmap.py",
        "line": 3684,
        "name": "cmd_candidates",
        "detail": "`cmd_candidates` is 108 lines (over 80)"
      },
      {
        "pillar": "abstraction",
        "kind": "prefix-family",
        "file": "plugin/scripts/reqmap.py",
        "line": 3821,
        "name": "render",
        "detail": "7 top-level functions start with `render`: _render_findings, _render_findings_raw, _render_findings_triaged, _render_region, render_html, render_json"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/scripts/reqmap.py",
        "line": 4195,
        "name": "_attach_translations",
        "detail": "data, reqs, reqs_dir travel together through 3 functions: _attach_translations, _map_check, _stale_artifacts"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/scripts/reqmap.py",
        "line": 4235,
        "name": "_assemble_map_data",
        "detail": "reqs, reqs_dir, root travel together through 3 functions: _assemble_map_data, _map_check, _stale_artifacts"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "plugin/scripts/reqmap.py",
        "line": 4300,
        "name": "cmd_next",
        "detail": "`cmd_next` is 128 lines (over 80)"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "plugin/scripts/reqmap.py",
        "line": 4591,
        "name": "lint_requirement",
        "detail": "`lint_requirement` is 241 lines (over 80)"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "plugin/scripts/reqmap.py",
        "line": 4957,
        "name": "cmd_lint",
        "detail": "`cmd_lint` nests 5 levels deep (over 4)"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "plugin/scripts/reqmap.py",
        "line": 5270,
        "name": "_test_suite_pairs",
        "detail": "`_test_suite_pairs` nests 5 levels deep (over 4)"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "plugin/scripts/reqmap.py",
        "line": 5518,
        "name": "cmd_audit",
        "detail": "`cmd_audit` is 124 lines (over 80)"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "plugin/scripts/reqmap.py",
        "line": 5779,
        "name": "_health_record",
        "detail": "`_health_record` is 83 lines (over 80)"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "plugin/scripts/reqmap.py",
        "line": 5864,
        "name": "cmd_health",
        "detail": "`cmd_health` is 105 lines (over 80)"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "plugin/scripts/reqmap.py",
        "line": 6005,
        "name": "_wipe",
        "detail": "`_wipe` nests 5 levels deep (over 4)"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "plugin/scripts/reqmap.py",
        "line": 7595,
        "name": "_verifies_proposals",
        "detail": "`_verifies_proposals` nests 5 levels deep (over 4)"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "plugin/scripts/reqmap.py",
        "line": 7805,
        "name": "_clarify_questions",
        "detail": "`_clarify_questions` is 90 lines (over 80)"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "plugin/scripts/reqmap.py",
        "line": 7973,
        "name": "cmd_implement",
        "detail": "`cmd_implement` is 82 lines (over 80)"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "plugin/scripts/reqmap.py",
        "line": 8108,
        "name": "cmd_retire",
        "detail": "`cmd_retire` is 97 lines (over 80)"
      },
      {
        "pillar": "abstraction",
        "kind": "prefix-family",
        "file": "plugin/scripts/reqmap.py",
        "line": 8461,
        "name": "design",
        "detail": "19 top-level functions start with `design`: _design_block_end, _design_brace, _design_chain_findings, _design_file, _design_files, _design_finding"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "plugin/scripts/reqmap.py",
        "line": 8755,
        "name": "_design_mask",
        "detail": "`_design_mask` nests 6 levels deep (over 4)"
      },
      {
        "pillar": "abstraction",
        "kind": "deep-nesting",
        "file": "plugin/scripts/reqmap.py",
        "line": 8812,
        "name": "_design_brace",
        "detail": "`_design_brace` nests 5 levels deep (over 4)"
      },
      {
        "pillar": "encapsulation",
        "kind": "global-state",
        "file": "plugin/scripts/reqmap.py",
        "line": 9004,
        "name": "apply_config",
        "detail": "`apply_config` writes module state: CODE_EXTS"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "plugin/scripts/reqmap.py",
        "line": 9044,
        "name": "main",
        "detail": "`main` is 290 lines (over 80)"
      },
      {
        "pillar": "standards",
        "kind": "file-too-long",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 1,
        "name": "excalidraw_builder.py",
        "detail": "1939 lines (over 500)"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 117,
        "name": "__init__",
        "detail": "h, w, x, y travel together through 15 functions: __init__, box, column, connector, data, decision"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 117,
        "name": "__init__",
        "detail": "h, x, y travel together through 16 functions: __init__, _vline, box, column, connector, data"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 159,
        "name": "__init__",
        "detail": "fill, group, stroke travel together through 3 functions: __init__, box, frame"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 159,
        "name": "__init__",
        "detail": "`__init__` takes 7 parameters (over 6)"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 178,
        "name": "__init__",
        "detail": "align, color, size travel together through 3 functions: __init__, label, title"
      },
      {
        "pillar": "metrics",
        "kind": "god-class",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 183,
        "name": "Scene",
        "detail": "`Scene` has 62 methods (over 20)"
      },
      {
        "pillar": "metrics",
        "kind": "high-response",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 183,
        "name": "Scene",
        "detail": "`Scene` reaches 154 methods (over 50): 62 of its own plus 92 it calls"
      },
      {
        "pillar": "metrics",
        "kind": "low-cohesion",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 183,
        "name": "Scene",
        "detail": "`Scene` scores LCOM 1409 (over 20) across 62 methods and 14 field(s)"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 186,
        "name": "excalidraw_builder.py",
        "detail": "9 line(s) wider than 100 columns, first at line 186"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 322,
        "name": "box",
        "detail": "h, text, w, x, y travel together through 10 functions: box, connector, data, decision, diamond, ellipse"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 322,
        "name": "box",
        "detail": "group, h, x, y travel together through 3 functions: _vline, box, frame"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 322,
        "name": "box",
        "detail": "font_size, x, y travel together through 7 functions: box, column, glossary, grid, legend, pipeline"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 322,
        "name": "box",
        "detail": "fill, font_size, shape travel together through 5 functions: _place, box, column, grid, row"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 322,
        "name": "box",
        "detail": "fill, font_size, h, shape, w, x, y travel together through 4 functions: box, column, grid, row"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 322,
        "name": "box",
        "detail": "group, text, x, y travel together through 3 functions: box, label, title"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 322,
        "name": "box",
        "detail": "text, x, y travel together through 12 functions: box, connector, data, decision, diamond, ellipse"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 322,
        "name": "box",
        "detail": "group, x, y travel together through 5 functions: _vline, box, frame, label, title"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 322,
        "name": "box",
        "detail": "fill, h, w, x, y travel together through 5 functions: box, column, frame, grid, row"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 322,
        "name": "box",
        "detail": "`box` takes 12 parameters (over 6)"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 479,
        "name": "section",
        "detail": "gap, title, x travel together through 3 functions: glossary, legend, section"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 479,
        "name": "section",
        "detail": "color, size, x travel together through 3 functions: label, section, title"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 496,
        "name": "pipeline",
        "detail": "connect, font_size, gap, x, y travel together through 3 functions: column, pipeline, row"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 496,
        "name": "pipeline",
        "detail": "font_size, gap, x, y travel together through 5 functions: column, glossary, legend, pipeline, row"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 496,
        "name": "pipeline",
        "detail": "`pipeline` takes 7 parameters (over 6)"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 557,
        "name": "frame",
        "detail": "`frame` takes 8 parameters (over 6)"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 604,
        "name": "row",
        "detail": "fill, font_size, h, items, shape, w, x, y travel together through 3 functions: column, grid, row"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 604,
        "name": "row",
        "detail": "`row` takes 10 parameters (over 6)"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 624,
        "name": "column",
        "detail": "`column` takes 10 parameters (over 6)"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 636,
        "name": "grid",
        "detail": "`grid` takes 11 parameters (over 6)"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 651,
        "name": "enclose",
        "detail": "`enclose` takes 8 parameters (over 6)"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 680,
        "name": "legend",
        "detail": "`legend` takes 8 parameters (over 6)"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 717,
        "name": "glossary",
        "detail": "`glossary` takes 7 parameters (over 6)"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 750,
        "name": "lane",
        "detail": "`lane` takes 7 parameters (over 6)"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 857,
        "name": "title",
        "detail": "`title` takes 7 parameters (over 6)"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 873,
        "name": "label",
        "detail": "`label` takes 7 parameters (over 6)"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 902,
        "name": "arrow",
        "detail": "color, dashed, end, group, label, start travel together through 3 functions: arrow, free_arrow, path"
      },
      {
        "pillar": "encapsulation",
        "kind": "data-clump",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 902,
        "name": "arrow",
        "detail": "color, dashed, group, label travel together through 4 functions: arrow, free_arrow, path, route_under"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 902,
        "name": "arrow",
        "detail": "`arrow` takes 10 parameters (over 6)"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 978,
        "name": "free_arrow",
        "detail": "`free_arrow` takes 8 parameters (over 6)"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 1003,
        "name": "path",
        "detail": "`path` takes 7 parameters (over 6)"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 1055,
        "name": "route_under",
        "detail": "`route_under` takes 7 parameters (over 6)"
      },
      {
        "pillar": "encapsulation",
        "kind": "long-parameter-list",
        "file": "plugin/skills/excalidraw-diagram/scripts/excalidraw_builder.py",
        "line": 1349,
        "name": "save",
        "detail": "`save` takes 9 parameters (over 6)"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "scripts/benchmark_scan.py",
        "line": 88,
        "name": "benchmark_scan.py",
        "detail": "1 line(s) wider than 100 columns, first at line 88"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "scripts/check_engine_bump.py",
        "line": 47,
        "name": "check_engine_bump.py",
        "detail": "3 line(s) wider than 100 columns, first at line 47"
      },
      {
        "pillar": "abstraction",
        "kind": "long-function",
        "file": "scripts/check_versions.py",
        "line": 92,
        "name": "main",
        "detail": "`main` is 98 lines (over 80)"
      },
      {
        "pillar": "standards",
        "kind": "line-too-long",
        "file": "scripts/check_versions.py",
        "line": 95,
        "name": "check_versions.py",
        "detail": "2 line(s) wider than 100 columns, first at line 95"
      }
    ],
    "advice": {
      "data-clump": "the same parameters travel through several functions: make them one object with those functions as methods",
      "deep-nesting": "nesting this deep hides the main path: return early, or extract the inner block",
      "file-too-long": "a file this long is several modules sharing a name: split it along the prefix families it already shows",
      "global-state": "module state mutated from inside a function has no owner: hold it in an object, or pass it in and return it",
      "god-class": "a class with this many methods is several classes sharing one name: split it along the fields its methods actually touch",
      "high-response": "a class that reaches this many distinct methods is hard to test alone, because its collaborators are part of its interface: narrow them",
      "line-too-long": "lines wider than the limit hide their tail in every diff and side-by-side view: wrap them",
      "long-function": "a function this long hides several steps: extract each step under a name that says what it does",
      "long-parameter-list": "a parameter list this long is an object waiting to be named: group the parameters that travel together",
      "low-cohesion": "methods that share no field are not one object: the groups that touch the same fields are the classes hiding in here",
      "prefix-family": "functions sharing a prefix are a namespace: a class (or module) with that name makes the boundary explicit",
      "too-many-definitions": "this many top-level definitions in one file is a package, not a module: group them"
    }
  },
  "health": {
    "score": 98,
    "total": 234,
    "healthy": 229,
    "confirmed": 230,
    "implemented": 225,
    "tested": 221,
    "drafts": 4,
    "orphans": 0,
    "untested": 1,
    "open_intent": 0,
    "drift": 0,
    "gate_errors": 0,
    "gate_link_sync_clean": true,
    "reviewed_score": 100,
    "reviewed_total": 230
  }
}