# Paths reqmap should not scan (one fnmatch glob per line, # comments ok).
# scripts/reqmap.py is intentionally NOT ignored: this repo hosts its own
# requirements there (its membership tags resolve to local requirements), so
# the engine must stay scanned. Add other vendored/generated paths below.
# The bundled single-file viewer is a generated artifact, never a member.
scripts/_map_viewer.html
# Local test caches (gitignored): CI never sees them, and they are not code.
.pytest_cache/**
*/.pytest_cache/**

# --- excalidraw-diagram skill ---
# SKILL.md and its cross-platform SKILL.universal.md variant: classified as the
# 'ignore' bucket by ARCH-PROSE-024 (user contract, not a capability
# implementation). The .universal.md is the same contract adapted for other
# platforms — same class, not its own capability.
skills/excalidraw-diagram/SKILL.md
skills/excalidraw-diagram/SKILL.universal.md
# examples/: transient generators — correctness proved by test_excalidraw.py.
# A new example should not require a new requirement.
skills/excalidraw-diagram/examples/*.py
# references/: Excalidraw JSON format reference doc — not a capability.
skills/excalidraw-diagram/references/excalidraw_format.md

# --- requirement-manager skill ---
# SKILL.md and its cross-platform SKILL.universal.md variant: same 'ignore'
# bucket classification. The engine's own skill contract is described by
# requirements in plugin/requirements/, not by tagging the SKILL files.
skills/requirement-manager/SKILL.md
skills/requirement-manager/SKILL.universal.md

# --- working docs / specs ---
# Design specs and superpowers working notes under docs/ are sync_only prose
# (ARCH-PROSE-024 bucket), not capability implementations.
docs/superpowers/specs/*.md

# --- app/ and repo-root scripts/ ---
# app/ (Vite+React viewer source) and scripts/check_versions.py live at the
# repo root — OUTSIDE plugin/'s scan root. They carry no implements: tags by
# design: CI's `artifacts` job runs `npm run smoke` and rebuilds the vendored
# viewer to prove it matches app/, but per-file tagging of app/src is still a
# known gap (TODO.md), not a claim that npm build already covers it. app/ is also
# excluded from the widened repo-root scan via the top-level .reqmapignore
# (app/**), so this comment documents intent for the narrow invocation too.
# The version-gate script is dev-CI tooling described in CLAUDE.md and
# cross-referenced in ARCH-CHECK-006.
