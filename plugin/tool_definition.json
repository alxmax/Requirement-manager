[
  {
    "type": "function",
    "function": {
      "name": "reqmap_init",
      "description": "First-use bootstrap: scaffold requirements/ and .reqmapignore if missing, draft requirements from existing code/prose, build the lock and map, and print guided next steps. Idempotent — safe to re-run; never clobbers an existing .reqmapignore. Run once per repo to get started.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "wipe": {
            "type": "boolean",
            "description": "Hard-reset: delete all non-generated requirements and strip membership tags from source files before re-extracting."
          },
          "no_site": {
            "type": "boolean",
            "description": "Skip the final site step (scaffolding docs/architecture.html)."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_new",
      "description": "Scaffold a new blank requirement from the built-in template. Use --from-todo and --id together to pre-fill from a TODO.md item instead.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "arg": {
            "type": "string",
            "description": "AREA-NAME-NNN"
          },
          "id": {
            "type": "string",
            "description": "Requirement ID in AREA-NAME-NNN format (e.g. AUTH-LOGIN-001). Required when using --from-todo."
          },
          "from_todo": {
            "type": "string",
            "description": "Scaffold the requirement from a TODO.md item matched by this name (use with --id; add --mark-done to flip the item to [x])."
          },
          "mark_done": {
            "type": "boolean",
            "description": "Also flip the matched TODO.md item to [x] (off by default). Only used with --from-todo."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_scan",
      "description": "List which code files belong to which requirement, grouped by capability. Shows all code members (implements:, generated-from:, validated-against:, tested-by:) discovered by scanning the repo.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_gate",
      "description": "Run the commit/CI gate (report-only): verify every code tag resolves to a real requirement, every confirmed requirement has at least one implements: member, and drift has not been introduced since the last sync. Exits non-zero on link-sync errors only (drift and test-link integrity are warnings). Never touches _reqlock.json. Run before every commit and in CI.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "strict": {
            "type": "boolean",
            "description": "Promote drift and test-link integrity warnings to errors. Useful in CI when all requirements are confirmed."
          },
          "json": {
            "type": "boolean",
            "description": "Emit structured JSON output instead of human-readable text."
          },
          "since": {
            "type": "string",
            "description": "Scope the gate to requirements whose member files changed since this git ref (e.g. 'main', 'HEAD~1')."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_sync",
      "description": "Rescan code members, advance the drift baseline, and regenerate the map in one step (a committed _findings.md is refreshed too). Run after editing requirement files or tagging new code members. Use --accept-drift when a confirmed or implemented contract changed.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "accept_drift": {
            "type": "boolean",
            "description": "Explicitly advance the baseline when a confirmed or implemented contract changed. Required when those contracts differ from the lock; sync exits non-zero without it."
          },
          "strict": {
            "type": "boolean",
            "description": "Promote drift and test-link integrity from warn to error."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_check",
      "description": "Deprecated alias for 'gate' (report-only) / 'sync' (with --update-lock). Preserved for backward compatibility with consumer hooks, CI, and the GitHub Action. Will be removed in the next major version — use 'gate' or 'sync' instead.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "strict": {
            "type": "boolean",
            "description": "Promote drift and test-link integrity warnings to errors."
          },
          "json": {
            "type": "boolean",
            "description": "Emit structured JSON output (for CI/badge consumption)."
          },
          "since": {
            "type": "string",
            "description": "Scope the gate to requirements whose member files changed since this git ref."
          },
          "update_lock": {
            "type": "boolean",
            "description": "Also regenerate the lock and map (mirrors legacy 'sync' behavior)."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_map",
      "description": "Generate requirements/_map.md (4 Mermaid diagrams), requirements/_map.json (graph with nodes, edges, todos), and requirements/_map.html (a self-contained React viewer). The viewer is only emitted when scripts/_map_viewer.html is vendored beside the engine.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "check_fresh": {
            "type": "boolean",
            "description": "Freshness gate: rebuild the map in memory and exit non-zero if the committed _map.* is stale. Use in CI alongside gate."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_export",
      "description": "Write requirements/_map.json (the graph with engine_version, nodes, edges) for feeding an external front-end. Same output as map, without rebuilding _map.md and _map.html.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "out": {
            "type": "string",
            "description": "Output path override. Use '-' for stdout (--out -); omit for requirements/_map.json."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_next",
      "description": "Show what to do next: a prioritized, actionable list of risk buckets (Orphans, Needs tests, Needs intent review, Drafts to review). Read-only, always exits 0. The best follow-up command to run after any action.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "show_all": {
            "type": "boolean",
            "description": "Expand all buckets to show every item instead of just the top few."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_lint",
      "description": "Readability and structure check on non-draft requirements: stacked conditions (3+ and/or joins in one normative line), contract clauses with an unnamed 'It' subject, over-long contract clauses, missing Contract or Acceptance sections. Read-only unless --decompose is passed; exit-neutral by default.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "strict": {
            "type": "boolean",
            "description": "Exit non-zero on error-severity findings (warnings remain advisory)."
          },
          "decompose": {
            "type": "boolean",
            "description": "Scaffold one draft requirement per statement-size finding. Opt-in and never used by the gate, the hook or CI — the only lint mode that writes."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_show",
      "description": "Print a consolidated dossier for one requirement: header, intent, Contract bullets, dependencies in both directions, code members grouped by role with file:line, open Verify intent questions, and risk signals. Answers 'what does this do / where is X' in one command. Read-only.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "arg": {
            "type": "string",
            "description": "ID"
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_dupes",
      "description": "Flag requirement pairs whose contracts overlap (TF-IDF cosine similarity), so a divergent re-implementation is caught before it lands. Read-only, advisory — a human decides if a flagged pair is a real duplicate.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "threshold": {
            "type": "number",
            "description": "Cosine similarity cutoff in (0,1] for reporting a pair (default 0.35). Lower = more pairs flagged."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_search",
      "description": "Rank requirements by lexical relevance to a free-text query (same TF-IDF cosine as dupes, reused). Read-only. Prints each hit's score, and says so explicitly when nothing clears the relevance floor rather than showing a spurious top result. Lexical, not synonym-aware.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "arg": {
            "type": "string",
            "description": "QUERY"
          },
          "top": {
            "type": "integer",
            "description": "Maximum number of ranked matches to show (default 5)."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_health",
      "description": "Print a corpus coherence snapshot: a headline score (percentage of requirements fully green on every axis: confirmed + member + tested + no open questions + not drifted) plus component counts. Use for a CI badge with --json.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "json": {
            "type": "boolean",
            "description": "Emit parseable JSON for a CI badge (--json)."
          },
          "badge": {
            "type": "boolean",
            "description": "Emit Shields.io endpoint JSON (schemaVersion, label, message, color)."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_draft",
      "description": "Draft one requirement per untagged file (code and prose). Input is existing untagged source code and Markdown. Emits draft requirements — never confirmed. After drafting, run gate and report the result. Remind the user to review and confirm the real ones.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_plan",
      "description": "Read-only JSON capability-extraction plan: emit a capability map from legacy code without writing any .md files. Safer than draft — a human authors and confirms each candidate. Use before draft to preview what would be extracted.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "out": {
            "type": "string",
            "description": "Write plan JSON here ('-' or omit = stdout)."
          },
          "md_glob": {
            "type": "string",
            "description": "Also discover .md files matching this glob (repeatable; comma-separated ok). Off unless given. e.g. --md-glob 'prompts/**' --md-glob 'modes/**'."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_findings",
      "description": "Aggregate open 'Verify intent' items across all requirements into requirements/_findings.md. Surfaces every open human-review question in one place.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "raw": {
            "type": "boolean",
            "description": "Ignore the triage sidecar and emit the raw grouped list."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_confirm",
      "description": "Mark a reviewed requirement as confirmed — the human sign-off step. Flips status to confirmed in the frontmatter. The engine refuses if the requirement has no implements: member (a confirmed requirement must point to code). Run sync after confirming.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "arg": {
            "type": "string",
            "description": "ID"
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_review",
      "description": "Emit a JSON review plan (intent, contract, acceptance criteria, structural anchors) for all requirements or one. Used as an AI feed for semantic quality review. Read-only.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "arg": {
            "type": "string",
            "description": "ID"
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_translate",
      "description": "Manual, opt-in: detect the corpus's majority language (per-file `lang:` frontmatter override honored first), then cache a `claude -p` translation of every requirement written in that language into requirements/_i18n/<target>.json. A structural-fidelity check (backticked spans, numbers, heading/bullet markers) gates every cache write; a missing `claude` CLI, a timeout, or a failed check skips that entry with a warning instead of aborting. `map`/`export` inline the cache into the graph read-only, with no `claude` call of their own — this command is the ONLY way a `claude` subprocess runs; it is never invoked by gate/sync/lint/map or the pre-commit hook.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "translate_to": {
            "type": "string",
            "description": "Target locale, 'ro' or 'en' (default: the other of the two from the detected corpus majority)."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_site",
      "description": "Inject or refresh engine-owned regions (nav links + stats counts) into a project presentation page. Scaffolds a full page if the target does not exist. Run after map to keep the page current.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "attach": {
            "type": "string",
            "description": "Target HTML page to inject engine-owned regions into (scaffolds it if absent)."
          },
          "regions": {
            "type": "string",
            "description": "Comma-separated list of regions to inject (nav,stats). Default: nav."
          },
          "diagram": {
            "type": "string",
            "description": "Relative path (from the page) to an Excalidraw HTML viewer; linked only if it exists."
          },
          "detect": {
            "type": "boolean",
            "description": "Scan docs/ and print the suggested command — writes nothing."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_coverage",
      "description": "Read-only report of untagged-code coverage signal: lists source files that carry no implements: tag, grouped by directory. Use to identify gaps in requirement traceability.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "json": {
            "type": "boolean",
            "description": "Emit structured JSON output (for CI consumption)."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "reqmap_suggest_verifies",
      "description": "Propose `# verifies: <id>#CASE-N` tags for tests already named after the criterion they check (e.g. `test_ac3_...`), so per-criterion coverage can be adopted on an existing corpus. Read-only; --apply writes the tags.",
      "parameters": {
        "type": "object",
        "properties": {
          "root": {
            "type": "string",
            "description": "Repo root where requirements/ lives; defaults to the current directory."
          },
          "apply": {
            "type": "boolean",
            "description": "Write the proposed tags into the test files (ambiguous matches are never written)."
          }
        },
        "required": []
      }
    }
  }
]
