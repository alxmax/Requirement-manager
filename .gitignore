__pycache__/
*.pyc
.consilium/

# Generated, regenerable views — rebuild with: cd plugin && python scripts/reqmap.py map
plugin/requirements/_map.html

# Opt-in scan cache (machine-local, keyed by file mtime — never committed; REQ-SCANCACHE-023)
plugin/requirements/_scancache.json

# Dev-only shortcuts (Windows, localhost — not for seeded repos)
*.cmd
*.url

# Superpowers plans and session artifacts
docs/superpowers/

# IEEE 29148 comparison presentations + their generator scripts (local artifacts)
docs/ieee29148_comparison.*
docs/before_after_ieee29148.*

# Superpowers session artifacts (top-level)
.superpowers/
